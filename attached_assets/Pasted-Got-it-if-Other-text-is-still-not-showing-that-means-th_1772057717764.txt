Got it — if **“Other” text is still not showing**, that means the issue is **not just display formatting**. One of these is happening (usually #1 or #2):

1. **The multi-select array is NOT saving `"other"`** into the selected values
2. **The “other text” input is NOT saving** (blank in DB)
3. Display helper is only swapping when `"other"` exists in the array (no fallback)
4. Edit/Draft flow saves differently than Submit (so it “works sometimes”)

Below are **Replit instructions with safeguards** that fix it *even if the array doesn’t include “other”*.

---

# REPLIT INSTRUCTIONS (WITH SAFEGUARDS)

## Hire a Tenant’s Agent → Actual Listing Display Fix

### Problem Fields

* **Non-Negotiable Amenities & Property Features**: selected “Other” + typed text, but listing display doesn’t show typed text.
* **Offered Lease Term**: selected “Other” + typed text, but listing display doesn’t show typed text.

---

## SAFEGUARDS (DO NOT BREAK ANYTHING)

* Do **NOT** rename or delete DB columns.
* Do **NOT** change existing option lists or keys that are already saved historically.
* Fix must work for **Create + Draft + Edit + Submitted listings**.
* Fix must be **backwards compatible** with previously saved listings.

---

# 1) Confirm the DB storage is correct (Save logic)

### A) Non-Negotiable Amenities & Property Features

**Ensure BOTH are saved:**

* `non_negotiable_features_amenities` (array/json) must include `"other"` when Other is selected
* `non_negotiable_features_amenities_other` (string) must store the typed text

**Save Rule (Create/Edit/Draft):**

* If user enters `*_other` text (non-empty), force `"other"` into the array **even if checkbox selection didn’t include it**.
* If user unselects Other and clears text, remove `"other"` from array and clear `*_other`.

> This prevents the exact bug where the user typed “Other” text but `"other"` never got saved in the array.

### B) Offered Lease Term

**Ensure BOTH are saved:**

* `offered_lease_term` (single value or array, depending on your setup)
* `offered_lease_term_other` (string)

**Save Rule:**

* If `offered_lease_term` == `"other"` OR `offered_lease_term_other` is non-empty → treat it as Other selected (store `"other"` and store the text).
* If user changes away from Other → clear `offered_lease_term_other`.

---

# 2) Fix the Display Layer (must show text even if “other” wasn’t saved)

Even after you fix saving, you still need a display fallback for old records.

### Display Rule (Non-Negotiable)

When rendering the list:

* If selections include `"other"` AND otherText exists → show otherText instead of “Other”
* If selections **do NOT include** `"other"` BUT otherText exists → **append otherText** to the displayed list (fallback)

### Display Rule (Offered Lease Term)

* If term == `"other"` → show `offered_lease_term_other`
* If term != `"other"` but otherText exists (legacy bad save) → show `term + " (" + otherText + ")"` OR just show otherText (your preference)

This ensures the listing display works for:

* New correct saves ✅
* Old incorrect saves ✅

---

# 3) Required: Fix must apply in ALL flows

Make sure the exact same logic is applied in:

* Create listing
* Save Draft
* Edit Draft
* Edit Submitted listing
* Submit listing

**Most common cause** of “still not showing” is:
✅ Submit saves correctly
❌ Draft/Edit saves missing the `"other"` value or missing the otherText

So apply the save normalization in **one shared method** used by all three actions (saveDraft, update, submit), not duplicated code.

---

# 4) Quick test checklist (must pass)

Login: `tenant@exp.com`

### Non-Negotiable Test

1. Select “Other” + type: `No carpet, must have balcony`
2. Save Draft
3. Open Draft → confirm Other checkbox still selected and text still present
4. Submit
5. View actual listing → must display the typed text (not just “Other”)

### Offered Lease Term Test

1. Select “Other” + type: `7 months`
2. Save Draft → Open Draft → verify retained
3. Submit → View actual listing → must show `7 months`

### Legacy Record Test

Find a listing where otherText exists but `"other"` is missing in array → listing display must still show the otherText.

---

# What to tell Replit AI (short directive)

**Implement “Other persistence + display fallback” for Non-Negotiable Amenities and Offered Lease Term.**
Force `"other"` into the saved selection array whenever otherText is non-empty, across Create/Edit/Draft/Submit.
In listing display, if otherText exists but `"other"` is missing, append otherText anyway.

---

If you paste me the **actual field keys** you’re using in Tenant for:

* Non-Negotiable selections field name
* Non-Negotiable other text field name
* Offered Lease Term field name
* Offered Lease Term other text field name

…I’ll rewrite this into **exact file/component-level instructions** (TenantAgentAuction.php / Edit component / view blade) with the exact conditions and mapping, so your dev doesn’t have to guess.
