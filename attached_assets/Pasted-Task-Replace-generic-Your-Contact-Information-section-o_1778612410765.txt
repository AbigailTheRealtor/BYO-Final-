Task: Replace generic “Your Contact Information” section on Accept & Submit Hire Request with role-specific Client Details from Counter Terms flow

Issue:
In Offer Listing Presets / Direct Hire flow, when a user clicks “Accept & Submit Hire Request,” the final section currently shows the same generic contact block for all roles:

Your Contact Information
Provide your contact details so the agent can reach you. All fields are optional.
Full Name
Phone Number
Email Address
Property Address
Street Address
City
State
ZIP Code

This is incorrect.

Expected behavior:
When a user accepts an agent preset / hire request directly, the final client information section must match the same role-specific “Client Details” section already used when the user chooses:

Request Changes / Counter Terms

Each role has different Client Details questions. The Accept flow must reuse/copy the correct Client Details fields based on user type:

- Seller → use Seller Client Details from Counter Agent Bid / Request Changes flow
- Buyer → use Buyer Client Details from Counter Agent Bid / Request Changes flow
- Landlord → use Landlord Client Details from Counter Agent Bid / Request Changes flow
- Tenant → use Tenant Client Details from Counter Agent Bid / Request Changes flow

Do NOT show the generic “Your Contact Information” section anymore.

Safeguards:
- Audit first before editing.
- Do NOT change database schema unless the existing Counter Terms Client Details fields already require a missing key and you prove it.
- Do NOT change routes unless absolutely required.
- Do NOT touch Create Offer Listing forms.
- Do NOT touch Hire Agent listing creation forms.
- Do NOT touch Limited Service / legacy flows.
- Do NOT rename existing saved meta keys unless required and documented.
- Preserve existing Accept & Submit Hire Request behavior, signatures, acknowledgments, accepted terms, and status transitions.
- Only replace the generic contact/details section with the correct role-specific Client Details section.
- Reuse existing Blade partials/components if available instead of duplicating markup.
- If no partial exists, extract the existing Counter Terms Client Details sections into reusable role-specific partials and include them in both Counter Terms and Accept flows.

Audit instructions:
1. Locate the Accept & Submit Hire Request flow/view/controller/component where the generic “Your Contact Information” block is rendered.
2. Locate the Request Changes / Counter Terms flow for Seller, Buyer, Landlord, and Tenant.
3. Identify the exact Blade/Livewire fields used in each role’s “Client Details” section.
4. Confirm whether those fields already save/load correctly in the Counter Terms flow.
5. Compare the Accept flow data model to the Counter Terms data model and determine whether the Accept flow can reuse the same properties/meta keys.
6. Document which files will be touched before editing.

Implementation instructions:
1. Remove or bypass the generic “Your Contact Information” block from the Accept & Submit Hire Request flow.
2. Add role-based rendering logic:
   - if role/user_type is seller, render Seller Client Details
   - if buyer, render Buyer Client Details
   - if landlord, render Landlord Client Details
   - if tenant, render Tenant Client Details
3. The rendered Client Details must match the Counter Terms version for that same role exactly:
   - same labels
   - same placeholders
   - same required/optional behavior
   - same conditional fields
   - same Select2 placeholders
   - same icon/input formatting
   - same validation expectations
4. Ensure Accept & Submit saves those Client Details fields correctly with the accepted hire request.
5. Ensure the accepted hire request summary / final record displays the submitted role-specific Client Details correctly.
6. Ensure Request Changes / Counter Terms still works exactly as before.

Done looks like:
- Seller accepting directly sees Seller Client Details, not generic contact fields.
- Buyer accepting directly sees Buyer Client Details, not Seller/Landlord/Tenant fields.
- Landlord accepting directly sees Landlord Client Details.
- Tenant accepting directly sees Tenant Client Details.
- Request Changes / Counter Terms still shows the same Client Details it showed before.
- Accept & Submit Hire Request still completes successfully.
- Accepted Hire Summary still shows the correct client details.
- No Create Offer Listing, Hire Agent creation, Limited Service, routes, or database schema were changed unless explicitly justified.

Final response required:
After finishing, report:
1. Files changed
2. The source Counter Terms Client Details section used for each role
3. Confirmation that Accept flow now uses role-specific Client Details
4. Confirmation that Counter Terms flow was not broken
5. Any fields that could not be reused and why