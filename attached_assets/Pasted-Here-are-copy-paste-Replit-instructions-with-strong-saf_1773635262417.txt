Here are **copy-paste Replit instructions with strong safeguards** for this exact **Hire a Tenant Agent** edit only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT AGENT ONLY — PAYMENT TIMING FOR BROKER FEES LISTING DISPLAY FORMAT FIX

## SCOPE

Make this fix **only for Hire a Tenant Agent**:

### Current issue

On the Tenant listing display, **Payment Timing for Broker Fees** is showing in a split / awkward format like:

* `Payment Timing for Broker Fees: Paid Within Calendar Days of Tenant Rent Payment`
* `Calendar Days to Pay: 3`

### Required behavior

Make the Tenant listing display match the cleaner **Landlord-style format** so it reads as **one combined sentence/value** instead of a split line.

Example target format style:

`Payment Timing for Broker Fees: Paid Within 3 Calendar Days of Tenant Rent Payment`

This should be a **display formatting fix only** unless the current data structure absolutely requires a tiny Buyer/Tenant-only rendering adjustment.

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Tenant Agent flow.**
2. **Do not modify Landlord, Buyer, or Seller flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change any selection input format.**
10. **Do not change Select2 initialization or behavior.**
11. **Do not change field labels, tooltips, placeholders, icons, or field order.**
12. **Do not change the saved values or database structure unless absolutely necessary.**
13. **Do not refactor global listing display logic.**
14. **Do not affect how Landlord currently displays this field since Landlord is already correct.**
15. **Keep this surgical and display-specific.**

---

## IMPORTANT

We do **not** want a broad compensation-section rewrite.

This should be a **Tenant-only listing display formatting fix** so the output reads naturally and matches the cleaner Landlord presentation style.

### Hard stop:

Before editing anything, first confirm the **actual active Tenant files** for:

* Tenant listing display
* Tenant Broker Compensation display section
* the display row for **Payment Timing for Broker Fees**
* any helper/formatter currently combining or splitting the Payment Timing text

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap the change so it affects **Tenant only**.

---

## STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Tenant Agent**:

1. Livewire/component or display file handling Tenant listing output
2. Blade/partial rendering the **Broker Compensation & Agency Agreement Terms** section for Tenant
3. The specific line/partial/helper responsible for showing:

   * `Payment Timing for Broker Fees`
   * `Calendar Days to Pay`

### Requirement

Return the exact files first, then edit **only those files**.

### Hard stop

**No other file should be edited or changed.**

---

## STEP 2 — FIX TENANT PAYMENT TIMING DISPLAY TO SHOW AS ONE CLEAN COMBINED VALUE

## Current bug

Tenant listing currently shows the payment timing in two pieces, for example:

* `Payment Timing for Broker Fees: Paid Within Calendar Days of Tenant Rent Payment`
* `Calendar Days to Pay: 3`

## Required behavior

Display it as one clean combined line/value, for example:

* `Payment Timing for Broker Fees: Paid Within 3 Calendar Days of Tenant Rent Payment`

This should mirror the **clean style** used by Landlord.

### Important

Keep the existing Tenant data logic intact if possible.
Prefer to fix the **display rendering only** by combining:

* the selected payment timing phrase/template
* the number of calendar days

Do **not** change how the value is saved unless absolutely necessary.

---

## STEP 3 — PRESERVE OTHER PAYMENT TIMING OPTIONS

Make sure this formatting fix works safely for the other possible Tenant payment timing choices.

### Required behavior

If the selected timing option does **not** need a day count, do **not** force one into the text.

Only combine the day count when the selected option actually uses it.

Examples:

* if timing option includes a day-count placeholder/meaning, combine with number
* if timing option is a fixed phrase with no count, show just that fixed phrase

### Important

Do not create duplicated text or awkward formatting.

---

## STEP 4 — HIDE “CALENDAR DAYS TO PAY” AS A SEPARATE ROW WHEN IT IS ALREADY COMBINED

If the day count is successfully combined into the main Payment Timing line, then do **not** show a second separate row like:

`Calendar Days to Pay: 3`

That separate row should be suppressed in the Tenant listing display for this case.

### Important

This is a **display rule only**.
Do not remove the stored value.

---

## STEP 5 — DO NOT AFFECT LANDLORD DISPLAY

Landlord already displays this correctly.

Do **not**:

* change Landlord files
* change Landlord formatting
* change any shared compensation formatting globally unless wrapped so it affects **Tenant only**

---

## STEP 6 — PRESERVE BACKWARD COMPATIBILITY

Older Tenant listings that already have:

* payment timing text saved
* calendar days saved separately

must still:

* render correctly after the fix
* not require migrations
* not lose data
* not break existing saved listings

---

## STEP 7 — TEST THESE EXACT CASES

### A. Tenant payment timing option that uses days

* expected: one combined line like
  `Payment Timing for Broker Fees: Paid Within 3 Calendar Days of Tenant Rent Payment`
* expected: no separate `Calendar Days to Pay` row

### B. Tenant payment timing option that does not use days

* expected: display only the payment timing phrase
* expected: no broken formatting

### C. Existing old Tenant listing

* expected: still renders correctly after the fix

### D. Landlord listing

* expected: unchanged

---

## STEP 8 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Landlord files changed
* no Buyer files changed
* no Seller files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Tenant fields changed

---

## STEP 9 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Tenant files were identified first
3. exact root cause for the split Payment Timing display
4. exact fix applied
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof Tenant now displays Payment Timing in one clean combined line like Landlord

---

## IMPORTANT FINAL RULE

This is a **Tenant-only, surgical display fix**.

Do **not**:

* edit unrelated files
* change selection input format
* refactor global compensation logic
* touch Landlord, Buyer, or Seller
* make broad UI changes

The final result should be:

* Tenant Payment Timing for Broker Fees displays in one clean combined line
* the separate `Calendar Days to Pay` row does not show when already incorporated
* Landlord remains unchanged
* no other file or selection behavior is changed

---

If you want, I can also make this into a **shorter hard-stop version** for Replit.
