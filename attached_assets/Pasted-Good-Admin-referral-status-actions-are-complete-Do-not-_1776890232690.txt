Good. Admin referral status actions are complete.

Do not rebuild or modify any existing referral logic.

ALREADY IMPLEMENTED — DO NOT TOUCH:
- referral DB schema
- referral link generation
- referral capture route
- referral visit logging
- session attribution
- cookie fallback attribution
- signup persistence
- listing persistence
- accepted hire persistence
- agent dashboard referral card (link + counters)
- admin referral tracking page
- admin referral status actions

NEXT TASK:
Add a “Recent Referral Activity” table to the AGENT dashboard only.

GOAL:
Give agents visibility into their actual referred activity (especially hires) using existing data. This is read-only and additive.

CRITICAL SAFEGUARDS:
- Surgical changes only.
- Do not redesign the dashboard.
- Do not modify any listing, bidding, or referral logic.
- Do not touch Livewire components or core flows.
- Do not change existing referral card or stats.
- Do not add payout or earnings logic yet.
- Do not introduce new writes — read-only display only.
- Stop and report if there is any risk of duplicating existing logic.

IMPLEMENT ONLY THIS:

1. Data source
Use accepted_bid_summaries as the primary dataset.

Filter:
WHERE referring_agent_id = current authenticated agent id

Order:
latest first (created_at DESC)

Limit:
last 10 rows

Fields to retrieve:
- id (summary id)
- referring_agent_id
- referral_source_code
- referral_status
- created_at
- listing id (via relationship if available)
- hired agent id (if available)

2. Controller update
In DashboardController:
- inside the agent-only logic block (same place referral link is loaded)
- query the recent referral activity
- pass to blade as $recentReferrals

3. Blade update (dashboard)
Add a new section BELOW the existing “Referral Partner Link” card.

Section title:
Recent Referral Activity

Display:
Simple table with columns:
- Date
- Listing ID
- Status
- Hired Agent

Rules:
- If no rows → show “No referral activity yet”
- Keep styling consistent with existing dashboard tables
- No buttons, no actions, no edits

4. Status display
Reuse same badge colors/labels used in admin page if possible:
- pending
- qualified
- closed
- paid
- void

5. Performance
- Limit to 10 rows
- No heavy joins if avoidable
- Use existing relationships if already defined, otherwise minimal joins

TEST REQUIREMENTS:

T1. Agent with referred hires
- confirm rows appear
- confirm correct status + dates

T2. Agent with no referrals
- confirm “No referral activity yet” message

T3. Multiple agents
- confirm each agent sees ONLY their own data

T4. No side effects
- confirm no DB writes occur

T5. Order
- confirm newest entries appear first

AFTER IMPLEMENTATION, REPORT:
1. exact controller changes
2. exact blade insertion point
3. query used
4. how listing id and hired agent were resolved
5. test results

DELIVERABLE:
Only agent dashboard “Recent Referral Activity” table. No admin changes, no earnings, no payout logic.