Task: Fix final bid detail parity issues after shared layout refactor

The shared bid-detail-layout refactor was the right direction, but there are still three remaining issues that must be fixed for true parity.

This task applies to:

Buyer
Seller
Landlord
Tenant

Do not do another broad refactor.
Fix the specific remaining issues below.

Issue 1 — Accept and Reject buttons are washed out / look disabled
Problem

On at least one bid detail page, the Accept Bid and Reject Bid buttons are visually washed out, while Counter Bid looks normal.

This makes the UI look broken even if the buttons still function.

What to check

Inspect the rendered classes and computed styles for:

Accept button
Counter button
Reject button

Check for:

unexpected opacity
inherited opacity from parent container
disabled attribute accidentally present
pointer-events: none
incorrect outline/ghost class
text color override making label unreadable
conditional class mismatch between buttons
Goal

All three action buttons must have clear, normal contrast and match the intended platform color system:

Accept = green, readable, normal opacity
Counter = yellow or blue depending on approved system, readable, normal opacity
Reject = red, readable, normal opacity
Safeguards
Do not change button logic or permissions
Do not make buttons look active if they are truly disabled by business logic
If they are meant to be enabled in this state, ensure the styling reflects enabled state
Fix the root cause, not by hardcoding inline styles unless absolutely necessary
Issue 2 — Tenant match score layout must match Buyer/Seller/Landlord
Problem

After the shared outer layout refactor, Tenant still does not use the same match score visual layout as the other three roles.

Goal

Tenant must use the same approved match score component/layout as Buyer, Seller, and Landlord.

That means same:

outer match score container
section header styling
main match badge placement
explanatory text placement
two inner comparison boxes
spacing and padding
border radius and card treatment
label formatting
Preferred implementation

Extract the match score area into one shared partial/component if not already done, and make all four roles render through it.

For example:

shared partial/component for match score wrapper
role-specific data passed in
no separate Tenant-only markup structure
Important

Do not just “make Tenant look closer.”
Use the same component/markup structure where possible.

Safeguards
Do not change match score calculations
Do not change mismatch logic
Do not change which counts appear
Only unify the layout/presentation layer
Issue 3 — Listing ID is wrong for Buyer, Seller, and Landlord
Problem

Buyer, Seller, and Landlord are showing the wrong Listing ID in the shared header.

Tenant is correct.

The header must display the actual listing’s public Listing ID, the same value shown on the real listing, not the bid id, auction id, or internal record id.

What to check

Inspect what value is passed into:

listingId prop for x-bid-detail-layout

For Buyer, Seller, and Landlord, verify whether it is currently using:

auction id
bid id
listing primary key
instead of the actual stored listing identifier field.
Goal

Pass the true listing identifier used by the listing page itself.

The displayed Listing ID in the bid header must exactly match the Listing ID shown on the corresponding listing page.

Implementation guidance

Find the correct field from the underlying listing model, for example whichever field Tenant already uses successfully:

public listing id
custom listing code
reference id
generated listing identifier

Then use that same concept consistently for Buyer, Seller, and Landlord.

Safeguards
Do not change database values
Do not generate a new listing ID on the fly
Do not fall back to model primary key unless that is truly the official displayed listing ID
Match the existing listing page source of truth
Required validation before marking complete
1. Buttons

Provide confirmation that:

Accept is no longer washed out
Reject is no longer washed out
buttons visually match enabled/disabled state correctly
2. Tenant match score

Provide confirmation that:

Tenant now uses the same match score layout/component as the other three roles
3. Listing IDs

Provide confirmation that:

Buyer header Listing ID matches Buyer listing page
Seller header Listing ID matches Seller listing page
Landlord header Listing ID matches Landlord listing page
Screenshot proof required

Do not mark done from code inspection only.

Provide updated screenshots showing:

action buttons with correct contrast
Tenant match score section
Buyer header Listing ID
Seller header Listing ID
Landlord header Listing ID
Files to inspect

At minimum, inspect:

resources/views/components/bid-detail-layout.blade.php
Tenant bid detail page
Buyer bid detail page
Seller bid detail page
Landlord bid detail page
any shared button styling partial/CSS
any shared match score partial/component
controller/view-model data passed into listingId
Final rule

This is a parity completion task, not another architecture rewrite.

Fix only the remaining gaps:

button contrast
Tenant match score layout
wrong listing ID values

Do not introduce unrelated layout changes.

My recommendation on the buttons

My guess is the Accept/Reject buttons are getting either:

btn-outline-* plus low-opacity text
or
a disabled/opacity class from a shared footer/action wrapper

So I would tell Replit to inspect the actual rendered button classes first before changing anything.