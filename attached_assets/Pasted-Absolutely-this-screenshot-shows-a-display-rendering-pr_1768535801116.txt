Absolutely — this screenshot shows a **display/rendering problem**, not a user-entry problem: the **Income property Buyer listing is saving data**, but the **public/listing-details view is not rendering the saved values** (it’s showing section headers like “Purchasing Terms” and “Broker Compensation & Agency Agreement Terms” with blank lines). That means the “details/preview” template for **Income** is either:

* pointing at the wrong fields/keys, **or**
* not including the Income-specific partials, **or**
* using conditionals that incorrectly evaluate to false for Income (so it hides the values), **or**
* failing to load the nested JSON objects used by those sections.

Here are **Replit instructions with safeguards** to fix it.

---

## Replit Instructions — Hire a Buyer’s Agent

### Income Property Listing Details Page Missing Filled-In Fields (Display Layer Fix)

## ✅ Goal

For **Hire a Buyer’s Agent → Income Property** listings:

* The **listing details view (public-facing / preview / “auction page”)** must display **all saved inputs** the same way it does for other property types.
* If the user filled in every field, the listing details page must show those values under:

  * **Purchasing Terms**
  * **Broker Compensation & Agency Agreement Terms**
  * **Buyer’s Info**
  * and any other completed sections

---

# ⚠️ CRITICAL SAFEGUARDS

* ✅ **Do NOT mass-update database rows**
* ✅ **Do NOT change user-entered data**
* ✅ **Do NOT change create/edit forms**
* ✅ Fix is **display-layer + data-loading only**
* ✅ Must work for:

  * New listings
  * Saved drafts
  * Existing published listings
* ✅ Apply only to **Hire a Buyer’s Agent** listing details rendering (auction page / preview page)
* ✅ Must not break Residential/Commercial/Business/Vacant Land display views

---

# 1) Confirm the Exact Page That’s Broken (Where to Fix)

From your screenshot, the broken view is the **listing/auction public view** (left side showing “Listing Details”, “Property Preferences”, then blank “Purchasing Terms”, etc.).

### ⚠️ WHERE THIS MUST UPDATE

* **Buyer Auction Listing Details page**
* The template/Livewire component that renders the **public listing view** for Buyer auctions (not the create/edit form)

This same fix must apply anywhere that view is reused:

* Share link view
* Logged-in user viewing their listing
* Agent viewing listing details

---

# 2) Likely Root Cause (What to look for)

## A) Income display template is missing the section partials

Example symptoms:

* Section headings render (Purchasing Terms), but rows beneath are empty.
* That usually means the view includes a partial for Residential but not Income, or an `if` statement excludes Income.

## B) The view is using wrong field names/keys for Income

Example:

* Form saves `buyer_financing_details` JSON
* Display view reads `financing_terms` or a different key

## C) The listing details query does not load the nested JSON used by those sections

If your model casts JSON, this should load automatically—but if you’re selecting only specific columns, you might be omitting them.

---

# 3) Required Fix Strategy (Non-destructive)

## ✅ The listing details view must render from the SAME “field map” used by the review/preview that works

You said: “I filled in every field” and it’s not showing.
That means the data exists; the display layer is not showing it.

### Implementation rule:

**Do not create a separate Income-only view.**
Instead, create or use a **single unified rendering partial** that:

* prints any field that is populated
* works for all property types, including Income
* uses “safe display” logic (show field if not empty)

---

# 4) Step-by-Step Replit Instructions

## Step 1 — Identify the Buyer listing details renderer

In Replit, search for:

* the route/controller for the public Buyer listing
* the Livewire component that renders Buyer auction details
* the Blade view that contains these headings:

  * `Listing Details`
  * `Property Preferences`
  * `Purchasing Terms`
  * `Broker Compensation & Agency Agreement Terms`
  * `Buyer’s Info`

**Search term to find it quickly:**

* `"Broker Compensation & Agency Agreement Terms"`
* `"Purchasing Terms"`
* `"No one has bid on this auction."`

Once found, that’s the view to fix.

---

## Step 2 — Ensure the query loads ALL needed fields

In the component/controller where the listing is loaded, confirm it retrieves:

* all standard Buyer fields
* all JSON columns for financing/currency details
* all broker compensation term fields
* all buyer info fields

### Safeguard

If you see something like `select(['id','title','...'])`, it may be excluding JSON columns.
✅ Fix: either remove the narrow `select()` or add the missing columns.

---

## Step 3 — Fix conditional logic that incorrectly excludes Income

Look for conditions like:

* `if ($property_type == 'Residential')`
* `if (!in_array($property_type, [...]))`
* `if ($listing->property_type != 'Income')`
* `switch(property_type)` missing a case for Income

### Required change

* Ensure **Income** is included wherever Purchasing Terms and Broker Compensation sections are rendered.

✅ Example:
If you have:

* Residential + Commercial included
  Add **Income** explicitly.

---

## Step 4 — Ensure the template prints values using “show if filled” logic

Your screenshot shows blank lines like it’s expecting values but not printing them.

### Required rule

For each row shown on the details page:

* If the value exists → print it
* If empty → hide the row entirely (or show “Not specified” only if you want that behavior platform-wide)

**Safeguard:** Don’t show empty rows (it makes it look broken).

---

## Step 5 — Fix field/key mismatches (most common)

Compare:

* the **create/edit form property names**
  vs
* the **details view property names**

### How to confirm quickly

On the listing details page component, temporarily dump/log the listing object (server logs only) and confirm the keys exist:

* purchasing terms fields
* broker compensation fields
* financing detail JSON

**Safeguard:** Remove debug logs after confirmation.

---

## Step 6 — Income uses Residential-like “Purchasing Terms”

Income properties (2–4 units etc.) often share the same purchase terms structure as Residential.

✅ Rule:

* If Income is “4 units or fewer,” render the **Residential purchase term rows**
* If you have Income 5+ as a different category, render the commercial-style rows

But based on your UI, you likely treat Income as its own property type. In that case:

* **Use the same row set as Residential unless you intentionally built a unique Income set.**

---

# 5) ⚠️ CRITICAL — WHERE THIS MUST UPDATE (Consistency)

The corrected rendering must appear consistently in:

* Public share link / auction page
* User’s “View Listing” page
* Agent-facing read-only listing details page
* Any “Review / Summary” page that shows listing details

**Safeguard:** Don’t patch only one view. Fix the shared partial so all views update automatically.

---

# 6) QA Checklist (Must Pass)

Using the listing you showed (Income, fully filled out):

✅ On listing details page:

* Purchasing Terms shows all relevant filled values
* Broker Compensation & Agency Agreement Terms shows filled values (commission, lease fee, lease-option, etc.)
* Buyer’s Info shows filled values
* No empty section rows appear
* Nothing is duplicated

✅ Regression test:

* Residential Buyer listing still renders correctly
* Commercial Buyer listing still renders correctly
* Draft edit still works

---

# 7) What I need from you (no back-and-forth—just paste one thing)

To make these instructions **file-specific and exact**, paste **either**:

* the URL route name/path for that listing details page (from Replit routes/web.php), **or**
* the Blade filename that contains “Broker Compensation & Agency Agreement Terms”

Then I’ll rewrite this into precise:

* “Open file X”
* “Find block Y”
* “Add Income to condition Z”
* “Render partial A”
* “Map keys B → C”

…with the same safeguards you require.
