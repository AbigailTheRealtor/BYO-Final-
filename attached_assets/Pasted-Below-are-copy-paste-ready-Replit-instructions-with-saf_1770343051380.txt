Below are **copy-paste-ready Replit instructions (with safeguards)** to fix this on **Hire a Landlord’s Agent** listings:

✅ **Goal:** If the user selects **“Other”** for **View Preference**, the UI should show **ONLY a blank text box** (for custom input) — **NOT** “Other View …” text above it — and this should work for **Create + Edit + Show pages (and PDF if you have one).**

---

## Replit Instructions (Safeguards First)

### SAFEGUARDS (DO NOT BREAK EXISTING FLOWS)

1. **Do NOT rename or delete any existing DB columns.**
2. **Do NOT change option text** for existing dropdown values except where noted.
3. Implement the fix using **display logic only** (Blade/Livewire), and if needed add a **new optional column** (add-only migration) **only if there isn’t already an “other” text field**.
4. Must work in **Create**, **Edit**, and **Show** views, and **Draft save** must retain the “Other” text.
5. Ensure no regression to Residential/Commercial switching logic.

---

## Step 1 — Find the Landlord “Hire” Livewire + Blade files

Search the codebase for the View Preference field.

**Global search (Replit):**

* Search: `View Preference`
* Search: `view_preference`
* Search: `viewPreference`

You’re looking for the Landlord hire component, typically something like:

* `app/Http/Livewire/LandlordAgentAuction*.php`
* `resources/views/livewire/landlord-agent-auction*.blade.php`

Also check any shared partials:

* `resources/views/livewire/partials/*.blade.php`
* `resources/views/components/*.blade.php`

---

## Step 2 — Fix the UI so “Other” does NOT render as label text

### Problem pattern (what to remove)

Most likely the Blade currently does something like this (bad pattern):

* It prints the selected value as a heading/label, e.g. `Other View Preference:` when selected.

### Required behavior

When **Other** is selected:

* show a normal label like **“View Preference (Other)”** (optional)
* show an **empty input**
* do **NOT** print the literal selected value (“Other …”) as a heading

### Implement this Blade logic

In the Blade file where View Preference renders, replace any “selected value heading” behavior with:

```php
{{-- View Preference --}}
<div class="mt-4">
  <label class="block font-semibold">View Preference:</label>

  {{-- Multi-select or select --}}
  <select wire:model="view_preference" class="w-full form-select">
    {{-- keep existing options --}}
  </select>

  {{-- If "Other" selected, show ONLY the textbox (no "Other View ..." text) --}}
  @if(
      (is_array($view_preference) && in_array('Other', $view_preference))
      || (!is_array($view_preference) && $view_preference === 'Other')
  )
    <div class="mt-2">
      <input
        type="text"
        wire:model.defer="view_preference_other"
        class="w-full form-input"
        placeholder="Type the View Preference (Other)..."
      />
    </div>
  @endif
</div>
```

**Important:**

* Do **NOT** echo `{{ $view_preference }}` anywhere as a heading/label.
* If you currently have something like `{{ $selectedViewPreferenceLabel }}` that becomes “Other View …” → remove/replace it with static label text.

---

## Step 3 — Make sure the “Other” textbox saves correctly (Create + Draft + Edit)

### In the Livewire component PHP

Verify you have:

* `public $view_preference;`
* `public $view_preference_other;` (string, nullable)

If `$view_preference_other` does not exist, add it (safe).

Add a tiny cleanup rule so “Other text” clears if user unselects Other:

```php
public function updatedViewPreference($value)
{
    $hasOther = is_array($value)
        ? in_array('Other', $value)
        : ($value === 'Other');

    if (!$hasOther) {
        $this->view_preference_other = null;
    }
}
```

### Saving logic (critical)

Where you save the listing (draft/save/submit), ensure BOTH fields persist:

* `view_preference` saves as-is (string or JSON/array depending on your schema)
* `view_preference_other` saves to its own column (preferred), OR to an existing “other” column if you already have one.

Example in save handler:

```php
$model->view_preference = $this->view_preference;
$model->view_preference_other = $this->view_preference_other;
$model->save();
```

---

## Step 4 — If there is NO column for `view_preference_other`, add an ADD-ONLY migration

**ONLY if needed** (safeguard: add-only, no renames).

Create migration:

```bash
php artisan make:migration add_view_preference_other_to_landlord_agent_auctions
```

Migration example:

```php
Schema::table('landlord_agent_auctions', function (Blueprint $table) {
    $table->string('view_preference_other')->nullable()->after('view_preference');
});
```

Run:

```bash
php artisan migrate
```

---

## Step 5 — Fix the SHOW page output (this is usually where “Other View …” appears)

On the public listing display page, ensure it renders like:

* If selected values include `Other`, display:

  * View Preference: (list of non-Other values)
  * Other: (the typed text)
* **Do NOT** print `Other` as if it’s a formatted label like “Other View Preference”.

Example Blade display:

```php
@php
  $vp = $listing->view_preference;
  $vpArr = is_array($vp) ? $vp : (json_decode($vp, true) ?: [$vp]);
  $vpClean = array_values(array_filter($vpArr, fn($x) => $x && $x !== 'Other'));
@endphp

<div>
  <div class="font-semibold">View Preference:</div>

  @if(count($vpClean))
    <div>{{ implode(', ', $vpClean) }}</div>
  @else
    <div>—</div>
  @endif

  @if(in_array('Other', $vpArr) && !empty($listing->view_preference_other))
    <div class="mt-1">
      <span class="font-semibold">Other:</span> {{ $listing->view_preference_other }}
    </div>
  @endif
</div>
```

---

## Step 6 — Apply same logic to EDIT view (tabs + skipping issues don’t matter here, but must be consistent)

In the edit Blade:

* bind the select to `view_preference`
* bind the textbox to `view_preference_other`
* ensure the component `mount()` loads both values from the model

Example mount pattern:

```php
public function mount($id)
{
    $listing = LandlordAgentAuction::findOrFail($id);

    $this->view_preference = $listing->view_preference;
    $this->view_preference_other = $listing->view_preference_other;
}
```

---

## Quick QA Checklist (Must Pass)

* Create Landlord listing → select **Other** → textbox appears **with no “Other View …” text**.
* Save Draft → refresh → textbox value persists.
* Edit listing → textbox is populated → user can update it.
* Show listing → displays non-Other values cleanly + “Other:” line only if typed.
* Unselect Other → textbox disappears and clears on save.

---

If you paste me the **current Landlord View Preference Blade snippet** (the part that’s printing “Other View …” above the input), I’ll rewrite it **exactly** to match your file structure (same CSS classes, same tab layout, same variable names) so your Replit dev can drop it in with zero guesswork.
