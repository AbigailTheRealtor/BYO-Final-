Use this for Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A SELLER’S AGENT ONLY — STILL NOT RESOLVED, DO NOT ADD POLLING WORKAROUNDS WITHOUT PROVING THE REAL UI IS FIXED

### ISSUE

This is **still not resolved**.

Please do **not** treat this as fixed based on:

* Playwright limitations
* polling theory
* internal logs
* “should appear within 100ms”
* programmatic value-setting tests

I need the **actual Seller Sale Terms UI** fixed in the real browser.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename or delete database columns.**
2. **Do not modify Buyer, Landlord, or Tenant flows.**
3. **Do not change labels, tooltips, or option values.**
4. **Do not add more workaround layers unless you first prove the actual root cause in the visible Seller DOM.**
5. **Do not rely on polling as the final explanation unless the actual browser UI is visually confirmed working in the exact real scenario.**
6. **Do not mark this complete from internal tests alone.**
7. **You must verify the actual visible browser UI, not just console state.**

---

# IMPORTANT

I am not asking whether the sections can be forced to display programmatically.

I am asking that the real Seller Sale Terms form work correctly when an actual user interacts with it.

Right now the issue is still that:

* **Assignment Contract** should immediately show **Seller Under Contract for Assignment**
* **Other** under **Special Sale Provision** should immediately show its input
* **Offered Financing/Currency** options like **Seller Financing** should immediately show their related sub-questions
* these should not require unrelated field changes
* these should not depend on Occupant Type
* these should not depend on delayed rerenders
* these should not depend on a hidden polling workaround unless the actual UI is visibly correct

---

# CONCERN WITH THE LATEST RESPONSE

The latest response says polling was added every 100ms.

That is **not enough** to close this unless you prove in the actual visible browser UI that:

1. the sections really appear for the user
2. they appear in the correct place
3. they appear from real manual interaction
4. they stay correct during normal use
5. they are not just correct when values are set programmatically

---

# REQUIRED ROOT-CAUSE CHECKS BEFORE CLAIMING FIXED

Please inspect and confirm all of the following in the **actual Seller Sale Terms form**:

## 1. Is Select2 updating the correct underlying field for real manual clicks?

Not just in code. Confirm in the real visible browser session.

For:

* `sale_provision`
* `offered_financing`

verify that real manual clicks update the correct underlying value and do not get overwritten immediately.

## 2. Are there duplicate select elements or duplicate IDs?

You mentioned there may be two `#offered_financing` elements.

Please fully inspect and confirm:

* whether duplicate IDs exist
* whether duplicate hidden/visible copies exist
* whether handlers are attached to the wrong element
* whether Select2 is rendering against one element while logic reads another

This is critical.

## 3. Are the conditional sections tied to the correct visible field instance?

If duplicate elements exist, the handler may be reading one select while the user interacts with another.

Please verify:

* the exact field instance the user clicks
* the exact field instance the visibility logic reads
* the exact section instance the visibility logic shows/hides

## 4. Are the sections appearing in the correct visible DOM location?

Even if polling sets display to block, verify the sections are:

* inside the visible Seller Sale Terms panel
* not under the wrong field
* not outside the visible wrapper
* not hidden by a parent container

---

# REQUIRED FIXES

The following must work in the real visible browser UI:

## A) Special Sale Provision → Assignment Contract

When selected manually by the user:

* **Seller Under Contract for Assignment** must appear immediately in the correct place

## B) Special Sale Provision → Other

When selected manually by the user:

* the **Other** input must appear immediately in the correct place

## C) Offered Financing/Currency

When the user manually selects options like:

* **Seller Financing**
* **Assumable**
* **Other**

their corresponding sub-sections must appear immediately in the correct place

---

# REQUIRED TEST — REAL UI ONLY

Please test the exact real Seller form in the browser like an actual user:

## Test 1

1. open Seller Sale Terms
2. manually click **Other** under Special Sale Provision
3. confirm the Other input appears immediately in the visible UI

## Test 2

1. manually click **Assignment Contract**
2. confirm **Seller Under Contract for Assignment** appears immediately in the visible UI

## Test 3

1. manually click **Seller Financing** under Offered Financing/Currency
2. confirm Seller Financing sub-questions appear immediately in the visible UI

## Test 4

1. manually click **Assumable**
2. confirm Assumable sub-questions appear immediately in the visible UI

## Test 5

1. manually click **Other** under Offered Financing/Currency
2. confirm the Other financing input/sub-section appears immediately in the visible UI

---

# REQUIRED OUTPUT BACK TO ME

Do not just say “fixed.”

Please tell me:

1. whether duplicate `#offered_financing` or other duplicate select IDs were found
2. whether duplicate visible/hidden field instances were found
3. which exact DOM element the user is interacting with
4. which exact DOM element the visibility logic is reading
5. whether polling is truly needed, or whether there was a deeper selector/duplicate-element issue
6. provide a screenshot of the actual Seller Sale Terms page showing:

   * Assignment Contract selected and sub-question visible
   * Other selected and input visible
   * Seller Financing selected and its sub-questions visible

---

# SUCCESS CRITERIA

This is only fixed when the actual visible Seller Sale Terms page shows:

* **Seller Under Contract for Assignment**
* **Other Special Sale Provision input**
* **Seller Financing / Assumable / Other financing sections**

immediately after real manual selection by the user.

---

# FINAL RULE

Do **not** close this based on polling theory, Playwright limitations, or programmatic tests.

The real visible Seller UI must work correctly for real user interaction.

---

Their latest response still sounds like a workaround plus theory. The line about there being **two `#offered_financing` elements** is the biggest red flag here. That is exactly the kind of thing that can make this bug persist.
