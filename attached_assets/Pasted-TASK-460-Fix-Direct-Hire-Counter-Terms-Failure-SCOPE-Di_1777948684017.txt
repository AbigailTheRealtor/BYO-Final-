TASK #460 — Fix Direct Hire Counter Terms Failure

SCOPE:
Direct Hire “Request Changes / Counter Terms” only.

DO NOT TOUCH:
- Offer Listing create/edit flows
- Agent Preset saved defaults
- Dashboards
- Accepted summaries
- Referral tracking
- Database schema / migrations
- migrate:fresh
- Accept/acknowledgment flow unless the exact exception proves it is involved

IMPORTANT:
Do this in two steps:
1. Diagnose from the actual runtime log.
2. Apply only the smallest fix for the confirmed root cause.

STEP 1 — REPRODUCE + LOG FIRST
Before changing code:

1. Clear or tail the log.
2. Click “Request Changes / Counter Terms” on buyer/residential direct hire.
3. Immediately capture the newest `HireAgentDirect confirm failed` entry.

Return:
- exception class
- message
- file
- line
- stack trace top 10 lines
- role
- agentId
- clientId
- bid_id/listing_id if logged
- network URL/method/status

Do not fix until this is captured.

STEP 2 — PINPOINT FAILURE LOCATION
Classify the failure as one of:

A. Token/session validation  
B. Request validation  
C. Listing creation  
D. Bid creation  
E. saveMeta failure  
F. Route::has / redirect  
G. Destination counter controller/view  

STEP 3 — FIX ONLY CONFIRMED ROOT CAUSE
Apply the smallest targeted fix.

Likely areas to verify:
- Correct FK column on role bid model:
  buyer_agent_auction_id
  seller_agent_auction_id
  landlord_agent_auction_id
  tenant_agent_auction_id
- saveMeta exists and meta tables exist for listing/bid models
- $listing->user_id is the logged-in client
- $bid->user_id is the selected agent
- Route::has($counterRoute) is true before redirect
- No token is consumed before a retry can happen

DO NOT:
- Guess
- Rename routes
- Refactor the controller
- Change shared marketplace counter behavior
- Change schema

STEP 4 — VERIFY
Test:
- Buyer residential direct hire counter
- Seller residential direct hire counter
- Landlord residential direct hire counter
- Tenant residential direct hire counter

Expected:
- No error flash
- Redirects to correct view-counter page
- No new `HireAgentDirect confirm failed` entries after successful submission

Return:
- root cause
- exact files changed
- before/after behavior
- browser verification result