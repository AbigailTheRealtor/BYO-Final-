Here are **copy-paste Replit instructions (with safeguards)** to add the missing icon for **“Expansion Commission for Lease Amendment:”** on the **Hire a Landlord’s Agent → Commercial → Broker Compensation & Agency Agreement Terms** section.

---

## ✅ Objective

Add an **icon matching the exact same size/color/style** used by the other fields in this section **ONLY** for the **Commercial Landlord listing** field:

**Expansion Commission for Lease Amendment:**

Right now it’s the only input in that block without the left icon inside the input group.

---

## SAFEGUARDS (DO NOT BREAK ANYTHING)

1. **Do NOT rename** any database columns, Livewire properties, or validation keys.
2. **Do NOT change** any existing tooltips, labels, placeholder text, or field order.
3. **Do NOT globally change** the icon component—apply this only to the **Commercial Landlord** version of this field.
4. Implement as **view-only markup** (Blade) unless the field itself is missing (it isn’t).
5. Must visually match **existing icons in this same section** (same wrapper divs, same classes, same spacing).

---

## Step 1 — Locate the Commercial “Broker Compensation” Blade block for Landlord

In Replit, search:

* `Expansion Commission for Lease Amendment`
* `expansion_commission`
* `Lease Amendment`

You should land in a Blade file similar to one of these patterns:

* `resources/views/livewire/landlord-agent-auction-edit.blade.php`
* `resources/views/livewire/landlord-agent-auction-create.blade.php`
* `resources/views/livewire/landlord-agent-auction.blade.php`
* or a shared partial like:

  * `resources/views/livewire/partials/broker-compensation/*.blade.php`

**Important:** Make sure you’re editing the **Landlord** hire flow and the **Commercial** subsection.

---

## Step 2 — Copy the exact icon-wrapper pattern from a nearby field

In your screenshot, fields like **“Landlord’s Broker Lease Fee”** and **“Payment Timing for Broker Fees”** have:

* a left icon block
* then the input
* and sometimes a right `%` addon

Find a nearby field that looks identical (best one: any field with a **$ icon** or **clock icon**) and copy its **entire “input group” wrapper structure** (not the label).

You’re looking for markup shaped like one of these patterns:

### Pattern A (common)

* `div` wrapper
* `div` for icon (prepend)
* `input` / `select`
* optional suffix

### Pattern B (Bootstrap input-group)

* `<div class="input-group">`
* `<span class="input-group-text">` (icon)
* `<input class="form-control">`

Use whatever your file already uses—**do not invent new styling**.

---

## Step 3 — Add the icon to “Expansion Commission for Lease Amendment” field

Inside the **Expansion Commission for Lease Amendment** field block, wrap the input with the same icon structure.

### ✅ Recommended icon meaning

Use an icon that communicates “expand / growth / increase / amendment”:

* **arrows-expand**, **up-right arrow**, **maximize**, or **trending-up**

### Implementation rule

**Use the same icon library already used in this file.**

* If the other fields use **Heroicons SVG**, use that.
* If they use **Font Awesome**, use that.
* If they use **Bootstrap icons**, use that.

---

## Copy-Paste Example (Bootstrap input-group style)

If your other fields use `input-group` + `input-group-text`, apply this:

```html
<!-- Expansion Commission for Lease Amendment -->
<div class="mt-3">
  <label class="form-label">
    Expansion Commission for Lease Amendment:
    <i class="fa fa-info-circle" data-bs-toggle="tooltip" title="..."></i>
  </label>

  <div class="input-group">
    <!-- ICON (match same classes used elsewhere) -->
    <span class="input-group-text">
      <i class="fa-solid fa-up-right-and-down-left-from-center"></i>
      <!-- If you already use a different FA style, match it exactly -->
    </span>

    <input
      type="number"
      step="0.01"
      wire:model.defer="expansion_commission_for_lease_amendment"
      class="form-control"
      placeholder="Enter percentage of original commission for expansion (e.g., 50)"
    />

    <span class="input-group-text">%</span>
  </div>
</div>
```

**BUT**: If your file uses SVG icons instead of `<i class="fa...">`, do not use Font Awesome. Use the same SVG style as the other fields.

---

## Copy-Paste Example (SVG icon style)

If the neighboring fields look like “icon inside a small colored square” using SVG, do this:

1. Copy the icon `<div>` used by an existing field (ex: Payment Timing)
2. Replace only the SVG path with a “expand/trending” icon (or reuse an existing icon component used elsewhere)

Example shape:

```html
<div class="flex items-center gap-2">
  <div class="icon-badge">
    <!-- Paste an existing SVG icon block from another field, then swap SVG only -->
    @include('icons.trending-up') 
  </div>

  <input ... />
  <div class="suffix">%</div>
</div>
```

(Use whatever your project’s actual icon include format is—some of your pages likely have `@include('icons.money')`, etc.)

---

## Step 4 — Restrict it to Commercial Landlord only

This is important since you asked **commercial listing**.

Wherever you branch Residential vs Commercial in the Landlord flow, apply the icon only in the Commercial block, e.g.:

```php
@if($property_type === 'Commercial')
   <!-- commercial fields including Expansion Commission w/ icon -->
@endif
```

or if you use `listing_property_category`, `lease_space_type`, etc—match your existing condition exactly.

**Do not add global CSS** and do not edit shared partials used by other flows unless you confirm it’s Landlord-Commercial-only.

---

## Step 5 — QA Checklist (Must Pass)

* The icon appears **left of the field** exactly like the other rows.
* Icon size and color match the other icons (no misalignment).
* No layout shift on mobile.
* Works in:

  * Create Landlord listing (Commercial)
  * Edit Landlord listing (Commercial)
* Does **NOT** appear in:

  * Residential Landlord broker comp section (unless it already does and you want it)
  * Tenant / Buyer / Seller flows

---

## If you want the “exact same icon” without guessing

Tell Replit dev to **reuse the exact icon component** already used by the “Payment Timing for Broker Fees” row but swap the glyph only if needed.

If you paste the Blade snippet for:

* one field that has an icon (ex: Payment Timing), and
* the Expansion Commission field block,

…I’ll rewrite it **exactly** in your project’s formatting (same classes, same wrappers, same icon system), so it’s a true drop-in patch.
