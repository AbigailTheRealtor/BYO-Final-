Audit and fix the Hire a Landlord’s Agent draft picker/load behavior.

Issue:
On /hire/agent/auction/landlord/{listingId}, the “Load Saved Draft” modal appears, but the draft row text is invisible/blank. When a draft row is clicked, the URL changes to the selected draft URL/id, but the form does not populate and the same Load Saved Draft modal remains open.

Example:
Current page:
/hire/agent/auction/landlord/18

Problems to fix:
1. Draft modal list rows are blank/invisible.
2. Clicking a draft changes the URL but does not hydrate/populate the form.
3. Modal stays open after selecting a draft.
4. This is happening in Hire a Landlord’s Agent edit/draft flow, not the Offer Listing Landlord flow.

Audit requirements:
- Compare Hire a Landlord’s Agent draft modal behavior against the working Seller/Buyer/Tenant Hire Agent flows.
- Confirm the unified TenantAgentAuction Livewire component correctly receives user_type = landlord and listingId/draft id.
- Check whether the draft modal links are pointing to the correct route:
  /hire/agent/auction/landlord/{draftId}
- Check whether the selected landlord draft is being loaded from the correct SellerAgentAuction record and meta fields.
- Check whether modal display logic still thinks drafts are available after a draft has already been loaded.
- Check CSS for the draft modal list text/buttons. The text should be readable, not white-on-white or hidden.

Expected behavior:
- Draft rows show visible identifying text, preferably listing title, draft date, or “Untitled Draft – [date]” fallback.
- Clicking a draft loads that draft and populates all saved Landlord Hire Agent form fields.
- After the draft loads, the Load Saved Draft modal closes/does not keep reappearing for the selected draft.
- Start New closes the modal and allows a blank Landlord Hire Agent form.
- Delete All Drafts still works.
- Do not touch Offer Listing landlord files unless the same shared code is confirmed to be used.
- Do not modify Limited Service legacy paths.
- Keep this fix scoped to Hire Agent / TenantAgentAuction draft loading logic.

Also verify after fix:
- /hire/agent/auction/landlord opens normally.
- /hire/agent/auction/landlord/{draftId} opens with saved draft data populated.
- Switching tabs does not clear hydrated draft fields.
- Save Draft still creates a new draft version.
- Save Edit still updates the loaded listing/edit record.