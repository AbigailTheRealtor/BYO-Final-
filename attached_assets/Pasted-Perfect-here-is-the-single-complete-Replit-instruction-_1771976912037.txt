Perfect — here is the **single, complete Replit instruction block** you should paste **one time**.
This includes:

* Add Download Listing button
* PDF snapshot system
* Safeguards
* Formatting rules
* Permission protection
* Future-proofing

Nothing conflicts with what we discussed.

---

# 🧠 PASTE THIS INTO REPLIT

## FEATURE: Add “Download Listing Snapshot (PDF)” button to all listings

### GOAL

Add a **Download Listing Snapshot (PDF)** button under the Edit button on:

* Seller listings
* Buyer listings
* Landlord listings
* Tenant listings

This will download a clean snapshot of the listing for debugging, review, and sharing.

---

# 🔒 HARD SAFEGUARDS (VERY IMPORTANT)

DO NOT:

* Modify database schema
* Rename models
* Rename routes
* Change listing edit pages
* Change listing display logic
* Expose hidden/private fields
* Break permission rules

ONLY:

* Add button
* Add download route
* Add controller
* Add PDF view

This is display/export only.

---

# BUTTON PLACEMENT

Place directly under Edit button.

Label:

```html
Download Listing Snapshot (PDF)
```

Style:
Match Edit button styling.

Each role page should have its own route.

---

# ROUTES

Add routes for each listing type:

```php
Route::get('/seller/listings/{listing}/download', [ListingDownloadController::class, 'seller'])
->name('seller.listings.download');

Route::get('/buyer/listings/{listing}/download', [ListingDownloadController::class, 'buyer'])
->name('buyer.listings.download');

Route::get('/landlord/listings/{listing}/download', [ListingDownloadController::class, 'landlord'])
->name('landlord.listings.download');

Route::get('/tenant/listings/{listing}/download', [ListingDownloadController::class, 'tenant'])
->name('tenant.listings.download');
```

Do NOT modify existing routes.

---

# CONTROLLER

Create:

```
app/Http/Controllers/ListingDownloadController.php
```

Controller should:

* Load listing model based on role
* Verify user can view listing
* Generate PDF if DomPDF exists
* Otherwise download HTML
* Use simple clean layout
* Respect hidden field rules

Do NOT dump raw database arrays directly to user long-term.

But for now, acceptable to use simple layout snapshot.

---

# PDF SNAPSHOT RULES

Snapshot must reflect **what user sees**.

Include:

* Listing ID
* Role
* Visible sections
* Property Details / Preferences
* Terms
* Services
* Compensation
* Party info (if visible)

Exclude:

* Hidden address if not yet revealed
* Internal notes
* Admin-only fields
* Emails not shown in UI

---

# DISPLAY FORMATTING RULE

If field has:

* **1 value** → show text
* **Multiple values** → show bubble chips

This applies to all listing types.

Normalize display wording:

* If value contains “Residential Property”
  → display as “Residential”

* If value contains “Commercial Property”
  → display as “Commercial”

Do NOT modify database values.
Display-layer only.

---

# FILE NAMING

```php
seller-listing-1234.pdf
buyer-listing-1234.pdf
landlord-listing-1234.pdf
tenant-listing-1234.pdf
```

---

# PERMISSIONS

Only allow download if user can view listing.

Use same permission logic as listing show page.

---

# FALLBACK

If PDF library fails:

* Download HTML instead
* Do not throw error

---

# FUTURE READY (do not implement yet)

Prepare controller so later we can generate:

* Accepted bid summary PDF
* Negotiation snapshot
* Agreement packet

No UI changes now.

---

# FINAL NOTE

This must NOT change any existing listing functionality.
Only add safe download snapshot capability.

---

# WHAT THIS BUILDS FOR YOU

You’re creating your platform’s:

**Listing Snapshot Engine**

You’ll reuse this for:

* Offer summaries
* Agent agreements
* Negotiation records
* Compliance snapshots

So this is infrastructure, not just a button.

---

# AFTER YOU PASTE THIS

Tell me what Replit says or builds.
I’ll review it and tighten anything needed before you test download.
