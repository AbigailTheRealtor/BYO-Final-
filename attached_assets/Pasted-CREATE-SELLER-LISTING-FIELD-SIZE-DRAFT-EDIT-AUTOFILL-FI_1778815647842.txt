CREATE SELLER LISTING — FIELD SIZE + DRAFT/EDIT AUTOFILL FIXES WITH SAFEGUARDS

Target flow:
Create Seller Listing only:
- /offer-listing/seller
- /offer-listing/seller/edit/{id} or draft/edit seller listing paths if applicable

DO NOT touch:
- Hire Agent flows
- Buyer, Landlord, Tenant flows
- Limited Service legacy paths
- routes, migrations, database schema, models, or unrelated controllers unless absolutely required for the listed bugs
- public views unless needed only to verify saved/displayed values

Goal:
Fix remaining Seller Offer Listing field UI inconsistencies and draft/edit autopopulation gaps without changing existing working fields, tab order, validation, save logic, or icons elsewhere.

ISSUES TO FIX

1. Oversized input boxes
The following Seller Create Listing fields render taller/larger than normal inputs. Match their size/height to correct standard fields such as “Tax Year” and “Parcel ID / Folio Number”:

- Additional Parcel IDs
- Legal Description
- Special Assessment Description
- Application Approval Process
- Additional Leasing Restriction
- Additional Seller Sale Terms

Requirements:
- Keep textarea behavior if field is intentionally a textarea, but normalize visible height/padding so it does not appear oversized.
- Do not convert fields to a different name/model binding.
- Do not alter labels, placeholders, tooltips, or validation.
- Apply the smallest CSS/class fix possible, scoped only to Seller Offer Listing wizard if possible.
- Verify create and edit/draft render the same.

2. Association Type “Other” icon issue
Bug:
When user selects “Other” for Association Type, the icon/field underneath appears incorrectly under or near “Association Name.”

Fix:
- Audit the Association Type “Other” companion input wrapper.
- Ensure it uses the same standard has-icon/input-cover structure as other companion “Other” fields.
- Ensure data-icon is applied to the correct input wrapper only.
- Ensure addIconsToInputs() does not inject duplicate/misaligned icons after the field is shown.
- Ensure the “Other” field appears directly under Association Type, not visually under Association Name.
- Do not change Association Name behavior.

3. Draft/Edit autopopulation gaps
The following fields do not correctly autopopulate when loading Seller draft/edit:

- Listing Type
- Special Sale Provision → when “Other” was selected, the custom Other text must repopulate
- Seller Under Contract Assignment
- Type of Loan

Requirements:
- First audit the actual saved keys/meta names for these fields.
- Fix create/edit/draft hydration so saved values repopulate into the correct inputs/selects.
- For Select2 fields, ensure values are set before/after Select2 init as needed and trigger change safely.
- For conditional “Other” fields, ensure the companion input becomes visible and contains the saved custom value.
- Do not rename database columns or meta keys.
- Do not break saving for fields already working.
- Do not overwrite existing draft values with blanks.

4. Audit for nearby missed Seller draft/edit fields
Because several fields were previously missing, audit the same Seller Create Listing file/partials for nearby conditional fields using similar logic:
- “Other” companion fields
- Select2 multi-selects
- conditional loan/financing fields
- assignment-related fields
- seller sale terms fields

Only fix confirmed Seller Offer Listing draft/edit autopopulation bugs found during this audit. Do not make speculative broad rewrites.

SAFEGUARDS

Before editing:
1. Identify exact Blade/Livewire/component/controller files serving /offer-listing/seller.
2. Confirm this is Offer Listing Seller, not Hire Seller Agent.
3. Check current git diff. If unrelated pending changes exist, stop and report them.
4. Locate the existing field names/wire:model/name attributes for each listed field.
5. Locate save + draft/load/hydration logic for Seller Offer Listing.

Implementation rules:
- Minimal surgical changes only.
- Prefer fixing existing bindings/hydration over adding duplicate fields.
- No schema changes.
- No route changes.
- No tab order changes.
- No changes to Buyer/Landlord/Tenant/Hire Agent.
- No Limited Service edits.
- Do not remove existing tooltips, placeholders, icons, or validation.
- Do not change existing working Select2 behavior such as closeOnSelect:false.

Validation/QA after changes:
1. Run code search confirming no Hire Agent files changed.
2. Run code search confirming no Buyer/Landlord/Tenant files changed unless shared Seller-only CSS is intentionally scoped.
3. Confirm oversized fields visually match Tax Year / Parcel ID height standard.
4. Confirm Association Type = Other shows the companion input in the correct place with correct icon alignment.
5. Create/save draft with these values, reload draft/edit, and confirm:
   - Listing Type repopulates
   - Special Sale Provision = Other repopulates and custom text shows
   - Seller Under Contract Assignment repopulates
   - Type of Loan repopulates
6. Confirm no console errors.
7. Confirm git diff only includes intended Seller Offer Listing files.
8. Provide changed file list and explain exactly what was fixed.