Use this:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT’S AGENT ONLY — FINAL TARGETED FIX FOR OFFERED LEASE TERM + ACCEPTABLE PROPERTY STYLES FLASH

### SCOPE

Fix **only** these 2 remaining issues in **Hire a Tenant’s Agent**:

1. Under **Offered Lease Term**, the user selects options and they appear selected in the UI, but the selected values are **not actually showing/persisting correctly**.
2. **Acceptable Property Styles** flashes/resets when the user fills in any other terms on the **Property Preferences** page.

Keep this **extremely targeted**. Do not patch unrelated issues.

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Tenant’s Agent flow.**
2. **Do not touch Buyer, Seller, or Landlord flows.**
3. **Do not rename, delete, or migrate any database columns.**
4. **Do not change labels, placeholders, tooltips, option text, or required-field rules.**
5. **Do not do a broad Select2 rewrite.**
6. **Do not refactor shared app-wide JS unless there is no safe Tenant-only fix.**
7. **Do not make trial-and-error edits.**
8. **First trace the exact Tenant-only binding, option source, and rerender trigger for these 2 fields before changing code.**
9. **If the fix cannot be done safely in Tenant only, stop.**

---

## REQUIRED APPROACH

Do **not** treat this as a generic UI bug.

Before editing, identify for **Tenant only**:

### Offered Lease Term

* exact Livewire property name
* exact Blade/select field binding
* whether Select2 is used
* whether there is a hidden JSON mirror/bridge
* where the value is normalized before save
* where the value is restored on edit/draft
* where the actual listing display reads the value

### Acceptable Property Styles

* exact Livewire property name
* exact parent dependency field or fields
* option source array
* filter logic
* whether Select2 is used
* what exact field updates on Property Preferences trigger the flash/reset
* whether the field is being destroyed/reinitialized on unrelated updates

Do not make changes until those paths are confirmed.

---

## ISSUE 1 — OFFERED LEASE TERM LOOKS SELECTED BUT DOES NOT ACTUALLY SHOW / PERSIST CORRECTLY

Fix **Offered Lease Term** so it is truly connected from UI → Livewire → save → restore → listing display.

### Requirements

* The selected values must not be visual-only.
* When the user selects Offered Lease Term, the actual Livewire property must update immediately.
* If Select2 is used, its change event must sync to the **real** Livewire property, not only to a visual widget state.
* If a hidden JSON bridge is being used, make sure:

  * it stays in sync with the real property
  * arrays remain arrays
  * encode/decode is correct
  * create, draft, edit, and submit all use the same format
* The same stored structure must be used for:

  * create
  * save draft
  * edit auto-populate
  * final listing display

### Specific rule

If the field is multi-select, do **not** let it switch formats between:

* JS array
* comma-separated string
* JSON string
* PHP array

Use one consistent format end-to-end.

### Success result for this issue

When the user selects Offered Lease Term:

* it stays selected
* it survives tab changes
* it saves in draft
* it restores on edit
* it displays correctly on the actual listing

---

## ISSUE 2 — ACCEPTABLE PROPERTY STYLES FLASHES / RESETS WHEN USER FILLS IN ANY OTHER PROPERTY PREFERENCES TERMS

Fix **Acceptable Property Styles** so unrelated field updates do **not** refresh, clear, or reinitialize it.

### Requirements

* Acceptable Property Styles should refresh **only** when its true parent dependency changes.
* It should **not** flash, clear, rebuild, or lose selections when the user edits unrelated Property Preferences fields.
* If Select2 is used, do not destroy/reinitialize it on every Property Preferences update.
* Narrow the refresh trigger so it runs only when the actual parent dependency for Property Styles changes.
* If a general Livewire rerender or message hook is causing this field to reset, add a **Tenant-only guard** so this field is not rebuilt on unrelated updates.
* Preserve currently selected property style values during non-related rerenders.

### Specific rule

Do **not** let broad tab-level updates re-run the Property Styles option-loading logic unless the actual dependency field changed.

### Success result for this issue

* no flash
* no reset
* no clearing
* no loss of selection
* no rebuild when typing or selecting unrelated fields on Property Preferences

---

## IMPORTANT IMPLEMENTATION RULES

### For Offered Lease Term

* Trace the field end-to-end and fix the real binding path.
* Confirm the saved value format matches the restore/display format.
* Do not rely on visual Select2 state alone.

### For Acceptable Property Styles

* Narrow the dependency watcher.
* Narrow the rerender trigger.
* Prevent unnecessary Select2 destroy/reinit cycles.
* Preserve selected values when unrelated fields update.

---

## TESTING REQUIREMENTS

Test **Hire a Tenant’s Agent only** in all 3 modes.

### Create

1. Open Property Preferences
2. Select Offered Lease Term
3. Move between tabs
4. Confirm it remains selected
5. Fill in other Property Preferences fields
6. Confirm Acceptable Property Styles does **not** flash/reset
7. Submit successfully
8. Confirm Offered Lease Term shows correctly on final listing

### Draft

1. Save draft with Offered Lease Term selected and Property Styles selected
2. Reopen draft
3. Confirm both restore correctly
4. Change unrelated Property Preferences fields
5. Confirm Property Styles does not flash/reset

### Edit

1. Open existing Tenant listing
2. Confirm Offered Lease Term values are restored correctly
3. Confirm Acceptable Property Styles does not flash when editing other fields
4. Change values
5. Save and reopen
6. Confirm persistence

---

## DO NOT CHANGE

* do not touch Buyer, Seller, or Landlord
* do not rewrite all tab JS
* do not do a global Select2 fix
* do not change required fields
* do not change field wording
* do not change database structure
* do not modify unrelated listing display logic

---

## SUCCESS RESULT

This task is complete only when:

1. **Offered Lease Term** truly persists and displays correctly
2. **Acceptable Property Styles** no longer flashes/resets when unrelated Property Preferences fields are changed
3. both fixes work in **create, draft, edit, and listing display**
4. no other role flow is affected

---
