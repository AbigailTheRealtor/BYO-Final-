TASK: Agent Dashboard Hire Agent listings — Phase 2 UI label updates ONLY

GOAL
Update labels and section titles ONLY for agent-created dashboard listings so they reflect agent-created listings instead of consumer-created listings.

This phase is UI/text only.
Do NOT touch:
- validation
- persistence
- save logic
- services logic
- compensation logic
- timers
- bid logic
- accepted summary logic
- referral payout logic

CRITICAL SCOPE
Only apply these label changes to AGENT-DASHBOARD-CREATED listings.
Do NOT change public/consumer flows such as tenant@exp.com or other original user-created hire-agent flows.

HARD SAFEGUARDS
1. Read-only audit first.
2. Identify the exact create/edit blades and listing detail/display blade(s) used by agent-dashboard-created listings.
3. Identify exactly how the app distinguishes agent-created listings from original consumer-created listings.
4. Do NOT make global text replacements.
5. If the same blade is shared by both public and agent flows, use conditional logic so only agent-created listings get the new labels.
6. If unsure how to safely scope the label change, STOP and report instead of applying it.

REQUIRED CHANGES

1) CREATE / EDIT FLOW LABELS — AGENT DASHBOARD ONLY

Where the agent-dashboard-created listing flow currently shows any of these role-specific labels:
- Tenant’s Info
- Buyer’s Info
- Seller’s Info
- Landlord’s Info

change them ONLY for the agent-dashboard-created flow to:
- Agent Credentials & Contact Info

This applies only where that section/tab is presented in the create/edit experience for agent users.

2) LISTING DETAIL / DISPLAY LABELS — AGENT-DASHBOARD-CREATED LISTINGS ONLY

On the actual listing detail/display page for agent-created listings only:

If the section title currently shows:
- Tenant’s Info
- Buyer’s Info
- Seller’s Info
- Landlord’s Info

change it to:
- Agent’s Info

IMPORTANT
- Preserve original role-based labels for public/consumer-created listings.
- Do not rename field values themselves unless needed.
- This is only about headings/section labels.

IMPLEMENTATION RULES
- Prefer conditional rendering:
  - if listing is agent-created/dashboard-created -> show agent wording
  - else -> preserve original role wording
- Do not touch database schema
- Do not refactor unrelated components
- Do not alter field requirements or tab logic

REQUIRED AUDIT OUTPUT BEFORE CHANGES
Report:
1. exact files/blades/components used for create/edit labels
2. exact files/blades/components used for listing detail/display labels
3. exact condition used to detect agent-created listings
4. whether any shared blade requires conditional branching

MANDATORY VERIFICATION
Test all four listing types created from the agent dashboard:
- Hire a Tenant Agent
- Hire a Buyer Agent
- Hire a Seller Agent
- Hire a Landlord Agent

Confirm:
A. create/edit flow shows “Agent Credentials & Contact Info” where appropriate
B. listing detail/display shows “Agent’s Info”
C. public/consumer-created listings still show original role labels:
   - Tenant’s Info
   - Buyer’s Info
   - Seller’s Info
   - Landlord’s Info

DELIVERABLE FORMAT
Return:
1. audit findings
2. files changed
3. exact conditional logic used to scope labels to agent-created listings
4. confirmation that public/consumer flows were not altered
5. manual verification results for all four agent listing types plus one public/consumer regression check