Here are **copy-paste Replit instructions with strong safeguards** for these exact **Hire a Buyer’s Agent** edits only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A BUYER’S AGENT ONLY — “OTHER” INPUTS / REQUIRED-FIELD UI / TYPE OF LOAN FIXES

## SCOPE

Make these fixes **only for Hire a Buyer’s Agent**:

1. **Minimum Bathrooms Needed**

   * when user selects **Other**, the custom input box must show when creating a listing
   * when user selects **Other**, the custom input box must also show when editing a listing
   * the custom value must display on the actual listing
   * the listing must not show `Bathrooms:` with a blank value

2. **Garage/Parking Features Needed**

   * currently says **Yes** and may show the **Other** input box, but does not show the actual selections correctly
   * when user selects **Yes**, the related input selections must appear
   * when editing, saved selections must repopulate correctly
   * listing display must show the saved values correctly

3. **Pets**

   * should **not** be a required field

4. **Type of Loan**

   * custom input/value does not save
   * does not show on listing
   * does not repopulate on edit

5. **Offered Financing/Currency**

   * this field is functionally required on submit, but the UI does **not** show it as required properly
   * it does not show the red required styling/message under the field
   * it does not show in the red required state box like other required fields before submit
   * it should visually behave like the other required Buyer fields

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Buyer’s Agent flow.**
2. **Do not modify Tenant, Landlord, or Seller flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change Select2 initialization.**
10. **Do not change selection box format, layout, styling, placeholder behavior, width, badge style, or dropdown behavior.**
11. **Do not change tooltips, icons, field labels, field order, or placeholders unless strictly needed for the required-state UI of Offered Financing/Currency.**
12. **Do not do a broad cleanup or refactor.**
13. **Do not touch any other “Other” fields except the exact Buyer fields listed below if needed.**
14. **Do not break the current required-field submit flow for Buyer.**
15. **Keep this surgical and field-specific.**

---

## IMPORTANT

We have had issues before with Replit editing the wrong file or breaking selection inputs.

### Hard stop:

Before editing anything, first confirm the **actual active Buyer files** for:

* Buyer create form
* Buyer edit form
* Buyer listing display
* Buyer Minimum Bathrooms Needed field
* Buyer Garage/Parking Features Needed field
* Buyer Pets field
* Buyer Type of Loan field
* Buyer Offered Financing/Currency field

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap changes so they affect **Buyer only**.

---

## STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Buyer’s Agent**:

1. Livewire/component file handling Buyer create/edit
2. Blade/partial rendering the **Minimum Bathrooms Needed** field
3. Blade/partial rendering **Garage/Parking Features Needed**
4. Blade/partial rendering **Pets**
5. Blade/partial rendering **Type of Loan**
6. Blade/partial rendering **Offered Financing/Currency**
7. Blade/partial rendering the **actual Buyer listing display**

### Requirement

Return the exact files first, then edit only those files.

### Hard stop

**No other file should be edited or changed.**

---

## STEP 2 — FIX MINIMUM BATHROOMS NEEDED “OTHER” ON CREATE + EDIT + LISTING DISPLAY

## Current bug

When user selects **Other** for **Minimum Bathrooms Needed**:

* the custom input box does not show during create
* the custom input box does not show during edit
* the listing shows `Bathrooms:` but the value is blank even though it was filled in

## Required behavior

For **Buyer Minimum Bathrooms Needed only**:

* if user selects **Other** during create, show the custom input immediately
* if saved selection is **Other** during edit, show the custom input and preload the saved value
* on the listing, show the custom value
* if there is no actual bathroom value, do not show the `Bathrooms:` row at all

## What to trace and fix

Confirm:

* main bathrooms property
* bathrooms “Other” property
* conditional logic that shows the custom input during create
* conditional logic that shows the custom input during edit
* save/store/update path
* listing display logic

### Important

Do not rewrite all “Other” logic globally.
Fix only the Buyer Minimum Bathrooms Needed path.

---

## STEP 3 — FIX GARAGE/PARKING FEATURES NEEDED CREATE/EDIT DISPLAY LOGIC

## Current bug

For **Garage/Parking Features Needed**:

* it may show **Yes**
* it may show the **Other** input box
* but it is not showing the actual saved selections correctly
* when user selects **Yes**, the related inputs are not popping up correctly

## Required behavior

For **Buyer Garage/Parking Features Needed only**:

* when user selects **Yes** during create, the related input selections must appear immediately
* if user selected options and saved them, those selections must repopulate correctly on edit
* if “Other” exists and was used, the custom input must repopulate correctly
* the listing must show the saved values correctly
* do not show a blank row on the listing if nothing meaningful was saved

## What to trace and fix

Confirm:

* parent yes/no trigger property
* features multiselect/select property
* “Other” property if applicable
* conditional display logic on create
* edit hydration logic
* listing display rendering logic

### Important

Do not change the select format.
Do not rewrite all garage/parking logic globally.
Fix only this exact Buyer field path.

---

## STEP 4 — MAKE PETS NOT REQUIRED

## Required behavior

For **Buyer Pets only**:

* remove required enforcement
* it should not block submit
* if there is a visual required indicator, remove it
* if it is included in JS required-field checks, remove it
* if it is included in PHP required validation, remove it

### Important

Align all 3 layers for Buyer Pets only:

1. visual required state
2. front-end submit blocking
3. server-side validation

Do **not** affect Pets in other flows.

---

## STEP 5 — FIX TYPE OF LOAN SAVE + EDIT + LISTING DISPLAY

## Current bug

**Type of Loan**:

* does not save input
* does not show on listing
* when user clicks edit, it does not repopulate

## Required behavior

For **Buyer Type of Loan only**:

* value must save correctly
* value must show on listing
* value must repopulate on edit
* if empty, do not show the row on the listing

## What to trace and fix

Confirm:

* exact property name
* whether a separate custom “Other” or free-text property exists
* `wire:model`
* save/store/update path
* edit hydration path
* listing display path

### Important

Do not change field format.
Do not rename properties.
Fix only the broken save/display/hydration path.

---

## STEP 6 — FIX OFFERED FINANCING/CURRENCY REQUIRED-STATE UI

## Current bug

**Offered Financing/Currency** is acting like a required field at submit time, but the UI is not showing required status correctly:

* no proper red required styling under the field
* no proper red-box required state like other Buyer required fields
* user only finds out at submit time

## Required behavior

For **Buyer Offered Financing/Currency only**:

* the field should visually show as required in the same format as the other required Buyer fields
* if your Buyer UI uses red helper text / red border / red required state box, this field must match that same pattern
* it should show in the red required validation UI before/when validation runs, consistent with other required fields

## Important

Do **not** change whether the field is required unless current Buyer business logic already says it must be required.
This task is to align the **UI required-state behavior** with the existing required logic.

## What to trace and fix

Confirm:

* whether the field already has required validation in JS/PHP
* why the required visual state is missing
* whether the field is missing:

  * required marker class
  * required helper span
  * invalid feedback block
  * red highlight class hook
  * inclusion in the Buyer required-field UI collector

Fix only what is needed so it behaves like the other Buyer required fields.

---

## STEP 7 — HIDE EMPTY LISTING ROWS FOR THESE FIELDS

For the actual Buyer listing display:

* if **Minimum Bathrooms Needed** has no real value, do not show the row
* if **Garage/Parking Features Needed** has no real value, do not show the row
* if **Type of Loan** has no real value, do not show the row
* if **Offered Financing/Currency** has no real value and your current display logic allows empty rows, do not show an empty row

### Important

Do not hide filled values.
Do not affect other listing types.

---

## STEP 8 — PRESERVE BACKWARD COMPATIBILITY

If older Buyer records already saved:

* Minimum Bathrooms Needed = Other with custom value
* Garage/Parking Features selections
* Type of Loan values
* Pets values
* Offered Financing/Currency values

They must still:

* load correctly on edit
* display correctly on listing
* not require migrations
* not break existing saved data

---

## STEP 9 — TEST THESE EXACT CASES

### A. Minimum Bathrooms Needed → Other

* create listing
* select Other
* expected: custom input appears immediately
* save
* click Edit
* expected: custom input shows and is populated
* expected: listing shows actual bathroom value
* expected: no blank `Bathrooms:` row

### B. Garage/Parking Features Needed

* select Yes
* expected: related inputs appear immediately
* select feature values and save
* click Edit
* expected: saved selections show correctly
* if Other/custom exists, expected: custom input repopulates
* expected: listing shows actual values

### C. Pets

* leave blank
* expected: submit is not blocked because of Pets
* expected: no required indicator remains

### D. Type of Loan

* enter value and save
* expected: shows on listing
* click Edit
* expected: value repopulates correctly

### E. Offered Financing/Currency

* leave blank
* expected: field shows proper required-state UI consistent with other Buyer required fields
* expected: required red UI/feedback appears correctly

---

## STEP 10 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Tenant files changed
* no Landlord files changed
* no Seller files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Buyer fields changed

---

## STEP 11 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Buyer files were identified first
3. exact root cause for:

   * Minimum Bathrooms Needed Other not showing on create/edit/display
   * Garage/Parking Features Needed selections not appearing/repopulating
   * Pets being incorrectly required
   * Type of Loan not saving/displaying/repopulating
   * Offered Financing/Currency missing required-state UI
4. exact fix applied for each
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof the Buyer listing now shows these fields correctly and hides empty rows

---

## IMPORTANT FINAL RULE

This is a **Buyer-only, surgical fix**.

Do **not**:

* edit unrelated files
* change selection box format
* refactor global select logic
* touch Tenant, Landlord, or Seller
* make broad UI changes

The final result should be:

* Minimum Bathrooms Needed Other works on create, edit, and listing display
* Garage/Parking Features Needed shows the correct inputs and saved selections
* Pets is not required
* Type of Loan saves, displays, and repopulates
* Offered Financing/Currency shows the correct required-state UI
* no other file or selection input behavior is changed

---

If you want, I can also make this into a **shorter hard-stop version** for Replit with even tighter language.
