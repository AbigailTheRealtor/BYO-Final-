Absolutely — the **Save Edit** button should match **Save Draft** in both **placement** and **styling**.

That is the better UX.

Right now the current **Save Edit** looks inconsistent because:

* it appears **after** Next instead of before it
* it uses a **different color/style**
* it does not visually match the existing action pattern users already understand

The cleanest fix is to make **Save Edit** use the **same button component/classes/order as Save Draft**, and only change the label text from **Save Draft** to **Save Edit** when in edit mode.

Here are copy-paste-ready Replit instructions with safeguards:

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — MAKE “SAVE EDIT” MATCH “SAVE DRAFT” STYLE AND POSITION

## GOAL

For **edit listing mode only**, make the **Save Edit** button match **Save Draft** exactly in:

* **button color**
* **button styling**
* **padding / border / radius**
* **icon behavior** if applicable
* **placement/order**

### Required UI result

The button order should be:

**Save Edit** → **Next**

not:

**Next** → **Save Edit**

And the **Save Edit** button should visually look like the existing **Save Draft** button, just with the text changed to:

**Save Edit**

Apply this consistently for all listing edit flows:

* Seller
* Buyer
* Landlord
* Tenant

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename, remove, or migrate any database columns.**
2. **Do not change save logic unless necessary for wiring the button to the existing edit-save action.**
3. **Do not break Save Draft anywhere in create mode.**
4. **Do not restyle unrelated buttons across the app.**
5. **Do not edit dashboard pages, listing cards, or non-listing forms.**
6. **Only change the shared tab action/footer area that controls listing form navigation/actions.**
7. **Reuse the same CSS classes / Blade component / button markup already used by Save Draft whenever possible.**
8. **Do not create a second custom style for Save Edit.**
9. **Do not change the Next button styling.**
10. **Do not duplicate logic across Seller/Buyer/Landlord/Tenant files if one shared file controls the action bar.**
11. **If the action bar is shared, update only the shared file.**
12. **Stop if this would require a large refactor. Use the smallest possible change.**

---

# REQUIRED RESULT

## In edit mode

On every tab/page:

* show **Save Edit**
* place it **before** the **Next** button
* use the **same style/classes/component as Save Draft**
* keep the **Next** button in its current style
* keep spacing consistent with Save Draft layout

## In create mode

Do **not** replace Save Draft.

Create mode should continue to show:

**Save Draft** → **Next**

Edit mode should show:

**Save Edit** → **Next**

So the pattern stays identical, only the label/action changes depending on mode.

---

# IMPLEMENTATION STRATEGY

## 1) FIND THE EXACT BUTTON MARKUP USED FOR “SAVE DRAFT”

Before making changes, locate the exact shared Blade/component markup used for the current **Save Draft** button.

Identify:

* the Blade file or partial rendering the tab footer buttons
* the CSS classes applied to Save Draft
* whether Save Draft uses:

  * a shared Blade component
  * plain button markup
  * icon markup
  * conditional rendering for create/edit mode

**Do not imitate it manually if it can be reused directly.**

Best practice:
Use the exact same markup/classes/component as Save Draft and only swap:

* label text
* click action / wire:click / submit target if needed
* mode condition

---

## 2) USE THE SAME ORDERING LOGIC AS SAVE DRAFT

In the action/footer area, the button order in edit mode should match draft mode.

### Correct order:

1. Save Edit
2. Next

### Incorrect order:

1. Next
2. Save Edit

If the current edit button was added separately or appended later in the markup, move it so it renders in the same spot where Save Draft renders during create mode.

Do not use CSS hacks like flex reverse unless the shared layout already depends on that.
Fix the actual render order in the Blade/component markup.

---

## 3) SAVE EDIT SHOULD REUSE SAVE DRAFT’S VISUAL STYLE

Do not invent new amber/yellow/orange styling for Save Edit.

Instead, Save Edit should use the same style source as Save Draft:

* same classes
* same variant
* same icon treatment if already present
* same spacing and size
* same border/radius

Only the text should change from:

* **Save Draft** → create mode
* **Save Edit** → edit mode

If Save Draft uses an icon and you want identical appearance, either:

* reuse the same icon styling, or
* remove icon from both only if both are intentionally standardized in the shared component

But do **not** make Save Edit visually inconsistent.

---

## 4) EDIT MODE SHOULD SWITCH LABEL, NOT VISUAL PATTERN

Use the existing mode detection and keep one visual pattern.

Preferred logic:

* if editing existing listing → show **Save Edit**
* if creating new listing → show **Save Draft**

That means the same button slot can likely be reused with conditional label/action logic.

This is cleaner than having two separately styled buttons.

---

## 5) KEEP EXISTING SAVE ACTIONS SEPARATE

Do not make Save Edit call the Save Draft backend.

Use the same visual component, but keep the correct backend action:

* **Save Draft** should keep draft behavior
* **Save Edit** should keep existing edit/update behavior

So this is mainly:

* a UI consistency fix
* button order fix
* shared component reuse

not a backend rewrite

---

# WHAT TO INSPECT FIRST

Find the file that controls the listing form tab footer/action row.

Specifically identify:

1. where **Save Draft** is rendered
2. where **Next** is rendered
3. where the newly added **Save Edit** is rendered
4. whether all four role types share the same footer file
5. whether the button is built using:

   * a shared Blade component
   * a reusable partial
   * direct button markup

Do **not** change role-specific files unless it is confirmed that each role truly has separate footer markup.

Given the project structure, check the shared TenantAgentAuction-based flow first before editing multiple files.

---

# REQUIRED CODE APPROACH

## Preferred approach

Use the exact **Save Draft** button markup/component and make it conditional:

* in create mode: render it as **Save Draft**
* in edit mode: render it as **Save Edit**

That means:

* same placement
* same styling
* same spacing
* different label
* different action target if needed

## Avoid this approach

Do **not** leave the existing custom Save Edit button in place and merely tweak colors manually if the Save Draft component already exists.

That creates long-term inconsistency.

---

# EXAMPLE EXPECTED LOGIC PATTERN

This is conceptual only — use the project’s actual syntax and button component:

* if edit mode:

  * render Save Draft-style button
  * label = **Save Edit**
  * action = existing edit/update save method
* else if create mode:

  * render Save Draft-style button
  * label = **Save Draft**
  * action = existing draft save method

Then render **Next** after it.

The key point is:
**same button shell, same position, different mode-based label/action**

---

# DO NOT CHANGE

* Next button style
* Previous button behavior
* Save Draft behavior in create mode
* final tab save/update behavior unless necessary
* validation architecture
* database schema
* unrelated page layouts
* unrelated components for dashboards or public listing pages

---

# ACCEPTANCE TESTS

## Test 1 — Create mode unchanged

Open a new listing for each role type.

Confirm button row still shows:

**Save Draft** → **Next**

Confirm Save Draft still looks the same as before.

---

## Test 2 — Edit mode layout fixed

Open an existing listing for each role type.

Confirm button row now shows:

**Save Edit** → **Next**

Confirm Save Edit is before Next.

---

## Test 3 — Styling match

Compare Save Edit in edit mode to Save Draft in create mode.

Confirm they match in:

* background/border color
* font size
* border radius
* padding
* height
* icon alignment if present
* spacing between buttons

---

## Test 4 — Save Edit behavior still works

On edit mode:

* change a field
* click Save Edit from a tab
* confirm the listing saves successfully
* reload page
* verify changes persisted

---

## Test 5 — Next still works

In both create and edit mode:

* click Next
* confirm tab navigation still works exactly as before

---

## Test 6 — Shared coverage

Verify the same UI fix appears for:

* Seller
* Buyer
* Landlord
* Tenant

without inconsistent styling between roles

---

# EDGE CASES TO CHECK

1. **Mobile / narrow widths**

   * ensure Save Edit and Next still align properly
   * do not let button wrapping break layout unexpectedly

2. **Tabs with Previous + Next**

   * ensure Save Edit appears in the same correct button cluster/order
   * do not accidentally place it after Next on some tabs and before Next on others

3. **Tabs without Next**

   * if any final tab layout is special, do not break it
   * preserve final action logic unless specifically intended

4. **Icons**

   * if Save Draft has a disk icon, Save Edit should follow the same component rules
   * do not mix one icon button with one plain text button unless intentional in the shared component

---

# PREFERRED FINAL RESULT

The listing form should feel consistent:

## Create mode

**Save Draft** | **Next**

## Edit mode

**Save Edit** | **Next**

Same position.
Same style.
Same visual weight.
Only the text/action changes.

---

# DO NOT DO THESE

* Do not leave Save Edit after Next
* Do not keep yellow/orange styling for Save Edit
* Do not create a one-off button class just for Save Edit
* Do not duplicate separate styling in four different files
* Do not alter Save Draft’s existing create-mode behavior
* Do not perform a broad refactor of the form footer unless absolutely required

---

# REPORT BACK AFTER CHANGES

Please report back with:

1. **Exact file(s) changed**
2. **Whether the footer/action row was shared across all four listing types**
3. **Whether Save Edit now reuses the Save Draft markup/classes/component**
4. **How edit mode vs create mode was detected**
5. **Confirmation that Save Draft behavior was not changed**
6. **Confirmation that Save Edit now renders before Next on every edit tab**

---

Yes — your instinct is right.
**Save Edit should look exactly like Save Draft, just renamed for edit mode.**
