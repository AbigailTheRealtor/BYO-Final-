Paste this into Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — FIX ONLY “VIEW PREFERENCE NEEDED” — HIRE A BUYER’S AGENT

### PAGE TO USE

Use the exact Buyer page/route already provided:

`/hire/agent/auction/buyer`

This fix is for the **Buyer flow only**.

---

## ONLY ISSUE TO FIX

### View Preference Needed

This field is still doing the same bug we had before:

* user selects option
* it briefly selects
* then deletes/disappears

It must work exactly like the already-working Buyer field:

* **Non-Negotiable Amenities and Property Features**

That means:

* selection should stick immediately
* no disappearing
* no second click
* no deleting after Livewire re-render
* should work on create
* should work on edit
* should work after save/reload
* should work after validation errors
* should preload saved selections correctly

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Fix only the Buyer flow at `/hire/agent/auction/buyer`.**
2. **Fix only the single field `View Preference Needed`.**
3. **Do not edit Tenant, Seller, or Landlord files.**
4. **Do not edit unrelated Buyer fields.**
5. **Do not edit shared/global helpers, shared JS, shared Select2 logic, shared traits, or shared partials unless absolutely unavoidable.**
6. **If a shared file seems necessary, stop first and explain why the Buyer-specific file cannot be fixed by itself.**
7. **Do not rename, delete, migrate, or repurpose any database columns.**
8. **Do not do a global Select2 rewrite.**
9. **Do not change labels, placeholders, options, tooltips, validation, or other business logic.**
10. **Use the exact same working Buyer-side pattern already used by `Non-Negotiable Amenities and Property Features`.**
11. **No refactor. No cleanup. No “while I’m here” edits.**
12. **Smallest Buyer-only fix wins.**

---

## FIRST STEP — VERIFY THE CORRECT FILES BEFORE EDITING

Before changing anything, trace and report the exact files being used for this Buyer page:

* active route
* active Livewire component
* active Blade
* exact Buyer partial containing **View Preference Needed**
* exact Buyer partial containing **Non-Negotiable Amenities and Property Features**
* any Buyer-only script/init code handling Select2 for this page

### Do not edit until this is confirmed.

We have had repeated issues from the wrong file being edited.

---

# REQUIRED FIX

## Make `View Preference Needed` use the exact same working pattern as `Non-Negotiable Amenities and Property Features`

Do not guess.
Do not create a new approach.
Mirror the already-working implementation pattern from the working Buyer field.

### Check all of these for `View Preference Needed`:

* how the saved value is loaded/hydrated
* whether the value is cast/decoded to array correctly
* how Select2 is initialized
* how change events sync to Livewire
* whether `wire:ignore` is blocking display refresh
* whether selected `<option>` state is being restored properly
* whether Livewire re-render is overwriting the selected values
* whether before-submit sync is pushing DOM values into Livewire correctly
* whether message processed hooks are re-initializing incorrectly
* whether stale server state is wiping the latest client selection

### Required outcome

After your fix:

* selecting one option must stay selected
* selecting multiple options must stay selected
* the UI must visually show the selected values immediately
* switching tabs must not wipe them
* save/reload must preserve them
* edit mode must preload them
* validation errors must not wipe them

---

## IMPORTANT IMPLEMENTATION DIRECTION

For this specific fix, compare `View Preference Needed` directly against the already-working Buyer field:

* **Non-Negotiable Amenities and Property Features**

I want you to identify the exact difference between the two implementations and make `View Preference Needed` follow the working one in the Buyer file chain.

Do **not** just say “it should now work.”
Actually compare:

* markup
* model binding
* init selector
* change handler
* array normalization
* preload/hydration
* re-render handling
* submit sync

---

# QA CHECKLIST — MUST TEST BEFORE FINISHING

Test only in `/hire/agent/auction/buyer`.

## A. Create mode

* select one View Preference Needed option → stays selected
* select multiple options → all stay selected
* click around/switch tabs → still selected

## B. Save/reload

* save listing
* reload page
* selected View Preference Needed values still show correctly

## C. Edit mode

* open an existing Buyer listing with saved View Preference Needed values
* field preloads correctly
* no disappearing after page load or tab switch

## D. Validation/re-render

* trigger a validation error somewhere else on the form
* return to View Preference Needed
* selected values must still remain

## E. Compare against working field

* confirm `View Preference Needed` now behaves the same as `Non-Negotiable Amenities and Property Features`

---

# FINAL RESPONSE FORMAT I WANT

When done, respond with:

1. **Exact Buyer files changed**
2. **Exact difference found between `View Preference Needed` and `Non-Negotiable Amenities and Property Features`**
3. **What was changed to make View Preference Needed match the working field**
4. **Confirmation that no shared/global files were changed unless first proven unavoidable**
5. **QA results for A–E**

---

## IMPORTANT

This is not a request for a broad Buyer cleanup.
This is a **single-field surgical fix** for `View Preference Needed` only.

---

If you want, I can make this even more aggressive so Replit is forced to do a literal **field-vs-field comparison checklist** before touching code.
