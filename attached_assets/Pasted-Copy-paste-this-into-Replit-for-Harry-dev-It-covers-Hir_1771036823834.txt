Copy/paste this into Replit for **Harry/dev**. It covers **Hire a Seller’s Agent → Commercial + Business + Vacant Land** (actual listing display + edit + draft persistence) with **hard safeguards** so the wrong fields can’t bleed across property types.

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS

### Module: Hire a Seller’s Agent (Commercial + Business + Vacant Land)

**Test login:** `tenant@exp.com`
**Must work in:** Create → Save Draft → Open Draft → Edit → Save → Live Listing Display

---

# 0) Non-Negotiable Rules (Global Safeguards)

1. **One Source of Truth for each property type**
   Create a single mapping that defines, per property type (Residential / Income / Commercial / Business / Vacant Land):

   * which fields are shown on create/edit
   * which fields are rendered on live listing
   * the display label text
   * the display order

2. **Never reuse “Residential” field components for Commercial/Business/Vacant Land without a gate**
   The current issue (bedrooms/pets/carport/garage spaces bleeding into commercial/business) happens when shared partials render without strict property-type checks.

3. **Draft/Edit must repopulate from DB only**
   Do not rebuild Business Type / Property Style from request defaults on edit; always rehydrate from saved values.

---

# 1) FIX: Business Listings — “Business Type” not displaying & blank on Edit

### Problem

* Live listing shows wrong Business Type OR not the selected value
* Edit listing shows Business Type input blank

### Required Behavior

* Business Type must display **exactly what was selected**
* Edit must repopulate the selection

## 1.1 Database + Model Safeguards

Confirm these fields exist for Business listings:

* `business_type` (string) OR `business_type_id` (string/int)
* If “Other” is allowed: `business_type_other` (string nullable)

✅ Safeguard:

* If using multi-select, store JSON: `business_type` cast to array.
* Add to model `$fillable`
* Add to `$casts` (if JSON)

## 1.2 Create Livewire Save (Business Type)

In Seller Create component:

* Ensure the input binds to **one** Livewire property:

  * `wire:model="business_type"`
* If “Other” is selected:

  * `wire:model="business_type_other"`

On save:

* Persist both fields explicitly:

```php
$listing->business_type = $this->business_type;
$listing->business_type_other = $this->business_type_other;
```

✅ Safeguard:

* Do NOT depend on text-label matching. Store a stable ID or the exact label, consistently.

## 1.3 Edit Livewire Mount (Business Type)

In Seller Edit component `mount()`:

```php
$this->business_type = $listing->business_type;
$this->business_type_other = $listing->business_type_other;
```

✅ Safeguard:

* Do not overwrite these in `render()` or computed getters.
* If you have defaults, apply defaults only if DB value is null.

## 1.4 Live Listing Display (Business Type)

On Business live listing view:

* Display:

  * **Business Type:** {selected value}
  * If Other: show **Business Type (Other):** {business_type_other}

✅ Safeguard:

* Live listing must read from DB fields, not from a “property style” field or property_type.

---

# 2) FIX: “Property Style” incorrectly displays Property Type & shows weird “( )”

### Required Terminology (Hard Rule)

* **Property Type** is the main category: Residential / Income / Commercial / Business / Vacant Land
* **Property Style** is the subtype selection (e.g., Residential: Single Family Home; Business: Hotel/Motel)

### Required Live Listing Display

* Show **Property Type:** “Commercial” (or Business, etc.)
* Show **Property Style:** the selected subtype (e.g., “Hotel/Motel”)
* **Do not show any options with parentheses** (remove “( )” formatting entirely)

## 2.1 Root Cause Safeguard

Right now Property Style is likely being populated from `property_type` or a mixed list.

✅ Fix:

* Ensure `property_style` is its own persisted field:

  * `property_style` or `property_style_id`
  * optional `property_style_other`

## 2.2 Edit Repopulation

In edit mount:

```php
$this->property_style = $listing->property_style;
$this->property_style_other = $listing->property_style_other;
```

## 2.3 Display Formatting Safeguard (No “( )”)

Wherever you build dropdown labels, remove any label formatting like:

* `"Office (Class A)"` style patterns or empty parentheses

✅ Implementation rule:

* Dropdown options must be clean strings (no parentheses variants for this section).
* If you must keep parentheses internally, strip them when rendering in listing display:

  * `preg_replace('/\s*\(.*?\)\s*/', '', $label)`
    (Only if needed; best is to fix the source list.)

---

# 3) FIX: Bedrooms should NOT show on Commercial & Business

### Required:

* Bedrooms must be hidden on **Commercial** and **Business** live listing display.
* Also remove from edit/create UI for these types if still appearing.

✅ Implement:
In the live listing blade (or shared partial):

```php
@if(in_array($listing->property_type, ['Residential','Income']))
  // show Bedrooms
@endif
```

✅ Safeguard:

* Do not check for “not Vacant Land” — must be **explicit allowlist** (Residential/Income only).

---

# 4) FIX: Heated Sqft placement for Commercial & Business

### Required order for Commercial & Business:

* Bathrooms
* **Heated Sqft** (immediately after Bathrooms)
* (then continue)

✅ Implement:
In commercial/business display section:

* Move the Heated Sqft row to render right after Bathrooms row.

✅ Safeguard:

* Make display order driven by a per-type “field order array” (see section 0).

---

# 5) FIX: Commercial/Business showing residential garage/carport fields instead of “Garage/Parking Features”

### Required:

For Commercial + Business:

* Show **Garage/Parking Features** (the correct field)
* Do not show:

  * Carport
  * Carport Spaces
  * Garage
  * Garage Spaces
    (those are residential/income terms)

## 5.1 Data Field

Confirm correct field exists:

* `garage_parking_features` (string or JSON array)
* optional `garage_parking_features_other`

✅ Save & Edit safeguards:

* Add to `$fillable`
* Cast to array if multi-select
* Save explicitly on create/update
* Rehydrate on edit mount

## 5.2 Live listing render logic

For Commercial/Business:

* Render **Garage/Parking Features:** {values}
* For Residential/Income:

  * keep existing Garage/Carport fields if needed

✅ Safeguard:

* Use property type gates to prevent residential partial from rendering for commercial/business.

---

# 6) FIX: Vacant Land — Remove Residential/Income fields entirely

### Must NOT show on Vacant Land live listing:

* Bedrooms
* Bathrooms
* Total Sqft
* Sqft Heated Source
* Appliances Included
* Carport
* Carport Spaces
* Garage
* Garage Spaces
* Age Restricted Community
* Amenities and Property Features
* Pets Allowed (and all pet subfields)

✅ Implement:
In vacant-land display template:

* Use a dedicated Vacant Land partial OR a strict allowlist of fields.

✅ Safeguard:

* Never reuse the Residential “Property Details” block for Vacant Land.

---

# 7) FIX: Pets fields must NOT show for Commercial or Business

### Must NOT show on Commercial/Business live listing:

* Pets Allowed
* Number of Pets Allowed
* Acceptable Pet Types
* Maximum Weight Per Pet (lbs)
* Pet restrictions

✅ Implement:
Pets section should render only if:

```php
if ($listing->property_type === 'Residential') { ... }
```

(or if you also allow Income to show pets, add Income explicitly.)

✅ Safeguard:

* This must be an allowlist (Residential [+ Income if applicable]), not “everything except vacant”.

---

# 8) REQUIRED Financial field order by type

### Commercial live listing: after Amenities & Property Features show:

1. **Included Property or Business Assets**
2. **Annual Net Income**
3. **Cap Rate**

### Business live listing: after Amenities & Property Features show:

1. **Business & Real Estate Purchase Requirements**
2. **Included Property or Business Assets**
3. **Annual Net Income**
4. **Cap Rate**

✅ Implement:

* Ensure these fields exist and are saved for these types.
* Render exactly in this order.

✅ Safeguard:

* If a field is empty/null, hide it (but keep order for those that exist).

---

# 9) Vacant Land Draft Bug: “Appliances Included (Other)” textbox appears under Total Acreage

### Required:

* On Vacant Land draft/edit, **do not show any Appliances Included UI**, including the “Other” textbox.

✅ Fix:

* In the create/edit Blade for Vacant Land:

  * gate the entire Appliances Included block:

```php
@if($propertyType !== 'Vacant Land')
  // Appliances Included
@endif
```

✅ Safeguard:

* Also clear stale values when property type changes to Vacant Land:

  * On property type update handler:

```php
if ($this->property_type === 'Vacant Land') {
  $this->appliances_included = null;
  $this->appliances_other = null;
}
```

(Prevents old residential values from leaking into vacant land drafts.)

---

# 10) QA Acceptance Tests (Must Pass)

Using `tenant@exp.com`:

## Business Listing

* Business Type selected shows correctly on live listing
* Business Type repopulates correctly on Edit
* Property Type displays correctly
* Property Style displays selected subtype (not property type)
* No “( )” displayed in Property Style

## Commercial Listing

* Bedrooms NOT shown
* Heated Sqft shown immediately after Bathrooms
* Garage/Parking Features shown (and saves in draft/edit)
* No pets fields shown
* After Amenities/Features: Included Assets → Annual Net Income → Cap Rate

## Vacant Land Listing

* No bedrooms/bathrooms/sqft/appliances/amenities/pets/garage/carport fields shown
* Appliances “Other” box does NOT appear under Total Acreage in draft/edit

---

## If you paste the file paths, I’ll make this “line-by-line” for Harry

Send (just once):

1. Seller Create Livewire file path
2. Seller Edit Livewire file path
3. Seller live listing display Blade path (the one showing wrong fields)

…and I’ll rewrite this into **exact edits per file** (with the precise `@if` gates + the exact field order arrays).
