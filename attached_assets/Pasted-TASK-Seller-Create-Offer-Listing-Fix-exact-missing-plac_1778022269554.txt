TASK: Seller Create Offer Listing — Fix exact missing placeholders/icons only

SCOPE:
Seller Create Offer Listing Blade views only.

DO NOT:
- Do not modify Buyer, Tenant, or Landlord files
- Do not modify Livewire PHP
- Do not modify JS
- Do not change wire:model names
- Do not change validation
- Do not add <i class="input-icon">

FIX 1 — Operating Statement Available:
Operating Statement Available is missing the left icon.

Make it match the same UI pattern as Rent Roll Available:
- Keep its existing wrapper
- Select must have class="form-control has-icon"
- Select must have a document/file data-icon
- First option should be:
  <option value="">Select</option>

Do not copy Rent Roll’s wire:model.

FIX 2 — Reason for Sale / Other custom input:
Find the Reason for Sale field in the Seller Create Offer Listing flow.

When Reason for Sale = Other, show a custom text input.

The Other input should have:
- class="form-control has-icon"
- data-icon="fa-solid fa-pen"
- placeholder="Enter reason for sale (e.g., Relocating for work)"

If an Other input already exists, update it. Do not create a duplicate.

FIX 3 — Missing “Select” placeholder text:
These fields are missing the visible empty-state placeholder:

1. Offered Financing/Currency
2. Special Sale Provision
3. Additional Documents Available

Compare against the working Property Condition field and use the same placeholder/display pattern.

Expected:
- Empty value displays: Select
- Field keeps its existing wire:model
- Field keeps/gets class="form-control has-icon"
- Field keeps/gets an appropriate data-icon
- No duplicate hardcoded text outside the input/select

DONE WHEN:
- Operating Statement Available has a left icon like Rent Roll Available
- Reason for Sale shows an Other input only when Other is selected
- Other input placeholder says: Enter reason for sale (e.g., Relocating for work)
- Offered Financing/Currency shows Select when empty
- Special Sale Provision shows Select when empty
- Additional Documents Available shows Select when empty