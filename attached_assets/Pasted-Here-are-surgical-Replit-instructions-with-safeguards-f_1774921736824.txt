Here are **surgical Replit instructions with safeguards** for this exact **Tenant counterbid display** issue.

The screenshots show the problem clearly:

* On the **counterbid detail/view**, some Tenant broker compensation terms are rendering in an **old simplified format**
* The **actual input values** are not being fully shown for combo fee structures
* Example:

  * it should show **Purchase Fee: $3,333.00 + 4% of Total Purchase Price**
  * but it is only showing the structure label without the actual combined values
* It also needs to correctly show:

  * **Tenant’s Broker Commission Structure: Tenant Pays Out-of-Pocket**
  * **Early Termination Fee: Yes**
  * **Termination Fee Amount: $3,434.00**

So this is a **detail-view formatting / term rendering** issue, not just a storage issue.

---

# Replit Instructions With Safeguards

## Goal

Fix the **Hire a Tenant Agent counterbid/detail display** so the **Broker Compensation & Agency Agreement Terms** render using the **current Tenant listing/bid format** and show the **actual saved values**, not old simplified labels.

This applies to the Tenant counterbid/detail page, including pages like:

* `/tenant/hire/agent/auction/bid/{id}/view-counter`

---

## Current issue

On Tenant counterbid display:

### 1. Purchase Fee is incomplete

For combo structures, the detail page is showing only a partial/old label like:

* `Purchase Fee: Percentage of the Total Purchase Price + Flat Fee`

instead of showing the actual formatted saved values like:

* **Purchase Fee: $3,333.00 + 4% of Total Purchase Price**

---

### 2. Early Termination Fee block is incomplete/inconsistent

It should show:

* **Early Termination Fee: Yes**
* **Termination Fee Amount: $3,434.00**

If the amount exists, it must display.

---

### 3. Commission Structure needs current wording

If the saved selection is:

* **Tenant Pays Out-of-Pocket**

then the detail view should show that exact current wording, not an old legacy phrasing.

---

## Source of truth

Use the **current Tenant bid/listing field structure and display format** as the source of truth.

The counterbid detail page must render the same final terms that are actually saved in the bid/counter, using the same modern formatting logic used elsewhere in the active Tenant flow.

Do **not** use any old legacy formatter or stale “summary label only” rendering.

---

# Safeguards

## 1. Scope only to Tenant counter/detail rendering first

Fix only the active files/components used for:

* Tenant counterbid detail view
* Tenant bid detail summary view if it shares the same formatter/partial

Do not touch:

* Buyer
* Seller
* Landlord

unless the formatting helper is shared and can be updated safely without side effects.

---

## 2. No destructive DB changes

* Do not rename/delete/drop columns or tables
* Do not migrate data
* Do not overwrite stored values
* This should be a rendering/formatter fix first

---

## 3. Do not change form field labels or saved data structure

Do not change:

* input names
* database keys
* form wording
* field order

This is a **display formatting and value resolution fix**, not a schema redesign.

---

## 4. Preserve conditional display rules

Only show dependent fields when applicable.

Examples:

* show `Purchase Fee` only if `Interested in Purchasing a Property = Yes`
* show `Termination Fee Amount` only if `Early Termination Fee = Yes`
* show lease-option compensation fields only if lease-option interest = Yes
* show retainer amount/application only if retainer fee = Yes

---

# What Replit should inspect first

Find the exact active code used by the Tenant counter/detail page for:

1. Broker Compensation & Agency Agreement Terms rendering
2. Any formatter/helper that builds:

   * commission structure display
   * lease fee display
   * purchase fee display
   * early termination display
   * retainer display
3. Any shared partial/blade/component for Tenant bid summary/detail output

Most likely the issue is one of these:

### A. Old formatter is being used

The detail page is using a legacy formatter that only prints the structure label, not the actual child values.

### B. Combo fields are not being composed

For fields like purchase fee, the display logic is not combining:

* flat fee amount
* percentage amount
* fee basis/structure label

into one final readable line.

### C. Child values are saved but not read

The amounts are present in saved meta/data, but the detail view only reads the parent dropdown field.

### D. Wrong fallback label

The counter view may be using an old label map for commission structure instead of the current Tenant wording.

---

# Required fixes

## Part 1: Use current Tenant display formatter for counter/detail page

Make the Tenant counter/detail page use the same current formatting rules as the live Tenant bid/listing display.

Do not render only the parent dropdown/structure label when child values exist.

---

## Part 2: Properly render Purchase Fee combo values

For **Tenant’s Broker Purchase Fee**, if the saved structure is a combo like:

* `Percentage of the Total Purchase Price + Flat Fee`

or

* `Flat Fee + Percentage of the Total Purchase Price`

the detail page must render the actual values in final readable format.

### Example expected output

* **Purchase Fee: $3,333.00 + 4% of Total Purchase Price**

or if reversed structure:

* **Purchase Fee: 4% of Total Purchase Price + $3,333.00**

Use the actual saved structure order.

### Important

Do not show only:

* `Percentage of the Total Purchase Price + Flat Fee`

That is incomplete.

---

## Part 3: Properly render Early Termination Fee and amount

If:

* `Early Termination Fee = Yes`

and an amount exists, then show both:

* **Early Termination Fee: Yes**
* **Termination Fee Amount: $3,434.00**

If `No`, then do not show amount.

---

## Part 4: Preserve current Commission Structure wording

If the saved value is:

* **Tenant Pays Out-of-Pocket**

the detail page must show that exact current wording.

Do not replace it with:

* legacy phrase
* alternate wording
* old label key

---

## Part 5: Render logical Tenant terms completely

For the Tenant counter/detail page, each logical term should display its resolved final value.

Examples:

### Tenant’s Broker Commission Structure

Show selected current label

### Broker Commission Fee / Lease Fee

Show fully formatted value based on structure + amounts

### Purchase Fee

Show fully formatted value based on structure + amounts

### Lease-Option compensation

Show actual saved amount/percentage values

### Early Termination Fee

Show yes/no and amount if applicable

### Retainer Fee

Show yes/no and amount/application if applicable

### Agency Agreement Timeframe

Show selected value or custom “Other” text if applicable

### Acceptable Brokerage Relationship

Show current saved label

### Additional Terms

Show if filled

---

# Implementation pattern Replit should follow

## A. Find the active Tenant counter/detail blade/component

Identify the exact blade/component for:

* `/tenant/hire/agent/auction/bid/*/view-counter`

Then find whether it:

* uses inline rendering logic
* uses a summary builder
* uses a shared formatter helper

---

## B. Replace old summary-only output with resolved display values

For combo fields, build the final rendered string from saved child values.

Example for purchase fee:

* read structure
* read flat fee amount
* read percentage amount
* format currency/percent
* output in the structure’s intended order

---

## C. Add a Tenant-specific formatter if needed

If current shared formatting is stale or risky, create a Tenant-specific helper for detail rendering, something like:

* `formatTenantPurchaseFeeForDisplay()`
* `formatTenantLeaseFeeForDisplay()`
* `formatTenantCommissionStructureForDisplay()`
* `formatTenantTerminationFeeForDisplay()`

Use that helper only for Tenant counter/detail flow first.

---

## D. Use stored child values, not just parent labels

Where the detail page currently prints only the dropdown selection text, update it to also read the dependent child inputs that belong to that term.

---

# Copy-paste prompt for Replit

Use this exactly:

> Fix the Hire a Tenant Agent counter/detail view so Broker Compensation & Agency Agreement Terms render using the current Tenant format and actual saved values, not old simplified summary labels. Scope this only to the active Tenant counter/detail rendering flow first. Do not modify Buyer, Seller, or Landlord unless a shared formatter must be updated safely without side effects. No destructive DB changes and no changes to form field names, saved data structure, tooltips, or field order.
>
> The current issue is that combo terms are rendering incompletely on the Tenant counter/detail page. For example, Purchase Fee is showing only the structure label instead of the full saved value. It must display the actual formatted saved values, such as `$3,333.00 + 4% of Total Purchase Price`, using the saved structure order. Likewise, if Early Termination Fee = Yes and a termination amount exists, the view must show both `Early Termination Fee: Yes` and `Termination Fee Amount: $3,434.00`. Also preserve the current commission structure wording, e.g. `Tenant Pays Out-of-Pocket`, instead of any legacy label.
>
> Find the exact active formatter/partial/blade used by `/tenant/hire/agent/auction/bid/*/view-counter` and update it so logical Tenant terms display their resolved final values. For combo fee fields, build the display string from the saved child inputs plus the selected structure, instead of printing only the parent dropdown label. Preserve conditional display rules so dependent fields appear only when applicable.
>
> After the fix, verify that the Tenant counter/detail page shows full current-format values for Commission Structure, Lease Fee, Purchase Fee, Lease-Option compensation, Early Termination Fee and amount, Retainer fields, Timeframe, Brokerage Relationship, and Additional Terms.

---

# What Replit should test

## Test 1: Purchase Fee combo format

Use a Tenant counter with:

* Interested in Purchasing = Yes
* Purchase Fee structure = combo
* flat fee amount saved
* percentage amount saved

Expected:

* detail page shows full final string, like:

  * `$3,333.00 + 4% of Total Purchase Price`
    or equivalent order based on selected structure

---

## Test 2: Early Termination Fee

Use:

* Early Termination Fee = Yes
* Termination Fee Amount = 3434

Expected:

* shows:

  * `Early Termination Fee: Yes`
  * `Termination Fee Amount: $3,434.00`

---

## Test 3: Commission Structure wording

Use:

* Tenant Pays Out-of-Pocket

Expected:

* detail page shows exactly:

  * `Tenant Pays Out-of-Pocket`

---

## Test 4: Lease-Option fields

If lease-option = Yes and values exist
Expected:

* both lease-option compensation lines show actual saved values

---

## Test 5: Retainer fields

If retainer = Yes and amount/application exist
Expected:

* both amount and application render

If retainer = No
Expected:

* dependent retainer fields hidden

---

## Test 6: Additional Terms

If filled
Expected:

* displays value correctly on counter/detail page

---

# Best likely root cause

Most likely the counter/detail page is still using an **old summary formatter** that prints:

* parent dropdown label only
  instead of
* fully resolved current field values from the bid/counter payload.

So the fix is probably:

1. find active Tenant counter/detail formatter
2. replace summary-only output for combo terms
3. render actual saved child values in the proper current format

If you want, I can also give you a **shorter hard-safeguard version** in your exact Replit style.
