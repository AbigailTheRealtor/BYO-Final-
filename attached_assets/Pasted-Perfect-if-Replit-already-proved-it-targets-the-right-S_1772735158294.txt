Perfect — if Replit already proved it targets the right Seller code, you can give it **goal-based instructions** that still **prevent collateral edits** without micromanaging file paths.

Copy/paste this exactly:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS (GOAL-BASED) — Hire A Seller’s Agent (EDITS + LISTING DISPLAY)

### SAFEGUARDS (HARD RULES)

1. **Only apply changes to Hire A Seller’s Agent** (Seller listing create/edit/show + Seller listing display).
2. **Do NOT modify Tenant, Buyer, or Landlord flows** in any way.
3. **Do NOT rename or delete any database columns.**
4. **Do NOT delete or modify existing listings, drafts, or agent bids.**
5. **Do NOT change labels, tooltips, or option values.**
6. Only fix: **field rendering, formatting style (bubble→text), multi-select support, missing tab, and persistence for saved URLs.**
7. All fixes must be **backwards compatible** with existing saved data.

---

# A) FIX: Property Condition not showing on actual Seller listing

### Goal

On the public/live Seller listing display, **Property Condition** must show whenever it exists in the listing record.

### Requirements

* Render condition value safely whether stored as string or array/JSON
* Do not convert to bubble styling — standard text display only
* Do not change how it is saved; only ensure it appears on display

---

# B) FIX: These must be REGULAR TEXT (not bubbles) on Seller listing display

### Fields

* **Occupancy Requirement**
* **Timing of Transfer**

### Requirements

* On Seller listing display only, replace chip/badge/bubble output with plain text output
* Keep all other fields unchanged

---

# C) FEATURE: Acceptable Exchange Item must support MULTI-SELECT on Seller listing creation

### Goal

During Seller listing creation, user can select **more than one** Acceptable Exchange Item.

### Requirements

1. Input must support **multiple selections** (multi-select).
2. Must **save correctly** and **load correctly** in:

   * Drafts
   * Edits
   * Listing display
3. Must persist reliably with Livewire + Select2 (if Select2 is used):

   * Use your existing “stable guarded init” approach
   * Implement **dual-sync** on change:

     * update the Livewire array property directly via `@this.set(...)`
     * keep any JSON hidden bridge updated if your component uses one
4. Backend normalization safeguard before saving:

   * If null → `[]`
   * If JSON string → decode to array
   * Ensure stored format remains consistent with existing DB approach (no schema change)

---

# D) FIX: Additional Details tab is missing (Seller create/edit)

### Goal

Bring back the **Additional Details** tab in Seller listing create/edit.

### Requirements

* Restore the tab button AND its content panel
* Ensure tab navigation works normally (no flicker, no multiple clicks)
* Do not alter other tab text/order/logic

---

# E) FIX: Personal Video & Personal Photo not showing + must persist across edits

### Problems

* Not showing on listing display even though URLs exist
* Personal Photo disappears after multiple edits
* Both must remain saved until explicitly replaced or removed

### Requirements

1. **Listing display**

   * If personal photo URL exists, show it (link and/or preview, consistent with current UI)
   * If personal video URL exists, show it (clickable link)

2. **Persistence on Edit (critical)**

   * When saving edits, if the user leaves photo/video fields blank, **do NOT overwrite** existing DB values with blank/null
   * Only overwrite if a NEW non-empty value is provided
   * Only clear if you add/use an explicit “Remove photo/video” action (otherwise keep)

3. Must persist through unlimited edits:

   * Make 3+ edits without re-entering the URLs → URLs must remain

---

# ACCEPTANCE TEST (MUST PASS)

1. Create Seller listing:

   * Set Property Condition
   * Multi-select 2+ Acceptable Exchange Items
   * Add Personal Photo URL + Personal Video URL
   * Save

2. Listing display:

   * Property Condition appears
   * Occupancy Requirement + Timing of Transfer are plain text (no bubbles)
   * Acceptable Exchange Items display all selected values
   * Personal Photo + Video show

3. Edit listing 3 times:

   * Change unrelated fields
   * Leave photo/video fields blank
   * Save each time
     ✅ Photo/Video still show every time after each edit

---

If you want, I can also add a **one-line “Seller-only verification guard”** for Replit (a quick search check like “confirm component includes SellerAgentAuction in class name before changing”), but the above should be enough to keep it on track while staying file-agnostic.
