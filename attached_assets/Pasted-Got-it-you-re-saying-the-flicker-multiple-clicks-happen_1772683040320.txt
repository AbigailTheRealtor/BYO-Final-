Got it — you’re saying the **flicker/multiple clicks happens specifically on EDIT**, and the prior attempt was effectively treated like CREATE. Here are **new, tighter Replit instructions** that target the **actual landlord edit component/blade** and fix the most common *edit-only* causes:

* Livewire re-render resets the active tab to the first tab
* `message.processed` is re-binding handlers (stacking)
* a “Next” click triggers Livewire (validation/update) unintentionally on edit
* Select2 init runs on every processed message and steals focus mid-navigation

Below is a **single copy/paste** you can give Replit that is **EDIT-only** and includes concrete safeguards and QA.

---

## Replit Instructions With Safeguards

### Hire a Landlord’s Agent — **EDIT only** Tab Next button flickers / requires multiple clicks

**Safeguards (must follow):**

* Do **not** rename or delete any database columns
* Do **not** delete/modify existing listings, drafts, or agent bids
* Do **not** change labels, tooltips, or option values
* Do **not** refactor other flows (Buyer/Tenant/Seller)
* Fix **ONLY** the **Hire a Landlord’s Agent EDIT** tab navigation behavior
* Do not add polling or repeating timers

---

# Step 0 — Confirm the exact EDIT route + blade/component (must confirm first)

1. Use `php artisan route:list | grep -i landlord` and identify the **edit URL** you are using.
2. Confirm which Livewire component and blade render the edit page (example patterns):

* `TenantAgentAuctionEdit.php` + `tenant-agent-auction-edit.blade.php` (shared edit component)
* or `LandlordAgentAuctionEdit.php` + `landlord-agent-auction-edit.blade.php`

**Safeguard:** Apply code changes ONLY to the confirmed edit blade/component.

---

# Step 1 — Make Next/Prev navigation 100% client-side (no Livewire call on tab click)

On the EDIT page only:

### 1A) Ensure Next/Prev buttons never submit the form

* Any Next/Prev buttons must be:

  * `<button type="button">`

### 1B) Remove any `wire:click`, `wire:submit`, or JS that triggers Livewire on Next

* Next/Prev should ONLY switch tabs in the browser.
* Save/Submit is the only action that should call Livewire.

**Why:** On edit, Livewire often validates or re-renders mid-click, causing flicker/reset.

---

# Step 2 — Persist the active tab across Livewire re-renders (EDIT-only)

**This is the edit-only fix that usually solves “keeps jumping back.”**

### 2A) Add a small JS active-tab memory using sessionStorage

* On tab shown:

  * store the active tab id in `sessionStorage` using a unique key for landlord edit, e.g.:

    * `landlord_edit_active_tab`

### 2B) Restore active tab after Livewire updates (but only once per update)

* In `Livewire.hook('message.processed')`, restore the tab from sessionStorage **once**, then stop (do not loop).

**Critical safeguard:**
Do not trigger a “click” on the tab repeatedly. Use Bootstrap’s `Tab.show()` (or your tab system equivalent) and guard it so it runs once per processed message.

---

# Step 3 — Stop handler stacking on EDIT (this causes multiple clicks needed)

On the edit blade, any event binding inside init functions must be de-duped:

* `$(document).off('shown.bs.tab.landlordEdit').on('shown.bs.tab.landlordEdit', ...)`
* `$('.next-tab').off('click.landlordEdit').on('click.landlordEdit', ...)`

**Safeguard:** Never bind tab events inside a function that runs multiple times without `.off()`.

---

# Step 4 — Prevent Select2 init from firing repeatedly during tab navigation (EDIT-only)

### 4A) Move Select2 init OFF of message.processed or heavily guard it

If edit blade currently does:

* initSelect2() inside `message.processed`

Change to:

* In `message.processed`, run only a guarded init for selects **inside the currently active tab**, and only if not already initialized.

**Required guard per select:**

* if element has `select2-hidden-accessible`, do nothing.

### 4B) Never trigger `.trigger('change')` or `.trigger('change.select2')` in message.processed

Remove any “sync select2 values” calls from `message.processed` that trigger change events.

**Why:** change → Livewire update → message.processed → init again → flicker loop.

---

# Step 5 — Add a short “navigation lock” to stop double-tab switching

On Next click:

* set `window.__tabNavLock = true` for ~250ms
* ignore additional clicks while locked
* then release lock

**Safeguard:** This is UI-only and does not touch backend.

---

# Step 6 — QA verification (must pass)

1. Open EDIT for a Landlord listing
2. Click Next across tabs (including the tabs that previously flickered)
   ✅ One click advances one tab
   ✅ No flicker / no bouncing back
   ✅ Network tab shows **no Livewire requests** when clicking Next/Prev
   ✅ Livewire requests only happen on Save/Submit
   ✅ After saving (which triggers a rerender), you remain on the same tab you were on

---

## Copy/Paste Prompt for Replit (EDIT-only, tight scope)

“Fix Hire a Landlord’s Agent **EDIT page only** where Next tab flickers and requires multiple clicks. First confirm the exact edit route and which Livewire component/blade renders the edit form, then modify ONLY that edit blade/component. Make Next/Prev 100% client-side: ensure Next/Prev buttons are type=button and do not call Livewire. Persist active tab across Livewire re-renders using sessionStorage (save on shown tab, restore once per message.processed with a guard). De-dupe all event bindings with namespaced .off().on() so handlers do not stack. Guard Select2 init so it only initializes selects in the active tab and only if not already select2-hidden-accessible; remove any trigger('change')/sync functions from message.processed. Add a short 250ms navigation lock to prevent double tab switching. Verify: one click advances, no flicker, and no Livewire requests on tab navigation.”

---

### One extra line that helps Replit hit the right file on the first try

Add this at the top of your prompt when you paste it into Replit:

**“The bug happens only when clicking Edit on a Hire a Landlord’s Agent listing (not Create). Do not change the create blade.”**

---

If you paste the **actual edit URL path** you’re using (the route), I can tailor the first “confirm route” step to exactly the two files Replit should touch, so it doesn’t waste time searching.
