cope: Fix ONLY the “Offered Services” display inside Actual Bids Display → Private “Terms” modal/panel for Hire a Tenant’s Agent (Residential + Commercial).

A) Non-Destructive Rules (Critical)

DO NOT delete, rename, or recreate any existing:

database tables

migrations

models

controllers

Livewire components

Blade files

routes

existing fields/columns

tooltips / labels / copy

DO NOT change any business logic outside of the services rendering for the Private “Terms” view.

DO NOT modify the Actual Listing services renderer (the listing display is correct).

Only reuse it, import it, or call it.

DO NOT hardcode service arrays inside Blade templates (no giant PHP arrays in *.blade.php).

If a canonical service definition already exists, use it.

DO NOT introduce “auto-mapping” or “fallback grouping” that silently moves items into “Other Services.”

B) File Safety Rules (Prevent Parse Errors)

No inline multi-line PHP arrays inside Blade views unless the same structure already exists there.

If a service definition array is needed, it must live in the existing canonical location (config/service file/class) already used by the listing.

Any time you touch a Blade file:

ensure all quotes are paired

ensure arrays/parentheses/brackets are closed

run a quick check by reloading the listing page after each small change

Make changes in small, isolated commits:

Commit #1: import/reuse renderer

Commit #2: “Other Services” logic

Commit #3: ordering + residential/commercial switch
If anything breaks, revert only the last commit.

C) Data Integrity Rules (No schema drift)

Do not add new columns to store service groupings.

Grouping must be derived from the same stored selections already used by the listing page.

Do not change how services are saved.

Only change how they are displayed in the Private “Terms” view.

D) Consistency Requirement (Single Source of Truth)

The Actual Listing services display is the source of truth.
The Private “Terms” bid view must render services using the same renderer + same grouping + same order.

If a service appears under a category on the listing, it must appear under that same category in bid “Terms.”

E) Acceptance Check (Required Before Marking Done)

After changes, test with:

1 Residential listing + bid

1 Commercial listing + bid
Confirm:

no “Other Services” bucket appears unless the Agent entered “Other”

category headers match listing

service order matches listing

no missing services

no re-sorting alphabetically