## ✅ REPLIT COPY/PASTE INSTRUCTIONS (WITH SAFEGUARDS)

### Feature: **Hire a Seller’s Agent → Actual Listing Display (Show Page)**

### Test Login: **[tenant@exp.com](mailto:tenant@exp.com)**

### Primary Scope: **INCOME property type show page** (you referenced Income-only rules + Income service order)

### Also includes: **display order fixes + missing Income fields + Unit Type Configuration section + Broker Comp placement**

---

# 0) QA REPRO STEPS (MUST FOLLOW)

1. Login: **[tenant@exp.com](mailto:tenant@exp.com)**
2. Create a **Hire a Seller’s Agent** listing
3. Select **Property Type = Income**
4. Fill:

   * SqFt Heated Source
   * Appliances Included (select multiple)
   * Pets questions (if present)
   * Included Property or Business Assets
   * Total Number of Units
   * Total Number of Buildings
   * Annual Net Income
   * Cap Rate
   * Add at least **1 Unit Type Configuration row** with all unit fields (beds/unit, baths/unit, etc.)
   * Select many services across multiple sections (including **Provide digital photo enhancements + enhancement types**)
   * Broker Compensation: Seller’s Broker Purchase Fee + Nominal Consideration Fee
5. Submit → Open **actual listing view**
6. Confirm current issues exist (Bedrooms/Bathrooms showing, missing Income fields, services out of order, Nominal Consideration Fee placement wrong).

---

# 1) INCOME SHOW PAGE RULE: DO NOT DISPLAY Bedrooms/Bathrooms (GLOBAL)

### Requirement

For **Income property type**, “Bedrooms” and “Bathrooms” must **NOT** display on the actual listing.

### Replit Instructions

1. In the Seller show page template, find the general property details block where Bedrooms/Bathrooms are printed.
2. Wrap those lines in a property-type conditional:

* If property_type == **Income** → **do not render** Bedrooms or Bathrooms at all.
* Else → render normally (Residential, etc.).

### SAFEGUARDS

* Do not rely on “if value exists”; must be explicitly excluded for Income.
* Also remove any “Beds/Baths” summary line that aggregates these values for Income.

### Acceptance Criteria

Income listing show page contains **no Bedrooms or Bathrooms** fields.

---

# 2) PROPERTY DETAILS DISPLAY ORDER FIX: Appliances Included must be AFTER SqFt Heated Source

### Requirement

In Income show page property details, “Appliances Included” must appear **immediately after** “SqFt Heated Source”.

### Replit Instructions

1. Locate “SqFt Heated Source” line on Income show page.
2. Move “Appliances Included” rendering block so it appears directly after that line.

### SAFEGUARDS

* Appliances Included must render as the same “options list/chips” style used by Amenities/Property Features.
* If empty, show blank or hide per your standard (no “Not Provided”).

### Acceptance Criteria

Order:

* SqFt Heated Source
* Appliances Included

---

# 3) AFTER PETS: ADD MISSING INCOME FIELDS (THESE MUST DISPLAY)

### Requirement

After Pets questions, render these Income-specific fields (if present):

* Included Property or Business Assets:
* Total Number of Units:
* Total Number of Buildings:
* Annual Net Income:
* Cap Rate:

### Replit Instructions

1. In Income show page, locate the Pets section end.
2. Immediately after Pets block, add a new Income-only block to render:

**Included Property or Business Assets:** `[value]`
**Total Number of Units:** `[value]`
**Total Number of Buildings:** `[value]`
**Annual Net Income:** `[currency formatted]`
**Cap Rate:** `[value]` (ensure percent formatting if stored as number)

### SAFEGUARDS

* Display must pull from the **persisted listing record** (same keys saved on create/edit).
* No “Not Provided” wording:

  * If value empty → hide that line OR show blank value (match your standard).
* Format:

  * Annual Net Income must format with commas and `$` (e.g., `$125,000`)
  * Cap Rate must show `%` if numeric (e.g., `7%` or `7.0%` depending on your standard)

### Acceptance Criteria

These fields appear after Pets when present.

---

# 4) CREATE “UNIT TYPE CONFIGURATION” SECTION (BIG MAIN TITLE)

### Requirement

Create a **Unit Type Configuration** section with a major heading (same visual weight as “Sale Terms”).

### Replit Instructions

1. In Income show page, add a new major section header:

   * **Unit Type Configuration**
2. Place it after the Income-specific fields (from section #3).

### SAFEGUARDS

* Use the same heading component/style used for “Sale Terms” to maintain consistency.

### Acceptance Criteria

Unit Type Configuration appears as a main section title (not a small sub-label).

---

# 5) UNIT TYPE CONFIGURATION: RENDER ALL QUESTIONS IN THIS SECTION

### Requirement

Under “Unit Type Configuration”, render these fields for each unit configuration row:

* Unit Type:
* Beds / Unit:
* Baths / Unit:
* Number of Garage Spaces:
* Number of Carport Spaces:
* Number of Other Spaces:
* Number of Units:
* Number Occupied:
* Expected Rent:
* Unit Type Description:

### Replit Instructions

1. Identify how unit configurations are stored (must support multiple entries):

   * Ideally an array/JSON: `unit_type_configurations[]`
2. On show page:

   * Loop each configuration item in saved order
   * For each row, render the fields above in the exact label order listed.

### Suggested output format

* **Unit Configuration #1**

  * Unit Type: ___
  * Beds / Unit: ___
  * Baths / Unit: ___
  * Number of Garage Spaces: ___
  * Number of Carport Spaces: ___
  * Number of Other Spaces: ___
  * Number of Units: ___
  * Number Occupied: ___
  * Expected Rent: ___
  * Unit Type Description: ___

### SAFEGUARDS

* Preserve the saved array order (do not sort rows unless your create UI sorts them).
* Formatting:

  * Expected Rent must show commas and `$` if numeric.
* No “Not Provided” anywhere:

  * Missing values → hide that line OR show blank (match standard).
* If there are zero configurations saved:

  * Do not show the section at all (or show only the header with no rows—choose the Buyer standard; but still no placeholder text).

### Acceptance Criteria

All unit configuration questions display for each row.

---

# 6) SERVICES ORDER (INCOME) MUST MATCH CREATE ORDER EXACTLY

### Requirement

Services on Income show page must render in the exact sequence you provided, including:

* Correct headings (📢, 🛠️, 📸, 🏘️, 📉, 🧾, 💡, ✍️)
* Digital photo enhancement sub-options shown under “Select enhancement types: …”

### Replit Instructions

1. Stop rendering services by looping through “selected services” (this causes ordering problems).
2. Create/confirm a **canonical ordered array** for **Seller → Income services** (exactly the list you pasted).
3. On Income show page:

   * Loop through the canonical ordered list in order.
   * Render only items the user selected.
   * Render headings regardless (or render heading only if at least one child item in that section is selected—match your platform’s existing behavior).

### SAFEGUARDS

* Digital enhancements:

  * If “Provide digital photo enhancements” is selected, render immediately beneath it:

    * “Select enhancement types:” + the selected enhancement types in the exact order shown on your create UI
  * If enhancement “Other” text exists, display it.
* Prevent section drift:

  * Store canonical service lists in one shared config so create + show stay synced.

### Acceptance Criteria

Income services display exactly in your provided order and structure.

---

# 7) BROKER COMPENSATION: “Nominal Consideration Fee” PLACEMENT

### Requirement

Under **Broker Compensation & Agency Agreement Terms**, “Nominal Consideration Fee” must appear **after Seller’s Broker Purchase Fee**.

### Replit Instructions

1. In the Seller show page Broker Compensation section, locate:

   * Seller’s Broker Purchase Fee
2. Immediately below it, render:

   * **Nominal Consideration Fee: [value]**
3. Ensure it reads from the correct saved key.

### SAFEGUARDS

* Currency formatting with commas if numeric.
* No placeholder text if empty.

### Acceptance Criteria

Broker Comp order:

* Seller’s Broker Purchase Fee
* Nominal Consideration Fee

---

## ✅ FINAL QA CHECKLIST (PASS/FAIL)

Using [tenant@exp.com](mailto:tenant@exp.com) with an INCOME Seller listing:

* [ ] Bedrooms and Bathrooms do **not** appear anywhere
* [ ] Appliances Included appears **after** SqFt Heated Source
* [ ] After Pets, these show when filled: Included Assets, # Units, # Buildings, Annual Net Income, Cap Rate
* [ ] “Unit Type Configuration” appears as a main header like Sale Terms
* [ ] All unit configuration fields display for each row
* [ ] Services appear exactly in the Income order provided (including enhancement types)
* [ ] Nominal Consideration Fee appears immediately after Seller’s Broker Purchase Fee

---

If your dev wants this even safer: tell them to **reuse the same canonical service/order configs** that drive the create UI, so show pages can’t drift again.
