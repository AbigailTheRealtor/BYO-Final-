Task: Restore public contact information on Seller Offer Listing public view

Issue:
Task #903 incorrectly removed agent contact information from the public Seller Offer Listing view. This is an Offer Listing (not a Hire Agent listing), so the agent’s public-facing contact information should remain visible on the listing page.

STRICT SAFEGUARDS:
- Modify ONLY:
  resources/views/offer-listing/seller/view.blade.php
- Display-only fix.
- Do NOT modify controllers, routes, models, migrations, Livewire components, validation, save logic, database schema, or create/edit forms.
- Preserve all other Task #903 launch-readiness fixes.
- Do NOT touch Buyer, Tenant, Landlord, Hire Agent, or Limited Service files.
- Before editing:
  1. Run git status
- After editing:
  1. Run git diff
  2. Confirm ONLY resources/views/offer-listing/seller/view.blade.php changed

Required fix:

Restore the public-facing contact information fields in the Contact Information section:

- last_name
- email
- phone_number

These should display again on Seller Offer Listings because this is a public property/offer listing intended for contact and inquiries.

Keep:
- first_name
- brokerage
- license number
- NAR member ID

Update the $hasContact guard accordingly so the Contact Information card still renders properly when these fields are populated.

Do NOT:
- expose hidden/internal/admin-only fields
- change any saved data
- alter privacy logic anywhere else in the app
- touch Hire Agent flows

Done looks like:
- /offer-listing/seller/view/38 loads without errors.
- Agent first name + last name display again.
- Email displays again.
- Phone number displays again.
- Brokerage/license/NAR fields still display correctly.
- All other Task #903 fixes remain intact.
- git diff confirms ONLY:
  resources/views/offer-listing/seller/view.blade.php
  changed.

After completion provide:
1. Files changed.
2. Exact contact fields restored.
3. Updated $hasContact logic summary.
4. Live QA confirmation for listing #38.
5. Confirmation no controllers/routes/models/migrations/Livewire/database files were modified.