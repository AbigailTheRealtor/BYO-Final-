#989 Follow-Up Fixes — Clean Validation, Remove Duplicate Attributes, Preserve Visibility Rules

Please perform a surgical cleanup pass for Task #989 before merge.

IMPORTANT SAFEGUARDS

* Do NOT change routes, migrations, database schema, models, permissions, workflow architecture, or Limited Service flows.
* Do NOT remove first_name / last_name support.
* Do NOT remove synthesized legacy `client_name` compatibility.
* Do NOT alter counter-bid/session persistence logic.
* Do NOT refactor unrelated files.
* Keep this limited strictly to cleanup/stabilization.

Files in scope only:

* app/Http/Controllers/HireAgentDirectController.php
* resources/views/hire-agent-direct/client-details/buyer.blade.php
* resources/views/hire-agent-direct/client-details/seller.blade.php
* resources/views/hire-agent-direct/client-details/landlord.blade.php
* resources/views/hire-agent-direct/client-details/tenant.blade.php
* resources/views/hire-agent-direct/counter.blade.php
* resources/views/hire-agent-direct/submitted.blade.php
* resources/views/hire-agent-direct/preview.blade.php

1. Fix duplicated validation rules in BOTH acknowledgeSubmit() and counterSubmit()

Current arrays contain duplicated keys causing required rules to be overwritten.

Use this exact structure:

```php
$baseRules = [
    'client_first_name' => 'required|string|max:255',
    'client_last_name'  => 'required|string|max:255',
    'client_name'       => 'nullable|string|max:255',
    'client_phone'      => 'required|string|max:50',
    'client_email'      => 'required|email|max:255',
];
```

Keep synthesized `client_name` logic intact.

2. Fix malformed HTML in all 4 client-details blade files

Remove:

* duplicated `<input>` fragments
* duplicated placeholders
* duplicated class attributes

Current broken pattern example:

```html
<input type="text" name="client_phone"
<input type="text" name="client_phone" id="buyer_client_phone"
```

and:

```html
placeholder="Client's phone number"
placeholder="(555) 555-5555"
```

Each field must contain:

* ONE input tag
* ONE placeholder
* ONE class attribute

Do NOT alter layout/styling.

3. Restore role-specific Additional Terms placeholders

In:

* resources/views/hire-agent-direct/counter.blade.php

Remove ONLY this hard overwrite:

```php
$addlTermsPlaceholder = 'Enter any additional terms, notes, preferences, or special requests you would like the Agent to review.';
```

It currently kills the role/property-specific match() logic above it.

Keep the role-specific placeholders.

4. Remove duplicate raw + formatted outputs in submitted.blade.php

Currently rendering both:

* raw values
* formatted values

Example:

* `450000`
* `$450,000`

Only render formatted display values.

Applies to:

* desired_sale_price
* target_purchase_price
* estimated_down_payment
* desired_monthly_rent
* max_monthly_lease_price
* household_monthly_income
* formatted dates

Keep all formatter helpers and conditional logic.

5. Carefully audit preview visibility logic

In:

* resources/views/hire-agent-direct/preview.blade.php

Do NOT accidentally hide all compensation/agreement terms from public users unless that was already intentional product behavior.

Referral fee rows should remain hidden from non-agents.

If public proposal visibility existed before:

* preserve public visibility
* hide ONLY referral rows

Do not silently change marketplace/business-rule behavior.

6. QA REQUIRED

After fixes:

* test buyer/seller/landlord/tenant direct hire flows
* test counter flow
* test submitted formatting
* test logged-out preview
* test logged-in consumer preview
* test logged-in agent preview
* verify no malformed HTML remains
* verify validation errors work correctly
* verify first/last name persistence works end-to-end

Keep this a surgical stabilization pass only.
