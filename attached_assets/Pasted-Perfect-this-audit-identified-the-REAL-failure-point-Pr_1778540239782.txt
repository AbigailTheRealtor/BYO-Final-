Perfect — this audit identified the REAL failure point.

Proceed with ONLY the two surgical fixes identified below.

ROOT CAUSE CONFIRMED:
The Create components already create append-only draft versions correctly.
The failure is in the Edit components, which overwrite the existing row instead of creating a new version row.

Required Fixes Only
===================

FIX 1 — EDIT COMPONENT VERSIONING (PRIMARY FIX)

Update ONLY these files:
- SellerOfferListingEdit.php
- BuyerOfferListingEdit.php
- LandlordOfferListingEdit.php

Current bug:
saveDraft() updates the current listingId row instead of creating a new draft version row.

Required behavior:
Mirror the SAME append-only draft versioning pattern already used in the corresponding Create components.

Implementation requirements:
1. Save Draft in Edit mode must:
   - create a NEW draft row
   - increment draft_version
   - save parent_draft_id
   - save draft_payload_hash
   - preserve previous versions unchanged
   - redirect to the NEW edit/{id} route

2. No-change detection must remain:
   - if payload hash matches current draft, do NOT create a duplicate version row

3. Do NOT overwrite the existing row anymore.

4. Reuse existing versioning logic from the Create components wherever possible.

5. Do NOT modify:
   - Tenant components
   - Hire Agent files
   - Limited Service
   - routes
   - migrations
   - models
   - validation
   - unrelated controllers

===================

FIX 2 — CREATE BLADE MODAL VISIBILITY (SECONDARY FIX)

Update ONLY:
- offer-seller-listing.blade.php
- offer-buyer-listing.blade.php
- offer-landlord-listing.blade.php

Current bug:
@if ($hasDrafts && !$listingId)

This removes the draft modal after a draft is loaded.

Required fix:
Change to:
@if ($hasDrafts)

Then mirror the Tenant behavior:
- currently loaded draft shows green "Current" badge
- current draft becomes non-clickable
- other versions remain selectable

===================

DO NOT:
- add new architecture
- add migrations
- change DB schema
- touch Tenant
- touch Hire Agent
- touch Limited Service

Verification Required
=====================

After edits provide:

1. git diff --name-only

2. Proof each Edit component now:
   - creates NEW draft rows
   - increments draft_version
   - saves parent_draft_id
   - saves draft_payload_hash
   - does NOT overwrite old drafts

3. Manual test for Seller, Buyer, Landlord:
   - create v1
   - edit/save v2
   - edit/save v3
   - confirm all prior versions still exist
   - load v1 and confirm original values still exist
   - save without changes and confirm no duplicate created

4. Confirm Tenant unchanged.
5. Confirm Hire Agent unchanged.
6. Confirm Limited Service untouched.