Got it — add these **additional fields** to the same Buyer-only “no bubbles” rule.

Below are **updated copy/paste Replit instructions** (delta + final list), with safeguards.

---

# Replit Instructions — Hire a Buyer’s Agent

## Remove Bubble/Pill Styling for Additional Fields (Live Listing + Snapshot)

### Applies To

* Login: `tenant@exp.com`
* Flow: Dashboard → **Hire a Buyer’s Agent**
* Must apply to:

  * Live Listing Display
  * Download Listing Snapshot (must match)

---

## Issue

These fields are **still rendering in bubble/pill format** on the Buyer live listing view and must be **regular text**.

---

## Add These Fields to the “No Bubble” List

Update the Buyer listing display so these ALSO render as plain text:

1. **Condition of Exchange/Trade Item:** (e.g., Very Good)
2. **Liens / Encumbrances Disclosure:** (Yes/No)
3. **Inspection / Verification Rights:** (Yes/No)
4. **Maintenance / Repair Responsibility:** (confirm remains plain text everywhere it appears)

---

## Required Display Rule (Buyer Listing Only)

For the full “No Bubble” list, render as:

**Label:** `Value` (plain text)

✅ No badge / pill / bubble UI
✅ No extra wrapper classes that style it as a chip

Examples:

* Condition of Exchange/Trade Item: **Very Good**
* Liens / Encumbrances Disclosure: **Yes**
* Inspection / Verification Rights: **Yes**

---

## Implementation Instructions

### A) Live Listing Display

1. Locate the Buyer listing template/component that renders these fields (Purchasing Terms / Financing Details area).
2. Identify the badge/pill renderer (chip component / `<span class="badge">` / helper like `renderPill()`).
3. Expand the Buyer-only exception condition to include these field keys/labels.
4. Ensure **Maintenance / Repair Responsibility** is not being rendered in a different block using a separate pill helper—apply the same override in every place it appears.

### B) Snapshot Must Match

If snapshot uses separate rendering:

* Apply the exact same Buyer-only “no bubble fields” list in the snapshot template/formatter.

---

## Safeguards

1. **Display-only change** — do not modify stored values or DB schema.
2. Buyer-only: do not affect Seller/Landlord/Tenant styling.
3. Do not remove bubbles globally—only for this explicit list of fields.
4. Draft/Edit persistence must remain unchanged.
5. Snapshot output must match the live listing formatting.

---

## QA Checklist

Using `tenant@exp.com`:

* Submit a Buyer listing with values filled for these fields
  ✅ Live listing shows all of them as plain text (no bubbles)
  ✅ Snapshot shows the same plain text formatting

---

If you want this to stop happening permanently, implement a single rule: **bubbles only for multi-select arrays; plain text for single-select + boolean + free-text.** But the instructions above stick strictly to your specific Buyer fields.
