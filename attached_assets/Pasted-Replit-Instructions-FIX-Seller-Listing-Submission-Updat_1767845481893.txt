Replit Instructions — FIX Seller Listing Submission + Update Phone Placeholder
Applies to:

Hire a Seller’s Agent → Listing submission (all listing types)

Hire a Tenant’s Agent → Tenant Information tab → Phone placeholder text

PROFILES USED FOR TESTING (REFERENCE)

Tenant/User profile example: tenant@exp.com

Agent profile example: testagent2@bidyouroffer.com

PART A — FIX SUBMIT BLOCKER
Hire a Seller’s Agent → Listings only submit if Listing Type = Bidding Period
ISSUE

Hire a Seller’s Agent listings currently only submit when Listing Type = Bidding Period, even if all required fields are completed.

REQUIRED BEHAVIOR

Hire a Seller’s Agent listings must submit for ALL listing types, including:

Bidding Period

Traditional

Any other valid listing type

Listing Type should control only bidding/timer UI behavior, NOT whether submission is allowed.

REQUIRED FIX (DO THIS EXACTLY)
1) Remove “Bidding Period” as a hard requirement for submission

Find the form submit validation (controller/service/request validation OR Livewire component validation) for Hire a Seller’s Agent and remove any logic like:

listing_type == bidding_period required to submit

required_if: listing_type=bidding_period applied to non-bidding fields incorrectly

hidden timer/bidding fields still required even when Listing Type is not bidding

✅ Listing submission must validate only:

required Seller listing fields

required Broker Compensation fields

required Seller Info fields
…and must NOT require Bidding Period.

2) Fix hidden required fields that silently block submission

If bidding-related fields are hidden when Traditional is selected, they must NOT still be required.

✅ Update validation rules so bidding-only fields are:

required ONLY IF Listing Type = Bidding Period

optional (nullable) otherwise

Examples of what to fix:

timer end date/time

bidding duration

bid edit window fields

any “auction/bidding” configuration fields

TESTING (REQUIRED)

After fix, confirm:

✅ Listing submits when Listing Type = Traditional
✅ Listing submits when Listing Type = Bidding Period
✅ Timer/bidding fields still only show when Bidding Period is selected
✅ No other flows (Tenant/Buyer/Landlord) are impacted

PART B — UPDATE PHONE PLACEHOLDER
Hire a Tenant’s Agent → Tenant Information tab → Phone placeholder formatting
CHANGE REQUEST

On Hire a Tenant’s Agent → Tenant Information tab:
The phone placeholder currently shows:
(xxx)-XXX-XXXX

It must be changed to:
555-555-5555

✅ This is placeholder text only (no formatting change required here unless it already exists).

REQUIRED EDIT

Locate the phone input field for:
Hire a Tenant’s Agent → Tenant Information tab → Phone Number

Change placeholder from:

(xxx)-XXX-XXXX

To:

555-555-5555

Also confirm this does NOT change:

the field name/key

validation rules

formatting/mask logic (if any)

SAFEGUARDS (MUST FOLLOW — VERY IMPORTANT)

🚫 DO NOT:

rename or delete database columns

change field keys/names for phone fields

change other placeholder text

change UI layout/styling/icons

alter Buyer, Landlord, or other listing flows

remove or rewrite bidding logic (timers / counters / bid history)

✅ DO:

scope the submission fix to Hire a Seller’s Agent only

scope the placeholder edit to Hire a Tenant’s Agent → Tenant Info only

keep all changes minimal and non-destructive