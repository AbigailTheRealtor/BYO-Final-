Here you go — same instructions, but rewritten to apply to **Seller** (not Landlord). This is **display-only**, and it must match the **Tenant Services formatting** exactly.

---

## Replit Instructions — Seller “Services” Display Must Match Tenant Formatting (All Seller Property Types)

### Applies To

**ONLY: Hire a Seller’s Agent** for ALL Seller property types:

* Residential
* Income
* Commercial
* Business Opportunity
* Vacant Land

Wherever **Services** are shown:

* Public / actual listing display
* Agent bid view (including private “Terms” / modal/panel)
* Counter-bid view
* Accepted bid summary view (if Services appear there)

---

## Problem

Seller Services are currently displayed as a **flat list** (no section headers, no emojis, no bullet formatting).

Tenant Services already display correctly with:

* **Section Titles**
* **Emojis**
* **Bulleted items**
* **Clean spacing + indentation**

Seller Services must display **the exact same way as Tenant Services.**

---

## Required Outcome

Seller Services must render as:

1. **Group by Service Category** (do not flatten)
2. Show **Category Header** formatted exactly like Tenant:

   * **Emoji + Title**
3. Under each header, show services as **bullet points** exactly like Tenant:

   * same bullet style
   * same font sizes
   * same spacing/indentation
4. Same behavior for ALL Seller property types listed above

---

## Implementation Requirement (Display-Only)

* **DO NOT edit** the service list content, wording, categories, or ordering.
* **DO NOT migrate** or rewrite data.
* This is strictly a **frontend rendering/layout fix**.

Use the **same component / renderer / template** used for Tenant Services and apply it to Seller Services.

If Tenant uses something like:

* `renderServicesByCategory(servicesGrouped)`
  or a reusable component like:
* `<ServicesSectionedDisplay />`

Then Seller must use the same pattern, pulling from Seller’s existing Services dataset.

---

## Safeguards (Non-Negotiable)

### DO NOT:

* change any service titles, text, or bullet wording
* add/remove services
* reorder services
* rename categories
* change icons/emojis or section headings
* change any Tenant Services code or styling
* change database models, columns, or stored JSON

### DO:

* only change **how Seller Services are displayed**
* reuse the **Tenant Services formatting exactly**

---

## Acceptance Criteria (How We Confirm It’s Done)

✅ Seller Services show:

* **Emoji + Category Title**
* **Bulleted services under each category**
* **Same styling as Tenant Services**

✅ Works in:

* Seller listing display
* Agent bid view
* Counter-bid view

✅ Works for:

* Residential, Income, Commercial, Business Opportunity, Vacant Land

Provide screenshots:

1. Tenant Services (existing correct formatting)
2. Seller Services after fix (must match format)

---

If you want, I can also add one extra safeguard line: **“Do not touch the Services selection UI — this task is display-only on the listing/bid views.”**
