REPLIT INSTRUCTIONS — Fix Accepted Bid Summary Formatting (Currency/% + Services)

Applies ONLY to: BidYourAgent → Hire a Tenant listings (Residential + Commercial)

🚨 SAFEGUARDS (MANDATORY — DO NOT BREAK ANYTHING)

DO NOT:

delete/rename database columns, models, migrations, or routes

change bid/counter/accept logic

change how bids are entered, validated, or stored

change listing creation fields, tooltips, placeholders, or UI layout

change the Accepted Bid Summary structure/sections (only formatting + value rendering)

“recalculate” or “normalize” compensation values

replace values with defaults if missing

YOU MAY ONLY:

change how the Accepted Bid Summary renders values so it matches the accepted bid display

use the accepted bid’s stored formatted values (preferred)

update the services rendering to match the bid’s grouped category + bullet list format

add helper functions for formatting, without changing stored data

1) PROBLEM STATEMENT (WHAT’S WRONG NOW)
A) Broker Compensation values are losing formatting

In the Accepted Bid Summary, fields that should display $ or % (and other formatting) are showing as plain numbers (example: 34, 343, 3,434) instead of:

$3,434

3%

90 days

$2,323 + 3% of Total Purchase Price

etc.

B) Services formatting is not matching the bid display

The Accepted Bid Summary currently lists services incorrectly (flattened / missing bullets / not grouped like the bid UI).

2) REQUIRED OUTCOME (MATCH THE BID EXACTLY)
A) Compensation values must display exactly like the Accepted Bid

For every term in the Accepted Bid Summary:

Show exactly the same formatted output as shown in the bid UI at the moment it was accepted

Preserve:

dollar signs $

percent signs %

commas

decimals (if present)

combined fee formulas (e.g., $3,233 + 3% of Total Purchase Price)

units (e.g., days, months)

yes/no values

✅ Do NOT strip symbols
✅ Do NOT cast to number (no intval, floatval, number_format applied blindly)
✅ Do NOT store-only numbers if the bid stores formatted strings

Key rule: The Accepted Bid Summary should be a snapshot mirror of the accepted bid’s displayed terms.

3) IMPLEMENTATION — FIX BROKER COMP VALUE RENDERING
Step 1: Identify how the summary is currently built

Find the code that generates the “Broker Compensation & Agency Agreement Terms” block for the summary.
It is likely:

converting values to numbers

using an array of raw inputs rather than the bid’s final formatted strings

formatting only commas (but dropping $ and %)

Step 2: Use bid snapshot fields (preferred)

If the accepted bid already stores:

display strings (like “$8,000 + 2% of Gross Lease Value”)

selected dropdown labels (like “Percentage of Total Purchase Price”)

Then the summary must output those stored strings directly.

Step 3: If bid stores raw numbers + type fields, apply formatting rules

Only if required, apply conditional formatting based on the field’s “type”:

Examples:

If field is currency amount → prefix $ and apply commas

If field is percent → suffix %

If field is timeframe days → suffix days

If field is months → suffix months

If field is a formula part → combine exactly as bid UI does

✅ Important: This formatting logic must be identical to whatever is used in the bid UI.

4) REQUIRED FIELD-BY-FIELD FORMATTING (MINIMUM)

Ensure these display correctly in the summary:

Purchase Fee Details

Purchase Fee Type: show label text (not numeric)

Purchase Fee: show $ and/or % exactly like bid

Legal Terms

Protection Period Timeframe: show unit (example: 90 days)

Early Termination Fee Amount: show $ if it’s money

Retainer Fee Amount: show $

Tenant Agency Agreement Timeframe: show exact selected value (example: 9 Months)

Lease-Option Details

Compensation (When Option Is Created): show % if percent

Compensation (If Purchase Option Exercised): show $ if money

✅ Bottom line: If the bid UI shows $ or %, the summary must show $ or %.

5) IMPLEMENTATION — FIX SERVICES RENDERING (MATCH BID DISPLAY)
Required services output format in summary:

Show category headings

Under each category, show bulleted list of services selected in the accepted bid

Do NOT flatten categories into a single list

Do NOT show categories that have no selected services

Do NOT show example content

Do NOT show only 1 service per category unless that’s actually all that was selected

✅ The summary services section should visually match the bid UI:

Category title line

Bullet points underneath

Data source:

Use the accepted bid’s selected services structure (same one used to display the bid).

If the bid UI pulls from:

accepted_bid.services JSON

grouped arrays

service IDs mapped to labels

Then summary must reuse the same mapping to produce the same output.

6) OUTPUT RULES (DO NOT LOSE CONTENT)

If a service is selected in the accepted bid, it must appear in the summary

If a term is populated in the accepted bid, it must appear in the summary

If a term is blank, hide it (do not show empty placeholders)

7) ACCEPTANCE TESTS (MUST PASS)

Accepted bid shows:
Tenant’s Broker Commission Fee: $8,000 + 2% of Gross Lease Value
→ Summary shows EXACTLY the same string.

Accepted bid shows:
Compensation (When Option Is Created): 3%
→ Summary shows 3% (not 3).

Accepted bid shows:
Retainer Fee Amount: $900.00
→ Summary shows $900.00 (not 900).

Services in bid show grouped categories with bullets
→ Summary shows the same grouped categories with bullets.

8) SCOPE LIMITATION

This fix is ONLY for:

Hire a Tenant Accepted Bid Summary

Do NOT modify Buyer/Seller/Landlord summary logic in this task.

9) FINAL NOTE FOR REPLIT

The Accepted Bid Summary must be a mirror snapshot of the accepted bid display.
Do not “sanitize” or convert to raw numbers. Preserve symbols and formatting.