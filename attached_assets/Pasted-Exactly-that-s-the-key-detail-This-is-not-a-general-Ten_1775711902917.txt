Exactly — that’s the key detail.

This is **not a general Tenant counter issue**.
It is specifically:

👉 **Tenant listing → agent side → View Bid**
👉 when a counter exists
👉 the agent must be able to **see and click View Counter Terms**

Right now, that agent-side View Bid state is still wrong.

Use this exact prompt for Replit:

---

**TENANT BUG — AGENT SIDE “VIEW BID” MUST SHOW VIEW COUNTER TERMS**

This is a **Tenant-only bug fix**.

Do not widen scope.
Do not touch Buyer, Seller, or Landlord in this step.
Do not revisit Phase 1, 2, or 3 UI work.

Do not touch:

* listing creation
* bidding logic
* counter submission
* match score
* accepted summary
* routes
* controllers
* Livewire
* database

---

## EXACT PROBLEM

This bug is specifically on the **agent side**.

### Path:

**Tenant listing → agent logs in → clicks View Bid**

When a counter term exists for that bid, the agent should be able to:

✅ see the counter-state UI
✅ see **View Counter Terms**
✅ click into the counter terms

But right now on the agent side, inside Tenant View Bid, the agent still cannot properly view the counter terms because the page is still rendering incorrect response/action state instead.

---

## EXPECTED BEHAVIOR

Use **Seller agent-side View Bid** as the exact reference.

For **Tenant agent-side View Bid**:

### If no counter exists

normal action buttons may appear if intended.

### If counter exists

the agent-side View Bid page must show:

* counter notice
* **View Counter Terms**

and must **not** show:

* Accept
* Counter
* Reject
* Respond to this Counter Offer

anywhere in that countered agent-side View Bid state.

---

## REQUIRED TASK

Inspect only:

`resources/views/hire_tenant_agent/view.blade.php`

### Step 1

Find every render path that affects the **agent-side View Bid** state for Tenant when a counter exists.

This includes:

* modal footer
* inline action/footer sections
* counter history loop
* any legacy counter block
* any agent-side conditional branch

### Step 2

Verify which condition is supposed to allow the agent to see **View Counter Terms**.

### Step 3

Patch the Tenant agent-side rendering so that when a valid active Tenant counter term exists for that bid:

* the agent sees **View Counter Terms**
* all conflicting response-action blocks are suppressed

---

## IMPORTANT

Previous fixes adjusted queries and one or more action blocks, but the actual agent-side View Bid experience is still not correct.

So this must be verified from the **agent-side rendered result**, not just by checking query code.

---

## QA REQUIRED

Test specifically as the **agent viewing the bid**:

### Case 1 — No counter exists

* normal action buttons may appear if intended

### Case 2 — Counter exists

* counter notice appears
* **View Counter Terms** appears for the agent
* agent can access/view the counter terms
* no Accept / Counter / Reject block appears anywhere in that countered state
* no “Respond to this Counter Offer” block appears anywhere

### Case 3 — Seller unchanged

* Seller remains unchanged

---

## OUTPUT REQUIRED

Return:

1. exact file(s) changed
2. exact agent-side Tenant render branch(es) that were wrong
3. exact condition/boolean now used to allow **View Counter Terms** for the agent
4. confirmation that this was verified specifically on the **agent side**
5. confirmation no protected systems were touched

---

## FINAL RULE

Do not report success until you verify that an **agent** on Tenant View Bid can actually see and access **View Counter Terms** when a counter exists.

---

That prompt is much tighter because it makes them focus on:

* **agent-side only**
* **View Counter Terms visibility**
* **actual rendered outcome**, not just query cleanup

If they send back their result, paste it here and I’ll tell you whether it actually solves it.
