Paste this to Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A LANDLORD’S AGENT ONLY — ACTIVE FLOW BUG FIXES STILL NOT RESOLVED

### SCOPE

The following **Hire a Landlord’s Agent** bugs are **still not fixed** and must now be addressed in the **actual active flow** where they occur.

Fix **only** these issues:

1. **Submit button not working** for Hire a Landlord listings
2. **Leasing Space conditional display is broken**

   * when switching between **Entire Property** and **Single Room**
   * sometimes both question groups show at once
   * sometimes duplicated fields appear, such as **Tenant Pays** showing twice
   * **Included Storage Space** icon/label appears but the actual input box does not
3. This must be fixed for **Commercial** landlord listings and also verified so it does **not happen in Residential** landlord listings

Do **not** do unrelated edits.

---

# IMPORTANT — THESE WERE PREVIOUSLY REPORTED AS FIXED, BUT THEY ARE NOT FIXED

The prior pass did not resolve the active issue.

This means one of the following likely happened:

* the wrong file was edited
* only the create flow was edited, not the actual active path
* only one partial was edited while the real conditional rendering lives elsewhere
* a visual/icon fragment was fixed, but not the full field/input rendering logic
* the Submit button fix was applied to the wrong form or wrong submit method

So this time, do **not** assume.
First identify the **real active component, blade, partials, and submit method** used by the exact Landlord flow where this bug is happening.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Landlord’s Agent flow.**
2. **Do not edit Tenant, Buyer, or Seller flows.**
3. **Do not rename, delete, or migrate any database columns.**
4. **Do not change labels, placeholders, tooltips, or unrelated option values.**
5. **Do not refactor the whole component architecture.**
6. **Do not replace Livewire, Select2, or existing frontend systems.**
7. **Do not make broad cleanup changes.**
8. **If shared files must be touched, isolate the fix so it only affects Landlord flow.**
9. **Do not break drafts, edits, or old saved listings.**
10. **Do not claim fixed unless tested in the exact active Landlord flow.**

---

# STEP 1 — FIRST IDENTIFY THE EXACT ACTIVE FILES

Before changing anything, confirm the exact active files used by:

* Hire a Landlord **create**
* Hire a Landlord **draft reopen**
* Hire a Landlord **edit**

Also identify the exact files/locations for:

* the **Submit button**
* the **Leasing Space** field
* the **Single Room** conditional section
* the **Entire Property** conditional section
* the **Included Storage Space** field
* the **Tenant Pays** field(s)
* the exact Livewire submit method used by the final action

Do **not** assume the create and edit paths use the same files.

Do **not** assume the visible blade is the only source of the conditional logic.

---

# FIX 1 — SUBMIT BUTTON NOT WORKING FOR HIRE A LANDLORD LISTINGS

## Current issue

The **Submit** button for Hire a Landlord listings is still not working.

## Required behavior

When all required fields are complete, clicking **Submit** must:

* trigger the correct Livewire submit method
* validate correctly
* save correctly
* complete the submission flow correctly
* show visible validation feedback if something is missing

## Investigate the true cause

Check for issues such as:

* wrong `wire:click`
* wrong `type="submit"`
* button outside the active form
* wrong Livewire method name
* final tab button pointing to create logic while current flow uses edit logic
* JS intercepting/blocking submit
* hidden validation failure with no visible error
* required JS-controlled fields not syncing before submit
* duplicate buttons / hidden overlays / disabled state never clearing
* final tab conditional rendering pointing to the wrong action
* submit request firing but failing server-side
* create path fixed, but draft/edit/final submit path still broken

## Required fix

Fix the **actual** cause in the **actual active Landlord flow**.

Do not use a superficial workaround.

---

# FIX 2 — LEASING SPACE CONDITIONAL LOGIC IS BROKEN

## Current issues

When switching **Leasing Space** between **Entire Property** and **Single Room**:

* sometimes fields from **both** sections show at once
* sometimes **Tenant Pays** appears twice
* **Included Storage Space** icon/label appears, but the input does **not**
* the form is not properly limiting fields to the selected Leasing Space mode

## Required behavior

### If Leasing Space = Entire Property

Show **only** the fields relevant to **Entire Property**

### If Leasing Space = Single Room

Show **only** the fields relevant to **Single Room**

### Never show both section groups at once

### Never duplicate fields such as Tenant Pays

### If Included Storage Space is supposed to appear:

* the **full field** must appear
* not just the icon
* not just the label
* not a partial fragment

---

# COMMERCIAL + RESIDENTIAL REQUIREMENT

This must be fixed in **Commercial** Hire a Landlord listings, and then checked so the same issue does **not** happen in **Residential** Hire a Landlord listings.

That means:

## Commercial

* switch between Entire Property and Single Room
* only relevant fields show
* no duplicate Tenant Pays
* Included Storage Space renders fully when appropriate

## Residential

* switch between Entire Property and Single Room
* only relevant fields show
* no mixed sections
* no icon without input
* no duplicate fields

---

# ROOT-CAUSE DIRECTION

This likely means the conditional rendering is not mutually exclusive.

Possible causes include:

* separate partials rendering without strict `if / elseif` exclusivity
* overlapping blade conditions
* stale Livewire state after switching Leasing Space
* JS show/hide logic and Livewire conditions out of sync
* old conditional markup remaining visible after switching modes
* duplicate field blocks included in both Single Room and Entire Property sections
* Included Storage Space split across multiple conditional fragments, so the icon renders but the input does not

Fix the **underlying conditional rendering logic**, not just the symptoms.

The final result must be:

* one clean set of fields for **Single Room**
* one clean set of fields for **Entire Property**
* no overlap
* no duplication
* no orphaned icons/labels without actual inputs

---

# REQUIRED IMPLEMENTATION APPROACH

## 1) VERIFY THE REAL ACTIVE LANDLORD PATH FIRST

Before editing, confirm the exact active:

* Livewire component
* blade
* included partials
* submit method
* conditional partials/sections

for the Landlord flow where this bug actually occurs.

## 2) MAKE LEASING SPACE CONDITIONS MUTUALLY EXCLUSIVE

The conditional logic for:

* Entire Property
* Single Room

must be strictly exclusive.

Do not allow both blocks to render at the same time.

## 3) FIX INCLUDED STORAGE SPACE AS A COMPLETE FIELD

If Included Storage Space is relevant for the chosen Leasing Space mode, the entire field must render:

* icon
* label
* input/control

not just part of it.

## 4) REMOVE DUPLICATE FIELD RENDERING

If Tenant Pays or any similar field appears twice, find where both render paths are being included and correct it so only one valid instance shows.

## 5) FIX SUBMIT IN THE ACTUAL FINAL ACTION PATH

Make sure the final visible Submit button is connected to the real submit method for the exact Landlord flow being tested.

---

# QA TESTING REQUIRED

After fixing, test all of these in the **actual active Hire a Landlord flow**.

## Submit button

1. Open Hire a Landlord create flow
2. Complete required fields
3. Go to final tab
4. Click Submit
5. Confirm it submits successfully

## Draft / reopen

1. Save a Landlord draft
2. Reopen it
3. Go to final tab
4. Click Submit
5. Confirm it submits successfully

## Commercial — Leasing Space

1. Open/create a Commercial Landlord listing
2. Select **Entire Property**
3. Confirm only Entire Property questions show
4. Confirm Tenant Pays appears only once if relevant
5. Switch to **Single Room**
6. Confirm Entire Property questions fully disappear
7. Confirm only Single Room questions show
8. Confirm Included Storage Space shows as a full field when relevant
9. Save and reopen
10. Confirm correct section still shows with no duplicates

## Residential — Leasing Space

1. Open/create a Residential Landlord listing
2. Select **Entire Property**
3. Confirm only Entire Property questions show
4. Switch to **Single Room**
5. Confirm only Single Room questions show
6. Confirm no overlap
7. Confirm no icon without input
8. Confirm no duplicate fields
9. Save and reopen
10. Confirm correct section still shows

---

# WHAT TO REPORT BACK

Report back with:

1. the exact active create/draft/edit files for Hire a Landlord
2. the exact file and method containing the final Submit action
3. the exact cause of the Submit failure
4. the exact file(s) changed for Submit
5. the exact cause of the Leasing Space overlap/duplication bug
6. the exact file(s) changed for:

   * Leasing Space
   * Included Storage Space
   * Tenant Pays duplication
7. confirmation that Commercial and Residential were both tested
8. confirmation that Tenant, Buyer, and Seller flows were not changed

---

# FINAL REMINDER

This is a **surgical Hire a Landlord bug-fix pass only**.

Fix only:

* Submit button not working
* Leasing Space overlap between Entire Property and Single Room
* duplicate fields like Tenant Pays
* Included Storage Space icon showing without input

Do not do unrelated refactors or cleanup.

---
