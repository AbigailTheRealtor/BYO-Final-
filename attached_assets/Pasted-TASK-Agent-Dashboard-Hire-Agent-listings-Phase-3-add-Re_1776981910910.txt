TASK: Agent Dashboard Hire Agent listings — Phase 3 add Referral & Cooperation Terms to listing/detail display

GOAL
Add a new display section to the actual listing/detail pages for Hire Agent listings created from the agent dashboard:

Referral & Cooperation Terms

and display:

Referral Fee: X%

This phase should only handle display/rendering and safe formatting for the Referral section on listing/detail pages.
Do NOT alter unrelated logic.

CRITICAL SCOPE
Apply this to the Hire Agent listing/detail pages where Broker Compensation & Agency Agreement Terms is already shown.

This should work for agent-dashboard-created Hire Agent listings and should not break public/consumer listings.
If the same detail blade is shared, use conditional display based on whether a referral fee value exists.

HARD SAFEGUARDS
1. Read-only audit first.
2. Identify:
   - which detail/view blades render Broker Compensation & Agency Agreement Terms
   - the exact field/meta key used for Referral Fee
   - whether Referral Fee is already saved in the database/meta for these listings
   - whether any existing controller normalization is needed before display
3. Do NOT change save logic unless absolutely required to display already-saved values.
4. Do NOT globally append % to raw stored values in the database.
5. Do NOT show duplicate percent signs.
6. If a value is missing, do not render a broken empty section.

REQUIRED DISPLAY CHANGE

On each applicable Hire Agent listing/detail page:

After:
Broker Compensation & Agency Agreement Terms

add:
Referral & Cooperation Terms

Inside that section display:
Referral Fee: X%

FORMATTING RULES
- If the stored referral fee is numeric like:
  25
  display:
  25%
- If the stored value already includes % like:
  25%
  display:
  25%
- If the stored value is blank/null, either:
  - hide the Referral Fee row, or
  - hide the section entirely if there is no referral/cooperation data
Choose the cleanest existing UI pattern already used in the app.

IMPORTANT
- Do not append % twice
- Do not mutate stored data
- Prefer display formatting logic in the blade or view helper only

IMPLEMENTATION RULES
- Use the narrowest possible change
- Do not refactor unrelated detail pages
- Do not alter validation, draft save, submit, services, timers, bids, accepted summaries, or referral payout logic
- If multiple listing types use different detail blades, update each one carefully
- Keep styling and card/section format consistent with the existing detail page layout

REQUIRED AUDIT OUTPUT BEFORE CHANGES
Report:
1. every detail/view blade that needs this section
2. exact variable/meta key used for Referral Fee in each flow
3. whether the data is already available in the blade or needs passing from the controller
4. how you will safely format percent display without double-appending

MANDATORY VERIFICATION
Test applicable Hire Agent detail pages and confirm:
A. Broker Compensation & Agency Agreement Terms still displays correctly
B. Referral & Cooperation Terms now appears directly after it
C. Referral Fee displays with a single %
D. No blank/broken section appears when no referral fee exists
E. Public/consumer listings are not broken

DELIVERABLE FORMAT
Return:
1. audit findings
2. files changed
3. exact display logic used
4. how percent formatting was handled safely
5. manual verification results