Audit first, then surgically fix ONLY the Hire a Landlord’s Agent flow.

Route:
 /hire/agent/auction/landlord

Scope:
ONLY touch files used by the Hire Landlord Agent listing flow, likely:
- TenantAgentAuction.php shared Livewire component
- hire landlord blade/partials
- broker-compensation partial used by Hire Agent landlord flow
- landlord property/leasing terms partials used by this route

Do NOT touch:
- Create Offer Listing flows
- Seller/Buyer/Tenant Hire Agent flows unless the file is shared and the fix is safely conditional for landlord
- Limited Service / legacy flows
- database schema
- routes
- unrelated validation
- unrelated tabs

Audit first:
Identify exact root causes before editing.

Bugs to fix:

1. Missing “Select” placeholder text on these fields:
- Appliances Included
- View
- Amenities and Property Features
- Desired Lease Term
- Tenant Pays
- Terms of Lease

Expected:
Each select/multiselect shows placeholder “Select” before selection.
For Select2 fields, confirm both:
- blank leading option if single select: <option value="">Select</option>
- JS init includes placeholder: "Select" and allowClear: true
Do not change option values, labels, wire:model names, or saved keys.

2. Lease Renewal/Extension Fee select is stuck on “Select”
Current behavior:
User cannot select an option; it returns to placeholder.

Expected:
User can select and persist a Lease Renewal/Extension Fee option.

Audit likely causes:
- Select2 change handler not syncing to Livewire
- wrong wire:model name
- duplicated Select2 initialization
- missing delegated change handler after Livewire re-render
- mismatch between blade field ID/name and Livewire property

Fix:
Add or correct the Select2 → Livewire bridge for this exact field.
Ensure it works after:
- initial load
- tab switch
- Livewire message.processed
- Save Draft / reload if applicable

3. Lease-Option compensation numeric inputs erase typed values:
When entering numbers for:
- Compensation for Creating the Lease-Option Agreement
- Compensation if Purchase Option is Exercised

Current behavior:
Typed $ or % values disappear/delete while typing.

Expected:
Both compensation inputs allow:
- typing numbers
- backspacing
- clearing
- retyping
- blur/tab without disappearing
- saving draft without losing value

Audit likely causes:
- value inputs using wire:model.lazy while unrelated Livewire updates fire
- paired type select has both wire:model and wire:change causing duplicate requests
- setType() clears the value on every re-render/type sync
- incorrect Livewire property names between lease/purchase type and value

Fix guidance:
- Use wire:model.defer for the compensation value inputs.
- Remove duplicate binding from the paired type select if it has both wire:model and wire:change.
- Keep one reliable sync path:
  either wire:change="setType(...)" OR wire:model with updated hook, not both.
- Ensure setType() only clears the value when the type actually changes, not on every render or repeated same-value selection.
- Apply same logic to both:
  lease-option creation compensation
  purchase-option exercised compensation

Verification:
After fixing, test:

A. Load:
 /hire/agent/auction/landlord

B. Confirm placeholder “Select” appears on:
- Appliances Included
- View
- Amenities and Property Features
- Desired Lease Term
- Tenant Pays
- Terms of Lease

C. Lease Renewal/Extension Fee:
- select each available option
- confirm selected option stays visible
- switch tabs and return
- confirm it does not reset to Select

D. Lease-Option:
- select Yes for lease-option agreement if needed
- enter dollar amount
- enter percentage amount
- backspace
- clear
- retype
- tab/blur
- confirm values do not disappear

E. Save Draft:
- save draft
- reload/resume draft
- confirm Lease Renewal/Extension Fee and both lease-option compensation fields persist

Deliverable:
Report:
1. Root cause summary
2. Exact files changed
3. Exact code areas fixed
4. Verification results
5. Confirmation that no Offer Listing flows, no Tenant/Seller/Buyer Hire Agent flows, no Limited Service flows, no routes, and no database/schema files were changed