Required additions before approval:

1. Avoid hardcoded tab indexes where possible
The task currently says:
"Re-index any hardcoded tab numbers affected by the insertion."

Instead, prefer dynamic tab targeting/selectors wherever possible to avoid future breakage when Buyer/Landlord/Tenant tabs are added later.

Especially audit:
- correction mode
- final Submit/Finish detection
- wizard next/back logic
- mobile wizard navigation

2. Explicitly preserve existing draft meta
Add:
"Do not overwrite or reset compatibility_preferences when loading older drafts that do not yet contain the key."

Need backward compatibility for existing drafts/listings.

3. Add explicit Select2 rehydration requirement
Add:
"All Select2 single-select and multi-select fields inside the new tab must correctly rehydrate after:
- draft load
- edit load
- tab switching
- Livewire re-render"

4. Add icon rendering requirement
Since your platform has had repeated icon/render issues:
"All fields inside the new tab must preserve existing icon behavior and must not create duplicate or disappearing icons after Livewire updates."

5. Add standardized placeholder requirement
Add:
"All placeholders must follow the platform format:
Enter [Field Title] (e.g., Example)"

6. Add explicit no-regression protection for existing Hire Seller flows
Add:
"Do not alter:
- bidding logic
- broker compensation logic
- services selection logic
- save draft behavior
- review formatting outside the new section
- Accept/Counter/Reject flow"

7. Add future-reuse architecture requirement
Very important for scaling Buyer/Landlord/Tenant later:
"Structure the compatibility tab partial and data shape so future Buyer/Landlord/Tenant versions can reuse the same rendering pattern and storage architecture."

8. Add QA for older listings
Need this:
"Verify older Seller drafts/listings without compatibility_preferences still open and save normally without errors."

After those additions, approve it.