Replit Instructions — Tenant@exp.com
Hire a Buyer’s Agent (All Property Types)
Tab 3: Purchasing Terms → Offered Financing/Currency → Assumable
✅ Placeholder Comma Fix Only (Down Payment Bridge the Gap)
Applies To

Tenant@exp.com
 → Hire a Buyer’s Agent
Property Types: All (Residential, Income, Commercial, Business Opportunity, Vacant Land)
Tab: Purchasing Terms (Tab 3)
Section: Offered Financing/Currency → when Assumable is selected
Field: Down Payment Buyer Can Afford to Bridge the Gap (Placeholder)

✅ Change Required (Placeholder Text Only)
Location

Hire a Buyer’s Agent
→ Tab: Purchasing Terms
→ Offered Financing/Currency
→ Select Assumable
→ Field: Down Payment Buyer Can Afford to Bridge the Gap
→ Placeholder text

Edit

In the existing placeholder text, replace only the example number:

Replace: 50,000

With: 50000

⚠️ Do NOT change any other wording, symbols, spacing, punctuation, or formatting.

Example (Formatting Fix Only)

Before:
Enter down payment amount to bridge gap (e.g., 50,000)

After:
Enter down payment amount to bridge gap (e.g., 50000)

🛡️ SAFEGUARDS (MUST FOLLOW — DO NOT VIOLATE)

Placeholder text edit only. Do NOT:

change field label, tooltip, help icon, or section headings

change input type, validation rules, required rules, or conditional logic

change currency formatting behavior (auto-formatting after user entry may remain)

change any other Assumable follow-up fields

modify database schema, migrations, models, or stored values

affect Seller, Tenant, or Landlord flows

change UI layout, spacing, styling, or formatting

refactor shared placeholder components unless scoped only to this field in Buyer → Assumable

If the placeholder is shared across multiple flows:

scope the change to Buyer → Assumable → Down Payment Bridge the Gap only

do not globally overwrite placeholders elsewhere

✅ Expected Outcome

Placeholder example shows 50000 (no comma)

User can still input numbers normally

Any existing numeric auto-formatting (if present after typing) remains unchanged

No impact to saving drafts, editing, countering, or submitting listings

🧪 Testing Required Before Completion

Login as Tenant@exp.com

Create Hire a Buyer’s Agent listing (test at least 2 property types)

Go to Tab 3: Purchasing Terms

Select Assumable

Confirm placeholder shows (e.g., 50000) with no comma

Enter 50000 and confirm field behavior works normally

Save draft → reload → confirm placeholder still correct

Submit listing → confirm no validation or console errors

⚠️ CRITICAL — WHERE THIS MUST UPDATE

This updated placeholder must render consistently everywhere this exact field is displayed, including:

Listing creation UI

Saved listing edit view

Draft preview / review screens

Agent-facing listing details (read-only) (if placeholders appear there)

❌ Do NOT mass-update database rows.
✅ Placeholder is display-layer only.