eplit Instructions — Match Buyer “Acceptable Brokerage Relationship” Bullet Format
Applies To

Hire a Seller’s Agent / Hire a Landlord’s Agent / Hire a Tenant’s Agent
Tab/Section: Broker Compensation & Agency Agreement Terms
Field: Acceptable Brokerage Relationship

Goal

When the user selects any of the 4 brokerage relationship types, the description panel beneath the dropdown must display in the exact same format as Buyer, including:

Heading line (same styling)

Bullet points for each sentence (one bullet per sentence)

Legal Notice box (same layout and styling)

Brokerage Relationship Types (Must Match Buyer)

Transaction Broker Representation

Single Agent Representation

Dual Agency Representation

No Brokerage Relationship

SAFEGUARDS (Must Follow)

Do NOT change dropdown option values, labels, order, or required status.

Do NOT change any existing role logic, field behavior, database columns, Livewire properties, or validation rules.

Do NOT modify the Buyer version. Buyer is the source of truth.

This is a formatting/display change only for Seller/Landlord/Tenant.

Do NOT remove the Legal Notice—only ensure it matches Buyer’s formatting and still displays.

Step 1 — Identify Buyer “Source of Truth” Rendering

Locate the Buyer implementation of the Acceptable Brokerage Relationship detail panel:

Find the Blade/Livewire view or shared component that renders the description block beneath the dropdown.

Confirm how Buyer outputs:

Title/heading line

Bullet list (UL/LI)

Legal Notice box

✅ Buyer’s markup/classes are the exact pattern to reuse.

Step 2 — Reuse Buyer Rendering for Seller/Landlord/Tenant
Requirement

Seller/Landlord/Tenant must use the same Blade partial/component as Buyer for rendering the selected relationship description.

Preferred implementation (most reliable):

Extract Buyer’s relationship description panel into a shared component/partial (if it isn’t already), e.g.:

components/brokerage-relationship-details.blade.php

Then include that same component in Seller/Landlord/Tenant.

Safeguard:

Only pass in the role-specific text variables (if the role differs), but keep the exact same HTML structure and CSS classes as Buyer.

Step 3 — Bullet Point Format Rule (One Bullet Per Sentence)

For Seller/Landlord/Tenant, each relationship type’s description must render as:

A single heading line: “Brokerage Relationship:” (or the same Buyer heading label)

Then a UL where:

each sentence is its own LI

punctuation stays intact

no combined multi-sentence bullets

Example format (must match Buyer behavior):

• Sentence 1.

• Sentence 2.

• Sentence 3.

✅ Even if the text comes from a stored string, it must be split into separate bullets exactly like Buyer.

Step 4 — Ensure the Same Legal Notice Box Displays

Under the bullets, the yellow Legal Notice box must match Buyer exactly:

Same icon

Same background color

Same padding/margins

Same wording (role references may differ only if they already differ by role)

Safeguard:

Do not remove or rewrite the notice text—only match Buyer’s layout/formatting.

Step 5 — Conditional Display (Must Match Buyer)

The description panel should behave exactly as Buyer:

If user selects Select (placeholder) → show no bullets panel (or show the same default behavior as Buyer)

If user selects one of the 4 types → show the bullets + legal notice

QA / Acceptance Checklist (Replit Must Verify)

Test Seller + Landlord + Tenant:

Select each of the 4 relationship types

Confirm the description panel shows:

heading line

bullet list with one sentence per bullet

legal notice box

Confirm styling visually matches Buyer (spacing, fonts, colors, alignment)

Confirm switching between options updates correctly

Confirm save draft / reload / submit are unaffected