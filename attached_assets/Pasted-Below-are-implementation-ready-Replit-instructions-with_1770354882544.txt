Below are **implementation-ready Replit instructions with safeguards** for **Hire a Landlord → Actual Listing page** (what users see when they click a draft/listing). These fixes apply to **Residential + Commercial**, with the specific issues you listed.

---

# Replit Instructions — Hire a Landlord Actual Listing Display Fixes

### Login / Repro

* Login: **[tenant@exp.com](mailto:tenant@exp.com)**
* Flow: **Hire a Landlord’s Agent**
* Action: **Open a saved draft / listing → view the actual listing page**

### Applies To

* **Hire a Landlord listings only**
* **Residential + Commercial**, unless specified

### Safeguards (Must Follow)

1. **Landlord flow only** — do not change Tenant/Buyer/Seller listing views.
2. **No DB schema renames/drops.**
3. Only modify:

   * Landlord listing show controller/route OR landlord listing Livewire “show” component
   * Landlord listing view template(s)
   * Landlord-specific formatting helpers (if any)
4. Preserve existing styling (icons, headings, bullet layout). Only fix data + order + missing sections.

---

## 1) Location visibility rules (City/County/State/Zip shown; address hidden)

### Requirement

On the **actual landlord listing page** (Residential + Commercial):

* ✅ Show: **City, County, State, ZIP**
* ❌ Hide: **full street address** (and unit # if it reveals address)
* ✅ If you have “Address visibility after hire” logic, landlord listing must follow it.

### Implementation

1. Locate the landlord listing “show” view:

   * search: `Hire a Landlord` + `show` + `listing` + `address`
2. Replace address output with a **safe location block**:

   * `City, County, State ZIP`
3. Ensure address is not printed anywhere else (top summary card, sidebar, map link, schema tags, etc.).

**Safeguard:** Do not remove address from DB; display only.

---

## 2) Services on actual listing must match user selection AND preserve original creation order

### Current problem

Your actual listing is:

* missing items the user selected
* sometimes includes fewer/incorrect items
* order is different than creation UI
* sometimes seems to “group” but not preserve within-group order

### Required behavior

For both **Residential + Commercial**:

1. Only show categories that have at least 1 selected service.
2. Within each category, show services in the **same order as the service checklist appears in the create/edit form**.
3. Show **only** services the user selected (no auto-added, no omitted).

### Implementation (must follow this exact strategy)

#### A) Create a Landlord-only “service definition map” in one place

Find where your service lists are defined (often an array used by the form). If not centralized, create a landlord-only config/constant:

* Residential landlord service categories + items (in correct order)
* Commercial landlord service categories + items (in correct order)

Each item must have a stable key/id.

Example structure (conceptual):

* `services_landlord_residential = [ category_key => [service_key1, service_key2, ...] ]`
* `services_landlord_commercial = [ ... ]`

**Safeguard:** Do not change text here unless it matches the create UI exactly.

#### B) Ensure selected services are stored as stable keys (not just display text)

In landlord listing record, confirm the stored value is either:

* array of `service_keys`, OR
* associative array by category, OR
* JSON with keys

If it’s storing raw text, do not migrate—just map text → key for display (landlord-only).

#### C) Render services by iterating the definition map and filtering by selected set

On the actual listing page:

1. Load selected services into a set (`selectedKeys`)
2. For each category in the definition map (in order):

   * create `visibleItems = definitionItems.filter(itemKey in selectedKeys)`
   * if visibleItems not empty → render category header + visibleItems in order

✅ This guarantees **correct order** and **only selected items**.

**Safeguard:** Do not “sort alphabetically.” Do not “sort by category count.” Use definition order only.

---

## 3) Residential — Broker Compensation display: Payment Timing missing number value

### Issue

Residential actual listing shows:

* “Paid Within Calendar Days After Executed Lease”
  but **missing the number** (e.g., 3, 5, 10)

### Requirement

Display must be:

* “Paid Within **{X}** Calendar Days After Executed Lease”

### Implementation

1. Locate the field(s) that store this:

   * commonly there is a dropdown choice AND a numeric input
   * e.g., `payment_timing_type` + `payment_timing_days`
2. On listing view:

   * if timing type is “Paid Within Calendar Days…” then append the days value
3. If your DB stores it as a combined string, ensure parsing doesn’t drop the number.

**Safeguard:** Do not hardcode a number. Render the value user entered/saved.

---

## 4) Commercial — Leasing Space “Entire Property” conditional sections missing

### Issue

When user selects **Commercial → Leasing Space = Entire Property**, these are missing on the actual listing:

* Space Features
* Neighboring Tenants Include
  (you noted they’re all missing)

### Requirement

If Commercial AND Leasing Space = Entire Property:

* show those fields exactly as entered

### Implementation

1. Identify the commercial show template sections for leasing space.
2. Confirm the condition is correct:

   * currently it may be checking the wrong value (e.g., “Entire Building” vs “Entire Property”)
   * or checking a boolean that never loads
3. Ensure the values are being pulled from the listing model and not from Livewire state that isn’t set on show.

**Safeguard:** Don’t change the user’s saved value; fix the condition / rendering.

---

## 5) Commercial — “Other” should never show as literal “Other” on actual listing

### Issues

* Tenant Pays & Owner Pays show “Other”
* Terms of Lease shows “Other”
  …but they should display the **custom text input** user entered.

### Required behavior (Commercial only)

If a field contains “Other” selected:

* actual listing must display the **Other text value**
* and should NOT print the word “Other” as the displayed value

### Implementation

For each field:

* `tenant_pays`, `owner_pays`, `terms_of_lease`
  Check:

1. If stored selection includes `Other`
2. If `*_other` text is non-empty
3. Replace “Other” display with the other text

**Display rules**

* If selection includes both standard items + Other text:

  * show standard items
  * then show the other text as its own line item
* If selection is only Other:

  * show just the other text

**Safeguard:** Don’t change how it saves; only fix show rendering.

---

## 6) Commercial — Lease Renewal/Extension Fee: missing “Sales Tax” display

### Issue

Sales Tax is missing on listing for applicable fields.

### Requirement

If the user selected a Lease Renewal/Extension option where Sales Tax applies:

* show the Sales Tax field/value on actual listing.

### Implementation

1. Identify the saved fields related to renewal fee and tax:

   * e.g., `lease_renewal_fee_type`, `lease_renewal_fee_amount`, `lease_renewal_sales_tax` or `sales_tax_percent`
2. In the commercial broker compensation display block:

   * if the field exists and has a value → render it
   * ensure it is not being filtered out by “empty check” logic or wrong field name

**Safeguard:** Only show Sales Tax if user entered/selected it or if the selected fee option requires it.

---

## 7) “This field should show for applicable fields”

You wrote: “This field should show for applicable fields” — this typically means the show template is missing conditional sections (like a “tax” subsection or “space features” subsection).

### Implementation safeguard pattern

For each conditional field group, implement:

* Condition based on:

  * property type (Residential vs Commercial)
  * leasing space selection
  * “Other” chosen
  * fee type chosen

Then render the stored values.

**Safeguard:** Do not make these fields globally visible. Only show when conditions match the create form logic.

---

# Acceptance Tests (must pass)

Using **[tenant@exp.com](mailto:tenant@exp.com)**:

### Address + Location

* ✅ City/County/State/ZIP show
* ✅ Street address is hidden everywhere on listing

### Residential Services

* ✅ Services show only what was selected
* ✅ Services order matches creation form order exactly
* ✅ No missing service items

### Commercial Services

* ✅ Same as above (selection-only + preserved order)

### Residential Broker Compensation

* ✅ Payment Timing includes the user-entered number

### Commercial Leasing Space

* ✅ Entire Property shows Space Features + Neighboring Tenants Include

### Commercial “Other” Fields

* ✅ Tenant Pays/Owner Pays/Terms of Lease display the user’s Other text, not the word “Other”

### Commercial Lease Renewal/Extension

* ✅ Sales Tax appears when applicable

---

# Copy/Paste Message to Replit (Use this exact text)

> “Implement Hire a Landlord **actual listing display** fixes ([tenant@exp.com](mailto:tenant@exp.com)). On landlord listing show page (Residential+Commercial), show City/County/State/ZIP but hide full street address. Services section must render **only selected items** and in the **exact same order as the create form** (both Residential and Commercial). Do not reorder or alphabetize—use the service definition order from the create checklist, and filter by selected keys.
> Residential broker comp: Payment Timing ‘Paid Within __ Calendar Days After Executed Lease’ is missing the number—display the saved days value.
> Commercial: if Leasing Space = Entire Property, show Space Features + Neighboring Tenants Include (currently missing). Commercial Tenant Pays/Owner Pays/Terms of Lease must show the saved ‘Other’ text instead of the literal word ‘Other’. Commercial Lease Renewal/Extension Fee section is missing Sales Tax—show it for applicable selections/values.
> Landlord flow only; no changes to tenant/buyer/seller. Provide list of changed files + screenshots proving the fixes.”

---

If you want this to land perfectly with zero guessing by them: paste **a screenshot of the Hire a Landlord “create services checklist” order** (Residential + Commercial), or tell me if your services are stored as **service IDs/keys** or **raw text**. Then I can give Replit the exact mapping strategy (keys vs text) they must use.
