Task #723 is not ready yet. The diff still contains broken/duplicate logic from the old implementation mixed with the new implementation.

Please clean up the Seller blade before finalizing.

Issues still present:

1. Duplicate labels / malformed markup
The diff shows both:
<label class="fw-bold">Seller Disclosure Available:
and
<label class="fw-bold">Property Documents & Disclosures:

Only the new label should remain.

2. Old implementation still partially exists
The diff still contains:
- seller_disclosure_available
- survey_available
- inspection_report_available
- hoa_condo_docs_available
- flood_disclosure_available
- lead_based_paint_disclosure
- environmental_report_available
and their upload blocks.

These old Yes/No disclosure sections must be completely removed from the Seller Create blade.

3. Duplicate Alpine state definitions
The x-data block currently contains duplicate properties:
- rows defined twice
- typeOptions defined twice
- duplicate Livewire listeners
- duplicate push logic in addRow()
- duplicate file_path assignments

The Alpine object must be cleaned so each property/method exists only once.

4. Mixed old/new option arrays
Both:
$docTypeOptions
and
$sellerDocTypeOptions
appear in the same file.

Only one final clean array should remain:
$sellerDocTypeOptions

5. Incorrect old “Other (custom)” logic still present
The new implementation should consistently use:
type === 'Other'
and:
custom_type

Remove all remaining:
- Other (custom)
- row.label
legacy logic.

6. Duplicate row structure remnants
The diff still shows:
- old p-2 wrapper
- new p-3 wrapper
- old Add Document label
- new + Add Another Document label

Only the new Landlord-style structure should remain.

7. Final structure requirement
The Seller blade should look architecturally similar to the Landlord repeatable-row implementation:
- one clean repeatable section
- one clean x-data object
- one clean options array
- one row template
- one upload flow
- one add-row button

No merged old/new code should remain.

Done looks like:
- Old disclosure dropdown system fully removed
- Only repeatable-row system remains
- No duplicate Alpine keys/methods
- No duplicate arrays
- No “Other (custom)” remnants
- “Other” is final dropdown option
- “+ Add Another Document” button remains
- Only Seller Create documents-disclosures blade modified