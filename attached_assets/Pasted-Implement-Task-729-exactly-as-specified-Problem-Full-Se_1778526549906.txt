Implement Task #729 exactly as specified.

Problem:
Full Service Seller Create Listing is missing Listing Details icons on first page load, while Buyer Create works correctly. HTML markup is already confirmed identical. This is a JS lifecycle/timing issue.

Use the pre-audited findings below and implement the smallest surgical Seller-only fix necessary.

PRE-AUDITED FINDINGS:
- Seller and Buyer Listing Details field markup are already identical.
- addIconsToInputs() function bodies are already identical.
- Seller is missing addIconsToInputs() inside the draftLoaded lifecycle flow.
- Buyer explicitly re-runs addIconsToInputs() during draftLoaded and with a deferred timeout.
- Seller uses more fragile timing around Livewire message.processed and DOMContentLoaded.

PRIMARY FILE:
- resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php

READ-ONLY COMPARISON:
- resources/views/livewire/offer-listing/buyer/offer-buyer-listing.blade.php

TASKS:
1. Audit initializeFullService() Seller vs Buyer.
2. Determine whether Seller initializeFullService() triggers a Livewire re-render that wipes injected icons after initial render.
3. Add addIconsToInputs() into Seller’s draftLoaded handler to mirror Buyer.
4. Add the same deferred setTimeout(addIconsToInputs, 500) pattern Buyer uses if needed.
5. Tighten/fix Seller DOMContentLoaded timing ONLY if necessary.
6. Ensure at least one icon initialization occurs AFTER any Livewire re-render completes.
7. Keep the fix Seller-only and surgical.

DO NOT TOUCH:
- Limited Service / initializeLimitedService()
- Landlord or Tenant flows
- Buyer logic except read-only comparison
- database/schema/migrations/routes/controllers
- validation/save/draft logic
- form fields
- placeholders
- labels
- wire:model names
- tabs
- CSS rewrites
- global icon rewrites

DONE LOOKS LIKE:
- Seller Create Listing Details icons appear immediately on first fresh page load with zero interaction.
- listing_title icon works
- listing_date icon works
- expiration_date icon works
- listing_type/auction_type icon works
- auction_time icon works
- Buyer still behaves identically
- No Limited Service changes
- No Landlord/Tenant changes

After implementation provide:
- exact files changed
- exact lifecycle issue found
- exact fix applied
- confirmation no other flows were modified