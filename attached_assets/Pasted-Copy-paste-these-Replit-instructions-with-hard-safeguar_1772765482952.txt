Copy/paste these **Replit instructions with hard safeguards**. They are written to fix only the **Hire a Tenant’s Agent** flow and to stop Replit from wandering.

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — Hire a Tenant’s Agent listing issues

### HARD SAFEGUARDS (NON-NEGOTIABLE)

1. Apply changes **ONLY** to the **Hire a Tenant’s Agent** shared Livewire flow.
2. **First check which route I’m on**:

   * If URL contains **`/auction/edit/`** → apply fixes in **`TenantAgentAuctionEdit.php`** and **`tenant-agent-auction-edit.blade.php`**
   * If URL contains **`/auction/`** without `/edit/` → apply fixes in **`TenantAgentAuction.php`** and its blade
3. **Do NOT touch legacy tenant files** outside these shared Livewire routes.
4. **Do NOT rename/delete DB columns, tables, or migrations.**
5. **Do NOT change labels, tooltips, or option values.**
6. Keep changes surgical:

   * only fix **save/load/rehydration**, **required field submit feedback**, **photo/video persistence**, and **embedded video display**
7. Do not refactor unrelated fields or other roles.

---

# ISSUES TO FIX

## A) Acceptable Property Conditions is not saving in drafts / not auto-populating in edits

## B) View Preference Needed is not saving in drafts / not auto-populating in edits

## C) Leasing Space is not saving in drafts / not auto-populating in edits

## D) View Preference and Leasing Space select briefly, then delete themselves

## E) Listing currently cannot submit because Leasing Space says “please select one item” even when selected

## F) All fields must save in drafts and auto-populate in edits

## G) If a required field is missing, do not fail silently — show a notification/banner saying what field is required

## H) Personal Photo and Personal Video Link must save in drafts and auto-populate in edits

## I) Personal Photo file picker is freezing / dinging / preventing submit

## J) Personal Video Link must show as embedded video on listing display

---

# ROOT CAUSE TO TREAT AS LIKELY

This looks like the same class of problem you had before:

* field property type mismatch (string vs array)
* missing JSON decode on load
* missing Select2 rehydration after Livewire render/tab change/draft load
* saveDraft/save flow not persisting hidden or re-rendered fields
* file input being wrapped/bound in a way that conflicts with Livewire or custom JS

---

# PART 1 — Fix these three fields end-to-end:

### Fields

* Acceptable Property Conditions
* View Preference Needed
* Leasing Space

## STEP 1 — Confirm Livewire property types are correct

### In `TenantAgentAuction.php` and `TenantAgentAuctionEdit.php`

For each of the 3 fields above:

1. If the UI is multi-select, the Livewire property must be an **array**:

* `public $acceptable_property_conditions = [];`
* `public $view_preference_needed = [];` or whatever exact property name the blade uses
* `public $leasing_space = [];` if multi-select
  If Leasing Space is single-select, keep it as string — but match the actual blade input type exactly.

2. Do not leave a Select2 multi-select field as `''` string property.

✅ Use the exact existing property names already bound in the blade. Do not invent new names.

---

## STEP 2 — Decode saved values correctly on load/edit/draft load

### In `TenantAgentAuction.php` and `TenantAgentAuctionEdit.php`

When loading the auction/draft data for these 3 fields:

* If saved value is JSON array string → decode to PHP array
* If saved value is legacy plain string → convert to `[string]` for multi-selects
* If empty → `[]` for multi-selects, or `''` for single-selects

### Important

Do not assign raw JSON string directly into a Select2 multi-select Livewire property.

---

## STEP 3 — SaveDraft and final save must preserve these fields

### In both create and edit components

For these same 3 fields:

* Ensure saveDraft uses the same normalized property values
* Ensure final save uses the same normalized property values
* Do not overwrite them with empty values when hidden or not re-rendered

### Non-wipe rule

If a field is already filled and the incoming request/update is empty because of UI/render issues, do not wipe the saved value unless the user explicitly cleared it.

---

# PART 2 — Fix the “selects then deletes itself” issue

### Problem

View Preference Needed and Leasing Space select briefly, then clear.

This is usually Select2 + Livewire re-render conflict.

## STEP 4 — Use stable Select2 sync pattern for ONLY these affected fields

### In the relevant blade:

* `tenant-agent-auction-edit.blade.php`
* and/or the create blade used by `TenantAgentAuction.php`

For:

* Acceptable Property Conditions
* View Preference Needed
* Leasing Space

### Required pattern

1. Wrap the Select2 field in `wire:ignore`
2. Initialize Select2 with a dedicated init function per field or a reusable helper
3. On change:

   * read selected value(s)
   * push into Livewire using `@this.set('property_name', values)`
4. On Livewire `message.processed`, re-initialize and rehydrate selected values
5. On `shown.bs.tab`, re-initialize and rehydrate selected values
6. On draft load event, rehydrate selected values again

### Important

If Leasing Space is single-select, set a single scalar value.
If it is multi-select, use array consistently.

---

## STEP 5 — Fix the false validation error for Leasing Space

### Problem

It says “please select one item” even though one is selected.

This means the UI selection is not reaching the Livewire property before validation.

### Fix

After implementing the stable Select2 sync above:

* confirm the property used in validation is the exact same property being updated by JS
* confirm validation rule matches the actual field type:

  * single-select → validate as string / required
  * multi-select → validate as array / min:1 or required array

Do not validate an array field like a string or vice versa.

---

# PART 3 — Drafts and edits must always restore all fields

## STEP 6 — Apply the same persistence rule to all tenant fields in this flow

### Goal

If the user clicks **Save Draft**, all currently entered fields must save.
If the user clicks **Edit**, all saved fields must auto-populate.

### Minimum scope

At least verify this for:

* Acceptable Property Conditions
* View Preference Needed
* Leasing Space
* Personal Photo
* Personal Video Link

### Requirement

Use the same load/save pattern in both:

* `TenantAgentAuction.php`
* `TenantAgentAuctionEdit.php`

---

# PART 4 — Submit must not fail silently

## STEP 7 — Show a clear required-field notification instead of doing nothing

### Apply to all listings within these shared Livewire flows:

* Seller
* Buyer
* Landlord
* Tenant

### Goal

If submit fails because a required field is missing:

1. show a clear top banner or toast:

   * “Please complete the required fields before submitting.”
2. include the first missing field name, and ideally a short list of missing fields
3. switch to the tab containing the first missing field
4. scroll to the field and focus it

### Implementation

In `TenantAgentAuction.php` and `TenantAgentAuctionEdit.php`:

* catch validation errors
* store:

  * `showSubmitErrors = true`
  * `submitErrorFields = [...]`
  * `firstSubmitErrorField = ...`

In the blade:

* render an error banner at top when validation fails
* JS finds the tab containing the first invalid field
* activate that tab and scroll to it

### Important

Do not allow the form to appear unresponsive.
If submit fails, user must immediately see why.

---

# PART 5 — Personal Photo must save and not freeze the desktop picker

## STEP 8 — Fix Personal Photo upload control

### Problem

When clicking Personal Photo and selecting a photo, file dialog freezes / dings / does not continue.

### Likely causes

* file input is hidden/overlaid incorrectly
* JS click handler is intercepting and reopening/closing dialog
* Livewire upload input is inside a repeatedly re-rendering block
* multiple click handlers bound to the same file input

### Fix instructions

1. Inspect the Personal Photo file input in the tenant create/edit blade
2. Remove duplicate click bindings or custom JS that re-triggers the file input
3. Ensure there is only one real `<input type="file">`
4. If inside a highly reactive section, wrap surrounding UI carefully so Livewire is not rerendering the input during selection
5. Keep the file input simple and native
6. Ensure accepted file types are normal image types only
7. Verify uploaded file persists in:

   * save draft
   * edit load
   * final submit

### Important

Do not redesign the uploader. Just stop the freezing/conflict.

---

# PART 6 — Personal Photo + Personal Video Link must save in drafts and auto-populate in edits

## STEP 9 — Persistence rules

### In both create and edit components

* If Personal Photo already exists, do not wipe it on draft save or edit save unless explicitly replaced/removed
* If Personal Video Link already exists, do not wipe it unless explicitly cleared
* On edit load, both must auto-populate

---

# PART 7 — Personal Video Link must embed on listing display

## STEP 10 — Embedded video display

### Goal

On the tenant listing display, Personal Video Link must render as an embedded video when possible.

### Requirements

1. Support common video URLs at minimum:

* YouTube
* Vimeo

2. Convert valid supported URLs into embed iframe URLs
3. If URL is unsupported, show clickable link as fallback instead of nothing
4. Do not break existing listing display layout

### Important

Only embed when URL is valid and safe.
Otherwise fallback to standard link.

---

# ACCEPTANCE TESTS (MUST PASS)

## Tenant create route

1. Select Acceptable Property Conditions

2. Select View Preference Needed

3. Select Leasing Space
   ✅ selections stay and do not disappear

4. Save Draft
   ✅ reopen draft → all 3 fields still populated

5. Add Personal Photo + Personal Video Link
   ✅ save draft → reopen → both still populated

6. Submit listing
   ✅ no false “please select one item” for Leasing Space

---

## Tenant edit route

1. Open existing listing
   ✅ Acceptable Property Conditions, View Preference Needed, Leasing Space auto-populate
   ✅ Personal Photo and Personal Video Link auto-populate

2. Save edits
   ✅ all values persist

---

## Validation UX

1. Leave a required field blank on another tab
2. Click Submit
   ✅ banner appears
   ✅ tells user required field(s)
   ✅ switches to the correct tab
   ✅ focuses missing field

---

## Listing display

1. Personal Video Link saved
   ✅ shows as embedded video when supported
   ✅ otherwise clickable fallback link

---

# STOP RULE

If Replit cannot fix this by editing only:

* `TenantAgentAuction.php`
* `TenantAgentAuctionEdit.php`
* the shared create blade
* `tenant-agent-auction-edit.blade.php`

…then STOP and report exactly which field/property name is mismatched or which JS event is clearing the value. Do not touch legacy tenant files or unrelated roles.

---

If you want, send me one screenshot of the Tenant fields for **View Preference Needed**, **Leasing Space**, and **Personal Photo**, and I’ll tighten this into an even more surgical prompt that tells Replit exactly which kind of control each one is and what property type it should use.
