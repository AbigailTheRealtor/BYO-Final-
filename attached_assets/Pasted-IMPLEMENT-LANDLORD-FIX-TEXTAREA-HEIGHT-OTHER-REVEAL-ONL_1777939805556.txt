IMPLEMENT LANDLORD FIX — TEXTAREA HEIGHT + OTHER REVEAL ONLY

Scope:
- /offer-listing/landlord
- Agent Dashboard Create Offer Listing flow
- Full-Service / Commission-Based path only

Allowed files only:
1. resources/views/livewire/offer-listing/landlord/offer-landlord-listing.blade.php
2. resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/property-preferences.blade.php
3. resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/lease-terms.blade.php
4. resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/tax-legal-hoa-disclosures.blade.php

DO NOT touch:
- Seller files
- Buyer files
- Tenant files
- Limited Service files
- Hire Agent legacy files
- Database schema
- Backend save/load logic
- Field/model property names

Fix 1 — Landlord textarea height:
Root cause:
- Master blade has .form-control { min-height: 50px; } at lines 56–58.
- rows="2" is already present but overridden.

Action:
- Do NOT change the global .form-control rule.
- Add a scoped Landlord-only textarea class, such as:
  landlord-compact-textarea
- Apply it only to affected landlord textareas.
- Add local scoped CSS to make these textareas compact and visually match normal fields.

Affected textareas:
- renewal_option_details
- additional_landlord_lease_terms
- cam_nnn_additional_rent_charges
- rent_escalation_terms
- tenant_improvement_buildout_terms
- permitted_use_restrictions
- signage_rights
- commercial_parking_terms
- commercial_approval_conditions
- additional_parcel_ids
- legal_description

Fix 2 — Landlord “Other” reveal:
Use/extend the existing unified mlsMultiSelects pattern where possible.

Verify already-configured fields:
- heating_fuel / heating_and_fuel
- air_conditioning
- water
- sewer
- property_utilities / utilities
- laundry_features
- floor_covering
- security_features
- road_surface_type
- electrical_service
- building_features

Add/fix reveal logic for:
- appliances
- view_preference
- desired_lease_length / desired_lease_term
- terms_of_lease
- rent_includes
- tenant_pays
- owner_pays
- additional_documents
- garage_parking_features, if existing handler is incomplete

Implementation rules:
- Standardize wrappers to ID-based selectors where the unified handler expects IDs.
- If a wrapper currently uses only classes, add an ID without removing existing classes.
- If a wrapper uses d-none, either remove d-none and use display:none, or update the handler to handle both d-none and display style.
- Selecting Other must show the input immediately.
- Removing Other must hide the input immediately.
- Visibility must remain correct after Livewire message.processed.
- Do not rename wire:model fields.

After implementation:
Run:
php artisan view:clear
php artisan cache:clear
php artisan config:clear

Browser verification required:
- Residential: test every Other field listed.
- Commercial: test every Other field listed.
- Confirm textareas are compact.
- Confirm Next/Back still works.
- Confirm no console errors.

Report:
- Files changed
- Root cause fixed
- Fields fixed
- Fields already working
- Any deferred issues