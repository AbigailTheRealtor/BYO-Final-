FIX LANDLORD "MY BIDS" TAB TO MATCH TENANT EXACTLY

Goal:
Make the "Hiring Landlord's Agent" tab under My Bids use the exact same layout, buttons, behavior, and routing as the working Tenant "Hiring Tenant's Agent" tab.

Tenant is the source of truth.
Do not invent a landlord-specific row layout.
Clone the Tenant My Bids row pattern and swap only landlord-specific route/data names.

==================================================
PROBLEM TO FIX
==================================================

Current Landlord My Bids tab is still wrong.

Issues visible now:
1. It does NOT match Tenant row layout
2. It shows property/address style text instead of Tenant-style bid row structure
3. It is missing the "View/Edit Bid" button
4. "Visit Listing" is routing incorrectly and shows "waiting for auction to end"
5. It is not behaving like a proper agent-side bid record
6. Landlord rows should look and function the same as Tenant rows

==================================================
REQUIRED END RESULT
==================================================

When user selects:
My Bids > Hiring Landlord's Agent

Each bid row must match Tenant exactly in:
- overall box layout
- spacing
- button placement
- badge placement
- text hierarchy
- row height
- action availability
- routing behavior

Each row must include:
1. Listing ID line
2. Status badge
3. View/Edit Bid button
4. Visit Listing button

Use same visual pattern as Tenant My Bids tab.

Do NOT use the old Landlord card style.

==================================================
EXPECTED LANDLORD ROW BEHAVIOR
==================================================

For each agent bid under "Hiring Landlord's Agent":

A. LEFT SIDE
- Show Listing ID in the same style Tenant uses
- If Tenant shows title + Listing ID pattern, Landlord must mirror the same pattern
- Do not replace this with only address-style display unless Tenant also does
- Match exact formatting hierarchy used by Tenant

B. CENTER / STATUS
- Show same style status badge as Tenant
- Active, Countered, Accepted, Rejected, etc. should render with same badge rules Tenant uses

C. RIGHT SIDE ACTIONS
Buttons must match Tenant behavior and placement:

1. View/Edit Bid
- visible whenever Tenant would show View/Edit Bid
- routes user to their own landlord bid detail/edit page
- must open the correct landlord bid record by bid_id
- must not go to listing page
- must not open wrong bid
- must not 403 for correct bidding agent

2. Visit Listing
- must route to the landlord listing page
- must NOT go to a waiting-for-auction-to-end gate unless Tenant would also do that in the same exact bid state
- if Tenant lets agent visit the listing from My Bids, Landlord must do the same
- must open the correct landlord listing tied to the bid

==================================================
IMPORTANT ROUTING RULES
==================================================

Fix the routing logic separately for the two buttons.

1. View/Edit Bid
This must use the bid record.
It should resolve by landlord bid id, not just listing id.

Expected behavior:
- open the agent's own submitted bid for that landlord listing
- allow edit only when status/state allows edit in the same way Tenant does
- if Tenant uses a shared "view/edit bid" route pattern, Landlord should mirror it exactly with landlord route/controller equivalents

2. Visit Listing
This must use the listing record.
It should resolve by the landlord auction/listing id and go to the actual listing detail page.

Expected behavior:
- open the landlord listing itself
- not the bid page
- not a waiting/intermediate screen unless that is truly the same rule Tenant uses
- if current Landlord route is hitting the wrong controller/view or wrong permission gate, replace it with the Tenant-equivalent listing destination pattern

==================================================
DO NOT KEEP OLD LANDLORD LEGACY BEHAVIOR
==================================================

Remove or replace any old Landlord My Bids row rendering that:
- shows only address/title style without Listing ID structure
- lacks View/Edit Bid
- uses incorrect action routing
- uses listing-only logic for both buttons
- points Visit Listing to a gated page that says waiting for auction to end
- does not follow Tenant layout

==================================================
IMPLEMENTATION APPROACH
==================================================

Step 1:
Audit the working Tenant My Bids implementation for:
- blade row markup
- route names
- controller methods
- button visibility conditions
- status badge rendering
- Listing ID display source
- View/Edit Bid route target
- Visit Listing route target

Step 2:
Compare with current Landlord My Bids implementation and identify exact differences causing:
- missing View/Edit Bid
- wrong row layout
- wrong Visit Listing route
- wrong displayed text structure

Step 3:
Make Landlord My Bids clone the Tenant implementation exactly, only swapping:
- landlord model names
- landlord route names
- landlord controller methods
- landlord bid/listing ids
- landlord view/edit destination
- landlord listing destination

==================================================
DATA DISPLAY RULES
==================================================

Landlord My Bids should display the same identifying data style as Tenant.

If Tenant row currently shows:
- Listing title/name
- Listing ID
- status
- View/Edit Bid
- Visit Listing

Then Landlord must match that same structure.

Do not improvise with a different landlord-only format.

The screenshot difference clearly shows Landlord is still rendering a different template than Tenant.

==================================================
PERMISSION / LOGIC RULES
==================================================

For the bidding agent viewing their own landlord bids:
- they must be able to access View/Edit Bid for their own bid when allowed
- they must be able to access Visit Listing for the correct listing
- no 403 for their own bid
- no redirect to wrong listing
- no redirect to waiting-for-auction-to-end unless Tenant would do the same in identical conditions

If waiting-for-auction-to-end is being triggered because the wrong route is being used, fix the route.
If it is being triggered because the wrong controller branch is being used, fix that branch.
If it is being triggered because Visit Listing is incorrectly using a bid/edit route or protected owner-only route, replace it with the correct public/agent-accessible listing view route used by Tenant.

==================================================
TEST CASES REQUIRED
==================================================

Test these specifically:

1. My Bids > Hiring Tenant's Agent
- capture current working layout and routing as source of truth

2. My Bids > Hiring Landlord's Agent
- verify row layout now visually matches Tenant
- verify Listing ID appears in same style/placement as Tenant
- verify View/Edit Bid button is visible
- verify Visit Listing button is visible

3. View/Edit Bid button
- click from Landlord My Bids
- opens correct landlord bid
- correct bid loads
- editable when appropriate
- no 403

4. Visit Listing button
- click from Landlord My Bids
- opens correct landlord listing
- does NOT show waiting-for-auction-to-end incorrectly
- does NOT open wrong page

5. Multiple landlord bids
- each row maps to correct bid_id and correct listing_id independently
- no duplicate routing bug
- no cross-row confusion

6. Countered / Accepted / Rejected states
- same button logic and row layout rules as Tenant
- if Tenant changes button visibility by state, Landlord must mirror it exactly

==================================================
GUARDRAILS
==================================================

Do NOT:
- change Tenant My Bids behavior
- change Seller or Buyer while fixing this
- rename DB columns
- change listing creation form logic
- change landlord bid form fields
- create a new landlord-specific layout
- use listing_id for View/Edit Bid if Tenant uses bid_id
- use the wrong route/controller for Visit Listing

Only fix the Landlord My Bids tab so it matches Tenant exactly.

==================================================
EXPECTED FINAL RESULT
==================================================

Under My Bids > Hiring Landlord's Agent, the user should see the same exact row structure and functionality as Tenant:

- Listing ID shown correctly
- status badge
- View/Edit Bid button
- Visit Listing button

And the buttons must work correctly:

- View/Edit Bid -> opens landlord bid
- Visit Listing -> opens landlord listing

No incorrect "waiting for auction to end" message when clicking Visit Listing from My Bids unless Tenant would do that too in the same state.