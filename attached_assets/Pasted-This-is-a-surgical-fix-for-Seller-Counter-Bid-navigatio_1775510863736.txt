This is a surgical fix for Seller Counter Bid navigation buttons.

Do NOT modify Tenant flow.
Use Tenant flow as exact reference.

---

## ISSUES

1. "Previous" button is appearing faded/disabled (white/low opacity)

   * only becomes visible on hover
   * should always be visible and clickable

2. "Submit Bid" button is missing on Seller counter bid final step

   * Tenant flow correctly shows Submit Bid
   * Seller flow does not

---

## FILES TO CHECK

* Seller counter bid Blade view
* Seller counter bid Livewire component (if used)
* Tenant counter bid view (REFERENCE ONLY)

---

## FIX 1 — PREVIOUS BUTTON STYLING

Find the "Previous" button in Seller counter bid view.

Check for:

* disabled attribute
* conditional disabling logic
* CSS class applying opacity

Fix:

* Remove any unintended disabled state
* Ensure button always has correct class:
  btn btn-secondary (or same as Tenant)

Ensure:

* opacity is NOT reduced
* button is clickable
* no pointer-events: none

---

## FIX 2 — ADD SUBMIT BUTTON (FINAL STEP)

Match Tenant logic exactly.

Find step logic (example):
$currentStep
or
$this->step
or
wizard index

Add conditional:

IF last step:
show:
- Previous
- Submit Bid (green button)

ELSE:
show:
- Previous
- Next

---

## SUBMIT BUTTON REQUIREMENTS

* Same label: "Submit Bid"
* Same color: green (btn-success)
* Same placement as Tenant
* Same action method as Tenant submit

Example:

@if($isLastStep) <button class="btn btn-success">Submit Bid</button>
@else <button class="btn btn-primary">Next</button>
@endif

---

## IMPORTANT

* Do NOT create new logic
* Copy Tenant behavior exactly
* Do NOT break navigation flow
* Do NOT change validation logic

---

## VERIFICATION

Confirm:

1. Previous button is fully visible (no fade)
2. Previous button is clickable on all steps
3. Submit Bid appears ONLY on final step
4. Next button does NOT appear on final step
5. Behavior matches Tenant exactly

---

## OUTPUT REQUIRED

Return:

1. files changed
2. exact condition used to detect final step
3. confirmation Previous button no longer appears disabled
4. confirmation Submit Bid now appears correctly
