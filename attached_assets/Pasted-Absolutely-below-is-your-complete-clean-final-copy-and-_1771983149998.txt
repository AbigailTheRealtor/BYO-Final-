Absolutely — below is your **complete, clean, final copy-and-paste Replit instruction set** for **Hire a Buyer’s Agent** listings, fully corrected (including “Other” always last and keeping bubble format for Acceptable Property Style).

This replaces all prior Buyer instructions.

---

# ✅ REPLIT IMPLEMENTATION INSTRUCTIONS

# Hire a Buyer’s Agent — Full Formatting + Ordering + Display Safeguards

### Applies To

* Login: `tenant@exp.com`
* Flow: Dashboard → **Hire a Buyer’s Agent**
* Must function correctly for:

  * Create Listing
  * Save Draft
  * Edit Draft
  * Submit Listing
  * Live Listing Display
  * Download Listing Snapshot

---

# 🔒 GLOBAL SAFEGUARDS (DO NOT SKIP)

1. Do NOT remove or rename database columns.
2. Do NOT break existing validation.
3. Do NOT remove bubble styling for Acceptable Property Styles.
4. All fixes must apply to:

   * Draft save
   * Draft reload
   * Edit reload
   * Live listing
   * Snapshot
5. No partial tab saves.
6. No tab overwriting when later tabs save.

---

# 1️⃣ OFFERED FINANCING / CURRENCY — ORDERING FIX

## 🔹 REQUIRED TOP-LEVEL DISPLAY ORDER (Exact Order)

Render financing options in this exact order:

1. Assumable
2. Cash
3. Conventional
4. Cryptocurrency
5. Exchange/Trade
6. FHA
7. Jumbo
8. Lease Option
9. Lease Purchase
10. No-Doc
11. Non-QM
12. NFT
13. Seller Financing
14. USDA
15. VA
16. **Other** ← MUST ALWAYS BE LAST

---

## 🔹 ORDERING SAFEGUARD RULE

* Do NOT rely on database order.
* Use a hard-coded ordered array.
* “Other” must always render last.
* If new options are added later, they must be inserted before “Other.”
* If sorting dynamically, always:

  * Remove “Other”
  * Sort remaining items
  * Append “Other” at the end

---

# 2️⃣ CONVENTIONAL LOAN GROUPING STRUCTURE

When any traditional loan option is selected, group them under this header:

**Conventional / FHA / Jumbo / VA / No-Doc / Non-QM / USDA**

Under that grouped header, display in this exact order:

1. **Buyer Pre-Approved for a Loan:**
2. **Buyer Pre-Approval Amount:**

These must appear before Exchange/Trade or other financing types.

Safeguards:

* Header must render as one grouped line.
* Questions must always appear in this order.
* Persist properly in Draft and Edit.

---

# 3️⃣ LABEL CORRECTION

Change everywhere:

`Current Representation Status with Broker?`

To:

`Current Representation Status with Broker:`

Must update:

* Create form
* Edit form
* Live listing
* Snapshot

No question mark anywhere.

---

# 4️⃣ SECTION HEADINGS MUST END WITH COLON (LIVE LISTING ONLY)

Update the following live listing section headers:

* Property Preferences:
* Purchasing Terms:
* Financing Details:
* Broker Compensation & Agency Agreement Terms:

Display formatting only.
Do not alter stored values.

---

# 5️⃣ ACCEPTABLE PROPERTY STYLE — KEEP BUBBLE FORMAT

Rules:

* Keep bubble/pill format.
* Allow multi-select.
* Must persist in Draft + Edit.
* Must render correctly on Live listing.
* “1/2 Duplex” must match Tenant formatting exactly.

Do NOT convert to regular text.

---

# 6️⃣ 1/2 DUPLEX FORMATTING STANDARD

Ensure exact formatting:

`1/2 Duplex`

Do NOT allow:

* Half Duplex
* ½ Duplex
* 1/2 duplex (lowercase)

Must display identically in:

* Create
* Draft
* Edit
* Live listing
* Snapshot

---

# 7️⃣ ADD “DOWNLOAD LISTING SNAPSHOT” TO BUYER LISTINGS

Implement same snapshot feature used by:

* Seller
* Landlord
* Tenant

Requirements:

* Button visible on Buyer live listing
* Uses same snapshot generator
* Reflects all formatting fixes in this document
* Must work after Submit

Do NOT create a new snapshot system.

---

# 8️⃣ ACCEPTABLE CITIES & COUNTIES — REMOVE STATE ABBREVIATION

On live listing and snapshot:

If stored as:
`Tampa, FL`

Display as:
`Tampa`

If stored as:
`Hillsborough, FL`

Display as:
`Hillsborough`

Implementation rule:

* If string contains comma, split at first comma
* Display substring before comma
* Do NOT modify database value

---

# 9️⃣ ACCEPTABLE PROPERTY CONDITIONS — REMOVE DUPLICATES

Issue:
“Updated/Renovated” repeating.

Fix:

* Run selections through unique filter before rendering.
* Preserve original selection order.
* Do NOT modify stored values.
* Apply to Snapshot as well.

---

# 🔟 CARPORT & GARAGE FORMATTING

## A) Capitalization Fix

Use:

* Carport Needed Spaces
* Garage Needed Spaces

“Spaces” must be capitalized everywhere.

---

## B) Yes / No Format

Display Yes / No as regular text (not bubble format).

Example:

Carport Needed: Yes
Garage Needed: No

This override applies even if other sections use bubbles.

---

# 1️⃣1️⃣ PETS SECTION — MATCH TENANT FORMAT

Replace Buyer pets display format with:

If pets allowed:
`Pets: Yes (3)`

If no pets:
`Pets: No`

Do NOT show bubble format for Yes/No in this section.

---

## 🔹 Rename Label

Change display label from:

* Types of Pets
* Type of Pets

To:

`Pet Types`

Apply this label change on actual listing display for:

* Seller
* Buyer
* Landlord
* Tenant

Display label only — do not rename database field.

---

# 1️⃣2️⃣ FINANCING DETAILS — BUBBLE DISPLAY RULE

Under Financing Details:

If only ONE value selected:
→ Render as regular text.

If MULTIPLE values selected:
→ Bubble/pill styling allowed.

Example:

Single:
Loan Type: Conventional

Multiple:
Loan Types: [Conventional] [FHA] [VA]

Apply consistently across all financing sub-questions.

---

# 1️⃣3️⃣ PERSISTENCE SAFEGUARDS

Every field must:

* Save in Draft
* Reload in Draft
* Reload in Edit
* Display correctly after Submit
* Appear correctly in Snapshot

No formatting changes after reload.
No missing fields.
No disappearing selections.

---

# 1️⃣4️⃣ QA CHECKLIST (MUST PASS BEFORE COMPLETE)

Using `tenant@exp.com`:

✅ Financing options appear in exact required order
✅ “Other” appears last
✅ Conventional loan group header displays correctly
✅ Pre-approval questions appear in correct order
✅ Section headings show colons
✅ Cities and Counties display without state
✅ Property Conditions do not duplicate
✅ Carport/Garage shows “Spaces” capitalized
✅ Pets shows “Pets: Yes (3)”
✅ Pet Types label updated
✅ Financing bubble rule works
✅ Snapshot downloads and matches display

---

If you want next, I can create a **Global Display Formatting Standard Document** that forces consistent logic across Buyer, Seller, Tenant, and Landlord so you stop having to fix formatting per role.
