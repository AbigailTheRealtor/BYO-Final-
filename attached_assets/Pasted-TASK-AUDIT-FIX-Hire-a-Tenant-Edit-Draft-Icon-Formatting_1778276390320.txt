TASK: AUDIT + FIX — Hire a Tenant Edit/Draft Icon Formatting Must Match Create Listing

IMPORTANT SAFEGUARDS:
- READ-ONLY AUDIT FIRST before editing.
- ONLY work on Hire a Tenant Offer Listing Edit/Draft flow.
- DO NOT touch Seller, Buyer, Landlord, or Hire Agent flows.
- DO NOT modify database schema.
- DO NOT modify fields, labels, placeholders, tooltips, validation, tab order, Save Draft logic, or submit logic.
- DO NOT delete or alter saved drafts.
- Minimal surgical edits only.
- Preserve all current Tenant draft data.

ISSUE:
The Tenant Edit/Draft page now shows icons, but the formatting is still wrong.

Example URL:
/offering-listing/tenant/edit/137 or /offer-listing/tenant/edit/137

Screenshot shows:
- icons appear inside fields
- some inputs have cramped/incorrect left padding
- Select2/multi-select fields do not match Create Listing
- edit/draft layout is visually different from the Create Tenant Listing flow
- prior Create Listing icon/format fix did not carry over to Edit/Draft

GOAL:
Make the Tenant Edit/Draft input/icon/select formatting match the working Create Tenant Listing formatting exactly.

AUDIT FIRST:
Compare these two files side-by-side:

1. Working Create Tenant Listing:
resources/views/livewire/offer-listing/tenant/offer-tenant-listing.blade.php

2. Broken Edit/Draft Tenant Listing:
resources/views/livewire/offer-listing/tenant/offer-tenant-listing-edit.blade.php

Specifically compare:
- `.input-cover`
- `.input-icon`
- `.has-icon`
- `.form-control.has-icon`
- Select2 styling
- multi-select/tag styling
- textarea styling
- compact field styling
- icon padding / left spacing
- CSS rules around icon positioning
- JS icon injection logic
- DOMContentLoaded / Livewire hook initialization
- Select2 rehydration timing

ROOT CAUSE TO FIND:
The icons are now being injected, but the Edit/Draft blade likely does not have the same CSS/layout rules as the Create blade. The fix should copy or align the exact working Create Tenant Listing formatting rules into the Edit Tenant Listing blade.

DO NOT guess. Identify exactly which CSS/JS/layout rules exist in Create but are missing or different in Edit.

FIX REQUIREMENTS:
- Make Edit/Draft match Create visually.
- Do not change the actual fields.
- Do not change saved values.
- Do not change Livewire properties.
- Do not change database.
- Do not change validation.
- Do not change tab order.
- Keep the existing deduped icon logic.
- Only add/align missing CSS or initialization logic from Create into Edit.

VERIFY:
After fix, test Tenant Edit/Draft:
1. Open existing Tenant edit listing.
2. Confirm icons display with correct spacing.
3. Confirm text does not overlap icons.
4. Confirm Select2 fields match Create Listing.
5. Confirm multi-select/tag fields match Create Listing.
6. Switch tabs and return to Property Preferences.
7. Save Draft and reload.
8. Confirm formatting still matches Create.
9. Confirm no duplicate icons.

FINAL REPORT:
Return:
- exact files changed
- exact Create rules copied/aligned
- confirmation no schema/data/field logic was changed
- screenshots or visual QA summary if possible