Use this exact Replit instruction:

---

# Hire a Seller’s Agent — Agent Bid Fixes with Safeguards

## Goal

Fix the **Seller Agent Bid** flow so that:

1. **Provide Digital Photo Enhancements** under **Offered Services** behaves exactly like listing creation

   * when selected, its tied follow-up options must immediately appear
2. **Next** and **Previous** buttons are not washed out / white unless truly disabled
3. **Broker Compensation & Agency Agreement Terms** must show the **exact selected options** the user chose when creating the listing
4. **Offered Services** must show the **exact selected options and exact format** the user chose when creating the listing
5. Seller Agent Bid must match the listing creation version as closely as possible for these sections, with no rewritten or simplified version

---

# Hard Safeguards

1. **Do not touch Tenant, Buyer, or Landlord flows**
2. **Do not change database schema**
3. **Do not rename columns, models, routes, or relationships**
4. **Do not rewrite Seller listing creation sections**
5. **Do not create a new custom Seller bid version of Offered Services**
6. **Do not create a new custom Seller bid version of Broker Compensation & Agency Agreement Terms**
7. **Reuse the same active logic/partials/components used by Seller listing creation wherever possible**
8. **Do not hardcode property types or service options**
9. **Do not patch inactive legacy files**
10. **First confirm the actual active Seller bid route, component, blade, and included partials before editing**
11. **Keep changes surgical and Seller-bid-only**
12. **Stop if the file being edited is not actually in the Seller bid render path**

---

# Main Problem Summary

The Seller Agent Bid flow still has these issues:

* **Provide Digital Photo Enhancements** does not trigger its dependent options the same way it does in Seller listing creation
* **Next** and **Previous** buttons still look white/washed out and only show color on hover
* **Broker Compensation & Agency Agreement Terms** does not fully reflect the exact listing selections
* **Offered Services** still looks different from listing creation and is not showing the exact selected options in the exact same way

This means Seller Agent Bid is still not reusing the correct active create-listing behavior for these sections.

---

# Required Fixes

## 1) Offered Services must behave exactly like Seller listing creation

### Required behavior

In **Seller Agent Bid**, the **Offered Services** section must use the same:

* field order
* grouping
* labels
* formatting
* conditional logic
* dependent option logic
* selected-state display
* “Other” behavior
* service sub-option behavior

as Seller **listing creation**.

### Specific issue to fix

When the user selects:

**Provide Digital Photo Enhancements**

the tied options that belong to that service must immediately appear exactly like they do in Seller listing creation.

### Important

Do **not** manually recreate only that one toggle in a custom way.

Instead:

* trace how Seller listing creation handles that service
* identify the exact active conditional logic / Alpine / Livewire / JS / partial
* make Seller Agent Bid use that same logic path

### Acceptance criteria

When **Provide Digital Photo Enhancements** is selected in Seller Agent Bid:

* the same tied options appear
* in the same format
* in the same place
* with the same show/hide behavior
* with the same saved-state behavior
  as Seller listing creation

---

## 2) Broker Compensation & Agency Agreement Terms must show the exact selected listing options

The Seller Agent Bid section for **Broker Compensation & Agency Agreement Terms** must display the exact options selected by the user in the original Seller listing.

That means:

* same property-type-specific structure
* same selected options
* same exact labels
* same same grouped display
* same same conditional fields
* same same formatting logic as listing creation / listing display where appropriate

### Property type requirement

This must work correctly for:

* Residential
* Income
* Commercial
* Business
* Vacant Land

### Important

Do not show a generic or simplified version.

The bid form must reflect the actual Seller listing selections for that listing’s property type.

---

## 3) Offered Services must show the exact selected listing options and same format

The Seller Agent Bid **Offered Services** section still looks different from Seller listing creation.

Fix it so it shows:

* the exact selected options from the listing
* the exact groupings
* the exact label text
* the exact nested option behavior
* the exact same structure as Seller listing creation

### Important

This is not just a data prefill issue.
It is also a **format parity** issue.

Seller Agent Bid should not use:

* simplified rendering
* flattened rendering
* old legacy rendering
* mixed formatting
* reordered service groups

### Required result

If the Seller listing has certain services selected, Seller Agent Bid should load those exact selections in the exact same Offered Services structure that the Seller saw when creating the listing.

---

## 4) Fix Next / Previous buttons styling

### Current issue

The **Next** and **Previous** buttons are still whited out and only show proper color on hover.

### Required fix

Buttons must display in their correct visible styled state at rest, without requiring hover.

### Check for the cause in the active Seller bid files

Inspect for:

* text-white on white / light background combinations
* opacity classes
* muted classes
* disabled classes being applied incorrectly
* hover-only background class with no base background class
* CSS specificity conflict
* shared button component missing base color class in Seller bid
* conditional disabled attribute applied when it should not be
* `wire:loading.attr="disabled"` or similar causing unintended style state
* custom class overrides affecting Seller bid only

### Required result

At rest:

* **Previous** must look like an active visible button
* **Next** must look like an active visible button
* they should not appear white/washed out
* only truly disabled buttons should look disabled

### Important

Do not “fix” this by only changing hover color.
Fix the base button state.

---

# Required Implementation Approach

## Step 1 — Confirm active Seller bid files first

Before changing anything, identify the actual active render path for:

**Hire a Seller’s Agent → Agent Bid**

Confirm:

* route
* controller or Livewire component
* top-level blade
* included partials
* any shared components
* any property-type includes
* any services / broker comp partials

Do not edit until this is confirmed.

---

## Step 2 — Find the active Seller listing creation source of truth

Identify the actual active files used in **Seller listing creation** for:

1. **Offered Services**
2. **Broker Compensation & Agency Agreement Terms**
3. the dependent logic for **Provide Digital Photo Enhancements**

This is the source of truth.

---

## Step 3 — Reuse active listing-creation logic instead of custom bid-only logic

Wherever possible, Seller Agent Bid should reuse the same:

* partials
* field rendering
* conditionals
* show/hide logic
* selected-state logic

used by Seller listing creation.

If some mode flag is needed for bid vs listing create, that is okay, but:

* structure must remain the same
* labels must remain the same
* options must remain the same
* dependent logic must remain the same

---

## Step 4 — Fix prefill from Seller listing

Make sure Seller Agent Bid loads the exact saved listing selections into:

* Offered Services
* Broker Compensation & Agency Agreement Terms
* any tied sub-options for selected services such as **Provide Digital Photo Enhancements**

### Required behavior

If the user selected items during listing creation, they must already be selected/shown when the agent opens the bid.

That includes:

* service main options
* service sub-options
* “Other” text
* compensation option selections
* relationship selections
* timeframe selections
* property-type-specific terms

---

## Step 5 — Fix conditional visibility for Digital Photo Enhancements

The active Seller bid form must use the same trigger logic as listing creation for:

**Provide Digital Photo Enhancements**

When selected:

* tied options appear immediately

When unselected:

* tied options hide appropriately

When loading saved data:

* tied options should already be shown if selected data exists

This must work on:

* initial load
* after validation rerender
* after save draft
* after reopen/edit
* after back/next navigation if the flow uses tabs or step state

---

## Step 6 — Fix button styling in the active Seller bid view

Inspect the actual rendered button classes and state logic.

Make sure the active Seller bid view has:

* a base visible background color
* a base visible text color
* no false disabled styling
* no opacity washout unless actually disabled

If using shared button classes, only fix the Seller bid render path unless the shared class is provably broken for all.

---

# QA Checklist — must pass

## A. Offered Services logic

In Seller Agent Bid:

* selecting **Provide Digital Photo Enhancements** immediately shows the tied options
* tied options match listing creation
* show/hide behavior matches listing creation
* saved-state behavior matches listing creation

## B. Offered Services parity

For each property type:

* Residential
* Income
* Commercial
* Business
* Vacant Land

Verify:

* correct service groups appear
* correct selected services appear
* format matches listing creation
* no simplified or legacy formatting remains

## C. Broker Compensation parity

For each property type:

* Residential
* Income
* Commercial
* Business
* Vacant Land

Verify:

* exact selected options from listing appear
* same structure as listing creation / active standardized Seller structure
* no wrong generic version appears

## D. Prefill

Create or use test Seller listings with known selections, then verify bid form loads:

* same services
* same service sub-options
* same “Other” text
* same broker compensation selections
* same agreement term selections

## E. Button styling

Verify:

* Previous is visible without hover
* Next is visible without hover
* buttons are not white/washed out
* hover still works, but base state is correct
* disabled state only appears when actually disabled

## F. Regression

Confirm unchanged:

* Tenant bid
* Buyer bid
* Landlord bid
* Seller listing creation
* Seller listing edit
* DB schema
* routes

---

# Required report back

When done, report back with:

1. **Exact active Seller bid route**
2. **Exact files changed**
3. **Exact active Seller listing creation files reused as source of truth**
4. **Root cause of why Provide Digital Photo Enhancements sub-options were not appearing**
5. **Root cause of why Next / Previous looked whited out**
6. **How Seller Agent Bid now loads the exact listing-selected services and broker comp terms**
7. **QA results for Residential, Income, Commercial, Business, and Vacant Land**
8. **Confirmation that Tenant / Buyer / Landlord were not modified**

---

## Final rule

If Offered Services or Broker Compensation in Seller Agent Bid is still using a custom rewritten version instead of the same active Seller listing logic, the fix is not complete.

---

I can also turn this into a tighter **“allowed files / forbidden files / stop conditions / acceptance tests”** version if you want.
