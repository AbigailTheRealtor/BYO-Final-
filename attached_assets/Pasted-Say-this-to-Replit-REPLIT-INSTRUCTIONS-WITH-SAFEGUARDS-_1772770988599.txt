Say this to Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — FINAL QA + FIX PASS FOR HIRE A TENANT’S AGENT LISTINGS

### HARD SAFEGUARDS (NON-NEGOTIABLE)

1. **Before editing anything, identify the active route, active Livewire component, active blade, and directly included partials for the Hire a Tenant’s Agent flow.**
2. **First check which route I’m on.**

   * If URL contains **`/auction/edit/`** → edit only:

     * `TenantAgentAuctionEdit.php`
     * `tenant-agent-auction-edit.blade.php`
     * directly included partials used by that blade
   * If URL contains **`/auction/`** without `/edit/`** → edit only:

     * `TenantAgentAuction.php`
     * its active blade
     * directly included partials used by that blade
3. **Do NOT touch legacy tenant files**
4. **Do NOT touch other roles** (Seller, Buyer, Landlord)
5. **Do NOT rename/delete DB columns, tables, or migrations**
6. **Do NOT change labels, tooltips, or option values unless explicitly required for a bug**
7. This is a **final Tenant listing stability + QA pass**:

   * create must work
   * save draft must work
   * edit must work
   * submit must work
   * all selected fields must persist and display
   * all unselected/blank fields must not display on the final listing

---

# GOAL

Make **Hire a Tenant’s Agent** fully production-ready with no known create/edit/draft/display issues.

This is not a one-field fix. This is a **full final pass** for the active Tenant shared Livewire flow.

---

# REQUIRED FINAL BEHAVIOR

## 1) CREATE

* Every field must select/work on first click
* No flickering
* No field should select and then delete itself
* Required fields must validate correctly
* Hidden conditional fields must appear only when triggered
* Hidden conditional fields must not block submit unless they are actually required and visible

## 2) SAVE DRAFT

* Every entered field must save correctly
* Reopening a draft must restore all values correctly
* Selects, multi-selects, uploads, links, text fields, checkboxes, radios, and conditional fields must all restore

## 3) EDIT

* Every saved field must auto-populate correctly
* Updating one field must not clear another field
* No field should disappear after edit/save
* Conditional sections must reopen with the correct saved values

## 4) SUBMIT

* Submit must work when all required visible fields are completed
* If submit fails, user must see a clear validation banner/message
* User must be taken to the correct tab/field
* No silent failures
* No false “please select an item in the list” when the item is actually selected

## 5) FINAL LISTING DISPLAY

* All fields that were selected or filled out must display correctly
* All fields left blank or unselected must not display
* Conditional sections should only show if relevant
* Embedded media should render correctly where supported
* Display labels and formatting must match the current intended Tenant listing format

---

# REQUIRED FULL QA CHECKLIST — HIRE A TENANT’S AGENT

## A) FIELD STABILITY PASS

Review **every Tenant input field** in Residential and Commercial, including:

* text inputs
* textareas
* dropdowns
* multi-selects
* Select2 fields
* radio/checkbox groups
* file uploads
* video links
* all conditional fields

For every field, verify:

1. can be selected/filled on first try
2. does not flicker or reset
3. saves in draft
4. auto-populates in edit
5. displays correctly on final listing if filled
6. does not display on final listing if blank/unselected

---

## B) REQUIRED FIELD + VALIDATION PASS

Audit every required field in the active Tenant flow.

For each required field:

* validation key must match the actual bound property
* validation rule must match field type
* if field is conditionally hidden, it must not block submit unless it is actually required in the current visible state

Add/fix validation UX so that:

* top error banner appears on submit failure
* first invalid tab opens automatically
* first invalid field scrolls into view

---

## C) SAVE/LOAD CONSISTENCY PASS

For every field:

* property type must match actual control type
* load logic must normalize saved values correctly
* draft save and final save must preserve values
* edit load must restore values correctly
* no empty overwrite if the user did not intentionally clear the field

---

## D) FINAL LISTING DISPLAY PASS

Audit the final Tenant listing display and ensure:

* selected values show
* unselected values do not show
* blank text fields do not show empty labels
* multi-selects render all selected values
* uploaded photo shows if present
* personal/listing video embeds if supported, fallback to link if not
* no incorrect labels or formatting

---

# SPECIFIC TEST SCENARIOS THAT MUST PASS

## Residential Tenant Listing

1. Fill out all major tabs

2. Save draft

3. Reopen draft
   ✅ every field restored

4. Submit listing
   ✅ submit succeeds if visible required fields are complete

5. Open final listing
   ✅ all selected/filled fields show
   ✅ blank/unselected fields do not show

6. Edit listing
   ✅ all fields auto-populate
   ✅ update/save works
   ✅ final listing still correct

---

## Commercial Tenant Listing

1. Fill out all major tabs, including:

* Leasing Space
* View Preference
* Garage/Parking
* any commercial-only fields

2. Save draft

3. Reopen draft
   ✅ all fields restored

4. Submit listing
   ✅ no false validation errors
   ✅ no select flicker/reset

5. Open final listing
   ✅ all selected/filled fields show
   ✅ blank/unselected fields do not show

6. Edit listing
   ✅ all fields auto-populate
   ✅ save works
   ✅ display still correct

---

# FIX RULE

Do not patch one field and stop.
Work through the full Tenant create/edit/display flow until **all** known Tenant field issues are resolved.

---

# REQUIRED OUTPUT FROM REPLIT

After finishing, provide a short completion report with these sections:

### 1) Files changed

List only active Tenant route files and direct partials changed.

### 2) Tenant field audit summary

For each Tenant field issue fixed, list:

* field label
* property name
* issue type
* fixed yes/no

### 3) Final QA results

Confirm tested:

* create
* save draft
* edit
* submit
* final listing display
  for both:
* Residential
* Commercial

### 4) Remaining blockers

If anything still fails, list exact field name, exact property name, and exact reason.

---

# STOP RULE

If this cannot be completed entirely within:

* active Tenant Livewire component
* active Tenant blade
* directly included partials

then STOP and report the blocker.
Do not expand into legacy files or other roles.

---

## Add this line at the very top

**Treat this as the final production-readiness pass for Hire a Tenant’s Agent listings only. The job is not done until create, save draft, edit, submit, and final listing display all work correctly for both Residential and Commercial, with selected fields showing and unselected fields hidden.**

---

That’s the strongest version.

For cheaper runs, I’d send this first and then add one sentence at the end:

**“Do not spend time explaining architecture. Spend time testing every Tenant field and fixing only what fails.”**
