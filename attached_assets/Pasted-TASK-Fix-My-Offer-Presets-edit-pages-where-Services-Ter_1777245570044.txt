TASK: Fix My Offer Presets edit pages where Services/Terms content flashes then goes blank

ISSUE:
On Agent Dashboard → My Offer Presets → Edit Preset, the Services and Terms sections briefly render correctly, then disappear/turn blank.

This affects the Agent preset edit pages across role/property combinations.

GOAL:
Services, terms, and agent overview/default profile sections should remain visible and editable after the page finishes loading.

SCOPE:
Fix My Offer Presets / Agent Presets edit UI only.

Applies to:
- Buyer Agent Presets
- Seller Agent Presets
- Tenant Agent Presets
- Landlord Agent Presets
- All property types under each role

CRITICAL SAFEGUARDS:
- Do NOT modify bid submission logic
- Do NOT modify counterbid logic
- Do NOT modify accepted bid summaries
- Do NOT modify public listing views
- Do NOT modify database schema
- Do NOT alter service/term text
- Do NOT remove existing fields, tooltips, or presets
- This should be a rendering/state hydration fix only

STEP 1 — READ-ONLY AUDIT:
Inspect these areas first:
- resources/views/agent-presets/edit.blade.php
- resources/views/agent-presets/index.blade.php
- app/Http/Controllers/AgentPresetController.php
- app/Models/AgentDefaultProfile.php
- app/Services/AgentPresetCatalog.php
- Any JavaScript/Alpine code used to expand/collapse preset sections
- Any Livewire usage on the preset edit page

Look specifically for:
- Alpine x-data defaulting sections to collapsed/empty
- JS hiding section bodies after initial render
- wire:ignore or x-cloak misuse
- missing initialization data after hydration
- role/property mismatch in preset catalog lookup
- empty arrays overwriting loaded services/terms
- accordion/collapse state script clearing content
- DOM selector conflict caused by duplicate IDs
- recent tab/counter JS changes accidentally targeting .nav-link, accordion, or section content globally

STEP 2 — IDENTIFY ROOT CAUSE:
Before editing, report:
- Exact file causing the blank content
- Whether this is caused by JavaScript, Alpine state, Livewire hydration, or data/catalog lookup
- Whether the issue affects all roles/property types or only some

STEP 3 — FIX REQUIREMENTS:
Fix so that:
- Services list remains visible after load
- Terms/default compensation fields remain visible after load
- Agent Overview section remains visible when expanded
- Previously saved selections still show
- Unsaved selections can still be changed
- Save Preset still works
- Accordion open/close still works

STEP 4 — LIKELY FIX AREAS:
If Alpine/JS is the issue:
- Scope accordion scripts only to the preset edit container
- Do not use global .nav-link, .accordion, .collapse, or section selectors that may affect unrelated pages
- Ensure x-show does not hide content because of undefined state
- Ensure x-cloak is removed after Alpine initializes

If Livewire hydration is the issue:
- Make sure services/terms arrays are initialized before render
- Do not overwrite loaded preset data with empty defaults after mount
- Preserve selected_services, terms, and bio/profile fields after hydration
- Add stable wire:key values for repeated services/fields if needed

If catalog lookup is the issue:
- Confirm role + property type keys match exactly:
  buyer/residential
  buyer/income
  buyer/commercial
  buyer/business
  buyer/vacant_land
  seller/residential
  seller/income
  seller/commercial
  seller/business
  seller/vacant_land
  tenant/residential
  tenant/commercial
  landlord/residential
  landlord/commercial

STEP 5 — TEST ALL ROLE TYPES:
Test at least one preset edit page from each role:
- Buyer Residential
- Seller Residential
- Tenant Residential
- Landlord Residential

Also test one non-residential property type:
- Buyer Income or Commercial

For each:
- Page loads
- Services remain visible
- Sections do not go blank after 2 seconds
- Existing saved services appear checked
- Can select/deselect services
- Can save preset
- Return to My Offer Presets shows correct selected count

STEP 6 — HARD STOP:
If fixing this requires changing service catalog text, database fields, bid logic, or shared public listing JavaScript, STOP and report before making changes.

EXPECTED RESULT:
The preset edit page should behave like the first screenshot:
- Services stay visible
- Terms/default profile sections stay visible when opened
- No blank white section after loading
- Works across all role/property combinations