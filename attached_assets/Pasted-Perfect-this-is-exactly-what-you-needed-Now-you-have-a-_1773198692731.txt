Perfect — this is exactly what you needed.

Now you have a real map of the app, and the answer is:

## You should **not** mark Seller as legacy

Because both Seller paths are actively used.

Same for Buyer and Landlord in practice:

* each has a **dedicated create path** for that account type
* and a **shared create path** for other user types
* plus a **shared edit path** for all roles

So the safest rule is **not** “make Seller legacy.”

The safest rule is:

## New Replit development rule

For every bug or edit, Replit must first determine whether the issue is on:

* the **dedicated create path**
* the **shared create path**
* or the **shared edit path**

and then only edit the files for that exact path.

---

# What you should send Replit now

Use this as your permanent project rule:

---

## PROJECT RULE — DO NOT ASSUME ONE FILE PATH PER ROLE

We have now confirmed the architecture for Tenant, Buyer, Seller, and Landlord.

Going forward, do **not** assume there is only one file path per role.

For each future fix, you must first determine whether the issue is happening on:

### 1) Dedicated create path

Examples:

* Tenant: `/hire/agent/auction/tenant`
* Buyer-type users: `/buyer/add-auction`
* Seller-type users: `/hire/agent/seller`
* Landlord-type users: `/landlord/hire/agent/auction`

### 2) Shared create path

Examples:

* `/hire/agent/auction/buyer`
* `/hire/agent/auction/seller`
* `/hire/agent/auction/landlord`

### 3) Shared edit path

Example:

* `/hire/agent/auction/edit/{auctionId}/{user_type}`
  Uses:
* `TenantAgentAuctionEdit`
* `tenant-agent-auction-edit.blade.php`

---

## REQUIRED WORKFLOW BEFORE EVERY FIX

Before editing anything, first confirm:

1. the exact route/path I am testing
2. whether it is:

   * dedicated create path
   * shared create path
   * shared edit path
3. the exact active Livewire component
4. the exact PHP class
5. the exact Blade view
6. the included partials
7. the exact files that should be edited for this path only

Do **not** start coding until you confirm this.

---

## IMPORTANT DEVELOPMENT RULE

Do **not** label Buyer, Seller, or Landlord files as legacy unless they are truly unused.

From the trace we now know:

* Seller dedicated path is active
* Seller shared path is active
* Buyer dedicated path is active
* Buyer shared path is active
* Landlord dedicated path is active
* Landlord shared path is active
* Edit for all roles uses the shared edit path

So multiple paths must be maintained where applicable.

---

## LEGACY FILE RULE

The only files that should be treated as legacy are obvious copies/deprecated files, such as:

* `*.blade copy.php`
* `*.blade copy 2.php`
* explicitly deprecated files with comments indicating they are no longer used

Do **not** confuse alternate active flows with legacy flows.

---

## SCOPE LOCK RULE

For every future bug report, only edit the files actively serving the exact route I am testing.

Do not edit a similar Buyer/Seller/Landlord/Tenant file from another path unless you first prove it is the active source for that route.

---

## REQUIRED RESPONSE FORMAT BEFORE FUTURE FIXES

Before editing, respond with:

* Route:
* Path:
* Flow type: dedicated create / shared create / shared edit
* Livewire component:
* PHP class:
* Blade view:
* Included partials:
* Exact files to edit:

Only after that should you implement the fix.

---

# What this means for you

Your new mental model should be:

### Tenant

* primary create = shared
* edit = shared

### Buyer

* buyer-type create = dedicated
* non-buyer-type create = shared
* edit = shared

### Seller

* seller-type create = dedicated
* non-seller-type create = shared
* edit = shared

### Landlord

* landlord-type create = dedicated
* non-landlord-type create = shared
* edit = shared

So from now on, when something breaks, the first question is:

## “Which of the 3 paths is this happening on?”

* dedicated create
* shared create
* shared edit

That will save you a huge amount of wasted debugging.

If you want, I can turn this into a **shorter reusable version** you can paste at the top of every Replit request.
