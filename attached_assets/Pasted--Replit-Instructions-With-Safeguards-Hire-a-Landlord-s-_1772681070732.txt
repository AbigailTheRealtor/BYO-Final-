## Replit Instructions With Safeguards

### Hire a Landlord’s Agent — **Commercial** listing display: “Garage/Parking Features: None” must show when selected

**Do NOT rename or delete any database columns.**
**Do NOT delete or modify any existing listings, drafts, or agent bids.**
**Do NOT change labels, tooltips, or option values.**
**Only modify the actual listing display logic for Hire a Landlord’s Agent listings (Commercial only).**

---

# Problem

On **Commercial** Hire a Landlord’s Agent listings, the field:

**Garage/Parking Features**

does not display on the actual listing when the user selects **None**, even though it was selected/saved.

This typically happens because the display code treats `"None"` as “empty/false” and filters it out.

---

# Required Behavior

If the saved value for **Garage/Parking Features** is:

* `"None"` (or the stored value that represents None)

Then the listing must display:

**Garage/Parking Features: None**

It must not be hidden.

---

# Implementation Instructions (Display Logic Only)

## Step 1 — Locate the Commercial listing display row builder for Parking/Garage

In the Hire a Landlord’s Agent **Commercial** listing display template / row builder, find where it renders:

* Garage/Parking Features

It is likely in **Property Details** or **Parking / Garage** section.

---

## Step 2 — Remove “empty/filled” filtering that hides None

Find any conditional like:

* `if (!empty($value))`
* `if (filled($value))`
* `if ($value)`
* `array_filter()` without a custom callback

These will often remove `"None"` depending on how the data is stored (empty string, `0`, `null`, or a special code).

### Required fix

Treat “None” as a valid display value.

Implement a display condition like:

* Render the row if:

  * the field is set (not null), OR
  * the field equals the “None” value, OR
  * the selected array contains “None”

**Safeguard:** Only change the condition for this field (Garage/Parking Features), not global filtering for all fields.

---

## Step 3 — Handle both storage formats (string OR array)

Depending on your schema, Garage/Parking Features may be stored as:

### A) Single select string

Example:

* `"None"`

### B) Multi-select array/JSON

Example:

* `["None"]`

**Required**
In the display layer:

* If it’s JSON string → `json_decode` to array
* If it’s already an array → use as-is
* If it’s scalar string → display it

**Safeguard:** Do not change how it saves, only decode/render.

---

## Step 4 — If it is a mapped value, map “none” properly (display label)

If the saved value is something like:

* `none`
* `no_parking`
* `0`
* `parking_none`

Then the listing display must map it back to the label **“None”** using the same options list used by the form.

**Safeguard:** Do not change option values—only display mapping.

---

# QA Test (Must Pass)

1. Create/Edit a Commercial landlord listing
2. Set Garage/Parking Features = **None**
3. View actual listing
   ✅ Shows “Garage/Parking Features: None”
4. Select a different value
   ✅ Shows that value correctly
5. If multi-select, include “None” alone
   ✅ Still shows “None” (not hidden)

---

## Copy/Paste Prompt for Replit AI

“Hire a Landlord’s Agent listing display (Commercial only): fix Garage/Parking Features so the value ‘None’ is not hidden. Do not rename/delete DB columns, do not modify listings/drafts/bids, do not change labels/tooltips/option values. Only modify display logic. Locate where Garage/Parking Features is rendered and adjust the conditional so ‘None’ (or the stored ‘none’ value) is treated as a valid value and prints ‘Garage/Parking Features: None’. Ensure display supports both string and JSON/array storage and decodes properly before rendering. Do not change save logic.”

If you tell me whether Garage/Parking Features is a **single select** or a **multi-select** in your form, I can tighten the instructions even more so Replit doesn’t guess the wrong decode path.
