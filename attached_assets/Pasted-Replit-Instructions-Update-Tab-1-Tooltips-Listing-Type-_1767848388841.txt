Replit Instructions — Update Tab 1 Tooltips
(Listing Type + Bidding Period Length)
Applies to

Tab 1: Listing Details for ALL Hire-Agent listing flows:

Hire a Tenant’s Agent

Hire a Buyer’s Agent

Hire a Seller’s Agent

Hire a Landlord’s Agent

SAFEGUARDS (MUST FOLLOW — VERY IMPORTANT)

DO NOT:

rename or delete database tables, columns, models, or migrations

change Listing Type dropdown values or logic

modify bidding logic, timers, or hire permissions

change Match Score calculations or visibility rules

alter validation rules or submission behavior

change UI layout, spacing, icons, or field order

modify any other tooltips or placeholders

DO:

update tooltip text only

apply the same tooltip text consistently across all four flows

1) Update Tooltip — Listing Type
Field

Listing Type → tooltip (“?” icon)

Replace the existing tooltip text with EXACTLY the following:

Bidding Period:
Choose Bidding Period to allow Agents to submit bids until the countdown ends. During the bidding period, you cannot view full bid details or hire an Agent. Once the bidding period ends, you may review all submitted bids and select an Agent.
Participating Agents can view other Agents’ Match Scores, as well as the listing’s requested Offered Services and Broker Compensation & Agency Agreement Terms.

Traditional:
Choose Traditional to hire an Agent at any time. For Traditional listings, bids are private. Only the User and the Agent who submitted a bid can view that bid’s Services, Broker Compensation & Agency Agreement Terms, and Match Score.

2) Update Tooltip — Bidding Period Length
Field

Bidding Period Length → tooltip (“?” icon)
(Shown only when Listing Type = Bidding Period)

Replace the existing tooltip text with EXACTLY the following:

Select how long Agents have to submit bids on this listing. Once the bidding period ends, no new bids can be submitted and you may review and select an Agent.

IMPLEMENTATION NOTES (DO EXACTLY THIS)

Update tooltip text only

Keep the same tooltip component, styling, and hover behavior

Do not create separate tooltips per option—Listing Type uses one tooltip

Do not change conditional display logic (Bidding Period Length remains hidden for Traditional)

QA / TESTING CHECKLIST

Open Tab 1 → Listing Details on each Hire-Agent flow

Hover Listing Type tooltip → confirm both Bidding Period and Traditional text appear

Select Bidding Period → hover Bidding Period Length tooltip → confirm simplified time-only text

Switch to Traditional → confirm Bidding Period Length tooltip does not appear

Confirm no other UI or logic changes