TENANT COUNTER BID FLOW — ROLE / STATUS / MESSAGE / ACTION AUDIT + FIX
READ FULLY BEFORE MAKING ANY CHANGES

We have major inconsistencies in the Tenant counter bid flow for BOTH sides:
1. Tenant / listing creator side
2. Agent / bidder side

This is not a one-line wording issue.
This is a full role/state/message/action audit and correction for the Tenant counter bid flow.

CURRENT CONFIRMED PROBLEMS

Across the Tenant listing page, bid card, modal, and View Counter Terms pages, the system is showing contradictory or incorrect behavior such as:

- “Agent submitted a counter bid” in one place
- “Counter Offer Received” in another
- “Test User has submitted a counter offer” in another
- “Your counter offer has been submitted. Awaiting the tenant’s response.” even when the page/action state does not match
- Agent-side page showing owner-side action buttons like:
  - Counter Back
  - Accept Counter
  - Reject Counter
  - View Listing
  when those actions are not appropriate for that role/state

This means the Tenant flow has role/state/message/action desync.

GOAL

Fix the Tenant counter bid flow so BOTH sides behave correctly and consistently:

A. Listing creator / Tenant side
B. Agent bidder side

The system must correctly determine:
- who submitted the latest counter
- who is now expected to respond
- what message should be shown
- what action buttons should be shown
- what message should NOT be shown

We need the Tenant flow fully corrected first.
After that, we will audit Seller / Buyer / Landlord for parity.

IMPORTANT RULE

Use the ACTUAL latest counter state and current viewer role as the source of truth.

Do NOT rely on stale text, copied wording, or generic counter labels.

For every Tenant counter bid page/state, the system should always answer these 3 questions correctly:

1. Who submitted the latest counter?
2. Who needs to respond now?
3. What actions are allowed for the current viewer?

SCOPE

Audit and fix ONLY the Tenant counter bid flow first, including all directly related Tenant bid/counter UI surfaces that display:
- bid status text
- counter notices
- “counter offer received” style banners
- “your counter offer has been submitted” style banners
- action buttons
- View Bid / View Counter Terms / View Listing related state messaging
- any Tenant-specific bid card / modal / view-counter page that participates in the current broken flow

OUT OF SCOPE — DO NOT TOUCH YET

Do not do broad parity work on Seller / Buyer / Landlord yet.
Do not change:
- counter submission logic itself unless a display/state variable is provably wrong
- routes
- schema or migrations
- notifications system globally
- accepted bid summary
- unrelated listing creation/edit flows
- global CSS / redesign
- business logic outside Tenant counter role/state handling

If a later cross-role audit is needed, report it separately after Tenant is corrected.

TENANT FLOW — REQUIRED CORRECT BEHAVIOR

We need the correct behavior for these two sides:

==================================================
1. TENANT / LISTING CREATOR VIEW
==================================================

If the AGENT submitted the latest counter:
- Tenant should see a message equivalent to:
  “Agent has submitted a counter offer.”
  or
  “Counter Offer Received.”

- Tenant should be able to:
  - Accept Counter
  - Counter Back
  - Reject Counter
  - View Listing
  as appropriate for the current active state

Tenant should NOT see:
- “Your counter offer has been submitted. Awaiting the tenant’s response.”
because Tenant is not waiting on themself

If the TENANT submitted the latest counter:
- Tenant should see a waiting message equivalent to:
  “Your counter offer has been submitted. Awaiting the agent’s response.”

- Tenant should NOT see owner-side response buttons if they are waiting on the agent

==================================================
2. AGENT VIEW
==================================================

If the TENANT submitted the latest counter:
- Agent should see:
  “Counter Offer Received.”
  or equivalent wording that clearly means the listing owner / tenant submitted the latest counter and the agent now must respond

- Agent should be able to:
  - Accept Counter
  - Counter Back
  - Reject Counter
  - View Listing
  as appropriate for the current active state

Agent should NOT see:
- “Your counter offer has been submitted. Awaiting the tenant’s response.”
because in this case the tenant submitted the latest counter

If the AGENT submitted the latest counter:
- Agent should see:
  “Your counter offer has been submitted. Awaiting the tenant’s response.”

- Agent should NOT see response buttons while waiting on the tenant

==================================================
3. CORE ROLE / STATE RULE
==================================================

The latest counter submitter determines the waiting state.

If latest counter user == current viewer:
- show waiting message
- hide response actions

If latest counter user != current viewer:
- show “counter received” message
- show response actions if bid is still active/actionable

This rule must be applied consistently across all Tenant flow surfaces.

AUDIT TASKS

1. Identify every Tenant file involved in the current broken experience, including:
- listing bid card view
- bid modal
- view-counter page
- any shared partials used by Tenant flow
- any Livewire component/view variables controlling notices/buttons

2. For each Tenant surface, identify:
- what variable currently determines latest counter ownership
- what variable currently determines waiting/responding state
- what variable currently determines action button visibility
- what text strings are currently used

3. Find every place where the Tenant flow is incorrectly:
- labeling the latest submitter
- showing the wrong waiting message
- showing the wrong “counter offer received” message
- showing the wrong action buttons for the current role/state

4. Fix the Tenant flow so all of those surfaces use one consistent role/state rule:
- latest counter submitter
- current viewer
- active bid state
- allowed actions

5. Keep the actual action permissions correct:
- only show response buttons to the side that needs to respond
- hide response buttons from the side currently waiting

6. Keep terminal state protection:
- accepted / rejected / resolved states must still suppress inappropriate actions

7. Do not “fake” this with copy-only changes.
The state logic and displayed actions/messages must match the real current state.

EXAMPLES OF WHAT TO FIX

If Agent is looking at a Tenant-submitted latest counter:
CORRECT:
- “Counter Offer Received”
- Accept Counter / Counter Back / Reject Counter visible
INCORRECT:
- “Your counter offer has been submitted. Awaiting the tenant’s response.”

If Tenant is looking at an Agent-submitted latest counter:
CORRECT:
- “Agent has submitted a counter offer”
- Accept Counter / Counter Back / Reject Counter visible
INCORRECT:
- owner-side waiting message
- duplicated incorrect wording
- response actions missing when they should be visible

If Agent submitted latest counter and is waiting:
CORRECT:
- waiting message only
- no response buttons
INCORRECT:
- showing Counter Back / Accept Counter / Reject Counter to same side that already submitted latest counter

HARD SAFEGUARDS

- Do not do a broad refactor across all roles yet
- Do not touch Seller / Buyer / Landlord except as read-only references
- Do not change routes
- Do not change schema
- Do not rewrite the whole counter system
- Do not change submission/storage logic unless you prove a state variable is wrong at the source
- Do not fix only one surface; audit the whole Tenant counter flow involved in this bug
- Do not stop at wording if action buttons are still wrong
- If shared partials are used, apply the narrowest safe fix needed for Tenant without breaking other roles

WHAT I WANT BACK

Return all of the following:

1. Exact Tenant files audited
2. Exact Tenant files changed
3. For each broken Tenant surface, explain:
   - what it was showing before
   - why it was wrong
   - what now determines the correct behavior
4. Exact rule now used to determine:
   - latest counter submitter
   - waiting side
   - responding side
   - visible actions
5. Confirmation of correct behavior for BOTH:
   - Tenant / listing creator view
   - Agent / bidder view
6. Confirmation that accepted/rejected/resolved states still suppress actions correctly
7. Any remaining issues that should be audited next for Seller / Buyer / Landlord after Tenant is corrected

FINAL RULE

This is a Tenant counter behavior integrity pass.
Do not treat this as copy cleanup only.
The final result must have correct role behavior, correct messages, and correct action buttons on both sides.