Replit Instructions — Hire a Tenant’s Agent (Residential + Commercial)
Actual Bids Display Fixes (Private “Terms” Order + Buttons + Counter Error + Services)
Scope

Applies to Hire a Tenant’s Agent listings (Residential + Commercial) wherever bids are displayed (listing page bid cards + bid “Terms” modal / private bid detail view).

1) Private “Terms” Section Order Must Match Exactly
Goal

Inside the private bid details (“Terms” / modal / private bid view), the sections must render in this exact sequence (no extras inserted in between):

Private Compensation & Agreement Terms

Agent Overview & Qualifications

About Agent

Why Hire This Agent

What Sets This Agent Apart

Marketing Strategy

Reviews Link

Website Link

Social Media Platforms

Licensed Year

Broker Compensation and Agency Agreement Terms

Additional Terms

Offered Services

Agent Presentation & Marketing Materials

Agent Credentials and Contact Information

Implementation instructions

In the private bid details Blade (the view that renders when clicking Terms), do not output sections in the current “template order” or “whatever has data first” logic.

Instead, enforce order using an explicit ordered list and then render each section in that order if it exists.

Do this:

Create an ordered array like $sections = ['private_comp', 'agent_overview', 'about_agent', ...] matching the 15 items above.

Render sections by iterating over that ordered array.

For any section that has empty content, either:

show “Not provided”, or

hide the section entirely (your choice—just keep the order consistent for the ones that do show).

✅ Acceptance test: When clicking Terms, the private view always shows those headings in that exact order.

2) Fix Accept / Counter / Reject Button Styling + Consistency
Current issues to fix

Accept Bid button looks “whited out” unless hovered.

Reject should be red background + white text.

Counter must match same size and formatting as Accept/Reject, but use a blue background + white text (use the blue styling currently on your reject button).

Implementation instructions

In the bid action button area (private bid modal/footer or bid action row):

Make all 3 buttons use the same base button classes for sizing:

same padding, font-size, border-radius, min-width, height

Apply consistent button variants:

Accept Bid → green background + white text (e.g., btn btn-success)

Counter Bid → blue background + white text (e.g., btn btn-primary)

Reject Bid → red background + white text (e.g., btn btn-danger)

Also fix the “whited out” effect:

Check if Accept is being rendered with:

disabled attribute accidentally

opacity: 0 / opacity: .X

bg-transparent / btn-outline-*

text-white missing

Ensure the Accept button is not being styled as outline/ghost unless intended.

✅ Acceptance test: All 3 buttons look consistent (same size), readable without hover, and match the correct colors.

3) Counter Bid Click Error — Missing Table tenant_counter_terms
Error shown

SQLSTATE[42P01]: Undefined table: relation "tenant_counter_terms" does not exist ... (View: resources/views/tenant_counter_terms/add.blade.php)

Fix required (choose ONE path, do not hack around it)
Path A (Recommended): Create the missing table + model flow

If the counter system is intended to store counters separately:

Create a migration for tenant_counter_terms (Postgres-safe):

Include at minimum:

id

tenant_agent_auction_id (foreign key or indexed int/bigint)

bid_id or tenant_agent_bid_id (whatever your bid table is)

counter_payload (JSON / JSONB) to store the countered terms/services snapshot

created_by_user_id

timestamps

Update the query in TenantAgentAuctionCounterTerm.php to match actual columns.

Confirm route/controller that opens tenant_counter_terms/add.blade.php writes to this new table.

Path B: Point the code to the CORRECT existing table

If you already have a counter table but the code is referencing the wrong name:

Find where it queries tenant_counter_terms

Replace with the actual existing table name

Update the model + relationships accordingly

Confirm the view still receives the correct data structure

✅ Acceptance test: Clicking Counter Bid loads the counter form without error.

4) Services Display in Private Bid Is Incorrect
Current issues to fix

Services are not fully correct

Wrong labels

Not showing all selected services

Services are not always grouped under the correct main categories like the listing view

Goal

In the private bid “Offered Services” section, show:

ALL selected services

under the correct category headings

with the same labels and same grouping/order as the “actual listing” services layout

Implementation instructions

Stop rendering services from partial/derived text fields

Do not render from a truncated list, or from only one category array, or from a reshuffled grouping.

Render services using the same source-of-truth services definition used in the bid form (the master array/config that defines categories + service labels + IDs).

Persist selections using IDs/keys, then at render time:

Loop categories in the correct order

Under each category, list the selected services that belong to that category

Include “Additional Services” as its own category and list each custom entry

Fix mismatched headings:

Ensure category titles match exactly what the listing displays (no renamed category headings, no swapped category names).

Confirm BOTH Residential + Commercial use the correct service definition set (don’t accidentally render Residential categories for Commercial bids, or vice versa).

✅ Acceptance test: Private bid “Offered Services” matches the “actual listing” services structure: correct labels, correct categories, and every selected service appears.

Final QA Checklist (Must Pass)

Private “Terms” modal/view section order matches the 15-item sequence exactly.

Accept button is not washed out; all buttons are visible without hover.

Reject is red/white, Counter is blue/white, all 3 same size.

Counter Bid opens without the tenant_counter_terms table error.

Services in private bid show all selected items with correct category labels and correct grouping/order.