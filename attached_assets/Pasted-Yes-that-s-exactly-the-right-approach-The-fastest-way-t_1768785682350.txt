Yes — that’s exactly the right approach. The fastest way to stop this permanently is to **copy the same wiring pattern** that’s already working for the fields that *do* show, and apply it to the missing fields.

Below is a **copy-paste Replit instruction prompt** (with your usual safeguards) that tells Replit AI to:

1. find how the working fields are **saved + reloaded + displayed**,
2. identify why the missing fields aren’t, and
3. wire the missing fields in the **same exact way**.

---

## Replit Instructions (WITH SAFEGUARDS) — Hire a Buyer’s Agent ([Tenant@exp.com](mailto:Tenant@exp.com))

### ✅ Goal

Under **[tenant@exp.com](mailto:tenant@exp.com) → Hire a Buyer’s Agent → Purchasing Terms**, fix the missing fields so they:

* **Save correctly** (including Save Draft)
* **Reload correctly** on Edit
* **Display correctly** on the public listing view

### ⚠️ CRITICAL SAFEGUARDS (DO NOT BREAK ANYTHING)

* **DO NOT rename** any database columns or JSON keys.
* **DO NOT delete** or refactor existing fields, tooltips, components, or logic.
* **DO NOT mass-update** database rows.
* **DO NOT reset/clear** any values when a toggle changes (visibility must NOT wipe saved data).
* **DO NOT change** the working fields that already display properly—use them as the reference pattern.
* Only make **additive**, minimal changes to connect the missing fields end-to-end.

---

## ✅ Step 1 — Identify the “working pattern” and mirror it

Find one Purchasing Terms field that is already working end-to-end (example: **Maximum Budget**, **Buyer Pre-Approval Amount**, **Desired Interest Rate**, etc.) and confirm these 3 places are wired:

1. Livewire state property / key name
2. SaveDraft/save() mapping to the model column/key
3. Edit/mount() hydration mapping from model back to Livewire
4. Public listing Blade view display

Use that exact pattern for the missing fields listed below.

---

## ✅ Step 2 — Fields currently NOT showing (must be fully wired)

### A) Seller Financing / Loan Structure

* **Amortization Type**
* **Payment Frequency**
* **Late Payment Fee**

### B) Lease Option

* **Maintenance / Repair Responsibility**
* **Extension Terms**

### C) Lease Purchase

* **Rent Credit Toward Purchase Price**
* **Rent Credit Amount**
* **Non-Refundable Deposit / Purchase Deposit**
* **Extension Terms**

### D) Cryptocurrency (transfer details)

* **Exchange / Conversion Method**
* **Custodian / Wallet for Transfer**
* **Transaction Fees Responsibility**
* **Timing of Transfer**

### E) NFT (valuation/transfer details)

* **NFT Valuation Method**
* **NFT Transfer Method**
* **Transaction Fees Responsibility (Gas Fees)**

---

## ✅ Step 3 — Implementation Requirements (do all 3 layers)

### 1) Save / Draft persistence

In **saveDraft()** and **save()**, confirm each missing field is explicitly included in the update/create mapping using the same key names as the working fields.

### 2) Edit hydration

In **mount() / loadListing()** for the Edit and Draft components, confirm each missing field is explicitly set back into Livewire state so the edit screen repopulates.

### 3) Public listing display

In the **public listing Blade view** for Buyer listings, add display blocks for each missing field using the same formatting as the existing Purchasing Terms fields.

**Important:**
Only show these fields if they are not null/empty (match the pattern used for other fields).

---

## ✅ Step 4 — Debug logging (temporary, remove after)

Add a temporary debug step to confirm values are present at each stage:

* After Save Draft: confirm values exist on the model
* On Edit mount(): confirm Livewire state contains values
* On listing view: confirm the blade receives the values

Remove debug after confirmation.

---

## ✅ Final Deliverable

Provide the exact file paths and exact code changes made, limited only to what is required to wire the missing fields.

---

### If you want the **even faster** version:

If you paste the file path (or code snippet) for:

* the **Livewire component** handling Purchasing Terms, and
* the **public listing blade** for Purchasing Terms,

…I can write the **exact code lines** to add for each missing field so Replit doesn’t have to “figure it out” and risk guessing wrong.
