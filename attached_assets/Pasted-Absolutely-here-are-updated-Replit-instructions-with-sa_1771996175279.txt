Absolutely — here are **updated Replit instructions with safeguards** for **Hire a Tenant’s Agent (actual listing display)** covering **all 3 issues**:

1. **Furnishings Needed shows quotes** → remove quotes / decode JSON safely
2. **Non-Negotiable Property Features & Amenities “Other” text not showing**
3. **Offered Lease Term “Other” text not showing**

---

## ✅ REPLIT INSTRUCTIONS (Hire a Tenant’s Agent) — WITH SAFEGUARDS

### 🔒 SAFEGUARDS (must follow)

* **DO NOT** change database schema/migrations.
* **DO NOT** rename any stored keys/values, Livewire properties, or input names.
* **DO NOT** change validation, required rules, or tooltips.
* **DO NOT** affect Buyer/Seller/Landlord flows.
* **ONLY** update **Tenant actual listing display** (and Tenant-only view helper/partial if needed).
* If a helper is shared, implement **Tenant-only wrapper** so outputs for other roles remain unchanged.

---

# 1) Furnishings Needed — remove quotes (Tenant actual listing display)

### ✅ Problem

It’s rendering like: `Furnishings Needed: "Furnished"`

### ✅ Fix (display-only)

In the Tenant listing show view, normalize the value before rendering:

* If it’s a JSON string like `"Furnished"` → decode to `Furnished`
* If it’s a JSON array string like `["Furnished","Unfurnished"]` → decode to array
* If it’s already an array → keep
* Never print `json_encode()` output for this field

### ✅ Formatting for this field

* Single value → plain text
* Multiple values → chips/bubbles (or comma-separated if that is your Tenant standard)

---

# 2) Non-Negotiable Property Features & Amenities — show “Other” selection text

### ✅ Problem

User selects **Other** and types custom text, but the actual listing does not display it.

### ✅ Required behavior

If “Other” is selected and custom text exists, display:

* `Other: <custom text>`

### ✅ Display logic (Tenant show)

When rendering the Non-Negotiable Features/Amenities list:

1. Render selected options normally
2. If selections contain `Other`:

   * read the existing stored “Other text” field (do NOT rename)
   * append `Other: <text>` (only if text is not empty)

✅ Must work whether “Other” is the only selection or one of several.

---

# 3) Offered Lease Term — show “Other” selection text

### ✅ Problem

Same issue: “Other” is selected and custom text exists but is not shown.

### ✅ Required behavior

If “Other” is selected and custom text exists, display:

* `Other: <custom lease term>`

### ✅ Display logic (Tenant show)

When rendering Offered Lease Term:

* If the stored value is `"Other"` or includes `"Other"` in multi-select:

  * read the existing “Other term text” field (do NOT rename)
  * display `Other: <text>` (only if not empty)

---

## ✅ Replit AI Copy/Paste Prompt (single block)

Paste this into Replit exactly:

```text
Fix Hire a Tenant’s Agent ACTUAL LISTING display (these issues are still present).

SAFETY RULES:
- Do NOT change DB schema/migrations.
- Do NOT rename stored keys/values or Livewire properties.
- Do NOT change validations/tooltips.
- Do NOT affect Buyer/Seller/Landlord.
- Tenant display-only fixes.

ISSUE 1 — Furnishings Needed shows quotes:
Currently shows: Furnishings Needed: "Furnished"
Fix rendering so it shows without quotes.
- If stored value is JSON string ("Furnished") => json_decode and display Furnished
- If stored value is JSON array string (["Furnished","Unfurnished"]) => json_decode to array and render properly
- If already array => render properly
- Never display raw json_encode output for this field
Formatting: single value => plain text; multiple => existing chips/bubbles (or comma-separated if Tenant standard)

ISSUE 2 — Non-Negotiable Property Features and Amenities “Other” text not showing:
When “Other” is selected and an Other text value exists, display it on the actual listing as:
Other: <custom text>
Do not modify saving logic; read existing stored fields.

ISSUE 3 — Offered Lease Term “Other” text not showing:
When “Other” is selected and an Other text value exists, display it on the actual listing as:
Other: <custom term text>
Do not modify saving logic; read existing stored fields.

QA:
- Open an existing Tenant listing where Furnishings Needed currently displays with quotes and confirm it no longer does.
- Open a Tenant listing where Non-Negotiable Features/Amenities includes Other + custom text and confirm it displays.
- Open a Tenant listing where Offered Lease Term includes Other + custom text and confirm it displays.
- Confirm no other roles changed.
```

---

### One extra note (to prevent Replit guessing wrong)

In your codebase, the “Other” text fields might be named like:

* `*_other`
* `other_*`
* `custom_*`

Replit should **not invent new fields**. It should **find the existing saved key** by checking:

* the Tenant creation form input name
* the Tenant edit form binding
* the DB column/JSON key used for saving

---

If you paste the **exact field names** used in the Tenant form for:

* Non-Negotiable features/amenities “Other”
* Offered Lease Term “Other”

…I can rewrite the last two sections with **exact key references** so there’s zero chance Replit wires it to the wrong property.
