TASK: Tenant Offer Listing — Fix Select Placeholders Only

SCOPE:
Create Tenant Offer Listing only:
- /offer-listing/tenant/tenant
- Full Service / commission-based Tenant Offer Listing flow only

LOGIN TEST:
Abigailbaschuk@gmail.com
12345678

DO NOT TOUCH:
- Seller Offer Listing
- Buyer Offer Listing
- Landlord Offer Listing
- Hire Agent flows
- Limited Service flows
- Agent Profile flows
- Routes
- Database schema
- Validation / Next button logic
- Save/load logic
- Field labels, options, or wire:model bindings

--------------------------------------------------
FIX SELECT PLACEHOLDERS
--------------------------------------------------

FIELDS:
- Acceptable Property Conditions
- Offered Lease Term
- Leasing Space

ISSUE:
These fields are missing the visible placeholder text:

Select

REFERENCE:
Use the working placeholder behavior from:
- View Preference Needed

FIX:
Each listed field must show placeholder exactly:

Select

REQUIREMENTS:
- If Select2 is used, confirm init includes:
  placeholder: "Select"
- If needed for Select2 rendering, add:
  data-placeholder="Select"
- If needed in the HTML select, add only one:
  <option value="">Select</option>
- Do not duplicate placeholder options.
- Do not change any options.
- Do not change wire:model bindings.
- Do not change Select2 behavior beyond placeholder display.

--------------------------------------------------
VERIFICATION
--------------------------------------------------

Test:
/offer-listing/tenant/tenant

Confirm:
- Acceptable Property Conditions shows “Select”
- Offered Lease Term shows “Select”
- Leasing Space shows “Select”
- View Preference Needed still works correctly
- No other Tenant fields were changed
- No Seller, Buyer, Landlord, Hire Agent, or Limited Service files were changed

RUN:
php -l on every edited PHP/Blade file.

HARD SAFEGUARDS:
- No migrations
- No migrate:fresh
- No schema changes
- No broad refactors
- Stop and report if unsure.