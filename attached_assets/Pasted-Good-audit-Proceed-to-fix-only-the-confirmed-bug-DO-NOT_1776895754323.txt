Good audit. Proceed to fix only the confirmed bug.

DO NOT modify or rebuild any existing referral logic.

CONFIRMED BUG:
TenantCriteriaAuctionController listing creation path is missing referral persistence.

GOAL:
Add the missing persistListingReferral hook so tenant_criteria_auctions listings are attributed the same way as all other listing types.

CRITICAL SAFEGUARDS:
- Surgical change only.
- Do not modify any other controller, service, or component.
- Do not touch Buyer/Seller/Landlord/Tenant Livewire flows.
- Do not modify existing referral logic.
- Do not change DB schema.
- Do not change transaction logic except inserting the hook.
- Do not add duplicate attribution.
- Do not move existing code.
- Only add one call at the correct insertion point.

IMPLEMENT ONLY THIS:

1. Inspect TenantCriteriaAuctionController::store()
- identify the exact location where the listing is fully saved
- confirm DB::commit() location if inside transaction

2. Insert ONE line:
\App\Services\ReferralLinkService::persistListingReferral($auction);

3. Placement rules:
- must run AFTER the listing is fully saved
- must run AFTER DB::commit() if a transaction is used
- must NOT run before save()
- must NOT run multiple times

4. Ensure:
- $auction is the saved model instance
- wasRecentlyCreated logic inside service handles duplicates
- no manual guards needed in controller

TEST REQUIREMENTS:

T1. Referred user creates tenant criteria listing
- confirm referring_agent_id is written
- confirm referral_locked = true
- confirm listing_count increments

T2. Non-referred user
- confirm no referral fields written
- confirm no listing_count increment

T3. Edit existing listing
- confirm no overwrite occurs

T4. Stale visit_id
- confirm no crash
- listing still attributed

T5. No side effects
- confirm no other flows are affected

AFTER IMPLEMENTATION, REPORT:
1. exact insertion point (line and context)
2. confirmation of transaction safety
3. test results

DELIVERABLE:
Only fix TenantCriteriaAuctionController missing referral hook.