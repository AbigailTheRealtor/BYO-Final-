This is a surgical bid-services rendering + recalculation consistency fix for ALL listing types/roles.

Do not change database schema.
Do not change listing creation service definitions.
Do not refactor unrelated bid logic.
Do not touch Broker Compensation & Agency Agreement Terms rendering except where needed to keep service recalculation accurate.
Use Tenant and Landlord service rendering as the gold standard visual/function reference.

---

## GOALS

Fix these issues:

1. Seller and Buyer Offered Services should NOT show a green checkmark icon next to each matched service
2. Seller and Buyer Offered Services should visually behave like Tenant and Landlord:

   * normal matched services shown plainly in the correct category
   * Extra Service Offered badge only for true extras
   * Not Offered by Agent badge only for true missing baseline services
3. If the USER edits their listing services later, or if the AGENT edits their bid services later, the agent bid match score and service comparison must update accurately
4. This must work for ALL listing types / roles:

   * Tenant: Residential, Commercial
   * Landlord: Residential, Commercial
   * Buyer: Residential, Income, Commercial, Business, Vacant Land
   * Seller: Residential, Income, Commercial, Business, Vacant Land

---

## HARD SAFEGUARDS

* Do NOT rename or delete DB columns
* Do NOT change listing creation forms or stored service labels
* Do NOT change scoring formulas unless required to make recalculation use the latest saved baseline/bid data correctly
* Do NOT hardcode residential-only behavior
* Do NOT create new one-off Seller or Buyer service layouts
* Use Tenant/Landlord active service rendering as the source of truth
* Only modify:

  * active Buyer private bid view service rendering
  * active Seller private bid view service rendering
  * active service comparison source-loading/recalculation path if stale data is being used
  * any minimal helper/controller/view-model code needed so edited listings/bids re-evaluate from latest saved values

---

1. REMOVE CHECKMARKS FROM BUYER + SELLER MATCHED SERVICES

---

Issue:

* Seller and Buyer currently show a green checkmark next to each matched service
* That is NOT the desired format

Requirement:

* Seller and Buyer should render matched services like Landlord/Tenant
* Do NOT show a green checkmark next to each matched service
* Keep category heading + plain service list formatting consistent with Tenant/Landlord

Expected:

* matched baseline services appear as normal service lines
* true extra services get “Extra Service Offered”
* true missing baseline services appear in the red “Services Requested But Agent Did Not Include” section with “Not Offered by Agent”

---

2. KEEP CORRECT EXTRA / MISSING BEHAVIOR

---

Do NOT break current meaning:

* MATCHED = service exists in both baseline listing and agent bid
* EXTRA = exists only in bid
* MISSING = exists only in baseline listing

Requirement:

* Only extras get the green “Extra Service Offered” badge
* Only missing services get the red “Not Offered by Agent” badge
* Matched services must not have checkmarks for Buyer/Seller
* Matched services must not be mislabeled as extra or missing

---

3. RE-CALCULATE FROM LATEST SAVED DATA AFTER EDITS

---

Critical requirement:

If the listing user edits the services after initial creation,
OR the agent edits the bid services after initial submit,

then the bid’s service comparison and match score must reflect the latest saved values.

This applies to all listing types and roles.

Examples:

* Tenant removes a service they originally requested → existing agent bids should now re-evaluate against the updated baseline
* Buyer adds/removes requested services → bid matches should update accordingly
* Seller edits requested services → bid matches should update accordingly
* Agent edits bid services → service match/missing/extra counts should update immediately using the current saved bid data

Audit and fix any stale-data issue such as:

* cached score arrays not recalculated
* score saved once and never refreshed
* view using old snapshot instead of latest listing meta
* helper called with stale baseline data
* controller/view loading old bid meta after edit

---

4. FULL AUDIT OF LIVE SOURCE DATA

---

Before coding, audit the active current flow for each role:

A. baseline listing services source

* exact current saved source used for comparison

B. compared bid services source

* exact current saved source used for comparison

C. when a listing is edited

* does the view recompute from latest listing data or stale data?

D. when a bid is edited

* does the view recompute from latest bid data or stale data?

If stale, fix it so every active bid view uses the latest saved baseline + latest saved bid values every time it renders.

---

5. VISUAL FORMAT FOR BUYER + SELLER

---

Make Buyer and Seller Offered Services match the Tenant/Landlord style more closely:

* same category heading style
* same plain matched service line treatment
* same extra service badge pattern
* same missing services red section pattern

Important:

* Do NOT copy Buyer/Seller wrong checkmark pattern forward
* Do NOT change Tenant/Landlord if they are already correct
* Only bring Buyer/Seller into parity with Tenant/Landlord

---

6. VERIFY ALL ROLE × PROPERTY TYPE COMBINATIONS

---

After the fix, verify these combinations:

Tenant:

* Residential
* Commercial

Landlord:

* Residential
* Commercial

Buyer:

* Residential
* Income
* Commercial
* Business
* Vacant Land

Seller:

* Residential
* Income
* Commercial
* Business
* Vacant Land

For each combination confirm:

* matched services show without checkmarks for Buyer/Seller
* Tenant/Landlord unchanged if already correct
* extra services only show as extras
* missing services only show as missing
* editing listing services updates bid comparison correctly
* editing bid services updates bid comparison correctly

---

7. REQUIRED OUTPUT

---

Return a numbered report with:

1. exact files checked
2. exact files changed
3. root cause of the stale/incorrect recalculation issue if found
4. confirmation that Seller matched services no longer show checkmarks
5. confirmation that Buyer matched services no longer show checkmarks
6. confirmation that Tenant/Landlord rendering remains correct
7. confirmation that service comparison now re-runs from latest saved data after listing edits and bid edits
8. note any remaining edge cases if found

Do not give a broad summary only.
This is a rendering + live recalculation accuracy fix across all listing types.
