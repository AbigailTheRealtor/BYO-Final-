FINAL CLEANUP — REMOVE DUPLICATE VALIDATION (REQUIRED BEFORE COMMIT)

Scope:
ALL FOUR Offer Listing files:
- seller
- buyer
- landlord
- tenant

--------------------------------------------------
GOAL
--------------------------------------------------
Ensure ONLY ONE validation system exists per file.

--------------------------------------------------
STEP 1 — REMOVE OLD LOOPS
--------------------------------------------------

DELETE ALL instances of:

requiredFields.forEach(function(input) {

Keep ONLY:

for (const input of requiredFields) {

--------------------------------------------------
STEP 2 — REMOVE NESTED LOOPS (CRITICAL)
--------------------------------------------------

If you see:

requiredFields.forEach(...)
    for (const input of requiredFields)

REMOVE the outer forEach completely.

There must NEVER be two loops over requiredFields.

--------------------------------------------------
STEP 3 — REMOVE DUPLICATE isEmpty
--------------------------------------------------

DELETE:

var isEmpty = false;

DELETE any old manual validation blocks.

KEEP ONLY:

const isEmpty = (
    field.type === 'file'     ? !field.files || field.files.length === 0 :
    field.type === 'checkbox' ? !field.checked :
    field.type === 'radio'    ? !document.querySelector(`input[name="${field.name}"]:checked`) :
    field.type === 'select-one' || field.type === 'select-multiple'
        ? field.value === '' || field.value === null || field.value === undefined
        : !field.value?.toString().trim()
);

--------------------------------------------------
STEP 4 — REMOVE DUPLICATE RETURNS
--------------------------------------------------

DELETE:

return field.value.trim() !== '';

KEEP ONLY:

return field.value?.toString().trim() !== '';

--------------------------------------------------
STEP 5 — VERIFY
--------------------------------------------------

Each validation section should have:

✔ ONE loop only  
✔ ONE isEmpty definition  
✔ ONE return statement  

--------------------------------------------------
DO NOT TOUCH
--------------------------------------------------

- initializeLimitedService()
- UI / Blade layout
- database
- routes
- business logic

ONLY CLEANUP DUPLICATES