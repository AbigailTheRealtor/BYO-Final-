Below are **Replit instructions with safeguards** to fix the **Hire a Seller’s Agent → Actual Listing View (Residential)** so it displays **every filled field**, with the **same formatting + section structure as Hire a Buyer’s Agent**.

---

## Goal

When viewing a submitted Seller listing (actual listing show page), the system must:

1. **Render every field that was filled in the create/edit form** (even if value is “No”, “None”, or “Not Applicable” when that was explicitly selected).
2. Match **Buyer listing display formatting** for:

   * **Offered Financing/Currency Terms** (label + grouped display)
   * **Broker Compensation & Agency Agreement Terms** (section headings + field order + value formatting)
3. Keep **Services** in the **exact same order as the create form**, and show nested “digital enhancements” **directly under** that service.
4. Ensure **number formatting** (commas) for currency + numeric fields like Desired Sale Price, SqFt, acreage, etc.

---

## Replit Repro Steps (baseline test)

1. Login: **[tenant@exp.com](mailto:tenant@exp.com)**
2. Go: **Hire a Seller’s Agent**
3. Create a **Residential** listing and fill these specifically:

   * Property Condition (select something)
   * City, County, State, Zip
   * Total SqFt, SqFt Heated Source, Total Acreage
   * Appliances Included, Pool
   * Pets = Yes + Pet Types + Max Weight + Restrictions
   * Desired Sale Price
   * Offered Financing/Currency Terms (select at least 2)
   * Services: select several, including **Provide digital photo enhancements**
   * Broker Compensation & Agency Agreement: fill out values similar to your example
   * Seller’s Current Status
4. Submit, then open the actual listing view.
5. Confirm current bugs (blank/missing fields, wrong order, formatting issues).

---

# A) Fix missing / blank Property Condition

### Problem

**Property Condition is blank** on show page.

### Instructions

1. Locate the **Seller listing show view** (the Blade that renders the public/actual listing).
2. Find the “Property Condition” output line.
3. Confirm it is reading the correct field key from the listing record.

### Safeguards

* **Do not render from request/session/draft objects** on the show page. Show page must render from the **saved listing model data** only.
* Add a fallback: if the value is null but listing exists, display **“Not Provided”** (but still fix the root cause).

### Acceptance Criteria

* If user selected “Updated”, it shows: **Property Condition: Updated**
* If user didn’t answer, show: **Property Condition: Not Provided**

---

# B) Show City / County / State / Zip on actual listing

### Problem

These are not showing at all.

### Instructions

1. Identify where these fields are stored for Seller listings (commonly in one of these patterns):

   * `listings.city`, `listings.county`, `listings.state`, `listings.zip`
   * OR `listings.location.city`, etc (JSON)
   * OR `seller_listing_details.city`, etc (related table)
2. Update the show page to read the correct source consistently for Seller listings.

### Safeguards

* If your platform hides full address, that’s fine — but **City/County/State/Zip must always display** for Seller listings.
* Do not hide these based on role (Tenant/Agent). These are non-sensitive by your spec.

### Acceptance Criteria

Shows in “Property Details” section:

* **City: Seminole**
* **County: Pinellas**
* **State: FL**
* **Zip Code: 33772**

---

# C) Show Total SqFt, SqFt Heated Source, Total Acreage

### Problem

All are missing from actual listing.

### Instructions

1. Add these fields to the “Property Details” display block for Seller→Residential.
2. Confirm they exist in the stored listing record and match the exact keys used in create/edit.

### Safeguards

* Apply numeric formatting:

  * Total SqFt: comma format (**2,150**)
  * Acreage: allow decimals (**0.27**)
* Show only if value exists (or if the user selected a related option that implies it).

### Acceptance Criteria

* **Total SqFt: 2,150**
* **SqFt Heated Source: Public Records** (or whatever option)
* **Total Acreage: 0.27**

---

# D) Show Appliances Included, Pool

### Problem

Not showing.

### Instructions

1. Ensure the show page renders:

   * Appliances Included (list)
   * Pool (Yes/No + type if applicable)

### Safeguards

* Appliances:

  * If stored as array/JSON, render as a clean comma-separated list or bullets.
  * If stored as string, display as-is.
* Pool:

  * If stored as boolean + subtype, display:

    * **Pool: Yes — Screened**
    * or **Pool: No**

### Acceptance Criteria

* **Appliances Included: Refrigerator, Range, Dishwasher**
* **Pool: Yes** (and subtype if applicable)

---

# E) Pets section fixes (labels + conditional fields)

### Problems

* “Pet Types” label should read **Acceptable Pet Types**
* Max Weight + Pet Restrictions missing when Pets = Yes

### Instructions

1. Update label text on show page:

   * Replace `Pet Types` with `Acceptable Pet Types`
2. Add conditional rendering:

   * If `pets_allowed == Yes`, display:

     * Acceptable Pet Types
     * Maximum Weight Per Pet (lbs)
     * Pet Restrictions

### Safeguards

* Treat “Yes” as true across all possible stored values (e.g., `"Yes"`, `1`, `true`).
* If Pets = Yes but a subfield is empty, show **Not Provided** instead of hiding (so you can spot data capture issues).

### Acceptance Criteria

When Pets = Yes:

* **Acceptable Pet Types: Dogs, Cats**
* **Maximum Weight Per Pet (lbs): 50**
* **Pet Restrictions: No aggressive breeds**

---

# F) Desired Sale Price comma formatting

### Problem

Missing comma format.

### Instructions

1. Apply currency formatting to sale price on show page:

   * `$325,000` not `$325000`

### Safeguards

* Format should be consistent across platform:

  * Currency: `$` + comma separators, no decimals unless entered.
* Only format if numeric; if stored as text, sanitize to numeric first or display raw.

### Acceptance Criteria

* **Desired Sale Price: $325,000**

---

# G) Offered Financing/Currency Terms formatting must match Buyer listings

### Problem

Needs to display the header **Offered Financing/Currency Terms:** and then show the selected financing types/questions under it (like Buyer listing).

### Instructions

1. Copy the same **display structure** used in Hire a Buyer’s Agent actual listing view:

   * A section header line:

     * **Offered Financing/Currency Terms:**
   * Then render the selected types beneath it in the correct order.
2. Ensure Seller show page uses the **same partial/component** pattern as Buyer if available.

### Safeguards

* DO NOT show an empty section if user selected none.
* If user selected “Other”, display the “Other” text value (not just the word Other).

### Acceptance Criteria

Example:

* **Offered Financing/Currency Terms:**

  * Cash
  * Conventional
  * Seller Financing (with sub-answers)

---

# H) Services must match create-order + digital enhancements placement

### Problems

* Services not in correct order
* Digital enhancements sub-options must show **immediately under** that service, after Additional Services

### Instructions

1. Stop rendering services from a static display list.
2. Render services in **the same order they appear in the create form**:

   * Use a single canonical “Seller Residential Services Order” array/config (the same concept you’ve used in other flows).
3. When “Provide digital photo enhancements” is selected:

   * Render the enhancement sub-options as nested bullets directly below it.
   * If “Other” enhancement text exists, show it.

### Safeguards

* If services are stored as:

  * `selected_services` array: map them against the canonical ordered list.
  * OR booleans per service: still output in canonical order by checking each flag.
* Prevent duplication if a service appears both in base list and “additional services”.

### Acceptance Criteria

* Services display exactly in the same order as create.
* Digital enhancements appear right under that line, like:

  * Provide digital photo enhancements

    * Basic edits (brightness, contrast, cropping)
    * Twilight conversion
    * Object removal
    * Other: (text)

---

# I) Broker Compensation & Agency Agreement Terms must match Buyer listing format (Seller-specific)

### Goal structure (must match Buyer show style)

Use headings and render fields in this order:

## 1) Broker Compensation:

* Seller’s Broker Purchase Fee:
* Buyer’s Broker Commission Structure:
* Buyer’s Broker Commission Fee:

## 2) Lease Terms:

* Interested in Offering a Lease Agreement:
* Seller’s Broker Leasing Fee:

## 3) Lease-Option Details:

* Interested in a Lease-Option Agreement:
* Compensation for Creating the Lease-Option Agreement:
* Compensation if Purchase Option is Exercised:

## 4) Legal Terms:

* Protection Period Timeframe:
* Early Termination Fee:
* Termination Fee Amount:
* Retainer Fee:
* Retainer Fee Amount:
* Retainer Fee Application:
* Seller’s Broker Share of Retained Deposits:
* Seller Agency Agreement Timeframe:

## 5) Brokerage Relationship:

* Acceptable Brokerage Relationship:

## 6) Additional Terms:

* Additional Terms:

### Instructions

1. Reuse the Buyer listing show template pattern:

   * same headings
   * same “Label: Value” style
   * same spacing/typography
2. Ensure **every one of the Seller fields above is pulled from the saved listing record** and displayed if present.

### Safeguards

* Conditional display rules:

  * Show Lease Terms section only if Lease = Yes (or if leasing fee exists).
  * Show Lease-Option section only if Lease-Option = Yes (or any lease-option compensation exists).
  * For Early Termination Fee / Retainer Fee:

    * If “Yes”, show the amount + application.
    * If “No”, show just the Yes/No line (and hide amount lines).
* Format:

  * Percent strings show as entered (e.g., `3% of Total Purchase Price`)
  * Flat fees display with currency formatting (e.g., `$5,000`)

### Acceptance Criteria (matches your example)

**Broker Compensation:**
Seller’s Broker Purchase Fee: 3% of Total Purchase Price
Buyer’s Broker Commission Structure: Seller to Pay Buyer’s Broker Separately
Buyer’s Broker Commission Fee: 3% of Total Purchase Price

…and so on for each section.

---

# J) Seller’s Current Status missing

### Problem

Not displaying.

### Instructions

1. Add “Seller’s Current Status” to the Seller listing “Property / Seller Details” block.
2. Ensure it maps to the correct stored key.

### Safeguards

* If it’s a dropdown, display the label not the internal value.
* If empty, show Not Provided.

### Acceptance Criteria

* **Seller’s Current Status: Living in the property** (example)

---

## Final Safeguard Checklist (must implement)

* **Single source of truth:** actual listing view renders from the saved listing model only.
* **No silent drops:** if a field is selected/filled, it must render.
* **Consistent formatting helpers:** currency + number formatting used everywhere.
* **Canonical ordered arrays:** Services order must be identical to create form order.
* **Conditional sections:** Lease/Lease-Option/Fees show only when applicable, but never hide required subfields when parent = Yes.
* **Role-safe display:** City/County/State/Zip always visible; full address remains hidden if required.

---

If you paste (or screenshot) the **Seller actual listing show page** and the **Buyer show page section that you want matched**, I can convert the above into a **copy/paste “Replit task list”** with exact “change X → to Y” instructions aligned to your file/component names.
