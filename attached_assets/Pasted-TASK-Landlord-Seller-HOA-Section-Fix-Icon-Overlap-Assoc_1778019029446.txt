TASK: Landlord/Seller HOA Section — Fix Icon Overlap + Association Application Fee Conditional Field

SCOPE:
Offer Listing Full Service create flows only:
- Landlord HOA / Association section
- Seller HOA / Association section if same shared markup issue exists

LOGIN:
Abigailbaschuk@gmail.com
12345678

DO NOT TOUCH:
- Buyer/Tenant flows
- Limited Service files
- Hire Agent flows
- Routes
- Schema
- Validation
- Tab structure
- Documents tab move task

ISSUES:
1. HOA/Association icons overlap labels around:
- Association Application Fee
- What Does the Association Fee Include?
- Association Amenities

2. When selecting “Yes” for Association Application Fee, the same broken overlap/error appears.

ROOT FIX:
Fix the HOA/Association markup and conditional field rendering. Do not move sections to hide the issue.

--------------------------------------------------
PART 1 — FIX ASSOCIATION APPLICATION FEE
--------------------------------------------------

Find:
Association Application Fee

If this is a Yes/No select:
- It must use the same clean select wrapper pattern as working Yes/No HOA fields.
- Icon must be inside the input/select row only.
- No standalone <i class="input-icon ..."></i>.
- No duplicate icons.
- Keep existing wire:model.

If selecting “Yes” reveals an amount/details field:
- The revealed field must be inside its own clean form-group/input-cover.
- Use the platform-standard $ prefix if it is a fee amount:
  <span class="input-group-text-seller">$</span>
  <input type="text" class="form-control" ...>
- Do NOT use has-icon/data-icon dollar sign for currency.
- Ensure exactly ONE input appears.
- No overlapping label/icon.

--------------------------------------------------
PART 2 — FIX SELECT2 MULTI-SELECT ICONS
--------------------------------------------------

Fields:
- What Does the Association Fee Include?
- Association Amenities

Fix:
- Remove any manual:
  <i class="input-icon ... input-icon2"></i>
- Use only:
  class="form-control has-icon select2-multiple"
  data-icon="fa-solid fa-ICON"

Do NOT include:
- input-icon2 in data-icon
- duplicate data-icon attributes
- duplicate select tags

Placeholder should be exactly:
Select

--------------------------------------------------
PART 3 — CLEAN BROKEN MARKUP
--------------------------------------------------

Audit the HOA / Association section for:
- duplicate <select> tags
- duplicate <input> tags
- duplicate data-icon attributes
- standalone input icons
- partial/broken tags
- currency fields mixing has-icon with $ prefix

Fix only the broken markup.

--------------------------------------------------
VERIFICATION
--------------------------------------------------

Test:
/offer-listing/landlord
/offer-listing/seller only if same HOA markup exists there

Confirm:
- No icon overlaps around Association Application Fee
- Selecting Yes for Association Application Fee does not create overlap
- Any revealed fee input displays cleanly
- Association Fee Include field has one icon only
- Association Amenities field has one icon only
- Placeholders show Select
- No console errors
- No Buyer/Tenant/Limited Service files changed

RUN:
php -l on edited files only.

HARD SAFEGUARDS:
- No migrations
- No migrate:fresh
- No schema changes
- No broad refactor
- Stop if the HOA section is shared and changes would affect unrelated flows.