This was only a partial fix.

You fixed:

* 3 model JSON decode issues
* Buyer edit creating duplicate bids

But you did NOT complete the main bid card parity work.

Do not assume the remaining issues are “already fixed” unless you verify them in the active rendered files and show exactly where.

Now do a second pass focused ONLY on the unresolved items below.

Hard rules:

* Do not touch unrelated files
* Do not change listing creation flows
* Do not stop early
* Do not say an issue is already fixed unless you verify it in the active file and rendered output
* For each item below, either fix it or prove with exact file/section why it is already correct

Unresolved items to verify and fix:

1. Buyer Match Score card

* Verify Buyer bid card now shows the same Tenant-style Match Score card
* If missing or visually different, fix it
* Must include same layout, green badge, Services Match box, Terms Match box

2. Seller and Landlord Match Score layout

* Verify Seller and Landlord no longer use old Match Summary style
* Must match Tenant exactly
* If still old, replace them

3. Terms counting

* Verify Additional Details is NOT included in Terms Match
* Verify Landlord can count up to 17 terms when all relevant fields are filled
* Verify Seller residential can count up to 16 terms
* Verify Buyer no longer shows 0/0 and calculates actual filled original terms
* If incorrect, fix helper logic

4. Landlord missing field

* Verify Landlord bid card/private panel displays:
  Tenant's Broker Commission Fee:
  directly after
  Tenant's Broker Commission Structure:
* If missing, add it and include it in term comparison logic

5. Services counting

* Verify 1 checkmark = 1 service across all property types
* Verify digital enhancement sub-options each count individually
* Verify checked “Other” counts as 1
* Verify each custom additional service entry counts as 1
* Verify Buyer no longer shows false 0/0 and false extras
* Verify Seller residential can reach 59 when all applicable options are selected
* Verify Landlord no longer shows false extras
* If wrong, fix helper logic

6. Extra services / extra terms logic

* Verify extras do not increase match %
* Verify matched original items are not mislabeled as extra
* If wrong, fix it

7. Buyer private Broker Compensation panel

* Verify Buyer private compensation / agency agreement section matches Tenant format
* If still legacy layout, replace with Tenant structure using Buyer labels

8. Styling

* Verify Buyer and Landlord lease-option compensation rows are black, not blue
* If still blue, fix styling/classes

9. Buyer action buttons

* Verify Buyer bid view has Accept / Counter / Reject in same position and style as Tenant
* If missing, add them and wire them correctly

10. Seller section order

* Verify Seller order is:
  Broker Compensation & Agency Agreement Terms
  Offered Services
  Additional Terms
  Additional Details
* If not, fix it

11. Upload rendering

* Verify business card and uploaded materials load correctly in active bid views for all roles
* Verify edit mode preserves them
* If not, fix it

12. Canonical score keys

* Verify active views now use canonical keys consistently:
  services_matched_count
  services_baseline_total
* Remove alias dependence if still present

Required output after this pass:
For each of the 12 items above, give:

* status: fixed or already correct
* exact file(s) checked
* exact file(s) changed
* short explanation of what was fixed or verified

Do not give a general summary only.
Give a numbered checklist 1 through 12.
