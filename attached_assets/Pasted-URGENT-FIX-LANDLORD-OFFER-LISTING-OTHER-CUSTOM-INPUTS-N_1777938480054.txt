URGENT FIX — LANDLORD OFFER LISTING “OTHER” CUSTOM INPUTS NOT SHOWING

Scope:
- Landlord Offer Listing create flow only
- Full-Service / Commission-Based path only
- Property Details tab first
- Fix reveal logic only

DO NOT touch:
- Seller, Buyer, Tenant flows
- Hire Agent flows
- Agent Preset flows
- Database schema
- Tab order
- Unrelated styling

Bug:
On /offer-listing/landlord, when selecting “Other” in multiple fields, the custom input box does not appear.

Confirmed from UI:
Other selected but custom input not showing for fields including:
- Heating and Fuel
- Air Conditioning
- Water
- Sewer
- Utilities
- Laundry Features
- Floor Covering
- Security Features
- Possibly Appliances Included, View, Amenities/Property Features, and related Residential/Commercial fields

Expected:
When “Other” is selected, show the matching custom input immediately.
When “Other” is removed, hide the input.
State should remain correct after Livewire re-render/message.processed.

Instructions:

1. Audit current Landlord Property Details “Other” logic.
File:
resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/property-preferences.blade.php

Also inspect wrapper JS in:
resources/views/livewire/offer-listing/landlord/offer-landlord-listing.blade.php

2. Compare against working Seller implementation.
Reference only:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-preferences.blade.php
resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php

3. Fix all Landlord Property Details Other reveal fields using the same stable pattern:
- Initial Blade visibility should be based directly on the bound PHP property value.
- JS Select2 change handler should toggle the custom input immediately.
- Livewire message.processed should re-apply visibility after re-render.
- Avoid relying only on Livewire server round-trip to show the field.
- Avoid Blade d-none + JS conflicts.

Fields to verify/fix:
- appliances_included → other_appliances
- view_preference → other_preferences
- amenities_property_features → other_amenities_property_features
- heating_and_fuel → other_heating_and_fuel
- air_conditioning → other_air_conditioning
- water → other_water
- sewer → other_sewer
- utilities → other_utilities
- laundry_features → other_laundry_features
- floor_covering → other_floor_covering
- security_features → other_security_features
- garage_parking_features → other_garage_parking_features
- building_features → other_building_features
- electrical_service → other_electrical_service
- road_surface_type → other_road_surface_type
- any Residential/Commercial duplicate versions of these fields

4. For each “Other” wrapper:
- Confirm correct id/class selector exists.
- Confirm wrapper is not permanently hidden with style="display:none" without JS override.
- Confirm JS selector matches the actual DOM id/class.
- Confirm multi-select fields use .val() array check:
  Array.isArray(values) && values.includes('Other')
- Confirm single-select fields use value === 'Other'.

5. Verification:
- Open Landlord create flow.
- Select Residential.
- Select “Other” in each relevant Property Details field.
- Confirm custom input appears immediately.
- Change away from “Other.”
- Confirm custom input hides.
- Repeat for Commercial fields.
- Confirm no console errors.
- Confirm Save Draft or Livewire re-render does not hide visible Other inputs incorrectly.

Report:
- Exact root cause
- Files changed
- Fields fixed
- Fields already working
- Any deferred Lease Terms “Other” fields to fix separately