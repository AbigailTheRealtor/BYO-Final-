Please stop refactoring logic and just help me restore the original Income flow for Hire a Buyer’s Agent → Full Service → Income. Right now, on /hire/agent/auction/buyer:

Residential + Commercial work

Income: Tabs 3–7 (Purchasing Terms, Services, Additional Details, Broker Compensation, Buyer Information) are completely blank.

I only see the yellow debug bar at the top. I do not see the green / blue / red debug boxes you said you added.

This means the problem is not the partials – it’s the conditions that decide whether the tab-pane divs and includes render at all.

1) Confirm which conditions are hiding the tab panes

In resources/views/livewire/tenant-agent-auction.blade.php:

Find the markup for the buyer tab panes (Tabs 3–7):

<div class="tab-pane fade" id="purchasing-terms" ...>
    {{-- buyer include here --}}
</div>

<div class="tab-pane fade" id="services" ...>
    {{-- buyer include here --}}
</div>

...


**Around each of those <div class="tab-pane ..."> blocks, check for any @if / @elseif / @endif that reference either:

$property_type

$service_type

or $user_type.

Add a VERY simple debug just inside each tab-pane div, before any conditions, for the BUYER section only:

{{-- TEMP DEBUG: Buyer Income tab visibility --}}
<div style="background:lime; padding:4px; font-size:12px;">
    BUYER TAB RENDERED: {{ $activeTab ?? 'n/a' }} |
    user_type={{ $user_type ?? 'n/a' }} |
    service_type={{ $service_type ?? 'n/a' }} |
    property_type={{ $property_type ?? 'n/a' }}
</div>


Put that at the top of each buyer tab pane (Purchasing Terms, Services, Additional Details, Broker Comp, Buyer Info), just under the <div class="tab-pane ...">.

Clear views + cache and reload:

php artisan view:clear && php artisan cache:clear


Test UI:

Go to /hire/agent/auction/buyer

Choose Buyer → Full Service → Residential → click Tabs 3–7

You should see the lime “BUYER TAB RENDERED” debug in each tab.

Then choose Buyer → Full Service → Income → Tabs 3–7

I currently see nothing, which means those tab panes are not rendered at all.

Confirm in your own test: do you see the lime box for Income or not?

If the lime box does not appear for Income, that proves the entire <div class="tab-pane"> block is being skipped for Income because of some @if condition in tenant-agent-auction.blade.php. That is the bug.

2) Fix the conditions so Income uses its own buyer tabs again

Still in tenant-agent-auction.blade.php:

For each buyer tab pane, make sure the outer condition includes Income. For example, change anything like:

@if ($property_type === 'Residential')
    <div class="tab-pane fade" id="purchasing-terms">
        @elseif ($user_type === 'buyer')
            @include('livewire.hire-buyer-agent.buyer-agent-auction-tabs.commission-based.purchasing-terms')
        @endif
    </div>
@endif


to something that explicitly allows Income:

@if (in_array($property_type, ['Residential', 'Income', 'Income Property']))
    <div class="tab-pane fade" id="purchasing-terms">
        @if ($user_type === 'buyer' && $service_type === 'full_service')
            @include('livewire.hire-buyer-agent.buyer-agent-auction-tabs.commission-based.purchasing-terms')
        @endif
    </div>
@endif


Do the same for Services, Additional Details, Broker Compensation, and Buyer Info tab panes.

Do NOT delete or merge any Income-specific content.
In the buyer tab files:

resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/services.blade.php

.../broker-compensation.blade.php

there is already an Income-specific block like:

@if ($property_type == 'Income' || $property_type == 'Income Property')
    {{-- Income-specific services content goes here --}}
@endif


Leave those blocks intact – just make sure the string matches the actual property_type value.

3) Make the property_type strings match exactly

We already know from your yellow debug bar that when I choose Income, the value is:

property_type="Income"


Please make sure every Income condition in the buyer templates accepts that:

In services.blade.php:

Confirm the Income block checks Income (and optionally Income Property), e.g.:

@if (in_array($property_type, ['Income', 'Income Property']))
    {{-- original Income buyer services --}}
@endif


In broker-compensation.blade.php:

Add an explicit Income case if it’s missing:

@if ($property_type === 'Residential')
    {{-- Residential buyer comp --}}
@elseif (in_array($property_type, ['Income', 'Income Property']))
    {{-- Income buyer comp --}}
@elseif (in_array($property_type, ['Commercial', 'Business']))
    {{-- Commercial/Business buyer comp --}}
@endif


Do not change Income to reuse Residential content. Just make the conditions line up so the existing Income blocks are actually reachable when property_type = 'Income'.

4) After the fix

Remove the temporary lime debug boxes.

Leave the Income-specific services and broker-comp content exactly as originally written, just with corrected @if conditions.

Confirm:

Buyer → Full Service → Residential → Tabs 3–7 show Residential content

Buyer → Full Service → Income → Tabs 3–7 show Income-specific content

Buyer → Full Service → Commercial/Business continue to work.