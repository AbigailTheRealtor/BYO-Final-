Perfect — that Replit message confirms the architecture, and it also explains **why you’re still seeing the Acceptable Property Condition bug** even though **Acceptable Unit Type is fixed**.

### What we know (based on Replit’s findings)

* ✅ **Acceptable Unit Type** was broken because the **tenant shared blades** had **no change handler**, so nothing ever synced to Livewire. They added it → fixed.
* ❌ **Acceptable Property Conditions** is *still not fixed* because **it’s not consistently reaching the server / persisting**, especially in the flows that route through the **Tenant shared component**:

  * Draft resume uses: `TenantAgentAuction.php` + `tenant-agent-auction.blade.php`
  * Edit uses: `TenantAgentAuctionEdit.php` + `tenant-agent-auction-edit.blade.php`

So the fix must be applied to the **tenant shared blades + the tenant component save/update path**, not just buyer files.

---

## Copy/Paste Replit Instructions With Safeguards

### Fix ONLY: Acceptable Property Conditions (condition_prop_buyer)

### Do NOT touch Acceptable Unit Type (already fixed)

**Goal:** In Buyer listings (Income/Residential), the user must be able to select all 4 property condition options in **Create + Draft + Edit**, and they must persist and display on the listing.

### Safeguards

* Do not change anything related to `number_of_unit_type` (already fixed).
* Do not rename DB fields.
* Do not add “No preference is exclusive” logic. User must be able to select all 4.

---

# Step 1 — Fix the correct files (this is the key)

Because Draft + Edit route through the Tenant shared flow, apply the fix here:

1. `resources/views/livewire/tenant-agent-auction.blade.php` (Draft resume)
2. `resources/views/livewire/tenant-agent-auction-edit.blade.php` (Edit)
3. Confirm the Livewire properties exist and are saved in:

   * `app/Livewire/TenantAgentAuction.php`
   * `app/Livewire/TenantAgentAuctionEdit.php`

**Important:** Even if Buyer create works, Draft/Edit will still fail unless these tenant shared files are correct.

---

# Step 2 — Ensure condition_prop_buyer has a real multi-select + correct option strings

In BOTH tenant blade files listed above:

### Confirm the `<select>` is:

* `multiple="multiple"`
* has the correct `id` (ex: `condition_prop_buyer`)
* wrapped in `wire:ignore` (if Select2)

### Confirm option values match EXACTLY:

* `Updated / Renovated`
* `Partially updated (some older finishes OK)`
* `Older but clean & well maintained`
* `No preference (open to any condition)`

**Safeguard:** No variants like “Open to any condition” or “No preference” (short) in any file.

---

# Step 3 — Add a JSON bridge backup (this is likely what’s missing for conditions)

Replit said: condition_prop_buyer has a change handler, but **no JSON bridge backup**.

That matters because in hidden tabs / Livewire rerenders, the Select2 value can disappear or not serialize reliably.

### REQUIRED in BOTH tenant blade files:

1. Add a hidden input JSON bridge (if not already present), ex:

* `condition_prop_buyer_json`

2. In the Select2 change handler, do **dual sync**:

* set Livewire array property
* set JSON bridge property

**Required behavior on change:**

* `values = $('#condition_prop_buyer').val() || [];`
* `@this.set('condition_prop_buyer', values);`
* `@this.set('condition_prop_buyer_json', JSON.stringify(values));`

**Safeguard:** Do not rely only on `@this.set('condition_prop_buyer', data, true)` without the JSON backup.

---

# Step 4 — Force pre-save sync right before submit (covers edge cases)

Even with change handlers, users can sometimes click “Save” without triggering a final change event (especially if the dropdown is still focused).

### REQUIRED:

In the form submit/save handler (or right before calling Livewire save):

* read Select2 values for `condition_prop_buyer`
* push them into Livewire again using `@this.set(...)`
* push JSON bridge too

This is the same concept Replit tried with `syncBuyerSelect2BeforeSave()`, but apply it specifically inside the tenant shared blades that actually handle Draft/Edit.

---

# Step 5 — Server-side safeguard: decode JSON bridge before saving

In BOTH tenant Livewire components (`TenantAgentAuction` and `TenantAgentAuctionEdit`), in the save/update method that persists listing metadata:

### REQUIRED:

If `condition_prop_buyer_json` is present and decodes into an array:

* overwrite `$this->condition_prop_buyer` with that decoded array before saving.

Then normalize safely:

* ensure array
* trim
* unique
* reindex

**Not allowed:**

* clearing the array when “No preference…” is selected
* mapping to empty
* saving as a string

---

# Step 6 — Verify listing display decodes and prints the array

On the listing display template:

* Confirm `condition_prop_buyer` is decoded to an array (if stored JSON)
* Render all selected values

**Safeguard:** Do not hide “No preference…” when other values exist.

---

## QA Test (Must Pass)

Login: `tenant@exp.com`

### Create Buyer Listing

* Select all 4 property conditions → Save → View listing → all 4 show

### Draft Resume flow (critical)

* Save draft with all 4 → reopen draft → all 4 still selected → save → listing shows all 4

### Edit flow (critical)

* Edit listing → all 4 pre-selected → save → listing still shows all 4

---

## One last note (why this is happening)

Your DB showing `condition_prop_buyer: []` means **the server never receives the array** in those flows. That is almost always:

* wrong component handling save (true here), OR
* Select2 not syncing on hidden tabs / rerender (fixed by JSON bridge + pre-save sync), OR
* update/save method decoding the wrong property (fixed by Step 5).

If you want, paste just these three snippets (no full files needed) and I’ll tell you exactly what to change:

1. the `condition_prop_buyer` `<select>` block from `tenant-agent-auction.blade.php`
2. the same block from `tenant-agent-auction-edit.blade.php`
3. the save/update section where those properties are written to DB in `TenantAgentAuctionEdit.php`
