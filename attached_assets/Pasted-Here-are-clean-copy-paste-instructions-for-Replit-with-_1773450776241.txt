Here are **clean, copy-paste instructions for Replit** with **clear safeguards** so nothing else in your system gets broken. I wrote them in the same **structured format you’ve been using for your other fixes.**

---

# REPLIT INSTRUCTIONS WITH SAFEGARDS

## HIRE A SELLER’S AGENT — FIELD REQUIREMENT + DROPDOWN UPDATE

### SCOPE

Make **two targeted UI updates** to the **Hire a Seller’s Agent listing flow only**.

These edits must **NOT affect Buyer, Tenant, or Landlord flows.**

Changes required:

1. **Total Acreage** → remove required status
2. **Heated Sqft** → remove required status
3. **Age-Restricted Community** → replace Yes/No with standardized options used in **Hire a Landlord**

---

# HARD SAFEGUARDS (NON-NEGOTIABLE)

1. **Only modify the Seller flow.**
   Do **NOT edit Buyer, Tenant, or Landlord components.

2. **Do NOT rename database columns.**

3. **Do NOT run migrations.**

4. **Do NOT modify validation logic used by other listing types.**

5. **Do NOT change shared form logic unless it is wrapped in a Seller-only condition.**

6. **Do NOT change any Select2 initialization logic.**

7. **Do NOT modify Livewire state syncing.**

8. **Do NOT touch unrelated fields or tabs.**

---

# STEP 1 — IDENTIFY THE SELLER COMPONENT

Before editing anything confirm the active files for **Hire a Seller’s Agent**.

Typical structure should be similar to:

```
SellerAgentAuction.php
seller-agent-auction.blade.php
seller-agent-auction-tabs/
```

Most likely locations:

```
resources/views/livewire/seller-agent-auction-tabs/property-details.blade.php
```

or

```
resources/views/livewire/seller-agent-auction-tabs/listing-details.blade.php
```

Confirm which partial contains:

```
Total Acreage
Heated Sqft
Age-Restricted Community
```

---

# STEP 2 — REMOVE REQUIRED STATUS

## Total Acreage

Find field:

```
Total Acreage
```

Remove:

```
required
```

Remove validation rule if present:

```
'required'
```

Example change:

### BEFORE

```
<input type="number"
       wire:model="total_acreage"
       required>
```

### AFTER

```
<input type="number"
       wire:model="total_acreage">
```

---

## Heated Sqft

Find field:

```
Heated Sqft
```

Remove:

```
required
```

Remove validation rule if present.

### BEFORE

```
<input type="number"
       wire:model="heated_sqft"
       required>
```

### AFTER

```
<input type="number"
       wire:model="heated_sqft">
```

---

# STEP 3 — REMOVE RED REQUIRED STAR

If the UI shows a red star like:

```
<label>Heated Sqft *</label>
```

Change to:

```
<label>Heated Sqft</label>
```

and

```
<label>Total Acreage</label>
```

Do **not** remove stars from other fields.

---

# STEP 4 — UPDATE AGE-RESTRICTED COMMUNITY FIELD

Current Seller field likely looks like:

```
Yes
No
```

This must match the **Hire a Landlord version**.

Replace options with:

```
Not Age-Restricted
55+ Community
62+ Community
```

---

### BEFORE

```
<select wire:model="age_restricted">
    <option value="">Select</option>
    <option value="Yes">Yes</option>
    <option value="No">No</option>
</select>
```

---

### AFTER

```
<select wire:model="age_restricted">
    <option value="">Select</option>
    <option value="Not Age-Restricted">Not Age-Restricted</option>
    <option value="55+ Community">55+ Community</option>
    <option value="62+ Community">62+ Community</option>
</select>
```

---

# STEP 5 — KEEP DATABASE VALUES SAFE

Do **NOT change the column name**.

The existing column:

```
age_restricted
```

should continue to store the selected string value.

Example stored values:

```
Not Age-Restricted
55+ Community
62+ Community
```

---

# STEP 6 — VERIFY EDIT MODE

Open a **Seller listing edit page** and confirm:

✔ Total Acreage can be empty
✔ Heated Sqft can be empty
✔ Red star removed
✔ Age-Restricted dropdown shows new options
✔ Value saves correctly
✔ Value reloads correctly on edit

---

# FINAL CHECK (IMPORTANT)

Confirm these fields **still display correctly on listing pages**:

If empty they should **not display on the public listing**, consistent with your rule:

> **Fields not filled out should not appear on the listing.**

---

✅ **Expected Result**

Seller listings will now:

• Allow blank **Total Acreage**
• Allow blank **Heated Sqft**
• Show correct **Age-Restricted Community options**
• Match the **Hire a Landlord dropdown structure**

---

If you'd like, I can also show you **one small improvement that will prevent these required-field bugs from happening again across all flows (Tenant, Buyer, Seller, Landlord)** so you stop having to fix them one by one.
