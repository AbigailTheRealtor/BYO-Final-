Below are **Replit instructions with safeguards** to fix the **Hire a Buyer’s Agent → Edit Listing** screen so it looks **identical to the Create Listing screen**, and so **all previously saved values repopulate** (including conditional sub-fields like financing). This is written to match your “draft edit works, but edit listing is off” situation.

---

## Replit Instructions — [tenant@exp.com](mailto:tenant@exp.com)

### Hire a Buyer’s Agent → “Edit Listing” Layout + Data Rehydration Must Match “Create Listing” (Exact UI Parity)

## 🎯 Goal (Non-Negotiable)

When a user clicks **Edit Listing** for a **Hire a Buyer’s Agent** listing:

1. The page must render in the **exact same layout, spacing, field order, and conditional logic** as **Create Listing**
2. Every field that was previously saved must **auto-populate** in the Edit screen (including **conditional sections** and **nested financing terms**)
3. Behavior must match the **Edit Draft** experience (your “Saved Draft” view is the benchmark)

---

# ⚠️ CRITICAL SAFEGUARDS

**Scope**

* ✅ Apply ONLY to **Hire a Buyer’s Agent → Edit Listing**
* ❌ Do not change Seller/Landlord/Tenant flows
* ❌ Do not change database schema unless absolutely required to fix missing saved values (prefer no schema changes)

**No destructive changes**

* ❌ Do NOT mass-update DB rows
* ❌ Do NOT reset/null fields on mount()
* ❌ Do NOT “default overwrite” saved values with empty defaults

**One source of truth**

* ✅ Create Listing UI is the visual standard
* ✅ Edit Listing must use the same Blade partials/components as Create Listing (no duplicated markup)

**Cross-screen consistency**
This fix must apply anywhere “Edit Listing” renders Buyer listings:

* User dashboard edit view
* Admin edit view (if exists)
* Agent-facing edit view (if exists)

---

# ✅ Root Cause (What’s almost certainly happening)

Your **Edit Listing** route is using a **different component/view** than Create Listing (or an older version), so:

* the CSS/markup renders “off”
* conditional sections aren’t triggered because the Livewire properties are not fully hydrated
* nested fields (like financing detail inputs) don’t rehydrate, so they appear empty even though they exist in DB

The fix is: **Edit Listing must use the same Livewire component + same view partials as Create**, and **mount() must fully hydrate every property BEFORE render**.

---

# 1) ⚠️ WHERE THIS MUST UPDATE

**User action:** Dashboard → Hire a Buyer listing → **Edit Listing**
**Target:** The route/controller/Livewire component used for Edit Listing
**Standard to match:** The Create Listing component/view (and the Edit Draft experience)

---

# 2) REQUIRED ARCHITECTURE CHANGE (Most Important)

## ✅ Make “Edit Listing” reuse the Create Listing UI

**Do NOT maintain two separate UIs.**

### Implementation Requirement

* “Create Listing” and “Edit Listing” must render the **same Livewire component** (or same base component) and the same Blade partials.

#### Accepted patterns (pick one)

### Pattern A (Preferred): One component handles both create + edit

* Component: `BuyerListingForm`
* Route:

  * `/buyer/create` → no ID (create mode)
  * `/buyer/{id}/edit` → with ID (edit mode)
* Component has:

  * `$mode = 'create' | 'edit'`
  * `$listingId`

### Pattern B: Wrapper components but same shared view partials

* `BuyerCreateListing` + `BuyerEditListing`
* Both use the same:

  * `resources/views/livewire/buyer/form.blade.php`
  * Shared partials for each section (financing, exchange, etc.)

**Safeguard:** If you find the Edit screen uses a different Blade file than Create, **stop** and refactor so it doesn’t.

---

# 3) FULL REHYDRATION REQUIREMENT (Why fields aren’t showing)

When editing, your Livewire `mount($id)` (or equivalent) must:

### A) Load the listing record

* Validate listing exists + user has permission
* If not found → redirect gracefully (no white screen)

### B) Hydrate ALL properties used by the form

This includes:

* Simple fields (text, selects, numbers)
* Multi-select arrays
* “Other” text fields
* **All Offered Financing/Currency selections**
* **All nested financing detail objects**
* Photos (paths/records)
* Additional Details

### C) Convert DB formats → UI formats

Examples:

* JSON → arrays
* numeric stored values → display value (with commas if UI uses masking)
* booleans → checkbox states

### D) Must hydrate BEFORE render

Use mount() or a dedicated `loadListing()` called from mount().
Do not rely on lazy loading after render.

---

# 4) CONDITIONAL SECTIONS MUST TRIGGER ON EDIT (Financing example)

Your screenshot shows complex financing/terms sections.

### Required behavior:

If the saved listing has:

* Seller Financing selected → show Seller Financing inputs AND populate them
* Assumable selected → show Assumable inputs AND populate them
* Exchange/Trade selected → show Exchange/Trade inputs AND populate them
  …and so on.

### Implementation rule:

After hydrating:

* the “selected financing types” array must already be populated
* the “details” objects must already be populated
  So the UI conditions evaluate TRUE and render properly.

**Safeguard:** Do not clear “details” objects when a type is selected on edit. Only clear when user explicitly unchecks a type and confirms (if you do that).

---

# 5) FIX THE “FORMAT IS OFF” (UI Parity)

This typically happens because Edit Listing uses:

* missing wrapper divs / sections
* different spacing classes
* different field grouping components
* missing “card/section” container markup

### Required fix:

* Make Edit Listing render the **same exact markup** as Create Listing by using the same Blade partial(s).

**DO NOT** “patch spacing” in Edit only. That creates drift.

---

# 6) PHOTO + FILES MUST REPOPULATE ON EDIT

Edit Listing must:

* load photo records/paths from DB
* show thumbnails/links exactly as Create/Draft edit does
* not require re-upload

**Safeguard:** Don’t rely on Livewire temp upload state. Use stored paths/DB records.

---

# 7) ⚠️ “CRITICAL — WHERE THIS MUST UPDATE” (Consistency List)

These exact fixes must apply in **all Buyer edit renders**:

* Listing creation UI (baseline)
* Edit Draft UI (already correct—do not break it)
* Edit Listing UI (this fix)
* Preview/review screens (read-only)
* Agent-facing listing details (read-only)

**Safeguard:** If you share components, this will happen automatically. If you don’t, you will miss one.

---

# 8) TEST PLAN ([tenant@exp.com](mailto:tenant@exp.com))

## A) Visual parity test

1. Login `tenant@exp.com`
2. Create a Buyer listing and fill:

   * Seller financing fields
   * Assumable fields
   * Exchange fields
   * Several numeric currency fields
3. Save listing
4. Click **Edit Listing**
5. Compare side-by-side with Create Listing layout:

   * ✅ same order
   * ✅ same spacing
   * ✅ same section headers
   * ✅ same input widths
   * ✅ same conditional section behavior

## B) Data persistence test

* ✅ Every previously entered value appears
* ✅ Multi-selects show correct chips
* ✅ All conditional sub-fields are filled
* ✅ Photo attachments show

## C) No regression

* Reopen a **Saved Draft** → still perfect
* Create new listing → unaffected

---

# 9) “If you see this, it’s still wrong” quick debug checklist

* If layout differs → Edit uses a different Blade view than Create
* If conditional sections don’t appear → selected arrays not hydrated before render
* If sections appear but are empty → details objects not hydrated / overwritten
* If only some values show → saveDraft stores more fields than saveListing OR edit load logic is incomplete

---

If you paste the **route name** or **component filename** used for Buyer “Edit Listing” (or a screenshot of the file tree showing it), I can rewrite this into **exact file-level steps** like:

* “Open `app/Http/Livewire/...`”
* “Replace mount() with…”
* “Use view `resources/views/livewire/...`”
* “Move markup into a shared partial…”

…but the above is the correct “safeguarded blueprint” to implement without guessing your file names.
