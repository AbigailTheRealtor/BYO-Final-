This is a CRITICAL Buyer bid services audit + fix.

Problem:
The Buyer bid card / private view is NOT showing the exact selected service options that were chosen in the Buyer agent bid form. Some selected services are disappearing on the bid card.

This is a data integrity problem, not just a display problem.

---

## GOAL

Make the Buyer bid card/private view show the EXACT selected options from the Buyer agent bid form.

No selected options should disappear.
No selected options should be silently dropped.
This must be fixed in a way that prevents the same issue from happening again for Buyer listings.

---

## HARD SAFEGUARDS

* Do NOT rename or delete DB columns
* Do NOT change listing creation forms
* Do NOT change service labels in the UI unless absolutely required to match the existing Buyer service source of truth
* Do NOT refactor unrelated bid logic
* Do NOT touch Tenant/Seller/Landlord except only to compare a correct implementation pattern if needed
* Do NOT change score calculation formulas unless required because selected services are being lost before scoring/rendering
* Only modify the active Buyer bid save/load/render pipeline and any minimal shared helper if absolutely necessary

---

## PHASE 1 — FULL AUDIT FIRST (DO NOT CODE YET)

Audit the full Buyer service pipeline from form selection to final bid card rendering.

Identify exactly:

1. Buyer bid form source

* exact file(s) that render the Buyer agent bid form services
* exact field names / keys used for:

  * standard selected services
  * custom/additional services
  * grouped/nested service options if any

2. Buyer bid save path

* exact component/controller/method that saves the Buyer bid services
* exact data shape saved to DB/meta
* verify what is actually stored for the current broken example

3. Buyer bid load path

* exact file(s)/method(s) that load services for the Buyer bid card/private view
* whether they load from:

  * latest saved bid meta
  * transformed helper array
  * hardcoded category arrays
  * filtered catalog arrays

4. Buyer bid render path

* exact file(s) that render the final Buyer bid services on the private bid card
* how they determine which selected options to display
* how they determine extras/missing/matched

5. Root cause
   Determine exactly where selected options are being lost:

* not saved at submit time?
* saved correctly but filtered out at load time?
* dropped because category display arrays don’t match saved values?
* text mismatch / normalization issue?
* service registry mismatch?
* old hardcoded service arrays still being used in the view?
* custom services mixed incorrectly with standard services?
* only services in the master catalog are rendered, causing valid selected options to disappear?

For the exact broken Buyer bid example, print:

* the exact selected options from the Buyer bid form
* the exact values stored in the bid record/meta
* the exact values reaching the final view
* the exact values rendered
* which selected options were lost and where they were lost

Do NOT code until the audit clearly identifies the exact break point.

---

## PHASE 2 — FIX THE PIPELINE

Once the audit identifies the break point, fix it so the final Buyer bid card/private view always shows the exact selected options that were saved in the bid.

Required behavior:

1. Standard Buyer services

* every selected standard service in the bid form must render on the bid card in the correct category

2. Additional / custom services

* every custom additional service entered by the user must render in Additional Services
* no custom additional service should disappear

3. No silent filtering

* do NOT drop valid saved selected options just because the display list is out of sync
* if a saved selected option exists, it must either:

  * render in its proper category
  * or be surfaced in a safe fallback area until mapped
* never silently omit it

4. Prevent future recurrence

* if the issue is caused by duplicated hardcoded service arrays, inconsistent labels, or mismatched category definitions, fix the Buyer flow so the render layer uses the same source of truth as the Buyer bid form
* do not leave a fragile one-off patch that only fixes the current example

---

## PHASE 3 — SOURCE OF TRUTH ENFORCEMENT

For Buyer services, make sure the same source of truth is used for:

* bid form options
* saved values
* bid display categories

If multiple hardcoded service definitions currently exist, identify them and consolidate Buyer to one active source of truth.

This must prevent future cases where:

* selected options appear in the form
* but disappear on the bid card because the display layer uses a different service list

---

## PHASE 4 — VERIFICATION

After the fix, verify with the exact broken Buyer example:

1. Every selected standard service from the form appears on the bid card
2. Every selected additional/custom service appears on the bid card
3. No selected services are missing
4. No duplicate service sections are created
5. Services still appear in the correct categories
6. Match score still uses the same final selected services accurately

Also verify Buyer property types:

* Residential
* Income
* Commercial
* Business
* Vacant Land

So this does not happen again on another Buyer listing type.

---

## REQUIRED OUTPUT

Return a numbered report with:

1. exact Buyer files checked
2. exact Buyer files changed
3. exact root cause found
4. exact point in the pipeline where selected services were being lost
5. confirmation that the final Buyer bid card now shows the exact selected options from the bid form
6. confirmation that custom/additional services also render correctly
7. confirmation that the fix is based on a stable Buyer source of truth and not a one-off patch
8. note any remaining edge cases if found

Do not give a broad summary.
This must be an audit-first, source-of-truth correction for Buyer bid service rendering.
