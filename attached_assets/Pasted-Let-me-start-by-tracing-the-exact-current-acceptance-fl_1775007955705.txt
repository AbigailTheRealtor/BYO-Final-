Let me start by tracing the exact current acceptance flow before making any changes.

Now let me check the counter bid acceptance, the notifications display in the layout, and whether there's a Tenant-specific notification class:

I now have the complete diagnosis. Let me implement all the fixes:

Root Cause:

    accept_bid() does redirect()->back() — Tenant never sees the summary
    No notification is created for the Tenant (listing owner) after acceptance — only the Agent is notified
    accept_counter_bid() has the same two problems

Fix plan: Create a TenantAgentHiredNotification, redirect both acceptance handlers to the summary, and notify the Tenant in both cases.

Now I'll create the Tenant notification class and then update both acceptance handlers:

Now update the controller — both accept_bid() and accept_counter_bid():

Now update accept_bid() to redirect to the summary and notify the Tenant:

Now update accept_counter_bid() the same way:

Now verify the summary tenant_user_id is correctly set so the Tenant can access it:

The tenant_user_id is correctly set from the listing owner. Now let me check the accepted bid summary view has a success flash display, then restart and run a live test:

The main layout doesn't globally show flash messages — the accepted bid summary view needs to show the session success flash itself. Let me add that:

Now let me verify the persistent access links already exist on the listing view for the Tenant, and check the notification routing is correct end-to-end:

Persistent Tenant links already exist on the listing view. Let me also confirm the Bidding Period section has the same:

All links and routing are correctly in place. Let me run a full simulation of the acceptance flow to verify it works:

Both notifications produce correct data with proper summary_link URLs. Let me run the end-to-end tests now:

The correct bid form URL is /tenant/agent/auction/bid/{auctionId}. Let me re-run the test with that:

The Accept button is inside a modal — the test didn't open it first. Let me check the exact location and rerun with a more targeted test using an existing bid:

The Accept button is inside the Private Data Modal. Let me check the accept route and controller, then run a targeted test:

Good. The route is confirmed. Now let me verify the accept_bid method has all our fixes applied:

All fixes are confirmed in the controller. Let me check listing #138 which already has bid #39:

The test passed. The full flow works end-to-end:

    Tenant expanded the bid accordion → opened Private Data Modal → clicked Accept Bid → redirected to /accepted-bid-summary/4 → page shows "Accepted Bid Summary" with all details.

Let me also run a quick check on the summary view to verify the flash message and both notification reads are correct:

The acceptance flow is confirmed working:

    Summary #4 created correctly (tenant=Test User, agent=Abby J S)
    Bid #39 is now accepted = 'accepted'
    Both notifications fired (Tenant notification + Agent notification)

Let me now verify the persistent summary links show correctly on the listing view:

The persistent links show text but no href. Let me investigate the template to find the broken variable:

The test's DOM scan used a[href^="/accepted-bid-summary/"] but Laravel's route() generates full URLs like https://domain.dev/accepted-bid-summary/4. Let me verify the links ARE present with full URLs:

All tests pass. The previous test failure was a false positive — the DOM scanner looked for /accepted-bid-summary/ (relative path) but Laravel's route() helper generates full https:// domain URLs. The links are correctly rendered and functional.

Here's the complete summary of everything that was fixed and verified across this session:
What was fixed and confirmed working
1. Post-acceptance redirect

Before: redirect()->back() sent the tenant back to the listing page after accepting a bid — confusing and unhelpful.
After: The controller now creates an AcceptedBidSummary record, then redirects immediately to /accepted-bid-summary/{id}. ✅ Confirmed by e2e test.
2. Dual notification flow

Before: Only the agent received a notification when a bid was accepted.
After: Both parties get notified:

    Agent → BidAcceptedNotification ("Your bid has been accepted!")
    Tenant → TenantAgentHiredNotification ("You've hired an agent!") — a brand new notification class created for this.

Both notifications include a direct summary_link to the Accepted Bid Summary. ✅ Confirmed database records created correctly.
3. AcceptedBidSummaryService compensation fields

Before: Fields like lease-option fees showed raw codes ('percent', 'flat') instead of readable values.
After: Full dual field name fallback (current→legacy column names), human-readable fee formatting, proper retainer_fee_application labels, and combo fee display.
4. Counter bid preload

Before: Counter bid form didn't prefill broker_fee_timing, days fields, or additional_details_broker.
After: TenantAgentAuctionBidCounter::mount() populates all fields with a 3-level fallback chain.
5. Flash messages on summary view

Before: After redirecting to the summary, no success message appeared.
After: accepted_bid_summary/view.blade.php now shows session('success') and session('info') alerts at the top.
6. Persistent summary links on listing view

The accepted bid card correctly shows "View Accepted Bid Summary", "Tenant: E-Sign Acknowledgement", and "Download Signed PDF" links for the listing owner. ✅ Confirmed clickable and navigating to the correct summary page.