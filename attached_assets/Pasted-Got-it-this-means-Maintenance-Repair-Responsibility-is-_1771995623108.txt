Got it — this means **Maintenance / Repair Responsibility** is being rendered through a **different badge/pill renderer** (or a different partial) than the other fields you fixed.

Here are **targeted Replit instructions** to fix **this exact field** for **Hire a Buyer’s Agent** listings (Live + Snapshot) with safeguards.

---

# Replit Instructions — Hire a Buyer’s Agent

## Fix Remaining Bubble Styling: “Maintenance / Repair Responsibility”

### Applies To

* Login: `tenant@exp.com`
* Flow: Dashboard → **Hire a Buyer’s Agent**
* Must apply to:

  * Live Listing Display
  * Download Listing Snapshot (must match)

---

## Issue

**Maintenance / Repair Responsibility** is still displaying as a **bubble/pill** (badge) even after other “no bubble” fields were fixed.

This indicates this field is likely rendered by:

* a different section block, OR
* a shared component/partial that always applies badge styling, OR
* a “Yes/No + Responsibility” formatter that forces pills.

---

## Required Fix (Buyer Listing Only)

Render this field as **plain text**, not bubble:

**Maintenance / Repair Responsibility:** Seller

✅ plain text
🚫 no badge/pill/chip wrapper

---

## Implementation Steps (Do This Exactly)

### 1) Find ALL Render Locations

Search in the Buyer live listing display files for any of these strings:

* `Maintenance / Repair Responsibility`
* `maintenance`
* `repair_responsibility`
* `maintenance_repair`
* `responsibility`

**You must update every render location** (it may appear in more than one section, e.g., Purchasing Terms and Exchange/Trade).

---

### 2) Identify the Badge/Pill Wrapper Used ONLY Here

When you find the render code, you’ll likely see something like:

* `<span class="badge ...">...</span>`
* `<div class="pill ...">...</div>`
* `@include('...badge...')`
* `renderBadge($value)`
* `formatAsPill($value)`
* `chip($value)`

Replace **only for Buyer listings** and **only for this field** with plain text output.

---

### 3) Buyer-Only Override Condition (Hard Rule)

Add/extend a Buyer-only conditional like:

* If listing type = Buyer AND field key = maintenance/repair responsibility
  → render as plain text

Do **not** change styling globally.

---

### 4) Apply Same Fix to Snapshot Template

If snapshot has a separate template/formatter:

* Search the snapshot template for the same field key/label
* Remove the badge wrapper there too
* Confirm snapshot matches live view

---

## Safeguards

1. **Display-only change** — do not change DB schema or stored values.
2. Buyer-only — do not affect Seller/Landlord/Tenant.
3. Do not remove badge styling globally.
4. Ensure Draft/Edit persistence is untouched (this is rendering only).
5. Snapshot must match live output exactly.

---

## QA Checklist

Using `tenant@exp.com`:

1. Submit Buyer listing with Maintenance/Repair Responsibility = Seller
   ✅ Live listing shows: `Maintenance / Repair Responsibility: Seller` (plain text)

2. Download Listing Snapshot
   ✅ Snapshot shows the same plain text format

---

### Why this keeps happening

This specific field is almost certainly rendered in a **different UI row component** than the others (or a “responsibility badge” helper). Fixing that one renderer/partial is the key.

If you paste the **file path** or a quick screenshot of the **code block** where it renders, I can tell you the exact one-line change to make.
