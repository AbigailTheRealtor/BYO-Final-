Please apply Task #966 exactly as scoped.

Task:
Fix Hire Landlord Save Draft behavior so it matches the working Seller draft flow.

Affected flow:
- /hire/agent/auction/landlord
- /hire/agent/auction/landlord/{draftId}

Shared component:
app/Http/Livewire/TenantAgentAuction.php

Critical safeguards:
- Do NOT touch the legacy LandLordAgentAuction.php component.
- Do NOT touch Hire Agent Direct flows.
- Do NOT touch Offer Listing flows.
- Do NOT touch initializeLimitedService().
- Do NOT rewrite the shared component architecture.
- Do NOT change Seller, Buyer, or Tenant behavior unless audit proves absolutely necessary.
- Do NOT change schema, migrations, routes, or validation rules unless audit proves they are the direct cause.
- Minimal targeted landlord-only fix.

Working reference:
Use Seller Save Draft behavior as the baseline reference.

Problem:
Landlord Save Draft silently fails:
- no visible success message
- draft appears not to save/reload
- likely failure inside:
  - validateOnlyFilledFields()
  - saveAllMetadata()
  - loadDraft()
  - or swallowed exception handling inside saveDraft()

Read-only audit first:

1. Trace Seller Save Draft path completely
Document:
- which saveDraft() branch runs
- validation flow
- metadata save flow
- success message/flash/toast
- listingId update behavior
- draft modal appearance
- loadDraft() hydration behavior

2. Trace Landlord Save Draft path completely
Compare against Seller:
- validateOnlyFilledFields()
- saveAllMetadata()
- loadDraft()
- landlord-specific meta persistence
- landlord-specific hydration
- exception handling
- success toast/message flow
- listingId update flow

3. Audit validateOnlyFilledFields() specifically for landlord
Read the entire method carefully.

Check for:
- required rules incorrectly firing for landlord
- array/string mismatches
- null assumptions
- landlord-only fields failing validation
- conditional branches missing landlord handling
- validation exceptions swallowed silently

Compare landlord path directly against seller path.

4. Audit saveAllMetadata() landlord coverage
Compare:
ALL landlord-specific component properties
vs
ALL landlord saveMeta() calls.

Specifically verify landlord fields such as:
- landlord_broker_*
- landlord_approval_conditions
- additional_landlord_lease_terms
- lease-related fields
- landlord compensation fields
- landlord property fields
- landlord-specific arrays/multiselects

Identify:
- missing saveMeta() calls
- incorrect meta keys
- wrong array handling
- landlord properties never persisted

5. Audit loadDraft() landlord hydration completeness
Verify:
- every landlord field saved by saveAllMetadata()
- is rehydrated correctly in loadDraft()

Identify:
- fields saved but never reloaded
- incorrect landlord meta key lookups
- wrong property assignments
- landlord arrays not restored correctly

6. Fix ONLY the root cause(s) identified
Apply minimal targeted fixes only.

Possible fixes:
- landlord-aware validation guard
- missing saveMeta() calls
- missing landlord hydration assignments
- landlord array casting fix
- exception visibility improvement
- proper listingId update after first save
- success flash/toast/browser event restoration

Do NOT change unrelated role logic.

7. Improve saveDraft() QA visibility
If saveDraft() currently swallows landlord exceptions silently:
- surface the error visibly during QA
- use the same visible messaging pattern Seller uses
- do not leave silent failures

Required behavior after fix:
- Seller Save Draft continues working unchanged.
- Landlord Save Draft behaves identically to Seller:
  - visible success confirmation
  - DB persistence
  - listingId update
  - draft modal visibility
  - direct draft URL loading
  - field hydration

Done looks like:
1. /hire/agent/auction/seller
   - Save Draft works (reference baseline).

2. /hire/agent/auction/landlord
   - Save Draft shows:
     “Draft saved successfully (Version X)...”
     or same Seller success pattern.
   - draft persists.

3. Database:
   - landlord_agent_auctions record exists
   - is_draft=true
   - user_id=current user

4. listingId updates after first save.

5. Draft picker modal shows landlord drafts.

6. /hire/agent/auction/landlord/{draftId}
   loads and hydrates the draft correctly.

7. /hire/agent/auction/landlord/38
   loads correctly if owned by the user.

8. Seller, Buyer, and Tenant draft save/load still work.

9. No silent save failure remains.

10. git diff only touches:
- TenantAgentAuction.php
- tenant-agent-auction.blade.php
- landlord draft model only if audit proves necessary
- routes/web.php only if audit proves absolutely necessary