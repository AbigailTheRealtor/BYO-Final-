Replit Instructions — REQUIRED FIELD LOGIC IS REVERSED + Buyer Listing Still Not Submitting or Routing Correctly
🚨 Critical Validation Bug (Must Fix First)

Current (Incorrect) Behavior

City is REQUIRED

County is NOT REQUIRED

Expected (Correct) Behavior

✅ County MUST be REQUIRED

❌ City must NOT be required

This applies to Hire a Buyer’s Agent listings.

County is required because it is used for matching, filtering, and legal/geographic relevance.
City is optional because Buyers may be open to multiple cities within a County.

Current Status (What Works vs What’s Broken)
✅ What Is Working

Both Submit buttons are now clickable (progress).

❌ What Is Still Broken

Buyer listings do not submit successfully (no listing appears after submit).

Buyer listings do not route to the “Hiring a Buyer’s Agent” tab on the website.

The system appears to either:

silently fail submission, or

save incorrectly (wrong role/type/tab), or

fail backend validation without surfacing errors.

What You Must Investigate (Backend + Frontend)
A) Fix Required Field Logic

Remove required validation from City

Add required validation to County

Ensure frontend and backend validations MATCH (no mismatches)

B) Confirm Submit Request Is Actually Completing

When clicking Submit, confirm:

request is sent

backend validation passes

database write succeeds

response returns success

user is redirected or shown confirmation

No silent failures allowed.
If submission fails, the UI must show the reason.

C) Verify Buyer Listing Routing

Expected behavior after successful submit:

listing saves as Buyer listing type

listing appears under Hiring a Buyer’s Agent tab

listing uses Buyer fields, not Tenant fields

If it appears under Tenant or nowhere at all, the mapping is still wrong.

Required Logging (Temporary)

Add temporary logging for:

submit request received

validation result (pass/fail + message)

listing type assigned (Buyer vs Tenant)

DB write success/failure

returned response code

Remove logs after fix is verified.

NON-NEGOTIABLE SAFEGUARDS

DO NOT:

rename or delete database columns

change existing field keys

alter UI layout, text, tooltips, placeholders, or icons

route Buyer listings into Tenant logic “temporarily”

create duplicate tables or models

DO:

fix validation correctly

fix routing at the source

preserve existing Tenant + Landlord functionality

Acceptance Criteria (How This Is Considered Fixed)

✅ County is required
❌ City is optional
✅ Submit completes successfully
✅ Listing appears under Hiring a Buyer’s Agent tab
✅ Listing fields match Buyer form inputs
✅ Clear error shown if submission fails

Provide:

screenshot/video of successful submit

confirmation of root cause + fix

⚠️ Urgent Performance + Refund/Credit Request

This issue stems from previous incorrect implementation and repeated failed fixes, resulting in significant delays and rework.

I am requesting:

priority handling

faster turnaround

refund or account credit for time lost due to Replit errors

Please confirm:

root cause

fix applied

refund or credit issued