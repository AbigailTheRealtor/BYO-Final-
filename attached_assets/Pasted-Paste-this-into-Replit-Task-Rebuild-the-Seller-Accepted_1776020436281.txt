Paste this into Replit:

**Task: Rebuild the Seller Accepted Bid Summary so it matches the Landlord Accepted Bid Summary structure/quality, but uses Seller-specific fields and Seller-specific service categories**

## Goal

The **Seller Accepted Bid Summary** is currently wrong in multiple ways:

* service items are not grouped under the correct labeled categories
* Agreed Compensation & Agency Terms is using the wrong format/raw values
* overall section structure/layout does not match the cleaner accepted-summary format already working for **Landlord**
* the Seller summary should visually and structurally follow the **Landlord Accepted Bid Summary pattern**, while using **Seller fields**, **Seller compensation wording**, and **Seller service categories**

## Hard safeguards

1. **Do not change database schema**
2. **Do not change routes**
3. **Do not change accept/counter/reject logic**
4. **Do not change signing flow**
5. **Do not change Landlord accepted summary behavior**
6. **Do not change Buyer/Tenant unless a truly shared helper must be extended safely**
7. **Do not touch live bid-card behavior unless required to reuse an existing formatter**
8. **Do not flatten or relabel Seller services incorrectly**
9. **Do not do a broad redesign of all accepted summaries**
10. Make the **smallest surgical Seller-only fix** possible

---

## Read-only audit first

**Do not edit yet.**

First, trace and report the exact files/methods for all of these:

### A. Seller Accepted Summary render path

Find:

1. exact file that builds the **Seller Accepted Bid Summary HTML**
2. exact method that builds the Seller sections
3. exact method/array supplying:

   * seller service categories + items
   * seller agreed compensation & agency terms
   * seller additional details

### B. Landlord Accepted Summary reference path

Find:

1. exact file/method that builds the **Landlord Accepted Summary**
2. exact section structure used there
3. exact formatting/helper methods Landlord uses for:

   * grouped services
   * display-ready compensation/agency terms
   * section ordering/layout

### C. Seller view-bid formatting source of truth

Find the exact file/method that already renders Seller bid info correctly for:

1. Seller service category grouping
2. Seller compensation/agency display strings
3. Seller property-type-specific services

**Report the file names + methods before coding.**

---

## Root objective

The Seller Accepted Summary should **replicate the Landlord Accepted Summary structure and cleanliness**, but with:

* Seller title/party labels
* Seller listing details
* Seller grouped services
* Seller display-ready compensation/agency terms
* Seller additional details
* Seller acknowledgement labels

This means:

* use **Landlord accepted-summary structure as the template**
* use **Seller data + Seller field labels + Seller categories**
* do **not** copy Landlord field names into Seller

---

## What must be fixed

# 1) Seller services must be grouped under the correct Seller categories

Right now the Seller accepted summary is dumping services incorrectly.

It needs to match the proper Seller grouped-service experience, similar to how Landlord accepted summary is cleanly structured.

### Required behavior

Seller services in the accepted summary must render under the proper Seller category headings, using the correct Seller service catalog for the applicable property type.

Examples of Seller-style category behavior:

* services should appear under their proper Seller category headings
* “Other” or “Additional” services must stay in the appropriate additional section, not mixed into standard categories
* category labels must be the Seller labels, not Buyer/Landlord/Tenant labels
* ordering should follow the Seller service structure already used elsewhere in the app

### Important

Use the same Seller service grouping source already used in Seller bid rendering if one exists.
Do not create a second inconsistent grouping map if avoidable.

---

# 2) Seller Agreed Compensation & Agency Terms must use display-ready values

Right now Seller accepted summary appears to be using raw/generic values.

It needs to follow the same pattern that was fixed for Buyer and Landlord:

* accepted summary should use the same human-readable display values as Seller view bid/private terms
* not raw internal enums
* not generic type labels when a composed value should be shown

### Examples of what must be corrected

* combo structures should show their real composed display, not separate awkward raw fragments unless that is the intended Seller display
* yes/no values should be `Yes` / `No`
* timeframe values should be display-ready like `30 days`, `6 Months`, `1 Year`
* percentage/flat-fee combinations should match the Seller view bid wording
* relationship labels must remain human-readable
* retainer / termination / protection values must be formatted consistently

### Critical rule

The Seller Accepted Summary Section 4 must use the **same display-ready formatting logic** as the Seller View Bid/private terms display.

Do not leave Seller accepted summary on a separate raw-value rendering path.

---

# 3) Seller accepted-summary layout must mirror Landlord accepted-summary structure

The Seller accepted summary should use the same **clean section layout pattern** as Landlord, including the same style of organization.

Use Landlord accepted summary as the structural reference for:

* section ordering
* card/section presentation
* grouped services rendering style
* compensation section layout style
* details/notice/signature section consistency

But keep Seller-specific content.

### Meaning

Replicate the **Landlord accepted summary format**, not the Landlord field set.

So Seller should still say Seller, not Landlord.
Seller sections should still use Seller fields.
Seller services should still use Seller service categories.

---

## Required final Seller structure

Use Landlord accepted summary as the model, but Seller-specific.

Expected Seller summary should be structured like:

1. **Parties**

   * Seller
   * Agent

2. **Listing Details**

   * property details relevant to Seller listing

3. **Services**

   * grouped by correct Seller service categories
   * correct Seller property-type-specific service headings
   * additional/other services in correct place

4. **Agreed Compensation & Agency Terms**

   * display-ready Seller values
   * correctly formatted timeframes, Yes/No, currency, percentages, combo displays

5. **Additional Details**

   * Seller additional details
   * Agent additional details, if applicable

6. **Important Notice**

   * same accepted-summary notice pattern as Landlord if that is the shared accepted-summary design

7. **Platform Referral Disclosure**

   * same accepted-summary disclosure pattern as Landlord, adjusted only if Seller wording differs by role

8. **Signature Acknowledgement**

   * Seller acknowledgement
   * Agent acknowledgement

Do not remove existing required legal/disclosure/acknowledgement content. Just make Seller align structurally with Landlord if that is the intended accepted-summary standard.

---

## Implementation approach

### Preferred approach

Use the **Landlord accepted summary section/template structure** as the reference and apply that structure to Seller.

Then wire in:

* Seller service grouping source
* Seller compensation display formatter
* Seller labels/details

### Strong preference

If Seller already has:

* a service grouping helper
* a compensation display helper
* a property-type service catalog formatter

reuse those.

If Seller formatting currently exists only in Blade/view logic, extract only the minimal reusable helper needed and use it in the accepted summary.

---

## Do not do

* do not hardcode Seller services manually into the accepted summary
* do not create a one-off map that can drift from Seller bid rendering
* do not copy Landlord raw fields into Seller
* do not patch only the labels while leaving wrong data underneath
* do not redesign the whole accepted-summary system unless absolutely required

---

## Formatting rules to preserve

Use the same normalization rules that worked for Buyer/Landlord:

### Boolean-like values

* `yes` -> `Yes`
* `no` -> `No`

### Timeframes

* `30` -> `30 days` when appropriate
* `6_months` or equivalent -> `6 Months`
* `1_year` or equivalent -> `1 Year`

### Currency

* `$323`
* `$1,000`
* `$5,000`

### Percentages

* `3%`
* `4%`

### Combo/composed fee displays

If Seller view bid shows a composed display like:

* `3% + $323`
  or similar,
  the accepted summary should use that same Seller display logic.

---

## QA checklist

After patching, test Seller accepted summary against the Seller view-bid/private terms display and Landlord accepted-summary structure.

### Services QA

1. Services appear under the **correct Seller category headings**
2. No Buyer/Landlord/Tenant category labels leak into Seller
3. Additional/Other services appear in the correct area
4. Service order matches Seller catalog order

### Compensation QA

1. No raw internal values like lowercase `yes`
2. No raw generic values where a composed display should appear
3. Protection period shows units like `30 days`
4. Agency timeframe is display-ready
5. Combo fee displays match Seller bid view wording
6. Brokerage relationship remains human-readable

### Layout QA

1. Seller accepted summary section structure mirrors Landlord accepted summary
2. Important Notice / Referral Disclosure / Signature sections align with standard accepted-summary format
3. Seller acknowledgement labels are correct
4. Summary still downloads/renders correctly
5. Sign flow still works

### Regression QA

1. Landlord accepted summary remains unchanged
2. Buyer accepted summary remains fixed
3. Seller view bid remains unchanged
4. No route/DB/signature-flow regressions

---

## Output required from you

Before editing, report:

1. exact Seller accepted-summary file and method
2. exact Landlord accepted-summary file and method being used as structure reference
3. exact Seller service grouping source file/method
4. exact Seller compensation display source file/method
5. minimal implementation plan

After editing, report:

1. exact files changed
2. exact methods changed
3. root cause
4. how Seller now gets:

   * correct grouped services
   * correct display-ready compensation values
   * Landlord-style accepted-summary structure
5. confirmation that no schema/routes/flow changed

## Stop rule

If Seller accepted summary cannot safely be aligned to the Landlord structure without touching shared architecture broadly, stop and report the exact blocker before making wider changes.

The main thing here is you are not asking for a tiny formatting patch anymore. You are asking Replit to make **Seller accepted summary use the Landlord accepted-summary architecture as the reference standard**, while keeping Seller-specific data and labels.
