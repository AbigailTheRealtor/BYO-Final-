Task: Audit Seller Create Offer Listing Fields vs Seller Listing View Display

Scope:
FULL SERVICE Create Offer Listing for Seller only.

Create route:
- /offer-listing/seller

Listing view route example:
- /seller/agent/auction/view/38

Problem:
The Seller listing view is missing many fields that were filled out during Create Offer Listing. We need a full audit comparing what the Seller fills in during creation versus what actually appears on the listing view.

Do NOT touch:
- Hire Agent flows
- Buyer/Tenant/Landlord flows
- Limited Service / legacy flows
- Database schema
- Routes
- Migrations
- Validation
- Submit logic unless audit proves display data depends on submit/save bug

Goal:
Every Seller Create field should be traced through:
Form field → Livewire property → saved meta/database value → view controller/data loader → listing view blade

Likely files:
- app/Http/Livewire/OfferListing/Seller/SellerOfferListing.php
- resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php
- resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/*.blade.php
- app/Http/Controllers/AgentController.php
- resources/views/agent/offer-listing-view.blade.php
- Any model/meta helper used for Seller offer listing view

Audit steps:
1. Open all Seller Create Full Service tab partials.
2. List every visible Seller field by tab/section.
3. For each field, identify:
   - field label
   - Livewire property
   - saved meta key/database column
   - whether it saves correctly
   - whether view controller loads it
   - whether listing view renders it
4. Use listing ID 38 as a real example:
   - compare saved values for listing 38 against what appears at:
     /seller/agent/auction/view/38
5. Mark each field status:
   - PASS: filled value saves and displays
   - SAVE ONLY: value saves but does not display
   - DISPLAY ONLY: view expects a value not collected in create form
   - BROKEN FORMAT: displays but wrong format/type
   - NOT SAVING: field appears in create but value is not saved
   - INTENTIONALLY HIDDEN: document reason

Create report:
.local/audit-reports/seller-create-vs-view-field-display-audit.md

Report must include:
- Route tested
- Listing ID tested: 38
- User/test account used
- Files inspected
- Field-by-field table
- Missing fields list
- Root cause category for each missing field:
   1. not saved
   2. saved under different key
   3. controller does not load it
   4. view does not render it
   5. formatting/helper issue
   6. intentionally hidden
- Recommended fixes

After audit:
Fix confirmed Seller display gaps only.

Preferred fixes:
- If value saves but does not load:
  add missing meta key to controller/data mapping.
- If value loads but does not display:
  add missing render block in listing view blade.
- If value saves under one key but view expects another:
  support the saved canonical key without renaming old data.
- If array/multiselect:
  render as comma-separated readable labels.
- If currency:
  format with dollar formatting.
- If Yes/No:
  render as Yes/No.
- If empty:
  hide the row unless current view pattern intentionally shows blanks.

Important:
- AI Questions / chatbot-only fields should remain hidden unless already intended for public display.
- Do not rename meta keys unless absolutely necessary.
- Do not remove existing display fields.
- Do not change Create form wording or validation.

Acceptance Criteria:
- Audit report exists at:
  .local/audit-reports/seller-create-vs-view-field-display-audit.md
- Listing view for ID 38 shows all filled Seller fields unless intentionally hidden.
- All confirmed SAVE ONLY Seller fields are fixed.
- No Hire Agent files touched.
- No Buyer/Tenant/Landlord files touched.
- No Limited Service files touched.
- No schema/route/migration changes.

Verification:
1. Show files changed.
2. Confirm no out-of-scope files changed.
3. Open /seller/agent/auction/view/38.
4. Confirm missing Seller fields now display.
5. Confirm arrays, currency, booleans, and dates format correctly.
6. Confirm empty fields do not create noisy blank rows.

Final Replit response must include:
- Root causes found
- Audit report path
- Files changed
- Fields fixed
- Fields intentionally hidden
- Verification results