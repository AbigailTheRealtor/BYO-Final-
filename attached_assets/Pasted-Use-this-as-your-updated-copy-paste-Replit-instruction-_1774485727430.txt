Use this as your updated **copy-paste Replit instruction block**:

```text
TASK: Fix the live Buyer Agent bid form so the “Broker Compensation & Agency Agreement Terms” section is prefilled exactly from the actual Buyer listing and renders in the same current format as the Buyer listing create/edit form.

LIVE ROUTE TO TRACE FIRST
- /buyer/agent/auction/bid/{id}

PRIMARY ISSUE
The previous Buyer fix did not work.
Current symptom:
- Buyer bid page still shows the Buyer compensation section mostly blank/default
- It is NOT prefilling from the actual Buyer listing data
- It does NOT visually/functionally match the current Buyer listing create/edit Broker Compensation & Agency Agreement Terms form
- The bid form should look and behave like the Buyer listing’s current broker compensation section, just preloaded into the agent bid flow

GOAL
When an Agent bids on a Buyer listing:
1) The Buyer Broker Compensation & Agency Agreement Terms section must load the exact values the Buyer entered on the listing
2) The bid form section must use the current Buyer create/edit format exactly
3) The bid form must support the correct property type version:
   - Residential
   - Income
   - Commercial
   - Business
   - Vacant Land
4) If editing an existing bid draft, the saved bid values should load first
5) If creating a new bid, the listing’s Buyer values should prefill automatically
6) The bid form must behave like the working Tenant bid pattern where listing values auto-populate into the bid form

CRITICAL SAFEGUARDS
- FIRST trace the exact live route/component/view chain for /buyer/agent/auction/bid/{id}
- Do NOT guess
- Do NOT edit old/orphaned Buyer files
- Do NOT rename DB columns
- Do NOT delete existing logic before confirming replacement works
- Do NOT modify Tenant, Seller, or Landlord bid flows except to reference working logic
- Do NOT change unrelated tabs
- Keep this scoped to Buyer bid hydration + Buyer compensation rendering only
- If shared files are used, make the fix Buyer-safe only
- If the live bid route is not using the expected component, STOP and report the exact live files before editing

IMPORTANT ROOT-CAUSE FOCUS
This is no longer just a “label mismatch” problem.
The real problem is likely one or more of these:
1) Buyer bid form is not hydrating from the Buyer listing record
2) Buyer bid form is using wrong field keys
3) Buyer create/edit and Buyer bid use different property names / JSON paths
4) Listing values exist, but the bid form component is not mapping them into the live inputs
5) Shared bid code is initializing blanks/defaults after listing hydration and wiping out the prefill
6) The bid view is not using the same current Buyer compensation partial/schema as create/edit

STEP 1 — TRACE THE LIVE BUYER BID ROUTE EXACTLY
Starting from:
- /buyer/agent/auction/bid/{id}

Identify and report the actual live:
- route definition
- controller or Livewire component
- Blade/view file
- Buyer Broker Compensation tab partial/component
- model/data source used to load the listing being bid on
- model/data source used to load saved bid draft values

Do not proceed until these are confirmed.

STEP 2 — TRACE THE LIVE BUYER CREATE/EDIT SOURCE OF TRUTH
Find the ACTUAL live Buyer create/edit implementation that currently works for Broker Compensation & Agency Agreement Terms.

Use the live Buyer create/edit files as the source of truth for:
- field order
- field labels
- placeholders
- tooltips
- conditional fields
- property-type-specific structure
- exact field names / model keys / JSON keys

IMPORTANT:
Do not copy only the HTML.
Also trace the actual data keys used in Buyer create/edit so the bid form uses the same keys.

STEP 3 — COMPARE FIELD KEYS BETWEEN BUYER CREATE/EDIT VS BUYER BID
Do a field-by-field mapping comparison between:
A) Buyer create/edit broker compensation fields
B) Buyer bid broker compensation fields

Find where the keys diverge.

Examples of likely issues to check:
- buyer_broker_commission_structure vs broker_commission_structure
- buyer_broker_purchase_fee_type vs buyer_purchase_fee_type
- lease fields stored under a nested object in listing but flat properties in bid
- retainer / termination / agency timeframe keys mismatched
- property-type-specific fields stored differently than the bid expects

Create a strict mapping so the Buyer bid form reads the same keys the Buyer listing actually saved.

STEP 4 — FIX HYDRATION PRIORITY
Implement or repair the hydration order for Buyer bids:

PRIORITY ORDER:
1) If editing/resuming an existing Buyer bid draft:
   load saved bid values first
2) Else if creating a new bid:
   load the actual Buyer listing’s Broker Compensation & Agency Agreement Terms values
3) Else:
   blank/defaults only when no source data exists

IMPORTANT:
Do not allow empty/default initialization later in the component lifecycle to overwrite loaded listing values.

Check for:
- mount()
- hydrate()
- booted()
- updated hooks
- tab initialization methods
- reset() calls
- property defaults assigned after load
- JS/alpine init wiping bound values
- form state being reinitialized when tab opens

STEP 5 — MAKE THE BUYER BID FORM RENDER THE CURRENT BUYER FORMAT
Update the live Buyer bid compensation tab so it uses the same current Buyer create/edit schema and layout.

It must match:
- same heading
- same field labels
- same input order
- same conditional child fields
- same placeholders
- same tooltips
- same property-type-specific version

The goal is:
When viewing Buyer create/edit and Buyer bid side-by-side, the Broker Compensation & Agency Agreement Terms section should be the same structure, with the bid version prefilled from listing data.

STEP 6 — PROPERTY-TYPE-SPECIFIC BUYER SUPPORT
Confirm the Buyer bid loads the correct Buyer compensation version for:
- Residential
- Income
- Commercial
- Business
- Vacant Land

If some property types intentionally share the same compensation schema, that is fine only if it matches the current Buyer create/edit implementation exactly.

Do not accidentally force all Buyer property types into one outdated generic version.

STEP 7 — VERIFY PREFILL ACTUALLY REACHES THE INPUTS
Do not stop at setting backend properties.
Confirm the values actually render in the form inputs on the live bid page.

Check for:
- wire:model vs property mismatch
- nested array binding not matching backend structure
- select options not setting selected state because values/types differ
- numeric/string mismatch preventing select prefill
- conditional fields hidden because parent value is not being set correctly
- Blade using old variable names while component has new ones

The page should visually load the Buyer values immediately when the agent opens the bid page.

STEP 8 — KEEP VIEW / SUMMARY CONSISTENT
After fixing the bid form prefill, confirm the downstream view pages still show the data correctly:
- Buyer bid view page
- Buyer bid edit page
- draft/save/reopen flow

Do not break currently working view rendering.

STEP 9 — NO REGRESSION
Do not break:
- Tenant agent bid prefill
- Seller bid flow
- Landlord bid flow
- other Buyer tabs
- submit / save draft / resume bid

If shared code must be touched, branch by role so Buyer changes do not impact other bid types.

STEP 10 — REQUIRED TESTS
Run these exact tests:

TEST A — Buyer Residential New Bid
1) Create or use a Buyer Residential listing with Broker Compensation fully filled in
2) Open /buyer/agent/auction/bid/{id}
3) Confirm all Buyer compensation values are prefilled
4) Confirm the section visually matches current Buyer create/edit format

TEST B — Buyer Income New Bid
Repeat for Income

TEST C — Buyer Commercial New Bid
Repeat for Commercial

TEST D — Buyer Business New Bid
Repeat for Business

TEST E — Buyer Vacant Land New Bid
Repeat for Vacant Land

TEST F — Existing Buyer Bid Draft
1) Start a bid
2) Modify some compensation values
3) Save draft/progress
4) Reopen bid
5) Confirm saved bid values load first and are not overwritten by listing values

TEST G — View/Edit Consistency
1) Submit or save a bid
2) Open buyer/agent/auction/view/{id}
3) Open buyer/agent/auction/edit/{id}/buyer
4) Confirm values remain consistent across bid form, saved record, and view/edit screens

TEST H — No Regression
1) Open a Tenant bid and verify it still prefills correctly
2) Confirm Seller/Landlord bid flows were not unintentionally changed

SUCCESS CRITERIA
- Buyer bid form now prefills from the actual Buyer listing on first load
- Existing bid draft values take precedence when editing
- Buyer bid Broker Compensation & Agency Agreement Terms matches the current Buyer create/edit format exactly
- Correct Buyer property-type-specific version loads
- Values render visibly in the inputs, not just in backend state
- No regression to Tenant, Seller, or Landlord bid flows

REQUIRED REPORT BACK
After completion, report:
1) Exact live route/component/view chain for /buyer/agent/auction/bid/{id}
2) Exact live files changed
3) Exact Buyer create/edit files used as the source of truth
4) The exact root cause found for why the prefill failed
5) The exact field/key mismatches fixed
6) How hydration priority was implemented
7) Which Buyer property types were tested successfully
8) Confirmation that Tenant/Seller/Landlord flows were not changed
```

The key here is that they need to stop treating this like a styling fix and trace the **actual hydration + field-key mismatch** between Buyer create/edit and Buyer bid.
