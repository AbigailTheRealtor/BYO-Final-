Update the new Tenant bid/detail **Agent Value Layer** wording and styling only. Do not change the underlying scoring logic.

### Scope

Tenant bid/detail view only for this step.

### Goal

Keep the current Match Score behavior exactly as-is, but make the extra-services line read more polished and feel more premium.

### Required wording change

Change:

`⭐ +1 Extra Services Offered`

to the format:

`⭐ Extra Value Added: 1 Service`

and pluralize correctly when needed:

* `⭐ Extra Value Added: 1 Service`
* `⭐ Extra Value Added: 2 Services`

Use the existing extra-services count already being calculated. Do not create a new count or new comparison pipeline.

### Transparency note

Keep the current transparency explanation/tooltip behavior, or add a tooltip if it is not already present, making clear that:

> Extra services were included by the Agent beyond the Tenant’s original request. These do not increase the match score but may provide additional value.

### Styling guidance

Refine the visual hierarchy slightly so the Agent Value Layer feels like a bonus indicator, not the main metric.

Requirements:

* keep the star/gold accent
* slightly reduce visual weight compared to the main Match % and Services Match heading
* keep it clearly visible
* do not overpower the score or the missing-services warning
* preserve desktop and mobile readability

### Important rules

* Do **not** change Match Score %
* Do **not** change Services Match %
* Do **not** change Terms Match %
* Do **not** change extra/missing calculations
* Do **not** change scoring logic
* Do **not** change Buyer, Seller, or Landlord yet unless a clearly shared presentation component is the direct root cause and can be safely updated without side effects

### Verification

After the change, verify:

* extra count value remains exactly the same as before
* the new wording pluralizes correctly
* the star line only appears when extra count > 0
* missing services still display normally
* the updated styling looks correct on desktop and mobile
