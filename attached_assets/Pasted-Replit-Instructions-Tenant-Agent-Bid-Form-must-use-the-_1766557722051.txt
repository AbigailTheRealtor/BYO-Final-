Replit Instructions — Tenant Agent Bid Form must use the NEW Broker Compensation input layout (not the old one)

Applies to: Hire a Tenant’s Agent → Agent bidding flow (Residential + Commercial)
Issue: Agent bid form is still rendering the old Tenant Broker Lease Fee UI (single $ prefix), while the listing/updated workflow uses the new UI (currency selector + left/right adornments).

✅ Goal: Make the Agent bid Broker Compensation section match the current/new format exactly.

🔒 SAFEGUARDS (MANDATORY)

Do NOT change any labels, tooltips, validation rules, placeholder text, or conditional logic.

Do NOT change any database columns or JSON keys.

This is a view/UI alignment fix only: the Agent bid form should render the same component/partial used by the updated Tenant listing format.

Must work for Residential + Commercial Tenant Agent bids.

1) Identify where the Agent bid Broker Compensation UI is coming from

Search in the codebase for the Agent bid view that renders Tab:
“Broker Compensation & Agency Agreement Terms” (inside the agent bidding modal/page).

Look for files like:

resources/views/livewire/tenant-agent-auction-bid*.blade.php

resources/views/tenant/hire/agent/*bid*

resources/views/hire_tenant_agent/*bid*

or any partial included in the agent bid form.

Search for the old markup text:

Enter flat fee amount

the simple input group that starts with <span>$</span> (old style)

2) Replace the old markup with the same partial/component used by the NEW Tenant listing format
✅ Required approach (best practice)

Do NOT rebuild the UI manually.

Instead:

Locate the new Broker Compensation partial/component used by the updated Tenant listing flow (the one producing the new input with currency selector and $ on both ends).

Include/reuse that same partial in the Agent bid form.

Examples of what to do:

If the new format lives in a Blade include, use:

@include('partials.tenant.broker_compensation_fields', [
  'mode' => 'agent_bid',
  'data' => $stateOrExistingData
])


If it’s a Livewire component, render the same component in the agent bid step:

<livewire:tenant-broker-compensation :mode="'agent_bid'" :existing="$existing" />


✅ Result: both places always stay in sync going forward.

3) Keep the same binding names (do not break saved values)

The agent bid form is currently saving values correctly (even if displayed with old UI), so the wire:model / name attributes must remain the same.

✅ Replit must ensure the new UI inputs bind to the existing keys, for example:

tenant_broker_lease_fee_type

tenant_broker_lease_flat_fee

tenant_broker_lease_percent
(whatever your project uses)

Do not rename these—just swap the visual wrapper/layout.

4) Ensure edit bid also uses the same new UI

Wherever the agent edits an existing bid, that edit view must also use the same updated component/partial so the UI is consistent.

✅ Requirement:

New bid UI = Edit bid UI = matches updated tenant format

5) Acceptance tests (must pass)

Create a Tenant listing (Residential)

Place an Agent bid → open Broker Compensation tab
✅ Tenant’s Broker Lease Fee renders in the new format (currency selector + correct adornments)

Repeat for Commercial listing
✅ Same new format

Place bid with Flat Fee = 5000
✅ Displays correctly and saves correctly

Edit bid and confirm values remain populated and unchanged unless edited
✅ No regressions

Quick note

You’re right: this confirms the agent bidding flow is pointing to an older Blade partial (or older Livewire step view). The fix is to standardize by reusing the same updated partial/componen