TASK: Offer Listing Photo Upload — finalize UX + enforce limits (no rebuild)

GOAL:
Ensure the existing multi-photo upload system is fully launch-ready with proper limits, helper text, and reliable save/reload behavior.

SCOPE:
- Agent Dashboard
- BidYourOffer
- Seller Offer Listing
- Landlord Offer Listing

DO NOT:
- Do not rebuild upload system
- Do not change storage structure
- Do not change file paths
- Do not touch Hire Agent flows
- Do not touch Limited Service
- Do not refactor unrelated code

--------------------------------------------------
1. ENFORCE MAX PHOTO COUNT
--------------------------------------------------

Add validation:

Max 50 photos per listing.

Requirements:
- If user tries to upload more than 50 total photos → show error
- Prevent upload beyond limit
- Count both:
  - existing photos
  - newly uploaded photos

--------------------------------------------------
2. ADD HELPER TEXT
--------------------------------------------------

Under Property Photos label add:

"You may upload up to 50 property photos. Supported formats: JPG, PNG, WEBP."

Keep styling consistent with existing helper text.

--------------------------------------------------
3. VERIFY SAVE + RELOAD (CRITICAL)
--------------------------------------------------

Ensure:
- Uploaded photos persist after Save Draft
- Existing photos load correctly in edit form
- Photos display as thumbnails
- Delete button works for each photo

If not fully implemented:
- Hook into existing processPendingPhotoUploads()
- Ensure loadDraft() returns saved images correctly

--------------------------------------------------
4. VERIFY NO UI BREAKS
--------------------------------------------------

Confirm:
- Upload button still works
- Multiple file select still works
- No Livewire re-render breaks upload
- No duplicate uploads on refresh

--------------------------------------------------
5. DO NOT IMPLEMENT YET
--------------------------------------------------

Do NOT:
- Add drag/drop sorting
- Add reordering
- Add galleries
- Add compression
- Add CDN logic

These are post-launch features

--------------------------------------------------
VERIFICATION
--------------------------------------------------

Test:

Seller:
- Upload 10 photos → save → reload → still there
- Upload 50 photos → allowed
- Upload 51 → blocked

Landlord:
- Same behavior

Confirm:
- Delete works
- No duplicates
- No crashes

--------------------------------------------------
FINAL REPORT
--------------------------------------------------

Provide:
- Validation added
- Helper text added
- Confirmation save/reload works
- Confirmation limit enforced
- Confirmation no system rebuild