#936 - Hire Seller Agent Form Polish + Select2 Behavior Audit

URL:
 /hire/agent/auction/seller

Goal:
Fix only the Hire a Seller Agent auction/create form UI issues listed below. This is a surgical frontend/form behavior fix only.

Hard safeguards:
- Do NOT change database schema, migrations, routes, models, controllers, validation rules, or saved field names.
- Do NOT touch Limited Service flows.
- Do NOT change Tenant, Buyer, or Landlord flows unless the same shared JS/CSS helper is proven to affect Seller and must be safely adjusted.
- Do NOT remove existing options, labels, tooltips, or dynamic logic.
- Preserve draft/load/save behavior.
- Preserve existing Select2 saved-value hydration.
- No duplicate Select2 containers.
- No global destructive Select2 reinitialization.

Issues to fix:

1) Down Payment to Cover the Gap Between Price and Loan
- On the Seller hire-agent form, this currency input should default to the $ symbol format, matching the existing Assumption Fee behavior.
- Do not change the field name or validation.
- Only adjust the icon/prefix/default UI behavior.

2) Assumption Fee placeholder
- Remove the comma from the placeholder example.
- Example should use clean formatting like:
  Enter assumption fee amount (e.g., $5000)
- Do not change saved data behavior.

3) Multi-select dropdown staying open
Audit Seller hire-agent auction form select fields and make View, Amenities, and Property Features behave like:
- Appliances Included
- Special Sale Provision
- Offered Financing/Currency

Expected behavior:
- User can select multiple options without the dropdown closing after every selection.
- Selected values remain visible.
- Saved draft values still reload correctly.
- No duplicate Select2 containers after tab switching or Livewire updates.

Audit all Seller multi-select fields for this same issue and fix only the fields that incorrectly close after each selection.

Likely Select2 setting:
closeOnSelect: false

Apply it consistently only to multi-select fields that should allow multiple selections.

4) Business property type → Property Style placeholder bug
- When Property Type = Business, Property Style appears but the "Select" placeholder disappears.
- Fix so Property Style always shows "Select" before selection.
- Preserve conditional show/hide logic.
- Preserve saved-value hydration when editing/loading draft.
- Do not alter property type options.

Likely requirements:
- Ensure the select has a blank leading option.
- Ensure data-placeholder="Select" exists.
- Ensure Select2 initializer uses placeholder: "Select" and allowClear where appropriate.
- Do not reinitialize destructively.

5) Acceptable Exchange Item formatting audit
- Audit the Acceptable Exchange Item selection UI.
- It should match the current larger selection format visually, but not break alignment, placeholder, dropdown width, or selected-value display.
- Fix only if the recent larger-selection formatting caused a regression.
- Preserve all options and saved values.

Acceptance checks:
- Visit /hire/agent/auction/seller.
- Confirm Down Payment to Cover the Gap Between Price and Loan shows the $ icon/prefix by default.
- Confirm Assumption Fee placeholder has no comma.
- Confirm View, Amenities, Property Features, and any other true multi-select fields allow multiple selections without closing after each selected item.
- Confirm Appliances Included, Special Sale Provision, and Offered Financing/Currency still work exactly as before.
- Select Business as Property Type and confirm Property Style shows "Select" before selection.
- Confirm Acceptable Exchange Item formatting is aligned, readable, and does not break placeholder/selected values.
- Switch tabs and return; no duplicate Select2 containers appear.
- Save draft and reload draft; selected values hydrate correctly.
- Git diff should show only relevant Seller hire-agent Blade/JS/CSS changes.