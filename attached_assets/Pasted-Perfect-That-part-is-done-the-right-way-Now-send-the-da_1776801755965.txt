Perfect. That part is done the right way.

Now send the **dashboard cleanup task** next.

Use this version:

---

Good. Leave the new Offer Playoff permission gate in place exactly as implemented.

Now do a second task: clean up the **normal agent dashboard** so it is focused only on **Hire Agent** launch flows.

## Objective

The normal agent dashboard should now function as a **Hire Agent–only dashboard** for launch.

Remove or hide non-Hire-Agent dashboard navigation items and quick actions that belong to Offer Playoff, property listings, rental listings, criteria listings, or service auctions.

This is a **UI/dashboard scope cleanup only**.
Do not delete underlying systems or routes unless clearly safe and unused.

## Audit first

Before changing code, audit and report:

1. sidebar/menu files that render these dashboard items
2. quick-action buttons/partials at the top of the dashboard
3. whether the “+ Create Listing” button/dropdown still exposes non-Hire-Agent listing paths
4. whether “My Bids” mixes Hire Agent and non-Hire-Agent activity
5. whether dashboard notices/widgets depend on any of the sections being removed

Then make the smallest safe change.

## Remove/hide from the normal agent dashboard

Remove/hide these items from the normal agent dashboard UI:

### Left sidebar

* Property Listings
* Rental Listings
* My Offer Listings
* Buyer Criteria Listings
* Tenant Criteria Listings
* Service Auction Bids

### Quick actions / top action buttons

Remove any non-Hire-Agent quick actions or shortcuts related to:

* Offer listings
* Property listings
* Rental listings
* Criteria listings
* Service auctions

## Keep in the dashboard

Keep and preserve:

* Dashboard home / notices
* My Hire Agent Listings
* Tenant Agent Bids
* Landlord Agent Bids
* Buyer Agent Bids
* Seller Agent Bids
* Messages
* QR Code & Hire Me Widget
* Profile Settings

## “Create Listing” requirement

Audit the current “+ Create Listing” button/dropdown carefully.

If it still exposes mixed listing types, change it so the normal agent dashboard only exposes **Hire Agent launch-relevant creation paths**.

Do not break any existing Hire Agent listing creation flow.

If this button is shared elsewhere, make the smallest scoped change possible for the normal agent dashboard only.

## Important rules

* Keep Offer Playoff underlying code intact
* Keep the new Offer Playoff permission gating intact
* No DB changes
* No destructive route deletions unless clearly safe and unused
* Do not break Hire Agent listing or bid flows
* Do not break Messages, QR & Hire Me, or Profile Settings
* Stop and report if the dashboard nav is generated from a shared config/partial that would affect other roles or dashboards

## Deliverable

After changes, report:

1. files changed
2. which sidebar items were removed/hidden
3. which quick actions were removed/hidden
4. what was done to the “+ Create Listing” button/dropdown
5. whether “My Bids” is still safe as-is or required adjustment
6. confirmation that the normal agent dashboard is now Hire-Agent-focused and launch-clean
7. confirmation that no Hire Agent flows were broken

---

That is the right next move.
