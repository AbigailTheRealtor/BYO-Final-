Fix Buyer bid card footer counter state logic to fully match Tenant behavior, WITHOUT breaking existing flows.

## GOAL
Ensure Buyer bid card footer correctly:
- shows "You have submitted a counter offer for this bid"
- shows View Counter Terms
- shows Edit Counter Terms
- hides Accept / Reject / Counter buttons

ONLY when the listing owner has submitted a counter.

---

## ROOT RULE (CRITICAL)

A bid is considered "countered by owner" ONLY if:

EITHER:
1. A BuyerCounterTerm exists for this bid created by the listing owner

OR

2. A BuyerCounterBidding exists for this bid where:
   - user_id = listing owner (auction owner)

---

## IMPLEMENTATION

### Step 1 — Locate Buyer modal footer state block

File:
resources/views/buyerAgentAuctionDetail.blade.php

Find the block where:

$mfStateB is computed

AND

$_mfCounterB is defined

---

### Step 2 — Replace $_mfCounterB logic

REPLACE existing $_mfCounterB logic with:

$_mfCounterB = !$_mfTermB && (
    \App\Models\BuyerCounterTerm::where('buyer_agent_auction_id', data_get($auction, 'id'))
        ->where('parent_counter_id', data_get($bid, 'id'))
        ->where('user_id', data_get($auction, 'user_id'))
        ->exists()

    ||

    \App\Models\BuyerCounterBidding::where('buyer_agent_auction_bid_id', data_get($bid, 'id'))
        ->where('user_id', data_get($auction, 'user_id'))
        ->exists()
);

---

### Step 3 — DO NOT CHANGE ANY OTHER STATE LOGIC

Do NOT modify:
- $mfStateB logic
- Accept / Reject / Counter button logic
- Modal structure
- Routing
- Controller logic

Only fix the condition that determines counter existence.

---

## EXPECTED RESULT

When OWNER submits counter (either path):
- Accept / Reject / Counter buttons DISAPPEAR
- Show:
  "You have submitted a counter offer for this bid."
  View Counter Terms
  Edit Counter Terms

When AGENT submits counter:
- Owner should STILL see action buttons
- NOT show "You have submitted" message

---

## SAFEGUARDS

- Do NOT move any UI blocks
- Do NOT duplicate footer
- Do NOT render outside modal
- Do NOT touch Tenant, Seller, Landlord
- Do NOT refactor entire file
- Do NOT change database
- Do NOT alter routes
- Do NOT modify counter history logic

---

## OUTPUT REQUIRED

Return:
1. exact lines changed
2. confirmation logic now checks BOTH models with owner filter
3. confirmation no UI sections moved
4. confirmation Buyer now behaves identical to Tenant for counter state