Implement Task #718 exactly as written.

Scope:
- Full Service Create Offer Listing only
- Seller Photos & Tours partial only
- Landlord Photos & Tours partial only
- Limited Service is legacy/frozen — DO NOT TOUCH

Files allowed:
1. resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/photos-tours-documents.blade.php
2. resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/photos-tours-documents.blade.php

Do NOT touch:
- routes
- migrations
- database schema
- models
- controllers
- draft logic
- submit logic
- bid logic
- other tabs
- Buyer/Tenant files
- Limited Service files

Required fix:
1. In seller initSellerDropzone(), prevent drag/drop event listener accumulation.
   - Store dragover, dragleave, and drop handler functions as named references on the dropzone element, such as dropzone._dropHandlers.
   - Before adding new listeners, remove any previously stored handlers with removeEventListener.
   - Then attach exactly one fresh set of listeners.

2. In landlord initLandlordDropzone(), apply the identical fix.
   - Use the same named-handler pattern.
   - Ensure only one dragover, dragleave, and drop listener is active after any Livewire re-render.

3. In both partials, add this to the hidden file input for newPropertyPhotos:
   - wire:loading.attr="disabled"
   - wire:target="newPropertyPhotos"

Do not change upload architecture, validation, storage, previews, SortableJS behavior, reorder, delete, draft, or edit reload logic.

Done looks like:
- One drop action triggers one upload event, even after many Livewire re-renders.
- Drag-and-drop does not duplicate uploads.
- Click-to-upload still works.
- Multiple photos upload and display correctly.
- Spinner still works.
- Reorder, move up/down, delete, draft-save, and edit-reload remain unchanged.
- Only the two allowed Blade files are changed.

Final report required:
- Files changed
- Exact listener-accumulation fix made
- Confirmation hidden inputs were disabled during upload
- Confirmation no Limited Service or unrelated files were touched
- Browser test results for seller and landlord drag/drop after multiple re-renders