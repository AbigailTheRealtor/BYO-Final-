Replit Instructions — Hire a Tenant’s Agent Listing
IMPORTANT SAFEGUARDS (Do Not Break Existing UI)

Do not delete or rename any existing tabs, tooltips, field labels, icons, or database columns.

Make minimal, targeted edits only to:

remove one duplicate service checkbox (UI only)

fix the Next button navigation bug on the Broker Compensation tab

After changes, confirm the tab content does not go blank and there are no Livewire/Laravel errors.

1) Remove Duplicate Service (Services Tab) — Keep It ONLY in Move-In Support & Coordination
Issue

The service “Confirm required move-in payments and assist the Tenant with tracking amounts due, deadlines, and accepted payment methods” appears twice across two different sections.

Required Fix

Keep this service checkbox under the correct section:
✅ Move-In Support & Coordination

Remove the duplicate occurrence under the earlier/wrong section (the section above Move-In Support & Coordination where it’s currently repeated).

Implementation Notes

This is a UI/services list cleanup only.

Do not remove the entire section—remove only the duplicate checkbox row.

Ensure the saved data structure still works (i.e., removing the duplicate from the wrong section should not break existing selected-service rendering).

If services are generated from an array/config list, remove that single service item from the wrong section’s list but keep the same item in Move-In Support & Coordination.

QA

Refresh the listing and confirm the service appears only once under Move-In Support & Coordination.

Confirm selecting it saves correctly and displays correctly in previews/bids/counters.

2) Fix “Next” Button Not Advancing (Hire a Tenant’s Agent → Broker Compensation Tab)
Issue

On Hire a Tenant’s Agent Listing → Broker Compensation tab, clicking Next does not advance to the next slide/tab. It must navigate forward correctly.

Required Fix (Must Work in All Cases)

Clicking Next must reliably move to the next tab/step every time.

It must work even when:

no fields were changed

fields were changed

validations are present

user toggles options

If validation fails, show errors and do not advance (but currently it’s not advancing even when it should).

Developer Checklist to Diagnose + Fix

Inspect the Next button:

Confirm it is not blocked by an overlay/div/div disabled state.

Confirm it is bound to the correct handler (Livewire / JS).

Check for silent JS errors in console when clicking Next.

Check for Livewire errors/network response when clicking Next.

Confirm the click handler is calling the actual navigation function (example patterns):

goToNextStep()

nextTab()

setActiveTab(tabIndex + 1)

If the Next button is inside a form:

Ensure it is not triggering an unintended submit event that gets prevented.

If it should be a button, ensure type="button" (not submit) unless submit is required.

If the tab requires validation before advancing:

Ensure validation is not incorrectly failing due to hidden/cleared fields.

Ensure hidden conditional fields are not still “required” in validation rules.

Ensure state updates are not preventing navigation:

If the tab uses conditional fields, make sure dependent fields are cleared properly so validation doesn’t block Next.

QA (Must Confirm)

On Broker Compensation tab:

Click Next with no changes → must advance

Make selections → click Next → must advance

Trigger a real validation error (if required fields exist) → must show error and not advance

Fix error → click Next → must advance

Final Confirmation Requested from Replit

After implementing:

confirm the duplicate move-in payment service is removed from the wrong section and appears only once

confirm Next navigation works on the Tenant Broker Compensation tab

provide the file(s) changed and a short note of what was fixed