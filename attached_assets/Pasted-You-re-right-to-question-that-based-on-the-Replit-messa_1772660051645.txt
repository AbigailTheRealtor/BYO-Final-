You’re right to question that — based on the Replit message you pasted, the architecture is:

* **Create Buyer listing:** `BuyerAgentAuction.php` + `hire-buyer-agent.blade.php`
* **Draft resume Buyer listing:** `TenantAgentAuction.php` + `tenant-agent-auction.blade.php`
* **Edit Buyer listing:** `TenantAgentAuctionEdit.php` + `tenant-agent-auction-edit.blade.php`
* And `BuyerAgentAuctionEdit.php` is **commented out / not used**

So I should **not** be telling Replit to “fix it in BuyerAgentAuctionEdit.php”. That file isn’t in the live route.

However: if your bug happens **during the initial create step**, then **BuyerAgentAuction.php + hire-buyer-agent.blade.php** still matter. If your bug happens when you **resume draft or edit**, then the fix must be in the **tenant shared blades/components**.

Since you said “we confirmed what files are edited,” here are **corrected Replit instructions** that follow your confirmed routing and **only touch the files that actually run**.

---

## Replit Instructions With Safeguards

### Buyer → Vacant Land only — Acceptable Property Styles “Other” input must appear, save, and display

✅ Use the correct routed files (Tenant shared for Draft/Edit)
✅ Do NOT touch Acceptable Unit Type (already fixed)
✅ Do NOT reference BuyerAgentAuctionEdit.php (not used)

---

# Confirmed live routing (do not change)

* **Create route:** `BuyerAgentAuction.php` → `resources/views/livewire/hire-buyer-agent.blade.php`
* **Draft resume route:** `TenantAgentAuction.php` → `resources/views/livewire/tenant-agent-auction.blade.php`
* **Edit route:** `TenantAgentAuctionEdit.php` → `resources/views/livewire/tenant-agent-auction-edit.blade.php`

**Safeguard:** Do not attempt fixes in `BuyerAgentAuctionEdit.php` — it is not used.

---

# Goal (Vacant Land only)

When **Property Type = Vacant Land**, under **Acceptable Property Styles**:

* User can select **Other**
* The UI immediately shows a custom text input
* That text saves and persists through Draft + Edit
* On listing display, show the custom text (not the word “Other”)

---

# Step 1 — Identify the exact Livewire properties used

In the Buyer Vacant Land section, locate the Livewire property backing the **Acceptable Property Styles** multi-select (example names you may already have):

* `property_style_buyer` OR `acceptable_property_styles` OR similar

Also define ONE text property for the “Other” input:

* **Use one consistent property**, e.g. `property_style_other_buyer`

**Safeguard:** Don’t create multiple “other” fields. Use one property name consistently in Create + Draft + Edit + Display.

---

# Step 2 — Fix Draft + Edit first (because they are routed through Tenant shared files)

### In BOTH files:

1. `resources/views/livewire/tenant-agent-auction.blade.php` (Draft resume)
2. `resources/views/livewire/tenant-agent-auction-edit.blade.php` (Edit)

Do the following in the Vacant Land Buyer section:

## 2A) Show the “Other” input when Other is selected

Add a Blade conditional that checks:

* property type is Vacant Land
* the selected styles array includes `"Other"`

Then render the input:

* label: `Other (Specify)`
* bound to: `wire:model.defer="property_style_other_buyer"` (or your chosen consistent name)

## 2B) Select2 dual-sync (mandatory)

In the Select2 change handler for Acceptable Property Styles, ensure:

* `values = $('#<styles_select_id>').val() || []`
* Set the Livewire array property directly:

  * `@this.set('<styles_livewire_property>', values)`
* Also set JSON bridge if present / add one if missing:

  * `@this.set('<styles_livewire_property>_json', JSON.stringify(values))`

**Safeguard:** If the field is in a hidden tab, without JSON bridge + direct set, it will “look selected” but save as `[]`.

## 2C) Clear the other text only when Other is removed

If values no longer include `"Other"`:

* `@this.set('property_style_other_buyer', '')`

**Safeguard:** Do NOT clear it on every change. Only clear when “Other” is not selected.

---

# Step 3 — Ensure the Livewire components save the “Other” field

Because Draft/Edit use Tenant components, update:

* `TenantAgentAuction.php`
* `TenantAgentAuctionEdit.php`

### Required:

* Ensure the “styles” field saves as JSON array
* Ensure the `property_style_other_buyer` text saves to DB as a normal string column (or as part of metadata, depending on your schema)

If you use JSON bridge:

* Decode before saving and overwrite the array property:

  * `$this-><styles_prop> = json_decode($this-><styles_prop>_json, true) ?? [];`

**Safeguard:** Do not drop `"Other"` during normalization. Don’t `implode()` the array.

---

# Step 4 — Fix Create only if the issue appears on initial creation too

Only if the same “Other input doesn’t appear” happens during **initial create** (not just edit/draft), then apply the same UI+sync logic in:

* `resources/views/livewire/hire-buyer-agent.blade.php`
* and ensure `BuyerAgentAuction.php` also has the property and save handling

**Safeguard:** Do not touch BuyerAgentAuctionEdit.php (not used).

---

# Step 5 — Listing Display: show custom text instead of “Other”

In the listing display template for Buyer Vacant Land:

If styles include `"Other"` and `property_style_other_buyer` is not empty:

* replace `"Other"` in the displayed list with the custom text

Example:

* Selected: `Agricultural`, `Other`
* Other text: `Billboard Site`
  Display:
* `Agricultural, Billboard Site`

**Safeguard:** Do not show both “Other” and “Billboard Site”.

---

## QA Test (Must pass)

Login `tenant@exp.com` → Buyer → Vacant Land

1. Select “Other” under Acceptable Property Styles → input appears
2. Type “Billboard Site”
3. Save Draft → reopen draft → still there
4. Publish → listing shows “Billboard Site” not “Other”
5. Edit listing → still there → save again → persists

---

If you tell me the **exact field/property name** you’re using for “Acceptable Property Styles” in Vacant Land (the Livewire property + the select id), I’ll rewrite the above with **your exact names** so Replit can implement it with zero guesswork.
