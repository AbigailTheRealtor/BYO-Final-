1️⃣ Listing Type — MAIN TOOLTIP (SHORT + NEUTRAL)

Tooltip Text (replace existing long tooltip):

Select how Agents submit bids for this listing.
This choice controls timing, bid visibility, and how Agent bids are reviewed.

✔ Purpose-driven
✔ No mechanics
✔ No overload

2️⃣ Dropdown Hover Tooltips (DETAILED EXPLANATIONS)

These appear only when the user hovers over each option inside the Listing Type dropdown.

🕒 Bidding Period — Hover Tooltip

Tooltip Text:

Agents may submit bids until the bidding timer ends.

During the bidding period, bids cannot be accepted or finalized. Once the timer ends, all Agent bids are revealed for review and comparison, including Services Offered, Broker Compensation & Agency Agreement Terms, and Match Scores.

After the bidding period closes, the listing continues in Traditional mode for reviewing, countering, and accepting bids.

📄 Traditional — Hover Tooltip

Tooltip Text:

Agents may submit bids at any time.

Agent bids are visible as they are received and may be reviewed, accepted, countered, or rejected immediately. There is no bidding timer, and bids are handled on a rolling basis.

3️⃣ REPLIT IMPLEMENTATION INSTRUCTIONS (WITH SAFEGUARDS)
Scope

Applies to:

Hire a Tenant’s Agent

Hire a Buyer’s Agent

Hire a Seller’s Agent

Hire a Landlord’s Agent

Must also apply consistently to:

Agent bids

Counter bids

🔒 SAFEGUARDS (DO NOT BREAK THESE)

DO NOT:

rename or delete existing database fields

modify listing type values (Bidding Period, Traditional)

change listing logic, timers, or bid visibility behavior

alter validation rules or submission flow

remove or modify existing tooltips outside this field

🛠️ Implementation Steps
Step 1 — Replace Listing Type Tooltip

Locate the tooltip attached to the Listing Type field label.

Replace the existing long tooltip text with the short tooltip text provided above.

Confirm tooltip is attached only to the info icon next to “Listing Type.”

Step 2 — Add Hover Tooltips to Dropdown Options

Add a hover-triggered tooltip (or helper popover) to:

Bidding Period

Traditional

Tooltip should appear:

on hover

on keyboard focus (accessibility)

Tooltip should disappear on mouse out / blur.

Step 3 — Display Logic Requirements

Tooltips must:

appear only when hovering over the option

NOT block dropdown selection

NOT change selected value

NOT trigger form validation

Tooltips must work on:

create listing

edit listing

saved drafts

counter bids

Step 4 — Consistency Check

Confirm:

Same hover tooltip text appears anywhere Listing Type is selectable

Same behavior across Buyer / Seller / Tenant / Landlord flows

Tooltips render correctly on desktop and tablet

Mobile tap behavior shows tooltip or info modal (if applicable)