Make the following dashboard UI polish updates only. Do not modify listing creation flows, bid logic, routing logic, database schema, or role-specific workflow behavior. This task is wording and styling only.

1. Change the page heading text when a user clicks each role tab in the client dashboard:
   - “Hire Tenant’s Agent Auctions” → “Hire Tenant’s Agent Listings”
   - “Hire Landlord’s Agent Auctions” → “Hire Landlord’s Agent Listings”
   - “Hire Buyer’s Agent Auctions” → “Hire Buyer’s Agent Listings”
   - “Hire Seller’s Agent Auctions” → “Hire Seller’s Agent Listings”

2. Apply this wording change only to the user-facing page headings/titles in those dashboard role pages.
   - Do not rename backend route names, controller names, method names, table names, or internal variables if they still use “auction”
   - Do not change any creation flow labels unless those exact user-facing page titles are being rendered there
   - This is a presentation-only text update

3. Fix the left sidebar/top button styling for the blue “Hire Agent” button above Dashboard so it matches the same blue used by the “Hire Agent” button near the Logout button.
   - Use the same background color
   - Match hover/focus state if appropriate
   - Keep spacing, border radius, and typography visually consistent

CRITICAL SAFEGUARD:
- Do not modify Hire a Tenant’s Agent, Hire a Landlord’s Agent, Hire a Buyer’s Agent, or Hire a Seller’s Agent creation flows
- Do not modify bid submission, counter, accept/reject, match score, or Accepted Bid Summary logic
- Do not rename routes, controllers, models, database columns, or tables just because the UI heading changes from “Auctions” to “Listings”
- This task is strictly user-facing heading text + button color consistency only

Acceptance criteria:
- each client dashboard role page says “Listings” instead of “Auctions”
- the blue sidebar/top “Hire Agent” button matches the blue of the top-right “Hire Agent” button
- no underlying logic or route names are changed