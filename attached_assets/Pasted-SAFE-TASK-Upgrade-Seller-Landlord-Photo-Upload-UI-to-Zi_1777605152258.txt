SAFE TASK — Upgrade Seller/Landlord Photo Upload UI to Zillow-Style Multi-Photo Gallery

GOAL:
Make the Seller and Landlord Offer Listing photo upload experience look and feel like a modern multi-photo uploader, similar to Zillow-style listing photo management.

SCOPE:
Offer Listings only:
- Seller Photos, Tours & Documents tab
- Landlord Photos, Tours & Documents tab
- Create and edit forms where these partials are reused

DO NOT:
- Do not change photo storage structure unless absolutely required.
- Do not remove existing multi-photo backend logic.
- Do not touch Broker Compensation, Services, Agent Presets, MLS fields, or unrelated tabs.
- Do not delete existing photos.
- Do not run migrations unless absolutely required.
- Do not break video tour, 3D tour, or documents.

TASK:
Replace the plain browser “Choose Files” input presentation with a custom upload card/dropzone UI.

REQUIREMENTS:

1. Hide the ugly default file input visually, but keep it functional.
- Use a styled button/card that triggers the hidden input.
- Text should clearly say:
  “Upload Photos”
  “Select multiple property photos”
  “Up to 50 photos”

2. Add Zillow-style upload area:
- Large bordered upload card
- Camera/photo icon
- Drag-and-drop style wording if supported:
  “Drag photos here or click to upload”
- Subtext:
  “Upload up to 50 property photos. The first photo will be used as the cover photo.”

3. Make multi-file support obvious:
- Add visible text:
  “You can select multiple photos at once.”
- The upload button should say:
  “Add Photos”
  not “Choose Files”

4. Photo gallery layout:
- Show uploaded photos in a clean grid.
- First photo should have badge:
  Cover Photo
- Each photo card should show:
  - Thumbnail
  - Remove button
  - Move left / Move right OR Move up / Move down
  - Drag handle if SortableJS is active

5. Upload count:
- Show:
  “0 / 50 photos uploaded”
  or “12 / 50 photos uploaded”

6. Empty state:
When no photos uploaded, show:
“Add property photos to help Agents better understand and market the property.”

7. Accessibility:
- Hidden input must still be keyboard-triggerable via label/button.
- Keep validation errors visible.

8. Preserve current functionality:
- 50-photo limit still works.
- Reorder still works.
- Delete still works.
- Existing uploaded photos still reload.
- Video/3D/document fields unchanged.

FILES TO INSPECT:
- Seller photos-tours-documents.blade.php
- Landlord photos-tours-documents.blade.php
- SellerOfferListing.php
- LandlordOfferListing.php
- Any CSS already used for listing photo cards/dropzones

VERIFY:
- User can click styled upload card/button and select multiple files.
- Uploaded photos appear in grid.
- First photo shows Cover Photo.
- Count updates correctly.
- Reorder still saves.
- Delete still works.
- No console errors.
- No backend errors.

REPORT:
- Files changed
- UI before/after summary
- Confirmation backend logic unchanged
- Verification results