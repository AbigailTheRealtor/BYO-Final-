Replit Instructions — Hire a Buyer’s Agent (Tenant Profile)
Tab 6: Broker Compensation & Agency Agreement Terms (Apply to Listing + Agent Bids + Counter Bids)

Applies to: When logged in as a Tenant/User profile (example: tenant@exp.com) and user selects Hire a Buyer’s Agent
Scope: Tab 6 only (Broker Compensation & Agency Agreement Terms) AND the same fields wherever they appear inside Agent Bids + Counter Bids (so the display/behavior matches everywhere).

SAFEGUARDS (MUST FOLLOW — VERY IMPORTANT)

DO NOT:

delete or rename any existing database tables/columns/models

rename any existing field keys/IDs used by Buyer Tab 6

change tab order for other tabs or change tooltips/placeholders in other agent types (Tenant/Seller/Landlord) unless explicitly referenced below

remove listing-page bid logic (Accept/Counter/Reject) or break timed vs traditional behaviors

DO:

make changes only to Buyer Tab 6 components + shared bid/counter components that render these same Buyer Tab 6 fields

ensure all actions are non-destructive (additive/conditional logic only)

keep UI layout and styling consistent with existing platform styles

1) GLOBAL INPUT BEHAVIOR (CRITICAL BUG FIX)
When switching compensation types, clear old values

Problem: If the user switches between Flat Fee / Percentage / Other, previously entered values can remain and create conflicting data.

Required behavior:

For every compensation field that has a “type selector” (e.g., Flat Fee vs % vs Other), when the user changes the selector:

Clear all inputs that belong to the previously selected type

Only the currently selected type should retain and display a value

Prevent multiple types from being saved at once

Apply this to ALL Buyer Tab 6 compensation groups (and mirrored Agent Bid + Counter Bid versions).

2) Buyer’s Broker Commission Structure — Rename Options + Tooltip
Field: Buyer’s Broker Commission Structure

Rename the option labels exactly:

Change: “Out of Pocket Payment” → “Buyer Pays Out-of-Pocket”

Change: “Included in Offer” → “Requested From Seller in the Offer”

Tooltip (replace current text with this exact text):
“Select how the Buyer’s Broker will be compensated—either paid directly by the Buyer (out-of-pocket) or requested from the Seller as part of the offer. If the Seller does not agree to pay, the Buyer remains responsible for the commission under the brokerage agreement.”

3) Buyer’s Broker Purchase Fee — Fix comma formatting + “Other” placeholder wording
Field: Buyer’s Broker Purchase Fee
A) Comma formatting (thousands separators)

Issue: Some $ inputs do not apply comma formatting consistently.

Required:

Ensure any dollar amount input in this section uses consistent formatting (e.g., typing 3000 displays/stores as 3,000 visually)

Apply formatting in:

“Percentage of Total Purchase Price + Flat Fee” (flat fee box)

“Flat Fee”

Any other $ amount inputs within Buyer Purchase Fee

B) “Other” placeholder text — remove the word “other”

When fee type = Other, change placeholder from:

“Enter other purchase fee amount (e.g., 1000 upfront + 2% at closing)”
to:

“Enter purchase fee structure (e.g., 1,000 upfront + 2% at Closing)”

(Also apply comma formatting in that example if your placeholder formatting engine supports it.)

4) Buyer’s Broker Lease Fee (All listing types) — Stop deleting Flat Fee input + placeholder cleanup
Field: Buyer’s Broker Lease Fee
A) Flat Fee input is being auto-deleted (BUG)

Issue: When user selects Flat Fee and types a number, it gets cleared automatically.

Fix required:

The Flat Fee amount must remain stored and displayed while that selection remains active

It should only clear if the user changes the fee type selector (per Section #1)

B) Placeholder text cleanup (remove the word “other” anywhere it appears)

If there is any placeholder that says “Enter other … fee amount”, remove “other”.

Example:

“Enter other lease fee amount …” → “Enter lease fee amount …”

5) “Interested in a Lease Agreement” — Commercial/Business dropdown options cleanup
Field: Interested in a Lease Agreement (Commercial + Business only)

Delete/remove these 3 options from Commercial/Business only:

Percentage of Monthly Rent

Percentage of the Gross Lease Value

Flat Fee + Percentage of the Gross Lease Value

Commercial/Business options must be EXACTLY:

Flat Fee

Percentage of the Net Aggregate Rent

Flat Fee + Percentage of the Net Aggregate Rent

Other

(Do not change Residential options unless they already match the correct structure.)

6) Lease-Option Section — Remove “Compensation Amount” labels + fix placeholders + remove yellow note
Fields:

Compensation for Creating the Lease-Option Agreement

Compensation if Purchase Option is Exercised

A) Remove label text “Compensation Amount” above both inputs

Requirement:

The section headers remain (the blue header text stays)

But the small text “Compensation Amount” above each input should be removed so the UI only shows the input row cleanly

B) Placeholders for $ selection must be EXACT

When the user selects the $ option for each field, placeholders should be:

For “Compensation for Creating the Lease-Option Agreement” ($):

“Enter flat fee amount (e.g., 1,500)”

For “Compensation if Purchase Option is Exercised” ($):

“Enter flat fee amount (e.g., 5,000)”

C) Delete the yellow helper note

Remove/delete this UI note (Buyer tab only):

“Note: Select $ or % to switch between entering a dollar amount or a percentage.”

(Keep functionality; remove the visual note.)

D) Tooltips — set to the exact text below

Tooltip for “Compensation for Creating the Lease-Option Agreement”:
“Specify how the Broker will be compensated at the time the lease-option agreement is created. This may include a flat fee or a percentage of the option consideration paid by the party granting the option. This compensation is typically paid upfront and is separate from any commission that may be owed if the purchase option is later exercised.”

Tooltip for “Compensation if Purchase Option is Exercised”:
“If the purchase option is exercised, the Broker may be entitled to additional compensation. Enter how the Broker will be compensated at that time, such as a flat fee or a percentage of the total purchase price. Any compensation already received under the lease-option agreement may be credited toward the final amount due, depending on the terms of the agreement.”

7) Acceptable Brokerage Relationship — update Single Agent Representation line + Legal Notice text + punctuation consistency
Field: Acceptable Brokerage Relationship
A) Update the “Single Agent Representation” requirement line

Change the line under Single Agent Representation from:

“Requires a Single Agent Notice signed by the Buyer.”
to:

“Requires written consent from both Buyer and Seller”

B) Legal Notice: replace to match Buyer wording

Replace the legal notice text with this exact version:

⚠️ Legal Notice: Certain brokerage relationships are not permitted in all states. If your selection is not allowed, the Broker will establish a permitted legal alternative. Real estate laws change frequently. Both the Broker and Buyer are responsible for complying with all current local, state, and federal laws.

(Do not reference Landlord or Seller in the Buyer flow’s legal notice.)

C) Format consistency: add periods at end of each bullet sentence

You mentioned the Buyer format is missing periods compared to Landlord/Seller.

Requirement:

In each brokerage relationship description (Transaction Broker Representation, Single Agent Representation, Dual Agency Representation, No Brokerage Relationship), ensure each bullet line ends with a period, matching the Landlord/Seller formatting style.

Example:

“Default in Florida unless otherwise specified” → “Default in Florida unless otherwise specified.”

“Must act honestly, fairly…” → “Must act honestly, fairly…”

8) Apply the SAME edits under Agent Bids + Counter Bids

You noted: “Also take these edits & apply them under Agent Bids and Counter Bids.”

Required:

Wherever Buyer Tab 6 fields are rendered inside:

Agent’s bid details

Tenant’s view of an agent bid

Counter-bid terms view

The same labels, options, tooltips, placeholders, formatting, and clearing behavior must be used.

Do not create duplicate versions of these strings in multiple places—centralize constants where possible to avoid future drift.

9) Quick test checklist (must pass before marking done)

Test while logged in as tenant@exp.com:

Switch Buyer compensation types (Flat Fee → % → Other)
✅ Old values clear correctly; only current selection saves.

Buyer’s Broker Commission Structure labels show correctly
✅ “Buyer Pays Out-of-Pocket” and “Requested From Seller in the Offer”

Buyer Purchase Fee “Other” placeholder doesn’t say “other”
✅ and comma formatting works where applicable.

Buyer Broker Lease Fee Flat Fee no longer auto-clears
✅ stays unless type changes.

Commercial/Business Lease Agreement dropdown matches exact 4 options
✅ and removed the 3 unwanted ones.

Lease-Option section:
✅ blue headers remain
✅ “Compensation Amount” labels removed
✅ yellow note removed
✅ placeholders match exactly
✅ tooltips match exactly

Brokerage relationship:
✅ Single Agent Representation line updated
✅ Legal Notice references Buyer (not Landlord/Seller)
✅ bullet sentences end with periods

Verify same behavior in Agent Bid + Counter Bid views
✅ identical labels/options/placeholders/tooltips and clearing behavior