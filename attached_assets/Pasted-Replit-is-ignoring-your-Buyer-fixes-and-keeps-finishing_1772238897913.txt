Replit is ignoring your Buyer fixes and keeps “finishing” the Select2 plan instead. You need to force it to do **only** the Buyer changes, with **hard stop constraints** and **file targets**.

Below is a **copy/paste Replit instruction set with safeguards** that prevents it from going off-track and also tells it exactly what to touch.

---

# ✅ REPLIT INSTRUCTIONS WITH SAFEGUARDS

## Priority

**DO NOT work on Select2, guards, restore hooks, PDF system, or any “session plan.”**
**Only implement the Buyer Agent fixes listed below.**
If you cannot find a referenced file/key, stop and print the file path + line where the closest match exists.

## Hard Safeguards

1. Do not modify Seller/Landlord/Tenant code. Buyer only.
2. Do not refactor Select2 code.
3. Do not create new tasks, do not “parallelize,” do not touch unrelated plans.
4. Any fix must apply to: **Create + Draft + Edit + Actual Listing Display** for Hire a Buyer’s Agent.
5. Do not rename DB/meta keys. Fix is wiring + save/hydrate + display only.

---

# ✅ Step 0 — Identify the Buyer component + views (must print paths)

Search and print the exact file paths for:

* Buyer create Livewire component (likely `BuyerAgentAuction.php` or similar)
* Buyer edit Livewire component
* Buyer create blade
* Buyer edit blade
* Buyer listing display blade (detail view)

You must list the paths before making changes.

---

# ✅ Fix 1 — Acceptable Property Conditions “No Preferences” not saving (ALL property types)

## Symptom

Option **“No Preferences (open to any condition)”** does not persist on create/draft/edit.

## Implementation Requirements

### A) Confirm storage key and option value

Locate the meta key used for Acceptable Property Conditions (example: `condition_prop_buyer` or `acceptable_property_conditions`) and confirm the “No Preferences” option value (recommended `no_preference`).

### B) Ensure it saves in the Buyer save pipeline

In the Buyer shared save method (the one used by **Save Draft** and **Submit** and **Update**):

* Save the Acceptable Property Conditions field.
* Ensure “no_preference” is included when selected.

### C) Ensure it hydrates on Edit + Draft

In Buyer edit/draft load method:

* Load the meta key into the Livewire property.
* If Select2 is used with `wire:ignore`, restore the selection in JS on init:

  * `.val(@this.get('FIELD')).trigger('change.select2')`

### D) If it should be exclusive (recommended)

When `no_preference` is selected:

* Clear all other selected conditions.
  When any other condition is selected:
* Remove `no_preference`.

Apply this exclusivity rule consistently across create/edit/draft.

---

# ✅ Fix 2 — Timing of Transfer should be regular text on actual listing (ALL property types)

## Requirement (display only)

On Buyer listing display:

* Render **Timing of Transfer:** as plain text (no bubble tags), even if stored as an array.

Implementation:

* In Buyer detail blade, force this field to render using the “single value text” renderer (not pills).
* Do not change storage.

---

# ✅ Fix 3 — INCOME Buyer: Acceptable Unit Type must be multi-select + must save + must display

## Symptoms

* Only allows one selection
* Doesn’t show on listing
* Income Property Criteria, Acceptable Number of Units, Acceptable Unit Type do not save

## Implementation Requirements

### A) Change input to multi-select

In Buyer Income create + edit blades:

* Ensure Acceptable Unit Type `<select>` has `multiple`
* Ensure Livewire property is an array (default `[]`)

### B) Save pipeline (create/draft/edit)

Ensure these are saved and restored:

* `income_property_criteria`
* `acceptable_number_of_units`
* `acceptable_unit_type` (array/multi)

### C) Display on actual listing

Add to Buyer Income detail view:

* Acceptable Number of Units
* Acceptable Unit Type
  Render multi-select as:
* 1 item = plain text
* 2+ items = bubble tags

---

# ✅ Fix 4 — COMMERCIAL/BUSINESS Buyer: Garage/Parking Features Needed Yes/No should be plain text

## Requirement (display only)

On Buyer detail view for Commercial/Business:

* **Garage/Parking Features Needed:** show “Yes” or “No” as regular text, not bubble.

Do not change feature bubbles on the next line if you keep them.

---

# ✅ Fix 5 — VACANT LAND Buyer: Acceptable Property Style “Other” text not saving

## Symptoms

Other selected + typed text does not persist.

## Implementation Requirements

* Confirm meta keys:

  * `acceptable_property_style`
  * `acceptable_property_style_other` (typed text)
* Save both in create/draft/edit pipelines.
* On display:

  * If selection includes Other and other text exists → show the text instead of “Other”
  * If other text exists but Other isn’t in selection (legacy) → append text anyway

---

# ✅ Fix 6 — ALL Buyer listings: Interested in a Lease Agreement not saving on edits/drafts

## Requirement

Ensure this field is:

* included in Buyer save pipeline
* included in Buyer draft/edit hydration pipeline
* restored in JS if it’s wire:ignore/select2

---

# ✅ REQUIRED OUTPUT (so we know it actually did the work)

After implementing, print:

1. List of files changed with paths
2. For each fix above, print the exact meta key names used
3. Confirm tested flows:

* Create → Save Draft → reopen → Save
* Create → Submit → View listing
* Edit submitted → Save → View listing

---

## Why your last attempt showed “no changes”

Because Replit chose to do the Select2 plan instead and never touched Buyer code. The instructions above prevent that.

---

If you want, paste **one Buyer listing URL/screenshot** where “No Preferences” fails, and I’ll also tell you the most likely meta key (it’s usually `condition_prop_buyer`) and what value your option is currently saving (often it’s blank or duplicated).
