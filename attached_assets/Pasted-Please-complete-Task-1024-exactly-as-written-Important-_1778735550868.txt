Please complete Task #1024 exactly as written.

Important safeguards:
- Audit first before editing.
- Scope is STRICTLY Landlord Direct Hire only:
  - /hire/agent/direct/{id}/landlord/{propertyType}
  - /hire/agent/direct/{id}/landlord/{propertyType}/counter
  - /hire/agent/direct/{id}/landlord/{propertyType}/acknowledge
- Do NOT touch Buyer, Seller, Tenant, marketplace Hire Agent, Offer Listing, or Limited Service flows.
- Do NOT change routes, controllers, models, migrations, schema, validation, or save logic.
- Blade/JS only unless a direct landlord-only config parity issue requires a minimal config addition.
- Do NOT touch initializeLimitedService().

Target files:
- resources/views/hire-agent-direct/client-details/landlord.blade.php
- resources/views/hire-agent-direct/client-details/tenant.blade.php (READ-ONLY reference for formatting pattern)
- resources/views/hire-agent-direct/counter.blade.php
- resources/views/hire-agent-direct/acknowledge.blade.php
- config/agent_preset_compensation.php (ONLY if landlord retainer fee parity truly exists in preset editor)

Part 1 — Desired Monthly Rent formatting
Audit existing formatting first.

Requirements:
- Apply the same comma-formatting pattern used in tenant.blade.php max_monthly_lease_price to:
  input[name="desired_monthly_rent"]
- Format while typing:
  545555 → 545,555
- Run formatter on prefilled values on load.
- Preserve existing $ prefix UI.

Before adding submit cleanup:
- Verify whether counterFormSubmit() already strips commas for all $-prefixed text inputs in #counter-form.
- If it already handles desired_monthly_rent indirectly, DO NOT add duplicate stripping there.

For acknowledge.blade.php:
- Add desired_monthly_rent comma stripping inside ackSubmit()
- Mirror the seller_desired_sale_price stripping pattern.

Part 2 — Review tab formatting
In counter.blade.php populateReviewTab():

Add formatting branch for:
- desired_monthly_rent
Behavior:
- strip commas
- parse integer
- prefix $
- format with toLocaleString('en-US')

Add formatting branch for:
- availability_date
Behavior:
- local-safe parsing from YYYY-MM-DD split
- new Date(year, monthIndex, day)
- format:
  May 27, 2026

Reuse the existing move_in_date formatting approach already present in the loop.

Do NOT alter unrelated review formatting logic.

Part 3 — Replace Flexibility with Desired Lease Term
In client-details/landlord.blade.php:

Remove entirely:
- Flexibility label
- Flexibility select
- Flexibility options
- Flexibility @error block

Replace with:
Label:
- Desired Lease Term

Select:
name="desired_lease_term"

Options:
- Month-to-Month
- 3 Months
- 6 Months
- 9 Months
- 1 Year
- 2 Years
- Other

Requirements:
- Preserve existing styling/icon conventions.
- Use the existing conditional reveal pattern already used in this partial.
- When "Other" is selected:
  - show ONLY a text input underneath
  - NO extra label above it
  - placeholder:
    Enter desired lease term (e.g., 8 Months)

Review tab updates:
- Remove flexibility from review fields array
- Add desired_lease_term
- Conditionally append desired_lease_term_other only when select value === "Other"

Also remove any leftover flexibility display from acknowledge/submitted summary output.

Part 4 — Interested in Selling conditional audit
Audit the landlord Comp Terms block inside counter.blade.php under:
@if($role === 'landlord')

Requirements:
- Confirm selecting:
  Interested in Selling the Property = Yes
  immediately reveals:
  - Landlord’s Broker Purchase Fee select
  - all nested compensation amount fields

If nested conditional init order is broken:
- fix initialization ordering safely
- OR add a post-init conditional refresh pass

Requirements:
- Preserve existing bindings and names.
- Confirm:
  - % suffixes render correctly
  - $ prefixes render correctly
  - comma-formatting still applies to flat-fee fields

Do NOT rewrite the entire conditional engine.

Part 5 — Full landlord Comp Terms parity audit
Compare landlord Direct Hire Comp Terms vs landlord preset editor.

Verify presence/parity for:
- Lease compensation fields
- Renewal fee fields
- Tenant broker compensation fields (residential only)
- Interested in Selling commission fields
- Brokerage relationship
- Agency agreement timeframe
- Protection period
- Early termination fee

Retainer fee audit:
- Check whether landlord preset editor actually supports retainer fee in:
  config/agent_preset_compensation.php
- ONLY if landlord preset supports it:
  - add landlord Retainer Fee block to Direct Hire landlord Comp Terms
- OTHERWISE:
  - leave existing exclusion unchanged

Do NOT add unrelated marketplace/listing-only fields.

QA required before merge:
- desired_monthly_rent formats while typing
- desired_monthly_rent formats on prefilled load
- desired_monthly_rent strips commas before acknowledge submit
- Review tab shows:
  $3,233,232
- availability_date review shows:
  May 27, 2026
- Flexibility fully removed
- Desired Lease Term appears everywhere correctly
- “Other” text field appears without extra label
- Interested in Selling = Yes reveals nested commission fields immediately
- Nested commission fields show proper $ / % suffixes
- No console errors
- No Buyer/Seller/Tenant regressions
- No marketplace/Offer Listing changes
- git diff limited to intended landlord Direct Hire files plus config ONLY if retainer parity requires it

Final response must include:
- files changed
- whether landlord retainer fee parity existed
- exact conditional-init fix used
- QA proof
- confirmation no out-of-scope flows/files were touched