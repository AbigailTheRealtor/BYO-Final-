That means the fix is still **not complete**.

What they fixed was mostly **server-side validation**, but your complaint is also about the **front-end missing-fields notification list**.

If **Minimum Bedrooms** and **Minimum Bathrooms** are required, then two things must happen:

1. they must actually block submit
2. they must appear in the **missing fields notification list** when blank

Right now, that means one of these is still wrong:

* the JS required-field collector is not including those fields
* the field selectors are pointing to the wrong inputs
* the labels used in the missing-fields list are wrong or missing
* those fields are using a different property/input name than the audit assumed
* the code is validating `bedrooms` / `bathrooms`, but the actual rendered fields are `minimum_bedrooms` / `minimum_bathrooms` or similar
* the submit banner is only listing a partial set of required fields instead of the full required set

So the next instruction to Replit should be very direct:

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT’S AGENT ONLY — FIX MISSING REQUIRED FIELDS LIST SO IT SHOWS EVERY REQUIRED FIELD

## SCOPE

The current Hire a Tenant’s Agent validation is still incomplete.

### Current bug:

When clicking **Submit** with required fields missing:

* **Minimum Bedrooms** and **Minimum Bathrooms** do not appear in the missing-fields notification
* if many required fields are blank, the notification only lists **some** of them
* the system must list **every single required field** that is missing before submit

This task is **not complete** until the missing-fields notification shows the **full and correct list** of all currently required fields for Hire a Tenant’s Agent.

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Tenant’s Agent flow.**
2. **Do not modify Buyer, Seller, or Landlord flows.**
3. **Do not rename any database columns, Livewire properties, wire:model names, or field keys.**
4. **Do not run migrations.**
5. **Do not refactor unrelated shared logic unless absolutely necessary, and if shared logic must be touched, wrap it so only Tenant flow behavior changes.**
6. **Do not change tooltips, labels, placeholders, wording, icons, or layout except where needed to correctly map the missing field labels.**
7. **Do not assume the field names. Use the real rendered code.**
8. **Do not stop at server-side validation. This task is specifically about the front-end missing-fields notification list showing every missing required field.**

---

## WHAT MUST BE FIXED

When the user clicks **Submit** on Hire a Tenant’s Agent:

* every required field that is currently blank must be detected
* every missing required field must be included in the notification list
* the list must use the **correct user-facing field label**
* this includes **Minimum Bedrooms** and **Minimum Bathrooms**
* if 10 required fields are blank, all 10 must appear
* if 20 required fields are blank, all 20 must appear
* do not return a partial list

---

## STEP 1 — TRACE THE REAL FIELDS, NOT ASSUMED FIELDS

Before editing, inspect the actual rendered Tenant create flow and confirm the exact field/property names for:

* Minimum Bedrooms
* Minimum Bathrooms

Do not assume these are `bedrooms` and `bathrooms`.

Confirm:

* visible label text
* input/select element
* `id`
* `name`
* `wire:model`
* whether Select2 is involved
* whether these are standard inputs, selects, or custom widgets
* how the missing-fields notification currently derives the field label

Output the exact mapping.

---

## STEP 2 — TRACE THE JS REQUIRED-FIELD COLLECTION

Find the exact code used on Submit for:

* collecting required fields
* checking validity
* building the `invalidItems` / missing-fields array
* rendering the banner / notification text

Specifically inspect:

* `getAllRequiredFields()`
* `isFieldValid()`
* any helper that extracts field labels
* any filter that excludes hidden/conditional fields
* any logic that deduplicates or truncates the missing list

I need the exact reason why **Minimum Bedrooms** and **Minimum Bathrooms** are not being listed.

---

## STEP 3 — FIX THE REQUIRED-FIELD COLLECTOR SO IT RETURNS THE FULL SET

The JS collector must return **every currently relevant required field** for the Tenant flow.

That includes:

* normal text/number inputs
* radios
* standard selects
* Select2 fields
* badge/tag-based required fields
* conditionally required fields only when their condition is active

If a field is required and currently relevant, it must be included in the required-field array used at submit time.

Do not let the collector skip fields just because:

* they are in another tab
* they are rendered differently
* they use a custom wrapper
* they do not have a plain `required` attribute but are still required in the flow

---

## STEP 4 — FIX THE LABEL RESOLUTION

The notification list must show the **correct visible field names**.

For example, if the actual user-facing labels are:

* Minimum Bedrooms
* Minimum Bathrooms

then those exact names should appear in the missing-fields notification.

Do not show:

* internal property names
* wrong shortened names
* blank labels
* duplicate labels
* hidden technical names

If label extraction is currently failing, fix the mapping so every required field has a reliable notification label.

---

## STEP 5 — DO NOT TRUNCATE THE MISSING LIST

Check whether the current banner logic is:

* capping the number of items
* stopping after the first few fields
* deduplicating incorrectly
* overwriting the array mid-loop
* only reading the active tab
* only reading fields with a certain selector

Fix it so the list contains the **full set of missing required fields across the full Tenant form**, not just a partial sample.

---

## STEP 6 — VERIFY MINIMUM BEDROOMS / MINIMUM BATHROOMS SPECIFICALLY

After the fix, explicitly test this exact case:

### Test case:

Leave **Minimum Bedrooms** blank
Leave **Minimum Bathrooms** blank
Click Submit

### Expected result:

The missing-fields notification must include:

* Minimum Bedrooms
* Minimum Bathrooms

If they are required in the current flow, they must appear every time.

---

## STEP 7 — VERIFY FULL BLANK SUBMIT

Run a blank-form submit test on Hire a Tenant’s Agent.

### Expected:

The notification must list **every required field currently needed to submit**.

Not “some of them.”
Not “most of them.”
Every one.

If certain fields are conditional and not currently applicable, they should not appear.
If they are applicable and required, they must appear.

---

## STEP 8 — VERIFY SERVER + JS + UI ARE ALIGNED

For each required Tenant field, verify:

1. visual required marker
2. submit-time JS detection
3. server-side Livewire validation
4. notification label output

These all need to match.

A field is not considered fixed unless:

* it is required in the UI
* it is checked in JS
* it is validated in Livewire
* it appears by the correct name in the missing-fields notification when blank

---

## STEP 9 — RETURN A FINAL REPORT

After fixing, return:

1. exact file(s) changed
2. exact reason Minimum Bedrooms / Minimum Bathrooms were missing
3. exact fix applied
4. whether the missing-fields list was previously truncated or incomplete
5. proof from testing that a blank submit now lists every required Tenant field
6. confirmation that only Hire a Tenant’s Agent was touched

---

## IMPORTANT

The task is **not done** just because server-side validation was added.

The real requirement is:

> When the user clicks Submit, the system must identify and list **every required field that is missing**.

That includes **Minimum Bedrooms** and **Minimum Bathrooms** if those are required in the current Tenant flow.

---

And separately, from what they sent you, I already see a possible issue: they audited `bedrooms` and `bathrooms`, but your wording suggests the actual UI may be **Minimum Bedrooms** and **Minimum Bathrooms**, which could be different fields or at least different labels. So I would make them verify the exact rendered field mapping instead of trusting that prior audit.

If you want, I can turn your Seller edit note, Pets colon fix, and this Tenant required-fields fix into one organized Replit message with separate sections.
