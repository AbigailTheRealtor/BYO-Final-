CRITICAL SAFEGUARD — DO NOT MODIFY CORE LISTING OR BID LOGIC

This task is strictly limited to dashboard UI, navigation, and presentation changes only.

DO NOT modify, refactor, or replace any part of the following systems:

1. Listing Creation Flows (ALL ROLES)
   - Hire a Tenant’s Agent flow
   - Hire a Landlord’s Agent flow
   - Hire a Buyer’s Agent flow
   - Hire a Seller’s Agent flow
   - All tabs, fields, tooltips, validation, and dynamic logic inside these flows must remain unchanged

2. Bid / Auction System
   - Agent bid submission
   - Accept / Counter / Reject logic
   - Counter flow behavior
   - Bid state machine (Active, Countered, Accepted, Rejected, etc.)
   - Bid comparison and match score logic
   - Accepted Bid Summary generation and structure

3. Database Schema
   - DO NOT rename tables
   - DO NOT delete tables
   - DO NOT rename columns
   - DO NOT change relationships
   - Only read existing data to display on dashboard

4. Routing & Controllers
   - Do not change existing routes for hire flows or bid actions
   - Do not alter controller logic that powers listing creation or bid handling
   - Dashboard may route to existing pages, but must not change how those pages function

5. Role-Based Logic
   - Do not remove or merge Tenant, Buyer, Seller, or Landlord logic
   - Do not change how roles are assigned or processed
   - Only change how the dashboard displays the user’s active role

6. Livewire / Blade Components Used in Core Flows
   - Do not modify shared components used in listing creation or bid workflows
   - If reuse is needed, create new wrapper views instead of editing core components

7. Messages System
   - Do not alter message saving, routing, or conversation logic in this task
   - Dashboard may link to Messages, but should not modify message system behavior

IMPORTANT:
This task must be implemented as a non-destructive UI layer on top of the existing system.

If any requested change requires modifying core listing, bidding, or database logic, STOP and report instead of proceeding.