Replit Instructions — Hire a Seller’s Agent Listing
Tab: Broker Compensation & Agency Agreement Terms

Applies to: Hire a Seller’s Agent listing creation + the same fields wherever they appear in Agent Bids / Counter Bids for Hire a Seller’s Agent.

Example user profile: tenant@exp.com (Seller/User side listing intake)
(Agents will later interact with these same fields through bids/counters—do not break those.)

1) Fix the two “Buyer’s Broker …” labels (structure vs fee)
Current issue

Two fields are showing as “Buyer’s Broker Commission Fee” but they represent different things:

Field A (dropdown): “Seller to Pay Buyer’s Broker Separately” etc. → this is STRUCTURE

Field B (dropdown + input): “% of Total Purchase Price / Flat Fee / Other” → this is FEE

Required changes

✅ Rename Field A label to:
Buyer’s Broker Commission Structure:

✅ Rename Field B label to:
Buyer’s Broker Commission Fee:

✅ Ensure the tooltips remain attached to the correct field after renaming (don’t swap tooltip text or icon behavior).

IMPORTANT:
Do not change any database keys/columns. This is label-only (front-end text), not schema.

2) Clear prior input when switching compensation options (Flat Fee ↔ % ↔ Other)
Required behavior (must work every time)

When the user changes the selected option under Buyer’s Broker Commission Fee, the input state must reset so old values do not “stick” or carry over.

✅ If user selects:

Flat Fee → enters a number → switches to % of purchase price
➡️ Flat fee amount must be cleared.

✅ If user selects:

% of purchase price → enters a number → switches back to Flat Fee
➡️ Percentage amount must be cleared.

✅ If user selects:

Other
➡️ Clear all numeric inputs tied to percentage/flat fee, and only show the “Other” text input (if applicable).

Implementation requirement (so it actually fixes the bug)

Clearing must happen in the same Livewire state variables used for saving/submitting (not just visually).

Also clear hidden fields so they do not submit in the payload.

✅ Add this clearing logic to:

listing creation form (Hire a Seller’s Agent)

Agent bid submission form for the same listing type

Agent counter + User counter forms for the same listing type

3) Commercial/Business wording change for Month’s Rent option
Current issue

For Business + Commercial, a lease-fee option displays as:
“Percentage of First Month’s Rent”
…but you want it to be generic for multiple months.

Required change

✅ Under Business and Commercial property types ONLY:
Change label text from:
Percentage of First Month’s Rent
to:
Percentage of Month’s Rent:
(exact text requested)

✅ Then set the placeholder text for the percent input to:
Enter percentage of month’s rent (e.g., 100)

Important:

This is a UI label + placeholder change; do not rename the underlying field keys.

Do not apply this change to Residential/Income/Vacant Land unless explicitly specified.

4) Seller’s Broker Leasing Fee duplicate option removal (Residential/Income/Vacant Land)

✅ Under Seller’s Broker Leasing Fee, for Residential, Income & Vacant Land:

Remove the duplicate option: Percentage of Each Rental Period (repeated)

Do not remove it from other property types unless it’s also duplicated there.

5) Safeguards (MUST FOLLOW)
DO NOT:

rename or delete any DB columns, migrations, models, enums, or form keys

change tab order, UI layout, icons, styling, or field placement

change Buyer / Landlord / Tenant flows as part of this task

remove or disable listing-page bidding/countering logic

“fix” by making everything nullable globally

DO:

apply label/placeholder changes only where specified

ensure clearing logic is state-level (Livewire) and prevents stale hidden values from saving/submitting

keep validations conditional so hidden fields are not required

6) Acceptance Tests (Replit must verify)

Hire a Seller’s Agent → Tab Broker Compensation

Field A label reads Buyer’s Broker Commission Structure

Field B label reads Buyer’s Broker Commission Fee

Enter a Flat Fee amount → switch to % → Flat Fee clears

Enter % → switch back to Flat Fee → % clears

Business/Commercial lease option shows Percentage of Month’s Rent + placeholder:
“Enter percentage of month’s rent (e.g., 100)”

Residential/Income/Vacant Land: duplicate option removed