Task: Hire Seller Agent — Full Service only — UI/Select2 placeholder + icon + service selection fixes

URL:
 /hire/agent/auction/seller

Scope safeguards:
- ONLY work on the Hire Seller Agent flow.
- This flow uses the shared Livewire component:
  app/Livewire/TenantAgentAuction.php
  and shared blade:
  resources/views/livewire/tenant-agent-auction.blade.php
- Do NOT touch Create Offer Listing, Edit Offer Listing, Buyer, Tenant, Landlord, Limited Service, routes, migrations, database schema, models, or validation unless a direct Hire Seller Agent bug requires a tiny component-side fix.
- Do NOT remove existing fields, tooltips, bidding logic, draft logic, or submit logic.
- This is a surgical UI/Select2/Livewire hydration fix.

Issues to fix:

1. Hire Seller Agent — placeholder cleanup
In the Seller flow only, these select/multiselect fields must show exactly:
Placeholder: Select

Fields:
- Special Sale Provision
- Offered Financing/Currency
- Included Property or Business Assets
- Acceptable Exchange Item

Do not use long placeholder text like “Select acceptable exchange items.” These should all display only “Select.”

2. Acceptable Exchange Item — missing icon
The Acceptable Exchange Item field is missing its icon on initial load and/or after Livewire updates.

Fix requirements:
- Ensure the field wrapper has the same icon structure used by working select fields:
  .has-icon
  data-icon="..."
  .input-cover / Select2-compatible wrapper if applicable
- Ensure icon injection runs after:
  - DOMContentLoaded
  - Livewire message.processed
  - Bootstrap tab shown event
  - draftLoaded or any existing draft hydration event
- Prevent duplicate icons with the existing sentinel/dedup pattern already used elsewhere.
- Do not create a new icon system if one already exists. Reuse the existing addIconsToInputs() pattern.

3. Acceptable Exchange Item — cannot select options
The Select2 field for Acceptable Exchange Item currently opens/fails or does not allow option selection.

Fix requirements:
- Confirm the wire:model / property name matches the backend property used in TenantAgentAuction.php.
- Confirm it is initialized as a Select2 field only once.
- Remove conflicting duplicate Select2 initialization for this field if present.
- Ensure it uses:
  width: '100%'
  placeholder: 'Select'
  allowClear: true when appropriate
- Ensure Select2 change events correctly update Livewire using @this.set(...) or the existing shared sync function.
- Confirm selected values persist after Livewire re-render and draft load.

4. Services tab — selected service options can be deleted/removed unexpectedly
In the Services section, selected options should not disappear or delete themselves after selecting another service, changing tabs, saving draft, or Livewire re-render.

Fix requirements:
- Audit the service checkbox/multiselect handling in TenantAgentAuction.php and tenant-agent-auction.blade.php for Seller flow.
- Do not reset Seller service arrays during render, tab changes, Select2 reinitialization, draft load, or unrelated field updates.
- If services are stored as an array, preserve existing selected values and merge/sync safely instead of replacing with an empty array.
- Ensure “Other – Specify additional services as needed” behavior still works.
- Ensure selected services remain checked/selected after:
  1. selecting a service
  2. switching tabs
  3. returning to Services tab
  4. clicking Save Draft
  5. loading the draft again

Technical audit steps before editing:
- Search tenant-agent-auction.blade.php for:
  special_sale_provision
  offered_financing
  currency
  included_property
  business_assets
  acceptable_exchange
  exchange_item
  services
- Identify the exact field/property names before editing.
- Compare Acceptable Exchange Item markup/init against a working Seller select field with icon + Select2.
- Check for duplicate Select2 init blocks affecting the same field.
- Check Livewire hooks that rehydrate Select2 fields after updates.

Acceptance criteria:
- Hire Seller Agent only.
- Special Sale Provision placeholder = Select.
- Offered Financing/Currency placeholder = Select.
- Included Property or Business Assets placeholder = Select.
- Acceptable Exchange Item placeholder = Select.
- Acceptable Exchange Item icon appears immediately on first page load.
- Acceptable Exchange Item opens and allows selecting one or more options.
- Acceptable Exchange Item selections persist after tab switch, Livewire update, Save Draft, and draft reload.
- Services selections do not disappear/delete after selecting options, switching tabs, saving draft, or loading draft.
- No Limited Service files touched.
- No Offer Listing files touched.
- No Buyer/Tenant/Landlord behavior changed.

Verification:
Use login:
Abigailbaschuk@gmail.com
12345678

Test:
1. Open /hire/agent/auction/seller.
2. Go through the Seller Hire Agent tabs.
3. Confirm all listed placeholders say only “Select.”
4. Confirm Acceptable Exchange Item has icon and can select options.
5. Select multiple Services.
6. Switch tabs and return.
7. Save Draft.
8. Reload/load draft.
9. Confirm Services and Acceptable Exchange Item selections are still present.

Report back:
- Files changed.
- Exact field/property names fixed.
- Whether duplicate Select2 init was found.
- Screenshots or browser-test notes confirming each acceptance item.