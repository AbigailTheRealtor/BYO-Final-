eplit Instructions — Add Comma Auto-Formatting to Lease-Option Compensation Fields
Applies To

All roles under Broker Compensation & Agency Agreement Terms (where Lease-Option fields appear):

Hire a Tenant’s Agent

Hire a Buyer’s Agent

Hire a Seller’s Agent

Hire a Landlord’s Agent

Trigger

Only relevant when Interested in a Lease-Option Agreement = Yes (because that’s when the fields show), but formatting should work anytime the fields are visible.

Fields (Must Update)

Compensation for Creating the Lease-Option Agreement:

Compensation if Purchase Option is Exercised:

SAFEGUARDS (Must Follow)

Do NOT rename any Livewire property, form name, input wire:model, database column, or validation key.

Do NOT change fee logic, calculations, or required/optional status.

Do NOT store commas in the database (store clean numeric values only).

Match the exact formatting behavior used by the existing “Desired Rental Amount” input (this is the source of truth).

Confirm after change: Save Draft, Edit Draft, Next Tab, Submit all still work for Tenant/Buyer/Seller/Landlord.

Step 1 — Reuse the Same Numeric Input Behavior as “Desired Rental Amount”
Reference (Source of Truth)

Locate the working input that formats commas:

Hire a Landlord → Desired Rental Amount

In code, identify how it’s implemented. It’s typically one of these:

A shared Blade component (recommended)

A JS input mask (Cleave.js / AutoNumeric / Inputmask)

A Livewire + Alpine formatter

A reusable CSS/JS class like money-input, currency-input, thousands-separator

✅ Whatever mechanism is used there must be applied to the two Lease-Option compensation inputs.

Step 2 — Apply the Same Mask/Class/Component to the 2 Lease-Option Inputs

In each role’s Broker Compensation & Agency Agreement Terms view, update the two inputs so they use the same formatting method as Desired Rental Amount.

Requirement

When user types 343434 it should display as 343,434

Must work while typing (not only on blur)

Must keep cursor behavior acceptable (same as Desired Rental Amount)

Implementation (Choose the method that matches your existing build)
Option A (Best): Convert Lease-Option fields to use the same shared component as Desired Rental Amount

If Desired Rental Amount uses a component like:

<x-currency-input ... /> or <x-money-input ... />

Then render BOTH Lease-Option fields using that same component.

✅ This guarantees identical behavior and reduces bugs.

Option B: If Desired Rental Amount uses a JS mask via a class or data attribute

Example patterns:

class="currency-format"

data-mask="money"

x-data="moneyMask()"

Then add the same class/data/x-data to:

Compensation for Creating the Lease-Option Agreement input

Compensation if Purchase Option is Exercised input

⚠️ Do not add a new library or new mask style. Reuse the existing one.

Step 3 — Ensure Clean Numeric Value is Saved (No Commas Stored)

Because these are currency fields, the UI can show commas, but Livewire/DB should store a clean value.

Required behavior

UI displays: 1,500

Stored value: 1500

Implement the same cleaning used by Desired Rental Amount

Wherever the app normalizes Desired Rental Amount before save/validation:

replicate that same normalization for both Lease-Option fields.

Examples of acceptable approaches (use whichever matches your current pattern):

A shared sanitizer used on submit

Livewire updated hooks (e.g., when the value changes)

A “before save” mutator

Server-side normalization during validation

✅ The key is: Lease-Option fields must be sanitized exactly the same way Desired Rental Amount is sanitized.

Step 4 — Required QA Tests (Replit Must Verify)
Formatting tests

For each role (Tenant/Buyer/Seller/Landlord):

Select Lease-Option = Yes

Type 1500 → shows 1,500

Type 50000 → shows 50,000

Paste 1000000 → shows 1,000,000

Delete/edit mid-string → formatting remains stable

Save & persistence tests

Save draft with 1,500 displayed

Reload draft → should display as 1,500

Submit listing → stored numeric value should be clean (no commas)

PDF / summaries (if any) should display formatted properly (optional, but confirm nothing breaks)

Completion Definition

✅ Both Lease-Option compensation inputs in Tenant/Buyer/Seller/Landlord:

auto-insert commas while typing

behave the same as Desired Rental Amount

do not store commas in DB

do not break drafts/submission