TASK: READ-ONLY AUDIT — Offer Listing Edit/Draft Icon + Select2 Formatting for Landlord, Buyer, Seller

IMPORTANT SAFEGUARDS:
- READ-ONLY AUDIT ONLY.
- DO NOT edit files yet.
- DO NOT modify database/schema.
- DO NOT run migrations, seeders, refresh, truncate, or data-changing commands.
- DO NOT touch Tenant flow; Tenant was already fixed.
- DO NOT touch Hire Agent flows.
- ONLY audit Offer Listing Edit/Draft flows for Landlord, Buyer, and Seller.
- DO NOT change fields, labels, placeholders, tooltips, validation, tab order, Save Draft, Submit, or Livewire properties.
- Stop after reporting findings.

BACKGROUND:
Tenant Edit/Draft icon issue was fixed by aligning the Edit/Draft blade with the working Create blade:
- robust addIconsToInputs() logic
- deferred icon reinjection after Livewire/Select2 hydration
- Select2 icon padding rule:
  .input-cover.has-select-icon .select2-container .select2-selection {
      padding-left: 44px !important;
  }
- icon z-index: 10
- no duplicate icons

Now audit the remaining Offer Listing Edit/Draft flows to see if they have the same problem.

AUDIT SCOPE:
Audit these Offer Listing flows only:
1. Landlord Edit/Draft
2. Buyer Edit/Draft
3. Seller Edit/Draft

Compare each Edit/Draft blade against its corresponding Create blade.

Likely file pairs:
- Landlord Create vs Edit/Draft:
  resources/views/livewire/offer-listing/landlord/offer-landlord-listing.blade.php
  resources/views/livewire/offer-listing/landlord/offer-landlord-listing-edit.blade.php

- Buyer Create vs Edit/Draft:
  resources/views/livewire/offer-listing/buyer/offer-buyer-listing.blade.php
  resources/views/livewire/offer-listing/buyer/offer-buyer-listing-edit.blade.php

- Seller Create vs Edit/Draft:
  resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php
  resources/views/livewire/offer-listing/seller/offer-seller-listing-edit.blade.php

AUDIT EACH ROLE FOR:
1. CSS parity:
   - .input-cover
   - .input-icon
   - .has-icon
   - .form-control.has-icon
   - .input-cover.has-select-icon .select2-container .select2-selection
   - Select2 styling
   - multi-select/tag styling
   - textarea styling
   - compact field styling
   - icon padding
   - icon z-index
   - mobile media rules
   - .set-button gap rules

2. JS icon initialization parity:
   - addIconsToInputs() implementation
   - data-icon-rendered sentinel usage
   - orphan cleanup:
     .input-icon:not(.data-icon-rendered)
   - closest('.input-cover') usage
   - DOMContentLoaded call
   - livewire:load call
   - Livewire.hook('message.processed') call
   - draftLoaded/draft-saved handlers
   - tab shown.bs.tab handlers
   - Select2 rehydration timing

3. Visual behavior risk:
   - icons missing on initial Edit load
   - icons appearing only after tab switch
   - icons showing but text overlaps
   - Select2 icon fields missing padding
   - duplicate icons after repeated refreshes
   - formatting differences from Create

REQUIRED REPORT:
For each role, return:

LANDLORD:
- Create blade path
- Edit blade path
- Whether Edit/Draft matches Create
- Missing CSS rules, if any
- Missing JS/icon lifecycle calls, if any
- Duplicate-icon risk, if any
- Recommended fix level:
  A. No fix needed
  B. CSS-only parity fix
  C. JS-only lifecycle fix
  D. CSS + JS parity fix

BUYER:
same report format

SELLER:
same report format

IMPORTANT:
Do not implement fixes yet. Only report findings and recommended smallest safe fix per role.