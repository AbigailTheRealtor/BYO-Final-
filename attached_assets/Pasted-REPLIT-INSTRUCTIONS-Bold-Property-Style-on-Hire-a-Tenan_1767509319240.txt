REPLIT INSTRUCTIONS — Bold Property Style on Hire a Tenant Listing Cards (Residential + Commercial)

Applies ONLY to:
BidYourAgent → Hire a Tenant listing cards (Residential + Commercial)

🚨 SAFEGUARDS (MANDATORY — ADDITION / DISPLAY-ONLY)

DO NOT:

delete/rename database columns, models, migrations, or relationships

change listing creation fields/keys, validations, or stored values

change bid/counter/accept logic

change the card layout structure, spacing, icons, tooltips, or wording

change what fields appear on the card (only adjust text styling)

refactor unrelated components or shared UI code outside this card display change

YOU MAY ONLY:

update the display styling of the Property Style text on Hire a Tenant listing cards

add a small helper/class name to apply bold styling

ensure the change is scoped ONLY to Hire a Tenant cards

🎯 GOAL

On Hire a Tenant listing cards (Residential + Commercial), make the Property Style line bold to improve visual hierarchy and scannability.

Examples of Property Style:

Residential: “Single-Family Home”, “Condo”, “Apartment”, “Townhome”

Commercial: “Office”, “Retail”, “Warehouse”, “Mixed Use”, etc.

✅ REQUIRED BEHAVIOR
1) Bold ONLY the Property Style line

The line that currently displays the property style (next to the house/building icon) must render in bold.

Example:

🏠 Single-Family Home → 🏠 **Single-Family Home**

🏢 Office → 🏢 **Office**

2) Do NOT bold any other lines

Keep these normal weight:

Header: Tenant • Residential / Tenant • Commercial

Beds/Baths/Sq Ft row

County/location line

“Tenant’s Agent Required”

Commission Structure + Commission Fee lines (keep existing bold formatting exactly as-is)

🔍 IMPLEMENTATION NOTES (SCOPE CONTROL)

Apply this bold styling only when:

Listing Type = Hire a Tenant

The change must apply to both:

Residential cards

Commercial cards

If the property style is stored as “Other” with a user-entered value, the displayed value (the user’s typed input) should still be bold.

✅ ACCEPTANCE CHECKS

On Hire a Tenant cards, Property Style is bold (Residential + Commercial).

No other text becomes bold or changes.

No database changes occur.

No listing types other than Hire a Tenant are affected.