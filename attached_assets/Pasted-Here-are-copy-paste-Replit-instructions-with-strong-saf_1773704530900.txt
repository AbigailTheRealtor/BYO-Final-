Here are **copy-paste Replit instructions with strong safeguards** for this exact **Hire a Buyer Agent** edit only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A BUYER AGENT ONLY — PETS NOT REQUIRED + OFFERED FINANCING/CURRENCY REQUIRED UI FIX

## SCOPE

Make these fixes **only for Hire a Buyer Agent**:

1. **Pets**

   * remove it as a required field
   * remove the **red star**
   * remove the red helper text underneath that says:
     **This field is required.**

2. **Offered Financing/Currency**

   * keep it required
   * make sure it shows the same required-field UI as the other required Buyer fields
   * specifically, it should show:
     **This field is required.**
     in red underneath the field like the others

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Buyer Agent flow.**
2. **Do not modify Tenant, Landlord, or Seller flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change Select2 initialization.**
10. **Do not change selection box format, width, styling, placeholder behavior, badge style, dropdown behavior, or general UI structure.**
11. **Do not change tooltips, icons, placeholders, or field order.**
12. **Do not do a broad cleanup or refactor.**
13. **Do not change any other Buyer fields except the exact two fields listed below.**
14. **Keep this surgical and field-specific.**

---

## IMPORTANT

We have had issues before with edits being made in the wrong file.

### Hard stop:

Before changing anything, first confirm the **actual active Buyer files** for:

* Buyer create/edit form
* Buyer field for **Pets**
* Buyer field for **Offered Financing/Currency**
* Buyer required-field UI / invalid-feedback display logic

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap the change so it affects **Buyer only**.

---

## STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Buyer Agent**:

1. Livewire/component file handling Buyer create/edit
2. Blade/partial rendering **Pets**
3. Blade/partial rendering **Offered Financing/Currency**
4. File/partial responsible for Buyer required-field red star display
5. File/partial responsible for Buyer red helper text:
   **This field is required.**

### Requirement

Return the exact files first, then edit **only those files**.

### Hard stop

**No other file should be edited or changed.**

---

## STEP 2 — REMOVE PETS AS A REQUIRED FIELD

## Required behavior

For **Buyer Pets only**:

* remove required enforcement
* remove the **red star**
* remove the red helper text:
  **This field is required.**
* it should no longer block submit

### Important

Align all 3 layers for **Buyer Pets only**:

1. visual required state
2. front-end submit blocking
3. server-side validation

### Do not:

* affect Pets in Tenant, Landlord, or Seller
* change field format
* change label text
* change options
* change select behavior

---

## STEP 3 — FIX OFFERED FINANCING/CURRENCY REQUIRED-STATE UI

## Current bug

**Offered Financing/Currency** is required, but it does **not** show the required helper text correctly like the other required Buyer fields.

## Required behavior

For **Buyer Offered Financing/Currency only**:

* keep the field required
* make sure it shows the same required UI pattern as the other required Buyer fields
* specifically, it should show in red under the field:

**This field is required.**

### Important

This is a **required-state UI alignment fix** only.
Do **not** remove required logic from Offered Financing/Currency.

## What to trace and fix

Confirm:

* whether the field already has JS required validation
* whether it already has PHP required validation
* why the field is missing:

  * red star
  * required helper text
  * invalid feedback block
  * required class hook
  * whatever pattern the other Buyer required fields use

Then make **Offered Financing/Currency** use that same pattern.

### Important

Do not change how other required Buyer fields work.
Just make this field match them.

---

## STEP 4 — VERIFY BUYER REQUIRED-FIELD UI STAYS CONSISTENT

After the fix:

### Pets

* should **not** have:

  * red star
  * red helper text
  * required validation blocking

### Offered Financing/Currency

* **should** have:

  * required status
  * same red helper text pattern as other required Buyer fields:
    **This field is required.**

---

## STEP 5 — TEST THESE EXACT CASES

### A. Pets

* open Buyer form
* expected: no red star
* expected: no red helper text
* expected: leaving it blank does not block submit

### B. Offered Financing/Currency

* open Buyer form
* expected: required-state UI matches the other required Buyer fields
* expected: red helper text appears correctly:
  **This field is required.**
* expected: leaving it blank still blocks submit

### C. Other Buyer required fields

* expected: unchanged

---

## STEP 6 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Tenant files changed
* no Landlord files changed
* no Seller files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Buyer fields changed

---

## STEP 7 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Buyer files were identified first
3. exact root cause for:

   * Pets being incorrectly required
   * Offered Financing/Currency missing the proper required helper text
4. exact fix applied for each
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof Pets is no longer required
8. proof Offered Financing/Currency now shows:
   **This field is required.**

---

## IMPORTANT FINAL RULE

This is a **Buyer-only, surgical fix**.

Do **not**:

* edit unrelated files
* change selection box format
* refactor global required-field logic
* touch Tenant, Landlord, or Seller
* make broad UI changes

The final result should be:

* Pets is not required
* Pets has no red star
* Pets does not show **This field is required.**
* Offered Financing/Currency remains required
* Offered Financing/Currency shows **This field is required.** in red like the other required Buyer fields
* no other file or selection input behavior is changed

---

I can also combine this with your other Buyer fixes into one grouped Replit instruction block if you want.
