Use this exact message for Replit:

---

## CRITICAL INVESTIGATION — CONFIRM THE EXACT ACTIVE COMPONENT PATH FOR SELLER VS TENANT

Before making any more fixes, I need you to trace the **actual active code path** for both of these routes:

### Seller route

```text
/hire/agent/auction/seller
```

### Tenant route

```text
/hire/agent/auction/tenant
```

I want to confirm whether the Seller flow is still running through the same shared Tenant-based component / PHP path, with role-based branching, instead of a truly separate Seller-only component.

---

# WHAT TO TRACE

For **each** of these two routes, please tell me the exact:

1. route definition
2. active Livewire component
3. active PHP class
4. active Blade view
5. included partials for the Sale Terms / Leasing Terms tab
6. where the Select2 initialization logic is attached
7. where the conditional visibility logic is attached for:

   * Special Sale Provision → Other
   * Special Sale Provision → Assignment Contract
   * Offered Financing/Currency dependent sections

---

# WHY I AM ASKING

The live Seller Sale Terms form is still not behaving correctly.

I want to confirm whether the Seller route is actually using the same shared wizard/component path as Tenant, with only the role switched internally.

If that is true, then fixes may need to be made in the shared component / shared PHP / shared Blade path rather than assuming a separate Seller-only logic path.

---

# REQUIRED RESPONSE FORMAT

Please respond exactly like this:

## Tenant route uses:

* Route:
* Livewire component:
* PHP class:
* Blade view:
* Sale Terms / Leasing Terms partial(s):
* Select2 initialization source:
* Conditional visibility source:

## Seller route uses:

* Route:
* Livewire component:
* PHP class:
* Blade view:
* Sale Terms partial(s):
* Select2 initialization source:
* Conditional visibility source:

## Final answer:

* Are Seller and Tenant using the same shared component path?
* If yes, what exact shared file is the real source of the Seller Sale Terms logic?

---

# IMPORTANT

Do **not** guess.

Please trace the actual active code path being used by the live form.

I need to know whether the Seller form is still running through the shared Tenant-based wizard/component logic.

---
