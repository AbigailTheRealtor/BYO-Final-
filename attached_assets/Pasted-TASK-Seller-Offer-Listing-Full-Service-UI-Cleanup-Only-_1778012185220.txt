TASK: Seller Offer Listing — Full Service UI Cleanup Only

SCOPE:
Create Seller Offer Listing only:
- /offer-listing/seller
- Full Service / commission-based Seller flow only

LOGIN TEST:
Abigailbaschuk@gmail.com
12345678

DO NOT TOUCH:
- Limited Service files
- Buyer Offer Listing
- Landlord Offer Listing
- Tenant Offer Listing
- Hire Agent flows
- Agent Profile flows
- Routes
- Database schema
- Validation / Next button logic
- Tab structure

--------------------------------------------------
PART 1 — FIX OPERATING STATEMENT AVAILABLE ICON
--------------------------------------------------

FIELD:
- Operating Statement Available

ISSUE:
The icon is not aligned correctly next to the select.

FIX:
Use the same working select/icon structure as:

- Rent Roll Available

Requirements:
- Icon should appear inside the input/select row, next to the select text.
- Do not leave a standalone icon outside the wrapper.
- Do not duplicate icons.
- Keep existing wire:model binding.
- Keep existing select options.

--------------------------------------------------
PART 2 — FIND/FIX REASON FOR SALE “OTHER” INPUT
--------------------------------------------------

FIELD:
- Reason for Sale

ISSUE:
Need to confirm where this field is located in the Seller create flow.
When user selects “Other,” a custom input box should appear.

FIX:
- Locate Reason for Sale in the Seller Full Service create flow.
- If Reason for Sale already exists, confirm/selecting “Other” shows a custom input.
- If it does not appear, fix the conditional display.

Other input placeholder must follow this format:

Enter reason for sale (e.g., Downsizing)

Requirements:
- Use Blade @if conditional display if that is the existing pattern.
- Do not use inline display:none.
- Do not create duplicate inputs.
- Do not add wrapper IDs unless existing nearby pattern requires it.
- Keep existing wire:model bindings.
- If select uses wire:model.defer and the @if does not update immediately, change only that select to wire:model so “Other” appears immediately.

STOP if:
- Reason for Sale cannot be found in the Seller Full Service create flow.
- There are multiple Reason for Sale fields and it is unclear which is active.

--------------------------------------------------
PART 3 — FIX SELECT PLACEHOLDERS
--------------------------------------------------

FIELDS:
- Offered Financing/Currency
- Special Sale Provision
- Additional Document Available

ISSUE:
These fields are missing the placeholder word “Select.”

FIX:
The placeholder must be exactly:

Select

Reference:
Use the working placeholder pattern from:

- Property Condition

Requirements:
- If this is a normal select, make sure the first placeholder option is:
  <option value="">Select</option>
- If Select2 is used, make sure placeholder is:
  placeholder: "Select"
- Do not use field-specific placeholder text.
- Do not use examples inside select placeholders.
- Keep existing options and wire:model bindings.

--------------------------------------------------
VERIFICATION
--------------------------------------------------

Test:
/offer-listing/seller

Confirm:
- Operating Statement Available icon matches Rent Roll Available structure.
- No icon overlap or duplicate icon.
- Reason for Sale can be located.
- Selecting Other for Reason for Sale shows the custom input.
- Custom input placeholder says:
  Enter reason for sale (e.g., Downsizing)
- Offered Financing/Currency placeholder says Select.
- Special Sale Provision placeholder says Select.
- Additional Document Available placeholder says Select.
- Property Condition still works correctly.
- No Limited Service files changed.
- No Buyer/Landlord/Tenant files changed.

RUN:
php -l on every edited PHP/Blade file.

HARD SAFEGUARDS:
- No migrations
- No migrate:fresh
- No schema changes
- No broad refactors
- No unrelated flow edits
- Stop and report if unsure.