Replit Instructions — Tenant Flow ONLY: Fix Services Display (Residential + Commercial) + Fix Missing Auto-Fill Fields on Agent Bids
Scope (STRICT)

✅ Apply changes ONLY to Hire a Tenant’s Agent flow
✅ Includes Residential + Commercial Tenant listings
❌ Do NOT modify Buyer/Seller/Landlord flows
❌ Do NOT change database/migrations/models
❌ Do NOT rename any field keys, inputs, or Livewire properties

0) SAFEGUARDS (MUST FOLLOW EXACTLY)

DO NOT:

edit anything in /vendor/

run composer updates / upgrades

delete/rename DB columns, migrations, models, controllers, or routes

rename request keys or input name="" attributes

change any tooltip/placeholder/service text in the checklist UI

modify how services are saved to the listing or how bids are saved

refactor or reformat files beyond the minimal changed lines

change bid action logic (Accept/Counter/Reject)

ONLY DO:

fix the Actual Listing Display “Services” renderer (read-only rendering fix)

fix the Agent Bid initialization auto-fill for specific compensation fields

PART A — Fix Actual Listing Display “Services” (Residential + Commercial) to Match Checklist Exactly
A1) Problem

The Actual Listing Display → Services is showing services:

missing items, and/or

out of order
compared to the agent checklist services list.

The checklist list is correct. The listing display is wrong.

A2) Required Fix

Update the listing display to render services using the same services config/source-of-truth used by the agent checklist — preserving:

identical category order

identical bullet order

no missing selected services

A3) Implementation Steps

Locate the services config/array(s) used by the agent checklist for:

Residential tenant services

Commercial tenant services
(search category headers like “Tenant Criteria Marketing & Promotion” and “Lease Preparation, LOI & Execution”)

Locate the file/component for the Actual Listing Display → Services section (the page that shows “Services:” with bullets).

Remove/stop using any hardcoded/outdated list used only for the listing display.

Render based on:

the listing’s saved selected service keys (normalize to an array)

the appropriate services config based on property type:

Residential → Residential config

Commercial → Commercial config

Rendering logic must:

loop categories in config order

within each category, loop services in config order

display only services that are selected on the listing

Add a safety fallback so no selected services disappear:

compute selectedKeys not found in config

display them under “Additional / Unmapped Services” at the bottom (rare edge case)

✅ Result: Listing Services display matches the checklist exactly, no missing, correct order.

A4) Acceptance Test (Required)

Create Residential listing → select many services → verify display matches checklist order and none missing

Create Commercial listing → select many services → verify display matches checklist order and none missing

PART B — Fix Agent Bid Auto-Fill on Broker Compensation & Agency Agreement Terms (Residential + Commercial)
B1) Problems

When an agent places a bid on a Tenant listing:

Residential bids:

Payment Timing for Broker Fees does NOT auto-fill

Retainer Fee Application does NOT auto-fill
Even though the listing creator filled these out during listing creation.

Commercial bids:

Payment Timing for Broker Fees does NOT auto-fill
Even though the listing creator filled it out during listing creation.

B2) Expected Behavior

When the agent opens the bid form (initial load of bid modal/page):

These fields must default to the listing creator’s values automatically.

Residential:

bid.PaymentTimingForBrokerFees = listing.PaymentTimingForBrokerFees

bid.RetainerFeeApplication = listing.RetainerFeeApplication

Commercial:

bid.PaymentTimingForBrokerFees = listing.PaymentTimingForBrokerFees

✅ This is a default-populate behavior.
✅ If an agent changes it, current override behavior should remain (do not change that in this task).

B3) Implementation Steps (Do exactly)

Identify the listing field keys used to store:

Payment Timing for Broker Fees

Retainer Fee Application
for both Residential and Commercial listings (keys may differ — do not rename them).

Identify where the agent bid form is initialized (Tenant only), typically:

Livewire component mount()

initBidForm() / openBidModal()

controller method creating a bid draft

In the bid initialization step:

If bid.PaymentTimingForBrokerFees is empty/null, set it from listing value.

If Residential and bid.RetainerFeeApplication is empty/null, set it from listing value.

Important: Use “set only if empty” to avoid overwriting an existing bid draft.

Pseudo-rule:

if empty(bid.field) then bid.field = listing.field

Confirm these values display correctly in:

the bid entry form inputs

any bid summary/terms view shown to agents/creator

B4) Acceptance Tests (Required)
Residential:

Create Residential Tenant listing and fill both fields

Login as agent → place bid
✅ both fields are pre-filled automatically

Commercial:

Create Commercial Tenant listing and fill Payment Timing

Login as agent → place bid
✅ Payment Timing is pre-filled automatically

REQUIRED REPORT BACK

After changes:

provide file paths edited for Part A and Part B

provide before/after snippets for:

listing services renderer loop

bid initialization mapping lines

confirm:

no schema changes

no edits outside Tenant flow

checklist UI not modified