We are restructuring the **listing list pages / role tabs** so they match the current BidYourAgent workflow.

This is **not** a deletion. It is a UI/UX correction.

## Apply this to these 4 user-side listing pages:

* **Hire Tenant’s Agent**
* **Hire Landlord’s Agent**
* **Hire Buyer’s Agent**
* **Hire Seller’s Agent**

These are the role-specific listing list/table pages the logged-in user opens from the dashboard sidebar under **My Listings**.

---

## GOAL

Turn each of these pages into a proper:

**Listing → View Bids / Manage Listing control panel**

These pages should help the listing creator:

* open the listing
* open bids on that listing
* edit the listing
* eventually open accepted summary if one exists

They should **not** contain old/legacy tools that don’t belong in the current flow.

---

## REQUIRED CHANGES

### 1. REMOVE these old dropdown items from the Action menu on all 4 pages:

* **Add Terms**
* **Manage Chat Bot Questions**

These do not belong in the current hire-agent workflow.

Apply removal on:

* Hire Tenant’s Agent listings table
* Hire Landlord’s Agent listings table
* Hire Buyer’s Agent listings table
* Hire Seller’s Agent listings table

---

### 2. FIX the current "View" behavior

The current generic "View" action must **not** send the user into counter terms or the wrong flow.

For each of the 4 role pages, make sure the actions are clearly separated as:

* **View Listing**
* **View Bids**
* **Edit Listing**

If there is an accepted summary already tied to that listing, optional:

* **View Accepted Summary**

---

### 3. ADD a visible primary button in each row:

## **View Bids**

This should be the main CTA because the platform revolves around bids and decisions.

Apply this on all 4 role listing pages:

* Hire Tenant’s Agent
* Hire Landlord’s Agent
* Hire Buyer’s Agent
* Hire Seller’s Agent

---

## ROUTING RULE

Use the correct existing role-specific routes/pages already in the system.

### The 4 role list/table pages should behave like this:

#### Hire Tenant’s Agent listings

* View Listing → tenant listing detail page
* View Bids → tenant bid/listing detail page where bids are reviewed
* Edit Listing → tenant listing edit page

#### Hire Landlord’s Agent listings

* View Listing → landlord listing detail page
* View Bids → landlord bid/listing detail page where bids are reviewed
* Edit Listing → landlord listing edit page

#### Hire Buyer’s Agent listings

* View Listing → buyer listing detail page
* View Bids → buyer bid/listing detail page where bids are reviewed
* Edit Listing → buyer listing edit page

#### Hire Seller’s Agent listings

* View Listing → seller listing detail page
* View Bids → seller bid/listing detail page where bids are reviewed
* Edit Listing → seller listing edit page

Do **not** invent new routes.
Do **not** change controller logic unless needed only to point to already-existing working routes.

---

## UX RULE

These listing pages are not “tool management” pages anymore.

They should function as:

* a list of the user’s listings
* with a direct path to **View Bids**
* and secondary access to **View Listing** and **Edit Listing**

---

## SAFEGUARDS

DO NOT:

* modify listing creation logic
* modify bids
* modify counter logic
* modify accepted summary logic
* modify acknowledgement/signature logic
* change business rules

This is a **UI/link cleanup only** for those 4 listing list pages.

---

## REQUIRED OUTPUT

After changes, report:

1. exact files changed
2. which of the 4 role pages were updated
3. exact old dropdown items removed
4. exact new actions shown in each row
5. confirmation that **View Bids** routes correctly for all 4 roles
6. confirmation no core logic was changed
