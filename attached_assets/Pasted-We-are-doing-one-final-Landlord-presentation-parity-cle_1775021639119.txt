We are doing one final **Landlord presentation parity cleanup** before moving to Seller.

I do **not** want interpretation here. I want exact Tenant-style presentation logic.

## Scope

Landlord bid preview card and Landlord private full bid modal only.

Do not modify:

* Tenant
* Buyer
* Seller
* scoring logic
* DB/schema
* saved values
* accepted summary logic

---

## Hard safeguards

* No destructive DB changes
* No schema changes
* No dropping/renaming columns
* Do not change field names
* Do not change stored values
* Do not change scoring/match calculations
* Do not redesign Tenant
* Only clean up Landlord presentation so it mirrors Tenant more closely

---

## 1. Remove `Tenant's Broker Compensation` from the Landlord bid preview card

### Problem

The Landlord compact preview card still shows:

* `Tenant's Broker Compensation:`

I do **not** want that line in the compact Landlord preview.

### Required fix

In the **Landlord bid card preview only**:

* remove the `Tenant's Broker Compensation` preview row entirely
* keep the preview focused on the Landlord’s own primary compensation summary
* do not dump extra technical compensation rows into the compact preview

### What the compact Landlord preview should show

Use a clean compact summary like:

* `Landlord's Broker Lease Fee:`
* resolved value
* optional one supporting line if useful, such as payment timing

Do not show tenant-broker compensation in the compact preview card.

### Important

This instruction applies to the **preview card only**.
Do not remove that field from the full modal if it is intentionally part of the full compensation detail.

---

## 2. Make `Agent Overview & Qualifications` match Tenant more closely

### Problem

The Landlord modal still does not visually feel identical enough to Tenant in the `Agent Overview & Qualifications` section.

### Required fix

Compare Tenant vs Landlord side by side and match Tenant exactly for:

* heading spacing
* heading color
* section spacing
* two-column layout rhythm
* paragraph spacing
* link spacing
* website / social / reviews / licensed year placement
* margin/padding between blocks

Do not redesign. Mirror Tenant.

### Important

This is presentation cleanup only.
Do not change the actual text content.

---

## 3. Final presentation parity check for Landlord modal

After the above changes, verify all of these:

### Bid preview card

* no `Tenant's Broker Compensation` line in compact preview
* Landlord compensation preview remains useful and readable
* card still shows correct match counts
* no scoring/data logic changed

### Agent Overview & Qualifications

* visually matches Tenant section as closely as possible
* same spacing rhythm
* same hierarchy
* same layout behavior

### Full modal

* Tenant-broker compensation may remain in full compensation details if intended
* no duplicate blocks
* no raw/internal values
* no visual drift from Tenant beyond role-specific content

---

## Deliverable

Give me:

1. exact Landlord file edited
2. screenshot of updated Landlord preview card
3. screenshot of updated Landlord `Agent Overview & Qualifications`
4. confirmation that `Tenant's Broker Compensation` was removed from the preview card only
5. confirmation that scoring/data logic was not changed
