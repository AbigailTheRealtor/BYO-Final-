Yeah—this is exactly the kind of “agent goes exploring and refactors the world” behavior that burns credits. We’re going to **stop that** and force Replit into a **single-bug, single-file, no-architecture-discovery** fix.

The key is: you don’t want Replit “tracing routes/components” at all. You want a **surgical patch**: **Edit form must preload whatever is already saved**, and **save must not wipe it**.

Copy/paste the instructions below. They are written to **block** the rabbit hole it went down last time.

---

## REPLIT INSTRUCTIONS (SELLER ONLY) — ONE BUG ONLY: “Acceptable Exchange/Trade shows blank on Edit”

### HARD SAFEGUARDS (NON-NEGOTIABLE)

1. **Do NOT investigate or “trace architecture.”** No route audits, no component discovery, no documenting, no refactors.
2. **Do NOT modify any other keys** (offered_financing vs financings, etc). Leave financing alone.
3. **Do NOT add new sections** or rebuild the Exchange/Trade UI.
4. **Do NOT touch Tenant/Buyer/Landlord** code.
5. **Touch at most TWO places**:

   * the Seller Edit form view **where the Acceptable Exchange/Trade input is rendered**
   * the Seller update/save handler **only if needed** to prevent wiping values
6. **No new JS** unless the field literally cannot appear without it.
7. Goal: **autopopulate the existing saved value** into the edit input and **keep it from clearing on save**.

---

# TARGET BUG

In **Hire a Seller’s Agent → Edit**, the field labeled **“Acceptable Exchange Item”** renders **blank**, even though the listing already has a saved value.

---

# FIX PLAN (CHEAP + SURGICAL)

## STEP 1 — Confirm the exact stored value key (NO exploration)

In the Seller update handler you already use for other fields, find where metadata/fields are read/written and identify the exact key that stores this value. It will be something like:

* `exchange_item`
* `acceptable_exchange_item`
* `acceptable_exchange_trade_item`

✅ Do not search the whole repo. Only look inside the Seller edit/save code that already handles Seller fields.

---

## STEP 2 — Pre-populate the edit input (PRIMARY FIX)

In the Seller **edit view** where the input `<input ...>` for “Acceptable Exchange Item” is displayed, set its value using **existing saved data**.

### Requirement

Whatever renders the edit form must set the input’s `value=` to:

* the previously saved value **as-is** if it’s a string
* if it is JSON/array, render it into a string display that matches your create UI expectation (comma-separated is fine unless your UI expects something else)

### Backward-compatible decoding rule (IMPORTANT)

If the stored value might be:

* JSON array string (e.g. `["Boat","RV"]`)
* plain string (e.g. `"Other"`)

Then:

* If it starts with `[` and decodes to an array → use array
* Else treat it as a plain string and keep it

**Do NOT use `json_decode($value) ?? []`** because that turns `"Other"` into blank. You must preserve the legacy single-string value.

✅ This one rule is the difference between “works” and “blank again.”

---

## STEP 3 — Prevent the value from being wiped on save (SECONDARY FIX)

In the Seller update/save handler:

* If the edit input is missing/empty in the request, **do NOT overwrite** the stored exchange value with null/empty.

Rule:

* Only update the stored key if the request contains a non-empty value OR if an explicit “remove” action exists (otherwise keep existing)

✅ This prevents the “it worked for 24 minutes then disappeared” issue when edits save without that input.

---

# ACCEPTANCE TEST (MUST PASS)

1. Open a listing that already has Acceptable Exchange Item saved.
2. Click Edit.
   ✅ The field shows the last saved value immediately.
3. Click Save without touching it.
4. Click Edit again.
   ✅ Still populated.

---

# COST CONTROL RULE (VERY IMPORTANT)

**If you cannot fix the bug by editing ONLY the edit view value binding + the save handler “don’t wipe if empty” guard, STOP and report exactly why. Do not implement any route/component rewiring.**

---

## If you want, I’ll rewrite this even tighter

If you paste **just these two snippets** (no more):

1. the HTML line(s) for the “Acceptable Exchange Item” input from the Seller edit view
2. the part of the Seller controller/update method where it saves metadata/fields

…I’ll give you a literal “replace this with that” patch that Replit can apply in under 2 minutes without wandering.

And yeah—your frustration is valid. That 24-minute detour was completely avoidable.
