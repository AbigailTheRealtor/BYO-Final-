Approve #895, but keep it extremely narrow.

Before editing, audit only the listed files and confirm the exact selector/ID for each field.

Safeguards:
- UI placeholder fix only
- Do NOT touch Limited Service files
- Do NOT touch controllers, routes, models, migrations, validation, database, or Livewire PHP classes
- Do NOT refactor global Select2 behavior
- Do NOT change wire:model names
- Do NOT change saved-value hydration logic
- Do NOT edit any select fields except these 3:
  1. Hire Buyer — Acceptable Property Type
  2. Hire Landlord — Property Type
  3. Seller Offer Listing — Business Property Style

Fix requirements:

1. Hire Buyer — Acceptable Property Type
- In resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/property-preferences.blade.php
- Confirm the select has leading:
  <option value="">Select</option>
- Add data-placeholder="Select" if missing.
- If this field is initialized with Select2, ensure its init includes:
  placeholder: "Select",
  allowClear: true,
  width: "100%"
- Saved values must still reload correctly.

2. Hire Landlord — Property Type
- In resources/views/livewire/hire-landlord-agent/landlord-agent-auction-tabs/commission-based/property-preferences.blade.php
- Confirm the select has leading:
  <option value="">Select</option>
- Add data-placeholder="Select" if missing.
- If this field is initialized with Select2, ensure its init includes:
  placeholder: "Select",
  allowClear: true,
  width: "100%"
- Saved values must still reload correctly.

3. Seller Offer Listing — Business Property Style
- In resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-preferences.blade.php
- Confirm the actual select ID is id="property_style_select"
- Confirm it has leading:
  <option value="">Select</option>
- Add data-placeholder="Select" if missing.
- In resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php
- Fix the Select2 initializer ID mismatch:
  it must target #property_style_select, not #property_items, unless the actual element ID is intentionally changed.
- Do not break the conditional Business show/hide behavior.
- Saved values must still reload correctly.

Verification:
- Hire Buyer Acceptable Property Type shows “Select” before selection.
- Hire Landlord Property Type shows “Select” before selection.
- Seller Offer Listing → select Business as Property Type → Property Style appears and shows “Select.”
- Existing saved values reload correctly on all 3 fields.
- No duplicate Select2 containers appear after tab switching or Livewire updates.
- No console errors.
- git diff shows only the relevant Blade/JS view files changed.