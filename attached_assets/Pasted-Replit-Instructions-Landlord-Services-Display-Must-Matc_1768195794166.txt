Replit Instructions — Landlord “Services” Display Must Match Tenant Formatting (Residential + Commercial)
Applies To

ONLY: Hire a Landlord’s Agent (Residential + Commercial)
Wherever Services are shown:

Public / actual listing display

Agent bid view (including private “Terms” / modal)

Counter-bid view

Accepted bid summary view (if Services appear there)

Problem

Landlord Services are currently displayed as a flat list (no section headers, no emojis, no bullet formatting).

Tenant Services already display correctly with:

Section Titles

Emojis

Bulleted items

Clean spacing + indentation

Landlord Services must display the exact same way as Tenant Services.

Required Outcome
Landlord Services must render as:

Group by Service Category

Show Category Header formatted exactly like Tenant:

Emoji + Title

Under each header, show Services as bullet points exactly like Tenant:

same bullet style

same font sizes

same spacing/indentation

Same behavior for Residential and Commercial Landlord listings

Implementation Requirement (Display-Only)

DO NOT edit the service list content, wording, categories, or ordering.

DO NOT migrate or rewrite data.

This is strictly a frontend rendering/layout fix.

Use the same component / renderer / template used for Tenant Services and apply it to Landlord Services.

If Tenant uses something like:

renderServicesByCategory(servicesGrouped)
or a reusable component like:

<ServicesSectionedDisplay />

Then Landlord must use the same pattern, pulling from Landlord’s existing Services dataset.

Safeguards (Non-Negotiable)

DO NOT:

change any service titles, text, or bullet wording

add/remove services

reorder services

rename categories

change icons/emojis or section headings

change any Tenant Services code or styling

change database models, columns, or stored JSON

DO:

only change how Landlord Services are displayed

reuse the Tenant Services formatting exactly

Acceptance Criteria (How We Confirm It’s Done)

✅ Landlord Services show:

Emoji + Category Title

Bulleted services under each category

Same styling as Tenant Services

✅ Works in:

Landlord listing display

Agent bid view

Counter-bid view

✅ Residential + Commercial both render correctly

Provide screenshots:

Tenant Services (existing correct formatting)

Landlord Services after fix (must match format)