READ-ONLY AUDIT ONLY — DO NOT CHANGE CODE YET

Issue:
On Create Offer Listing for Seller:
URL: /offer-listing/seller

Several fields are still missing visible placeholder text “Select” even after repeated fixes.

Observed examples:
1. Sale Terms tab:
- Special Sale Provision
- Offered Financing/Currency

2. Documents & Disclosures tab:
- Additional Documents Available

Some fields show icons but no “Select” text. Others show Select correctly, such as Target Closing Timeframe and Occupant Type.

Goal of this audit:
Find the exact reason why “Select” is not appearing on these Seller Create Offer Listing fields.

Audit requirements:
1. Locate the Seller Create Offer Listing Blade/Livewire partials for:
   - Sale Terms tab
   - Documents & Disclosures tab

2. For each affected field, report:
   - File path
   - Line number
   - Field name / wire:model
   - Whether it is a `<select>`, text input, Select2 field, multiselect, custom component, or other custom UI
   - Whether it has an empty placeholder option like:
     `<option value="">Select</option>`
   - Whether it uses `placeholder`, `data-placeholder`, or Select2 initialization
   - Whether CSS/icon padding may be hiding the text
   - Whether `wire:ignore` is preventing Livewire updates
   - Whether JS/Select2 is replacing the original select and losing placeholder text

3. Compare a working Seller field:
   - Target Closing Timeframe
   - Occupant Type

Against the broken fields:
   - Special Sale Provision
   - Offered Financing/Currency
   - Additional Documents Available

4. Identify the exact pattern difference between working and broken fields.

5. Do NOT edit yet.
6. Do NOT run migrations.
7. Do NOT touch Buyer, Tenant, Landlord, Hire Agent, Agent Preset, or profile flows.
8. Do NOT replace large sections.
9. Return only the audit findings and a proposed minimal fix plan.

Important:
If a field is actually a text input or custom multiselect instead of a normal select, explain why adding `<option value="">Select</option>` would not work.