Task: Replace all bid detail pages with ONE shared layout component (no more patching)

The previous fixes improved padding, but the pages still do not fully match visually.

We are no longer fixing individual issues.

We are now enforcing a single shared layout component for all bid detail pages.

New rule (non-negotiable)

All four pages must render using the SAME layout wrapper:

Tenant
Buyer
Seller
Landlord

No exceptions.

No role-specific layout structures.

Step 1 — Identify canonical layout

Use Tenant page as the visual + spacing standard.

Use Landlord page as the structural reference for:

outer card
header bar
footer containment
Step 2 — Create ONE shared layout wrapper

Create a shared Blade layout:

Example:

partials/bid_detail_layout.blade.php

This must contain:

A. Outer card container
shadow
border-radius
overflow hidden
B. Teal header bar
title (Agent Bid Detail / Full Bid Preview)
listing ID
status badge
C. Match score section
consistent wrapper
consistent padding
consistent spacing below header
D. Content container
uses .bid-preview-body
padding: 30px
Tenant spacing rules ONLY
E. Footer
left: back button
right: Accept / Counter / Reject
consistent padding
no centered layouts
Step 3 — Move ALL role pages into this layout

Refactor these files:

tenant_agent/bid_preview.blade.php
hire_buyer_agent/bid_detail.blade.php
hire_seller_agent/bid_detail.blade.php
hire_landlord_agent/view-bid.blade.php

They must:

extend or include the SAME layout wrapper
NOT define their own outer containers
NOT define their own header blocks
NOT define their own footer blocks

Only inject:

@section('bid-content')
Step 4 — Remove duplicate structure

Delete or refactor:

duplicate outer <div class="card"> wrappers
duplicate header sections
duplicate footer layouts
inline spacing hacks
role-specific layout containers
Step 5 — Standardize spacing at the layout level ONLY

Do NOT fix spacing per page anymore.

All spacing must come from:

.bid-preview-body
.section
ul li
h3/h4 spacing

Tenant spacing = source of truth

Step 6 — Visual validation required

Do NOT validate with HTML inspection.

Provide:

screenshots of all 4 pages
side-by-side comparison
confirmation that:
outer card is identical
header is identical
match score position is identical
spacing is identical
footer is identical
Step 7 — Do not stop early

This task is NOT complete if:

pages “look similar”
CSS is shared but layout is still different
only padding was fixed

This is complete ONLY when:

👉 All 4 pages look like the same exact component with different data

Safeguards
do not change business logic
do not change routes
do not change data structure
do not remove working content
do not reintroduce inline styles