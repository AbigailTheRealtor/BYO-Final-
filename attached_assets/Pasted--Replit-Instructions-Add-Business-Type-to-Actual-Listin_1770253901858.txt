
Replit Instructions — Add “Business Type” to Actual Listing (Hire a Buyer’s Agent → Business)
Issue: On the actual listing view for Hire a Buyer’s Agent when Property Type = Business, the Business Type field is missing.
Requirement: Display Business Type under the section “Acceptable Property Type” (same place/format where Business property type details are shown).
________________________________________
SAFEGUARDS (Must Follow)
1.	Do not rename any database tables or columns.
2.	Do not delete or overwrite any existing listing data.
3.	Do not change the Create/Edit form questions, wording, or field order.
4.	Display-layer change only (listing view/template). Only add backend reads if the listing view is not receiving the value.
5.	Do not impact Residential, Income, Commercial, Vacant Land listing views.
6.	Must be backwards compatible with existing saved listings (if Business Type is empty, do not show a blank row).
________________________________________
Step 1 — Locate the Source Field (Read-Only)
Developer must confirm where Business Type is stored for Buyer Agent Business listings:
•	Check Buyer listing record/table used by Hire a Buyer’s Agent (Business type listing).
•	Identify the exact column name (examples could be business_type, acceptable_business_type, business_type_other, etc.)
•	Confirm whether it is:
o	a single value (string)
o	a multi-select (array/json)
o	has an Other free-text companion field
✅ This step is read-only. No schema changes.
________________________________________
Step 2 — Confirm the Listing Page Rendering Path
Where: The file/component that renders the actual listing details for Hire a Buyer’s Agent (Business property type).
Developer must identify whether the listing view is rendered by:
•	a Blade template directly, or
•	a Livewire component that passes a $listing / $auction object to the view.
✅ Do not refactor the page. Only add the missing field output.
________________________________________
Step 3 — Add “Business Type” Under “Acceptable Property Type”
Display Rules
Only show Business Type when:
•	Listing belongs to Hire a Buyer’s Agent
•	Listing’s Property Type = Business
•	Business Type has a value
Placement:
•	Under the existing section header “Acceptable Property Type”
•	Display as a normal label/value row consistent with other fields:
o	Label: Business Type:
o	Value: (stored business type text)
Formatting Requirements
•	If multi-select: display as comma-separated
•	If “Other” was selected and other text exists: display only the custom text (and do not display “Other”)
(same “Other removal” rule you’re using for Garage/Parking Features)
________________________________________
Step 4 — Ensure the Value Is Available to the Listing View
If the Business Type exists in the database but is not showing, it’s usually one of these:
Case A — Listing query isn’t selecting/including the column
•	Fix by ensuring the listing fetch includes the Business Type field (example: add to select list if not using *).
Case B — The listing view uses a “formatted fields array” that doesn’t include Business Type
•	Add Business Type to that array only when property type is Business.
Case C — The field is stored inside a JSON column (like property_preferences)
•	Extract it the same way other property-type-specific fields are extracted.
•	Do not restructure JSON; just read it safely.
✅ Any of the above fixes must be minimal and isolated to this listing page path.
________________________________________
Step 5 — Testing Checklist (Must Provide Proof)
Developer must test on actual listing view:
Scenario 1: Business Listing with Business Type Selected
•	Property Type = Business
•	Business Type = “Restaurant”
✅ Listing shows under “Acceptable Property Type”:
•	Business Type: Restaurant
Scenario 2: Business Type = Other + custom text
•	Business Type = Other
•	Other text = “Car wash with detail bay”
✅ Listing shows:
•	Business Type: Car wash with detail bay
✅ Must NOT show the word “Other”
Scenario 3: Business Type blank
✅ Do not show the “Business Type” row at all (no blank labels)
Regression
•	Residential/Income/Commercial listings unchanged
•	Draft/Submit/Edit flows unaffected
________________________________________
Developer Notes (Do NOT Skip)
•	This is a display omission, not a data-loss issue, unless verified otherwise.
•	If Business Type is truly not being saved, stop and report where the value is being dropped (Create vs Save Draft vs Submit). Do not implement a “fake display” workaround.
Below is a copy/paste-ready Replit instruction set (with strict safeguards) to force the Business Buyer Agent Services to display in the exact same order everywhere it appears: actual listing, agent bids, counter-bids, edits, drafts, summaries, PDFs, and any “services” output.
