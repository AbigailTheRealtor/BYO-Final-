TASK #410 — SELLER OFFER LISTING CRITICAL FIXES ONLY

Scope:
- Seller Offer Listing create flow only
- Full-Service / Commission-Based path only
- Fix critical bugs from Task #409 audit
- Make surgical changes only

DO NOT touch:
- Landlord Offer Listing
- Buyer Offer Listing
- Tenant Offer Listing
- Hire Agent / Agent Preset flows
- Counter/Accept flows
- Database schema
- Unrelated styling/polish items

Critical fixes required:

1. Offered Financing Select2 Placeholder
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/seller-terms.blade.php

Fix:
- Add a blank first option inside select#offered_financing:
  <option value=""></option>
- Do not change existing financing options.

2. Select2 Required Validation on Next
File:
resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php

Problem:
Select2 multi-select fields can bypass the wizard’s required validation.

Fix:
Inside the wizard Next handler, manually validate required Select2 fields when they are visible in the current tab.

Check at minimum:
- #offered_financing
- #sale_provision
- #appliances

Validation logic:
- Use $(selector).val()
- Treat null, empty string, or empty array as invalid
- Show/trigger the same error behavior used for normal required fields
- Block moving to the next tab if invalid
- Do not break normal required field validation

3. Select2 Required Validation on Final Submit
File:
resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php

Fix:
- On final submit / wizard-step-finish click, validate all required fields across all visible/relevant tabs.
- Include the required Select2 checks.
- If invalid:
  - Prevent submit
  - Move user to the first tab containing the invalid field
  - Show the validation error clearly
- Do not bypass the existing Livewire store() server-side validation.

4. Non-Negotiable Amenities “Other” Race Condition
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-preferences.blade.php

Problem:
other_non_negotiable_amenities visibility is controlled by both Blade d-none logic and JS, causing a race/flicker.

Fix:
- Remove Blade d-none visibility logic for the Other input wrapper.
- Use a single inline style based on the PHP array value:
  style="{{ is_array($non_negotiable_amenities) && in_array('Other', $non_negotiable_amenities) ? '' : 'display:none;' }}"
- Keep the existing JS toggle.
- Confirm Livewire message.processed still reapplies the correct state.

5. Retainer Fee Error Placement Verification
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/broker-compensation.blade.php

Verify:
- retainer_fee_amount error span is NOT inside the input wrapper/input-group.
- If already fixed, do not change it.
- If still inside, move it immediately after the wrapper.

6. Carport Label Verification
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-preferences.blade.php

Verify:
- Carport label uses class="fw-bold"
- If already fixed, do not change it.

Important constraints:
- Do not refactor the entire wizard.
- Do not extract shared functions in this task unless absolutely necessary for the validation fix.
- Do not alter Limited-Service behavior.
- Do not change tab order.
- Do not change field names or model properties.

After implementation, run verification:
1. Confirm only approved files changed.
2. Confirm Seller full-service wizard still loads.
3. Confirm Next is blocked when required Select2 fields are empty.
4. Confirm final Submit is blocked when required Select2 fields are empty.
5. Confirm selecting “Other” for Non-Negotiable Amenities shows the custom field and remains visible after Livewire re-render.
6. Confirm Carport label and retainer fee error placement are correct.

Report:
- Files changed
- Exact fixes made
- Any already-fixed items
- Any deferred issues