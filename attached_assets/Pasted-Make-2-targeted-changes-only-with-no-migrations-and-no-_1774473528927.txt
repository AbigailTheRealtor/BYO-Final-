Make 2 targeted changes only, with no migrations and no schema changes.

GOAL 1:
On the live Hire a Tenant public/search listing card UI only, remove the displayed field "Maximum Monthly Lease Price". Do not remove the database field, form field, validation, or any unrelated pricing logic. Remove only the card display block from the actual live Tenant card component/blade.

GOAL 2:
For the live public/search results for all 4 Hire-an-Agent listing types (Seller, Buyer, Landlord, Tenant), update the "Ending Soon" sort logic so it uses one unified urgency deadline:
- if the listing has an active bidding deadline, use that
- otherwise use expiration date
- sort ascending by the earliest valid upcoming deadline
- expired or null deadlines must not float to the top
- tie-break by created_at desc

IMPORTANT BUSINESS RULE:
Mix bidding-period urgency and traditional expiration urgency together into one Ending Soon sort. Example: a traditional listing expiring in 24h must show before a bidding-period listing ending in 48h.

SAFEGUARDS:
- do not rename columns
- do not create or run migrations
- do not change create/edit flows
- do not change bid submission logic
- do not change other sort options
- do not change Seller/Buyer/Landlord cards
- only remove the field from the Tenant card
- only change the live search query path(s) actually used by the public/search pages

IMPLEMENTATION:
First confirm the exact live route/component/blade for the Tenant card and the live query path(s) for all 4 search pages.
Then implement Ending Soon only in the relevant query branch.

Use this logic pattern, mapping to the actual live column names:

if ($sort === 'ending_soon') {
    $query->selectRaw("
        CASE
            WHEN bidding_end IS NOT NULL AND bidding_end > NOW()
                THEN UNIX_TIMESTAMP(bidding_end)
            WHEN expiration_date IS NOT NULL AND expiration_date > NOW()
                THEN UNIX_TIMESTAMP(expiration_date)
            ELSE 9999999999
        END AS ending_soon_rank
    ")
    ->orderBy('ending_soon_rank', 'asc')
    ->orderBy('created_at', 'desc');
}

If expiration_date is stored as date-only and the platform already treats it as end-of-day, safely mirror that same existing platform rule rather than inventing a new one.

After changes, test:
1. Tenant card no longer shows Maximum Monthly Lease Price
2. Ending Soon across mixed listing types/orders works correctly
3. expired listings do not rise to the top
4. all other sorts still work

Report back with:
- exact files changed
- exact live routes/components confirmed
- actual column names used
- whether query logic was shared or role-specific
- proof of test results