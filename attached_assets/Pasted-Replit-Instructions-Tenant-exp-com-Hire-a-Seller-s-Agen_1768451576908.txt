Replit Instructions — Tenant@exp.com
Hire a Seller’s Agent → Tab 2: Property Details
Residential + Income Listing Types — Tooltip Text Edits Only (Pets Section)
Applies To

Tenant@exp.com

Flow: Hire a Seller’s Agent

Tab: 2 — Property Details

Listing Types: Residential and Income only

Change Type: Tooltip text edits only

Where this must apply:

Listing creation form tooltips

Edit listing form tooltips

Any preview/summary screens that reuse these tooltip keys (if applicable)

Required Updates (Replace Tooltip Text With Exactly)
1) Field: Pets Allowed

Replace tooltip text with exactly:

Select whether pets are allowed at the property. If “Yes” is selected, provide details including the number of pets allowed, acceptable pet types, maximum weight per pet, and any applicable pet restrictions.

2) Field: Number of Pets Allowed

Replace tooltip text with exactly:

Enter the maximum number of pets the Seller will allow for this property (e.g., 2).

3) Field: Acceptable Pet Types

Replace tooltip text with exactly:

Enter the types of pets the Seller will allow (e.g., Dog, Cat).

4) Field: Maximum Weight Per Pet (lbs)

Replace tooltip text with exactly:

Enter the maximum allowed weight for each individual pet, in pounds. Leave blank if there is no weight restriction.

5) Field: Pet Restrictions

Replace tooltip text with exactly:

Enter any pet restrictions the Seller requires. Include any HOA or insurance-related restrictions if applicable.

Scope Rules

These tooltip updates apply only to Residential and Income listings within Hire a Seller’s Agent.

Do not apply these changes to Commercial, Business Opportunity, Vacant Land, or any other Seller listing types.

Do not apply these changes to Buyer, Tenant, or Landlord flows.

SAFEGUARDS (MUST FOLLOW)
DO NOT

Modify field logic, conditional behavior, validations, or required/optional status.

Change labels, placeholders, field order, icons, styling, emojis, or section titles.

Rename field keys, component props, Livewire/React state variables, database columns, or API payloads.

Apply these tooltip edits globally—scope them only to Seller → Residential/Income.

Remove or refactor shared tooltip components beyond updating the specified text strings.

MUST

Update only the tooltip text values for the five fields listed.

Preserve capitalization and role usage (Seller) exactly as written.

Ensure the updated tooltips appear in Create, Edit, and Display/Preview contexts.

If tooltips are centrally stored, map updates precisely to the Seller flow + property type.

Verification Checklist (QA)

Create Seller → Residential listing → Tab 2 → hover each Pets-related tooltip → text matches exactly.

Create Seller → Income listing → Tab 2 → same result.

Create Seller → Commercial listing → confirm these tooltips did not change.

Edit an existing Seller Residential/Income listing → tooltips persist and render correctly.