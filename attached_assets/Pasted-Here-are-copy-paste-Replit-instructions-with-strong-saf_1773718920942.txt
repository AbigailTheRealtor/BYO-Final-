Here are **copy-paste Replit instructions with strong safeguards** for these exact **Hire a Landlord Agent** fixes only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A LANDLORD AGENT ONLY — FALSE REQUIRED ERROR ON EDIT + PROPERTY CONDITION EDIT/DRAFT LIST FIX

## SCOPE

Make these fixes **only for Hire a Landlord Agent**:

### 1. False required notification on edit

When user clicks **Edit Listing**, a red required notification appears saying these fields are required even though they are already filled out:

* **Offered Lease Term**
* **Offered Lease Space**

This is a false validation error and must be removed.

### 2. Property Condition list is wrong on edit/draft

When user edits a Landlord listing, the **Property Condition** options/list shown on edit is not matching the current create-list version.

The correct Landlord list must include the updated text:

* **New Construction**
* **Updated / Renovated**
* **Partially Updated**
* **Older but Clean & Well Maintained**

The old wording:

* **Older but Well Maintained**

should no longer be what the user sees when editing or reopening a draft.

### Required behavior

For **Hire a Landlord Agent only**:

* edit mode must use the same current Property Condition list as create mode
* drafts must also reflect the updated label text
* false required notification must not appear for Offered Lease Term / Offered Lease Space when those fields are already filled in

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Landlord Agent flow.**
2. **Do not modify Tenant, Buyer, or Seller flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change Select2 initialization.**
10. **Do not change selection box format, layout, styling, placeholder behavior, width, badge style, or dropdown behavior.**
11. **Do not change field labels, placeholders, icons, tooltips, or field order outside the exact Property Condition visible label text update described here.**
12. **Do not do a broad validation refactor.**
13. **Do not remove the true required nature of Offered Lease Term / Offered Lease Space when they are actually empty.**
14. **Keep this surgical and limited to these exact Landlord issues.**
15. **Make sure the correct active Landlord files are identified first before editing anything.**

---

## IMPORTANT

We have had issues before with:

* edits being made in the wrong file
* edit mode using a different option list than create mode
* stale saved values causing false required-field errors
* changes accidentally affecting other roles

### Hard stop:

Before changing anything, first confirm the **actual active Landlord files** for:

* Landlord create form
* Landlord edit form
* Landlord field for **Offered Lease Term**
* Landlord field for **Offered Lease Space**
* Landlord field for **Property Condition**
* Landlord draft/edit hydration logic
* Landlord required-field JS/banner logic

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap the change so it affects **Landlord only**.

---

# PART 1 — FALSE REQUIRED ERROR ON EDIT FOR OFFERED LEASE TERM + OFFERED LEASE SPACE

## CURRENT BUG

When user clicks **Edit Listing**, a red required notification appears saying:

* Offered Lease Term
* Offered Lease Space

are required, even though they are already filled out.

## REQUIRED BEHAVIOR

For **Landlord only**:

* if **Offered Lease Term** is already filled in, it should **not** be flagged as missing
* if **Offered Lease Space** is already filled in, it should **not** be flagged as missing
* the required notification must only appear if the fields are actually empty

---

## STEP 1A — TRACE WHY THEY ARE FALSELY FLAGGED

Find the exact reason these two Landlord fields are being flagged as missing even though they are already filled out on edit.

Check for issues like:

* saved values are present in Livewire/PHP but not reflected in the DOM at validation time
* Select2/custom multiselect values are not rehydrated before required-field check runs
* validator is reading the wrong property or wrong element
* field is stored as array/JSON but validation is treating it as empty
* required banner logic is running before edit hydration finishes
* stale edit-state mismatch between displayed value and validation source

### Important

Do not guess.
Find the exact reason these two fields are false positives.

---

## STEP 1B — FIX ONLY THE FALSE REQUIRED DETECTION

For **Landlord only**:

* make **Offered Lease Term** count as filled when its saved value is present
* make **Offered Lease Space** count as filled when its saved value is present
* on edit, the red required notification must not list them unless they are truly empty

### Important

Do **not** remove them from required validation entirely.
They should still be required when actually blank.

This fix must only remove the false error, not the real requirement.

---

# PART 2 — PROPERTY CONDITION LIST ON EDIT/DRAFT MUST MATCH CREATE LIST

## CURRENT BUG

When user edits a Landlord listing or reopens a draft, the **Property Condition** list/selected text is wrong.

You previously changed:

* **Older but Well Maintained**

to:

* **Older but Clean & Well Maintained**

But edit mode and/or draft mode is still showing the old wording or wrong list.

## REQUIRED BEHAVIOR

For **Landlord only**:

* create mode and edit mode must use the same current Property Condition list
* draft reopen must use the same current Property Condition list
* the visible text must now be:
  **Older but Clean & Well Maintained**
* user should not see the outdated old wording in edit or draft UI

---

## STEP 2A — TRACE THE LANDLORD PROPERTY CONDITION OPTION SOURCE

Find exactly where the Landlord Property Condition list is defined for:

* create mode
* edit mode
* draft mode

Check whether:

* create mode and edit mode use different arrays/lists
* one side is still using the old label text
* stored value is being mapped to an outdated display label
* draft hydration is still loading the old option text

### Important

Do not guess.
Find the exact source of the mismatch.

---

## STEP 2B — MAKE EDIT/DRAFT USE THE SAME CURRENT LANDLORD LIST AS CREATE

For **Landlord only**, make sure the visible Property Condition options are exactly:

* **New Construction**
* **Updated / Renovated**
* **Partially Updated**
* **Older but Clean & Well Maintained**

### Important

Keep the same field binding and selection format.

If needed for compatibility:

* it is acceptable to preserve the underlying stored value if older data still uses the old label/value
* but the visible text shown to the user in edit/draft should now be the updated wording

### Do not:

* change Tenant, Buyer, or Seller Property Condition lists
* change selection input format
* do a global list refactor

---

## STEP 2C — PRESERVE DRAFT/EDIT PERSISTENCE

After the fix:

* if user saves a draft with the updated Property Condition selection, reopening the draft must show the correct updated label text
* if user edits an existing listing, the correct updated label text must show
* the selected value must remain selected
* values must not disappear

---

# TESTING REQUIRED

## FALSE REQUIRED NOTIFICATION

### Test A

Open existing Landlord listing with **Offered Lease Term** already filled
Expected: no false required notification

### Test B

Open existing Landlord listing with **Offered Lease Space** already filled
Expected: no false required notification

### Test C

Open listing with both filled
Expected: neither appears in the red required notification

### Test D

Clear one field intentionally
Expected: truly empty field is still flagged as required

---

## PROPERTY CONDITION LIST

### Test E

Open Landlord create form
Expected list:

* New Construction
* Updated / Renovated
* Partially Updated
* Older but Clean & Well Maintained

### Test F

Open existing Landlord draft
Expected: same list and updated visible text

### Test G

Open existing Landlord listing in edit mode
Expected: same list and updated visible text

### Test H

Save draft again and reopen
Expected: updated Property Condition label still shows correctly

---

# VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Tenant files changed
* no Buyer files changed
* no Seller files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Landlord validation changed
* no unrelated Landlord fields changed

---

# RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Landlord files were identified first
3. exact root cause for:

   * false required error on Offered Lease Term / Offered Lease Space
   * Property Condition edit/draft list mismatch
4. exact fix applied for each
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof the false required notification is gone when those fields are already filled
8. proof Property Condition now shows the updated Landlord wording in create, draft, and edit

---

## IMPORTANT FINAL RULE

This is a **Landlord-only, surgical fix**.

Do **not**:

* edit unrelated files
* change selection box format
* refactor global validation logic
* refactor global option-list logic
* touch Tenant, Buyer, or Seller
* remove the true required nature of Offered Lease Term / Offered Lease Space when actually blank

The final result should be:

* **Offered Lease Term** no longer falsely shows as required when already filled on edit
* **Offered Lease Space** no longer falsely shows as required when already filled on edit
* **Property Condition** uses the updated Landlord wording in create, draft, and edit
* no other file or selection input behavior is changed

---

If you want, I can also make this into a **shorter hard-stop version** for Replit.
