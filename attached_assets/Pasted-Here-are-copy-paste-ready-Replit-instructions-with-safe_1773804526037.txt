Here are copy-paste-ready Replit instructions with safeguards:

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — AUDIT AND ALIGN REQUIRED FIELDS ACROSS SELLER / BUYER / LANDLORD / TENANT

## GOAL

We need a full audit of **required fields** across all 4 listing types:

* Seller
* Buyer
* Landlord
* Tenant

Right now, I believe the required fields were **not updated correctly** and may not be consistent.

The rule should be:

**Any field showing the same red star used on listing creation must be treated as required.**

We need Replit to go through each listing flow and make sure the required fields are aligned correctly.

---

# REQUIRED STANDARD

Use this as the source-of-truth rule:

## Required field rule

If a field shows the **red star** on listing creation, that field must be treated as required.

That must be true for:

* Seller
* Buyer
* Landlord
* Tenant

And it must be consistent in:

* listing creation
* edit listing
* save draft on edit
* submit / save edit

This is **not** permission to invent new required fields.
It is an instruction to audit and make sure the current required-field logic matches the fields visually marked with the red star.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename, remove, or migrate any database columns.**
2. **Do not rename wire:model names, backend property names, validation keys, or storage keys.**
3. **Do not broad-refactor the entire form system unless absolutely necessary.**
4. **Do not invent new required fields beyond what the UI already marks with the red star.**
5. **Do not remove required status from fields that are intentionally marked required.**
6. **Do not change unrelated flows, dashboards, public pages, or listing display pages.**
7. **Do not weaken create validation.**
8. **Do not weaken edit validation.**
9. **Do not break Save Draft on edit or Submit / Save Edit behavior.**
10. **Do not assume all four roles use identical files. Confirm exact files first.**
11. **If the project uses shared TenantAgentAuction-based create/edit logic, inspect that first before editing multiple role files.**
12. **Do not hardcode required rules in multiple places if one shared source-of-truth can be fixed safely.**
13. **Do not change conditional logic unless needed to match the existing red-star UI behavior.**
14. **Use the smallest safe fix.**
15. **Stop if this would require a destructive rewrite.**

---

# FIRST STEP — DO A FULL REQUIRED-FIELD AUDIT BEFORE CHANGING ANYTHING

Before making changes, inspect all 4 listing flows and document:

## For each role:

* Seller
* Buyer
* Landlord
* Tenant

identify:

1. which fields currently show the **red star**
2. which fields are actually treated as required in validation
3. whether create and edit are using the same required rules
4. whether Save Draft on edit and Submit / Save Edit use the same required rules
5. whether any red-star field is currently **not** enforced
6. whether any field is being enforced as required **without** the red star

Do **not** start patching blindly.

---

# REQUIRED AUDIT OUTPUT

For each role, compare:

## A) UI required marker

Fields showing the red star on listing creation

vs

## B) Actual validation behavior

Fields that are truly enforced as required in:

* create
* edit
* save draft on edit
* submit / save edit

We need those to match.

---

# WHAT TO CHECK

Inspect where the required-field source of truth currently lives. It may be in one or more of these:

* shared Livewire rules
* role-specific Livewire validation methods
* frontend JS validation
* Blade metadata / required markers
* helper methods
* tab-by-tab required field lists
* field configuration arrays

Specifically check whether the red star is driven by:

* a config array
* a Blade condition
* a required field list
* hardcoded markup

Then make sure the actual validation uses the same source-of-truth.

---

# LIKELY ROOT CAUSES TO CHECK

## A) UI red stars were updated, but validation rules were not

Example:

* field shows red star
* validation never enforces it

## B) Validation was updated, but red stars were not

Example:

* field is enforced as required
* UI does not visually show it

## C) Create and edit use different required sources

Example:

* create correctly enforces required fields
* edit uses older or weaker rules

## D) Different roles drifted apart

Example:

* Seller has correct required list
* Buyer/Landlord/Tenant still use old rules

## E) Conditional required fields are inconsistent

Example:

* field should only be required for a certain property type
* red star and validation don’t agree on when the condition applies

---

# REQUIRED IMPLEMENTATION RULE

The required-field source-of-truth must be consistent.

Preferred rule:

* the same logic that determines whether a field shows the **red star**
* should also determine whether that field is treated as required

If the project already has a central config or helper for this, fix that shared source instead of maintaining separate mismatched lists.

If not, align the validation carefully to the current red-star UI.

---

# CONDITIONAL REQUIRED FIELDS

This is very important.

If a field is required only when certain conditions apply, that conditional logic must stay consistent across:

* red star display
* create validation
* edit validation
* save draft on edit
* submit / save edit

Examples may include:

* property type
* listing type
* leasing type
* residential vs commercial
* other dynamic branch conditions

Do **not** flatten these into always-required or never-required rules.

---

# SCOPE

Apply this audit/fix to all 4 listing types:

* Hire a Seller’s Agent
* Hire a Buyer’s Agent
* Hire a Landlord’s Agent
* Hire a Tenant’s Agent

And ensure consistency across:

* listing creation
* edit listing
* Save Draft on edit
* Submit / Save Edit

---

# IMPORTANT UX REQUIREMENT

The red star should mean the same thing everywhere.

If a field shows the red star:

* user should understand it is required
* validation should enforce it

If a field does **not** show the red star:

* it should not suddenly behave like required unless there is a dynamic condition that also updates the UI

---

# DO NOT CHANGE

* field labels
* DB schema
* route names
* controller names
* unrelated public listing displays
* unrelated dashboard views
* unrelated business logic
* optional fields unless they are incorrectly marked/enforced
* create/edit architecture beyond what is needed to align required logic

---

# ACCEPTANCE TESTS

## Test 1 — Seller required-field audit

For Seller, compare every red-star field on listing creation to actual required validation.

Confirm:

* every red-star field is enforced as required
* no non-red-star field is incorrectly enforced as required unless conditionally correct

---

## Test 2 — Buyer required-field audit

Do the same for Buyer.

---

## Test 3 — Landlord required-field audit

Do the same for Landlord.

---

## Test 4 — Tenant required-field audit

Do the same for Tenant.

---

## Test 5 — Edit behavior matches create

For each role:

* remove a field that has a red star
* click **Save Draft** on edit
* confirm save is blocked
* click **Submit / Save Edit**
* confirm save is blocked

---

## Test 6 — Non-required fields stay non-required

For each role:

* leave a truly optional field blank
* confirm save is not blocked unless that field is conditionally required and shows red star accordingly

---

## Test 7 — Conditional required fields

For each role, test at least one conditional required field scenario.

Confirm:

* red star appears only when condition applies
* validation enforces required only when condition applies
* create and edit behave the same

---

## Test 8 — Red star consistency

Visually inspect each form and confirm the red stars now correctly match the actual required behavior.

---

# REPORT BACK AFTER AUDIT / CHANGES

Please report back with:

1. **Exact files inspected**
2. **Exact files changed**
3. **Where the current required-field source-of-truth lives**
4. **Whether red stars and validation were mismatched**
5. **Which roles had missing or incorrect required-field enforcement**
6. **Whether the fix was done through a shared file or separate role files**
7. **Confirmation that create, edit, Save Draft on edit, and Submit / Save Edit now all match the red-star required rules**
8. **Confirmation that DB fields, wire:model names, and storage keys were not renamed**

---

## QUICK SUMMARY

Please audit every Seller / Buyer / Landlord / Tenant listing flow and make sure:

* the **same red star used on listing creation**
* means the field is actually **required**
* across create and edit
* for **Save Draft on edit** and **Submit / Save Edit**

Do not guess. Audit first, then align the required logic to the red-star UI exactly.
