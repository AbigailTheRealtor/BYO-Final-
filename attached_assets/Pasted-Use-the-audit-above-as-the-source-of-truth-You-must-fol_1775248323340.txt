Use the audit above as the source of truth.

You must follow the audit findings exactly and implement fixes surgically.

Before writing any code:

* Confirm you understand ALL root causes listed in the audit
* Confirm which files will be modified
* Confirm no unrelated files will be touched

Hard Safeguards:

* Do NOT rename or delete database columns
* Do NOT change listing creation flows, tooltips, labels, or field options
* Do NOT modify Tenant behavior except to reuse it as the source of truth
* Do NOT introduce new architecture or refactors
* Do NOT hardcode service totals or term totals
* Do NOT create new bid records when editing Buyer or Seller bids
* Only modify the exact files identified in the audit
* STOP if a change would impact other flows outside bid cards

---

IMPLEMENTATION TASKS

Follow in this exact order:

---

1. FIX MODEL JSON DECODING (CRITICAL ROOT BUG)

Fix getGetAttribute() in:

* SellerAgentAuctionBid.php
* BuyerAgentAuctionBid.php
* LandlordAgentAuctionBid.php

Change json_decode(...) to:

json_decode($row->meta_value, true)

This must return associative arrays (not stdClass).

Do NOT change Tenant model.

---

2. FIX BUYER BID EDIT (CRITICAL)

File:
Buyer/BuyerAgentAuctionBid.php

Fix submit() so that:

IF editing:

* find existing bid using $this->editBidId
* update it

ELSE:

* create new bid

Must match logic used in Tenant/Seller/Landlord.

DO NOT allow duplicate bids on edit.

---

3. FIX MATCH SCORE UI PARITY

Buyer, Seller, and Landlord must match Tenant EXACTLY.

Apply to:

* buyerAgentAuctionDetail.blade.php
* hire_seller_agent/view.blade.php
* hire_landlord_agent/view.blade.php

Requirements:

* Replace ALL old “Match Summary” layouts
* Use Tenant Match Score layout exactly
* Same structure:

  * Match Score title
  * Green badge top right
  * Services Match box
  * Terms Match box
* Same wording, spacing, and structure

Buyer currently missing Match Score → ADD IT

---

4. FIX TERMS COUNTING

Rule:
Every filled field under Broker Compensation & Agency Agreement Terms = 1 term

Exclude:

* Additional Details
* presentation materials
* agent bio
* uploads

Fix:

* Landlord must calculate up to 17 terms when applicable
* Seller residential must calculate up to 16 terms when applicable
* Buyer must NOT show 0/0 — dynamically calculate real total (example ~15)

Additional Details must NEVER count toward terms

---

5. ADD MISSING LANDLORD FIELD

Add:
Tenant’s Broker Commission Fee:

Display:
Directly AFTER
Tenant’s Broker Commission Structure:

Include in:

* display
* match comparison logic

---

6. FIX SERVICES COUNTING (CRITICAL LOGIC)

Universal rule:
1 checkmark = 1 service

INCLUDING:

Digital enhancements sub-options:

* Basic edits
* Twilight conversion
* Object removal
* Virtual twilight
* Color correction / sky replacement
* Other

Each = 1 service

Also:

* “Other – Specify additional services” checkbox = 1
* EACH custom additional service entry = 1

Fix all roles:

* Buyer (currently broken 0/0 + false extras)
* Seller (must reach 59 for full residential)
* Landlord (currently miscounting + false extras)

Do NOT:

* treat grouped services as one
* mark original services as extra

---

7. FIX EXTRA SERVICES / TERMS LOGIC

Extras:

* must display separately
* must NOT increase match %

Fix false positives where:

* matched services are being labeled as extra

---

8. FIX BUYER PRIVATE TERMS PANEL

File:
buyerAgentAuctionDetail.blade.php

Replace entire Broker Compensation section layout with Tenant format.

Keep Buyer labels, but match:

* grouping
* spacing
* typography
* structure

---

9. FIX COLOR STYLING

For Buyer + Landlord:

Change to black text:

* Compensation for Creating Lease-Option Agreement
* Compensation if Purchase Option is Exercised

Remove old blue styling

---

10. ADD BUYER ACTION BUTTONS

Buyer must include:

* Accept
* Counter
* Reject

Must match Tenant exactly:

* placement
* colors
* permissions
* behavior

---

11. FIX SELLER PROMO MATERIALS BUG

File:
hire_seller_agent/view.blade.php

Fix promoMaterials loop in private modal:

Convert stdClass → array before access

---

12. FIX BUYER MISSING COUNT DISPLAY

Add:
services_missing_count

Display:
“• X missing” like Tenant

---

13. STANDARDIZE SCORE KEYS

Use canonical keys everywhere:

* services_matched_count
* services_baseline_total

Remove alias usage:

* services_matched
* services_total

---

14. FIX FILE UPLOAD DISPLAY

Ensure for ALL roles:

* business card loads
* uploaded materials load
* edit preserves files
* no broken paths or null data

---

FINAL VERIFICATION REQUIRED

Before finishing, confirm:

* Buyer, Seller, Landlord match Tenant visually and functionally
* Buyer no longer shows 0/0
* Seller and Landlord term totals correct
* Services counts correct across all property types
* No false “extra services”
* Additional Details not counted in terms
* Buyer edit does NOT create new bid
* Business card + materials load correctly
* All buttons present and working

Then provide:

* files changed
* what was fixed for each issue
* confirmation per role (Tenant / Seller / Buyer / Landlord)

Do not skip verification.
