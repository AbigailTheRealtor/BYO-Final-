Offer Listing Presets — Counter Agent Bid Required Client Details + Clean Counter Summary Display

SAFETY / SCOPE RULES:
- Only work on Offer Listing Presets / Offer Listing counter-agent-bid flow.
- Do NOT touch Hire Agent listing flows.
- Do NOT touch Limited Service flows. Limited Service is legacy/frozen.
- Do NOT change database schema unless absolutely required.
- Do NOT change routes unless absolutely required.
- Do NOT alter existing accepted-bid, direct hire, or regular listing creation logic.
- Preserve all existing saved field keys and counter-term persistence.

ISSUE 1 — Counter Agent Bid submission must require client details:
When a user counters an Agent bid from an Offer Listing Preset and submits the counter, require the same client detail/contact fields that are shown in that counter flow.

Required fields:
- Full Name
- Phone Number
- Email Address

Also require property/location details based on role:
- Seller: Property Address fields required
- Landlord: Property Address fields required
- Buyer: Location & Target fields required
- Tenant: Location & Target fields required

Important:
- Use role-specific validation.
- Do not make Seller/Landlord use Buyer/Tenant target-location fields.
- Do not make Buyer/Tenant use Seller/Landlord property-address fields.
- Keep field labels and existing UI text intact unless needed for required indicators.
- Add visible required markers where missing.
- Validation should block Submit Counter until required fields are complete.
- Error messages should clearly identify the missing field.

ISSUE 2 — Clean up Agent’s Counter Terms display:
In the submitted counter summary / Agent’s Counter Terms section, remove this message completely:

“No Match score available - no requirements were provided in the baseline.”

This message should not appear anywhere in the Offer Listing Preset counter summary when no match score exists.

ISSUE 3 — Agent Services display should not use match-style indicators:
Because this counter flow does not have a match score baseline, Agent Services should display as plain service bullets only.

Remove from Agent Services display:
- Green “Added” badges
- Green plus signs
- Any “added,” “changed,” “removed,” or match-comparison styling
- Any match-score comparison language for services

Replace with:
- Standard neutral bullet points for each Agent service
- No green styling
- No + icon
- No status label

Important:
- Only change the display for this Offer Listing Preset counter flow.
- Do not remove match-score comparison styling from flows where a real baseline/match score exists.
- Do not change how services are saved.
- Do not change how counter terms are stored.
- This is a presentation cleanup only, except for the required-field validation above.

ISSUE 4 — Remove “View Listing” button/link:
In this Offer Listing Preset counter flow, remove the “View Listing” button/link because this is not an actual listing flow.

Important:
- Only remove “View Listing” from this Offer Listing Preset counter flow UI.
- Do NOT remove View Listing buttons from real Offer Listings, Hire Agent listings, dashboards, search results, or any other flow.
- Do not break navigation or existing listing pages.

AUDIT FIRST:
Before editing, identify the exact controller, Blade, Livewire component, and partials involved in:
1. Offer Listing Preset counter submission
2. Client Details rendering
3. Agent’s Counter Terms summary rendering
4. Agent Services comparison/badge rendering
5. View Listing button rendering

Then make the smallest targeted changes.

DONE LOOKS LIKE:
- Submitting a Counter Agent Bid from an Offer Listing Preset is blocked if Full Name, Phone, Email, and the role-specific property/location details are missing.
- Seller and Landlord require Property Address details.
- Buyer and Tenant require Location & Target details.
- After submitting a counter, Agent’s Counter Terms no longer shows “No Match score available - no requirements were provided in the baseline.”
- Agent Services display as plain neutral bullet points only.
- No green + signs, no green “Added” badges, and no added/changed/removed labels appear in this counter summary.
- “View Listing” no longer appears in this Offer Listing Preset counter flow.
- Existing Hire Agent flows still work unchanged.
- Limited Service files are untouched.