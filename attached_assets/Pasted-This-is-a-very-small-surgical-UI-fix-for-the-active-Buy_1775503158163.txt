This is a very small, surgical UI fix for the active Buyer bid private view only.

Do not change scoring logic.
Do not change listing creation flows.
Do not change database schema.
Do not refactor unrelated bid card sections.
Do not touch Tenant, Seller, or Landlord unless needed only as visual reference.

Tenant is the gold standard for:

* button colors
* button styling
* section placement for Offered Services

---

## GOALS

Fix ONLY these two issues in the active Buyer Private Compensation & Agreement Terms bid view:

1. Offered Services is still missing from Buyer’s bid card / private compensation area
2. Buyer Accept Bid / Counter Bid / Reject Bid button colors do not match Tenant

---

## HARD SAFEGUARDS

* Do NOT rename or delete database columns
* Do NOT change helper logic unless the section cannot render without an existing view variable
* Do NOT change route structure
* Do NOT change button functionality, only visual styling/classes unless a broken class name is causing the wrong appearance
* Do NOT create a new Buyer layout variation
* Use Tenant’s current active bid private view as the exact source of truth for placement and button colors

---

1. OFFERED SERVICES PLACEMENT FIX

---

Issue:

* Offered Services is still missing from Buyer’s bid card for Private Compensation & Agreement Terms

Requirement:

* Add / restore Offered Services in the active Buyer private bid view
* Placement must be:
  right after Additional Details
* This should match the intended Buyer layout and remain consistent with the current card structure

Important:

* Do not move Additional Details back inside Broker Compensation & Agency Agreement Terms
* Additional Details stays separate
* Offered Services should render after Additional Details
* Use the existing active services data already available in the Buyer bid / score context
* If a missing variable or omitted section is the cause, restore it surgically

---

2. BUYER BUTTON COLOR PARITY

---

Issue:

* Buyer button colors do not match Tenant

Required final colors for Buyer private view buttons:

* Accept Bid = green
* Counter Bid = blue
* Reject Bid = red

Use Tenant’s active button styling/classes as the exact source of truth.

Buyer buttons must match Tenant in:

* button color
* text color
* font weight
* icon color
* hover state if shared class already provides it
* visual appearance overall

Important:

* Keep wording as:

  * Accept Bid
  * Counter Bid
  * Reject Bid
* Do not leave teal/yellow styling on Buyer buttons
* Do not change functionality, routes, or permissions
* This is a style/class parity change only

---

## FILES TO CHECK

Check only:

* active Buyer private bid view
* active Tenant private bid view for exact visual reference

Modify only the active Buyer file unless a shared style class used by Buyer is wrong and Tenant already uses the correct one.

---

## VERIFICATION REQUIRED

After coding, provide:

1. exact files checked
2. exact files changed
3. confirmation that:

* Offered Services now appears on Buyer private bid view
* it is placed right after Additional Details
* Accept Bid is green
* Counter Bid is blue
* Reject Bid is red
* Buyer functionality was not changed

Do not give a broad summary.
This is a surgical Buyer-only fix.
