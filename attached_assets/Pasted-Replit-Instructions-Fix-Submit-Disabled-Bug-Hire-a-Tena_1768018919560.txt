Replit Instructions — Fix “Submit Disabled” Bug (Hire a Tenant’s Agent)
Applies to (ONLY)

Profile: Tenant@exp.com

Flow: Hire a Tenant’s Agent → Create Listing

Issue: Submit button remains disabled even when all required inputs are completed

SAFEGUARDS (MUST FOLLOW — DO NOT BREAK OTHER FLOWS)

DO NOT:

Delete/rename any database tables, columns, models, migrations, enums, or IDs

Change any field names/keys used anywhere (especially Tenant listing keys)

Change tab order, UI layout, styling, icons, tooltip text, placeholders, or required-field red star logic

Modify Buyer/Seller/Landlord flows

Remove existing validation rules

DO:

Fix the root cause by correcting:

form “valid” calculation

required field registration

conditional field validation logic

disabled-state logic for Submit button

Keep changes scoped to Hire a Tenant’s Agent listing creation only

1) Reproduce + Identify the Exact “Disabled” Condition
Task

Locate the component that renders the Submit Listing button for Hire a Tenant’s Agent and identify what controls disabled state, typically something like:

disabled={!isValid}

disabled={!canSubmit}

disabled={isSubmitting || !formComplete}

disabled={!watchRequiredComplete}

Requirement

Log/debug the exact variable(s) keeping it disabled:

isValid

errors

dirtyFields

touched

requiredIncompleteFields[]

missingRequiredCount

Add temporary console logs ONLY in this Tenant flow to identify the blocker:

list of required fields evaluated

list of missing fields detected

current errors object

current isValid or canSubmit boolean

Remove logs after fix (or guard them behind a dev flag).

2) Fix Most Common Root Cause: “Required Fields Not Registered / Not Triggering Validation”
Problem pattern

Some inputs look filled but are not registered in the form library (React Hook Form, Formik, custom reducer), so validation never flips to “valid”.

Required Fix

For each required field in Hire a Tenant’s Agent:

Confirm it is registered with the form system using the correct name key

Confirm the input calls onChange / setValue correctly

Confirm values are actually stored in the form state object

Specific checks

Dropdowns / custom selects must call setValue(fieldName, value, { shouldValidate: true, shouldDirty: true })

Multi-selects must update arrays and trigger validation

Masked/format inputs (phone, currency, % fields) must still write a real value into state

3) Fix Conditional Required Logic Blocking Submit (Hidden Required Fields)
Problem pattern

A conditional field is marked required in the validator, but it’s currently hidden (not applicable), so the form never becomes “valid”.

Required Behavior

Fields should only be required when they are visible/applicable

If the UI hides a section, its required validations must not block submit

Implementation Requirements

For Hire a Tenant’s Agent conditional sections (examples):

Residential vs Commercial toggles

Leasing Space selection

“Other” text fields

Assumable / Financing follow-ups

Pet sections / parking sections / occupancy sections

Do ONE of the following (preferred #1):

Dynamic schema: mark conditional fields required only when their trigger condition is active

Unregister hidden fields: if using React Hook Form, unregister(fieldName) when hidden

Clear hidden required errors: ensure hidden fields don’t keep stale errors in state

Do NOT remove the required rules entirely—only scope them to the right conditions.

4) Fix “Form Validity Mode” (React Hook Form Specific)
Problem pattern

If using React Hook Form and the form is set to validate only on submit, isValid may stay false until submit attempt.

Required Fix

If Submit disabled relies on isValid, ensure form is created with:

mode: "onChange" (or "all")

reValidateMode: "onChange"

And whenever setting values programmatically, ensure:

shouldValidate: true

⚠️ Safeguard: Apply this ONLY inside Hire a Tenant’s Agent form component, not globally.

5) Fix “Whitespace / Default Values / Null vs Empty String” Blocking Validity
Problem pattern

Form completeness checker treats values incorrectly:

" " counts as empty

0 treated as empty

null vs "" mismatch

arrays treated as empty even with one item

Required Fix

Wherever you calculate canSubmit / required completeness:

Trim strings before checking emptiness

Treat valid numeric values correctly (0 only invalid if business rule says so)

Treat dropdown selected objects consistently (store scalar value, not object)

Ensure multi-select uses array length > 0

6) Ensure Submit Button Uses the Same Source of Truth as Required Stars
Problem pattern

UI shows required stars, but submit gating uses a separate “requiredFieldsList” that is missing fields or includes wrong fields.

Required Fix

The submit-enabled logic must match the actual validator rules.

If there is a duplicated required list, replace it with:

form library isValid (preferred) OR

a single shared schema used by both UI + validation

⚠️ Safeguard: Do NOT refactor across the entire app. Fix only in Tenant listing flow.

7) Acceptance Tests (Must Pass)
Test A — Standard required fields

Login as Tenant@exp.com

Go to Hire a Tenant’s Agent → Create Listing

Fill all required fields (red star)

Submit button becomes enabled

Click submit → listing creates successfully

Test B — Conditional field path

Choose a path that reveals conditionals (e.g., select “Other”, select Commercial, etc.)

Fill all required conditionals

Submit enables properly

Switch path so conditionals hide → submit still enables (hidden fields must not block)

Test C — No console errors

No React warnings about uncontrolled inputs

No missing field registration warnings

Output Required from Replit AI / Developer

When you finish, report back:

What variable kept Submit disabled (ex: isValid=false due to hidden required field lease_end_date)

What exact fix was applied (ex: conditional unregister + setValue(...shouldValidate:true) for selects)

Confirmation it works for both Residential + Commercial tenant listing creation