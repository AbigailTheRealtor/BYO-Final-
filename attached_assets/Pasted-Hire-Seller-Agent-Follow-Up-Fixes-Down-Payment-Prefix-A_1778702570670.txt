Hire Seller Agent Follow-Up Fixes — Down Payment Prefix + Assumption Placeholder + Remaining Multiselect Behavior

URL:
https://3ce01d2c-f79e-44c2-b048-2e8dc64316ef-00-tu7aronbf5wk.spock.replit.dev/hire/agent/auction/seller

Before editing:
Do a read-only audit first and confirm the exact files and lines that control:
1. Down Payment to Cover the Gap Between Price and Loan
2. Assumption Fee placeholder
3. Amenities multiselect behavior
4. Property Features multiselect behavior

Apply only the smallest surgical fix needed.

Do NOT modify:
- routes
- migrations
- database schema
- validation rules
- Limited Service files/flows
- Buyer/Landlord/Tenant hire flows
- Seller Offer Listing architecture
- unrelated Select2 fields

What needs fixing:

1. Down Payment to Cover the Gap Between Price and Loan
Currently the field does not show the $ prefix by default.
It should behave like Assumption Fee:
- default selection should be $
- the $ prefix box should show by default before the input
- if % is selected, show the % suffix instead
- do not change saved-value behavior

2. Assumption Fee placeholder
Current placeholder includes "$5000".
Change it to remove the dollar sign.

Use:
Enter assumption fee amount (e.g., 5000)

Do not change the percentage placeholder.

3. Amenities multiselect
The Amenities field was not fully fixed.
It should behave like:
- Appliances Included
- Special Sale Provision
- Offered Financing/Currency

Expected behavior:
- user can select multiple options without the dropdown closing after each selection
- selected values still save correctly
- saved draft values still reload correctly
- no duplicate Select2 containers after tab switching or Livewire updates

4. Property Features multiselect
Same required behavior as Amenities:
- user can select multiple options without the dropdown closing after each selection
- selected values still save correctly
- saved draft values still reload correctly
- no duplicate Select2 containers

Important implementation guidance:
- Do not use a multi-select initializer on single-select fields.
- Only true multiselect fields should use closeOnSelect: false.
- If Amenities or Property Features are being initialized by a field-specific Select2 block that overrides the shared global Select2 settings, add closeOnSelect: false there too.
- If they are initialized before the shared initializer runs, fix the field-specific initialization instead of adding broad unrelated changes.

Likely files to audit:
- resources/views/livewire/hire-seller-agent/seller-agent-auction-tabs/commission-based/seller-terms.blade.php
- resources/views/livewire/hire-seller-agent/seller-agent-auction-tabs/commission-based/property-preferences.blade.php
- resources/views/livewire/hire-seller-agent/hire-seller-agent.blade.php
- public/js/select2-stable.js

QA required:
1. Load /hire/agent/auction/seller.
2. Confirm Down Payment to Cover the Gap shows $ by default, matching Assumption Fee.
3. Confirm changing Down Payment to % switches to percent behavior.
4. Confirm Assumption Fee amount placeholder says:
   Enter assumption fee amount (e.g., 5000)
5. Confirm Amenities stays open while selecting multiple values.
6. Confirm Property Features stays open while selecting multiple values.
7. Confirm Appliances Included, Special Sale Provision, and Offered Financing/Currency still work.
8. Confirm no duplicate Select2 containers appear after tab switching.
9. Confirm no Buyer/Landlord/Tenant hire forms changed.