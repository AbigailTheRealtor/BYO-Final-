## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A BUYER’S AGENT ONLY

### SCOPE

Fix **only** the remaining **Hire a Buyer’s Agent** issues listed below.

Keep this **surgical** and limited to:

* Buyer flow only
* the exact broken fields below
* multi-select stability
* missing **Other** input logic
* missing **Offered Financing/Currency** dependent questions
* missing **Property Styles** population after Property Type selection

Do **not** modify Seller, Landlord, or Tenant in this pass.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename or delete database columns.**
2. **Do not modify Hire a Seller’s Agent, Hire a Landlord’s Agent, or Hire a Tenant’s Agent.**
3. **Do not change labels, tooltips, or option text unless absolutely required to restore intended Buyer behavior.**
4. **Do not broad-refactor unrelated shared components unless absolutely necessary for these Buyer-only fixes.**
5. **Do not remove or alter saved drafts, listings, or bids.**
6. **Do not mark this complete based only on code inspection.**
7. **You must test the actual Buyer fields in the browser.**
8. **If a shared helper must be touched, do it surgically and confirm no regressions to other flows.**

---

# HIRE A BUYER’S AGENT — ISSUES TO FIX

## 1) Acceptable Property Styles says “No Results Found”

### Current issue

After selecting **Property Type**, **Acceptable Property Styles** says **No Results Found**.

### Required fix

* once **Property Type** is selected, the correct matching **Acceptable Property Styles** options must populate
* the field must not show **No Results Found** when valid options should exist
* if option sets are dynamic, refresh them correctly without stale Select2 state
* property type changes must correctly update the option list

### What to check

* Property Type → Property Styles mapping source
* dynamic option loading logic
* `wire:key` if options change based on Property Type
* Select2 re-init after option changes
* stale DOM reuse
* correct Livewire property binding

---

## 2) Multi-select fields still delete selections

These Buyer fields still delete earlier selections when selecting additional options:

* **Acceptable Property Conditions**
* **View Preference Needed**
* **Non-Negotiable Amenities and Property Features**
* **Required Property or Business Assets**
* **Acceptable Unit Type**

### Current bug behavior

Example:

* select 2 options
* both appear selected
* select a 3rd option
* earlier selections get deleted or collapse

### Required fix

For each field:

* selecting additional options must keep all prior selections
* no flashing or wiping of prior values
* values must remain stable through tab switches, draft save, edit, and reopen

### Required root-cause checks

For each field, verify:

1. outer wrapper `wire:ignore`
2. inner wrapper `wire:ignore`
3. proper `wire:key` if options are dynamic
4. Select2 init logic
5. namespaced change handler
6. full `.val()` array is being sent, not just the last selected option
7. guarded reinit only
8. no duplicate event handlers
9. no duplicate DOM elements / duplicate includes
10. Livewire state is not overwriting browser state during rerender

---

## 3) Non-Negotiable Amenities and Property Features flashes

### Current issue

When selecting an option in **Non-Negotiable Amenities and Property Features**, the field flashes.

### Required fix

* eliminate flashing/flicker when selecting options
* no destroy/recreate loop on each change
* no duplicate Select2 reinit on `message.processed`
* keep the field visually stable while selecting multiple options

### What to check

* repeated Select2 initialization
* re-render loops
* duplicate handlers
* bad `wire:ignore` structure
* unnecessary DOM replacement by Livewire

---

## 4) Acceptable Special Sale Provisions & Offered Financing/Contingency — Other box does not show

### Current issue

The options properly select, but if **Other** is selected, the custom input box that should appear does **not** show.

### Required fix

When **Other** is selected:

* custom input box must appear immediately
* must remain visible after rerender if Other remains selected
* must save and reload correctly in draft/edit

### What to check

* condition correctly checks for `"Other"` in a multi-select array
* correct companion “other” property
* dependent input block is under the correct conditional
* rerender is not immediately hiding it

---

## 5) Offered Financing/Currency dependent questions do not show

### Current issue

The follow-up questions associated with **Offered Financing/Currency** do not appear depending on the option selected.

### Required fix

Restore all intended conditional follow-up questions for **Offered Financing/Currency**.

That means:

* when a financing/currency type is selected, the correct dependent questions must appear
* hidden sections must remain hidden unless their trigger option is selected
* dependent fields must persist on edit/reload if the triggering option remains selected

### What to check

* conditional Blade logic
* correct Livewire property being watched
* array membership checks for selected options
* whether dependent sections are inside the correct wrapper/conditional
* whether rerender clears them
* whether stale hidden sections interfere

---

# REQUIRED IMPLEMENTATION APPROACH

## A. Fix in this order

Please fix in this order:

1. **Acceptable Property Styles → No Results Found**
2. **Multi-select deletion fields**
3. **Non-Negotiable Amenities flashing**
4. **Other box logic**
5. **Offered Financing/Currency dependent questions**

This order matters because the foundational field stability should be fixed before conditional display logic.

---

## B. Use working reference patterns

For each broken field, compare to a known-good equivalent already working elsewhere in the project.

Do not invent a new structure if the project already has a working pattern for:

* stable multi-selects
* Other-selected custom inputs
* financing-dependent follow-up questions

---

# REQUIRED TESTING — BROWSER ONLY, NOT THEORY

For **each** broken Buyer multi-select field:

1. select option 1
2. select option 2
3. confirm both remain
4. select option 3
5. confirm all 3 remain
6. select option 4 if available
7. confirm all remain
8. switch tabs and return
9. confirm all remain
10. save draft or save listing
11. reopen edit
12. confirm all remain

If adding a new option deletes earlier ones, the field is still broken.

---

# REQUIRED TESTING — ACCEPTABLE PROPERTY STYLES

1. select **Property Type**
2. confirm **Acceptable Property Styles** populates with the correct options
3. confirm the field does **not** show **No Results Found** when valid options should exist
4. save and reopen
5. confirm the correct styles still load

---

# REQUIRED TESTING — SPECIAL SALE / FINANCING OTHER BOX

1. select one normal option
2. select **Other**
3. confirm custom input appears immediately
4. enter custom value
5. save
6. reopen edit
7. confirm **Other** is still selected
8. confirm custom input is still visible with saved value

---

# REQUIRED TESTING — OFFERED FINANCING/CURRENCY FOLLOW-UP QUESTIONS

1. select financing/currency options that should trigger dependent questions
2. confirm the correct follow-up questions appear immediately
3. fill out those dependent fields
4. save
5. reopen edit
6. confirm the dependent questions still appear if trigger options remain selected
7. confirm saved data is still present

---

# SUCCESS CRITERIA

This task is only complete when all of the following are true in **Hire a Buyer’s Agent**:

* **Acceptable Property Styles** populates correctly after selecting Property Type
* **Acceptable Property Conditions** no longer deletes selections
* **View Preference Needed** no longer deletes selections
* **Non-Negotiable Amenities and Property Features** no longer deletes selections or flashes
* **Required Property or Business Assets** no longer deletes selections
* **Acceptable Unit Type** no longer deletes selections
* **Acceptable Special Sale Provisions & Offered Financing/Contingency → Other** correctly shows custom input
* **Offered Financing/Currency** dependent questions display correctly
* create / draft / edit / save / reopen all remain stable

---

# FINAL RULE

Do **not** respond that this is fixed based on code inspection or one partial test.

This must be verified in the browser for the exact Buyer fields listed above using the real behavior:

* select multiple options without deletion
* select **Other** and confirm custom input appears
* select financing/currency options and confirm dependent questions appear
* confirm draft / edit / save / reopen all remain stable
