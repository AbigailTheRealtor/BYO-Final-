This audit is complete and very helpful. Now proceed to PHASE 2 — IMPLEMENT FIXES.

Do NOT expand scope.
Do NOT introduce new features.
Do NOT modify Match Score logic.
Focus ONLY on fixing the confirmed blockers from the audit.

---

## PRIORITY ORDER (STRICT)

Fix these in this exact order:

1. Seller Accepted Bid Summary (CRITICAL)
2. Landlord Agent Counter Prefill (CRITICAL)
3. Buyer + Landlord Signature Rendering (CRITICAL)

Do not work on anything else until these are complete.

---

## FIX 1 — SELLER ACCEPTED BID SUMMARY

Problem:
Seller uses Tenant AcceptedBidSummaryService → TypeError → no summary created.

Required Fix:

* Create SellerAcceptedBidSummaryService

* Model it AFTER:

  * BuyerAcceptedBidSummaryService
  * LandlordAcceptedBidSummaryService

* It must:

  * accept SellerAgentAuctionBid
  * generate correct HTML summary
  * include:

    * Broker Compensation & Agency Agreement Terms
    * Offered Services
    * Additional Terms
    * Additional Details
    * Agent + Seller signature sections

* Update:
  SellerAgentAuctionController::acceptSABid()

Replace Tenant service with SellerAcceptedBidSummaryService

Ensure:

* summary is created successfully
* summary_id is returned
* summary is saved to accepted_bid_summaries table
* view/sign/download links appear on Seller bid page

---

## FIX 2 — LANDLORD AGENT COUNTER PREFILL

Problem:
LandlordAgentAuctionBidCounter::mount() has NO prefill logic.
Agent sees blank form instead of landlord’s latest counter.

Required Fix:

In LandlordAgentAuctionBidCounter::mount():

* Query:
  latest LandlordCounterTerm for this bid (by bid_id)

IF exists:
source = latest LandlordCounterTerm
ELSE:
source = original agent bid

Then:

* call hydrateFromMetaMap(source)

Must populate:

* Broker Compensation & Agency Agreement Terms
* Additional Terms
* Additional Details
* Offered Services

Rules:

* must use SAME logic as Tenant/Buyer
* must always use latest opposite-party submission
* must NOT use listing baseline here

---

## FIX 3 — SIGNATURE RENDERING (BUYER + LANDLORD)

Problem:
Signatures are saved in DB but not displayed.
UI still shows “—” or “—”

Root cause:
HTML templates do NOT use consistent placeholders.

Required Fix:

Standardize ALL summary HTML to use:

{{tenant_signature_name}}
{{agent_signature_name}}

Do NOT hardcode:

* —
* —
* pre-replaced values

Then update rendering so:

AcceptedBidSummaryService::getRenderedHtml()

replaces:
{{tenant_signature_name}}
{{agent_signature_name}}

for ALL roles:

* Tenant
* Buyer
* Seller
* Landlord

Important:

* Do NOT create role-specific rendering logic if not needed
* Use ONE consistent placeholder system

---

## DO NOT CHANGE

* Do NOT modify Match Score logic
* Do NOT change service catalogs
* Do NOT change broker compensation field structure
* Do NOT change routes unless broken
* Do NOT refactor unrelated files
* Do NOT introduce new database columns

---

## POST-FIX VERIFICATION (REQUIRED)

After all 3 fixes:

Test EACH role:

Tenant:

* bid → counter → counter-back → accept → sign → summary visible

Buyer:

* bid → counter → counter-back → accept → sign → signature shows correctly

Seller:

* bid → accept → summary created → sign → summary visible

Landlord:

* bid → counter → agent counter-back prefilled correctly → accept → sign → signature shows

---

## OUTPUT REQUIRED

Return:

1. exact files changed for each fix
2. confirmation each bug is resolved:

   * Seller summary now generates
   * Landlord counter no longer blank
   * Buyer + Landlord signatures now display
3. before vs after behavior (short explanation per fix)
4. confirmation no regression in Tenant flow

Do not expand scope beyond these 3 fixes.

Once complete, we will proceed to Match Score audit separately.
