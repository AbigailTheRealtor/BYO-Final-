Task — Improve Direct Hire Preview Page UX

GOAL:
Improve the existing Direct Hire preview page so clients clearly understand they are reviewing this Agent’s preset terms before creating a client-owned Hire Agent request.

IMPORTANT SAFEGUARDS:
- Do NOT modify HireAgentDirectController@confirm logic.
- Do NOT modify AgentBidMapperService.
- Do NOT modify listing/bid creation logic.
- Do NOT modify accepted bid summary logic.
- Do NOT change existing bid/counter/accept behavior after the request is created.
- This task is UX/display only for the Direct Hire preview page.

CURRENT PAGE:
resources/views/hire-agent-direct/preview.blade.php

CURRENT ISSUE:
The page currently feels like a basic hire form. It should feel like:
“Review this Agent’s proposed services and terms, then start a Direct Hire request.”

REQUIREMENTS:

1. Update page heading to something like:
“Review This Agent’s Proposed Terms”

Subheading:
“These services and terms are based on the Agent’s saved preset. You can submit the request as-is or request changes after the Direct Hire request is created.”

2. Display Agent summary at top:
- Agent name
- Brokerage
- License number if available
- Role
- Property type

3. Display selected services clearly:
Section title:
“Services Included in This Agent’s Proposal”

Only show services from the Agent’s preset.
Do NOT show the full service catalog.

4. Keep service whitelist behavior:
Client can only submit services from the Agent preset.
Do not allow services outside the preset except through an “Additional Services Requested” textarea.

5. Add/keep field:
“Additional Services Requested”
Placeholder:
“List any additional services you would like this Agent to consider.”

6. Add a Broker Compensation & Agency Terms preview section if mapped data is available from the preset.

Display only clean, client-facing fields.
Do not show raw database field names.
Do not show empty values.

Suggested section title:
“Proposed Broker Compensation & Agreement Terms”

Important:
This is a preview only. Do not change mapping logic.

7. Add notice:
“Submitting this request does not finalize an agreement. The Agent will receive your request, and both parties may accept, counter, or reject terms before anything is finalized.”

8. Button text:
Change “Send Hire Request” to:
“Start Direct Hire Request”

9. Owner behavior:
If the logged-in user is the Agent owner:
- Do not show submit button
- Show message:
“You are previewing your own Direct Hire page. Clients will use this page to start a hire request.”

10. Logged-out behavior:
Do not change auth middleware in this phase.
If login is required before this page, leave it as-is.

11. Do not create a new route.
Use existing hire.agent.public / hire.agent.direct.preview flow.

TESTING:
After implementation, verify:

1. Agent owner cannot submit self-hire.
2. Client sees Agent summary.
3. Client sees preset services only.
4. Client sees Additional Services Requested field.
5. Compensation/terms preview only shows non-empty client-facing values.
6. Submit still creates the same existing listing + auto-bid through unchanged confirm logic.
7. After submission, client still lands on existing listing detail page.
8. No accepted summary or bid logic changed.

AFTER COMPLETION, REPORT:
- Files changed
- Exact display sections added
- Whether compensation preview was added and which fields show
- Confirmation no backend creation logic changed
- Test results