Use this exact Tenant bid as the reproduction case for the current issues:

`/tenant/hire/agent/auction/view/140`

This is the exact bid/listing I tested and the one you should use for tracing, debugging, and verification.

For this specific Tenant bid, I need you to trace and fix all four of these issues in the active Tenant bid/detail/match-score flow:

1. **Additional Services are undercounted in Services Match**

   * The denominator is showing `29/29`, but it is not including the checked `Other – Specify additional services as needed` option and is not counting each filled additional service input individually.
   * `Example 1` must count as 1
   * `Example 2` must count as 1

2. **Agent View Bid is showing the wrong Offered Services**

   * The services shown on the bid detail page do not match the services actually checked/saved in the bid form.

3. **Terms Match is still incorrect**

   * It is showing a mismatch even though Broker Compensation & Agency Agreement Terms were not actually changed from the Tenant’s original request.
   * I need the exact logical term list being scored for this bid and the exact term being marked changed.

4. **Tenant Broker Commission Structure is still using old language**

   * The bid detail page must show the exact current wording/value selected on the form, not an older alias or legacy wording.

### Scope

Tenant bid/detail/match-score flow only.

### Hard safeguards

* No destructive DB changes
* No schema changes
* No renaming fields
* No changes to form labels or service wording
* Do not modify Buyer, Seller, or Landlord unless a clearly shared helper is the direct root cause and can be updated safely without side effects
* Preserve the existing Tenant role/property-type filtering protections
* Preserve the working contamination and counter-save fixes unless a directly proven Tenant bug requires a safe adjustment

### Required diagnosis for this exact bid

For `/tenant/hire/agent/auction/view/140`, trace and report:

#### Services

* original Tenant requested service set used as baseline
* bid form checked services at save time
* saved services payload in DB/meta
* services used by Agent View Bid
* services used by Services Match denominator
* services used by matched / missing / extra bucket generation

#### Additional Services

* whether the `Other – Specify additional services as needed` checkbox is saved
* how `Example 1` and `Example 2` are stored
* whether each is included in:

  * bid detail display
  * services denominator
  * matched bucket
  * missing bucket
  * extra bucket

#### Terms Match

Output the full logical term comparison list for this exact bid, including:

* logical term key
* baseline normalized value
* current normalized value
* match/mismatch result
* why it was counted

Do not only dump raw payload arrays. I need the exact logical terms being scored for this bid.

### Required fixes

Then fix the logic so that for this active Tenant flow:

* Additional Services are counted correctly in Services Match
* each filled additional service input counts individually
* the bid detail page uses the exact canonical saved service set
* Services Match and bid display use the same canonical service set
* Terms Match compares only the correct logical Broker Compensation & Agency Agreement Terms
* the bid detail page uses the exact current wording/value from the form for Tenant Broker Commission Structure and related broker compensation display

### Required verification

After the fix, verify on this same bid `/tenant/hire/agent/auction/view/140` that:

* unchanged Additional Services count as matched
* `Example 1` and `Example 2` are counted individually
* Agent View Bid shows the exact saved checked services
* Terms Match only counts the correct logical terms
* unchanged Broker Compensation & Agency Agreement Terms produce full match with zero changed from baseline
* Tenant Broker Commission Structure shows the exact current wording selected on the form

### Deliverable

Give me:

* exact files changed
* exact root causes found for this bid
* exact fixes applied
* the final before/after verification results for `/tenant/hire/agent/auction/view/140`
