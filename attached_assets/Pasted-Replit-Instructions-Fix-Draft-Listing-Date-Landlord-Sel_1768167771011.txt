Replit Instructions — Fix Draft Listing Date (Landlord + Seller) + Fix Multi-Click Submit + Fix Submit Redirect (WITH SAFEGUARDS)
Scope (DO NOT expand)

Applies to these forms only:

Hire a Landlord’s Agent form

Hire a Seller’s Agent form
And the shared Submit behavior used by all 4 hire types (Tenant/Buyer/Landlord/Seller) if they share the same button/form component.

SAFEGUARDS (MUST FOLLOW — NON-NEGOTIABLE)
DO NOT:

delete/rename any database tables, columns, models, or migrations

rename routes, controller methods, Livewire components, or blade files

change UI text, tooltips, placeholders, icons, tab names, or tab order

refactor the listing architecture or merge Buyer/Seller/Landlord into Tenant

change working Tenant or Buyer form fields (except shared submit behavior if the button is shared)

modify listing status values (Active/Pending/etc.) naming

ONLY ALLOWED:

make Listing Date input editable in Landlord + Seller draft/edit screens

ensure Listing Date value is persisted when draft is saved

fix submit event wiring so one click triggers one request

prevent double-submit by disabling the button while processing

implement a Livewire-compatible redirect after successful Submit

remove any leftover JS that overrides Livewire redirects

PART A — Fix Listing Date Editable in Draft (Landlord + Seller)
Problem

Tenant/Buyer draft allows editing Listing Date, but Landlord/Seller does not.

Required Behavior

For BOTH Landlord + Seller forms:

Listing Date auto-fills on first create (keep existing)

When editing a Draft, Listing Date must:

display the saved value

be editable via date picker

persist the updated date on Save Draft

Implementation Steps

Locate Landlord draft/edit component/view:

Livewire component typically: Landlord...Edit or similar

Locate Seller draft/edit component/view:

Livewire component typically: Seller...Edit or similar

REQUIRED UI change (no text changes)

Ensure Listing Date input is not disabled, not readonly, and is a proper date input.

REQUIRED backend change (if needed)

Confirm Save Draft method accepts listing_date from the request/component state.

Ensure the save method does NOT overwrite listing_date after user edits it.

Only set listing_date if empty/null.

If user provided a value, persist it.

✅ Do NOT change Listing Date logic for Tenant/Buyer if already working.

PART B — Fix “Submit Delay / Must Click Multiple Times” + Multiple Success Notifications
Problem

Submit requires multiple clicks; then success notification fires multiple times.
This is classic double submit / mixed submit mechanisms (form submit + wire:click) OR the button isn’t disabled while Livewire runs.

Required Behavior

One click = one submission

Submit button is disabled while processing

Only one success notification

No duplicate submissions

Implementation Steps (apply to ALL hire forms if shared button exists)
B1) Use ONLY one submit mechanism (must be consistent)

Pick ONE pattern and standardize:

✅ Option A (recommended):

<form wire:submit.prevent="submit">

Submit button: type="submit"

NO wire:click="submit" on the button

OR

✅ Option B:

Submit button: type="button" + wire:click.prevent="submit"

No native form submit triggering submit separately

Do NOT combine Option A + Option B, or the method may fire twice or not fire reliably.

B2) Add button lock to prevent multi-click queue

On Submit button (no UI text changes):

wire:loading.attr="disabled"

wire:target="submit"

If Save Draft also suffers, apply same locking to draft button:

wire:loading.attr="disabled"

wire:target="saveDraft"

B3) Remove any JS onclick navigation (global search)

Search project for and remove from submit buttons/components:

onclick=

setTimeout(

window.location

location.href

document.location

If found in shared partials/components, remove there.

✅ Goal: No JavaScript redirects should run after clicking Submit.

PART C — Fix Submit Redirect (Must go directly to listing detail page)
Problem

Even when submission succeeds, it does not navigate to the detail page.

Required Behavior

After successful Submit for each listing type:

redirect to the correct listing detail page:

Tenant → Tenant detail

Buyer → Buyer detail

Landlord → Landlord detail

Seller → Seller detail

Implementation Steps

In each submit method (Tenant/Buyer/Landlord/Seller), after save succeeds:

REQUIRED: Use Livewire-compatible redirect return

Use the most compatible pattern:

return redirect()->to(route('ROUTE_NAME', $listing->id));


Do NOT only dispatch a toast and stop. Redirect must be returned.

Ensure correct route per listing type

Tenant submit uses tenant detail route

Buyer submit uses buyer detail route

Landlord submit uses landlord detail route

Seller submit uses seller detail route

PART D — Verification Checklist (Must Pass)
Landlord Draft Listing Date

Create Landlord listing → Save Draft

Re-open draft → edit Listing Date → Save Draft
✅ Listing Date persists and reflects edited value

Seller Draft Listing Date

Repeat same steps for Seller
✅ Listing Date persists and reflects edited value

Submit (All 4)

For Tenant/Buyer/Landlord/Seller:

Click Submit once
✅ Only one success toast
✅ Button disables while processing
✅ Redirects immediately to detail page (shows listing ID)

Click submit rapidly multiple times
✅ Only one submission occurs

Required Developer Proof (so we don’t get “it’s fixed” again)

When you mark this task complete, paste back:

The final HTML for the Submit button + surrounding <form> tag for one hire type (Tenant or Landlord) showing the final wiring

The exact redirect line used in the submit method

Confirmation that global search returned 0 matches for window.location and onclick="setTimeout" in listing forms