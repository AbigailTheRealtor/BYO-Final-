Fix the **Accepted Bid Summary formatting** for the active Tenant flow so Broker Compensation & Agency Agreement Terms display in the same resolved, user-facing format as the working Tenant bid detail / counter detail views.

## Current problem

The Accepted Bid Summary is still showing raw or partially legacy field formatting instead of the final resolved values.

Example from the current summary:

* `Purchase Fee Type` appears as its own row
* `Purchase Fee` displays incorrectly as `Percentage of the Total Purchase Price + Flat Fee%`
* instead of showing the actual resolved accepted value, such as:

  * `$3,333 + 4% of Total Purchase Price`

This means the summary is still using an outdated display structure for compensation fields.

---

## Goal

Make the **Accepted Bid Summary** use the **exact same resolved display logic** already used in the working Tenant bid detail / counter detail pages.

The summary should show:

* the final accepted values
* in the correct current wording
* with combo fields fully resolved
* without redundant raw “type” rows unless intentionally part of the final display format

---

## Scope

Tenant Accepted Bid Summary only for this step.

Do not modify Buyer, Seller, or Landlord unless a clearly shared formatting helper is the direct root cause and can be safely updated without side effects.

---

## Hard safeguards

* No destructive DB changes
* No schema changes
* No dropping/renaming columns
* Do not change accepted data itself
* Do not change negotiation terms or saved values
* Do not break the now-working Tenant bid detail / counter detail formatting
* Preserve the accepted-summary routing and notification fixes already implemented

---

## Required diagnosis first

For the exact Tenant Accepted Bid Summary shown in the screenshot, trace the full display chain for each compensation field.

I need to know:

1. what accepted data keys are being read
2. which rows are generated from raw field names
3. which rows are being resolved/formatted correctly
4. which rows are still using legacy/partial formatting
5. where the summary diverges from the working Tenant bid detail formatter

Do not just say “it’s legacy formatting.” Identify the exact field-to-row mapping causing the bad output.

---

## Required fix

### Part 1 — Use resolved display values, not raw type rows

For fields like Purchase Fee, Lease Fee, Lease-Option compensation, etc.:

* do not show separate raw “Type” rows unless that is intentionally part of the final UI format
* instead show the final accepted resolved value in the same style used in the working bid detail views

### Example

Instead of:

* `Purchase Fee Type: Percentage of the Total Purchase Price + Flat Fee`
* `Purchase Fee: Percentage of the Total Purchase Price + Flat Fee%`

show:

* `Purchase Fee: $3,333 + 4% of Total Purchase Price`

using the actual accepted amounts and structure order.

---

### Part 2 — Match the working Tenant detail format exactly

The Accepted Bid Summary should use the same final formatting rules already working in:

* Tenant bid detail view
* Tenant counter detail view

For example:

* current commission structure wording
* resolved lease fee values
* resolved purchase fee values
* lease-option values
* retainer display
* termination fee display
* timeframe display
* brokerage relationship display
* Additional Terms display

Do not maintain a second inconsistent formatting system for the summary.

---

### Part 3 — Remove redundant/awkward rows

If the final detail view does not need rows like:

* `Purchase Fee Type`

then remove them from the Accepted Bid Summary so the summary is cleaner and matches the rest of the flow.

The summary should reflect the final accepted deal in a user-friendly way, not expose raw implementation fields.

---

### Part 4 — Preserve exact accepted data

Do not alter the accepted values themselves.

Only fix how they are displayed so the Accepted Bid Summary reflects the exact accepted deal in readable form.

---

## Verification required

After the fix, verify the Accepted Bid Summary shows:

* **Tenant’s Broker Commission Structure** in the current wording
* **Tenant’s Broker Commission Fee** in correct resolved format
* **Purchase Fee** in correct resolved format
* **Compensation for Creating the Lease-Option Agreement** in correct resolved format
* **Compensation if Purchase Option is Exercised** in correct resolved format
* **Early Termination Fee Amount** correctly
* **Retainer Fee Application** correctly
* **Tenant Agency Agreement Timeframe** correctly
* **Acceptable Brokerage Relationship** correctly
* **Additional Terms** correctly

And confirm:

* no malformed rows like `Flat Fee%`
* no redundant raw “type” rows unless intentionally required
* the Accepted Bid Summary now visually matches the working Tenant detail formatting style

---

## Deliverable

Give me:

* exact files changed
* exact root cause of the bad summary formatting
* which raw rows were removed or consolidated
* before/after verification showing the Accepted Bid Summary now matches the correct Tenant detail formatting
