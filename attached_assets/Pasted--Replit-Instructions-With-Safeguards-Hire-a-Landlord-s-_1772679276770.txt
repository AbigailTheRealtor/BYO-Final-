## Replit Instructions With Safeguards

### Hire a Landlord’s Agent — Fix Leasing Space display order + missing fields for **Residential + Commercial** (Single Room + Entire Property)

**Do NOT rename or delete any database columns.**
**Do NOT delete or modify any existing listings, drafts, or agent bids.**
**Do NOT change labels, tooltips, or option values.**
**Only modify the actual listing display logic for Hire a Landlord’s Agent listings.**

---

# Scope

Apply to **Hire a Landlord’s Agent** listing display for:

* **Residential** listings when `leasing_space` is selected
* **Commercial** listings when `leasing_space` is selected

**Goal:** Display must match the **listing creation order** exactly, and must not omit completed fields.

---

# Critical Implementation Rule (prevents wrong order + missing fields)

In the Hire a Landlord’s Agent listing display **row builder**, remove any logic that:

* merges leasing-space row sets, OR
* prints “all non-empty leasing-space fields,” OR
* loops generically through leasing-space fields and outputs in arbitrary order

Replace with:

* **strict gating based on `leasing_space`**
* **fixed ordered key lists**
* **deduping** using an associative array keyed by stable field keys so each field renders once

---

# Step 1 — Implement associative-row dedupe (required)

Build leasing-space rows into:

* `$rows = [];`

Add each row once:

* `if (!isset($rows[$key]) && filled($value)) { $rows[$key] = ['label'=>..., 'value'=>...]; }`

Then render using a fixed `$order` array (below).

**Safeguard:** Ensure **Restrictions Include** is pulled from the same saved field used by the form and renders only once.

---

# Step 2 — Strict gating based on leasing_space (Residential + Commercial)

## If `leasing_space = Single Room` (Residential + Commercial)

Display **ONLY** these fields in this exact order (must match listing creation order):

1. Leasing Space
2. Guests are
3. Restrictions Include
4. Shared Areas Available
5. Maintenance and Repairs Are Handled By
6. Maintenance Response Time
7. Utilities
8. Common Area Maintenance
9. Included Storage Space
10. Storage Space Size
11. Bathroom Facilities
12. Approximate Room Size

**Required missing-field fix:** Ensure these keys are actually included and pulled from the correct saved fields:

* Restrictions Include (was missing)
* Shared Areas Available (was missing)
* Common Area Maintenance (was missing)

**Do NOT display these fields for Single Room:**

* Shared Amenities Include
* Building Hours
* 24/7 Access Available
* Zoning Allows
* Space Features
* Neighboring Tenants Include

---

## If `leasing_space = Entire Property` (Residential + Commercial)

Display **ONLY** these fields in this exact order:

1. Leasing Space
2. Restrictions Include
3. Maintenance and Repairs Are Handled By
4. Maintenance Response Time
5. Included Storage Space
6. Storage Space Size
7. Shared Amenities Include
8. Building Hours
9. 24/7 Access Available
10. Zoning Allows
11. Space Features
12. Neighboring Tenants Include

**Do NOT display these fields for Entire Property:**

* Guests are
* Shared Areas Available
* Utilities
* Common Area Maintenance
* Bathroom Facilities
* Approximate Room Size

---

# Step 3 — Use stable field keys + map to the exact saved fields used by the form

For each label above, map to the existing stored value used by the form (do not rename anything). Use stable internal keys like:

* `leasing_space`
* `guests_are`
* `restrictions_include`
* `shared_areas_available`
* `maintenance_repairs_handled_by`
* `maintenance_response_time`
* `utilities`
* `common_area_maintenance`
* `included_storage_space`
* `storage_space_size`
* `bathroom_facilities`
* `approx_room_size`
* `shared_amenities_include`
* `building_hours`
* `access_24_7`
* `zoning_allows`
* `space_features`
* `neighboring_tenants_include`

**Safeguard:** If your stored property names differ, only adjust the mapping layer. Do not modify DB columns or option values.

---

# Step 4 — Render in fixed order only

Create `$order` based on leasing_space:

* If Single Room: use the Single Room order list
* If Entire Property: use the Entire Property order list

Then render:

* foreach `$order` as `$k`: if isset($rows[$k]) render it

**Safeguard:** Do not sort rows. Do not append “remaining non-empty rows.” Do not merge row sets.

---

# QA Test (Must Pass)

Residential test:

1. Create Residential listing with Leasing Space = Single Room
2. Fill all fields including Shared Areas Available
3. Listing display shows all fields in exact Single Room order, none missing

Commercial test:

1. Create Commercial listing with Leasing Space = Single Room
2. Fill all fields including Restrictions Include + Shared Areas Available + Common Area Maintenance
3. Listing display shows all fields in exact Single Room order, none missing

Entire Property test (both):

1. Create listing with Leasing Space = Entire Property
2. Listing display shows exact Entire Property list and does not include Single Room-only fields

Deduping test:

* Restrictions Include renders once only (no duplicates)

---

## Copy/Paste Prompt for Replit AI

“Fix Hire a Landlord’s Agent listing display for Residential + Commercial when leasing_space is selected. Do not rename/delete DB columns, do not modify any listings/drafts/bids, do not change labels/tooltips/option values. In the landlord listing display row builder, remove any logic that merges leasing-space row sets or prints all non-empty leasing-space fields. Add strict gating based on leasing_space and implement deduping using an associative array keyed by stable field keys so each field renders only once.
If leasing_space = Single Room (Residential + Commercial), display ONLY in this exact order: Leasing Space, Guests are, Restrictions Include, Shared Areas Available, Maintenance and Repairs Are Handled By, Maintenance Response Time, Utilities, Common Area Maintenance, Included Storage Space, Storage Space Size, Bathroom Facilities, Approximate Room Size. Ensure Restrictions Include, Shared Areas Available, and Common Area Maintenance pull from the same saved fields used by the form and are not missing. Do not display Shared Amenities Include, Building Hours, 24/7 Access Available, Zoning Allows, Space Features, or Neighboring Tenants Include for Single Room.
If leasing_space = Entire Property (Residential + Commercial), display ONLY in this exact order: Leasing Space, Restrictions Include, Maintenance and Repairs Are Handled By, Maintenance Response Time, Included Storage Space, Storage Space Size, Shared Amenities Include, Building Hours, 24/7 Access Available, Zoning Allows, Space Features, Neighboring Tenants Include. Do not display Guests are, Shared Areas Available, Utilities, Common Area Maintenance, Bathroom Facilities, or Approximate Room Size for Entire Property. Apply only to Hire a Landlord’s Agent listing display.”

If you want this to land perfectly on the first try, tell Replit to **search for the landlord “leasing space” row builder function** and paste it back to you before editing, so it modifies the exact correct file/section.
