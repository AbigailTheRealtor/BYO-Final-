Perfect — this is the **real issue** now. You’re not just fixing layout… you’re enforcing **1:1 duplication of listing → bid form**.

Here is your **clean, copy-paste Replit instruction with strict safeguards**:

---

# 🔧 Seller Agent Bid — MUST Mirror Listing EXACTLY (No Rewrites)

## 🚨 Core Problem

The **Agent Bid form (Seller)** is currently:

* Using incorrect / rewritten services
* Using incorrect Broker Compensation & Agency Agreement Terms
* Not matching property type (Residential vs Commercial etc.)
* Using outdated or mixed structures
* “Next / Previous” buttons are visually disabled (whited out)

## 🎯 Goal

The **Agent Bid form must be an EXACT DUPLICATE of the Seller Listing form** for:

### 1. Offered Services

### 2. Broker Compensation & Agency Agreement Terms

NOT similar.
NOT restructured.
NOT rewritten.

👉 **It must use the SAME source, SAME structure, SAME fields, SAME options, SAME grouping.**

---

# 🔒 HARD SAFEGUARDS (DO NOT BREAK THESE)

1. **DO NOT recreate services manually**
2. **DO NOT recreate compensation fields manually**
3. **DO NOT hardcode Residential / Commercial / etc**
4. **DO NOT create a “new version” of these sections**
5. **DO NOT modify listing creation files**
6. **DO NOT rename database fields**
7. **DO NOT touch Tenant / Buyer / Landlord flows**
8. **DO NOT change routes**
9. **ONLY reuse existing listing form partials/components**
10. **STOP if you are not using the same exact source as listing creation**

---

# 🧠 REQUIRED ARCHITECTURE (THIS IS THE FIX)

## ✅ The Bid Form must REUSE the Listing Form Components

Instead of having separate logic, the Seller Agent Bid must:

### 👉 INCLUDE the SAME partials used in:

```
Hire a Seller → Create Listing
```

---

## 🔍 STEP 1 — FIND SOURCE OF TRUTH (MANDATORY FIRST STEP)

Locate the files used for Seller listing creation:

### You are looking for:

* Services section partial
* Broker Compensation section partial

Example (names will vary):

```
resources/views/livewire/seller/partials/services.blade.php
resources/views/livewire/seller/partials/broker-compensation.blade.php
```

OR inside:

```
TenantAgentAuction.php (shared component)
```

---

## 🚨 IMPORTANT

👉 Whatever file renders:

* Services
* Broker Compensation

in **CREATE LISTING**

👉 THAT EXACT FILE must be used in:

**AGENT BID**

---

# 🔁 STEP 2 — REMOVE WRONG BID IMPLEMENTATION

In Seller Agent Bid:

❌ Remove:

* Any manually written services list
* Any duplicated compensation fields
* Any “simplified” version
* Any old/legacy Seller bid structure

---

# 🔁 STEP 3 — INCLUDE LISTING PARTIALS DIRECTLY

Inside Seller Bid form:

### Replace incorrect sections with:

```
@include('PATH_TO_SELLER_SERVICES_PARTIAL')
@include('PATH_TO_SELLER_BROKER_COMP_PARTIAL')
```

OR if using Livewire components:

```
<livewire:seller.services :mode="'bid'" />
<livewire:seller.broker-compensation :mode="'bid'" />
```

---

# 🧠 STEP 4 — PASS MODE = BID (CRITICAL)

You must pass a flag:

```
mode = 'bid'
```

### Why:

* So the same component can behave differently if needed (readonly vs editable, etc.)
* But structure remains identical

---

# 🔁 STEP 5 — PREFILL FROM LISTING (NOT EMPTY)

The bid form must:

### Pull values from:

```
$listing->services
$listing->broker_terms
```

NOT empty defaults.

---

### Required behavior:

* All selected services = pre-selected
* Correct property type loads correct services:

  * Residential → Residential services
  * Commercial → Commercial services
  * Income → Income services
  * Business → Business services
  * Vacant Land → Land services

---

# ⚠️ CRITICAL RULE

👉 DO NOT SWITCH PROPERTY TYPE LOGIC

The listing already knows its type.

Use:

```
$listing->property_type
```

NOT:

* manual conditionals
* hardcoded arrays
* duplicated configs

---

# 🔁 STEP 6 — ENSURE FULL 1:1 MATCH

The bid form must match listing form in:

* field order
* labels
* checkboxes
* grouping
* “Other” inputs
* conditional fields
* tooltips
* formatting

If listing shows it → bid must show it
If listing hides it → bid must hide it

---

# 🔘 STEP 7 — FIX NEXT / PREVIOUS BUTTONS (WHITED OUT)

## Problem

Buttons are disabled or styled as disabled.

## Fix

### 1. Check for:

```
disabled
opacity-50
pointer-events-none
```

### 2. Remove incorrect conditions like:

```
:disabled="true"
wire:loading.attr="disabled"
```

### 3. Ensure validation is not falsely failing

Check:

* required fields not prefilled
* Livewire state not hydrated

---

## REQUIRED BEHAVIOR

Buttons must:

* always be clickable
* only block on actual validation
* not appear greyed out unless truly disabled

---

# 🧪 QA TESTS (MUST PASS)

## 🔹 Property Type Tests

Test ALL:

* Residential
* Income
* Commercial
* Business
* Vacant Land

### Verify:

* Correct services load
* Correct compensation fields load
* No cross-type leakage

---

## 🔹 Prefill Tests

1. Create Seller listing
2. Select services + comp terms
3. Open Agent Bid

✅ Must match EXACTLY

---

## 🔹 Persistence Tests

* Save draft
* Reload
* Submit bid
* Edit bid

Nothing should:

* disappear
* reset
* change format

---

## 🔹 UI Tests

* No old card layout
* No simplified fields
* Matches listing visually
* Buttons clickable (not white/disabled)

---

## 🔹 Regression Tests

Confirm unchanged:

* Tenant bid
* Buyer bid
* Landlord bid
* Seller listing creation
* Seller listing edit

---

# 📋 REQUIRED REPORT BACK

1. Seller bid route
2. Files changed
3. Exact listing partials reused
4. How prefill is mapped
5. Confirmation property-type switching works
6. Confirmation buttons fixed
7. Before vs after difference (root cause)

---

# 🚨 FINAL RULE

If the bid form is not using the **same exact file as the listing form**,
👉 IT IS WRONG.

---

If you want next step, I can:
👉 Map your exact file names based on your Replit structure
👉 Or give you a “diff-style patch” so dev can’t mess it up again
