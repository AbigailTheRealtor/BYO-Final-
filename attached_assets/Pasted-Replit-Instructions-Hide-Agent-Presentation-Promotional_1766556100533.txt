Replit Instructions — Hide “Agent Presentation & Promotional Materials” for Agents in “View Full Services & Broker Compensation Terms”

Applies to: Hire a Tenant’s Agent (Residential + Commercial)
Where: Bid details view opened by “View Full Services & Broker Compensation Terms”
Goal: If the viewer is an Agent, do NOT show the Agent Presentation & Promotional Materials section. Listing creator can still see it.

🔒 SAFEGUARDS (MANDATORY)

Do NOT change labels, headings, section order, tooltips, or styling for listing creators.

Do NOT change any save logic, DB schema, or what data is stored.

Only add a visibility condition to hide one section for Agent viewers.

Must apply to Tenant Agent bids for both Residential + Commercial.

1) Locate the view/partial that renders the expanded bid details

Find the template used when clicking:
“View Full Services & Broker Compensation Terms”

Likely in:

resources/views/hire_tenant_agent/view.blade.php (modal/accordion section)

or an included partial like:

resources/views/hire_tenant_agent/partials/_bid_terms.blade.php

resources/views/.../_private_terms.blade.php

Search for section heading text:

Agent Presentation & Promotional Materials

or Marketing Materials

or variables like promoMaterials, marketingMaterials, agentPresentation

2) Add role-based visibility logic (Agent vs Listing Creator)
✅ Required behavior

Listing Creator: Can see everything (including Agent Presentation & Promotional Materials)

Agents: Must NOT see Agent Presentation & Promotional Materials inside this “View Full…” panel

Implementation (safe + minimal)

Wrap ONLY that section with:

@php
  // "Viewer is the listing creator" check — use your existing logic if present.
  // Replace these checks with the actual variables your view already has (do not invent new system roles).
  $viewerId = auth()->id();

  // Example: listing creator id is usually $auction->user_id or $listing->user_id
  $listingCreatorId = data_get($auction, 'user_id') ?? data_get($listing, 'user_id');

  $isListingCreator = $viewerId && $listingCreatorId && ((int)$viewerId === (int)$listingCreatorId);
@endphp

@if ($isListingCreator)
  {{-- Agent Presentation & Promotional Materials section (UNCHANGED content) --}}
  ...existing section markup here...
@endif


✅ This avoids guessing “agent role” and uses the most reliable rule:
Only the listing creator sees this private section.

3) Do NOT hide Offered Services or Broker Compensation

Only hide:

Agent Presentation / Promotional Materials

Marketing materials uploads/links

Business card / virtual presentation links (if those are part of that same section)

Everything else remains visible per your rule:

Agents can still see Full Offered Services

Agents can still see Full Broker Compensation & Agency Agreement Terms

4) Acceptance tests (must pass)
As Listing Creator:

Open “View Full Services & Broker Compensation Terms”
✅ You STILL see “Agent Presentation & Promotional Materials”

As an Agent viewing the listing:

Open “View Full Services & Broker Compensation Terms”
✅ “Agent Presentation & Promotional Materials” is NOT shown
✅ “Offered Services” and “Broker Compensation & Agency Agreement Terms” still show normally

Note to Replit

If there is already a “Private — visible only to listing creator” flag in the UI, reuse that same condition and apply it to the entire Agent Presentation & Promotional Materials block. Do not add new permissions systems.