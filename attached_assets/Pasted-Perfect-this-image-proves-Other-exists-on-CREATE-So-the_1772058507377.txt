Perfect — **this image proves “Other” exists on CREATE** ✅
So the bug is *not* “missing option.” It’s one of these two things:

1. **The “Other text” input is not being saved** (so display has nothing to show), **or**
2. It *is* being saved, but the **listing display is reading the wrong key / wrong casing** (e.g., saved value is `"Other"` but your helper expects `"other"`), so it never swaps/prints the text.

Below are **bulletproof Replit instructions with safeguards** to fix it end-to-end **without guessing**.

---

## ✅ REPLIT INSTRUCTIONS (WITH SAFEGUARDS)

### Scope

**Hire a Tenant’s Agent**
Fix: **“Other” text for Offered Lease Term + Non-Negotiable Amenities not showing on actual listing display**

### Safeguards

* Do **not** rename DB columns.
* Do **not** change existing option lists.
* Use **add-only** changes if any new helper is added.
* Fix must work for **Create → Draft → Edit → Submit → Actual Listing Display**.

---

# 1) Confirm what is actually saved (this is the fastest way to stop guessing)

In the **actual listing display component/controller** (the one that renders the public listing), temporarily dump/log these exact values for ONE listing that has Other selected + typed text:

* `offered_lease_term`
* `offered_lease_term_other` (or whatever the other-text key is)
* `non_negotiable_amenities_features` (array)
* `non_negotiable_amenities_features_other` (other text)

**Goal:** confirm whether other-text is empty **or** display is reading wrong keys.

✅ If other-text is **empty** → this is a SAVE problem (go to section 2)
✅ If other-text has the text → this is a DISPLAY mapping/casing problem (go to section 3)

---

# 2) SAVE-SIDE FIX (most common): ensure “Other text” is persisted in drafts + edits

### Issue pattern

Create screen shows the input, but **Draft/Edit doesn’t persist it**, or it’s never included in the final save payload.

### Fix requirement

In the Tenant component that handles saving (shared save path like `saveAllMetadata()`):

## A) Force the “Other” token + persist text

### Offered Lease Term (single select or tag list)

Right before saving:

* If `offeredLeaseTermOtherText` is not empty:

  * force the selected term to include “Other”
  * save the other text

### Non-Negotiable Amenities (multi-select)

Right before saving:

* If `nonNegotiableOtherText` is not empty:

  * ensure the selections array includes **Other**
  * save the other text

## B) Normalize the “Other” token *exactly as stored by your UI*

Your UI shows the chip as **`Other` (capital O)**. Many Select2/tag inputs store the label value exactly (`"Other"`) not `"other"`.

So your normalization MUST treat all these as “other”:

* `"Other"`, `"other"`, `"OTHER"`, `" Other "`

…and then store ONE canonical token (pick one and stick to it):
✅ recommended canonical token for your system: **`Other`** (because that’s what the UI chip shows and likely what is saved already)

---

# 3) DISPLAY-SIDE FIX: make “Other” substitution case-insensitive and key-correct

Even if saving is correct, display commonly fails because:

* helper checks `"other"` but saved value is `"Other"`
* display is pulling `*_other` from the wrong meta key
* display helper only substitutes when the token exists

### Requirements

For BOTH fields:

### A) Offered Lease Term display

If selected term contains/equals Other (case-insensitive):

* display the other text (not the word “Other”)

Also: if other text exists but “Other” is missing (legacy bad save):

* still display the other text

### B) Non-Negotiable Amenities display

When rendering tags:

* if selection contains “Other” (case-insensitive) and other text exists:

  * replace the “Other” chip with the text (or append a chip for the text)
* if other text exists but “Other” not in selection:

  * append the other text chip anyway

---

# 4) The actual fix you should implement (drop-in logic)

Wherever your `normalizeList()` / `normalizeListDeduped()` runs, update it to treat **Other token case-insensitively**:

```php
// Pseudocode requirements (implement in ListingDisplayHelper)
$values = cast_to_array($values);              // handles null, json string, csv, array
$clean  = array_map(fn($v) => trim((string)$v), $values);

$hasOther = collect($clean)->contains(fn($v) => strtolower($v) === 'other');

if ($hasOther && filled($otherText)) {
  // remove any "Other" variants
  $clean = array_values(array_filter($clean, fn($v) => strtolower($v) !== 'other'));
  // add the actual other text
  $clean[] = trim($otherText);
}

// Legacy fallback: otherText exists but no "Other" token was stored
if (!$hasOther && filled($otherText)) {
  $clean[] = trim($otherText);
}

return dedupe($clean);
```

**Important safeguard:** do this transformation only for display; don’t mutate stored values.

---

# 5) One more thing that breaks this (and matches your screenshots)

Your “Other” text inputs appear to be **separate meta keys**:

* `Example: Offered Lease Term Other`
* `Example: Non-Negotiable Amenities and Property Features`

If your display template is calling the wrong keys (even by 1 character), it will never show.

✅ Action: In the display blade, verify you are passing the correct other-text key into the helper.

Example pattern (you may have different names):

```php
$leaseTerms = meta('offered_lease_term');
$leaseOther = meta('offered_lease_term_other'); // MUST match exact save key
```

Same for non-negotiable:

```php
$nnList  = meta('non_negotiable_amenities_features');
$nnOther = meta('non_negotiable_amenities_features_other');
```

---

## ✅ What I need from you to make this 100% exact (no placeholders)

Paste the **exact meta keys** (or Livewire properties) for these 4 fields:

1. Offered Lease Term key =
2. Offered Lease Term Other Text key =
3. Non-Negotiable list key =
4. Non-Negotiable Other Text key =

Once you paste those, I’ll rewrite the **exact** Replit instructions naming the exact component methods and the exact lines to change (save + display) so it works immediately.
