✅ Instruction Set #1 (BLOCKER): Draft + Edit Must Rehydrate Financing Follow-Ups
Replit Instructions — Hire a Buyer Agent (ALL property types)
Fix: Offered Financing/Currency follow-up inputs do NOT pre-fill in Draft reopen or Edit listing
⚠️ CRITICAL SAFEGUARDS

Apply ONLY to Hire a Buyer Agent flow

Applies to ALL property types: Residential, Income, Commercial, Business, Vacant Land

Do NOT mass-update DB rows

Do NOT change existing meta keys

Do NOT reset/clear follow-up values on load/hydration

Must work for: Save Draft → reopen draft → Edit listing

A) Define the single source of truth

All values must come from:

buyer_agent_auction_metas via $auction->get-><meta_key>

Draft/Edit must load these into the Livewire properties used by the inputs.

B) Confirm the FOLLOW-UP inputs bind to the SAME property names in Create and Edit

In:
resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/purchasing-terms.blade.php

For each follow-up field, confirm the input uses a property name like:

wire:model="seller_amortization_type"

wire:model="exchange_transfer_method"

wire:model="crypto_custodian_wallet"

wire:model="nft_gas_fees"

etc.

⚠️ If Create uses one property name but Edit uses another, Draft/Edit will appear blank.

C) Load Draft: ensure ALL fields are assigned AFTER the auction is loaded

In:
app/Http/Livewire/HireBuyerAgent/BuyerAgentAuction.php

Inside the method that loads a draft (ex: loadDraft()), ensure it assigns every financing follow-up field like:

$this->seller_amortization_type = $auction->get->seller_amortization_type ?? '';
$this->seller_payment_frequency = $auction->get->seller_payment_frequency ?? '';
$this->seller_late_fee_amount   = $auction->get->seller_late_fee_amount ?? '';

$this->exchange_transfer_method = $auction->get->exchange_transfer_method ?? '';
$this->exchange_liens           = $auction->get->exchange_liens ?? '';
$this->exchange_inspection_rights = $auction->get->exchange_inspection_rights ?? '';

$this->crypto_exchange_method   = $auction->get->crypto_exchange_method ?? '';
$this->crypto_custodian_wallet  = $auction->get->crypto_custodian_wallet ?? '';
$this->crypto_transaction_fees  = $auction->get->crypto_transaction_fees ?? '';
$this->crypto_timing_of_transfer = $auction->get->crypto_timing_of_transfer ?? '';

$this->nft_valuation_method     = $auction->get->nft_valuation_method ?? '';
$this->nft_transfer_method      = $auction->get->nft_transfer_method ?? '';
$this->nft_gas_fees             = $auction->get->nft_gas_fees ?? '';


✅ This must happen before the view renders.

D) Load Edit: ensure the Edit component assigns the same fields

In:
app/Http/Livewire/HireBuyerAgent/BuyerAgentAuctionEdit.php

Inside the Edit hydration method (ex: loadAuctionData()), add the same assignments.

E) Prevent the wipe: fix updatedOfferedFinancing() so it does NOT clear on hydration

This is likely still wiping your follow-ups when Select2 re-syncs offered_financing during load.

Required behavior:

On initial load, do not clear anything.

Only clear fields when the user actually removes a financing type.

Only clear the fields belonging to that removed type.

✅ Add/confirm:

$isLoadingData flag (or equivalent)

$previousOfferedFinancing snapshot

Compare removed types only

Absolutely forbid: “blanket reset all follow-up fields on any change event.”

F) Select2 hydration must not trigger a destructive update

If Select2 triggers Livewire updates during draft/edit hydration, it can fire updatedOfferedFinancing.

Required safeguard:

Either prevent sync handler during hydration, OR

Ensure updatedOfferedFinancing ignores changes while $isLoadingData = true.

✅ Acceptance Test (Draft/Edit)

For each property type:

Create listing → select Seller Financing + Exchange + Crypto + NFT

Fill follow-ups

Save Draft

Reopen draft
✅ Inputs must be pre-filled

Click Edit listing
✅ Inputs must be pre-filled

If this fails, do not proceed to listing display work.

✅ Instruction Set #2: Public Listing Must Print Lease Option/Purchase Follow-Up Fields
Replit Instructions — Hire a Buyer Agent
Fix: Lease Option / Lease Purchase follow-up fields do NOT show on public listing output
Fields missing (based on your listing output)

Under Lease Option / Lease Purchase, these do not appear:

Maintenance / Repair Responsibility

Extension Terms

Rent Credit Toward Purchase Price (Yes/No)

Rent Credit Dollar Amount

Non-Refundable Deposit / Purchase Deposit

A) Confirm they are stored in Meta (save step)

In both components (BuyerAgentAuction.php and BuyerAgentAuctionEdit.php) inside saveAllMetadata():
Add saveMeta() calls for the lease follow-up fields using the exact Livewire property names.

Example meta keys (adjust to match your real properties):

$auction->saveMeta('lease_maintenance_repair_responsibility', $this->lease_maintenance_repair_responsibility);
$auction->saveMeta('lease_extension_terms', $this->lease_extension_terms);
$auction->saveMeta('lease_rent_credit_enabled', $this->lease_rent_credit_enabled);
$auction->saveMeta('lease_rent_credit_amount', $this->stripCommas($this->lease_rent_credit_amount));
$auction->saveMeta('lease_non_refundable_deposit_amount', $this->stripCommas($this->lease_non_refundable_deposit_amount));

B) Confirm they load in Draft/Edit

Add corresponding load lines in draft/edit hydration methods.

C) Display them in the public listing view

In:
resources/views/buyerAgentAuctionDetail.blade.php

Under the Lease Option and Lease Purchase sections, add show-if-filled blocks:

@if(@$auction->get->lease_maintenance_repair_responsibility)
  <div>Maintenance / Repair Responsibility: <span>{{ @$auction->get->lease_maintenance_repair_responsibility }}</span></div>
@endif


Repeat for the other fields.
For rent credit, show the amount only if enabled.

✅ Acceptance Test (Listing)

Create a new listing, fill lease follow-ups, publish/view listing:
✅ Those lease fields must appear

The key point (why you’re seeing “listing has data but edit is blank”)

That happens when:

the public listing view reads from meta keys that are saved, but

the edit/draft hydration loads into the wrong Livewire property names or gets wiped by updatedOfferedFinancing

So you’re not crazy — both can be true at once.