Paste this one into Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — STRICT MODE

### FINAL SUBMIT REQUIRED FIELD ENFORCEMENT ONLY

### Scope: Hire a Tenant, Buyer, Landlord, and Seller listings

This is a **strict, targeted fix only**.

We need to enforce required fields on the **final Submit button only** for these four flows:

* Hire a Tenant’s Agent
* Hire a Buyer’s Agent
* Hire a Landlord’s Agent
* Hire a Seller’s Agent

### Goal

On the **last tab**, if the user clicks **Submit** and any required fields are still missing, the listing must **not submit**.

Instead, the system must:

1. Block submission
2. Show the **same required-field notice behavior already used by the Next button**
3. Tell the user what required fields are missing
4. Return the user to the **first tab containing the missing required field(s)** if the current architecture already supports that pattern

If all required fields are complete, Submit should continue exactly as it does now.

---

# HARD SAFEGUARDS — DO NOT VIOLATE

## Do not touch anything outside this exact request.

1. **Do not refactor shared architecture**
2. **Do not rewrite or “clean up” any shared form systems**
3. **Do not change Select2 initialization, hydration, rehydration, syncing, display, persistence, or event hooks**
4. **Do not change multi-select behavior**
5. **Do not change any field rendering logic**
6. **Do not change any conditional show/hide logic**
7. **Do not change any tab navigation behavior except the minimum needed to send the user to the first invalid tab after failed final submit**
8. **Do not change existing Next button validation**
9. **Do not change existing create, edit, draft, autosave, update, or preview behavior except to block final submit when required fields are missing**
10. **Do not change labels, tooltips, placeholders, icons, option values, wording, or section layout**
11. **Do not rename, remove, or migrate database columns**
12. **Do not change business logic, compensation logic, brokerage logic, or listing display logic**
13. **Do not touch any recently fixed issues involving disappearing selections, Select2 value loss, tab flicker, duplicated fields, or submit button placement**
14. **Do not modify JS unless absolutely necessary**
15. **Prefer Livewire/PHP validation changes only**
16. **Make the smallest possible additive fix**
17. **If solving this requires touching Select2, shared rendering, or tab systems in a risky way, stop and report before changing anything**

---

# FIRST STEP — VERIFY EXACT FILES AND METHODS

Before editing, identify the exact files and methods that control **final submit** for each flow:

* Tenant
* Buyer
* Landlord
* Seller

For each one, identify:

* active Livewire component
* final submit method name
* validation method(s)
* blade containing the final submit button
* any shared validation helper or trait actually used

Do **not** assume all four flows use the same files or methods. Verify first.

Do **not** edit unrelated files.

---

# IMPLEMENTATION RULE

## This is a final-submit validation enforcement task only.

Do **not** rewrite the whole form validation system.

The **Next** button already represents the correct behavior pattern.
Reuse that same system for **final Submit**.

That means:

* reuse existing rules where possible
* reuse existing validation messaging where possible
* reuse existing “first invalid tab” behavior where possible

Do **not** create a separate competing validation framework.

---

# REQUIRED FINAL SUBMIT BEHAVIOR

Before any final persistence/submission logic runs:

1. Build/check the full set of **currently required** fields based on the user’s actual selections
2. Run server-side validation using the existing validation system
3. If validation fails:

   * do not submit
   * do not finalize
   * do not bypass with partial save
   * show the same required-field notice pattern used by Next
   * route user to the first tab with missing required fields if supported by current architecture
4. If validation passes:

   * continue current submit flow unchanged

---

# IMPORTANT VALIDATION RULE

Validate only fields that are **actually required right now**.

## Do not validate:

* hidden fields
* inactive branches
* fields from unselected property types
* inactive compensation branches
* untriggered follow-up fields

## Do validate:

* visible required fields
* conditionally required fields that were triggered
* active follow-up fields
* required “Other” text boxes when “Other” is selected

---

# PREFERRED FIX LOCATION

Use the **Livewire/PHP final submit methods** whenever possible.

That is the preferred place to fix this.

Avoid JS changes unless the current architecture absolutely requires a minimal browser event to show the same notice or switch tabs.

Do **not** change:

* Select2 JS
* `message.processed` hooks
* reinit/rehydration code
* sync methods
* tab click handlers
* display-only logic

unless there is no other safe option.

---

# REQUIRED TESTING

Test only this scope.

## For each of Tenant / Buyer / Landlord / Seller:

1. Leave one required field blank on an earlier tab → final Submit must be blocked
2. Leave multiple required fields blank across tabs → final Submit must be blocked
3. Complete all required fields → final Submit must work
4. Existing entered values must remain intact after failed validation
5. Edit flow must still work
6. Draft flow must still work
7. No Select2 selections should disappear
8. No tab flicker should be introduced
9. No dynamic field regressions should be introduced

---

# DO NOT “IMPROVE” ANYTHING ELSE

Do not make unrelated improvements.
Do not do cleanup.
Do not “standardize” shared logic.
Do not touch anything that is already working.

Only implement the final-submit required-field enforcement requested above.

---

# REQUIRED REPORT BACK

After implementation, provide:

1. Exact files changed
2. Exact methods changed
3. Exact summary of what was changed
4. Confirmation that Select2, shared rendering, and prior fixes were not altered unless absolutely necessary
5. Confirmation that only final-submit required validation behavior was changed
6. Test results for Tenant, Buyer, Landlord, and Seller

---

# SUCCESS CRITERIA

This task is complete only if:

* final Submit is blocked when required fields are missing
* the same required-field notice pattern used by Next appears on final Submit
* the user can tell what fields are missing
* the user is sent to the first invalid tab if supported by the current architecture
* all four flows work
* no regressions are introduced to Select2, multi-selects, tabs, drafts, edit mode, or dynamic fields

---

If you want extra protection, add this line at the top before pasting:

**“Do not touch any code related to recent Select2 fixes unless you prove it is directly required for final-submit validation.”**
