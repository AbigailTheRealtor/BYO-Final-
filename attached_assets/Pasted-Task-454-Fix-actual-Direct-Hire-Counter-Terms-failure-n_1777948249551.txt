Task #454 — Fix actual Direct Hire Counter Terms failure, not just logging

Task #453 only hardened logging and added a Route::has() guard. The error still occurs, so the root cause was not fixed.

Failing flow:
Direct Hire Review This Agent’s Proposed Terms
Example:
 /hire/agent/direct/142/buyer/residential

Action:
Click “Request Changes / Counter Terms”

Current issue:
Still shows:
“Error, Something went wrong please try again.”

Scope:
- Direct Hire flow only
- HireAgentDirectController
- Direct Hire preview blade/button/form only
- Counter redirect/action only

DO NOT touch:
- Offer Listing create/edit flows
- Agent preset flows
- Dashboards
- Accepted summaries
- Referral tracking
- Database schema
- migrate:fresh

Step 1 — Use the new logging from Task #453
Reproduce the error and immediately read:

storage/logs/laravel.log

Find the new log entry:
HireAgentDirect confirm failed

Copy the exact:
- exception class
- message
- file
- line
- trace first 10 lines

Step 2 — Check the frontend request
In browser DevTools → Network, click “Request Changes / Counter Terms” and record:
- request URL
- method
- status code
- response body

Step 3 — Identify whether failure happens:
A. before commitListingAndBid()
B. inside commitListingAndBid()
C. during listing creation
D. during bid creation
E. during redirect to counter route
F. inside the destination counter controller/page after redirect

Do not make any code changes until this is identified.

Step 4 — Fix the actual cause
Expected likely causes to inspect:

1. Counter route needs a parameter name other than bid_id.
Check route:list for the exact counter route signature.
If route expects {id}, {bid}, {listing}, or something else, update redirect()->route() accordingly.

2. Counter controller cannot find the bid/listing because of model binding mismatch.
Fix parameter names or route model binding only for Direct Hire counter routes.

3. Counter destination throws because required bid meta/listing fields are missing.
Add safe defaults/null checks only for Direct Hire-created bids.

4. The counter route redirects into a page requiring data that Direct Hire does not create yet.
Ensure Direct Hire counter flow creates the minimum required listing + bid data before redirect.

5. Frontend AJAX is receiving an HTML redirect/error instead of JSON.
If this button uses JS/fetch, either:
- return the JSON shape the frontend expects, or
- convert the button to a normal POST form redirect with @csrf.

Step 5 — Verification
Must test in browser:
- buyer/residential direct hire counter
- seller/residential direct hire counter
- landlord/residential direct hire counter
- tenant/residential direct hire counter

Each must:
- not show generic “Something went wrong”
- redirect/open the counter terms page correctly
- create no new Laravel log error

Step 6 — Report back
Return:
- exact exception from logs
- exact failed Network request
- actual root cause
- files changed
- route signature before/after if applicable
- verification results

Important:
Do not mark fixed if the only change is logging, Route::has(), or catch handling. The button must work in browser.