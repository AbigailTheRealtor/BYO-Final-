Task #860 follow-up — visual QA fixes still needed on Seller Offer Listing view

The prior audit claims full implementation, but visual QA of /offer-listing/seller/view/{id} shows several items are still not rendering correctly or are not visible at all.

IMPORTANT SAFEGUARDS

* ONLY edit:
  resources/views/offer-listing/seller/view.blade.php

* Do NOT modify:
  controllers
  routes
  validation
  models
  database schema
  Livewire components
  CSS assets outside this blade
  JavaScript assets outside this blade
  Hire Agent flows
  Buyer/Landlord/Tenant flows
  Limited Service code

* This is a surgical visual rendering + UX cleanup pass only.

TASKS

1. FIX VIDEO EMBED RENDERING

Current issue:
The page still shows a plain YouTube URL instead of an embedded video player.

Requirements:

* Properly detect:
  youtube.com/watch?v=
  youtu.be/
  youtube.com/embed/
  vimeo.com/

* Render responsive iframe embeds

* Keep fallback clickable URL only if parsing fails

* Prevent broken iframe output when URL is invalid

Done looks like:

* YouTube/Vimeo videos display as embedded players directly inside the Photos & Tours section.

2. VERIFY/FIX PHOTO LIGHTBOX MODAL

Current issue:
Only thumbnails are visibly confirmed. Modal/lightbox behavior is not verified.

Requirements:

* Clicking any property photo opens larger image modal/lightbox
* Navigation between photos works
* No duplicate modal markup
* No JS console errors
* Preserve existing thumbnails

Done looks like:

* Clicking a thumbnail opens the full-size image overlay/modal successfully.

3. RESTORE/RENDER PROPERTY DESCRIPTION

Current issue:
No visible Property Description section appears in visual QA.

Requirements:

* Render description section when populated
* Preserve nl2br(e()) formatting
* Hide section entirely when empty
* Place section near top of listing for marketing readability

Recommended placement:
Below Photos & Tours and above detailed technical sections.

Done looks like:

* Property Description section visibly appears with formatted text.

4. FIX BROKER COMPENSATION DISPLAY

Current issue:
Only generic commission wording renders.
Actual compensation values/details are missing.

Requirements:

* Render actual saved compensation structure values when present
* Support:
  percentage
  flat fee
  hybrid
  custom text
* Show timing/details fields if populated
* Hide empty rows

Done looks like:

* Broker Compensation section visibly shows real compensation terms instead of only generic wording.

5. VERIFY FINANCING DETAILS SECTION RENDERING

Current issue:
The new Financing Details mega-section does not appear in visual QA.

Requirements:

* Verify section actually renders when financing-related fields are populated
* Hide only truly empty subsections
* Ensure headings render cleanly
* Prevent empty card rendering

Supported subsections:

* Cash
* Assumable
* Cryptocurrency
* Exchange/Trade
* Lease Option
* Lease Purchase
* NFT
* Seller Financing

Done looks like:

* Financing Details section appears whenever saved data exists.

6. LIGHT UX CLEANUP (VIEW ONLY)

Current issue:
Page still feels like a raw admin table dump.

Requirements:

* Improve visual grouping inside existing blade only
* Add spacing between major sections
* Improve readability of long rows
* Prevent overcrowded text wrapping
* Preserve existing data rendering logic

Do NOT redesign the page or introduce new frameworks.

FINAL REQUIRED QA

After edits:

* Manually test /offer-listing/seller/view/{id}
* Confirm:
  embedded video works
  property description visible
  photo modal works
  financing section appears when populated
  broker compensation values display correctly
  no Blade errors
  no JS console errors

Then provide:

* exact sections fixed
* any intentionally hidden conditional sections
* confirmation no non-view files were modified.
