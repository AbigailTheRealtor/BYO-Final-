## Replit Instructions — FIX Buyer Listing “Broker Compensation & Agency Agreement Terms” Formatting (Add Sections + Bullet Layout)

### Applies To

**ONLY: Hire a Buyer’s Agent** listings (all Buyer listing types)

Wherever this section appears:

* Public / actual Buyer listing display
* Agent bid view (including private “Terms” modal/panel)
* Counter-bid view
* Accepted bid summary view (if shown)

---

## Problem (What’s Wrong Right Now)

Buyer **Broker Compensation & Agency Agreement Terms** is currently displayed as one continuous block with mixed formatting (badges/pills and values on separate lines), and it is **NOT broken into the correct sections**.

Example of current issues:

* Lease fee shows a “Compensation Type” badge and then the numbers on the next line (`$6,000 + 4%`)
* Fields are not grouped under the correct headers (Buyer’s Broker Compensation, Buyer’s Broker Lease Fee, Lease-Option Details, Legal Terms, Brokerage Relationship, Additional Terms)

---

## Required Outcome (Match This Structure)

Buyer Broker Compensation must display in **separate labeled sections** with **bullet formatting** and **divider lines**, like your example:

1. **Buyer’s Broker Compensation**
2. **Buyer’s Broker Lease Fee**
3. **Lease-Option Details**
4. **Legal Terms**
5. **Brokerage Relationship**
6. **Additional Terms**

Each section must show:

* Section header
* Bullet lines under that header
* Divider line between sections (same as your formatting style)

---

## Display Rules (IMPORTANT)

### 1) Value-first formatting (no “type badge” display when value exists)

* Do **NOT** display pills/badges like “Flat Fee + Percentage of the Gross Lease Value” in the listing output.
* If a dollar/percent amount exists, show the **final value line** instead.

Examples (display-only):

* `Buyer’s Broker Purchase Fee: 2% + $2,000`
* `Compensation Amount: $6,000` (for lease fee — show only the flat fee if that is the selected type)
* Lease-option:

  * `Compensation (When Option Is Created): 2%`
  * `Compensation (If Purchase Option Is Exercised): $7,000`

### 2) Keep conditional sub-bullets ONLY for Yes/No fields that require them

Under **Legal Terms**, if “Yes”:

* Early Termination Fee: Yes

  * Termination Fee Amount: $X
* Retainer Fee: Yes

  * Retainer Fee Amount: $X
* Retainer Fee Application: (display value)

If “No”, do not show the amount sub-bullet.

### 3) Use consistent bullets + spacing exactly like Tenant-style

* Use the same bullet style and indentation as the Tenant formatting you provided.
* Keep “Interested in a Lease Agreement: Yes/No” inside the **Buyer’s Broker Lease Fee** section.

---

## Implementation Requirement (Display-Only)

This is a **presentation / rendering** fix only:

* Group the already-saved Buyer broker comp fields into the correct section blocks
* Output bullets under each section
* Remove “type badge/pill” output in the display layer when values exist
* Do NOT change the saved data, field keys, or form UI.

---

## Safeguards (Non-Negotiable)

**DO NOT:**

* change database tables/columns, JSON keys, or saved values
* change the Buyer listing creation form fields, validations, tooltips, placeholders, or options
* change the meaning of compensation logic (percent vs flat vs combo)
* change Tenant formatting (Tenant stays the baseline)
* hardcode example values

**DO:**

* update only the Buyer listing/bid display template/component
* reuse the existing stored values and format them into sections + bullets

---

## Acceptance Criteria (How We Approve It)

✅ Buyer “Broker Compensation & Agency Agreement Terms” shows these section headers in this order:

* Buyer’s Broker Compensation
* Buyer’s Broker Lease Fee
* Lease-Option Details
* Legal Terms
* Brokerage Relationship
* Additional Terms

✅ Each section has bullet lines beneath it
✅ No “Compensation Type” pills/badges are shown in the listing output when numeric values exist
✅ Conditional sub-bullets appear only when applicable (Early Termination, Retainer)
✅ Works in listing view + agent bid view + counter view

**Provide screenshots:**

1. Before (current unsectioned display)
2. After (sectioned + bullets matching the example)

---

If you want, I can also add one extra line for Replit: **“Use the same divider line styling used in other sections of the listing (the horizontal rule / underscore divider) so it matches the rest of the UI.”**
