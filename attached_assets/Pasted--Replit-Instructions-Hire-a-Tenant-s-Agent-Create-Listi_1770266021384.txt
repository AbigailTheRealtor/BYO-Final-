## Replit Instructions — Hire a Tenant’s Agent (Create Listing) Fixes + Safeguards

**Applies To:** `tenant@exp.com` → Hire a Tenant’s Agent → **Create Listing**
**Property Types:** Residential + Commercial (as specified below)

You must complete **three fixes** with strict safeguards.

---

# SAFEGUARDS (Non-Negotiable)

1. **No destructive DB changes**

   * Do NOT rename tables/columns, do NOT delete fields, do NOT mass update records.
2. **UI + conditional logic only**

   * These are form behavior fixes (rendering + conditional show/hide + clearing dependent fields).
3. **Do NOT change tooltips/labels** unless explicitly instructed below.
4. **Backward compatible**

   * Existing drafts must still load without crashing.
5. **No regressions**

   * Fix must not break Buyer/Seller/Landlord flows.
6. **Commercial switch behavior** must clear dependent state (pets) without affecting unrelated fields.

---

# FIX 1 — “View Preference” → Other should show ONLY the input box (not “Other View Preference:”)

### Issue

When user selects **Other** under **View Preference**, the UI displays an incorrect label like:

* “Other View Preference:”
  This should NOT happen.

### Required Change

When **View Preference = Other**:

* Show **only** a single input box for custom text (with existing placeholder/tooltip behavior if any).
* The label should remain the normal field label (e.g., “View Preference:”).
* Do **not** display “Other View Preference:” as a label or a duplicate heading.

### Implementation Notes

* The “Other” input should be conditionally rendered as an **inline conditional input** tied to the same field.
* Do NOT concatenate the word “Other” into a new label.
* Ensure the saved value stores the custom text in the correct “other” field/key (do not overwrite the main selection unless that’s the established pattern).

### Test

* Select View Preference → Other
* Confirm only the input appears (no “Other View Preference:” label)
* Save draft → reload → confirm it still renders correctly.

---

# FIX 2 — “Acceptable Property Condition” options must be EXACTLY these 5 (and only these 5)

### Issue

Acceptable Property Condition currently contains incorrect or extra options.

### Required Options (Exact Text — Must Match)

This dropdown/multi-select must include **only** the following options:

1. **No updates needed: Completely updated**
2. **Semi-updated: Needs minor updates**
3. **Not updated: Requires a complete update**
4. **Open to any type of property**
5. *(No other options)*

✅ Keep the exact punctuation and capitalization above.

### Safeguards for This Fix

* Do NOT change how the field is stored (keep existing DB field).
* If older drafts contain now-removed values:

  * Do not crash the form
  * Either (a) show them as “(Previously selected)” in a non-selectable way, or (b) silently ignore them on render but preserve record integrity.
  * Preferred: ignore on render, but do not throw errors.

### Test

* Create new Tenant listing → verify only those 4 options appear (plus the “Open to any type of property” option = total 4? Actually list has 4+1 = 4? Re-check: you listed 5 including “Open to any type of property.” That’s 4 total plus open = 4? It’s **4 options**?
  **Implement exactly the list provided** (do not add more).
* Load an older draft with legacy values → form still loads.

---

# FIX 3 — Switching Property Type to Commercial must REMOVE Pets section and CLEAR pet sub-field state

### Issue

User fills out Pets (Residential), then switches Property Type to **Commercial**.
The Pets sub-questions remain visible or remain populated.

### Required Change

When Property Type is changed to **Commercial**:

1. **Hide the entire Pets section** immediately.
2. **Clear all pets-related values from the live form state** (and draft state if applicable), including:

   * Has Pets / Pet Types
   * Breed restrictions
   * Pet count
   * Pet weight
   * Any pet deposit/fee related inputs (if present)
   * Any pet notes fields (if present)

### Important Behavior Rules

* If the user switches back to **Residential**, the Pets section may reappear.

  * Do NOT auto-restore the previously entered pet values unless your platform already supports state restoration.
  * Preferred safe approach: switching to Commercial clears them; switching back shows blank Pets fields.

### Implementation Notes (Livewire/Reactive)

* Add a watcher/handler on `property_type` changes:

  * When new value is Commercial → call `resetPetsFields()`
* Ensure conditional rendering checks property_type:

  * `if property_type === 'Commercial'` → Pets section must not render at all.

### Safeguards

* Do NOT delete saved pet data in the database for already-submitted Residential listings.
* This clearing applies to the **in-progress create/edit session** state when user toggles property type.
* If a listing is already Commercial, pets must never show.

### Test

1. Create Tenant listing as Residential
2. Fill Pets + subquestions
3. Switch Property Type to Commercial

   * Pets section disappears
   * Subquestions disappear
   * Values cleared (confirm by switching back to Residential—fields are blank)
4. Save draft while Commercial
5. Reload draft

   * Pets section remains hidden and does not reappear

---

# REQUIRED PROOF OF COMPLETION

Replit must provide:

* File paths changed
* Before/after screenshots or short screen recording
* Confirmation tests passed for:

  * View Preference → Other
  * Acceptable Property Condition options list
  * Residential → Commercial switch clears Pets correctly

---

If you want, tell me whether **Acceptable Property Condition** is a **single-select** or **multi-select** on the Tenant form (I’ll assume multi-select unless you say otherwise), and I can add the exact “legacy value” handling behavior you prefer.
