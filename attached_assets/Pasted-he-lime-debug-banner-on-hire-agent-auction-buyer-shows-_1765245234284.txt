he lime debug banner on /hire/agent/auction/buyer shows:

DEBUG MAIN: user_type="buyer" | service_type="full_service" | property_type="income"

So the variables are correct. The page is still blank on Tabs 3–7 for Income, so the bug must be in the tab partials that render when:

user_type = buyer

service_type = full_service

From your own log you wrote:

“for /hire/agent/auction/buyer, the tab content depends on $service_type.
I see from lines 1850–1851 that for buyer user type, it DOES include
livewire.hire-buyer-agent.buyer-agent-auction-tabs.commission-based.services”

That means the Services tab is coming from:

resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/services.blade.php


Please work in THIS file and the matching “additional-details / broker-compensation / buyer-info” files, not in the tenant-agent-auction-tabs directory.

1) Add a debug line in the correct Services partial

Open:

resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/services.blade.php


At the very top of that file, add:

<div style="background:yellow;color:black;font-weight:bold;padding:6px;">
    DEBUG SERVICES (buyer auction): property_type = "{{ $property_type ?? 'NULL' }}"
</div>


Then run:

php artisan view:clear && php artisan cache:clear


and I’ll:

Go to /hire/agent/auction/buyer

Pick Income as the property type

Click the Services tab

I should now see a yellow debug box at the top of the Services tab. If I don’t, we’re still in the wrong file.

2) Once the yellow box appears, wire REAL Income services

In that same services.blade.php file, find the conditional block that switches on $property_type (or $buyer_property['property_type']), e.g. something like:

@if ($property_type === 'Residential')
    @include('hire-buyer-agent.partials.services-residential-full-service')
@elseif ($property_type === 'Commercial')
    @include('hire-buyer-agent.partials.services-commercial-full-service')
@elseif ($property_type === 'Vacant Land')
    @include('hire-buyer-agent.partials.services-land-full-service')
@endif


Change this so Income has its own branch.
Use the actual Income services partial you already created (where you have the 2–4 unit wording). For example:

@php
    $type = strtolower($property_type ?? '');
@endphp

@if (in_array($type, ['income', 'income property', 'income_property', 'income 2–4 units']))
    @include('hire-buyer-agent.partials.services-income-full-service') {{-- your Income services partial --}}
@elseif ($type === 'residential')
    @include('hire-buyer-agent.partials.services-residential-full-service')
@elseif ($type === 'commercial')
    @include('hire-buyer-agent.partials.services-commercial-full-service')
@elseif ($type === 'vacant land' || $type === 'vacant_land')
    @include('hire-buyer-agent.partials.services-land-full-service')
@endif


Please do NOT normalize Income to Residential anymore (no $normalizedPropertyType = 'Residential' logic) – I want Income to use its own partial.

3) Repeat the same pattern for the other Income tabs

For each of these files:

resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/additional-details.blade.php
resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/broker-compensation.blade.php
resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/tenant-info.blade.php   {{-- buyer info --}}


(exact filenames may differ slightly, but they’re in the same buyer-agent-auction-tabs/commission-based folder)

Add a small yellow debug box at the top showing property_type, like in Services.

Update the @if / @elseif blocks so that when the lowercased $property_type is “income” (or any Income variant), the file includes the Income-specific partial:

purchasing-terms-income

additional-details-income

broker-compensation-income

buyer-information-income

Use the same Income partials we previously used when Income was working.

After changes, clear caches again:

php artisan view:clear && php artisan cache:clear


Then I will re-test /hire/agent/auction/buyer with property type Income and make sure Tabs 3–7 show the correct Income layouts (not Residential, not blank).