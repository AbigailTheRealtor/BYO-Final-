Fix Seller Offer Listing HOA / Association “Other” field layout + icon issues.

Scope:
Only Seller Full Service Offer Listing files.
Do not touch Buyer, Tenant, Landlord, Hire Agent, Limited Service, routes, database schema, or validation.

Issue 1:
On /offer-listing/seller, when Association Type = Other, the custom “Other” input appears correctly under Association Type, but an extra unwanted Select field appears under Association Name. Remove that extra Select field. Association Name should be followed directly by Fee Frequency.

Likely target:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-details.blade.php
or the Seller HOA / Association partial being rendered by that tab.

What to audit:
Find the HOA / Association section.
Find the markup between:
- Association Name
- Fee Frequency

There is likely an orphaned select/input-cover block rendering a Select placeholder without a label. Remove only that orphaned unlabeled Select block. Do not remove Association Type, Association Type Other input, Association Name, Fee Frequency, or any wire:model properties.

Issue 2:
Icon alignment/rendering is still inconsistent in this HOA section.
The icon for the unwanted orphan Select appears by itself, and some icons are sitting oddly/duplicating around fields.

Fix rules:
- Every icon-prefixed field must be wrapped consistently in the same pattern used by working Seller fields:
  <div class="input-cover has-icon" data-icon="...">
      field here
  </div>
- Do not place standalone <i> icons outside the input wrapper.
- Do not duplicate icons manually if the page already injects icons from data-icon.
- Custom “Other” text inputs should use the same has-icon/data-icon wrapper as normal text inputs.
- Select2/multiselect fields should not get orphan standalone icons.

Acceptance checks:
1. Go to /offer-listing/seller.
2. Open the Property Details / HOA Association section.
3. Set “Is there a HOA?” = Yes.
4. Set “Association Type” = Other.
5. Confirm only one custom input appears under Association Type: “Describe the association type.”
6. Confirm there is NO extra unlabeled Select box under Association Name.
7. Confirm Association Name is followed by Fee Frequency.
8. Confirm icons align inside fields and no standalone blue icon appears on its own line.
9. Confirm Save Draft and reload preserves Association Type Other, Association Name, Fee Frequency, and Association Amenities.
10. Confirm no changes were made outside Seller Full Service Offer Listing files.

Report exactly which file(s) changed and what orphan field/block was removed.