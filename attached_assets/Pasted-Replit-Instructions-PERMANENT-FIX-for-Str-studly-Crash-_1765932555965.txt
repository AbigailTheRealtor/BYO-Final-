Replit Instructions — PERMANENT FIX for Str::studly() Crash + Fix Next Button + Prevent Losing Progress
A) Stop Guessing: Locate the FIRST Non-Vendor File Causing the Crash

Do not change vendor files.
When the crash happens, open the exception page → Stack trace.

Click “Expand vendor frames”

Scroll until you see the first file path that is NOT in /vendor/ (it will usually be in:

app/Http/Livewire/...

resources/views/livewire/...

or another Blade view)

That file is the real source. Fix must happen there.

✅ Deliverable requirement: In the response back to me, you must paste:

the first non-vendor file path + line number

the snippet around it

If you can’t provide that, the fix wasn’t actually found.

B) Fix the Actual Cause: Livewire Helper Is Being Called With EMPTY INPUT

This specific error almost always comes from one of these in our code:

1) Empty wire:key

Examples that MUST NOT exist:

wire:key=""

wire:key="{{ $variable }}" where $variable can be null/empty

wire:key="{{ Str::studly($something) }}" where $something can be empty

✅ Fix rule:
All wire:key must be stable and never derived from a label that can be blank. Use IDs + constants:

Good pattern:

wire:key="tenant-broker-comp-{{ $listingId }}-{{ $loop->index }}"

If no ID exists, generate one once in mount() and reuse it (never blank).

2) Empty wire:model

Examples that MUST NOT exist:

wire:model=""

wire:model.defer=""

wire:model="{{ $variable }}" where $variable becomes empty during tab load

✅ Fix rule:
Every wire:model must reference a real Livewire property that is initialized in mount().

3) Rendering a Livewire component with an empty name

Examples:

@livewire($componentName) where $componentName is sometimes ''

<livewire:{{ $componentName }} />

✅ Fix rule:
Guard it:

If $componentName is empty → don’t render it OR render a safe fallback component.

C) Initialize Broker Compensation State BEFORE the Tab Renders (Commercial)

This error can happen on tab load if Commercial has uninitialized properties.

In the Livewire component that owns Broker Compensation:

In mount() (or the initialization method), ensure ALL Broker Comp properties exist with defaults:

the selected type fields

the dependent amount fields

any arrays for dropdown options

If any option set is built dynamically, ensure it never returns null/empty arrays.

✅ Required: Commercial must not rely on “user selection” to create required properties.

D) Users Must NOT Lose Progress If an Error Happens

Right now, if the page crashes, the user shouldn’t have to start over.

Implement both:

1) Autosave Listing Draft by Tab

When the user clicks Next (or when leaving the tab), save a “draft” snapshot:

store the active tab’s fields immediately

so refresh/crash reloads the last saved state

2) Persist Tab Index in Session

Store the current tab index in session/local storage so a refresh returns user to the same tab.

✅ Minimum acceptable: after a refresh, the listing reopens with the data that was already entered.

2) Fix “Next” Button Not Working on Broker Compensation (ALL Property Types)

This needs to be treated as a separate bug and fixed like one.

A) Confirm Button Type / Event

If the Next button is inside a <form>, it may be submitting or being prevented.

✅ Required:

If Next is navigation only: set type="button"

Ensure it triggers the correct handler:

wire:click="nextStep" or your equivalent function

B) Find What’s Blocking Navigation

On click Next:

Check browser console for JS errors

Check Network → Livewire request response

Check if validation is failing silently

✅ Required:

If validation fails, show errors and keep the user on the tab.

If validation passes, the tab index MUST increment and render the next tab.

C) Hidden Field Validation Must Not Block Next

If the tab has conditional fields (based on dropdown selection), validation must only require visible fields.

✅ Required:

Update rules so hidden fields are not required

Clear dependent values on option change

Call:

$this->resetValidation();

$this->resetErrorBag();

3) Required QA (No Exceptions)

After applying fixes, test:

Commercial → Broker Compensation

open tab (no crash)

select each Tenant’s Broker Lease Fee option

switch options rapidly 10–15x

click Next (advances)

Residential + Other Property Types

click Next on Broker Compensation (advances)

Crash Recovery

force refresh mid-form and confirm the listing data remains and user returns to same step (or at least draft is restored)

Why it’s still not working (what to tell Replit)

Add this line verbatim:

“We need the first non-vendor stack-trace file + line number. Fixes that don’t identify the first non-vendor call site are not accepted, because the crash is being triggered by an empty Livewire directive (wire:key / wire:model / empty dynamic component) during render.”