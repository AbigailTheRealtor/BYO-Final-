Below are **copy/paste Replit instructions with safeguards** for **Hire a Seller’s Agent → Actual Listing Display (Residential)** using **[tenant@exp.com](mailto:tenant@exp.com)** for QA. These fixes also include your note that **City/County/State/Zip must display for ALL Seller property types**.

---

## ✅ REPLIT COPY/PASTE INSTRUCTIONS (WITH SAFEGUARDS)

### Feature: Hire a Seller’s Agent → Actual Listing Display (Show Page)

### Test Login: [tenant@exp.com](mailto:tenant@exp.com)

### Primary Scope: Residential Seller listings

### Also Applies (Location fields): ALL Seller property types (Residential/Income/Commercial-Business/Vacant Land)

---

# 1) QA REPRO STEPS (DO NOT SKIP)

1. Login: **[tenant@exp.com](mailto:tenant@exp.com)**
2. Create **Hire a Seller’s Agent** listing → **Residential**
3. Fill out:

   * City, County, State, Zip
   * Assignment Contract = **Yes**
   * Seller Under Contract for Assignment = **Yes**
   * Assignment Contract Fee to Broker = enter a value (ex: 5% or $5,000 depending on your field type)
   * Pool = Yes
   * Pets = Yes + Pet Restrictions (enter restrictions)
   * Broker Compensation: Seller’s Broker Purchase Fee
   * Buyer’s Broker Commission Structure (choose a CURRENT option)
   * Legal Terms: Retainer Fee = Yes + Amount + Application
   * Seller’s Current Status
4. Submit listing → open **actual listing display** (Seller show page)
5. Confirm fields are currently missing/misplaced.

---

# 2) FIX: City / County / State / Zip not showing (ALL PROPERTY TYPES)

### Issue

City, County, State, Zip Code do not appear on Seller actual listing display.

### Instructions

1. Open the **Seller listing show page** Blade/view used for Hire a Seller’s Agent actual listing display.
2. Locate the **Property Details / Location** section.
3. Render these four fields on the show page using the **persisted listing record** (not request/draft):

   * **City**
   * **County**
   * **State**
   * **Zip Code**
4. Ensure the show page is reading the same stored keys used by Seller create/edit (common patterns):

   * direct columns on listing model, OR
   * JSON `property_details` blob, OR
   * related “details” table/object
5. If your Seller flow has different storage for Residential vs other property types, normalize the show page to pull from the correct source for each property type.

### SAFEGUARDS

* **No “Not Provided”** text on show page. If empty, show blank or hide line (match your platform standard).
* Full address may remain hidden, but City/County/State/Zip must always show.
* Add a dev-only log/assert (optional): if create/edit has values but show page renders blank, the key mapping is wrong.

### Acceptance Criteria

On Seller show page (all property types):

* City, County, State, Zip Code display correctly.

---

# 3) FIX: Assignment Contract conditional fields missing (Residential)

### Issues

When user selects **Assignment Contract = Yes**, the show page is missing:

* **Seller Under Contract for Assignment**
* **Assignment Contract Fee to Broker**

### Instructions

1. In Seller show page under **Sale Terms** (or the section where assignment contract is displayed), add conditional rendering:

**If Assignment Contract Allowed/Selected == Yes:**

* Render line: **Seller Under Contract for Assignment: [Yes/No]**
* Render line: **Assignment Contract Fee to Broker: [value]**

2. Ensure the show page reads the correct saved keys for these fields from the Seller listing record.

### SAFEGUARDS

* Only show these two lines if Assignment Contract = Yes.
* If Assignment Contract = Yes but these fields are empty:

  * display blank value OR hide the specific line (NO “Not Provided”).
* Confirm create/edit saves both fields into the listing model (not draft-only). If they save in drafts but not on final submit, fix the submit payload mapping.

### Acceptance Criteria

When Assignment Contract = Yes:

* Both fields show on actual listing.

---

# 4) FIX: Pool missing (Residential)

### Issue

Pool is not showing on Seller actual listing display.

### Instructions

1. Add **Pool** to Seller show page in the **Property Details** section.
2. Render:

* **Pool: Yes/No**
* If there are subfields (Pool Type / Screened / Heated), show them under Pool when present.

### SAFEGUARDS

* Normalize truthy values: `true`, `1`, `"Yes"` must render “Yes”.
* If user explicitly selected “No”, show “Pool: No” (unless your platform hides “No” fields—follow your standard consistently).

### Acceptance Criteria

Pool shows on listing.

---

# 5) FIX: Pet Restrictions label/field wrong (Residential)

### Issue

Listing displays “Breed Restrictions” (wrong label) and Pet Restrictions is missing.

### Instructions

1. On Seller show page in the Pets block:

   * Replace label **Breed Restrictions** with **Pet Restrictions**
2. Ensure the value displayed is the Seller field that stores **Pet Restrictions** (not the landlord “breed restrictions” field).

### SAFEGUARDS

* This is a key-mapping bug: ensure Seller show page is not reusing the Landlord/Breed restriction field key.
* If Pets = No, hide Pet Restrictions line (or show blank—follow standard).

### Acceptance Criteria

* Label reads **Pet Restrictions**
* Correct value displays.

---

# 6) FIX: Broker Compensation missing fields + outdated options

## 6A) Seller’s Broker Purchase Fee missing

### Instructions

1. In Seller show page under **Broker Compensation** section, add:

* **Seller’s Broker Purchase Fee: [value]**

2. Pull from the saved Seller compensation field key used by create/edit.

### SAFEGUARDS

* If stored as numeric + type, display using the same formatted string pattern used by Buyer show page (e.g., “3% of Total Purchase Price”).
* No placeholders (“Not Provided”).

## 6B) Buyer’s Broker Commission Structure shows old options

### Instructions

1. Locate the mapping used on Seller show page for “Buyer’s Broker Commission Structure”.
2. Replace any legacy enum-to-label array with the **current options list used on the Seller create form**.
3. If your create form stores the label directly, do not remap—display the stored label as-is.

### SAFEGUARDS

* Backwards compatibility:

  * If older listings contain old enum values, map those to the closest current label.
  * If no match, display the raw stored value (better than blank).
* Ensure Seller show page uses the same config/options source as Seller create form.

### Acceptance Criteria

* Seller’s Broker Purchase Fee displays.
* Buyer’s Broker Commission Structure displays using the current option wording.

---

# 7) FIX: Legal Terms – Retainer Fee lines missing

### Issues

Missing on Seller show page:

* Retainer Fee
* Retainer Fee Amount
* Retainer Fee Application

### Instructions

1. In Seller show page under **Legal Terms**, render in this order:

* **Retainer Fee: Yes/No**
* If Retainer Fee = Yes:

  * **Retainer Fee Amount: [currency formatted]**
  * **Retainer Fee Application: [value]**

### SAFEGUARDS

* Normalize Yes/No casing to capitalized output (“Yes”, “No”) if you are formatting.
* Currency formatting for amount: `$5,000` style.
* No “Not Provided” text. If missing, blank or hidden line.

### Acceptance Criteria

Retainer fields display correctly when selected.

---

# 8) FIX: Seller’s Current Status placement (under First Name)

### Issue

Seller’s Current Status is in the wrong place; must be under First Name in Seller Info.

### Instructions

1. In Seller show page, locate **Seller Info** block (where First Name/Last Name/etc appear).
2. Move the “Seller’s Current Status” line so it appears directly under **First Name**.
3. Ensure it is removed from its old location to prevent duplicates.

### SAFEGUARDS

* Only render if value exists (otherwise blank/hidden per standard).
* Must remain in Seller Info for all property types if field exists.

### Acceptance Criteria

Seller’s Current Status displays directly under First Name.

---

## ✅ FINAL QA CHECKLIST (PASS/FAIL)

Using [tenant@exp.com](mailto:tenant@exp.com) on a Residential Seller listing:

* [ ] City/County/State/Zip display (and also display on other Seller property types)
* [ ] Pool displays
* [ ] Assignment Contract = Yes → shows Seller Under Contract for Assignment + Assignment Contract Fee to Broker
* [ ] Pet Restrictions displays and label is correct (not Breed Restrictions)
* [ ] Seller’s Broker Purchase Fee displays
* [ ] Buyer’s Broker Commission Structure uses current options
* [ ] Retainer Fee + Amount + Application display when Retainer Fee = Yes
* [ ] Seller’s Current Status appears directly under First Name

---

If you paste the **current Seller show page section** (even just the Broker Compensation + Legal Terms block), I can rewrite this into **exact “replace this block with this block”** instructions tailored to your exact Blade/component names—but the above is already structured for your dev to implement safely without guessing.
