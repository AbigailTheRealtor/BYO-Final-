Phase 3 is complete. Now do a small polish pass only on the new My Offer Presets feature.

Objective

Improve usability and adoption of the Agent Preset UI without changing the underlying architecture.

This is not a rebuild.
Do only the following targeted improvements:

improve services list visibility/contrast
add clear required/recommended guidance
add “Last Updated” on preset cards
add “Copy Hire Me Link” button on each preset card

Do not expand beyond this scope.

Audit first

Before changing code, audit and report:

where service item text/colors are currently styled in the preset edit page
which preset fields are truly required vs only recommended
whether AgentDefaultProfile already has usable created_at / updated_at timestamps for display
what the current direct-hire route pattern is for generating a per-preset Hire Me link
safest place to add a “Copy Hire Me Link” button on preset cards without cluttering the layout

Do not code until the audit is complete.

Fix 1 — Improve services visibility / contrast

On the preset edit page, make the services list easy to read.

Problem

The service items are currently too light / low contrast and are hard to see.

Required change

Improve contrast and readability for:

service labels
checkboxes
count text if needed
service list container if needed
Rule

Use the smallest styling change possible.
Do not redesign the form.

Fix 2 — Add Required / Recommended guidance

Add visual guidance so agents know what matters most.

Required

Mark Services as:

Required
Recommended

Mark Agent Overview / Bio as:

Recommended
Optional

Credentials and presentation links can remain unmarked or lightly described, unless already clearly useful.

Rule

Do not add heavy validation unless it already exists safely.
This is primarily a UX guidance improvement.

Use small labels/microcopy such as:

Required
Recommended

Keep it clean and lightweight.

Fix 3 — Add “Last Updated” to preset cards

On the My Offer Presets index page, show when each saved preset was last updated.

Required behavior

For saved presets, display something like:

Last updated: 2 days ago
or
Updated Mar 12, 2026

Use the existing timestamp on the preset model if available.

Rule

Do not add new DB fields.
Reuse existing timestamps only.

If a preset is not set, no last-updated text is needed.

Fix 4 — Add “Copy Hire Me Link” button to each preset card

On the My Offer Presets index page, add a button for saved presets:

Copy Hire Me Link
Behavior

The button should copy the direct-hire URL for that exact preset combination:

selected agent
role
property type

Use the current direct-hire route pattern already implemented.
Do not change the route structure in this polish pass.

Scope

Only show this button when:

preset exists and is usable

If the preset is missing or incomplete, do not show Copy Link.

UX

Use the smallest clean implementation:

button click copies link to clipboard
optionally show a lightweight success state such as:
Copied!
Link copied

Do not build a full share modal.

Preset usability rule

A preset card should now clearly communicate:

whether it exists
whether it is usable
when it was last updated
how to edit it
how to copy its Hire Me link

Keep the card layout clean and compact.

Important restrictions

Do NOT:

change the Hire Me route structure yet
add widget/embed work yet
redesign the preset system
change DB schema
change core bid logic
change AgentBidMapperService
change direct-hire flow behavior
change marketplace flow

This is polish only.

Safeguards
No DB renames
No DB deletes
No route refactor
No new tables
No broad CSS rewrite
No breaking preset save/edit behavior
No breaking direct Hire Me flow
No breaking generic marketplace Hire Agent flow

Stop and report if any requested polish item requires broader structural changes.

Deliverable

After implementation, report:

files audited
files changed
what contrast/readability changes were made
what sections were labeled required/recommended
how “Last Updated” is rendered
how “Copy Hire Me Link” works
confirmation that no core flows were changed
Priority order

If you do these in sequence, do them in this order:

services visibility/contrast
Copy Hire Me Link
Last Updated
Required/Recommended labels