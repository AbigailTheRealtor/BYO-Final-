## Replit Instructions With Safeguards — Hire a Landlord’s Agent Edits (Listing Display)

### Safeguards (must follow)

1. Do not rename or delete any database columns
2. Do not delete or modify any existing listings, drafts, or agent bids
3. Do not change option values, field labels in the form UI, or tooltips
4. Any database changes must be add-only, nullable, non destructive (these fixes should be display-only)
5. Only modify the **actual listing display** logic for **Hire a Landlord’s Agent** listings (Commercial + shared sections), do not modify Buyer, Seller, or Tenant listing displays

---

# A) Property Condition is not showing on listing

### What to do

1. Find the **Hire a Landlord’s Agent actual listing display** row builder (Blade/Livewire/helper) that outputs “Property Details” / “Property Condition” rows.
2. Confirm the saved field name used by the form (example patterns: `property_condition`, `landlord_property_condition`, or a JSON key under listing details).
3. Add a listing display row for Property Condition using the exact saved field/key.

### Requirement

* If a value exists, display:
  **Property Condition:** {value}
* If empty, do not render the row

---

# B) Rename two labels on the actual listing display

### Requirements (display-only, do not change the form field label)

* Change **Pet Types:** → **Acceptable Pet Types:**
* Change **Pet Weight (lbs):** → **Maximum Weight Per Pet (lbs):**

### What to do

In the listing display label map (or the row builder where labels are written):

* Update the display label strings only for Hire a Landlord’s Agent listings.

---

# C) Rent Includes not showing when “None” is selected

This is a classic “empty/falsey” display bug: “None” is likely stored as a string or an option that your display logic treats as empty and skips.

### What to do

1. Locate the display logic for `rent_includes` (or equivalent).
2. Update the condition that decides whether to display it:

   * Treat “None” as a valid selection that should still render.

### Requirement

* If Rent Includes is set to **None**, listing must show:
  **Rent Includes:** None
* If it is truly null/empty (user never answered), then omit.

---

# D) Digital Photo Enhancements — show sub-questions directly under the parent

### Requirement

“Digital Photo Enhancements” must display like this:

* **Provide Digital Photo Enhancements:** Yes/No

  * (If Yes) show the sub-question answers directly beneath it (indented/stacked), not as a separate standalone section elsewhere.

### What to do

1. Find the listing display block that prints:

   * “Provide digital photo enhancement” (parent)
   * and any related fields (sub-questions)
2. Ensure sub-questions are only rendered when parent is Yes.
3. Render them immediately after the parent row in the same section.

### Important

* Do not create a second “Digital Photo Enhancements” section
* Do not print sub-questions if parent is No

---

# E) Early Termination Fee — compress into one line

### Current issue

It prints:

* Early Termination Fee: Yes
* Termination Fee Amount: $400

### Required output

* If Yes and amount exists: **Early Termination Fee:** Yes ($400)
* If Yes and amount is empty: **Early Termination Fee:** Yes
* If No: **Early Termination Fee:** No
* If No: do not show any amount line

### What to do

1. Locate where Early Termination Fee and Termination Fee Amount rows are added.
2. Replace with a single computed display value:

   * Build a formatted string based on selection + amount
3. Do not render the “Termination Fee Amount” row at all anymore for Hire a Landlord’s Agent listing display.

---

# F) Tenant’s Broker Commission Structure — hide fee when “No Compensation Offered” is selected

### Current issue

User changed selection but old fee still shows (stale data leakage).

### Required logic (display gating)

* If `tenant_broker_commission_structure` == “No Compensation Offered to Tenant’s Broker”

  * Show only: **Tenant’s Broker Commission Structure:** No Compensation Offered to Tenant’s Broker
  * Do NOT show **Tenant’s Broker Commission Fee** even if it still has an old stored value.

### What to do

1. In the Broker Compensation display block, add strict gating:

   * Only render Tenant’s Broker Commission Fee row when the structure selection is a compensation-offered option.
2. Do not rely on whether the fee has a value; rely on the structure selection.

---

# G) Additional Details tab missing on Actual listing

### What to do

1. Confirm the Additional Details tab fields are being saved (they likely are).
2. Find the actual listing display template/section list where tabs/sections are rendered.
3. Add the Additional Details section block to the actual listing display for Hire a Landlord’s Agent listings.
4. Ensure it appears in the same order as your UI tab order.

### Requirement

* The section heading must appear (e.g., “Additional Details”)
* Only show fields inside it that have values (except where “None” must still display)

---

# H) Payment Timing for Broker Fees shows “full_execution” instead of the readable label

### Cause

You’re displaying the stored enum/key, not the human-readable label.

### Required output

Show the selected label, for example:
**Payment Timing for Broker Fees:** Full amount upon execution of lease, sales contract, or other transfer agreement

### What to do

1. Find where Payment Timing is displayed.
2. Replace raw value output with label mapping:

   * Map `full_execution` → “Full amount upon execution of lease, sales contract, or other transfer agreement”
   * Include all other enum values in the same mapping (so none show raw keys)
3. Use the same mapping source you use in the form options if it exists (best), otherwise create a display-only map.

---

# I) Leasing Space display fix — Commercial only (Single Room vs Entire Property)

Use the exact logic below (this is the combined, correct instruction set):

### Required rule

In Hire a Landlord’s Agent (Commercial) listing display:

* Do not merge leasing space row sets
* Do not print all non-empty leasing space fields
* Add strict gating based on selected leasing_space
* Use associative-array keyed rows to dedupe
* Output rows in explicit order per selection

#### If leasing_space = Single Room, show only in this order:

Leasing Space,
Guests are,
Restrictions Include,
Shared Areas Available,
Maintenance and Repairs Are Handled By,
Maintenance Response Time,
Utilities,
Common Area Maintenance,
Included Storage Space,
Storage Space Size,
Bathroom Facilities,
Approximate Room Size.

Do not display: Shared Amenities Include, Building Hours, 24/7 Access Available, Zoning Allows, Space Features, Neighboring Tenants Include.

#### If leasing_space = Entire Property, show only in this order:

Leasing Space,
Restrictions Include,
Maintenance and Repairs Are Handled By,
Maintenance Response Time,
Included Storage Space,
Storage Space Size,
Shared Amenities Include,
Building Hours,
24/7 Access Available,
Zoning Allows,
Space Features,
Neighboring Tenants Include.

Do not display: Guests are, Shared Areas Available, Utilities, Common Area Maintenance, Bathroom Facilities, Approximate Room Size.

Also ensure Restrictions Include is pulled from the same saved field used by the form and renders only once.

---

## Copy/Paste Prompt for Replit AI (Everything Above, One Shot)

Paste exactly:

Apply display-only fixes for Hire a Landlord’s Agent listings. Safeguards: do not rename or delete database columns, do not delete or modify listings/drafts/bids, do not change form option values, labels, or tooltips. Only modify the actual listing display logic for Hire a Landlord’s Agent.

1. Property Condition: add it to the actual listing display using the exact saved field/key from the form, render only if not empty.

2. Label changes on actual listing display only: change “Pet Types:” to “Acceptable Pet Types:” and change “Pet Weight (lbs):” to “Maximum Weight Per Pet (lbs):”.

3. Rent Includes: if “None” is selected, it must still render “Rent Includes: None”. Do not treat “None” as empty.

4. Digital Photo Enhancements: treat sub-questions as children of “Provide Digital Photo Enhancements”. Render the parent row first, then render the sub-question answers directly underneath only when parent is Yes. Do not render sub-questions when parent is No and do not create a separate section elsewhere.

5. Early Termination Fee: compress to one line. If Yes and amount exists, display “Early Termination Fee: Yes ($amount)”. If Yes without amount, show “Yes”. If No, show only “No” and never show any amount row. Remove/hide the separate “Termination Fee Amount” row in listing display.

6. Tenant’s Broker Commission Structure: if selection is “No Compensation Offered to Tenant’s Broker”, do not display “Tenant’s Broker Commission Fee” even if a stale value exists. Gate fee display strictly by the structure selection.

7. Additional Details tab: add the Additional Details section back to the actual listing display for Hire a Landlord’s Agent in the proper order, rendering its fields when they have values (except where “None” must still show).

8. Payment Timing for Broker Fees: replace raw stored enum output like “full_execution” with the human-readable label the user selected. Use a display mapping for all enum values.

9. Commercial Leasing Space display fix (Hire a Landlord’s Agent only): remove any logic that merges leasing-space row sets or prints all non-empty leasing-space fields. Add strict gating by leasing_space and dedupe with associative row keys so fields never print twice. If leasing_space = Single Room, display only in this order: Leasing Space, Guests are, Restrictions Include, Shared Areas Available, Maintenance and Repairs Are Handled By, Maintenance Response Time, Utilities, Common Area Maintenance, Included Storage Space, Storage Space Size, Bathroom Facilities, Approximate Room Size. If leasing_space = Entire Property, display only in this order: Leasing Space, Restrictions Include, Maintenance and Repairs Are Handled By, Maintenance Response Time, Included Storage Space, Storage Space Size, Shared Amenities Include, Building Hours, 24/7 Access Available, Zoning Allows, Space Features, Neighboring Tenants Include. Ensure Restrictions Include is pulled from the same saved field as the form and renders once. Do not modify Buyer/Seller/Tenant listing displays.

---
