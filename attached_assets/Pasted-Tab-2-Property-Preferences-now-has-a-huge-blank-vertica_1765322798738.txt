Tab 2 (Property Preferences) now has a huge blank vertical gap at the bottom for all 5 property types.

After that, Tabs 3–7 are completely empty.

Originally, this gap only happened for Income and only Income’s tabs 3–7 were blank.

This strongly suggests that the problem is in one place:

resources/views/livewire/hire-buyer-agent/buyer-agent-auction-tabs/commission-based/property-preferences.blade.php

…specifically near the bottom of the file, where the tab content closes.

Can you please do this, very specifically:

Open property-preferences.blade.php (commission-based Buyer)

Look at the last ~50–80 lines of the file.

Check for:

Any @if without a matching @endif

Any @foreach without @endforeach

Extra / missing <div> or </div> tags

Any code that was moved outside the main tab <div> when Income or limited_service logic was edited.

Compare to a known-good version

If possible, use Replit checkpoints or Git history to compare this file to the last version where:

Residential worked

Tabs 3–7 worked

Restore the same closing structure (same number of </div> and @endif lines, in the same order) and then re-apply any text changes inside the existing structure, not around it.

Add one simple debug marker at the very end of this file
At the bottom of property-preferences.blade.php, right before the final closing </div>/@endif, temporarily add:

<div style="background:#ff0; padding:4px; font-size:12px;">
    END OF PROPERTY PREFERENCES | property_type={{ $property_type }}
</div>


When I go to Buyer → Full Service → Residential / Income, Tab 2 should show this yellow “END OF PROPERTY PREFERENCES” line immediately after the last question, with no giant blank space below it.

Once that looks correct and the gap disappears, Tabs 3–7 should start rendering again.

Do NOT change the main buyer tab includes in tenant-agent-auction.blade.php again

The current problem is clearly inside this Property Preferences partial, not in the global tab logic. Please leave the @elseif($user_type === 'buyer') and includes as they were when Residential worked.

Once the bottom of Property Preferences is fixed and the structure matches the working version, the original Income issue should also be much easier to isolate (since it likely started as a mis-placed Income block in this same file).