SAFE TASK — Fix Literal Unicode Typos in Seller/Landlord Service Labels

Issue:
Some Seller and Landlord preset service labels still show literal escaped text like:
\u2019

Examples shown:
- Buyer\u2019s Agent
- Tenant\u2019s Agent

This appears inside Agent Preset service selection cards for Seller and Landlord.

Scope:
- Seller service catalog/config
- Landlord service catalog/config
- Any preset service display formatter only if needed
- Existing saved preset values only if necessary after catalog fix

Do NOT:
- Change service meanings
- Change service order
- Change selected/unselected services
- Change preset scope logic
- Change broker compensation
- Change agency agreement terms
- Change Offer Listing flows
- Change database schema

Tasks:
1. Search the codebase for literal:
   \u2019
   and:
   \u2014

2. Identify whether the bad text lives in:
   - config/seller_services_order.php
   - config/landlord_services_order.php
   - AgentPresetCatalog
   - database/preset profile_data
   - Blade rendering

3. Fix source catalog labels so apostrophes render normally:
   - Buyer’s Agent
   - Tenant’s Agent

4. If existing saved presets still contain literal escaped strings:
   - Do NOT blindly overwrite all preset data.
   - Add safe display-level decoding where preset services are rendered in the edit form.
   - If a data migration is recommended, report first before changing stored preset values.

5. Verify all service display locations:
   - Agent preset edit page
   - Hire Me preview page
   - Public agent profile / role-specific profile if applicable

Testing:
- Open Seller Residential preset edit page.
- Confirm no \u2019 appears.
- Open Landlord Residential preset edit page.
- Confirm no \u2019 appears.
- Open Seller Hire Me preview.
- Confirm no \u2019 appears.
- Open Landlord Hire Me preview.
- Confirm no \u2019 appears.
- Confirm selected services remain selected.

Return:
- Root cause
- Files changed
- Whether issue was in config, stored data, or rendering
- Whether saved preset data needed any change
- Test results