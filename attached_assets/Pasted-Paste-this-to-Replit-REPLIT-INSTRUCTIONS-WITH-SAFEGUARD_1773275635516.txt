Paste this to Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT’S AGENT ONLY — GARAGE/PARKING FEATURES “OTHER” INPUT NOT SHOWING ON EDIT

### SCOPE

Fix **only** this bug in the **Hire a Tenant’s Agent edit flow**:

* **Garage/Parking Features: the “Other” custom input does not show on edit**

The screenshot confirms this is happening on a **Tenant edit URL**, not create.

Example route pattern:

`/hire/agent/auction/edit/{id}/tenant`

So this must be fixed in the **actual active Tenant edit flow files**, not just the create flow.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Tenant’s Agent edit flow.**
2. **Do not edit Buyer, Seller, or Landlord flows.**
3. **Do not rename, delete, or migrate any database columns.**
4. **Do not change labels, tooltips, placeholders, option text, or business logic.**
5. **Do not refactor the whole component architecture.**
6. **Do not replace Select2, Livewire, or existing shared JS systems.**
7. **Do not touch unrelated fields or tabs.**
8. **Do not make broad cleanup edits.**
9. **Keep this surgical and limited to Garage/Parking Features “Other” visibility on Tenant edit.**
10. **If shared files are involved, isolate the fix so it only affects the Tenant edit flow unless absolutely necessary.**

---

# IMPORTANT — VERIFY THE ACTUAL ACTIVE EDIT FILES FIRST

Before editing anything, confirm the exact active files used by:

`/hire/agent/auction/edit/{id}/tenant`

Identify:

1. the exact Livewire component used for the Tenant edit route
2. the exact blade view used by that route
3. the exact partial containing **Garage/Parking Features**
4. the exact JS/init code responsible for Select2 initialization and “Other” field visibility for that field in edit mode

Do **not** assume the create flow files are the same as the edit flow.

Do **not** fix only the create flow.

---

# CURRENT ISSUE

In the **Tenant edit flow**, when **Garage/Parking Features** includes **Other**, the custom input box does **not** show.

The screenshot shows:

* **Other** is visibly selected inside the field
* but the related custom input box is missing

That means the selected value exists, but the **conditional visibility logic for the custom “Other” input is not running correctly on edit load and/or change**.

---

# REQUIRED BEHAVIOR

For **Garage/Parking Features** in the **Tenant edit flow**:

### On edit load

* If **Other** was already selected and a custom value exists, the custom input must automatically appear
* The saved custom value must populate correctly

### On user change

* If the user selects **Other** while editing, the custom input must appear immediately
* If the user removes **Other**, the custom input should hide appropriately

### Persistence

The custom input/value must persist after:

* save
* tab change
* refresh
* reopen edit
* listing display/output if applicable

---

# ROOT-CAUSE DIRECTION

This is likely one of these issues:

* the “Other” visibility logic only runs on manual `change`, not on initial edit hydration
* the Select2 field is hydrated with `Other`, but the dependent input visibility is not re-triggered
* the wrong selector or field name is being checked in the edit flow
* Livewire state contains `Other`, but the JS show/hide logic is not reading it correctly on edit load
* the custom input exists in markup but is hidden due to stale conditional state
* the edit flow uses a different partial/init path than create, and only create was handled

Fix the **underlying edit-mode sync/visibility logic**, not just the symptom.

---

# REQUIRED IMPLEMENTATION APPROACH

## 1) WORK ONLY ON THE ACTUAL TENANT EDIT PATH

Because I am testing on:

`/hire/agent/auction/edit/{id}/tenant`

you must verify the actual active edit component/blade/partial first.

Do not assume the create files are correct.

---

## 2) FIX “OTHER” VISIBILITY ON BOTH LOAD AND CHANGE

For **Garage/Parking Features**, the “Other” input visibility must run in both cases:

1. **on initial edit load / hydration**
2. **when the user changes the field**

Do not rely only on a manual `change` event.

---

## 3) PRESERVE SAVED VALUE

If a custom “Other” value is already stored:

* show the input automatically
* populate the saved value
* do not clear it on tab switch or refresh

---

## 4) DO NOT BREAK THE WORKING SELECT2 FIELD

The selected chips/tags already appear in the UI.
Do **not** break the existing multi-select display.

Only fix the missing dependent custom input behavior.

---

# QA TESTING REQUIRED

After fixing, test in the **actual Tenant edit flow**.

## Test 1 — Existing saved “Other”

1. Open a Tenant edit URL like `/hire/agent/auction/edit/{id}/tenant`
2. Go to the tab containing Garage/Parking Features
3. Confirm **Other** is selected
4. Confirm the custom input appears automatically
5. Confirm the saved custom value is populated

## Test 2 — New selection

1. Open Tenant edit
2. Select **Other** in Garage/Parking Features
3. Confirm the custom input appears immediately
4. Enter a value
5. Move to another tab
6. Return to the tab
7. Confirm the value remains

## Test 3 — Refresh/reopen

1. Save the edit
2. Refresh or reopen the edit route
3. Confirm **Other** still shows the custom input with saved value

---

# WHAT TO REPORT BACK

Report back with:

1. the exact active Livewire component for `/hire/agent/auction/edit/{id}/tenant`
2. the exact blade and partial containing Garage/Parking Features
3. the exact file(s) changed
4. the root cause of the missing “Other” input on edit
5. how the fix was implemented
6. confirmation that it was tested on the real Tenant edit route, not just create
7. confirmation that Buyer, Seller, and Landlord flows were not changed

---

# FINAL REMINDER

This is a **surgical Tenant edit-flow bug fix only**.

Fix only:

* Garage/Parking Features “Other” input not showing on edit

Do not do unrelated edits or broad refactors.

---
