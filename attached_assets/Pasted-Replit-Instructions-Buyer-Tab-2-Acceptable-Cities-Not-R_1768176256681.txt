Replit Instructions — Buyer Tab 2 “Acceptable Cities” Not Required + Fix Disabled Submit Button (Hire a Buyer’s Agent)
Applies to

Hire a Buyer’s Agent → Tab 2 → Acceptable Cities

Hire a Buyer’s Agent → Submit button enable/disable logic (all tabs)

✅ SAFEGUARDS (MUST FOLLOW — NON-NEGOTIABLE)

DO NOT:

Delete, rename, or repurpose any database tables/columns.

Rename any existing field keys/names (front-end OR backend).

Change any UI layout, styling, labels, tooltips, icons, or placeholder text.

Change which other fields are required/optional.

Remove validation globally or loosen unrelated validations.

Modify Tenant/Seller/Landlord flows in this task.

Add hardcoded “always enabled” logic to the submit button.

ALLOWED:

Make Buyer Tab 2 Acceptable Cities optional (frontend + backend validation only for that field).

Fix Buyer-specific validity checks so the submit button enables when valid.

Add temporary console logging for form validity debugging and remove after QA.

1) MAKE “ACCEPTABLE CITIES” OPTIONAL (BUYER TAB 2)
Expected Behavior

User should be able to submit a Buyer listing without selecting any Acceptable Cities.

If Acceptable Cities is blank, the system should still submit normally (and not throw validation errors).

Required Fixes
1.1 Frontend: remove “required” enforcement for Acceptable Cities only

In the Buyer Tab 2 form:

Ensure the Acceptable Cities input does NOT have:

HTML required

validation rule requiring length > 0

“must be selected” checks in checkFormValidity()

✅ It can remain a multi-select field — just optional.

1.2 Backend: remove validation requirement for Acceptable Cities only

In the Buyer listing submit endpoint/request validation:

Change Acceptable Cities rule from required to optional/nullable.

If it’s an array field, allow:

missing key

empty array

Do not change any other validation rules.

1.3 Ensure “Acceptable Counties” auto-mapping does not force Cities required

If your code auto-populates counties from cities:

If cities is empty → counties should remain empty and that should be allowed.

2) FIX: BUYER SUBMIT BUTTON STAYS DISABLED EVEN WHEN FORM IS VALID
Symptoms

User fills all required fields, but Submit remains disabled and cannot be clicked.

Expected Behavior

Submit button becomes clickable when all required fields across tabs are valid.

Required Fixes
2.1 Ensure checkFormValidity() evaluates the correct required fields (and excludes Acceptable Cities)

Update Buyer’s checkFormValidity() logic so:

It does not treat Acceptable Cities as required.

It does not require hidden/conditional fields unless they are currently visible AND required by the user’s selections.

Common failure: validity check includes fields that are:

hidden by conditional logic

not present in DOM

disabled inputs

optional but still checked

✅ The validity check should only include:

visible required inputs

required selects currently active

required fields for the listing type/property type selected

2.2 Toggle BOTH disabled attribute AND “disabled” CSS class (Buyer only)

Buyer submit button must remove:

disabled attribute

.disabled class (if your CSS prevents clicks)

Tenant is your reference pattern:

when valid → remove attribute + remove class

when invalid → add attribute + add class

2.3 Make sure the Buyer submit handler isn’t blocked by an unmet “agree/acknowledge” checkbox (if present)

If Buyer flow has a required acknowledgement checkbox (Terms/Consent):

confirm it’s included in validity logic

confirm its state persists on tab change

confirm the selector points to the correct checkbox ID/name

This is a very common reason the button stays disabled even when “everything looks filled out.”

2.4 Add TEMP debug logs to identify the exact failing field

Temporarily log which field fails validation:

Print a list of required fields being checked

Print which ones are invalid/empty

Remove logs after fix confirmed

Example output Replit should produce during QA:

“Buyer validity check failing: purchase_price_min missing”

“Buyer validity check failing: acceptableCities incorrectly required” (should NOT happen after fix)

✅ QA TESTS REPLIT MUST RUN (PASS/FAIL)
Test A — Acceptable Cities not required

Create Buyer listing

Leave Acceptable Cities blank

Fill all other required fields

Submit should be enabled and should submit successfully ✅

Test B — Submit enables reliably

Fill all required fields including any required checkboxes

Confirm Submit becomes clickable ✅

Refresh → load draft → confirm Submit still becomes clickable when valid ✅