Fix the client dashboard role count badges so they match the actual default count shown when the user clicks that role tab.

Current bug:
- The red sidebar counts for Tenant / Landlord / Buyer / Seller do not match the count shown on the destination page
- Example: sidebar Seller badge shows 58, but the Seller page opens with “Live (43)”
- This creates a mismatch and makes the dashboard feel inaccurate

Required behavior:
1. Audit how each sidebar red badge count is currently calculated for:
   - Hire Tenant’s Agent
   - Hire Landlord’s Agent
   - Hire Buyer’s Agent
   - Hire Seller’s Agent

2. Audit how the destination page dropdown count is calculated when the page first loads, especially the default “Live (X)” count.

3. Make the sidebar red badge use the SAME counting rule as the default page view the user sees after clicking that role tab.
   - If the destination page defaults to Live listings, then the sidebar badge must also count Live listings only.
   - Do not show total/all-status counts in the sidebar if the destination page opens to Live only.

4. Apply this consistently across:
   - Tenant
   - Landlord
   - Buyer
   - Seller

5. Do not change routing, listing creation flows, bid logic, or database schema.
   This is a count consistency fix only.

CRITICAL SAFEGUARD:
- Do not modify Hire a Tenant’s Agent, Hire a Landlord’s Agent, Hire a Buyer’s Agent, or Hire a Seller’s Agent creation flows
- Do not modify bid submission, counter, accept/reject, match score, or Accepted Bid Summary logic
- Do not rename routes, controllers, tables, or status fields unless absolutely necessary
- This task is strictly to make the dashboard sidebar badge counts match the default page count the user sees

Acceptance criteria:
- sidebar role count matches the default page count after click
- Seller badge matches Seller default page count
- Tenant badge matches Tenant default page count
- Landlord badge matches Landlord default page count
- Buyer badge matches Buyer default page count
- no logic outside count calculation/display is changed