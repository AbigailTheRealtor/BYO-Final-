Below are **copy-paste Replit instructions with safeguards** for **Hire a Landlord’s Agent** (Commercial + Residential where noted). This is **display-logic only** except where you explicitly said **Edit Submit wouldn’t go through** (that part is a minimal non-destructive fix for the submit blockage).

---

## Replit Instructions With Safeguards

### Hire a Landlord’s Agent — Fix listing display + tab missing + payment timing label + edit submit not working

**Do NOT rename or delete any database columns.**
**Do NOT delete or modify any existing listings, drafts, or agent bids.**
**Do NOT change labels, tooltips, or option values.**
**Only modify display logic for Hire a Landlord’s Agent listings, except the one “submit not going through” fix (non-destructive).**

---

# A) Digital Photo Enhancements section placement + visibility (Commercial + Residential)

### Problem

* “Digital Photo Enhancements” sub-questions should appear **directly under** **Provide digital photo enhancement** (same format as Seller listings).
* It’s currently **showing only on Residential**, and **in the wrong placement**.
* Needs to display correctly on **Commercial too**.

### Required change

1. In the **Hire a Landlord’s Agent listing display template**, locate the row builder/renderer for **Marketing / Media / Photography** rows.
2. Find the parent field (example key names may vary):

   * `provide_digital_photo_enhancements` (or similar boolean/enum)
3. Immediately after rendering the parent row:

   * if parent indicates “Yes / Included”, render the **Digital Photo Enhancements** sub-rows **right below** it, before moving on to the next category.
4. **Mirror the exact sub-question set and order from Seller listings** (same options) without changing any option values.

### Safeguards

* Do not duplicate the sub-rows in other sections.
* Do not show sub-rows if parent is not selected/true.
* Apply to **Commercial and Residential** landlord listings (only this section).

---

# B) Additional Details tab missing on Actual Listing

### Problem

* “Additional Details” tab content exists in the workflow but is **not displayed** on the actual listing.

### Required change

1. In the **Hire a Landlord’s Agent listing display view**, confirm the tab navigation includes an entry for **Additional Details**.
2. Confirm the corresponding tab content container is present and is being passed data.
3. If the tab exists but is empty:

   * ensure the template includes the correct partial/section for Additional Details (the same one used by Seller/Tenant/Buyer patterns).
4. Ensure it appears for **both Residential and Commercial** landlord listings.

### Safeguards

* Do not change tab names/labels.
* Do not move any other tabs.
* Only add back the missing tab/section rendering.

---

# C) Edit Listing “Submit” wouldn’t go through (minimal non-destructive fix)

### Likely cause (based on your recent patterns)

* A JS/Livewire loop or a Select2/hidden-tab init issue is preventing the form submit from completing, or validation is failing silently.

### Required change (safe + minimal)

1. Open the landlord edit blade (the one actually used by the route for landlord edits).
2. Ensure the `<form>` submit is not blocked by JS listeners that call `event.preventDefault()` without then calling Livewire update.
3. If there are Select2 fields:

   * confirm all are wrapped in `wire:ignore`
   * confirm init functions are guarded (do not re-init if already select2-hidden-accessible)
4. Add a lightweight submit debug (temporary) to surface why it fails:

   * If validation fails, ensure errors render at top of form.
   * If JS throws an error, fix the console error (most common: selector not found / double init).

### Safeguards

* Do not refactor the entire Select2 manager.
* Do not add polling.
* Keep changes limited to removing the block / surfacing validation errors.
* Remove any temporary debug logging after confirming submit works.

---

# D) Payment Timing for Broker Fees displays `full_execution` instead of the selected label

### Problem

* On actual listing, the saved value `full_execution` is being printed raw.
* It must display the human label:

  * **“Full amount upon execution of lease, sales contract, or other transfer agreement”**

### Required change

1. In the **Hire a Landlord’s Agent listing display renderer** (where broker fee timing is shown), replace raw output with a mapping lookup:

   * If stored value is `full_execution`, render the label text exactly as your option label.
2. Implement mapping for this field using the same options list used by the form (do not invent new labels).

### Safeguards

* Do not change the stored DB value.
* Do not change the dropdown option values.
* Only change how the display prints it.

---

# E) Commercial-only Leasing Space display: strict gating + exact field lists + dedupe (CORE REQUIREMENT)

### Problem

* Current row builder merges leasing-space row sets or prints all non-empty leasing-space fields.
* Needs strict gating based on `leasing_space` with **exact allowed fields and order**, and **no duplicates**.

### Required change (display row builder only)

**In the Hire a Landlord’s Agent listing display row builder:**

1. Remove any logic that:

   * merges leasing-space sets, OR
   * iterates “all non-empty leasing fields” generically for Commercial leasing space
2. Replace with strict gating based on `leasing_space`.
3. Implement deduping by storing rows in an associative array keyed by stable field keys so each field renders only once.
4. Ensure **Restrictions Include** is pulled from the same saved field used by the form and renders only once.

---

## E1) If `leasing_space = Single Room` render ONLY these fields in this exact order

1. Leasing Space
2. Guests are
3. Restrictions Include
4. Shared Areas Available
5. Maintenance and Repairs Are Handled By
6. Maintenance Response Time
7. Utilities
8. Common Area Maintenance
9. Included Storage Space
10. Storage Space Size
11. Bathroom Facilities
12. Approximate Room Size

**Do NOT display** for Single Room:

* Shared Amenities Include
* Building Hours
* 24/7 Access Available
* Zoning Allows
* Space Features
* Neighboring Tenants Include

---

## E2) If `leasing_space = Entire Property` render ONLY these fields in this exact order

1. Leasing Space
2. Restrictions Include
3. Maintenance and Repairs Are Handled By
4. Maintenance Response Time
5. Included Storage Space
6. Storage Space Size
7. Shared Amenities Include
8. Building Hours
9. 24/7 Access Available
10. Zoning Allows
11. Space Features
12. Neighboring Tenants Include

**Do NOT display** for Entire Property:

* Guests are
* Shared Areas Available
* Utilities
* Common Area Maintenance
* Bathroom Facilities
* Approximate Room Size

---

## E3) Dedupe implementation requirement

In the row builder, store each row as:

* key: stable field key (example: `leasing_space`, `guests_are`, `restrictions_include`, etc.)
* value: row object/array (label + rendered value)

Before adding:

* `if (!isset($rows[$key])) { $rows[$key] = $row; }`

Then output rows in the required order by iterating your explicit ordered key list and printing only keys that exist.

### Safeguards

* Do not modify how values are saved.
* Do not alter form logic.
* Do not apply this to other listing types (Seller/Buyer/Tenant).
* Apply to **Hire a Landlord’s Agent listings (Commercial only)** and only inside the leasing-space display block.

---

# Verification checklist (must pass)

1. **Digital Photo Enhancements** sub-questions appear directly under the parent line on **Commercial and Residential** landlord listings.
2. **Additional Details** tab appears on the actual landlord listing and renders its content.
3. **Edit Submit** works (no hanging, no silent failure).
4. Payment timing prints the label (not `full_execution`).
5. Commercial leasing-space display:

   * Single Room shows only the Single Room list in exact order
   * Entire Property shows only the Entire Property list in exact order
   * No duplicates
   * Restrictions Include appears once and is correct

---

## Tight prompt to paste into Replit AI (so it doesn’t wander)

“Implement the above Hire a Landlord’s Agent fixes with safeguards. Do not rename/delete DB columns. Do not modify listings/drafts/bids. Do not change labels/tooltips/option values. Only modify Hire a Landlord’s Agent listing display logic, except for a minimal fix so landlord edit submit works (remove JS block/surface validation error). For Commercial leasing_space display, remove merged/non-empty generic printing and implement strict gating with exact field lists and associative-array dedupe keyed by stable field keys. Fix broker fee payment timing display mapping for full_execution.”

If you paste **one screenshot or the route path** you use to view a landlord listing + the one you use to edit it, I can tell you exactly which landlord display blade and which landlord edit blade Replit should touch (so it edits the right files the first time).
