TASK: Fix the live Seller Agent bid flow so Seller listings load the correct Seller Broker Compensation & Agency Agreement Terms and Seller Offered Services instead of incorrectly loading Tenant bid content.

PRIMARY ISSUE
A Seller listing is incorrectly connected to the Tenant bid configuration. Because of that:
- the Broker Compensation & Agency Agreement Terms tab shows Tenant fields/text instead of Seller
- the Offered Services tab does not show the correct Seller services structure
- the bid form is not using the current Seller listing format
- the bid form is not prefilling from the actual Seller listing data correctly

GOAL
When an Agent bids on a Seller listing, the bid form must:
1) Use the CURRENT Seller Broker Compensation & Agency Agreement Terms format
2) Use the CURRENT Seller Offered Services format
3) Match the actual Seller listing type:
   - Residential
   - Income
   - Commercial
   - Business
   - Vacant Land
4) Prefill from the exact values the Seller/listing creator entered on the listing
5) Behave like the working Tenant bid flow in terms of prefilling from the listing
6) Stop showing any Tenant wording, Tenant fields, or Tenant services on Seller bids

CRITICAL SAFEGUARDS
- First trace the ACTUAL live Seller bid route and files before editing anything
- Do NOT guess
- Do NOT edit old/orphaned Seller files
- Do NOT modify Tenant bid behavior except to reference it as the working example
- Do NOT rename database columns
- Do NOT delete existing logic until replacement is confirmed working
- Do NOT change Buyer or Landlord bid flows unless a shared file absolutely requires a role-safe fix
- If shared code is used, make the fix role-safe so Seller uses Seller mappings only
- Do NOT hardcode outdated Seller forms
- Use the current Seller listing create/edit implementation as the source of truth
- Keep this scoped to Seller agent bids only
- If the live route resolves somewhere unexpected, STOP and report the exact route/component/view chain before making changes

STEP 1 — TRACE THE LIVE SELLER BID ROUTE
Starting from the live Seller bid URL, identify the actual:
- route definition
- controller or Livewire component
- blade/view file
- Broker Compensation tab partial/component
- Offered Services tab partial/component

Do not edit until the exact live files are confirmed.

IMPORTANT:
The current symptom strongly suggests the Seller bid route is incorrectly reusing Tenant bid config/partials/data mapping.
Confirm exactly where that wrong handoff occurs.

STEP 2 — FIND THE CURRENT SELLER SOURCE OF TRUTH
Find the actual live Seller listing create/edit files that define the CURRENT Seller:
- Broker Compensation & Agency Agreement Terms
- Offered Services

These live Seller listing files are the source of truth for:
- field set
- field labels
- field order
- placeholders
- tooltips
- conditional logic
- property-type-specific structure

Do NOT use any outdated Seller bid-only copy if the live Seller listing version already exists.

STEP 3 — FIX ROLE MAPPING: SELLER MUST LOAD SELLER, NOT TENANT
Repair the role mapping so Seller bid routes/components load Seller configuration only.

Expected behavior:
- Seller listing → Seller bid mapping
- Buyer listing → Buyer bid mapping
- Landlord listing → Landlord bid mapping
- Tenant listing → Tenant bid mapping

There should be no Seller path that falls through to Tenant config, Tenant labels, Tenant service sets, or Tenant compensation fields.

Search for:
- wrong conditional branching
- shared partial includes using tenant by default
- fallback mappings that default to tenant
- wrong role_type/listing_type detection
- wrong model attribute being checked
- wrong tab include path
- wrong config key
- wrong property-type resolver

Fix the exact incorrect handoff, not just the visible text.

STEP 4 — MAKE SELLER BID REUSE CURRENT SELLER COMPENSATION FORMAT
Update the live Seller bid form so Broker Compensation & Agency Agreement Terms uses the same current Seller format as Seller listing creation/edit.

It must support the correct Seller property type version for:
- Residential
- Income
- Commercial
- Business
- Vacant Land

If some Seller property types intentionally share the same schema, that is acceptable only if it matches the current Seller listing format exactly.

The bid form must NOT show Tenant field names such as:
- Tenant’s Broker Compensation Structure
- Tenant’s Broker Lease Fee
- Tenant Agency Agreement Timeframe
or any other Tenant-specific wording.

It must show the correct Seller wording and current Seller field structure.

STEP 5 — MAKE SELLER BID REUSE CURRENT SELLER OFFERED SERVICES FORMAT
Update the live Seller bid “Offered Services” tab so it uses the same current Seller offered-services structure as Seller listing creation/edit.

Requirements:
- must load the correct Seller services layout for Residential, Income, Commercial, Business, and Vacant Land
- must render the latest service groups and service items
- must render in the same order as Seller listing creation
- must support any “Other – Specify additional services as needed” behavior currently used in Seller create/edit
- must not show Tenant service language or Tenant-specific intro text
- must pre-check/prefill based on what the Seller selected on the listing when appropriate for first-load bid hydration

If the current issue is caused by the Seller services tab incorrectly including the Tenant services partial, replace it with the correct Seller path.

STEP 6 — PREFILL FROM THE ACTUAL SELLER LISTING
For a NEW Seller bid, prefill from the actual Seller listing record being bid on.

Prefill priority must be:
1) Existing saved bid values, if the agent is editing/resuming a draft Seller bid
2) Otherwise the Seller listing’s saved Broker Compensation / Agency Agreement / Offered Services values
3) Otherwise blank

Do NOT prefill from:
- agent default profile
- tenant defaults
- generic bid defaults
- old seller defaults
- wrong property type defaults

This should behave like the working Tenant bid flow, where the listing data appears automatically on first bid load.

STEP 7 — VERIFY FIELD-LEVEL MAPPING
Confirm every current Seller field used in Seller listing creation has a matching:
- load path
- bind path
- save path
- edit path
for Seller agent bids

This includes:
- selects
- checkboxes
- radios
- nested arrays
- json-backed fields
- conditional child inputs
- textareas
- “Other” custom values
- property-type-specific service groups

If Seller listing creation was updated recently and the bid form still points to old keys, update Seller bid to the latest keys.

STEP 8 — MATCH THE CURRENT SELLER UI/STRUCTURE
The Seller bid form must visually and structurally match the current Seller listing format for:
- heading text
- field labels
- tooltips
- placeholders
- field order
- group headings
- conditional visibility
- service-section structure

The goal is that the Seller listing form and Seller bid form look like the same framework, with the bid form prefilling from the listing.

STEP 9 — DO NOT BREAK SAVE AS DEFAULT PROFILE
Do not let this Seller compensation/services fix interfere with the “Save as Default Profile” behavior.

Default profile should remain limited to the intended agent profile sections only and must not override Seller listing-driven compensation/services prefills.

If shared code touches this area, preserve the rule:
- Agent profile defaults should not replace listing-driven Broker Compensation / Agency Agreement / Offered Services in Seller bids

STEP 10 — TESTING REQUIREMENTS
Run these exact tests after coding:

TEST A — Seller Residential
1) Create or use a Seller Residential listing with current broker compensation fields and offered services filled in
2) Open the live Seller bid route
3) Confirm Broker Compensation shows current Seller Residential format
4) Confirm Offered Services shows current Seller Residential format
5) Confirm values are prefilled exactly from the listing
6) Confirm no Tenant wording appears anywhere

TEST B — Seller Income
Repeat for Income

TEST C — Seller Commercial
Repeat for Commercial

TEST D — Seller Business
Repeat for Business

TEST E — Seller Vacant Land
Repeat for Vacant Land

TEST F — Existing Seller Bid Draft
1) Start a Seller bid
2) Change compensation/services values
3) Save draft/progress
4) Reopen the same bid
5) Confirm bid-specific values load first and are not overwritten by original listing values

TEST G — No Regression
1) Open a Tenant bid and confirm Tenant compensation/services still prefill correctly
2) Confirm Buyer and Landlord bid flows were not changed unintentionally

SUCCESS CRITERIA
- Seller bid route now loads Seller mappings instead of Tenant
- Seller Broker Compensation & Agency Agreement Terms matches the current Seller listing format
- Seller Offered Services matches the current Seller listing format
- Correct Seller property-type-specific version loads for Residential, Income, Commercial, Business, and Vacant Land
- Listing-entered values are prefilled exactly on first load
- Existing Seller bid draft values take precedence when editing
- No Tenant wording/fields/services appear on Seller bids
- No regression to Tenant, Buyer, or Landlord bid flows

REQUIRED REPORT BACK
After completion, report:
1) Exact live Seller bid route traced
2) Exact live files changed
3) Exact Seller listing create/edit files used as the source of truth
4) Where the wrong Tenant mapping was found
5) How the Seller prefill priority was implemented
6) Which Seller property types were tested successfully
7) Confirmation that Tenant/Buyer/Landlord flows were not changed