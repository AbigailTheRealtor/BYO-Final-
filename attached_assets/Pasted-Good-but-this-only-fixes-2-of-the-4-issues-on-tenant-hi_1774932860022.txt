Good, but this only fixes 2 of the 4 issues on `/tenant/hire/agent/auction/view/140`.

The fixes for:

1. counting `Example 1` and `Example 2` in Services Match, and
2. showing the current commission structure label

are good.

But the following are still unresolved and must be fixed on this same exact bid `/tenant/hire/agent/auction/view/140`:

### Remaining issue 1 — Agent View Bid still shows the wrong Offered Services

The bid detail page must show the exact saved checked service set from the bid form. I still need you to verify and fix the mismatch between:

* bid form checked services at save time
* saved services payload
* services rendered on Agent View Bid

Do not assume this is fixed just because the denominator changed from 29 to 31. I need the actual displayed Offered Services on the bid detail page to match the exact saved checked services.

### Remaining issue 2 — Terms Match is still incorrect

I still need the exact logical term list being scored for this bid and the exact term causing:

* `Matched Original: 14/15`
* `Changed from Baseline: 1`

For this exact bid `/tenant/hire/agent/auction/view/140`, output the full logical Broker Compensation & Agency Agreement Terms comparison list, including:

* logical term key
* baseline normalized value
* current normalized value
* match/mismatch result
* why it was counted

Do not dump only raw payload arrays. I need the actual logical scoring list.

### Required outcome

After the next fix, verify on `/tenant/hire/agent/auction/view/140` that:

* Agent View Bid shows the exact saved checked services
* Terms Match only counts the correct logical terms
* if no Broker Compensation & Agency Agreement Terms were changed from the Tenant’s original request, Terms Match shows full match and `Changed from Baseline: 0`

### Safeguards

* Tenant only
* no destructive DB changes
* no schema changes
* no changing field labels or service wording
* preserve the custom additional-services fix
* preserve the commission-structure display fix
* do not modify Buyer, Seller, or Landlord unless a clearly shared helper is the direct root cause and can be safely updated
