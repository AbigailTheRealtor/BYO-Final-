Hire a Tenant’s Agent → Actual Listing Display (Residential + Commercial)

This ticket is DISPLAY-ONLY.
The goal is to make the Actual Listing Display mirror the Listing Creation layout and order, using the same stored data, with no schema or form changes.

0) NON-DESTRUCTIVE SAFEGUARDS (MANDATORY)

DO NOT:

Modify listing creation forms, tabs, fields, logic, validations, or options

Modify database schema, saved keys, enums, or stored values

Create new fields for Listing Status or Garage/Parking

DO:

Update only display rendering (labels, order, placement, visibility)

Render fields that already exist in saved data but are missing from display

Keep Residential vs Commercial logic intact

1) CREATION → DISPLAY ALIGNMENT RULE (CRITICAL)

The Actual Listing Display must mirror the Listing Creation layout and field order.

If a field appears in a certain section and position during listing creation, it must appear in the same section and relative position on the Actual Listing Display.

This rule applies to:

Section placement

Field order

Conditional visibility

A) LISTING STATUS (RESIDENTIAL + COMMERCIAL)
2) Listing Status — EXACT LOCATION & ORDER (NO INTERPRETATION)
Creation reference

On listing creation, Listing Status appears:

Under the Listing Details tab

As the FIRST field

Above Listing Title

Required display behavior

On the Actual Listing Display, render Listing Status:

Inside the Listing Details section

As the FIRST line item

Above Listing Title

Before all other Listing Details fields

Exact format
Listing Status: Active
Listing Title: [Listing Title]

Allowed display values (must match exactly)

Active

Pending

Hired Agent

Expired

Logic rules

Use the existing stored listing status

Do NOT create a new field

Do NOT hardcode values

If internal values differ, map them cleanly:

active → Active

pending → Pending

hired / hired_agent → Hired Agent

expired → Expired

B) COMMERCIAL ONLY — PROPERTY PREFERENCES
3) Garage / Parking Fields — EXACT PLACEMENT (CRITICAL)
Creation reference

On Commercial listing creation, the following fields appear:

Under Property Preferences

Directly BELOW:

Minimum Net Leasable Sqft Needed

Required display behavior

On the Actual Listing Display (Commercial only), under Property Preferences, insert the following IMMEDIATELY AFTER
Minimum Net Leasable Sqft Needed:

Garage/Parking Features Needed:

Garage/Parking Features:

❗ These fields must NOT:

Appear in Leasing Terms

Appear at the bottom of Property Preferences

Be placed elsewhere on the page

They must appear directly under Minimum Net Leasable Sqft Needed, matching listing creation order.

Rendering rules

Display the stored values exactly

If multi-select, display cleanly (comma-separated or badges per existing UI)

If empty/null, hide the row (match current UI behavior)

Use labels exactly as written above

C) REQUIRED DEBUG & VERIFICATION STEP (NON-OPTIONAL)
4) Verify correct view is being edited

Before implementing changes:

Confirm the exact route/controller used to render the listing page shown in my screenshot

Confirm the exact Blade/view file used

Temporarily add a comment at the top of the file:

<!-- DEBUG: Hire Tenant Actual Listing Display -->


Verify the comment appears in page source

❗ Do not proceed unless this confirms the correct file is being edited.

D) ACCEPTANCE TESTS (MUST PASS ALL)

This ticket cannot be closed unless all items below are true:

Listing Status

Listing Status appears under Listing Details

Listing Status is the first field

Listing Status appears above Listing Title

Works for Residential

Works for Commercial

Commercial Garage/Parking

Garage/Parking Features Needed appears under Property Preferences

Appears directly under Minimum Net Leasable Sqft Needed

Garage/Parking Features appears immediately after

Values render correctly from saved data

Proof required

Screenshot of Residential listing showing Listing Status placement

Screenshot of Commercial listing showing Listing Status + both Garage/Parking fields

❌ COMMON FAILURES TO AVOID

Placing Listing Status only in sidebar or as a badge

Guessing field keys instead of verifying stored data

Editing a Blade file that is not actually rendered

Claiming “implemented” without UI proof

🔐 FINAL NOTE

This is a mirror-and-restore task, not a redesign.
The Actual Listing Display must stay in lockstep with Listing Creation.