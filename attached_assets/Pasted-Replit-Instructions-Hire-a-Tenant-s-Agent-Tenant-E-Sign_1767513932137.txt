Replit Instructions — Hire a Tenant’s Agent → “Tenant: E-Sign Acknowledgement” Panel (Bid Summary)

Applies to: Hire a Tenant’s Agent listings ONLY → Accepted Bid Summary → user clicks “Tenant: E-Sign Acknowledgement” (right-side signature panel)
Goal: (1) Update the IP label text, (2) enforce checkbox-required behavior, (3) store checkbox + audit fields on submit (without changing schema/structure).

SAFEGUARDS (MUST FOLLOW — DO NOT VIOLATE)

DO NOT:

delete, rename, or alter any database tables/columns/migrations/models

change any route names, controller names, or existing method signatures

change the Accepted Bid Summary PDF layout, HTML template structure, or CSS styling outside the e-sign panel

change any tab flows or other listing types (Buyer/Seller/Landlord)

refactor/reformat unrelated code, or “clean up” files not involved in this screen

change labels or formatting anywhere else in the platform

ONLY edit:

the Tenant E-Sign Acknowledgement panel’s label text for IP

the checkbox required logic + submit button disabled state

the server-side submit handler only to persist audit fields that already exist (or store them in the same existing place you’re already storing the signature record)

If a required field/table does not exist, do not add schema. Instead, store the values in the existing “acknowledgement/signature” record structure already used.

1) UI COPY CHANGE — IP Label Text (NO LOGIC CHANGE)

Where: The Tenant E-Sign Acknowledgement form (right-side panel)
Change ONLY the label text:

From: IP Address

To: IP Address (Captured at Signing)

✅ Do not change the input value or source.
✅ Do not change styling; label text only.

2) REQUIRE CHECKBOX TO SUBMIT (BUTTON DISABLED UNTIL CHECKED)

Where: Same Tenant E-Sign Acknowledgement form

Required behavior:

The “Tenant: E-Sign Acknowledgement” submit button must be disabled by default

It becomes enabled only when the checkbox is checked:

“I confirm that I have reviewed the Accepted Bid Summary and acknowledge its contents.”

If the user clicks submit without the checkbox (edge case / dev tools), server must reject the submission with a clean validation error message:

“You must confirm you reviewed the Accepted Bid Summary before signing.”

Implementation constraints:

Use the existing front-end framework already used in this screen (do not rewrite it)

Use minimal changes:

add required attribute to checkbox (if applicable)

add simple JS binding or framework binding to toggle disabled state

Do not modify form layout, spacing, icons, fonts, or colors.

3) ON SUBMIT — PERSIST AUDIT FIELDS (NO NEW DB FIELDS)

Where: The existing submit handler that saves “Tenant acknowledgement/signature” for this Accepted Bid Summary.

Must store (wherever you currently store signature/ack logs):

typed_name (what the user types)

role = “Tenant” (already displayed)

signed_at timestamp (server-side, not client-side)

timezone (use user’s browser timezone string, e.g. America/New_York)

ip_address (server request IP, public IP if possible; if behind proxy use the standard forwarded header you already support)

checkbox_confirmed = true

✅ No new schema. Save these only in existing signature/ack structures.
✅ Use server time as the source of truth for signed_at.
✅ Timezone can come from the browser but should be validated/sanitized.

4) TIMEZONE MUST USE USER DEVICE TIMEZONE (NOT HARDCODED)

Problem: Timezone shows as ET regardless of user device.

Fix (minimal, UI + submit payload):

In the front-end, detect timezone via:

Intl.DateTimeFormat().resolvedOptions().timeZone

Display that string in the “Your Timezone” field (read-only)

Submit it with the acknowledgement form payload so it can be saved into the audit record

✅ If timezone is unavailable, fallback to “Unknown” (do not guess).

5) IP ADDRESS DISPLAY RULES (DO NOT REQUIRE USER INPUT)

IP is NOT typed by the user. It is auto-captured.

Display rules:

Show the IP value currently displayed only if it is a real user IP

If your environment sometimes produces an internal/private IP (ex: 10.x.x.x, 192.168.x.x, 172.16-31.x.x):

Display: Captured on file (internal routing) OR Will be captured at signing

But still store the best available IP server-side

✅ Do not block signing if IP is private/blank.
✅ IP is helpful evidence but not required to make an acknowledgement enforceable.

6) CONFIRMATION / SUCCESS UX (MINIMAL)

After successful Tenant acknowledgement:

Update status at top of Accepted Bid Summary page:

Tenant Acknowledgement: Completed

Ensure Agent can see status change immediately (page refresh is fine)

Do not change notification systems in this task (only status change)

QA TESTS (MUST RUN)

Checkbox gating

Load Tenant E-Sign page → submit button is disabled

Check box → button enables

Uncheck → button disables

Server-side validation

Attempt submit without checkbox (devtools) → server rejects with message

Timezone correctness

Change your OS/browser timezone → page reflects it

Submit → saved record reflects same timezone

IP correctness

Confirm stored IP matches request IP (or forwarded IP if you already support it)

If private/internal IP appears in UI, ensure it does not block signing

No regressions

Accepted Bid Summary PDF layout unchanged

No changes to other listing types

Notes (DO NOT BUILD EXTRA FEATURES)

Do not add email/SMS notifications in this ticket

Do not redesign the page

Do not change typography/layout/colors

Focus ONLY on these acknowledgement panel fixes