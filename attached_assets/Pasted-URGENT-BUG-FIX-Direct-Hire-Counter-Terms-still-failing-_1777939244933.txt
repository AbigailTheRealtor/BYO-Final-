URGENT BUG FIX — Direct Hire Counter Terms still failing

The previous fix did NOT resolve the issue.

Failing page:
Review This Agent’s Proposed Terms
Route example:
 /hire/agent/direct/142/buyer/residential

Action failing:
Clicking “Request Changes / Counter Terms” still shows:
“Error, Something went wrong please try again.”

IMPORTANT:
Do NOT guess. Do NOT make cosmetic changes. Do NOT say fixed until the button works in browser.

SCOPE:
Direct Hire Agent flow only:
- /hire/agent/direct/{agent}/{role}/{propertyType}
- Review This Agent’s Proposed Terms page
- Request Changes / Counter Terms action/button/modal/form

DO NOT TOUCH:
- Offer Listing create/edit flows
- Seller/Landlord/Buyer/Tenant offer listing forms
- Agent preset save/edit flows
- Dashboards
- Accepted Bid Summary
- Referral tracking
- Database schema
- migrate:fresh
- CompensationFormatter unless directly required
- ServicesFormatter unless directly required

STEP 1 — REPRODUCE WITH LOGS OPEN
Open terminal and run:

tail -f storage/logs/laravel.log

Then in browser:
1. Log in as a normal client/test user.
2. Visit:
   /hire/agent/direct/142/buyer/residential
3. Click “Request Changes / Counter Terms.”
4. Capture the exact Laravel error stack trace.

Also check browser DevTools → Network:
- failed URL
- status code
- response body

STEP 2 — FIND THE REAL FAILURE POINT
Search the codebase for all button/action text:

Request Changes
Counter Terms
Something went wrong
hire.agent.direct
hire-agent-direct
counter

Identify:
1. The Blade file rendering the button.
2. The JS handler or Livewire handler showing the generic error.
3. The controller route/method being called.
4. The backend exception from laravel.log.

STEP 3 — FIX THE ACTUAL ROOT CAUSE ONLY

Possible likely issues to check:

A) Button points to wrong route or missing route
- Fix route name/action.
- Make sure route accepts agent, role, propertyType.

B) Button expects an existing listing or bid but none exists yet
- Direct Hire preview may not have a listing/bid until accept/counter.
- If counter requires listing/bid, create or redirect into the proper counter setup flow safely.
- Do not assume listing_id exists.

C) Property type typo or validation failure
- Ensure only valid property types are allowed:
  residential, income, commercial, business, vacant_land
- If invalid, show clean message.
- Do not crash.

D) Auth/session issue
- If user is not logged in, redirect to login cleanly and return afterward.
- Do not return JSON error to a normal page action.

E) AJAX expects JSON but server returns HTML
- If the button uses fetch/AJAX, make backend return JSON consistently.
- If not necessary, convert to normal form POST/redirect to avoid hidden AJAX failure.

F) CSRF issue
- Ensure form includes @csrf.
- Ensure AJAX includes CSRF header if fetch is used.

G) Missing required request fields
- Add validation errors visible to user.
- Do not throw generic exception.

STEP 4 — REMOVE GENERIC FAILURE MASKING FOR THIS ACTION
For this specific action, replace generic frontend-only error behavior with useful handling:

- If validation fails: show validation message.
- If unauthenticated: redirect to login.
- If route/model not found: show clear page message.
- If server error occurs: log detailed error and show “Unable to open counter terms. Please contact support.”

STEP 5 — VERIFY IN BROWSER BEFORE CLAIMING FIXED
Required browser tests:

1. Logged-in user:
   /hire/agent/direct/142/buyer/residential
   Click “Request Changes / Counter Terms”
   EXPECTED: opens counter terms page/modal/form OR redirects to correct counter flow.

2. Logged-out user:
   Same page/action
   EXPECTED: clean login redirect, no generic error.

3. Bad property type:
   /hire/agent/direct/142/buyer/residentia
   EXPECTED: clear redirect/message, no crash.

4. Other valid direct hire routes:
   /hire/agent/direct/142/seller/residential
   /hire/agent/direct/142/landlord/residential
   /hire/agent/direct/142/tenant/residential

STEP 6 — REPORT BACK WITH PROOF
Return:
- Exact Laravel log error found
- Failed Network request URL/status
- Root cause
- Files changed
- Lines changed
- Browser verification results
- Confirmation that no Offer Listing, dashboard, referral, preset, or database schema files were changed