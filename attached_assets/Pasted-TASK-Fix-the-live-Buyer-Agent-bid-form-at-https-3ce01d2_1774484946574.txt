TASK: Fix the live Buyer Agent bid form at:
https://3ce01d2c-f79e-44c2-b048-2e8dc64316ef-00-tu7aronbf5wk.spock.replit.dev/buyer/agent/auction/bid/116

GOAL
When an Agent bids on a Buyer listing, the “Broker Compensation & Agency Agreement Terms” section must match the CURRENT Buyer listing-creation format and prefill from the actual Buyer listing being bid on.

CRITICAL ROUTE SAFEGUARD
- The live route to inspect first is:
  /buyer/agent/auction/bid/{id}
- Start by tracing this exact route/controller/component/view chain before editing anything.
- Only edit the files actually used by this live Buyer bid route.
- Do not guess.
- Do not edit old Buyer files, unused bid forms, or shared files unless confirmed they are part of this exact live route.
- If this route resolves into a shared auction/bid component, make the fix role-safe so it affects Buyer only.

GLOBAL SAFEGUARDS
- Do NOT rename database columns
- Do NOT delete any existing logic until the replacement is confirmed working
- Do NOT modify Tenant, Landlord, or Seller bid behavior except to reference their working logic
- Do NOT change unrelated tabs or fields
- Do NOT change Buyer listing creation behavior except to reuse it as the source of truth
- Do NOT hardcode outdated Buyer compensation fields
- Keep this scoped to the live Buyer bid flow only
- If unsure which file powers the section, STOP and report instead of guessing

WHAT MUST BE FIXED
Right now, when an agent bids on a Buyer listing, the Buyer Broker Compensation section is outdated and does not match the current Buyer listing format.

It must be updated so that:
1) The Buyer bid compensation section uses the latest Buyer listing-creation format
2) It supports the correct Buyer property type version for:
   - Residential
   - Income
   - Commercial
   - Business
   - Vacant Land
3) It pre-fills with the exact values the user entered when creating the Buyer listing
4) It behaves like the working Tenant bid flow, where the listing’s compensation terms automatically appear in the bid form
5) If editing an existing bid draft, the existing bid values should load first and should not be overwritten by the original listing values

STEP 1 — TRACE THE LIVE BUYER BID ROUTE
Starting from:
  /buyer/agent/auction/bid/{id}

Identify and report the actual live:
- route definition
- controller or Livewire component
- blade/view file(s)
- partial/component for Broker Compensation & Agency Agreement Terms

Do not proceed until the exact live files are confirmed.

STEP 2 — FIND THE CURRENT BUYER LISTING SOURCE OF TRUTH
Find the actual live Buyer listing create/edit files that define the CURRENT Buyer Broker Compensation & Agency Agreement Terms section.

These live Buyer listing files are the source of truth for:
- field list
- field labels
- field order
- conditional logic
- placeholders
- tooltips
- property-type-specific schema

Do not use an old Buyer bid-only version if the current Buyer listing version already exists.

STEP 3 — MAKE BUYER BID REUSE THE CURRENT BUYER FORMAT
Update the live Buyer bid compensation section so it reuses or mirrors the same current Buyer listing schema.

The Buyer bid form must render the same Buyer compensation format the listing creator sees, not an outdated version.

It must load the correct property-type-specific Buyer version:
- Buyer Residential
- Buyer Income
- Buyer Commercial
- Buyer Business
- Buyer Vacant Land

If some property types intentionally share the same schema, that is fine only if it matches the current Buyer listing format exactly.

STEP 4 — PREFILL FROM THE ACTUAL LISTING
For a new Buyer bid, load Broker Compensation & Agency Agreement Terms from the actual Buyer listing record being bid on.

Priority order must be:
1) Existing saved bid values, if editing/resuming a draft bid
2) Otherwise the Buyer listing’s saved broker compensation/agency agreement values
3) Otherwise blank

Do NOT prefill from:
- agent profile defaults
- generic bid defaults
- tenant mappings
- seller mappings
- landlord mappings
- old Buyer bid defaults

This should behave like the working Tenant bid flow.

STEP 5 — VERIFY ALL CURRENT BUYER FIELDS ARE MAPPED
Confirm every current Buyer compensation/agency field used in Buyer listing creation has a matching:
- load path
- bind path
- save path
- edit path

This includes:
- selects
- text inputs
- textarea fields
- date/timeframe fields
- yes/no toggles
- “Other” fields
- conditional child fields
- nested/json-backed fields if used

If Buyer listing creation was updated to latest keys and Buyer bid still points to old keys, update Buyer bid mapping to the latest keys.

STEP 6 — KEEP THE UI MATCHING THE CURRENT BUYER FORM
Make the live Buyer bid compensation section match the Buyer listing version for:
- heading text
- field labels
- order
- placeholders
- tooltips
- conditional visibility
- grouping/layout

The goal is that the Buyer listing and Buyer bid versions look like the same form, with the bid version prefilled from the listing.

STEP 7 — DO NOT BREAK WORKING FLOWS
Do not break:
- Tenant agent bids
- Landlord agent bids
- Seller agent bids
- other Buyer tabs
- save draft
- submit
- edit existing bid

If shared code is involved, add role-safe branching so Buyer only uses Buyer logic.

STEP 8 — TESTING REQUIREMENTS
Run these exact tests:

TEST A — Buyer Residential
1) Create or use a Buyer Residential listing with compensation fields filled in
2) Open the live route /buyer/agent/auction/bid/{id}
3) Confirm the compensation section matches the latest Buyer Residential listing format
4) Confirm all entered listing values are prefilled exactly

TEST B — Buyer Income
Repeat for Income

TEST C — Buyer Commercial
Repeat for Commercial

TEST D — Buyer Business
Repeat for Business

TEST E — Buyer Vacant Land
Repeat for Vacant Land

TEST F — Existing Bid Draft
1) Start a Buyer bid
2) Change compensation values
3) Save draft/progress
4) Reopen the same bid
5) Confirm bid-specific values load first and do not get overwritten by original listing values

TEST G — No Regression
1) Open a Tenant bid and confirm its compensation prefill still works
2) Confirm Landlord and Seller bid flows are unaffected

SUCCESS CRITERIA
- Live Buyer bid route /buyer/agent/auction/bid/{id} now shows the latest Buyer compensation format
- Correct property-type-specific Buyer version loads
- Listing-entered values are prefilled exactly on first bid load
- Existing bid draft values take precedence when editing
- No regression to Tenant/Landlord/Seller flows

REQUIRED REPORT BACK
After completion, report:
1) Exact live files changed for /buyer/agent/auction/bid/{id}
2) Exact Buyer listing files used as the source of truth
3) How the prefill priority was implemented
4) Which Buyer property types were tested successfully
5) Confirmation that unrelated bid flows were not changed