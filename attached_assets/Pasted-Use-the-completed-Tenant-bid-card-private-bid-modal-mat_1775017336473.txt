Use the completed **Tenant bid card + private bid modal + match score system** as the exact source of truth and make the **Landlord bid card, private bid modal, Services Match, and Terms Match** match Tenant behavior exactly.

This is **not** just a compensation preview tweak.
This is a **full Landlord parity pass** for:

* bid card preview
* private full bid modal
* services presentation
* compensation summary
* Services Match
* Terms Match
* mismatch highlighting
* extra/missing services display

## Goal

Landlord must match Tenant **to a tee** in:

* structure
* layout
* visual hierarchy
* preview logic
* comparison behavior
* resolved compensation display
* match score correctness

Only the Landlord-specific content should differ.

---

## Scope

Landlord only for this step.

This includes:

* Landlord bid card preview
* Landlord private full bid modal
* Landlord Services Match
* Landlord Terms Match
* Landlord compensation summary preview
* Landlord mismatch highlighting
* Landlord extra/missing services display

Do **not** modify:

* Tenant
* Buyer
* Seller
* DB schema
* existing working Tenant logic

---

## Hard safeguards

* No destructive DB changes
* No schema changes
* No dropping/renaming columns
* Do not change field names
* Do not change stored values
* Do not change service wording
* Do not redesign Tenant
* Do not create new Landlord-only visual patterns
* Do not use raw UI input counting
* Do not count child inputs as separate logical fields
* Do not reintroduce cross-role contamination
* If anything cannot be matched exactly, stop and report

---

# Part 1 — Make the Landlord bid card preview match Tenant exactly

## Problem

The Landlord bid card preview still does not match Tenant:

* layout differs
* compensation preview differs
* visual hierarchy differs
* match score block differs in usefulness
* card still feels role-specific instead of system-consistent

## Required fix

Use the Tenant bid card preview as the exact visual and structural reference.

### Required behavior

Landlord bid card preview must match Tenant in:

* header structure
* match badge placement
* status placement
* Offered Services line format
* Extra Value Added line
* Match Summary block
* Broker Compensation Summary structure
* View Full Bid / action area
* compliance note positioning

### Compensation summary preview

Do not show weak raw type-only output like:

* `Landlord's Broker Lease Fee Type`

Instead, show actual resolved compensation value first, the same way Tenant preview shows meaningful deal info.

Use:

* main landlord broker compensation value
* supporting details like payment timing / renewal fee only if useful
* compact stacked preview

Do not dump every field.

---

# Part 2 — Make the Landlord private full bid modal match Tenant exactly

## Problem

Landlord private modal still differs from Tenant in structure and presentation.

## Required fix

Use the Tenant private bid modal as the exact rendering/template reference.

### Required parity

Landlord private full bid modal must match Tenant in:

* section order
* section spacing
* typography
* row layout
* left-label / right-value pattern
* mismatch badge style
* missing services box style
* extra services style
* Additional Services section style
* Agent Presentation / Agent Information section placement

### Compensation section

Landlord compensation section must match Tenant style exactly:

* same sectioned structure
* same row formatting
* same mismatch treatment
* same conditional rendering behavior
* no duplicate additional terms/details block
* no raw/internal values
* no boxed/tile compensation display
* no alternative custom layout

Only Landlord fields/content should differ.

---

# Part 3 — Make Landlord Services Offered match Tenant exactly

## Problem

Landlord Services Offered still drifted from Tenant before. This must now be locked to true parity.

## Required fix

Landlord Services Offered must use the same presentation pattern as Tenant:

* categorized sections
* bullet list structure
* same legend style
* same matched / extra / missing rendering
* same Additional Services handling
* same missing-services block style

### Important

Use Landlord service names/content only.
Do not convert Landlord services into Tenant services.
Only copy the rendering pattern.

### Required rule

If Tenant and Landlord are placed side by side, they should look like the same component with different data.

---

# Part 4 — Audit and verify Landlord Services Match like Tenant

## Problem

Landlord Services Match must be tested the same way Tenant was tested so the exact matched/missing/extra counts are correct.

## Required diagnosis

For at least one Landlord bid with:

* matched services
* extra services
* missing services
* custom additional services

trace all of these:

1. baseline requested Landlord services
2. bid checked services at save time
3. saved services payload
4. services used by bid card preview
5. services used by full modal
6. services used by Services Match denominator
7. services used by matched / extra / missing buckets
8. custom additional services handling

### Required verification

Confirm:

* denominator is correct
* each custom additional service counts individually
* no service appears in both extra and missing
* extra count is correct
* missing count is correct
* displayed services match the saved services exactly

---

# Part 5 — Audit and verify Landlord Terms Match like Tenant

## Problem

Landlord Terms Match must be tested the same way Tenant was tested so the denominator and changed count reflect true logical terms only.

## Required diagnosis

For at least one Landlord bid with compensation changes, output the full logical term audit.

For each logical term counted, show:

* logical term key
* human-readable label
* baseline normalized value
* current normalized value
* match/mismatch result
* why it was counted
* whether it is conditionally active

Do not dump only raw payload arrays.

### Required rules

Landlord Terms Match must:

* compare logical groups, not raw inputs
* combine related fee inputs into one logical term
* exclude inactive child fields
* normalize money / percent / blank / null consistently
* support Additional Terms as a real logical term
* align row-level mismatch badges with changed_terms exactly

### Required verification

Confirm:

* denominator reflects true active logical terms only
* changed count reflects only real logical differences
* no phantom child-field changes
* no stale hidden-field mismatches
* Additional Terms behaves correctly

---

# Part 6 — Compensation values must reflect exact actual amount/value

## Problem

Both Services Match and Broker Compensation summary need to reflect exact deal values correctly, not just raw types or incomplete labels.

## Required fix

Make sure all Landlord compensation displays use resolved values, not weak raw types.

For example:

* actual purchase fee format
* actual lease-option compensation format
* actual renewal fee / management fee / timing values
* actual tenant broker compensation values
* actual agency timeframe values

Do not show:

* raw type-only rows as the main preview
* broken combined strings
* raw stored codes
* incomplete fee previews

### Applies to:

* bid card preview
* private full bid modal
* match score summary context where compensation preview is shown

---

# Part 7 — Tenant-style QA pass for Landlord bid card + modal + scoring

Use the same QA rigor that was used for Tenant.

## Minimum required scenarios

### A. Landlord bid card preview

1. unchanged bid
2. bid with extra services
3. bid with missing services
4. bid with changed terms

Verify:

* preview layout matches Tenant
* compensation preview is meaningful
* match badge and status placement match Tenant
* Offered Services line format matches Tenant
* Extra Value Added line matches Tenant
* Match Summary block matches Tenant

### B. Landlord private full bid modal

5. unchanged bid
6. changed bid
7. bid with extra/missing services
8. bid with additional/custom services
9. bid with changed Additional Terms

Verify:

* compensation section layout matches Tenant
* services section layout matches Tenant
* mismatch highlighting works
* no duplicate blocks
* no raw/internal values

### C. Services Match

10. one matched-only bid
11. one bid with extra services
12. one bid with missing services
13. one bid with custom additional services

Verify:

* exact denominator
* exact matched count
* exact extra count
* exact missing count

### D. Terms Match

14. one unchanged bid
15. one bid with 1 logical change
16. one bid with 2+ logical changes
17. one bid with Additional Terms change

Verify:

* denominator correct
* changed count correct
* row mismatch badges align with score

### E. Stability

18. reload unchanged bid page 3 times
19. reload changed bid page 3 times

Verify:

* same Services Match denominator every time
* same Terms Match denominator every time
* same changed count every time
* same services display every time

---

# Part 8 — Component reuse recommendation

If the cleanest path is to reuse the Tenant rendering partial/component for:

* bid card preview
* services offered display
* row rendering patterns

do that safely.

The goal is to prevent future drift between Tenant and Landlord.

But:

* do not break Tenant
* do not change data logic
* only reuse presentation safely where appropriate

---

# Required deliverable

When done, give me:

1. exact Landlord files changed
2. whether any Tenant rendering partial/component was safely reused
3. exact Services Match audit for at least one Landlord bid
4. exact Terms Match logical audit for at least one Landlord bid
5. screenshots of:

   * Landlord bid card preview
   * Landlord private full bid modal
   * Landlord services section
   * Landlord compensation section
6. confirmation that Landlord now matches Tenant presentation and comparison behavior exactly except for role-specific content
7. confirmation that Tenant was not changed or broken

## Critical rule

This is a **full Landlord parity pass** for bid card + modal + scoring behavior.

Do not stop at “looks cleaner.”
Do not stop at “it renders.”
It is only done when:

* Landlord matches Tenant visually
* Landlord matches Tenant structurally
* Landlord Services Match is correct
* Landlord Terms Match is correct
* compensation preview reflects actual accepted/bid amounts correctly
