Replit Instructions — Hire a Tenant’s Agent (Residential + Commercial): Bid View Label + Timer Lock + Button Styling (WITH SAFEGUARDS)
Applies to:

Hire a Tenant’s Agent → Agent Bids Display (Residential + Commercial)
Includes:

Listing Creator viewing agent bids

Bid “Private Terms / modal” where Accept/Counter/Reject appears

Any “View Full …” link shown on bids

❌ Do NOT apply to Buyer/Seller/Landlord in this task.

0) SAFEGUARDS (MUST FOLLOW EXACTLY)
DO NOT:

Edit anything in /vendor/

Add/remove/rename DB columns, migrations, models, controllers, routes

Rename any existing field keys, request keys, or Livewire property names

Change how bids are saved, validated, ranked, anonymized, or scored

Change which bids are visible to other agents (anonymity/visibility rules stay exactly as-is)

Refactor layouts, reorder tabs, or rewrite tooltips/placeholders

Change any business logic outside the three items listed below

ONLY ALLOWED CHANGES:

Rename a single UI label from “View Full Services & Broker Compensation Terms” → “View Full Bid” (Tenant bids only)

Enforce timer-based action locking for listing creator actions (Accept/Counter/Reject) on Bidding Period listings

Adjust button CSS/classes only for Accept/Counter/Reject to correct colors and sizing (no handler changes)

1) CHANGE #1 — Rename “View Full Services & Broker Compensation Terms” to “View Full Bid” (Tenant Bids Only)
Requirement

Whenever a listing creator is viewing agent bids for Hire a Tenant’s Agent (Residential + Commercial), replace the link/button text:

From: “View Full Services & Broker Compensation Terms”

To: “View Full Bid”

Constraints

Do NOT change what it opens

Do NOT change permissions or visibility rules

Do NOT change this label in other flows (Buyer/Seller/Landlord)

✅ Only update the display text in the Tenant bids UI.

2) CHANGE #2 — Bidding Period Listings: Lock Accept/Counter/Reject Until Timer Ends (Tenant Only)
Requirement

In the listing creator’s bid actions area (where Accept/Counter/Reject appear):

Traditional Listing Type

✅ Listing creator can Accept / Counter / Reject at any time (immediately)

Bidding Period Listing Type

❌ Listing creator cannot Accept / Counter / Reject until timer ends
✅ After timer ends, buttons become active

Implementation Rules (Minimal + Safe)

Identify listing type field used in Tenant flow (example: $auction_type or listing_type):

Traditional vs Bidding Period

Identify end time:

end_at = created_at + bidding_period_days

Use a single boolean computed server-side or in component state:

actionsLocked = (listing_type == 'Bidding Period') AND (now < end_at)

UI Behavior (choose simplest, no redesign)

If actionsLocked == true:

Hide Accept/Counter/Reject OR disable them

If disabled, show a small note:

“Actions unlock when the bidding period ends.”

If actionsLocked == false:

Buttons behave normally

🚫 Do NOT change Accept/Counter/Reject handlers, only gating visibility/disabled state.

3) CHANGE #3 — Fix Accept/Counter/Reject Button Styling (Tenant Only)
Goal

Buttons must look correct without hover and must be identical sizing.

Final Required Styles

Accept Bid: green background + white text (always when enabled)

Counter Bid: blue background + white text (use current reject blue color)

Reject Bid: red background + white text (change from blue)

Size Requirement

Accept / Counter / Reject must be the same height, padding, font-size, border-radius.

Counter is currently too large — remove any btn-lg or custom oversized padding so it matches the others.

Implementation Rules (Safe)

Do NOT change button click handlers (wire:click, routes, JS events)

Do NOT change conditional rendering logic beyond the lock in Section 2

Only update classes / styles on these three buttons

Specific Fixes

Accept button hover issue

Remove any styling that makes it appear white until hover

Ensure it has a persistent “success/green” class and white text

Example pattern: btn-success text-white

Confirm no CSS is overriding its default background to transparent/white

Counter button size

Remove any size modifiers unique to Counter (example: btn-lg)

Ensure all three share the same base class (example: btn + same padding class)

Reject button color

Change Reject from blue to red

Example pattern: btn-danger text-white

Counter uses old reject blue

Apply the same blue class previously used by Reject

Example pattern: btn-primary text-white (or your custom blue class)

4) ACCEPTANCE TESTS (MUST PASS)
Test A — Label

View Tenant agent bids (Residential and Commercial)
✅ Link text now says “View Full Bid”

Test B — Timer gating
Traditional

✅ Accept/Counter/Reject visible/enabled immediately

Bidding Period

✅ Accept/Counter/Reject hidden/disabled until timer ends
✅ After end, buttons unlock

Test C — Buttons

✅ Accept is green/white without hover
✅ Counter is blue/white and same size as other buttons
✅ Reject is red/white and same size as other buttons
✅ Clicking buttons still works exactly the same (no logic changes)

5) REQUIRED REPORT BACK

After completing:

File paths edited

Before/after snippet for:

label text change

action lock condition

button class changes

Confirm:

no vendor edits

no schema changes

Tenant flow only

If you want to make this even harder for Replit to mess up, add this one line at the top of your prompt:

Do not touch any code under Buyer/Seller/Landlord folders/components. Only edit the Tenant bid display + Tenant bid action modal.