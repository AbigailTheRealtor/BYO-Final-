Below are **copy/paste Replit instructions with safeguards** to implement your **Hire a Buyer’s Agent** edits across **listing creation + actual listing display**, without breaking any other flows.

---

## ✅ REPLIT INSTRUCTIONS (Hire a Buyer’s Agent) — WITH SAFEGUARDS

### 🔒 SAFEGUARDS (must follow)

* **DO NOT** change database schema, migrations, or column names.
* **DO NOT** rename existing fields/keys used in Livewire/forms/controllers.
* **DO NOT** remove or rewrite tooltips unless explicitly stated below.
* **DO NOT** change Seller/Tenant/Landlord flows.
* **ONLY** make **display-order + label + formatting** changes for **Buyer listings** and only in:

  1. Buyer listing creation/edit component(s)
  2. Buyer actual listing show/view component(s)
* Keep all existing data mapping intact; this is **presentation-layer only** unless noted.

---

# 1) Offered Financing/Currency — fix ORDER (Creation + Actual Listing)

### ✅ Requirement

For **Buyer** listings, the **Offered Financing/Currency** options (and their related sub-questions) must display in this order on:

* **Listing Creation/Edit UI**
* **Actual Listing display UI**

### ✅ Correct Order (exact)

1. **Assumable**
2. **Conventional / FHA / Jumbo / VA / No-Doc / Non-QM / USDA**
3. **Exchange/Trade Terms**
4. **Lease Option**
5. **Lease Purchase**
6. **Non-Fungible Token (NFT)**
7. **Seller Financing**

### ✅ Implementation Instructions (Replit)

* Locate the Buyer listing creation/edit form section that renders **Offered Financing/Currency** options (likely a checkbox list or multi-select).

* If options are built from:

  * an array → reorder the array to match the exact order above
  * constants/enums → reorder the display array (do NOT reorder DB stored values)
  * config file → reorder only the Buyer display list

* Locate the Buyer “actual listing” view where it prints “Offered Financing/Currency” and the sub Q&A.

* Ensure the **render loop follows the same ordered list**, not alphabetical, not database order.

### 🔒 Safeguard note

If other listing types (Seller/Tenant/Landlord) use shared enums, **do not change the global enum order**.
Instead: create a **Buyer-only ordered display array** like:

* `BUYER_FINANCING_DISPLAY_ORDER = [...]`

---

# 2) Answer Formatting Rule — bubble ONLY for multi-select answers

### ✅ Requirement

On the **actual listing display** (Buyer listings):

* If a field has **one value selected** → show **regular text** (not bubble).
* If a field has **multiple values selected** → show values in **bubble/chip format**.

### ✅ Implementation Instructions

* Find the shared “render answer” logic used on Buyer actual listing display.
* Add a small helper:

**Pseudo-logic (must match your stack):**

* If value is array/list:

  * If `count == 1`: render first item as plain text
  * If `count > 1`: render as bubbles
* If value is string:

  * render as plain text

### 🔒 Safeguard note

Apply this only to Buyer listing display **unless you already have a shared helper** and you confirm it won’t break other listing types. If uncertain: **Buyer-only change**.

---

# 3) Acceptable Property Type (Buyer) — force regular text, NOT bubble

### ✅ Requirement

On the **actual Buyer listing**:

* “Acceptable Property Type” must display in **regular text format** (even if multiple are selected)
* **Do NOT** use bubble/chip formatting for this specific field.

### ✅ Implementation Instructions

* Locate the section in the Buyer actual listing view that outputs:

  * `Acceptable Property Type`
* Override the default formatting:

  * Join multiple selections with commas (example: `Residential, Income, Vacant Land`)
  * Render as normal text

### 🔒 Safeguard note

Do not change how “Acceptable Cities/Counties” or other multi-select fields behave unless requested. This override is **field-specific**.

---

# 4) Listing Creation: Pets = Yes → rename “Types of Pets” to “Pet Types” (Buyer)

### ✅ Requirement

In Buyer listing creation/edit:

* If user selects **Pets: Yes**
* The follow-up label must read:
  ✅ **Pet Types**
  ❌ not “Types of Pets”

### ✅ Implementation Instructions

* Locate the Buyer listing creation/edit form conditional that shows when `Pets == Yes`
* Change the field label text only:

  * from **“Types of Pets”** → **“Pet Types”**
* Do not change the stored key/field name unless it’s currently wrong. This is a **label-only** change.

### 🔒 Safeguard note

If this field is shared across roles, keep the underlying key untouched and update only the label in the Buyer form component.

---

# 5) QA Checklist (must verify)

1. Create a Buyer listing and select financing options.

   * Confirm the financing list appears in the exact order above on creation UI.
2. View the actual Buyer listing page.

   * Confirm financing options + sub-questions appear in the exact order above.
3. Check formatting:

   * Single-value answers show plain text.
   * Multi-value answers show bubbles.
4. Confirm exception:

   * Acceptable Property Type always shows plain text (comma-separated).
5. Pets flow:

   * Set Pets = Yes → label shows “Pet Types”.

---

## If you want the “exact files” version

If you paste the file names (or screenshots of your Replit file tree) for:

* Buyer creation component
* Buyer show/listing view

…I’ll rewrite this into a **file-by-file patch plan** like:

* `app/Livewire/BuyerAgentAuction.php`
* `resources/views/livewire/buyer-agent-auction.blade.php`
* `resources/views/buyer/listings/show.blade.php`

…but the instructions above are already safe to paste into Replit AI as-is.
