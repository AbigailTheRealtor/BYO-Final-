We are starting Phase 3: Agent Preset Management UI for the Hire Me system.

Objective

Allow agents to create and manage their default offer presets by:

role (Seller, Buyer, Tenant, Landlord)
property type

These presets power:

Hire Me link
Hire Me widget
direct hire flow
default bid generation
Core rule

Each agent can have one preset per:

role
property type

Example:

Buyer + Residential = one preset
Seller + Commercial = one preset
Phase 3 Scope (KEEP SIMPLE)

We are building the minimum usable version.

Do NOT over-engineer.

Required UI
New dashboard section

Add:

👉 “My Offer Presets”

Preset list page

Show all role/property combinations:

Buyer → Residential, Income, Commercial, Business, Vacant Land
Seller → same
Tenant → Residential, Commercial
Landlord → Residential, Commercial

Each card shows:

preset exists or not
Edit button
Preset edit page

Reuse the existing bid form structure.

Preload:

AgentDefaultProfile if exists
otherwise empty form
Required fields
1. Services
show only role/property-type relevant services
checkbox list
selected = included services

RULE:
These services become the ONLY services shown in Hire Me flow

2. Broker Compensation & Agency Terms

Reuse existing fields exactly

3. Agent Overview

Simple textarea

4. Agent Presentation & Materials

Reuse existing structure if possible

5. Agent Credentials
brokerage
license #
NRDS ID

Reuse existing agent profile fields if possible

Data handling

Use existing:

👉 AgentDefaultProfile model

Store:

role
property_type
profile_data (JSON)

Do NOT create new tables unless necessary

Save behavior

On save:

overwrite existing preset for that role/property
or create new if none exists
Integration rule

These presets must be used by:

AgentBidMapperService
Hire Me direct flow
future widget

Do NOT duplicate logic

Audit first

Before coding, report:

existing AgentDefaultProfile structure
existing service catalog by role/property type
existing bid form reusable sections
best way to reuse existing UI instead of rebuilding
Safeguards
No DB renames
No DB deletes
No breaking bid system
No breaking Hire Me flow
No breaking marketplace flow
Do not duplicate service lists
Reuse existing components wherever possible
Deliverable

After implementation, report:

files created/changed
how presets are stored
how role/property type selection works
how services are rendered
how existing components were reused
confirmation that Hire Me flow now pulls from these presets
🧠 My advice (important)

Keep this phase:

👉 simple, fast, usable

NOT:

perfect
overly flexible
enterprise-level
🔥 What this unlocks

Once Phase 3 is done:

agents can configure their offer
Hire Me link becomes real
your system becomes sellable
🎯 After Phase 3

Next steps (later):

Hire Me link generator
Widget embed
Agent marketing page