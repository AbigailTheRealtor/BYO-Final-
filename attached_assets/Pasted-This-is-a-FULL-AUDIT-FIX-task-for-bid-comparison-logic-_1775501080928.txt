This is a FULL AUDIT + FIX task for bid comparison logic and private bid rendering.

Scope:

* Tenant
* Buyer
* Seller
* Landlord

Property types actually used:

* Tenant: Residential, Commercial
* Landlord: Residential, Commercial
* Buyer: Residential, Income, Commercial, Business, Vacant Land
* Seller: Residential, Income, Commercial, Business, Vacant Land

Primary goals:

1. Every changed field inside Broker Compensation & Agency Agreement Terms must show as a mismatch
2. Mismatch must appear for BOTH sides:

   * the listing user’s private compensation & agreement terms view
   * the bidding agent’s private compensation & agreement terms view
3. Services scoring and display must be audited and corrected across all supported role/property type combinations
4. Offered Services must appear correctly on Buyer bids inside the Private Compensation & Agency Agreement Terms area if that is how Tenant is structured
5. This must be verified field by field, not assumed

---

## HARD SAFEGUARDS

* Do NOT rename or delete database columns
* Do NOT change listing creation forms or field names
* Do NOT change stored data structure unless absolutely required to correctly compare existing fields
* Do NOT change unrelated dashboard pages
* Do NOT refactor unrelated components
* Do NOT touch routes unless a currently active bid/private view route is broken
* Use Tenant as the source of truth for:

  * mismatch behavior
  * private compensation rendering style
  * Offered Services placement/behavior
* Only modify:

  * active match-score helpers
  * active private compensation / agreement term rendering
  * active bid comparison / mismatch mapping code
  * active current bid views
* Stop and report if any issue is caused by the same field being stored under inconsistent keys between listing and bid

---

## AUDIT REQUIREMENTS

Do a real audit first before coding.

For each role and property type, audit:

A. BROKER COMPENSATION & AGENCY AGREEMENT TERMS FIELD COMPARISON

* identify every field/group that should be compared
* identify the exact storage key(s) for the listing side
* identify the exact storage key(s) for the bid side
* identify whether the comparison is:

  * exact match
  * grouped/composite match
  * conditional match based on parent yes/no field

B. MISMATCH RENDERING

* verify that if a field changes, the mismatch badge/indicator actually appears
* verify it appears in:

  * listing owner private view
  * bidding agent private view
* verify no changed field is silently ignored

C. SERVICES

* verify Offered Services exists where it should in active private bid views
* verify service match/missing/extra counts are correct
* verify Offered Services is not missing on Buyer bids

---

## CRITICAL BUG TO FIX

I just changed fields inside Lease-Option Details on an agent bid and they did NOT show mismatch.

Example:

* Buyer’s Broker Commission Structure mismatch showed correctly
* Buyer’s Broker Purchase Fee mismatch showed correctly
* BUT lease-option detail fields did NOT show mismatch

This means the audit must specifically verify ALL fields inside Lease-Option Details for all roles/property types.

If any field inside Lease-Option Details changes, it must show as a mismatch in private compensation & agreement terms.

This applies to all applicable roles/property types.

---

## FULL FIELD-BY-FIELD AUDIT MATRIX REQUIRED

For EACH supported role × property type combination, audit all Broker Compensation & Agency Agreement Terms sections and confirm every compareable field is covered.

Expected sections include, where applicable:

* Broker Compensation
* Purchase Fee Details
* Lease Fee Details
* Lease-Option Details
* Legal Terms
* Brokerage Relationship
* Additional Terms
* Additional Details must remain outside this area and must NOT count as terms

Important:

* If a field is visible in the private compensation section, and the bid changes that field, it must show mismatch
* No hidden exceptions unless the field truly should not be compared

---

## COMPARISON RULES

1. Every actual filled baseline term group = 1 countable term group
2. Additional Details must NEVER count as a term
3. If a displayed field is part of a logical composite group, mismatch logic must still surface clearly in the UI
4. If one subfield changes inside a composite group, that group must show mismatch
5. Lease-Option fields must not be skipped
6. Parent/child conditional fields must work correctly:

   * if parent says Yes and child values are filled, child values must compare
   * if parent says No and child values should be inactive, do not falsely mismatch inactive fields

---

## SERVICES RULES

1. Offered Services must appear in active private bid views where Tenant shows it
2. Every checked service = 1 service
3. Each digital enhancement sub-option = 1 service
4. Each custom additional service entry = 1 service
5. Extras must NOT inflate match percentage
6. Matched services must NOT be mislabeled as extra
7. Missing Offered Services on Buyer bids must be fixed

---

## SPECIFIC DELIVERABLES

PHASE 1 — AUDIT REPORT FIRST

Before coding, return a structured audit with:

1. Exact files used for:

* Tenant bid private compensation view
* Buyer bid private compensation view
* Seller bid private compensation view
* Landlord bid private compensation view
* all active match/mismatch helpers

2. For each role/property type:

* list every term group/field compared
* list any field currently rendered but NOT compared
* list any field currently compared but NOT rendered
* list any field whose mismatch does NOT currently appear

3. Specifically call out Lease-Option Details for every applicable role/property type and state:

* which exact fields are compared
* which exact fields currently fail mismatch rendering if any

4. For services:

* confirm whether Offered Services is present on each active private bid view
* confirm whether Buyer is missing it
* confirm service count logic per role/property type

Do NOT code yet in Phase 1.

---

## PHASE 2 — FIXES AFTER AUDIT

After the audit is returned, then implement only the required fixes.

Required fix standard:

* if any displayed Broker Compensation & Agency Agreement Terms field changes, mismatch must visibly appear
* on both listing owner and bidding agent private views
* across all supported role/property types

Also:

* restore/fix Offered Services in Buyer private bid view
* verify services calculations still work after fixes

---

## VERIFICATION AFTER FIXES

After coding, provide a numbered checklist:

1. Tenant

* Residential
* Commercial

2. Landlord

* Residential
* Commercial

3. Buyer

* Residential
* Income
* Commercial
* Business
* Vacant Land

4. Seller

* Residential
* Income
* Commercial
* Business
* Vacant Land

For each combination confirm:

* all changed Broker Compensation & Agency Agreement Terms fields now show mismatch
* Lease-Option fields now show mismatch when changed
* both listing owner and bidding agent private views show the mismatch
* Offered Services appears correctly where required
* services counts are correct
* Additional Details remains outside Broker Compensation & Agency Agreement Terms

Also include:

* exact files changed
* exact root cause for each mismatch bug found
* any remaining edge cases still needing follow-up
