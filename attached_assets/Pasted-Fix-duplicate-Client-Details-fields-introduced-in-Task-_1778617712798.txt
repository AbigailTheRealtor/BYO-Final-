Fix duplicate Client Details fields introduced in Task #847 (Offer Listing preset counter bids)

Task #847 correctly added required Client Details validation + persistence for Offer Listing preset counter bids, but several Blade files now contain duplicated labels and duplicated inputs because the new required versions were appended instead of replacing the existing fields.

This is causing:

* duplicate labels
* duplicate inputs
* duplicate icons
* broken spacing/layout
* possible Livewire hydration problems

STRICT SCOPE
Only touch the duplicated Client Details Blade markup introduced in #847.

DO NOT change:

* validation rules
* controller logic
* saveMeta/hydration logic
* routes
* database
* match score logic
* offer listing detection logic
* tab structure
* service comparison logic

FILES TO FIX
1.
resources/views/livewire/landlord-agent-auction-bid-counter-tabs/client-contact-information.blade.php

2.

resources/views/livewire/seller-agent-auction-counter-tabs/client-contact-information.blade.php

3.

resources/views/livewire/seller-agent-auction-counter-tabs/counter-terms.blade.php

PROBLEM PATTERN
These files currently contain duplicated markup like:

```blade
<label class="fw-bold">Full Name</label>
<label class="fw-bold">Full Name <span class="text-danger">*</span></label>

<input ... >
<input ... required>
```

The old field and the new required field are both rendering.

FIX REQUIREMENT
Replace the duplicated pairs with ONE single field.

Correct pattern:

```blade
<label class="fw-bold">
    Full Name @if($isOfferListing)<span class="text-danger">*</span>@endif
</label>

<input
    type="text"
    class="form-control has-icon"
    wire:model="client_name"
    placeholder="Client's full name"
    @if($isOfferListing) required @endif
>
```

Apply this same cleanup pattern to:

* Full Name
* Phone Number
* Email Address
* Street Address
* City
* State
* ZIP Code

RULES

* ONE label only
* ONE input only
* Preserve existing wire:model names
* Preserve existing icons/classes
* Preserve @error blocks
* Preserve conditional required behavior for offer listings only
* Do not alter layout structure
* Do not add new wrappers
* Do not change validation rules

DONE LOOKS LIKE

* No duplicated labels
* No duplicated inputs
* No duplicated icons
* Required asterisk only appears for Offer Listings
* Required HTML attribute only appears for Offer Listings
* Existing Livewire bindings continue working
* Layout spacing returns to normal
* Seller + Landlord counter forms render cleanly again
