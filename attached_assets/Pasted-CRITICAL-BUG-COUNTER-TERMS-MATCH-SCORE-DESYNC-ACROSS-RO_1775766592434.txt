CRITICAL BUG — COUNTER TERMS + MATCH SCORE DESYNC ACROSS ROLES
READ FULLY BEFORE MAKING ANY CHANGES

We have a major data consistency issue across Seller, Buyer, Landlord, and Tenant counter term views.

The same counter is rendering DIFFERENT:
- values
- services
- match scores

depending on which side is viewing it.

This is NOT a UI issue.
This is a baseline + data source inconsistency.

CONFIRMED BUGS

1. FIELD VALUE DESYNC

Example:
Agent changed:
- Protection Period → 23 days

Agent view shows:
- 23 days (correct)

Seller view shows:
- 30 days (OLD value, incorrect)

This means Seller view is NOT using the correct counter data.

---

2. SERVICES MISCLASSIFIED

Example:
Agent ADDED services:
- List property on MLS

Seller view shows:
- Services Not Included

This is reversed logic.

---

3. MATCH SCORE DESYNC

Agent view:
- Match Score: 96%

Seller view:
- Match Score: 95%
- Different breakdown
- Different totals

This should NEVER happen.

Both sides must see:
- same numbers
- same counts
- same logic

---

ROOT ISSUE

Each role is using a different baseline and/or different data source.

We must enforce a SINGLE source of truth:

FOR ANY VIEWED COUNTER:

counterData = the current counter being viewed  
baselineData = the immediately previous counter (or original only if first round)

ALL calculations MUST use:
- same counterData
- same baselineData

regardless of role

---

REQUIRED FIX

1. UNIFY DATA SOURCE

For ALL roles:
- Seller
- Buyer
- Landlord
- Tenant

Ensure:

counterData is identical regardless of viewer  
baselineData is identical regardless of viewer  

Viewer role must NOT change:
- which counter is used
- which baseline is used

---

2. FIX FIELD DISPLAY

Ensure ALL displayed values come from:
counterData (NOT listing, NOT old baseline)

Example:
- Protection Period must always show latest value from counterData

---

3. FIX SERVICES CLASSIFICATION

Services must be derived strictly as:

Added = in counterData but not in baselineData  
Removed = in baselineData but not in counterData  
Unchanged = in both  

No role-based variation allowed

---

4. FIX MATCH SCORE (CRITICAL)

Match score MUST be identical across all roles.

Use ONE calculation source:
- same baselineData
- same counterData
- same scoring function

Remove any role-based variation such as:
- "Compared to original bid"
- "Compared to listing"
- mixed logic

---

5. REMOVE ROLE-BASED BASELINE SWITCHING

The current logic uses viewerRole to decide baseline.

THIS IS WRONG.

Replace with:
baseline = immediately previous counter

Only fallback:
- original listing (if no previous counter exists)

---

6. VERIFY ORDER + DISPLAY

Ensure services:
- maintain listing creation order
- do not reorder incorrectly
- do not flip added vs removed

---

HARD SAFEGUARDS

- DO NOT change bid submission logic
- DO NOT change database schema
- DO NOT create new counters
- DO NOT refactor entire system
- ONLY fix:
  - baseline selection
  - counterData consistency
  - display mapping
  - scoring input consistency

If baseline selection is tied to deeper logic:
STOP and report exact file and variable path

---

WHAT I WANT BACK

1. Exact files where baselineData and counterData are selected
2. Exact fix applied to unify them
3. Confirmation that ALL 4 roles now:
   - show identical field values
   - show identical services classification
   - show identical match score
4. Explanation of what caused the desync
5. Before vs after explanation of baseline logic

---

FINAL RULE

The SAME counter must render IDENTICALLY across all roles.

No exceptions.