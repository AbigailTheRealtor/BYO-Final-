Good audit. Proceed with additive improvements only. Do not rebuild or duplicate any existing referral phases.

ALREADY IMPLEMENTED — DO NOT TOUCH OR REBUILD:
- referral DB schema
- referral link generation
- referral capture route
- referral visit logging
- session-based first-click-wins attribution
- signup persistence
- listing persistence
- accepted hire persistence
- agent dashboard referral card
- admin referral tracking page

NEXT TASK:
Implement only Gap 1 — cookie fallback for referral attribution.

GOAL:
Preserve referral attribution beyond the PHP session by setting a safe referral cookie on /invite/{code}, then using that cookie as a fallback in signup persistence when session referral data is missing.

CRITICAL SAFEGUARDS:
- Surgical changes only.
- Do not redesign anything.
- Do not modify Buyer/Seller/Landlord/Tenant flows.
- Do not touch Livewire listing logic, bid logic, match scores, timers, counters, summaries, PDFs, dashboard layout, or admin layout.
- Do not rebuild existing referral logic.
- Do not change current session-based first-click-wins behavior inside an active session.
- Do not introduce duplicate counting.
- Do not overwrite an existing user referral if already set.
- Do not overwrite an existing session referral with a cookie value.
- Stop and report if the current signup persistence method cannot safely access the request/cookie context.

IMPLEMENT ONLY THESE ADDITIVE CHANGES:

1. Referral capture cookie
Inspect the existing ReferralController::capture() implementation first.

When a valid active referral code is captured:
- keep the current session behavior exactly as-is
- additionally set a referral cookie with:
  - referral_code = clicked code
  - 30-day expiration
  - HttpOnly = true
  - SameSite = Lax
  - Secure = true when appropriate for the environment
- if there is already an active session referral, do not overwrite the session
- cookie may still be refreshed when the same code is revisited, but do not create duplicate referral_visits rows and do not increment click_count again if first-click-wins skip is already active in session

2. Signup persistence fallback
Inspect the existing ReferralLinkService::persistSignup(...) implementation first.

Current priority should become:
A. existing session referral
B. referral cookie fallback
C. otherwise none

If session referral exists:
- keep existing behavior unchanged

If session referral is missing but referral_code cookie exists:
- look up an active row in agent_referral_links by code
- if found, attach to the newly created user only if:
  - users.referred_by_agent_id is null/empty
- populate:
  - referred_by_agent_id
  - referral_source_code
  - referral_captured_at = now() or a safe fallback if original capture time is unavailable
- increment signup_count only when a fresh attribution is actually written
- do not crash if cookie is stale or invalid

3. referral_visits behavior for cookie fallback
- if session referral.visit_id exists, keep current behavior
- if signup is happening from cookie fallback only and there is no matching visit_id in session, do not fabricate or backfill a fake referral_visits row in this phase
- fail gracefully and still allow user attribution

4. No other changes
Do not implement:
- admin status actions
- dashboard recent activity
- payout math
- cookie fallback for listing creation yet
- cross-device identity logic
- IP/device deduplication

TEST REQUIREMENTS:
Run and report all of these:

T1. Session path unchanged
- valid /invite/{code}
- register in same browser session
- confirm existing session-based signup attribution still works exactly as before

T2. Cookie fallback works
- valid /invite/{code}
- clear or simulate missing session while preserving cookie
- register new user
- confirm users.referred_by_agent_id and referral_source_code are populated from cookie fallback
- confirm signup_count increments by 1

T3. Invalid/stale cookie
- set referral_code cookie to invalid code
- register new user
- confirm no crash
- confirm referral fields remain null
- confirm signup_count does not increment

T4. No overwrite
- user already has referred_by_agent_id set
- cookie exists
- confirm existing referral is not overwritten
- confirm signup_count does not increment

T5. No duplication
- same valid referral should not create duplicate signup attribution or duplicate count increments for one user creation path

AFTER IMPLEMENTATION, REPORT:
1. exact files and methods patched
2. exact cookie settings used
3. fallback priority order implemented
4. whether existing session behavior stayed unchanged
5. how invalid/stale cookies behave
6. results of all 5 tests

DELIVERABLE:
Only cookie fallback for referral attribution on capture + signup persistence fallback. No other referral enhancements.