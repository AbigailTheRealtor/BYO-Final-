Replit Instructions — Restore Blue Listing Type Icon (Do NOT Break Tooltip Fix)
Goal

Listing Type field should show the blue document icon (like before)

The hover tooltips on Bidding Period / Traditional must remain working

No other fields/icons should change

SAFEGUARDS

DO NOT:

change any tooltip code added for option hover

change Listing Type values or logic

change icon library globally (site-wide)

change styles for all icons

Fix must be scoped to Listing Type field icon only.

Step 1 — Identify what icon is being used now vs before

In the Listing Type field label/left icon area, locate the icon markup.

Common patterns:

FontAwesome: <i class="fa ...">

Bootstrap Icons: <i class="bi bi-...">

SVG inline: <svg ...>

React/Lucide component: <FileText /> etc.

✅ Confirm:

the current icon name/class

whether it lost a color class (e.g., text-primary, text-info, text-blue-500)

whether CSS is overriding color/fill

Step 2 — Restore the original icon (preferred)

If you already had a known good icon used elsewhere (like the first screenshot), reuse that exact icon class/component.

Examples of “document” icon equivalents:

FontAwesome: fa-file / fa-file-lines / fa-file-alt

Bootstrap Icons: bi-file-earmark-text

Lucide: FileText

Action:

Replace the current icon with the original document icon used previously in the Listing Type field.

Step 3 — Restore blue styling ONLY for Listing Type icon

Whatever system you use, apply the blue styling ONLY to that icon.

If using Bootstrap-style classes

Add back the class that made it blue:

text-primary (or whatever your platform used)

Example:

<i class="bi bi-file-earmark-text text-primary"></i>

If using Tailwind

Apply your blue class:

text-sky-500 or your exact platform blue

Example:

<FileText className="text-sky-500" />

If using SVG

Make sure the icon uses currentColor and the wrapper sets the color:

.listing-type-icon { color: #00BFFF; }
.listing-type-icon svg { fill: currentColor; stroke: currentColor; }


✅ Scope this CSS to Listing Type only (e.g., .listing-type-icon inside the Listing Type field container).

Step 4 — Make sure the tooltip edit didn’t change global icon styles

Check if the dev added/changed CSS like:

.select-icon { color: #333; }

.form-icon svg { fill: #666; }

✅ Fix:

Revert any global change that affected icons everywhere

Replace it with a Listing Type–only selector

Step 5 — QA Checklist (must pass)

Listing Type shows blue document icon

Meeting Preferences and other fields remain unchanged

Bidding Period / Traditional hover tooltips still show immediately

No layout shifts or spacing changes

Acceptance Criteria

✅ Listing Type icon matches the original blue document icon style
✅ Tooltip behavior remains fixed
✅ No global icon color/style changes