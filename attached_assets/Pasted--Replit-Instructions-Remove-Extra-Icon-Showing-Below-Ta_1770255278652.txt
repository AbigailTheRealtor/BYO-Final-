## Replit Instructions — Remove Extra Icon Showing Below “Target Closing Timeframe” (Hire a Buyer’s Agent → Create Listing)

**Issue:** On **Hire a Buyer’s Agent** during **Create Listing**, when user selects **Yes** for **“Buyer Open to Purchasing an Assignment Contract:”**, an **extra/duplicate icon** appears **below “Target Closing Timeframe”** (right above **“Offered Financing/Currency”**). This icon should **not** be there.

**Goal:** Remove the extra icon so only the correct field icons remain.

---

# SAFEGUARDS (Non-Negotiable)

1. **UI-only fix** — do **not** change database schema, stored values, validation rules, or business logic.
2. **Do not remove** the legitimate icons for:

   * Target Closing Timeframe (calendar icon)
   * Offered Financing/Currency (money icon)
3. **Do not affect** Residential/Income/Commercial/Business/Vacant Land logic; this is purely a visual cleanup.
4. Fix must not break:

   * Draft save / reload
   * Submit
   * Field required indicators (*)
   * Tooltips (ⓘ)

---

# Steps to Fix (Implementation)

### Step 1 — Locate the exact UI file/component

Find the **Create Listing** view/component for **Hire a Buyer’s Agent** where these fields render:

* Acceptable Special Sale Provisions
* Buyer Open to Purchasing an Assignment Contract
* Assignment Fee to Broker
* Target Closing Timeframe
* Offered Financing/Currency

✅ Likely locations (examples — use your project’s actual paths):

* `resources/views/...buyer...create...blade.php`
* `resources/views/livewire/...buyer...auction...blade.php`
* `app/Http/Livewire/...Buyer...php` (if icons are injected via component config)

Search the codebase for one of these labels:

* `Target Closing Timeframe`
* `Offered Financing/Currency`
* `Buyer Open to Purchasing an Assignment Contract`

---

### Step 2 — Identify the stray icon markup

The extra icon is appearing **between** the “Target Closing Timeframe” field and the “Offered Financing/Currency” field.

Look for an orphaned icon element such as:

* An `<i>` tag (FontAwesome / Bootstrap icons)
* An inline `<svg>`
* A reusable “field icon” component included twice
* A leftover `<span class="...icon...">` block
* A duplicated “prepend” container (common in input groups)

Common patterns to look for:

* A second icon block rendered outside the input group
* A conditional block like:

  * `@if($buyerOpenAssignmentContract === 'Yes') ... icon ... @endif`
* A component include that is placed **after** the closing timeframe input:

  * `@include('components.icon-money')`
  * `<x-input-icon ... />`

✅ The fix is to **remove the orphaned/duplicate icon element** that renders on its own line.

---

### Step 3 — Remove only the incorrect icon (do not change field icons)

**Remove only** the icon markup that renders as a standalone row/line.

**Do NOT** remove:

* The calendar icon inside the “Target Closing Timeframe” input group
* The money icon inside the “Offered Financing/Currency” input group

If icons are controlled via a shared component, ensure:

* “Offered Financing/Currency” still has its icon
* Only the extra duplicate icon line is removed

---

### Step 4 — Confirm no CSS is causing a duplicate display

If you don’t see duplicate markup, check if CSS is accidentally showing a hidden icon:

* Inspect element in browser dev tools
* Confirm if an element is being displayed due to a class like:

  * `.d-block` / `.show` / `.visible`
* If it’s CSS-driven, fix by tightening the selector so it only applies inside the intended input group (not as a standalone line).

✅ Still UI-only—no logic changes.

---

# Testing Checklist (Required)

1. Go to **Hire a Buyer’s Agent → Create Listing**
2. Under **Acceptable Special Sale Provisions**, select **Assignment Contract**
3. Set **Buyer Open to Purchasing an Assignment Contract = Yes**
4. Confirm:

   * ❌ The extra icon line under “Target Closing Timeframe” is gone
   * ✅ The “Target Closing Timeframe” field still shows its correct icon
   * ✅ The “Offered Financing/Currency” field still shows its correct icon
5. Repeat with **Buyer Open to Purchasing an Assignment Contract = No**

   * No stray icons appear
6. Save Draft → reload → confirm no icon duplication
7. Submit → confirm no UI break

---

# Deliverable

* Provide the **file path(s)** changed
* Provide a **before/after screenshot** of that section
* Confirm tests passed (Yes/No paths + Draft + Submit)

---

If you paste the file path Replit is using for the Buyer Create Listing UI (or the Livewire component name), I’ll rewrite this again with a **precise “remove this block / keep this block”** instruction so they don’t have to hunt.
