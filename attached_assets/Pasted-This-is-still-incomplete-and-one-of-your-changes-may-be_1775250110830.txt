This is still incomplete and one of your changes may be incorrect.

You fixed only a few display/count items, but you did NOT complete the required parity work.

Also, I need you to re-check Seller terms carefully:

* You changed Seller max terms from 16 to 15
* My requirement was that Seller residential can reach 16 when all relevant original fields are filled
* So do NOT assume removing additional_details_broker means the total should become 15
* Re-audit the Seller term groups from the actual original listing fields and confirm the correct denominator from real field mapping, not from the old helper structure alone

Now do a third pass and address ONLY the remaining unresolved items below.

Rules:

* Do not touch unrelated files
* Do not stop after partial fixes
* Do not claim “already correct” unless you verify it in the active rendered file and logic
* For each item, either fix it or prove with exact file/section and exact rendered behavior that it is already correct

Remaining unresolved items:

1. Buyer Match Score card parity

* Verify Buyer uses the full Tenant-style Match Score card, not a partial imitation
* Must match Tenant structure, badge, wording, Services Match box, Terms Match box, and supporting text

2. Seller and Landlord Match Score parity

* Verify Seller and Landlord no longer use any old Match Summary layout anywhere in the active bid card/private view
* Must match Tenant exactly

3. Seller terms denominator re-audit

* Rebuild the Seller residential term denominator from actual listing fields
* Confirm whether correct maximum is 16 or 15 based on real original fields
* Show exact term keys/groups counted
* Do not rely on old helper array assumptions

4. Landlord terms denominator

* Verify Landlord can correctly reach 17 when all relevant original fields are filled
* Show exact counted fields/groups

5. Buyer terms denominator

* Verify Buyer no longer shows 0/0 and dynamically counts actual original filled fields
* Show exact counted fields/groups for residential example

6. Landlord missing field

* Verify Landlord shows:
  Tenant's Broker Commission Fee:
  directly after
  Tenant's Broker Commission Structure:
* If missing, add it in display and comparison logic

7. Services counting logic

* Verify across Buyer / Seller / Landlord:

  * 1 checkmark = 1 service
  * each digital enhancement sub-option counts individually
  * checked Other counts as 1
  * checked “Other – Specify additional services as needed” counts as 1
  * each custom additional service entry counts as 1
* Show exact service extraction logic or fix it

8. False extra-services / false extra-terms logic

* Verify matched original items are not mislabeled as extra
* Verify extras do not inflate match %
* Fix if needed

9. Buyer private Broker Compensation panel

* Verify Buyer private panel truly matches Tenant grouped format
* If still legacy layout, replace it

10. Styling parity

* Verify Buyer and Landlord lease-option compensation labels/values are black, not blue
* Fix if needed

11. Buyer action buttons

* Verify Buyer has Accept / Counter / Reject in same placement/style/functionality as Tenant
* Fix if missing

12. Seller section order

* Verify Seller order is:
  Broker Compensation & Agency Agreement Terms
  Offered Services
  Additional Terms
  Additional Details
* Fix if not

13. Upload rendering

* Verify business card and uploaded materials render correctly in active views for all roles
* Verify edit mode preserves them
* Fix if not

14. Canonical score keys

* Verify active views consistently use canonical keys:
  services_matched_count
  services_baseline_total
* Remove alias dependence if still present

Required output:
Give a numbered checklist 1 through 14.
For each item provide:

* status: fixed / already correct / needs follow-up
* exact files checked
* exact files changed
* exact reason
* if verified, say what rendered behavior now proves it

Do not give a short summary only.
