hat to send back to Replit now (copy–paste)

We’re still not hitting the right template for the Income tabs.

I can see from your log that tenant-agent-auction.blade.php includes:

@include('livewire.tenant-agent-auction-tabs.flat-fee.location-and-meeting-details')
@include('livewire.tenant-agent-auction-tabs.flat-fee.service_selection_pricing')
@include('livewire.tenant-agent-auction-tabs.commission-based.additional-details')
@include('livewire.tenant-agent-auction-tabs.commission-based.tenant-info')


That means the actual tab content for /hire/agent/auction/buyer is coming from:

resources/views/livewire/tenant-agent-auction-tabs/***.blade.php

not from the hire-buyer-agent/buyer-agent-auction-tabs files where you added the yellow debug box.

That explains why I don’t see the yellow debug box – it was added to a file that’s not used on this page.

Also, the line you added in tenant-agent-auction.blade.php:

$normalizedPropertyType = in_array($property_type, ['Income', 'Income Property', 'income', 'income_property'])
    ? 'Residential'
    : $property_type;


Maps Income to Residential, which I do not want, and

Isn’t actually used by the tab partials.

What I actually need

For the auction Buyer Agent wizard on /hire/agent/auction/buyer:

Tabs 3–7 (Purchasing Terms, Services, Additional Details, Broker Compensation, Buyer Information) should use the Income-specific layouts when property type is Income.

The Income tabs already exist – they just aren’t being selected.

Please do the following, focusing only on the tenant-agent-* files:

1. Add debug inside the correct Services tab file

Open:

resources/views/livewire/tenant-agent-auction-tabs/flat-fee/service_selection_pricing.blade.php


(If that’s not the exact path, use:

ls resources/views/livewire/tenant-agent-auction-tabs


and open the file that corresponds to the Services tab.)

At the very top of that Services tab file, add:

<div style="background:yellow;color:black;font-weight:bold;padding:6px;">
    DEBUG SERVICES (tenant tabs): property_type = "{{ $property_type ?? 'NULL' }}"
</div>


Then clear views/cache and reload /hire/agent/auction/buyer, pick Income, click Services.

I should now see this yellow debug box. If I don’t, it means we’re still editing the wrong file.

2. Once the yellow box shows, wire Income to the Income partials

In that same Services tab file, find any @if / @elseif blocks that check $property_type or similar.

For example (pseudo-code, actual partial names may differ):

@if ($property_type === 'Residential')
    @include('livewire.hire-buyer-agent.services-residential-income-mix') 
@elseif ($property_type === 'Commercial')
    ...
@endif


Change this so that Income has its own branch that includes the Income services partial:

@php
    $type = strtolower($property_type ?? '');
@endphp

@if (in_array($type, ['income', 'income property', 'income_property', 'income property 2-4 units']))
    @include('livewire.hire-buyer-agent.services-income-full-service') {{-- use the actual Income services partial --}}
@elseif ($type === 'residential')
    @include('livewire.hire-buyer-agent.services-residential-full-service')
@elseif ($type === 'commercial')
    @include('livewire.hire-buyer-agent.services-commercial-full-service')
@elseif ($type === 'vacant land' || $type === 'vacant_land')
    @include('livewire.hire-buyer-agent.services-vacant-land-full-service')
@endif


Do the same pattern in the other tab files under tenant-agent-auction-tabs that correspond to:

Purchasing Terms

Additional Details

Broker Compensation

Buyer Information

Each one should have an explicit Income branch that includes the correct Income partial (the same ones used anywhere Income was working previously).

Please do not normalize Income to Residential anymore – I want Income to use its own partials, not Residential’s.

After each change, clear caches:

php artisan view:clear && php artisan cache:clear


Then I’ll re-test /hire/agent/auction/buyer with property type = Income and make sure all tabs 3–7 show the correct Income content.

If you’d like, once they manage to get that yellow debug box to appear in the Services tab, tell me what it prints for property_type and we can write exact @if conditions for Income using that value.