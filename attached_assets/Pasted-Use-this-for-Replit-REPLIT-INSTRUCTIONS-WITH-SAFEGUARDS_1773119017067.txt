Use this for Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A SELLER’S AGENT ONLY — FULL ROOT-CAUSE FIX FOR SALE TERMS CONDITIONAL LOGIC

### SCOPE

These **Hire a Seller’s Agent** Sale Terms issues have been reported multiple times and are **still not fully resolved**.

This time, do **not** apply another partial patch.
Please **fully investigate the conditional logic and field dependencies** in the Seller **Sale Terms** tab and fix the actual root cause.

Fix **only** the Seller flow issues listed below.

Do **not** modify:

* Hire a Buyer’s Agent
* Hire a Landlord’s Agent
* Hire a Tenant’s Agent

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename or delete database columns.**
2. **Do not change labels, tooltips, or option values except where needed to restore intended Seller behavior.**
3. **Do not broad-refactor unrelated shared components unless absolutely necessary for this Seller-only fix.**
4. **Do not remove or alter saved drafts, listings, or bids.**
5. **Do not break working fields while fixing the broken ones.**
6. **Do not mark this complete based only on code inspection.**
7. **You must verify the fixes visually in the browser.**
8. **Do not treat delayed appearance as fixed. These dependent fields must appear immediately.**
9. **This must be treated as a dependency / conditional rendering investigation, not just a cosmetic field fix.**

---

# CORE PROBLEM

Something in the Seller **Sale Terms** conditional logic is cross-wired.

The current behavior strongly suggests that fields are incorrectly tied to the wrong trigger field, delayed by rerender timing, or inside the wrong conditional blocks.

This needs a proper root-cause investigation and correction.

---

# ISSUES TO FIX

## ISSUE 1 — DESIRED SALE PRICE DISAPPEARS WHEN OCCUPANT TYPE = TENANT

### Current issue

If the user selects **Tenant** for **Occupant Type**, **Desired Sale Price** disappears.

That should not happen.

### Required fix

* **Desired Sale Price** must remain visible regardless of selecting **Tenant** under **Occupant Type**, unless there is an explicit intended rule saying otherwise
* selecting Occupant Type must not hide unrelated Sale Terms fields
* Occupant Type must only affect the fields it is actually supposed to affect

### What to check

1. whether **Desired Sale Price** is incorrectly placed inside an Occupant Type conditional block
2. whether changing Occupant Type triggers a rerender that hides unrelated fields below it
3. whether a wrapper/container below Occupant Type is conditionally removed by mistake
4. whether the wrong `@if`, `@elseif`, or Livewire property is controlling visibility

---

## ISSUE 2 — SPECIAL SALE PROVISION → OTHER SHOULD IMMEDIATELY POP UP

### Current issue

If the user selects **Other** under **Special Sale Provision**, the custom input is not appearing immediately and may be tied to the wrong field/event.

### Required fix

When **Other** is selected under **Special Sale Provision**:

* the custom input must appear **immediately**
* it must appear directly under the correct field
* it must hide immediately when **Other** is unselected
* it must remain visible on edit/reload if **Other** remains selected
* it must save and reload correctly

### What to check

1. whether the **Other** conditional is tied to the correct Seller property
2. whether the custom input block is placed under the correct field
3. whether the visibility is delayed because it relies only on server-side rerender timing
4. whether the field is cross-wired to another condition like Occupant Type or Target Closing Timeframe

---

## ISSUE 3 — ASSIGNMENT CONTRACT SHOULD IMMEDIATELY POP UP SELLER UNDER CONTRACT FOR ASSIGNMENT

### Current issue

If the user selects **Assignment Contract** under **Special Sale Provision**, the sub-question:

* **Seller Under Contract for Assignment:**

does not pop up immediately.

### Required fix

When **Assignment Contract** is selected:

* **Seller Under Contract for Assignment:** must appear **immediately**
* it must hide immediately when Assignment Contract is removed
* it must remain visible on edit/reload if Assignment Contract remains selected
* its saved value must persist correctly

### What to check

1. whether the Assignment Contract conditional is tied to the correct property
2. whether the condition is correctly checking array membership
3. whether the dependent sub-question block is under the correct conditional section
4. whether the visibility is delayed by rerender timing
5. whether the question is incorrectly tied to another field such as Target Closing Timeframe

---

## ISSUE 4 — OFFERED FINANCING/CURRENCY QUESTIONS SHOULD IMMEDIATELY POP UP

### Current issue

As soon as the user selects an option under **Offered Financing/Currency**, the corresponding questions should pop up.

Right now they do not.

They may be:

* delayed
* tied to the wrong field
* tied to Occupant Type
* inside the wrong conditional block

### Required fix

When a financing/currency option is selected:

* the corresponding questions must appear **immediately**
* they must hide immediately when the trigger option is removed
* they must not depend on Occupant Type unless there is an explicit intended rule
* they must remain visible on edit/reload if the trigger option remains selected
* entered values must save and reload correctly

### What to check

1. whether Offered Financing/Currency visibility or sub-questions are incorrectly nested under Occupant Type
2. whether the wrong property is being watched for conditional display
3. whether the dependent sections are inside the wrong wrapper
4. whether rerender timing is causing the delayed appearance
5. whether the options themselves are correct but the sub-question blocks are listening to the wrong field

---

## ISSUE 5 — TARGET CLOSING TIMEFRAME APPEARS TO BE TIED TO WRONG TRIGGERS

### Current issue

It looks like **Target Closing Timeframe** may be tied to:

* **Other** from Special Sale Provision
* Assignment Contract sub-questions

That suggests fields are cross-wired.

### Required fix

* **Target Closing Timeframe** must only be controlled by its intended logic
* it must not appear/disappear because of **Other** or **Assignment Contract** unless that is explicitly intended
* unrelated dependent sections must be separated correctly

### What to check

1. whether Target Closing Timeframe is placed inside the wrong conditional block
2. whether special sale provision conditions are leaking into unrelated field wrappers
3. whether closing tags / wrapper structure are causing later fields to fall inside the wrong conditional

---

# REQUIRED ROOT-CAUSE INVESTIGATION

Please do a full investigation of the **Seller Sale Terms tab structure** and confirm:

1. which field controls which dependent block
2. whether any dependent blocks are inside the wrong conditional
3. whether any wrappers/divs are misplaced so later fields are being hidden/shown by the wrong trigger
4. whether delayed appearance is caused by:

   * wrong Livewire property watch
   * message.processed timing
   * rerender lag
   * missing immediate client-side visibility toggle
5. whether Occupant Type is unintentionally controlling fields below it

This needs to be fixed at the dependency level, not patched one field at a time without tracing the structure.

---

# REQUIRED IMPLEMENTATION APPROACH

## A. Correct field ownership

Make sure each dependent block is tied only to its correct trigger:

* **Special Sale Provision → Other** controls only its own custom input
* **Special Sale Provision → Assignment Contract** controls only **Seller Under Contract for Assignment**
* **Offered Financing/Currency** controls only its own dependent questions
* **Occupant Type** does not hide unrelated Sale Terms fields like Desired Sale Price
* **Target Closing Timeframe** is not tied to Special Sale Provision conditions unless explicitly intended

## B. Immediate display behavior

For all these dependent fields, visibility must update **immediately on selection**, not after a delayed rerender.

## C. Rerender-safe persistence

The fields must still remain correct after:

* tab switches
* draft save
* edit reopen
* normal Livewire rerenders

---

# REQUIRED TESTING — BROWSER ONLY

## A) OCCUPANT TYPE TEST

1. open **Hire a Seller’s Agent**
2. go to **Sale Terms**
3. confirm **Desired Sale Price** is visible
4. select **Tenant** under **Occupant Type**
5. confirm **Desired Sale Price** remains visible
6. confirm unrelated lower fields do not disappear

---

## B) SPECIAL SALE PROVISION → OTHER TEST

1. select **Other** under **Special Sale Provision**
2. confirm the custom input appears **immediately**
3. unselect **Other**
4. confirm the custom input hides immediately
5. reselect **Other**
6. enter custom value
7. save
8. reopen edit
9. confirm the input remains visible with saved value

---

## C) ASSIGNMENT CONTRACT TEST

1. select **Assignment Contract**
2. confirm **Seller Under Contract for Assignment:** appears **immediately**
3. unselect it
4. confirm the sub-question hides immediately
5. reselect it
6. save
7. reopen edit
8. confirm the sub-question remains visible if still selected

---

## D) OFFERED FINANCING/CURRENCY TEST

1. select financing/currency option(s) that should trigger questions
2. confirm the corresponding questions appear **immediately**
3. remove the trigger option
4. confirm the questions hide immediately
5. reselect it
6. save
7. reopen edit
8. confirm the questions remain visible if the trigger options remain selected

---

## E) TARGET CLOSING TIMEFRAME / CROSS-WIRING CHECK

1. select **Other** under Special Sale Provision
2. confirm only the correct Other input appears
3. select **Assignment Contract**
4. confirm only **Seller Under Contract for Assignment** appears
5. confirm **Target Closing Timeframe** is not incorrectly affected unless explicitly intended
6. change **Occupant Type**
7. confirm unrelated dependent sections are not broken

---

# SUCCESS CRITERIA

This task is only complete when all of the following are true in **Hire a Seller’s Agent**:

* **Desired Sale Price** no longer disappears when **Occupant Type = Tenant**
* **Special Sale Provision → Other** custom input appears immediately
* **Assignment Contract** immediately shows **Seller Under Contract for Assignment**
* **Offered Financing/Currency** immediately shows its corresponding questions
* **Target Closing Timeframe** is no longer cross-wired to unrelated triggers
* **Occupant Type** no longer breaks unrelated Sale Terms fields
* create / draft / edit / save / reopen all remain stable

---

# FINAL RULE

Do **not** respond that this is fixed based on code inspection or a partial improvement.

This needs a true dependency/conditional logic fix.

Please verify visually in the browser that:

1. selecting **Tenant** under **Occupant Type** does not hide **Desired Sale Price**
2. **Other**, **Assignment Contract**, and **Offered Financing/Currency** all trigger their correct dependent fields **immediately**
3. unrelated fields are no longer cross-wired.
