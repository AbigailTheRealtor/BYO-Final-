Fix the “Save Documents” button rendering/state on the Accepted Bid Summary optional document sharing section.

Current issue:
- The upload can succeed, and success messages appear
- But the “Save Documents” button still renders as a blank/white rectangle
- This makes the UI look broken even when upload behavior works

This task is focused on the button’s rendering, enabled/disabled state, and visible label only.

CRITICAL SAFEGUARD:
- Do not modify Accepted Bid Summary business logic
- Do not modify upload storage logic unless required for button state binding
- Do not modify listing creation, bidding, counter, accept/reject, or messaging flows
- This is a UI/state fix for the document submit control

==================================================
PART 1 — AUDIT THE BUTTON ELEMENT
==================================================

Inspect the Accepted Bid Summary document form and identify:

1. What element is being used for “Save Documents”
   - button
   - input[type=submit]
   - Blade component
   - Livewire component/button

2. Why it renders as a blank white box:
   - text color same as background
   - missing label text
   - hidden span/icon
   - wrong CSS class
   - disabled state class always applied
   - conditional class not updating after file selection

3. Whether the success message is appearing from the same form submit cycle

Return the exact file(s) and markup responsible.

==================================================
PART 2 — FIX BUTTON VISUAL RENDERING
==================================================

Required result:
The button must visibly render as a normal submit button with readable text.

Requirements:
1. Button must display a visible label:
   - preferred: “Save Documents”

2. Button must have proper contrast:
   - readable text color
   - visible background color
   - visible border if secondary style is intended

3. Button must not render as a blank white rectangle

4. Keep styling consistent with the platform’s primary/secondary button system

==================================================
PART 3 — FIX ENABLE / DISABLE STATE
==================================================

Required behavior:
1. If no valid file is selected:
   - button may be disabled, but must still look intentionally disabled
   - disabled styling should be clear (not broken-looking)

2. If a valid file is selected:
   - button should become enabled
   - visual style should update clearly

3. After successful save:
   - button should remain visible and usable for future uploads
   - it should not become an empty white block

4. If validation fails:
   - button state should recover correctly without requiring a page refresh

==================================================
PART 4 — LABEL / STATE BINDING
==================================================

Check whether the button text is conditionally swapped during:
- idle
- uploading
- saving
- success

If there is a loading state, make sure it still shows visible text such as:
- “Saving…”
- or “Save Documents”

Do not allow a state where the button has no visible text.

==================================================
PART 5 — STYLING CONSISTENCY
==================================================

Apply safe UI styling so the button always looks intentional.

Examples of acceptable states:
- enabled primary button
- disabled muted button
- loading button with text

Do not leave it unstyled.

==================================================
PART 6 — VERIFY THESE CASES
==================================================

After the fix, verify:

1. No file selected
   - button visible
   - disabled styling clear

2. Valid file selected
   - button visible
   - enabled styling clear

3. Save clicked successfully
   - success message appears
   - button still visible afterward

4. Invalid file / oversized file
   - friendly error
   - button not visually broken

==================================================
OUTPUT REQUIRED
==================================================

Return:
1. exact file(s) changed
2. what caused the blank white rectangle
3. what now controls the button label and visual state
4. confirmation that:
   - “Save Documents” text is visible
   - button enabled/disabled states are visually correct
   - successful uploads no longer leave the button looking broken