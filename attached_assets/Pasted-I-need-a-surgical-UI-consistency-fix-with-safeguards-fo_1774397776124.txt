I need a surgical UI consistency fix with safeguards for Listing Status colors.

GOAL:
Make the Pending status color on LISTING CREATION / EDIT match the Pending status color used on the ACTUAL LISTING page.

APPLIES TO:
- Seller
- Buyer
- Landlord
- Tenant

IMPORTANT:
The correct Pending color is the one already showing on the ACTUAL LISTING page.
Use the actual listing Pending color as the source of truth.

That means:
- listing creation Pending color must be updated to match actual listing Pending color
- listing edit Pending color must also match actual listing Pending color if status is shown there

HARD SAFEGUARDS:
- Do NOT rename DB columns
- Do NOT run migrations
- Do NOT refactor architecture broadly
- Do NOT change status logic
- Do NOT change status transitions
- Do NOT change Active / Hired Agent / Expired behavior unless a shared badge class safely requires consistency
- Do NOT touch unrelated search/listing cards unless they share the same status badge partial
- Make .bak copies before editing files
- Keep this focused on Pending badge color consistency only

TASK 1 — FIND THE SOURCE OF THE PENDING COLOR ON THE ACTUAL LISTING PAGE
First identify exactly where the Pending status styling is defined on the actual listing page.

Need to determine whether Pending color there comes from:
- hardcoded blade classes
- shared status badge component
- conditional class helper
- CSS utility class mapping
- Livewire/component-specific markup

Do NOT guess.

IMPORTANT:
The actual listing Pending color is the correct target color.
Use that as the source of truth.

TASK 2 — FIND THE SOURCE OF THE PENDING COLOR ON LISTING CREATION / EDIT
Find where the Pending badge/status styling is defined on:
- listing creation pages
- listing edit pages
for:
- Seller
- Buyer
- Landlord
- Tenant

Need to determine whether:
- creation/edit uses a separate class mapping
- creation/edit hardcodes a different Pending color
- or a shared helper is being overridden

TASK 3 — MAKE CREATION / EDIT PENDING MATCH ACTUAL LISTING PENDING
Update listing creation and edit so the Pending badge/status uses the exact same styling/classes as Pending on the actual listing page.

IMPORTANT:
- Use the ACTUAL LISTING Pending color as the source of truth
- Do not make a close approximation
- Make it match exactly

TASK 4 — KEEP THE FIX MINIMAL
This is a color consistency fix only.

Do NOT change:
- the status text
- status order
- badge icon
- badge size
- padding
- font size
- status logic
- which status is displayed

Only fix the Pending color inconsistency so creation/edit matches actual listing.

TASK 5 — CHECK WHETHER THIS SHOULD BE CENTRALIZED
If actual listing and creation/edit are using separate Pending class mappings, and there is a safe minimal way to reuse one shared Pending style source, that is preferred.

But:
- do NOT do a broad refactor
- minimal-risk consistency fix is preferred

DO NOT DO:
- do not change DB schema
- do not alter status resolution logic
- do not accidentally recolor Active / Hired Agent / Expired unless the shared style source safely requires it
- do not break current listing header layout

TESTING REQUIRED:

A. ACTUAL LISTING PAGE
For each role:
- Seller listing with Pending status
- Buyer listing with Pending status
- Landlord listing with Pending status
- Tenant listing with Pending status

Confirm the exact Pending color currently shown there.
That is the source-of-truth color.

B. CREATION PAGE
For each role if applicable:
- Seller
- Buyer
- Landlord
- Tenant

Expected:
- Pending status color on creation now matches actual listing exactly

C. EDIT PAGE
For each role if applicable:
- Seller
- Buyer
- Landlord
- Tenant

Expected:
- Pending status color on edit now matches actual listing exactly

D. REGRESSION CHECK
Verify:
- Active still looks correct
- Hired Agent still looks correct
- Expired still looks correct
- no badge layout regressions

SUCCESS CRITERIA:
- Pending color on listing creation/edit exactly matches Pending color on actual listing
- consistent across Seller, Buyer, Landlord, and Tenant
- no status logic changes
- no DB changes
- no unrelated regressions