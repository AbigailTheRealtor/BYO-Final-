Perfect — those links tell us exactly which flow to target.

Because your routes are:

* **Create:** `/hire/agent/auction/buyer`
* **Edit:** `/buyer/agent/auction/edit/115/buyer`
* **View:** `/buyer/agent/auction/view/115`

this is a **Buyer Auction flow**, and based on your shared setup, the safest assumption is:

* **Create/Edit form UI** is coming from the shared Livewire auction flow and Buyer-specific tab blades
* **Actual listing view** is coming from the Buyer auction view blade/controller path

So below are the **updated Replit instructions with route-specific safeguards**.

---

# Hire a Buyer’s Agent Fixes — Instructions with Safeguards

## Goal

Fix these two issues only:

### 1) All Buyer property types

On the **actual listing view**, `Acceptable Property Conditions` should display:

* `Older but Clean & Well Maintained`

and **not**:

* `Older but Clean`

### 2) Hire a Buyer’s Agent — Income property

On **listing creation**, reorder this section to match the actual listing order.

### Current wrong order on create:

* Required Property or Business Assets:
* Acceptable Number of Units:
* Minimum Annual Net Income Needed:
* Minimum Cap Rate Needed:
* Acceptable Unit Type:
* Additional Details:

### Correct order:

* Required Property or Business Assets:
* Acceptable Number of Units:
* Acceptable Unit Type:
* Minimum Annual Net Income Needed:
* Minimum Cap Rate Needed:
* Additional Details:

---

# Hard Safeguards

## 1. Confirm exact routes before editing

Use the provided routes as the source of truth:

* **Create route:** `/hire/agent/auction/buyer`
* **Edit route:** `/buyer/agent/auction/edit/{id}/buyer`
* **View route:** `/buyer/agent/auction/view/{id}`

This means:

* **Do not edit Seller, Tenant, or Landlord files**
* **Do not touch legacy/non-auction Buyer files**
* **Do not change anything outside the Buyer auction create/edit/view path**

---

## 2. Do not make architecture changes

Do **not**:

* rename DB columns
* add migrations
* change validation rules
* refactor component structure
* touch Select2/Livewire sync unless absolutely required
* change any unrelated labels/options

This is a **surgical text/order fix only**.

---

## 3. Make backups first

Before editing, duplicate each file you change with a `.bak` copy.

Example:

* `filename.blade.php.bak_buyer_conditions_order_fix`

---

# Part 1 — Fix listing display text for Acceptable Property Conditions

## What to fix

On the **actual Buyer listing view page** (`/buyer/agent/auction/view/115`), the display text currently shows:

* `Older but Clean`

It should show:

* `Older but Clean & Well Maintained`

---

## Safe approach

This should be treated as a **display-label fix**, not a data/storage fix.

### Important:

Do **not** change the saved database value unless you confirm the app is actually storing the wrong label.
First assume the stored value is something like:

* `Older but Clean`
  or
* a mapped key that is being rendered incorrectly

The safest fix is to correct the **view mapping/rendered output** for Buyer listing display.

---

## Step A — Find the Buyer listing view file actually used by this route

Search globally in Replit for:

```php
Older but Clean
```

Also search for:

```php
acceptable_property_conditions
```

and

```php
property_condition
```

Look specifically for the file used by:

* `/buyer/agent/auction/view/{id}`

Likely candidates are Buyer auction view blades or shared listing view partials.

---

## Step B — Only change the displayed label

Wherever the Buyer listing view renders the label, replace only the displayed text:

### Replace:

```php
Older but Clean
```

### With:

```php
Older but Clean & Well Maintained
```

---

## Safety rule

If the code uses an array like:

```php
'Older but Clean' => 'Older but Clean'
```

change only the label/output side, not the underlying storage key, unless both create/edit already use the longer text and the listing view is just outdated.

Preferred safe pattern:

```php
'Older but Clean' => 'Older but Clean & Well Maintained'
```

or update the display conditional/mapping only.

---

## Do not do this

Do not mass-replace `Older but Clean` across the entire project without checking context.
Only fix the **Buyer auction listing display path** unless you verify the same Buyer-specific mismatch exists in create/edit too.

---

# Part 2 — Reorder Buyer Income fields on listing creation

## What to fix

On the **Buyer create page**:

* `/hire/agent/auction/buyer`

for **Income** property type, reorder the displayed fields to match the actual listing order.

---

## Step A — Find the create form file actually used

Because your create route is:

* `/hire/agent/auction/buyer`

this is likely running through the shared auction component.

Search for these exact labels:

```php
Required Property or Business Assets
```

```php
Acceptable Number of Units
```

```php
Minimum Annual Net Income Needed
```

```php
Minimum Cap Rate Needed
```

```php
Acceptable Unit Type
```

Likely location:

* shared Buyer property details tab
* shared commission-based property details blade
* Buyer conditional section inside shared auction tabs

---

## Step B — Reorder HTML blocks only

Do not rewrite logic.
Do not rename any variables.
Do not change `wire:model`, `name`, `id`, conditional wrappers, or validation hooks.

Only move the field blocks so the Income section appears in this exact order:

### Correct order

1. `Required Property or Business Assets:`
2. `Acceptable Number of Units:`
3. `Acceptable Unit Type:`
4. `Minimum Annual Net Income Needed:`
5. `Minimum Cap Rate Needed:`
6. `Additional Details:`

---

## Important

This is a **UI order fix only**.

That means:

* same fields
* same bindings
* same conditions
* same save behavior
* only different display order

---

## Safety check while moving blocks

When moving the `Acceptable Unit Type` block upward:

* keep it inside the same **Income property-type conditional**
* keep any surrounding grid/row wrappers balanced
* do not accidentally move `Additional Details` into the wrong group
* do not duplicate the block

If there is a wrapper like:

```php
@if($propertyType === 'Income')
```

or similar, keep all these fields inside that same wrapper.

---

# Route-specific verification plan

## 1. Create page test

Open:

* `/hire/agent/auction/buyer`

Choose **Income** property type.

Verify the section order is now:

* Required Property or Business Assets
* Acceptable Number of Units
* Acceptable Unit Type
* Minimum Annual Net Income Needed
* Minimum Cap Rate Needed
* Additional Details

---

## 2. Save draft / continue edit test

Create a Buyer Income listing with sample values in all of those fields.

Then:

* save draft if available
* reopen edit page:

  * `/buyer/agent/auction/edit/115/buyer`

Verify:

* all values are still present
* no fields disappeared
* no duplicated fields
* order still looks correct if the same section appears in edit

---

## 3. Listing view test

Open:

* `/buyer/agent/auction/view/115`

Verify under `Acceptable Property Conditions` that the listing now shows:

* `Older but Clean & Well Maintained`

not `Older but Clean`

---

# If you find separate create/edit/view files

Use this exact rule:

* **Create route fix** → edit only the create/shared Buyer form blade used by `/hire/agent/auction/buyer`
* **Edit route fix** → only touch if the same wrong order appears there
* **View route fix** → edit only the Buyer auction listing display blade/partial used by `/buyer/agent/auction/view/{id}`

Do not assume fixing one automatically fixes the others.

---

# What not to touch

Leave these alone unless you confirm they are directly involved:

* `TenantAgentAuction.php`
* Select2 init scripts
* Livewire hydration code
* validation arrays
* non-Buyer listing templates
* controller logic unrelated to Buyer auction view
* any DB values or migrations

---

# Good final outcome

After this fix:

## Buyer listing view

`Acceptable Property Conditions` shows:

* `Older but Clean & Well Maintained`

## Buyer Income create page

The section order matches the actual listing order:

* Required Property or Business Assets
* Acceptable Number of Units
* Acceptable Unit Type
* Minimum Annual Net Income Needed
* Minimum Cap Rate Needed
* Additional Details

---

# Paste-this-to-Replit version

Use this if you want a clean instruction block to paste directly:

```text
Surgical Buyer Auction fix only. Do not touch Seller, Tenant, Landlord, migrations, DB schema, validation, or Select2 logic.

Routes confirmed:
- Create: /hire/agent/auction/buyer
- Edit: /buyer/agent/auction/edit/{id}/buyer
- View: /buyer/agent/auction/view/{id}

Task 1:
Fix Buyer auction listing view so Acceptable Property Conditions displays:
"Older but Clean & Well Maintained"
instead of:
"Older but Clean"

Important:
- Treat this as a display-label fix first
- Search for: "Older but Clean", "acceptable_property_conditions", and "property_condition"
- Find the exact blade/partial used by /buyer/agent/auction/view/{id}
- Change only the displayed label/mapping for the Buyer auction listing view
- Do not mass replace project-wide unless the same Buyer route path clearly uses the same outdated mapping

Task 2:
Fix Buyer create form order for Income property type on /hire/agent/auction/buyer

Current wrong order:
- Required Property or Business Assets
- Acceptable Number of Units
- Minimum Annual Net Income Needed
- Minimum Cap Rate Needed
- Acceptable Unit Type
- Additional Details

Correct order:
- Required Property or Business Assets
- Acceptable Number of Units
- Acceptable Unit Type
- Minimum Annual Net Income Needed
- Minimum Cap Rate Needed
- Additional Details

Important:
- Search for these exact labels in the Buyer auction/shared create form blade
- Move only the HTML field block for "Acceptable Unit Type" upward
- Do not rename wire:model, name, id, or change any save logic
- Keep all fields inside the same Income property-type conditional
- Do not duplicate blocks or break wrapper divs

Before editing:
- Create .bak copies of each changed file

After editing, test exactly:
1. /hire/agent/auction/buyer → choose Income → verify order
2. Save/open /buyer/agent/auction/edit/{id}/buyer → verify values persist
3. Open /buyer/agent/auction/view/{id} → verify property condition shows "Older but Clean & Well Maintained"
```

If you want, I can also turn this into an even tighter **“check exact files first, then edit only these sections”** version.
