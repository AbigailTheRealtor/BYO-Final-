The Landlord bid modal still does **not** match the Tenant format closely enough.

The main remaining mismatch is the **Services Offered** section. Tenant uses a **categorized bullet-point list**, while Landlord is still rendering services in a **boxed tile/grid layout**. That is not a true Tenant mirror.

## Goal

Make the **Landlord private bid modal** match the **Tenant private bid modal format exactly** in presentation style and structure, while keeping Landlord-specific service names and Landlord-specific compensation fields.

This is a **format/presentation parity fix**, not a scoring or data rewrite.

---

## Scope

Landlord private bid modal / bid detail view only.

Do not modify:

* Tenant files
* Buyer
* Seller
* scoring logic
* data structure
* service catalogs
* saved bid values

---

## Hard safeguards

* No destructive DB changes
* No schema changes
* No dropping/renaming columns
* Do not change field names
* Do not change service wording
* Do not change Landlord-specific compensation content
* Do not change scoring logic
* Do not change match score calculation
* Only update the Landlord modal/detail rendering so it mirrors Tenant presentation exactly

---

## Required parity target

Use the Tenant private bid modal as the exact visual/template reference.

The Landlord modal must match Tenant in:

1. **Overall section flow**

   * Agent Overview & Qualifications
   * Broker Compensation & Agency Agreement Terms
   * Offered Services
   * Agent Presentation & Promotional Materials
   * Agent Information
   * Confidential note / close area

2. **Services Offered format**

   * must use the same **categorized bullet-point list style** as Tenant
   * not boxed cards
   * not tile/grid format
   * services grouped under headings like Tenant does
   * extra services highlighted in the same visual style as Tenant
   * missing services shown in the same visual style as Tenant
   * Additional Services / Other Services shown in the same style as Tenant

3. **Compensation section format**

   * must use the same row-based structured format as Tenant
   * same typography hierarchy
   * same left-label / right-value pattern
   * same mismatch highlighting style
   * no custom alternative layout patterns

4. **Additional Terms / Additional Details**

   * must appear once only
   * same layout treatment as Tenant
   * no duplicate blocks

---

## Specific required fix: Services Offered section

Replace the current Landlord boxed service grid with the same rendering style used in Tenant:

* category heading
* bullet list of included services
* extra services highlighted inline the same way Tenant does
* missing services section shown the same way Tenant does
* additional/custom services listed the same way Tenant does

### Important

Do not convert Landlord services into Tenant services.
Keep the Landlord service names/content exactly as they are.
Only change the **display format** so it mirrors Tenant.

---

## Specific required fix: Match Tenant presentation exactly

Do a side-by-side comparison between:

* `resources/views/hire_tenant_agent/view.blade.php`
* Landlord equivalent view

Then port the **same presentation pattern** for:

* section spacing
* heading style
* list style
* mismatch badges
* extra service highlighting
* missing service block
* additional services block

Do not reinterpret or redesign.

---

## Verification

After the fix, verify with screenshots that the Landlord modal now matches Tenant in the following ways:

1. Services Offered uses categorized bullet-point list format, not tile/grid cards
2. Extra services are highlighted the same way Tenant does
3. Missing services block is displayed the same way Tenant does
4. Compensation section still renders correctly
5. Additional Terms / Additional Details appears once only
6. Tenant view remains unchanged

---

## Deliverable

Provide:

* exact Landlord file(s) changed
* confirmation the boxed/tile service layout was removed
* confirmation the categorized bullet-point format now matches Tenant
* before/after screenshots of the Landlord modal
* confirmation that scoring/data logic was not changed
