TASK — Fix Seller HOA conditional layout still broken

SCOPE:
Create Offer Listing → Seller only
Tab: Tax, Legal & HOA
Section: HOA / Association

ISSUE:
Landlord HOA conditional layout is fixed, but Seller still breaks.

Current Seller problems:
- Fee Frequency = Other shows the follow-up input with placeholder “Select” instead of “Describe the fee frequency”
- Association Approval Required for Purchase appears collapsed/overlapped
- “What Does the Association Fee Include?” overlaps beneath it
- Icons/labels stack incorrectly around approval + fee include fields

DO NOT TOUCH:
- Landlord flow
- Buyer/Tenant flows
- Hire Agent flows
- Counter Terms
- Database/schema

AUDIT FIRST:
Compare Seller HOA partial against the now-working Landlord HOA partial.

Report:
1. Seller HOA Blade file path
2. Landlord HOA Blade file path used as working pattern
3. Exact Seller conditional blocks that differ from Landlord:
   - association_fee_frequency_other
   - association_approval_required
   - association_approval_process
   - association_application_fee
   - association_fee_includes
4. Whether Seller is missing wrapper structure, labels, input-cover, or spacing classes.

FIX:
Make Seller HOA conditional layout match the working Landlord pattern.

Required structure:
- Each conditional follow-up field must be in its own:
  col-12 > form-group mt-2 mb-3 > input-cover
- No nested form-groups
- No duplicate IDs
- No duplicate wire:model
- No duplicate error spans
- Do not place conditional input inside the select wrapper

Specific fixes:
1. Fee Frequency = Other:
   - Placeholder must be “Describe the fee frequency”
   - Must appear directly under Fee Frequency with clean spacing

2. Association Approval Required for Purchase = Yes:
   - Approval Process Details must appear cleanly
   - Association Application Fee must appear cleanly
   - “What Does the Association Fee Include?” must remain below these fields with normal spacing
   - No icon overlap

3. What Does the Association Fee Include:
   - Label and icon must align correctly
   - Select placeholder must remain visible

VALIDATION:
Seller:
- Association Type = Other works
- Fee Frequency = Other works
- Approval Required for Purchase = Yes works
- Toggle back to No/other values hides fields cleanly
- No overlap
- No duplicate labels
- No duplicate error messages

Run:
php -l on edited Blade file

Return:
- Exact file changed
- What differed from Landlord
- Confirmation Landlord was not changed