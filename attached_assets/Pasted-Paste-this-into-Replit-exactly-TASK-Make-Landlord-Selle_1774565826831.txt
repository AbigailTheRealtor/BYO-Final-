Paste this into Replit exactly:

---

# 🚨 TASK: Make Landlord, Seller, and Buyer bid cards, full bid view, and bid actions behave exactly like Tenant

## Goal

Use the **Tenant bid system** as the **source of truth** for all bid display and action behavior, and make **Landlord, Seller, and Buyer** match it from start to finish.

This includes:

* bid card layout
* collapsed summary view
* full bid view
* hidden/private bid visibility logic
* traditional vs bidding-period visibility rules
* listing creator view
* bidding agent view
* other-agent/public competing-agent view
* Accept / Counter / Reject behavior
* accepted bid summary behavior

The format should stay the same as Tenant, but the labels, compensation text, and service sections must change correctly by role:

* Tenant → Tenant fields
* Landlord → Landlord fields
* Seller → Seller fields
* Buyer → Buyer fields

---

# Scope

This task applies only to the **bid display / bid action / bid visibility flow** for:

* Hire a Tenant Agent
* Hire a Landlord Agent
* Hire a Seller Agent
* Hire a Buyer Agent

Do **not** change listing creation or listing edit flows.

Do **not** rewrite compensation logic itself unless needed only to display the correct existing fields.

---

# 🛑 Critical safeguards

1. **Tenant is the source of truth**

   * Use Tenant bid card, Tenant full bid view, Tenant bid actions, and Tenant visibility rules as the exact working model.

2. **Trace the actual files first**
   Before editing, identify the real:

   * route
   * controller or Livewire component
   * blade/view
   * shared partials/helpers/services
     used for:
   * bid cards
   * full bid view
   * accept/counter/reject actions
   * accepted bid summary
   * bid visibility logic

3. **Do not assume Landlord, Seller, and Buyer use the correct components now**
   Confirm the actual files first.

4. **Do not create duplicate parallel systems**
   If these roles already share display/action components, update the shared logic carefully instead of cloning Tenant code into separate files.

5. **Do not change unrelated listing pages**
   Scope changes only to bid display/action/view logic.

6. **Do not change DB schema unless truly necessary**
   This should primarily be a view/rendering/logic parity fix.

7. **Do not hardcode Tenant labels into non-Tenant roles**
   The layout/logic should match Tenant, but the field labels/content must reflect the correct role.

8. **Do not break existing accepted/countered/rejected bid data**
   Only normalize how it is displayed and handled.

9. **Do not start with Blade styling first**
   First verify:

   * route/component mapping
   * role resolution logic
   * bid visibility logic
   * action handling logic
     then update UI.

10. **No refactor outside this task**
    No folder cleanup, no variable renames across the app, no unrelated code changes.

---

# Step 1 — Trace the real files first

Before coding, trace the actual files used for all of the following for Tenant, Landlord, Seller, and Buyer:

## A. Bid card / summary card

The collapsed card that shows things like:

* Agent name
* status
* offered services count
* compensation summary
* view full bid
* edit bid

## B. Full bid view

The expanded/full bid view that Tenant already uses, including:

* match score
* summary cards
* agent overview
* compensation & agency agreement terms
* additional details
* offered services
* private compensation terms visibility
* anything else shown in Tenant full view

## C. Bid actions

The actual logic and UI for:

* Accept
* Counter
* Reject
* Edit Bid
* View Full Bid

## D. Accepted bid summary

The post-acceptance summary / accepted bid record shown after a bid is accepted

## E. Visibility rules

The logic that decides:

* what the listing creator sees
* what the agent who placed the bid sees
* what other competing agents see
* what is hidden for traditional listings
* what is shown/hidden during bidding period vs traditional

Return a file map first like:

* Tenant bid card → `[component/view]`
* Landlord bid card → `[component/view]`
* Seller bid card → `[component/view]`
* Buyer bid card → `[component/view]`
* Tenant full bid view → `[component/view]`
* Landlord full bid view → `[component/view]`
* Seller full bid view → `[component/view]`
* Buyer full bid view → `[component/view]`
* Accept/Counter/Reject action handler(s) → `[component/controller/service]`
* Accepted bid summary → `[component/view]`
* Visibility rules source → `[component/helper/service]`

## CRITICAL

Do not start coding until this mapping is confirmed.

---

# Step 2 — Match Tenant bid card layout and behavior exactly

Make **Landlord, Seller, and Buyer bid cards** behave the same way Tenant bid cards behave.

## Required Tenant behaviors to copy

Tenant bid cards currently do things like:

* show the number of selected services
* show a compensation summary section
* show a View Full Bid option
* show Edit Bid for the bidding agent where appropriate
* show status consistently
* render cleanly without flashing or broken layout behavior

Landlord, Seller, and Buyer must use this same structure and behavior.

## Required result

For Landlord, Seller, and Buyer:

* same card structure as Tenant
* same spacing/layout
* same expand/collapse behavior if applicable
* same action placement
* same stable rendering with no flashing/flicker
* same summary logic pattern

But with correct role-specific labels and fields, for example:

* Landlord’s Broker Compensation Structure
* Seller’s Broker Compensation Structure
* Buyer’s Broker Compensation Structure
  instead of Tenant’s Broker Compensation Structure where applicable

Also:

* Offered Services must show the same way Tenant does, including the number of services
* If Tenant shows “75 Services” style counts, Landlord/Seller/Buyer must do the same based on their own selected services

---

# Step 3 — Match Tenant full bid view exactly

Make **Landlord, Seller, and Buyer full bid views** match Tenant full bid view in layout and logic.

## Required Tenant behaviors to copy

When Tenant full bid is opened, it shows a complete structured bid view with sections such as:

* match score / comparison cards if applicable
* Agent Overview
* Broker Compensation & Agency Agreement Terms
* Additional Details
* Offered Services
* private compensation terms when appropriate

Landlord, Seller, and Buyer full bid views must use this same structure.

## Required result

Landlord, Seller, and Buyer full bid view must:

* use the same section order as Tenant
* use the same visual hierarchy as Tenant
* use the same show/hide behavior as Tenant
* use the same private/public visibility logic as Tenant
* use the same action placement pattern as Tenant

But the content must map to the correct role-specific fields.

Examples:

* Tenant compensation section → Tenant labels
* Landlord compensation section → Landlord labels
* Seller compensation section → Seller labels
* Buyer compensation section → Buyer labels

Do not show Tenant terminology inside Seller, Buyer, or Landlord bid views.

---

# Step 4 — Match Tenant visibility rules exactly

This is critical.

Use the exact same visibility logic Tenant already has and apply it to Landlord, Seller, and Buyer.

## Match the same visibility modes Tenant has for:

### A. Listing creator view

The person who created the listing should see the same type of bid information Tenant listing creators see.

### B. Agent who placed the bid

The bidding agent should see the same type of bid details Tenant bidding agents see.

### C. Other competing agents

Other agents should see the same level of summary/hidden information Tenant competing agents see.

### D. Traditional vs bidding-period logic

Whatever Tenant does for:

* traditional listings
* bidding-period listings
* hidden/private bid info
* when “View Full Bid” is available
* when compensation details are private

Landlord, Seller, and Buyer must do the same.

## Important

If Tenant hides bid information for traditional listings in certain views, Landlord/Seller/Buyer must hide it the same way.

If Tenant shows private compensation & agency agreement terms only to the listing creator or the bidding agent, Landlord/Seller/Buyer must apply the same logic.

---

# Step 5 — Match Accept / Counter / Reject behavior exactly

Use Tenant as the source of truth for all bid action flows.

Make Landlord, Seller, and Buyer behave exactly like Tenant for:

* Accept
* Counter
* Reject

## Required result

Landlord, Seller, and Buyer must follow the same:

* button visibility rules
* who can click which action
* status changes
* UI refresh behavior
* post-action rendering
* accepted/countered/rejected display state
* notice/summary logic

## Important

Do not invent different workflows for Landlord, Seller, and Buyer.

If Tenant supports:

* listing creator can accept/counter/reject
* bidding agent sees corresponding result
* other agents see proper status/hidden behavior

then Landlord, Seller, and Buyer must do the same.

---

# Step 6 — Match accepted bid summary behavior exactly

Once a bid is accepted, Landlord, Seller, and Buyer should produce the same style of accepted bid summary Tenant uses.

## Required result

The accepted bid summary must:

* look the same structurally as Tenant accepted bid summary
* show the same kinds of sections
* follow the same private/public rules
* show correct role-specific compensation and services language

Examples:

* Tenant accepted bid summary → Tenant labels
* Landlord accepted bid summary → Landlord labels
* Seller accepted bid summary → Seller labels
* Buyer accepted bid summary → Buyer labels

Do not leave Tenant wording in accepted summaries for non-Tenant roles.

---

# Step 7 — Fix rendering instability / flashing

The Landlord example currently flashes and acts differently.

That must be fixed.

## Required result

Landlord, Seller, and Buyer bid card/full bid rendering must be as stable as Tenant:

* no flashing
* no collapsing into broken narrow layout
* no unstable re-render loops
* no mismatched partial loading
* no different behavior caused by wrong component/view

## Safeguards

* identify whether flashing is caused by:

  * wrong component being used
  * Livewire re-render issue
  * conditional block mismatch
  * missing shared partial data
  * wrong role-based view selection
* fix the root cause, not just CSS

---

# Step 8 — Role-specific content mapping rules

Keep the Tenant format, but map fields correctly by role.

## Required rule

The same display structure should apply, but content labels and values must come from the correct role-specific field set.

Examples:

* Tenant should show Tenant compensation terminology
* Landlord should show Landlord compensation terminology
* Seller should show Seller compensation terminology
* Buyer should show Buyer compensation terminology

The same applies to:

* services
* compensation summary labels
* agency agreement labels
* accepted bid summary labels

## Important

Do not use one generic text blob that causes the wrong role wording to appear.

---

# Step 9 — Agent view, listing creator view, and competing-agent view must all match Tenant logic

For Landlord, Seller, and Buyer, make sure these three perspectives match Tenant exactly in logic and layout behavior:

## A. Listing creator view

* what they can see
* what actions they can take
* when full bid is visible
* when private terms are visible

## B. Bidding agent view

* what they can see about their own bid
* when edit bid is available
* how status appears
* what happens after counter/accept/reject

## C. Other competing agents view

* what summary they can see
* what is hidden
* what remains private
* how traditional vs bidding-period affects visibility

---

# Step 10 — Implementation rules

## Logic first, UI second

Order of work must be:

1. route/component/view mapping
2. visibility logic mapping
3. action handler mapping
4. role field mapping
5. stabilize rendering
6. then final UI cleanup

## Reuse shared structure if possible

If Tenant already uses a good shared component/partial for bid display or actions:

* reuse and extend that logic safely
* do not duplicate whole files unless there is no safe shared option

## Keep changes minimal

Only edit the files needed for:

* bid cards
* full bid view
* bid actions
* accepted bid summary
* visibility logic

---

# Step 11 — Testing checklist

After coding, test all four roles:

* Tenant
* Landlord
* Seller
* Buyer

## A. Bid card summary

Confirm each role:

* shows service count like Tenant
* shows compensation summary like Tenant
* shows View Full Bid like Tenant
* shows Edit Bid where appropriate
* renders without flashing

## B. Full bid view

Confirm each role:

* opens full bid view like Tenant
* same section order
* same layout
* same private/public behavior
* correct role-specific labels

## C. Listing creator view

Confirm each role:

* listing creator sees same level of detail Tenant listing creator sees
* Accept/Counter/Reject behaves the same as Tenant

## D. Bidding agent view

Confirm each role:

* bidding agent sees their bid the same way Tenant bidding agent does
* Edit Bid availability matches Tenant logic
* post-action updates match Tenant logic

## E. Competing-agent view

Confirm each role:

* competing agents see the same style of limited/hidden info Tenant competing agents see
* private bid information stays hidden where Tenant hides it

## F. Traditional vs bidding-period

Confirm each role:

* traditional listing bid visibility matches Tenant traditional logic
* bidding-period visibility matches Tenant bidding-period logic
* private/full bid display rules match Tenant

## G. Accepted bid summary

Confirm each role:

* accepted bid summary matches Tenant structure
* role-specific wording is correct
* no Tenant labels appear in Landlord/Seller/Buyer accepted summaries

## H. Regression

Confirm Tenant still behaves exactly as before.

---

# Step 12 — Required output after coding

Provide:

1. exact route/component/view mapping for Tenant, Landlord, Seller, Buyer bid cards and full views
2. exact files changed
3. whether a shared component/partial was reused or separate views were updated
4. exact source of visibility logic
5. exact source of Accept/Counter/Reject handling
6. proof that:

   * Landlord, Seller, and Buyer now match Tenant bid behavior
   * service counts show correctly
   * full bid view matches Tenant structure
   * private visibility rules match Tenant
   * Accept/Counter/Reject matches Tenant
   * accepted bid summary matches Tenant
   * flashing/render instability is fixed

---

# Definition of done

This task is complete only when:

* Landlord, Seller, and Buyer bid cards look and behave the same as Tenant
* Landlord, Seller, and Buyer full bid views look and behave the same as Tenant
* listing creator, bidding agent, and competing-agent views follow Tenant logic exactly
* traditional vs bidding-period visibility matches Tenant exactly
* Accept / Counter / Reject matches Tenant exactly
* accepted bid summary matches Tenant exactly
* role-specific labels correctly change by role
* flashing/render instability is fixed
* Tenant remains intact

---

This needs to be treated as a parity fix using Tenant as the exact reference model.
