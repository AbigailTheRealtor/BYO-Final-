ADD THIS TO THE FULL AUDIT:

---

## MATCH SCORE AUDIT — REQUIRED

Do a complete Match Score audit for BOTH:

1. Broker Compensation & Agency Agreement Terms
2. Offered Services

across every supported role × property-type combination.

---

## SUPPORTED COMBINATIONS

Tenant:

* Residential
* Commercial

Landlord:

* Residential
* Commercial

Buyer:

* Residential
* Income
* Commercial
* Business
* Vacant Land

Seller:

* Residential
* Income
* Commercial
* Business
* Vacant Land

---

## FOR EACH COMBINATION, VERIFY:

A. TERMS COUNT

* exact countable Broker Compensation & Agency Agreement Terms groups
* exact fields included in each term group
* whether any property-type-specific extra fields exist
* whether those extra fields are counted correctly
* whether Additional Details is excluded
* whether displayed mismatch fields align with counted term groups

B. SERVICES COUNT

* exact countable services for that role/property type
* whether every checkmark counts correctly
* whether grouped sub-options count correctly
* whether custom/additional services are handled correctly
* whether displayed service categories align with counted services

C. BASELINE RULES
Verify that Match Score only scores against what the user actually provided:

* only filled term groups count in baseline
* only selected services count in baseline

D. ZERO-BASELINE DISPLAY RULE
If user did not provide baseline terms and/or services:

1. If no baseline terms:

* hide Terms Match score block
  OR show safe message like “No terms provided”

2. If no baseline services:

* hide Services Match score block
  OR show safe message like “No services requested”

3. If neither terms nor services baseline exists:

* hide Match Score entirely

Do NOT show misleading:

* 0/0
* 0%
* fake low scores
* fake high scores

E. RENDERING ALIGNMENT
For each role/property type verify:

* counted fields are rendered correctly
* rendered fields are counted correctly
* mismatch badges align with counted fields
* service match/extra/missing display aligns with actual score math

---

## OUTPUT REQUIRED

For each role/property type, provide:

1. terms baseline total logic
2. services baseline total logic
3. property-type-specific extra fields/services
4. whether zero-baseline behavior is correct
5. whether Match Score should show, partially show, or hide
6. any incorrect count/mapping found
