Yes — this is now pointing to a **counter save/hydration bug**, not just a comparison bug.

From your screenshot, the system is behaving like this:

* you removed only a few services in the counter
* but after save, the counter shows **many more services missing**
* Terms Match is also collapsing at the same time

That usually means one of these is happening during the **counter submit/load cycle**:

1. the counter form is not preloading the full original bid service set correctly
2. unchecked services are being saved from a **partial rendered set** instead of the full valid set
3. the save logic is filtering against the wrong catalog or wrong property type
4. the counter form is dropping services from hidden/unclicked sections on submit
5. services are being overwritten by a stale payload during hydrate/save

So this needs a **counter form integrity fix**.

# Replit instructions with safeguards

## Goal

Fix the **Hire a Tenant Agent counter form** so when the user removes only specific services, the counter saves **only those intended changes** and does not silently remove additional services.

Right now, the counter flow is deleting many more services than the user actually removed.

This means the counter save pipeline is not preserving the full service state correctly.

---

## Scope

Fix only the active **Tenant counter flow** first, including:

* counter form preload
* counter form checkbox state
* counter submit/save logic
* counter reload/hydration logic
* counter services comparison output

Do not modify Buyer, Seller, or Landlord unless a shared helper must be fixed safely without side effects.

---

## Safeguards

### 1. No destructive DB changes

* do not rename/drop/delete tables or columns
* do not bulk rewrite old counters
* do not migrate service data destructively

### 2. Do not change service wording/order

* do not change labels
* do not change section titles
* do not change property-type catalogs
* this is a preload/save integrity fix

### 3. Preserve existing Tenant catalog filtering

Keep the current Tenant-only catalog protections already added:

* role lock
* property-type filtering
* wrong-role exclusion

### 4. Tenant-only first

Do not broaden until Tenant counter behavior is fully stable.

---

# Problem to fix

When creating a counter on a Tenant listing, the user removes only a few services, but after saving, the counter shows many more services removed than intended.

That means the saved counter services payload is not equal to the user’s actual final checked state.

---

# Required diagnosis first

Before changing logic, Replit must trace the service state through the full counter lifecycle.

For one affected Tenant counter submission, log all of these:

## A. Baseline source used to build the counter form

* listing id
* bid id
* counter id
* property type
* original bid services used to prefill counter

## B. Services loaded into the counter form on initial render

* full normalized service set used to pre-check the counter form

## C. Services present in the browser payload on submit

* exact checked services submitted

## D. Services after server-side normalization/filtering

* final normalized set before save

## E. Services actually saved to counter record

* final stored set

## F. Services loaded back from saved counter for display

* final set shown after reload

These must all be compared side-by-side.

The goal is to find exactly where extra services are being dropped:

* preload
* front-end form state
* submit payload
* server normalization
* save
* reload

---

# Most likely root causes to inspect

## 1. Partial form-state save

The counter may be saving only the services present in currently rendered DOM sections, not the full checked service state.

## 2. Hidden/unclicked sections dropped

If some service groups are collapsed, conditionally rendered, or not properly hydrated, unchecked/missing payload keys may be treated as intentionally removed.

## 3. Wrong baseline used for counter preload

The counter may not be preloading from the correct original bid/current bid state.

## 4. Replace instead of merge behavior

On submit, the app may be replacing the stored service set with an incomplete incoming array instead of a correctly resolved final service set.

## 5. Additional services handled separately and inconsistently

Custom services like Example 1 / Example 2 may be going through a different pipeline than checkbox services.

## 6. Livewire hydration drift

If this is Livewire, the services array may be mutating between mount, render, update, and submit.

---

# Required fix

## Part 1: Hard-lock the correct counter baseline

For Tenant counter creation, define exactly what the initial checked state should be.

Most likely:

* counter form baseline = the agent’s current bid services being countered

Use that exact baseline every time.

Do not let the counter form preload from:

* listing services alone
* stale prior counter
* partial snapshot
* mixed sources

---

## Part 2: Build one canonical full service state before save

On submit, do not save from fragmented raw payload pieces.

Instead:

1. resolve the valid Tenant catalog for the correct property type
2. resolve all checked standard services from the form
3. resolve all valid custom/additional services
4. normalize/dedupe
5. build one canonical final counter service set
6. save that exact set

This final set must represent the user’s actual checked state, not a partial form artifact.

---

## Part 3: Do not interpret absent payload keys as removed unless that section was part of the full form state

If a service section is not present in the payload because of render/hydration issues, the system must not silently treat all missing services as intentionally removed.

Replit needs to confirm the counter form is submitting the full state of all service checkboxes.

---

## Part 4: Add a before-save integrity check

Before saving counter services, compare:

* baseline checked set
* submitted checked set
* final normalized saved set

If the final saved set unexpectedly drops more services than were actually unchecked by the user, log/report it and stop the bad save.

This can be temporary while debugging, or retained as a safety check.

---

## Part 5: Use same canonical set for display/comparison

After save, the exact same canonical saved set must be used for:

* counter detail display
* missing services
* added services
* services match score

Do not rebuild different sets in different places.

---

## Part 6: Additional/custom services must be preserved correctly

Custom services must go through the same save pipeline integrity check.

If the user left `Example 1` and `Example 2` checked/present, they must remain saved and must not affect unrelated standard services.

---

# Copy-paste prompt for Replit

Use this exactly:

> Fix the Hire a Tenant Agent counter form so it preserves the full intended service state and does not silently remove extra services during counter save. Right now when I remove only a few services in the counter, the saved counter shows many more services removed than I actually deleted. This means the counter preload/save/hydration pipeline is dropping services incorrectly.
>
> Scope this only to the active Tenant counter flow first. Do not modify Buyer, Seller, or Landlord unless a shared helper must be fixed safely without side effects. No destructive DB changes. Do not change service labels, section order, property-type catalogs, or wording. Preserve the existing Tenant role/property-type filtering fixes.
>
> First, trace the full service state through the counter lifecycle for an affected example: baseline source used to prefill the counter, normalized services loaded into the form on render, exact checked services submitted from the browser, normalized services after server-side filtering, exact services saved to the counter record, and exact services loaded back for display after save. I need to know the exact step where extra services are being dropped.
>
> Then refactor the Tenant counter save flow so it uses one canonical full service-state builder before save. Resolve the correct Tenant property-type catalog, collect all checked standard services plus valid custom/additional services, normalize and dedupe them, and save that exact final set. Do not allow absent or partially rendered payload keys to silently remove unrelated services. The saved counter service set must equal the user’s actual final checked state.
>
> Also make the post-save comparison and display layers use that same canonical saved set so missing/added services and services match are all generated from the exact same saved data.
>
> After the fix, if I remove only specific services in a Tenant counter, only those intended services should show as removed after save, and no additional unrelated services should disappear.

---

# What Replit should test

## Test 1: Remove one service only

Start with a fully populated Tenant bid
Remove exactly one service in counter

Expected:

* only that one service appears missing after save

## Test 2: Remove three services in different sections

Expected:

* exactly those three removed
* all others preserved

## Test 3: Leave all services unchanged

Expected:

* zero missing
* full services match

## Test 4: Preserve custom services

Keep `Example 1` and `Example 2`

Expected:

* both remain saved
* no unrelated standard services dropped

## Test 5: Reload stability

Open saved counter multiple times

Expected:

* same service set every time
* same missing/added counts every time

---

# What I think is happening

The strongest likelihood is:

## the counter form is saving from an incomplete service array

instead of a fully reconstructed canonical service set.

That is why you remove a few things, but the system behaves like many more were removed.

So yes — your next Replit step should focus on the **counter form state pipeline**, not just the comparison engine.

I can also combine this with the earlier terms/services instability into one **single master “Tenant counter integrity + canonical comparison” instruction** if you want.
