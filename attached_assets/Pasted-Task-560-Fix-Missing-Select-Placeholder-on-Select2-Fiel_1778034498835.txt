Task #560 — Fix Missing “Select” Placeholder on Select2 Fields (Surgical Fix)

READ-ONLY AUDIT FIRST:
Load all four Create Offer Listing flows:
- /offer-listing/seller
- /offer-listing/landlord
- /offer-listing/buyer
- /offer-listing/tenant

Identify ONLY Select2 fields that:
- show blank placeholder (no “Select” text), OR
- do not restore placeholder after clearing

DO NOT touch fields that already work correctly.

---

SCOPE:
Offer Listing Create flows only:
- Seller
- Landlord
- Buyer
- Tenant

---

DO NOT TOUCH:
- Routes
- Controllers
- Schema/migrations
- Validation
- Save/load logic
- wire:model bindings
- Option values
- Tab navigation
- Working Select2 fields

---

STEP 1 — BLADE FIX (Missing data-placeholder)

Add:
data-placeholder="Select"

ONLY to confirmed broken fields.

Confirmed minimum fixes:

Seller:
- #appliances
- #non_negotiable_amenities

Landlord:
- #terms_of_lease (wire:model version)

Tenant:
- #appliances (wire:model version)
- #credit_scroe_rating

If audit reveals more broken fields, fix those ONLY.

---

STEP 2 — SINGLE SELECT ONLY (if applicable)

If a Select2 field is NOT multiple:
Ensure first option is:

<option value=""></option>

Do NOT modify multi-select fields.

---

STEP 3 — JS FIX (LIMITED ONLY TO BROKEN FIELDS)

In these files:
- offer-seller-listing.blade.php
- offer-landlord-listing.blade.php
- offer-buyer-listing.blade.php
- offer-tenant-listing.blade.php

Find ONLY the Select2 initializers tied to broken fields.

Update them to:

placeholder: $(element).data('placeholder') || 'Select',
allowClear: true,

DO NOT rewrite all Select2 initializers.
DO NOT touch working ones.

---

STEP 4 — DOUBLE INIT GUARD (ONLY WHERE MISSING)

Wrap initialization ONLY if missing:

if (!$(element).hasClass('select2-hidden-accessible')) {
    $(element).select2({...});
}

---

VERIFY:

Run:
php -l on all edited Blade files

Manual QA:

For each role:
- Seller
- Landlord
- Buyer
- Tenant

Confirm:
- “Select” appears when field is empty
- Placeholder returns after clearing
- Dropdown works normally
- No duplicate Select2 UI
- No console errors
- Save Draft works
- Tab switching does not break Select2

---

STOP RULE:

If a field already:
- shows “Select”
- clears properly
- reloads correctly

DO NOT MODIFY IT.

---

GOAL:

Fix only broken placeholders without impacting working Select2 behavior.