FOLLOW-UP TASK — EXTEND AI QUESTIONS VIEWER TO SUPPORT ADDON QUESTIONS.

Use:
.local/offerlisting_ai_questions_review_report.md

Goal:
Update the existing read-only AI Questions viewer component so it displays addon question answers (commercial, income, business opportunity, vacant land, etc.), not just base questions.

Only modify:
resources/views/.../listing-ai-knowledge-base.blade.php (viewer component)

Do NOT modify:
OfferListing create/edit flows
Livewire PHP components
config files
routes
dashboard
seller_property/**
Hire Agent flows

--------------------------------------------------

TASKS

1. Add support for 'addons' in config files:
Each config file includes:
'addons' => [
    'key' => [
        'label' => '',
        'visible_for' => [],
        'questions' => []
    ]
]

2. Render addon sections below base questions:
- Loop through config('...')['addons']
- For each addon:
  - Check visible_for against property type
  - Only render if applicable

3. For each addon:
- Display section label
- Loop through addon['questions']
- Show only questions with non-empty answers (same rule as base questions)

4. Maintain existing behavior:
- Do NOT change base question rendering
- Do NOT show empty answers
- Keep accordion/grouped UI structure

5. Property-type logic:
Use the same property type variable already passed to the component (do not introduce new logic sources).

--------------------------------------------------

SAFETY

- Do NOT modify how answers are saved
- Do NOT modify config structure
- Do NOT add new fields
- Do NOT change OfferListing flows
- Do NOT touch Livewire controllers
- Do NOT touch API endpoints

--------------------------------------------------

VERIFICATION

1. Base questions still display correctly
2. Addon questions now display when applicable:
   - Commercial
   - Income
   - Business Opportunity
   - Vacant Land
3. Addon questions do NOT show when property type does not match
4. No empty answers are shown
5. No UI regression in existing viewer

--------------------------------------------------

Write report to:
.local/ai_questions_viewer_addons_fix_report.md