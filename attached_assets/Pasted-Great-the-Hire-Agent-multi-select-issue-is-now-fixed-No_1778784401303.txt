Great — the Hire Agent multi-select issue is now fixed.

Now apply the same architectural fix to Create Offer Listing flows.

Scope:
- Create Offer Listing Seller
- Create Offer Listing Buyer
- Create Offer Listing Landlord
- Create Offer Listing Tenant
- Include edit/draft pages only if they use the same broken Select2 destroy/reinit pattern

Core issue to audit:
Do not only check for missing closeOnSelect: false.

Also check for Livewire re-render race:
- select2('destroy') inside message.processed
- select2('destroy') inside tab-change handlers
- select2('destroy') inside property_type change handlers
- immediate Livewire updates after each selection
- safeLivewireSet(..., false) or @this.set(..., false) causing morphdom refresh while dropdown is open

Use the same proven fix from Hire Agent:
1. Every true multi-select must initialize with:
   closeOnSelect: false,
   width: '100%',
   placeholder: 'Select'

2. Before destroying/reinitializing Select2, check whether it is currently open:
   var _s2Open = false;
   try { _s2Open = !!($el.data('select2') && $el.data('select2').isOpen()); } catch(e) {}
   if (_s2Open) return;

3. Avoid immediate Livewire round-trips from multi-select change handlers while user is still selecting.
   Use debounced/batched updates where needed.

4. Do not apply this to single-select fields.

Do not change:
- schema
- field names
- validation
- controllers
- saved values
- Limited Service frozen code
- unrelated Hire Agent files already fixed

QA:
For every Create Offer Listing role, test all true multi-selects:
- select 4+ options without reopening dropdown
- confirm dropdown does not close after each pick
- confirm single-select fields still close normally
- confirm draft/edit reload preserves saved selections

Final response must include:
- files changed
- each true multi-select initializer patched
- any destroy/reinit guards added
- any immediate Livewire update changed to debounced/batched
- confirmation no single-selects or Limited Service flows were changed