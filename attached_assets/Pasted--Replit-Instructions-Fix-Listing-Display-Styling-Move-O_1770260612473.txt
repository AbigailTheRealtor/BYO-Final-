## Replit Instructions — Fix Listing Display Styling + Move “Offered Financing/Currency” Into Financing Details (Hire a Buyer’s Agent)

**Applies To:** Hire a Buyer’s Agent → **Actual Listing Display** (all property types where shown)
**Login Example:** `tenant@exp.com`

You must implement **two fixes**:

1. **Revert the answer/value color change** (answers should NOT be dark gray)
2. **Move “Offered Financing/Currency” so it displays under “Financing Details” (Financing Terms section)** instead of being above it

---

# 1) SAFEGUARDS (NON-NEGOTIABLE)

1. **Display-layer only**

   * Do NOT change database schema, stored values, validation, or form logic.
2. **No global styling regressions**

   * Only adjust styles used on **Hire a Buyer’s Agent actual listing display**.
   * Do NOT affect Create Listing, Edit forms, tooltips, bids/counters, or PDFs unless explicitly required.
3. **Do not change wording**

   * Do NOT change any labels, headings, chip text, or option names.
4. **No order changes except the one explicitly requested**

   * Only move **Offered Financing/Currency** into the Financing Details section.
5. **Backward compatible**

   * Must work on existing saved listings without re-saving.

---

# 2) FIX #1 — Answers/Values Should NOT Be Dark Gray

### Issue

On actual listing display, the **answers/values** (ex: “Yes”, dollar amounts, percentages) are showing as dark gray and reducing readability/contrast.

### Required Change

Revert so:

* **Field labels** may remain dark/black as-is
* **Answers/values** must return to the **original value color** used before this change (the “normal” readable value color)
* Chips/badges like **“Yes”** should keep their existing badge styling (do not dim them)

### Implementation Notes

* Identify the CSS class or style that was recently applied to values/answers (likely a shared selector targeting value spans).
* Remove/override it **only for value text**, not labels.

✅ Target outcome:

* Labels: bold/dark (unchanged)
* Values: normal readable value color (not dark gray override)

**Important:** This should not affect the section headers styling (Financing Details / Purchasing Terms). Only the value text needs to be reverted.

---

# 3) FIX #2 — Move “Offered Financing/Currency” Under Financing Details (Financing Terms)

### Issue

“Offered Financing/Currency” is currently displaying **above** the “Financing Details” header, but it belongs conceptually under financing terms.

### Required Change

On the **actual listing display**, move the “Offered Financing/Currency” label + its selected option chips so it appears **inside Financing Details**, under **Financing Terms** (the financing section), and not above it.

### Placement Rule

* “Financing Details” should appear as the main header.
* Under it, show the financing-related sections, including:

  * **Offered Financing/Currency:** (chips)
  * Then Loan Pre-Approval Status, Seller Financing Terms, Assumable Terms, etc. (as currently structured)

✅ Display order under Financing Details should be:

1. **Offered Financing/Currency**
2. **Loan Pre-Approval Status**
3. **Seller Financing Terms**
4. **Assumable Terms**
5. **Exchange/Trade Terms**
6. Any other financing sub-sections already present

*(Do not reorder sub-sections beyond placing Offered Financing/Currency inside Financing Details at the top.)*

### Safeguard Note

This is a **display move only**:

* Do NOT change where the user selects financing during creation
* Do NOT change stored data
* Do NOT change chips component behavior

---

# 4) WHERE TO APPLY (STRICT SCOPE)

Apply both fixes to:

* **Hire a Buyer’s Agent → actual listing display page**

Do NOT apply to:

* Create Listing screen
* Edit Listing screen
* Draft forms
* Bid/counter screens
* PDF/exports
  (unless those screens reuse the same listing display template—if so, scope the CSS/move to only the listing display component)

---

# 5) TESTING REQUIREMENTS (PROOF REQUIRED)

Developer must confirm:

### Styling Test

* Answers/values (Yes, dollar amounts, % amounts) are **not forced dark gray**
* Labels remain correct
* Section headers remain bold/black as desired

### Placement Test

* “Offered Financing/Currency” now appears **under Financing Details**
* Chips display correctly and are unchanged
* No duplication (must not appear both above and under)

### Regression Test

* Create Listing and Edit forms unchanged
* No other tabs/sections moved
* Listing still renders for Residential + Commercial + Business + Vacant Land + Income

Provide:

* Before/after screenshots of the same listing section
* File paths changed

---

## QUICK NOTE (So Dev Doesn’t Misinterpret)

You’re not saying “make everything black.”
You’re saying: **don’t dim the actual answers/values**—keep them readable and consistent with the original display styling.

---

If you want, paste the name/path of the file that renders the Buyer listing display (or the Livewire component name), and I’ll rewrite this into “edit this exact block / move this exact component” instructions so Replit can implement without guessing.
