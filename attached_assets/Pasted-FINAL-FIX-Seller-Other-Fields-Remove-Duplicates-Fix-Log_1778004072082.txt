FINAL FIX — Seller “Other” Fields (Remove Duplicates + Fix Logic)

Scope:
ONLY fix Seller "Other" conditional fields.

Do NOT:
- Add new properties
- Modify validation
- Touch other flows

--------------------------------------------------
ISSUE 1 — REMOVE DUPLICATE INPUTS
--------------------------------------------------

For BOTH fields:
- existing_lease_type
- reason_for_sale

REMOVE any duplicate input.

Each field must have ONLY ONE input.

--------------------------------------------------
ISSUE 2 — USE ONLY BLADE CONDITION (NO MIXED LOGIC)
--------------------------------------------------

REMOVE:
- inline style="display:none"
- JS-based wrapper logic

USE ONLY:

@if ($existing_lease_type === 'Other')
    <div class="form-group mt-2">
        <div class="input-cover">
            <input type="text"
                   wire:model.defer="other_lease_type"
                   class="form-control has-icon"
                   data-icon="fa-solid fa-file-contract"
                   placeholder="Enter Existing Lease Type (e.g., Month-to-Month)">
        </div>
    </div>
@endif

AND:

@if ($reason_for_sale === 'Other')
    <div class="form-group mt-2">
        <div class="input-cover">
            <input type="text"
                   wire:model.defer="other_reason_for_sale"
                   class="form-control has-icon"
                   data-icon="fa-solid fa-tag"
                   placeholder="Enter Reason for Sale (e.g., Relocation)">
        </div>
    </div>
@endif

--------------------------------------------------
RULES
--------------------------------------------------

- ONE input per field ONLY
- NO duplicate wire:model
- NO mixed Blade + JS visibility
- Blade @if handles everything

--------------------------------------------------
EXPECTED RESULT
--------------------------------------------------

- Selecting "Other" shows ONE clean input
- Switching away removes it cleanly
- No duplicate values
- No UI flicker
- No Livewire conflicts