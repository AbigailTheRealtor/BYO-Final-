IMPLEMENT LANDLORD FIX — TEXTAREA HEIGHT + OTHER REVEAL ONLY

Scope:
- /offer-listing/landlord
- Full-Service / Commission-Based path only
- Active offer-listing landlord files only

Allowed files:
- resources/views/livewire/offer-listing/landlord/offer-landlord-listing.blade.php
- resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/property-preferences.blade.php
- resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/lease-terms.blade.php
- resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/tax-legal-hoa-disclosures.blade.php

DO NOT touch:
- Seller
- Buyer
- Tenant
- Hire Agent legacy files
- Limited Service
- Database schema
- Backend save/load logic

Fix 1 — Landlord textarea height:
Root cause:
.form-control { min-height: 50px; } in the master blade is forcing textareas taller even when rows="2".

Action:
- Do not change global .form-control.
- Add a scoped landlord textarea override/class only for affected Landlord textareas.
- Example:
  .landlord-compact-textarea {
      min-height: 42px;
      height: 42px;
      resize: vertical;
  }
- Apply only to affected textarea fields.

Fix 2 — Landlord multi-select “Other” reveal:
Root causes:
- Missing otherId entries in mlsMultiSelects config
- Some wrapper selector mismatches
- desired_lease_term has no handler
- tenant_pays / owner_pays wrappers use class selectors instead of ID-based pattern

Action:
- Update unified mlsMultiSelects config so every multi-select with “Other” has the correct otherId.
- Standardize wrappers to ID-based selectors where the unified handler expects IDs.
- Add handler support for desired_lease_term.
- Fix tenant_pays and owner_pays wrapper selectors to match the unified pattern.

Verify these fields:
- appliances_included
- view_preference
- desired_lease_term
- heating_and_fuel
- air_conditioning
- water
- sewer
- utilities
- laundry_features
- floor_covering
- security_features
- garage_parking_features
- building_features
- electrical_service
- terms_of_lease
- rent_includes
- tenant_pays
- owner_pays
- additional_documents_available

Verification:
- Select Other → custom input appears immediately.
- Remove Other → custom input hides.
- Custom input stays correct after Livewire re-render.
- Textareas visually match compact fields.
- No console errors.
- No non-landlord files changed.

Report:
- Files changed
- Root cause fixed
- Fields verified
- Any deferred issues