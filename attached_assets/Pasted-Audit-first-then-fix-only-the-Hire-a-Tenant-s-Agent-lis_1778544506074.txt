Audit first, then fix only the Hire a Tenant’s Agent listing flow.

Scope:
ONLY this route:
 /hire/agent/auction/tenant

ONLY these files/components if needed:
- TenantAgentAuction.php
- tenant-agent-auction.blade.php
- related tenant hire-agent partials only if this exact flow uses them

Do NOT touch:
- Create Offer Listing flows
- Seller/Buyer/Landlord offer listing files
- Limited Service/legacy flows
- database schema
- routes
- unrelated validation logic

Task:
First perform a read-only audit and identify root causes for these Hire a Tenant Agent issues:

1. Placeholder text missing “Select” on:
- Acceptable Property Conditions
- Offered Lease Term
- Leasing Space
- Garage/Parking Features

Expected:
Each select/multiselect should display placeholder “Select”.

2. Lease-Option Agreement compensation inputs broken:
When user selects Yes for “Interested in a Lease-Option Agreement,” user cannot enter numbers for either $ or % fields. Text erases in:
- Compensation for Creating the Lease-Option Agreement
- Compensation if Purchase Option is Exercised

Expected:
Dollar and percentage inputs allow normal numeric entry, deletion, blur formatting, and Livewire updates without wiping values.

3. Phone Number input broken:
User cannot erase numbers properly; numbers delete unexpectedly.

Expected:
Phone input allows typing, backspacing, clearing, and retyping without fighting the user.

4. Personal Photo upload freezes:
After selecting Personal Photo, screen stays in uploading state, user cannot exit, save, or submit.

Expected:
Upload spinner clears after success or failure, user can continue using the form, and save/submit is not permanently blocked.

5. Property type switching leaves wrong conditional fields visible:
When switching Residential ↔ Commercial, fields specific to the previous property type remain visible.
Example:
Garage/Parking Features stayed visible after switching property type incorrectly.

Expected:
Residential-only fields show only for Residential.
Commercial-only fields show only for Commercial.
Changing property type should clear/hide stale conditional UI and avoid retaining old visible sections.

Fix rules:
- After audit, apply the smallest possible surgical changes.
- Preserve all existing field names, wire:model bindings, saved data keys, draft behavior, and submit behavior unless directly required to fix the listed bug.
- Do not rewrite the component.
- Do not rename labels except adding/fixing placeholder text.
- Do not remove fields.
- Do not change unrelated tabs.

Verification required:
After fixing, test manually or with Playwright:

A. Load:
 /hire/agent/auction/tenant

B. Confirm these fields show placeholder “Select” before selection:
- Acceptable Property Conditions
- Offered Lease Term
- Leasing Space
- Garage/Parking Features

C. Select Yes for Lease-Option Agreement.
Confirm both compensation sections allow:
- typing dollar amounts
- typing percentages
- backspacing
- clearing
- retyping
- tabbing/blur without value disappearing

D. Phone Number:
Confirm typing, deleting, clearing, and retyping works normally.

E. Personal Photo:
Upload a small image.
Confirm uploading indicator clears and user can still Save Draft and Submit.

F. Property type switching:
Switch Residential → Commercial → Residential.
Confirm stale fields from the prior property type hide correctly.

Deliverable:
Provide:
1. Root cause summary
2. Exact files changed
3. Exact fixes made
4. Verification results
5. Confirmation that no Create Offer Listing, Seller, Buyer, Landlord, Limited Service, route, or database files were changed