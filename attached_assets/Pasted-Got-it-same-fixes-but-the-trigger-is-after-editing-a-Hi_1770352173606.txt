Got it — same fixes, but the trigger is **after editing a Hire a Landlord listing and viewing the actual listing page** (not draft-only). Here are corrected **Replit instructions with safeguards** scoped to **Hire a Landlord → Actual Listing view after Edit/Submit/Update**.

---

# Replit Instructions — [Tenant@exp.com](mailto:Tenant@exp.com)

## Hire a Landlord Listings → Actual Listing Page (After Edit) Display + Rendering Fixes

### Applies to Residential + Commercial unless noted

---

## Applies To

* Login: **[tenant@exp.com](mailto:tenant@exp.com)**
* Flow: **Hire a Landlord’s Agent**
* Scenario: **User edits an existing Hire a Landlord listing → clicks Submit/Update → views Actual Listing page**
* Property Types: **Residential + Commercial**

---

# GLOBAL SAFEGUARDS (DO NOT SKIP)

1. **Landlord-only scope:** All changes must apply ONLY to **Hire a Landlord** components/views/routes.
2. **Do NOT modify** Tenant/Buyer/Seller flows, templates, constants, DB tables, or service lists.
3. **Do NOT rename** DB columns/tables.
4. **No destructive migrations.** Display-level fixes only unless a value is genuinely not being saved.
5. **Do NOT change the order of master service lists**—only fix how the Landlord *actual listing* renders selected services and order.
6. **Backward compatible:** Existing listings must render even if some fields are null.

---

# 1) ACTUAL LISTING LOCATION: SHOW CITY/COUNTY/STATE/ZIP — HIDE STREET ADDRESS

## Requirement

On the **actual Hire a Landlord listing page** (post-edit):

* ✅ Show: **City, County, State, Zip**
* ❌ Hide: **Property Address** (street address)

## Implementation

In the Landlord listing detail view/template:

* Remove/disable any rendering of:

  * `property_address`, `street_address`, `full_address`
* Replace with a location block that only prints:

  * `city`, `county`, `state`, `zip`

### Acceptance

After updating a listing, the actual listing page shows the 4 location fields and never shows the street address.

---

# 2) “INCLUDED STORAGE SPACE” MISSING ON ACTUAL LISTING (RES + COMM)

## Requirement

Add “Included Storage Space” to the **actual listing page**.

## Implementation

In Landlord listing detail view:

* Add row:

  * **Included Storage Space:** {{ value }}

### Acceptance

Field appears on actual listing after edit/update.

---

# 3) SERVICES ON ACTUAL LISTING: SAME ORDER AS CREATE/EDIT UI + ONLY SELECTED SERVICES

## Problem (Confirmed)

Actual listing:

* changes order
* omits selected services
* shows services that weren’t selected (or “category defaults”)

## Requirement

On actual listing page:

* Categories and services must render in **exact UI order**
* Must show **ONLY** selected services for that listing
* Must work for **Residential** and **Commercial**

---

## Implementation (Landlord Only)

### Step 3A — Identify the saved selection source

Confirm the Landlord listing record contains either:

* `selected_services` JSON array of keys/ids
  or
* category arrays stored separately
  or
* pivot table rows

✅ Use those saved selections.
❌ Do not rebuild services from a default list.

---

### Step 3B — Render using “ordered master list” filtered by selected keys

Create a Landlord-only ordered map (must match Create/Edit order exactly):

* `LandlordServices::orderedResidential()`
* `LandlordServices::orderedCommercial()`

Then render as:

* loop categories in the ordered map order
* for each category, filter items by “selected keys”
* only render category header if at least one item remains

**Do NOT alphabetize. Do NOT DB-sort. Do NOT dedupe by label.**

### Acceptance

* Residential: actual listing matches selection + order exactly
* Commercial: actual listing matches selection + order exactly

---

# 4) BROKER COMPENSATION DISPLAY: MISSING NUMBER INPUTS (RESIDENTIAL)

## 4A — Tenant’s Broker Commission Fee missing numeric value

Requirement:
Display numeric + type label.
Example:

* **10 Percentage of the First Month’s Rent**

Implementation:
On Landlord actual listing view:

* output `fee_value` + `fee_type_label`

---

## 4B — Payment Timing for Broker Fees missing number input

Requirement:
Display numeric days + timing label.
Example:

* **Paid 3 Calendar Days After Executed Lease**

Implementation:
Render:

* `payment_timing_days` + `payment_timing_label`

### Acceptance

Both numeric values show on actual listing after edit.

---

# 5) LEASE-OPTION LABELS MUST MATCH CREATE/EDIT SCREEN (RESIDENTIAL)

## Requirement

Replace listing display labels with exactly:

1. **Compensation for Creating the Lease-Option Agreement**
2. **Compensation if Purchase Option is Exercised**

Implementation:
Text-only change on Landlord actual listing template.

---

# 6) COMMERCIAL: REMOVE “FURNISHING” FROM ACTUAL LISTING DISPLAY

## Requirement

Commercial listings must never show Furnishing.

Implementation:
Wrap furnishing display with:

* render only if property type = Residential (or non-commercial)

---

# 7) COMMERCIAL: GARAGE/PARKING FEATURES MISSING

## Requirement

Add Garage/Parking Features to actual listing display.

Implementation:
Render saved selection(s) exactly as stored.

---

# 8) COMMERCIAL: LEASING SPACE DEPENDENT FIELDS NOT RENDERING (ENTIRE SPACE)

## Requirement

If Commercial Leasing Space = “Entire Building/Entire Property”:
Display:

* Included Storage Space
* Storage Space Size
* Space Features
* Neighboring Tenants Include

Implementation:
On actual listing template, add conditional block:

* If leasing_space == ENTIRE:

  * render all dependent fields

---

# 9) COMMERCIAL: “OTHER” SHOULD SHOW USER INPUT (NOT THE WORD “OTHER”)

## Applies To

* Tenant Pays
* Owner Pays
* Terms of Lease

Requirement:
If “Other” selected + custom text exists:

* Show the custom text instead of the word “Other”.

Implementation:
Display transform only:

* Replace “Other” token with corresponding `*_other_text`

---

# 10) COMMERCIAL: LEASE RENEWAL/EXTENSION FEE MISSING NUMBER + MONTHS

## Requirement

Show:

* numeric value + fee type label
* AND show associated months value if present

Example:

* **100 Percentage of Month’s Rent (3 Months)** *(format can match your UI, but must show both values)*

---

# 11) COMMERCIAL: SALES TAX SHOWS BUT VALUE BLANK

## Requirement

Sales Tax should:

* Display only if value exists
* Otherwise hide the row entirely on actual listing

Implementation:
Conditional render:

* if `sales_tax_value` not empty → show
* else → do not render

---

# 12) IMPORTANT: THIS MUST WORK AFTER “EDIT LISTING” UPDATE FLOW

## Requirement

After clicking **Submit/Update** on Edit Listing:

* user should land on the actual listing page
* the listing page must render the corrected display rules above

### Implementation Check

Confirm the post-update redirect goes to the Landlord listing show route, and that the show route uses the corrected Landlord-only template.

---

# QA CHECKLIST (MUST PASS)

### Residential (after edit/update)

* [ ] City/County/State/Zip visible
* [ ] Address hidden
* [ ] Included Storage Space shows
* [ ] Tenant broker fee numeric shows
* [ ] Payment timing numeric shows
* [ ] Lease-option labels match create/edit screen
* [ ] Services only selected + correct order

### Commercial (after edit/update)

* [ ] Furnishing not shown
* [ ] Garage/Parking Features shown
* [ ] Entire-space dependent fields shown
* [ ] Other fields show custom input text
* [ ] Renewal fee numeric + months shown
* [ ] Sales tax hidden if blank
* [ ] Services only selected + correct order

### Regression

* [ ] Tenant/Buyer/Seller listing pages unchanged

---

## One last critical note (to prevent more “missing selected services”)

If services are currently stored as **labels** instead of **keys**, Replit must NOT compare strings with punctuation differences. The safe approach is:

* store **service keys** (stable IDs)
* render labels from the ordered map

But do **not** migrate existing data destructively—only implement key usage going forward if needed.

---

If you paste the **Landlord “actual listing” route name** (or the Blade/Livewire filename that renders the show page), I can rewrite this in your exact “Location → File → Change Required → Before/After” format without guessing components.
