Below are **Replit instructions with safeguards** to fix the **Hire a Seller’s Agent → Actual Listing View (Residential + all property types where noted)** using **[tenant@exp.com](mailto:tenant@exp.com)** as the test login. This is written so your dev can implement without guessing, while keeping the output **identical in structure to Hire a Buyer’s Agent listing view**.

---

## Repro (must use for QA)

1. Login: **[tenant@exp.com](mailto:tenant@exp.com)**
2. Create **Hire a Seller’s Agent** listing (Residential first, then repeat for Commercial/Income/Vacant Land).
3. Fill:

   * City / County / State / Zip
   * Appliances Included (multiple options)
   * Pool (Yes + type if applicable)
   * Pets = Yes, Pet Types, Max Weight, Pet Restrictions
   * Offered Financing/Currency (select at least 2 + sub-questions)
   * Services (select many across multiple sections)
   * Broker Comp & Agency fields noted below
4. Submit, open the **actual listing show page**.
5. Compare to expected output below.

---

# 1) City / County / State / Zip missing (ALL PROPERTY TYPES)

### Problem

These fields are not rendering on the Seller actual listing show page.

### Replit Instructions

**A. Identify the Seller show page template**

* Locate the Blade/view used for: **Hire a Seller’s Agent → actual listing display** (show page).
* Find the “Location” / “Property Details” section.

**B. Confirm the data source (this is usually the real cause)**

* Determine where Seller listings store location fields:

  * Either as direct columns (e.g., `city`, `county`, `state`, `zip`)
  * OR inside a JSON blob (e.g., `property_details->city`)
  * OR in a related details table (e.g., `sellerListingDetails->city`)
* Update the show page to read from the **same source that create/edit saves to**.

**C. Render these four lines for every Seller listing**

* City:
* County:
* State:
* Zip Code:

### Safeguards

* **Do not render from request/session/draft objects.** Show page must render from the **persisted listing record** only.
* If address is hidden, that’s fine — but **City/County/State/Zip must ALWAYS display** (non-sensitive).
* If any are missing, show **“Not Provided”** (so you can detect save issues), but still fix mapping.

### Acceptance Criteria

On actual listing (all property types):

* **City: ___**
* **County: ___**
* **State: ___**
* **Zip Code: ___**

---

# 2) Appliances Included formatting must match Amenities / Property Features

### Problem

Appliances Included isn’t formatted like other multi-select “options” sections.

### Replit Instructions

**A. Locate how Amenities/Property Features are displayed**

* Find the Buyer/Landlord/Tenant show page block that renders “Amenities” as **option chips / bullet list / stacked selections**.

**B. Reuse that same rendering method for Appliances Included**

* Appliances Included must render as:

  * Either the exact same “pills/chips”
  * Or the same bullet list style used in Amenities/Property Features
* Ensure it supports a multi-select array (or CSV/string if that’s how it’s stored).

### Safeguards

* If stored as JSON array: render each item.
* If stored as comma-separated string: split/trim before rendering.
* Preserve the **same label casing** used elsewhere.

### Acceptance Criteria

Example:

* **Appliances Included:**

  * Refrigerator
  * Range
  * Dishwasher

---

# 3) Pool not showing

### Problem

Pool field isn’t rendering on Seller show page.

### Replit Instructions

* Add **Pool** to the Seller show page “Property Details” section.
* Render:

  * **Pool: Yes/No**
  * If you have subfields (Pool Type / Screened / Heated), display them under Pool when provided.

### Safeguards

* Pool often breaks due to boolean normalization. Treat as “Yes” when value is: `true`, `1`, `"1"`, `"Yes"`.
* If Pool = No, still show **Pool: No** (if user explicitly selected it).

### Acceptance Criteria

* **Pool: Yes** (and subtype if exists) OR **Pool: No**

---

# 4) Pets: Label + “Not Provided” bug for Max Weight & Restrictions

### Problems

* “Pet Types” label should be **Acceptable Pet Types**
* Max Weight and Pet Restrictions show **Not Provided** even though they were entered.

## 4A) Fix label

### Replit Instructions

* Change show label text from:

  * `Pet Types`
  * to **Acceptable Pet Types**

## 4B) Fix values incorrectly showing Not Provided

### Root causes (one of these is happening)

* The show page is reading the wrong key (common).
* The show page checks the wrong “Pets allowed” condition (so it falls into Not Provided).
* The field is stored under a different path for Seller than Buyer/Landlord.

### Replit Instructions

1. In the Seller show page, locate the Pets block.
2. Verify the stored keys used by create/edit for:

   * `pets_allowed`
   * `pet_types` (or `acceptable_pet_types`)
   * `max_pet_weight`
   * `pet_restrictions`
3. Update show page to read the exact same keys.

### Safeguards

* Conditional display logic must be:

  * If Pets Allowed = Yes → display Pet Types + Max Weight + Restrictions
* When Pets Allowed = Yes but a subfield is empty:

  * display **Not Provided**
* When Pets Allowed = No:

  * do **not** show the subfields (or show them as N/A, whichever your platform standard is).

### Acceptance Criteria

When Pets Allowed = Yes:

* **Acceptable Pet Types: Dogs, Cats**
* **Maximum Weight Per Pet (lbs): 50**
* **Pet Restrictions: No aggressive breeds**

---

# 5) Offered Financing/Currency Terms format must match Buyer listing

### Problem

Seller show page format doesn’t match Buyer: it must display the **header first**, then the questions underneath.

### Replit Instructions

**A. Locate Buyer show page financing block**

* Find the Buyer listing show template section that prints:

  * **Offered Financing/Currency Terms:**
  * then lists selected terms
  * then shows the follow-up Q&A for those terms

**B. Copy that exact structure into Seller show page**

* Use the same order:

  1. **Offered Financing/Currency Terms:** (header)
  2. Selected terms list
  3. Follow-up questions/answers beneath

### Safeguards

* Do not display the section if no terms were selected.
* If “Other” selected: show the user’s “Other” text value.
* Ensure the Seller show page reads the same financing data structure that Seller create/edit saves.

### Acceptance Criteria

Seller show page displays header first, identical to Buyer.

---

# 6) Services order must be identical to create order (Residential list you provided)

### Problem

Some services are rendering out of order.

### Replit Instructions

**A. Stop rendering services based on “whatever comes back”**

* Don’t loop over selected services directly if that loses ordering.

**B. Implement a canonical ordered list for Seller Residential services**

* Create/confirm a single “Seller Residential Services Order” array/config in the same place you store other service order configs.
* That ordered list must match EXACTLY the sequence you provided (📢 … ✍️ Additional Services).

**C. Rendering logic**

* On show page:

  * Loop through the canonical ordered list
  * For each item, display it only if selected
  * Preserve headings (📢, 🛠️, 📸, 🏡, 📑, 🧾, 💡, ✍️)

### Safeguards

* This must be property-type aware:

  * Residential uses the Residential canonical list
  * Commercial/Income/etc use their own canonical lists
* Prevent duplication if a service is stored in multiple places (e.g., “Additional Services” free text).

### Acceptance Criteria

Services on Seller show page appear exactly in the same order as your provided list.

---

# 7) Broker Compensation & Agency Agreement Terms must match Buyer format (Seller-specific fixes)

## 7A) Seller’s Broker Purchase Fee missing

### Replit Instructions

* Add field output under **Broker Compensation:** on the Seller show page:

  * **Seller’s Broker Purchase Fee: [value]**

### Safeguards

* Render only if present; otherwise “Not Provided”.

## 7B) Buyer’s Broker Commission Structure shows OLD options

### Root cause

The show page is using an outdated mapping (label dictionary) or legacy enum values.

### Replit Instructions

1. Find the mapping used to convert stored values → display labels for this field on Seller show page.
2. Replace it with the **current options list used on the Seller create form**.
3. If the stored value is already the label text, don’t remap it—display as-is.

### Safeguards

* Backward compatibility:

  * If listing was created before the new options, map old values to nearest new label.
  * Otherwise, default to showing raw stored value (so it never appears blank).

## 7C) Seller’s Broker Leasing Fee must show numeric input + correct label

### Problem

It displays generic text like “Percentage of the Rent Due Each Rental Period” instead of the actual input result like “100% of the Rent Due Each Rental Period”.

### Replit Instructions

* Update the show output to include:

  * the **number** entered (e.g., 100)
  * plus the descriptor text
* Example output must be:

  * **Seller’s Broker Leasing Fee: 100% of the Rent Due Each Rental Period**

### Safeguards

* If user chose a different leasing fee type (flat fee, months, etc.), format must mirror Buyer format logic:

  * Show the full formatted phrase, not just the type name.

## 7D) Legal terms formatting + missing fields

### Fix capitalization

* **Protection Period Timeframe:** “90 Days” (capitalize Days)
* **Early Termination Fee:** “Yes/No” must be capitalized

### Show missing fields

* Termination Fee Amount
* Retainer Fee
* Retainer Fee Amount
* Retainer Fee Application

### Retained deposits must be % not $

* **Seller’s Broker Share of Retained Deposits: 3%**

### Replit Instructions

1. In Legal Terms section of Seller show page, ensure it renders these lines in order:

   * Protection Period Timeframe
   * Early Termination Fee
   * Termination Fee Amount (only if Early Termination Fee = Yes)
   * Retainer Fee
   * Retainer Fee Amount (only if Retainer Fee = Yes)
   * Retainer Fee Application (only if Retainer Fee = Yes)
   * Seller’s Broker Share of Retained Deposits (format percent)
2. Apply formatting helpers:

   * currency formatting for $1,000 / $5,000
   * percent formatting for 3%

### Safeguards

* Conditional rendering must be driven by normalized Yes/No values (handle `true/1/"Yes"`).
* Never show `$3` when it should be `3%`:

  * If stored as `3`, display `3%`
  * If stored already includes `%`, display as-is

---

# 8) Seller’s Current Status placement

### Problem

It’s displaying in the wrong section.

### Replit Instructions

* Move “Seller’s Current Status” to the **Seller Info** section of the show page (not Property Details / not Broker Comp).
* Keep label exactly:

  * **Seller’s Current Status: [value]**

### Safeguards

* Must render for all property types if field exists.
* If blank: Not Provided.

---

## Final QA Checklist (must pass before marking done)

Using [tenant@exp.com](mailto:tenant@exp.com), confirm on actual listing:

* City/County/State/Zip show for **all property types**
* Appliances Included renders like Amenities/Property Features (same style)
* Pool shows
* Pets:

  * Label is “Acceptable Pet Types”
  * Max Weight + Pet Restrictions show values (no false Not Provided)
* Offered Financing section matches Buyer structure (header first)
* Services show in your exact provided order
* Broker Comp:

  * Seller’s Broker Purchase Fee shows
  * Buyer’s Broker Commission Structure uses current options
  * Leasing Fee displays as “100% of the Rent Due Each Rental Period” style
  * Legal terms: capitalization, missing amounts display, retained deposits show as %
* Seller’s Current Status appears under Seller Info

---

If you tell me the **exact Replit file names** (or paste a snippet of the Seller show Blade + the Buyer financing block), I’ll rewrite this into **line-by-line, file-specific instructions** (“edit X file, replace Y block, reuse Z partial”) with zero ambiguity.
