Below is a **copy/paste-ready Replit instruction set** (with safeguards) for **Hire a Seller’s Agent → Offered Financing/Currency** to fix: **old terms/questions, alphabetical order (Assumable first), correct question order matching the actual listing, and missing answers showing BLANK (no “Not Provided”).**
Use **[tenant@exp.com](mailto:tenant@exp.com)** for QA.

---

## ✅ REPLIT COPY/PASTE INSTRUCTIONS (WITH SAFEGUARDS)

### Feature: Hire a Seller’s Agent → Offered Financing/Currency (Create + Edit + Actual Listing View)

### Login for testing: [tenant@exp.com](mailto:tenant@exp.com)

### Applies to: ALL Seller property types (Residential, Income, Commercial/Business, Vacant Land)

---

# 0) REQUIRED OUTCOME (DO NOT SKIP)

1. The Seller **create form**, **edit form**, and **actual listing show page** must use **ONE shared canonical config** for:

   * Term labels (current wording only)
   * Term order (alphabetical by displayed label)
   * Follow-up question labels (current wording only)
   * Follow-up question order (exact order as the actual listing format)
2. **No legacy/old-format wording** may appear anywhere.
3. On the **actual listing view**: if an answer is missing, it must be **BLANK** (no “Not Provided”, no “Not Provided Yet”, no “N/A”, no dashes).
4. Terms must display **alphabetically** everywhere:

   * Example: **Assumable Terms** must appear first.
5. Questions must display in the **exact same order as defined by the canonical config** and must match what the Seller actual listing format expects.

---

# 1) CREATE A SINGLE CANONICAL CONFIG (SOURCE OF TRUTH)

### Task

Create a single canonical “Seller Offered Financing/Currency” config and use it everywhere.

### Implementation

1. Create a config file / constant class / helper that returns a structured array for Seller Offered Financing/Currency.
2. Each term must include:

   * `term_key` (stable internal key)
   * `term_label` (display label)
   * `questions` (ordered array)
3. Each question must include:

   * `question_key` (stable internal key)
   * `question_label` (display label EXACTLY as intended)
   * `type` (text/number/select/yesno/multiselect)
   * `options` (if select)
   * `order` (integer for ordering)

### SAFEGUARDS

* **term_key/question_key must never be the label text** (labels change).
* All renderers (create/edit/show) must call the same config function.
* Do not duplicate term/question definitions in Blade templates.

---

# 2) ENFORCE ALPHABETICAL TERM ORDER (CREATE + EDIT + SHOW)

### Task

Terms must render alphabetically by `term_label` (case-insensitive).

### Implementation

* Before rendering term checkboxes (create/edit) and before rendering selected terms (show page), sort:

  * `sort terms by strtolower(term_label)`

### SAFEGUARDS

* Do not use DB order, insertion order, or selection order.
* Ensure **Assumable Terms** appears before Cash/Conventional/etc.

---

# 3) FIX “OLD FORMAT” TERMS/QUESTIONS BY REMOVING LEGACY MAPPINGS

### Task

Remove any legacy enum → label maps or hardcoded arrays used only in Seller flow.

### Implementation

1. Search Seller code for:

   * old label arrays
   * old enums for financing/currency
   * hardcoded question text in Blade
2. Replace all references with the canonical config labels.

### SAFEGUARDS

* If any Seller code path is not using the canonical config, it will drift again.
* Add a quick dev assertion/log (in dev only): if a rendered term/question key is not found in config, skip it.

---

# 4) DATA STORAGE STANDARD (PREVENT MISMATCH)

### Task

Normalize how Seller financing terms and answers are saved.

### Required structure

* `selected_terms` = array of `term_key`
* `answers` = nested map:

  * `answers[term_key][question_key] = value`

### Implementation

1. On create/edit save:

   * Save selected term keys into `selected_terms`
   * Save each answer into the nested `answers` structure using keys
2. Ensure draft/edit reload populates from the same structure.

### SAFEGUARDS

* Do not save answers keyed by label text.
* Validate keys on save:

  * If term_key/question_key not found in canonical config, do not save it.

---

# 5) CREATE + EDIT UI RENDERING RULES (SHOW ALL QUESTIONS WHEN TERM SELECTED)

### Task

When a user selects a term checkbox, show all follow-up questions for that term in the exact configured order.

### Implementation (Create/Edit forms)

1. Render term checkboxes sorted alphabetically.
2. When term is selected, render:

   * `for question in config[term].questions sorted by question.order:`

     * render the input field with `question_label`
3. Inputs must bind to:

   * `answers[term_key][question_key]`

### SAFEGUARDS

* Question order must come from config `order` only.
* Do not render questions by iterating saved answers (that breaks ordering and drops questions).

---

# 6) ACTUAL LISTING VIEW (SHOW PAGE) RENDERING RULES

## 6A) HEADER STRUCTURE MUST MATCH BUYER FORMAT

### Task

Seller show page must match Buyer structure:

* Show **Offered Financing/Currency Terms:** first
* Then show selected terms (alphabetical)
* Then show the answered questions under each term

### Implementation (Show page)

1. If `selected_terms` is empty → do not render the section at all.
2. Otherwise render:

   * **Offered Financing/Currency Terms:**
3. Sort selected terms alphabetically by config term_label.
4. For each selected term:

   * Render the term label
   * Render questions in config order (question.order)

## 6B) MISSING ANSWERS MUST BE BLANK (NO PLACEHOLDERS)

### Your rule (must follow)

If an answer is missing, it should be blank — meaning:

* Do **NOT** show “Not Provided”, “Not Provided Yet”, “N/A”, “—”
* Preferred implementation: **skip the entire question line** when value is empty

### Implementation

For each question:

* Read `value = answers[term_key][question_key]`
* If `value` is null/empty string/whitespace/empty array:

  * **DO NOT RENDER THAT QUESTION ROW**
* Else render:

  * `Question Label: value`

### SAFEGUARDS

* Define “empty” carefully:

  * Treat `null`, `""`, `"   "`, `[]` as empty
  * Treat numeric `0` as NOT empty if it’s a valid answer
* Never print fallback text.

---

# 7) BACKWARD COMPATIBILITY (OLD SAVED DATA MUST STILL DISPLAY CORRECTLY)

### Task

If older Seller listings stored answers under old keys, map them to new keys at display time.

### Implementation

1. Create a mapping dictionary:

   * `legacy_term_key -> new_term_key`
   * `legacy_question_key -> new_question_key` per term
2. On load for show/edit:

   * Convert legacy data into the new `selected_terms` + `answers` structure before rendering.

### SAFEGUARDS

* If a legacy key cannot be mapped, ignore it (do not display broken labels).
* Do NOT overwrite stored legacy data unless you are doing a controlled migration.

---

# 8) QA TEST SCRIPT (MUST PASS BEFORE MERGE)

Using **[tenant@exp.com](mailto:tenant@exp.com)**:

## Create Test

1. Create Seller listing → Offered Financing/Currency:

   * Select at least 5 terms including **Assumable Terms**
   * Fill some questions, leave others blank
2. Confirm on CREATE screen:

   * Terms are alphabetical
   * Questions appear for selected terms in correct order

## Show Page Test

3. Submit listing → open actual listing:

   * Section shows header: **Offered Financing/Currency Terms:**
   * Terms display alphabetical (Assumable first)
   * Only answered questions appear (blank ones do not show at all)
   * No “Not Provided” anywhere

## Edit Test

4. Edit listing:

   * Previously entered answers populate correctly
   * Ordering remains alphabetical
   * Update answers, save, verify show page matches

---

# 9) FINAL SAFETY RULES (DO NOT VIOLATE)

* Do not hardcode Seller financing labels/questions in Blade.
* Do not use any “fallback” display text on show page.
* Ordering must always come from the canonical config:

  * Terms alphabetical by label
  * Questions by explicit order field
* All property types must use the same Seller financing engine (config may vary by property type only if required, but still must be canonical and consistent).

---

If you want, paste the **exact current term list** (or a screenshot of the Seller financing section on the actual listing) and I’ll format the canonical config list **with your exact labels + question order** so Replit can implement it without inventing wording.
