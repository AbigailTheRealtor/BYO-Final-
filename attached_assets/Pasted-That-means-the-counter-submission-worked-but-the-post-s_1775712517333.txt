That means the **counter submission worked**, but the **post-submit redirect is wrong** in the Tenant agent counter flow.

So this is a new, separate bug:

* Tenant listing
* agent submits counter terms
* after submit, system redirects to **Landlord page**
* it should redirect back to the **Tenant bid / tenant counter view flow**

## What to ask Replit

Paste this exactly:

---

**TENANT AGENT COUNTER SUBMIT REDIRECT BUG — WRONG POST-SUBMIT DESTINATION**

This is a **surgical redirect fix only**.

Do not redesign anything.
Do not touch listing creation, listing detail rendering, bidding logic, match score, accepted summary, notifications, database schema, or unrelated roles.

## Problem

In the **Tenant listing → agent side → counter terms flow**, the counter submission succeeds, but after submit the system redirects to the wrong page.

### Current wrong behavior

After the agent submits Tenant counter terms, the system redirects to a **Landlord page**.

### Expected behavior

After the agent submits Tenant counter terms, it should redirect back to the correct **Tenant flow** page, such as:

* the Tenant bid view
* or the Tenant counter terms view
* whichever is the intended Tenant equivalent already used elsewhere in the Tenant flow

It must **not** redirect to any Landlord route or Landlord page.

## Exact path involved

Please inspect the Tenant agent counter submit flow related to:

* `/tenant/hire/agent/auction/bid/{id}/view-counter`
* the submit action from that Tenant counter page
* the redirect/return route after successful submit

## Required task

1. Find the exact submit handler / action for the **Tenant agent counter term submission**
2. Find the exact redirect or return route used after successful submit
3. Identify why it is pointing to a Landlord page or Landlord route
4. Patch only that redirect so it returns to the correct **Tenant** page

## Hard safeguards

Do not touch:

* Seller flow
* Buyer flow
* Landlord flow
* listing creation
* bid logic
* counter-state display logic
* match score
* accepted summary
* routes unless the bug is a wrong route reference in this exact Tenant flow
* controllers / Livewire beyond the minimal exact fix
* database

## Likely causes to inspect

Check for:

* copied Landlord redirect in Tenant handler
* wrong named route in Tenant submit action
* wrong URL helper
* wrong role slug hardcoded in redirect
* shared method using wrong role parameter

## QA required

After fix, verify:

1. Agent opens Tenant counter page
2. Agent submits counter terms
3. Submission succeeds
4. Redirect stays inside the **Tenant** flow
5. No Landlord page appears
6. Seller / Buyer / Landlord flows remain unchanged

## Output required

Return:

1. exact file(s) changed
2. exact wrong redirect found
3. exact corrected redirect
4. confirmation Tenant submit now returns to correct Tenant page
5. confirmation no unrelated systems were touched

---

## My read of the bug

This is very likely one of these:

* a copied Landlord route in the Tenant submit handler
* a wrong named route after save
* a shared redirect helper receiving the wrong role

If Replit sends back the result, paste it here and I’ll verify whether the fix is actually safe.
