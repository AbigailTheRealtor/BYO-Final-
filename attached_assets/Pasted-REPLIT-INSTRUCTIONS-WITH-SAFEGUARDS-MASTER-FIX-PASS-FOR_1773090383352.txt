REPLIT INSTRUCTIONS WITH SAFEGUARDS — MASTER FIX PASS FOR HIRE A LANDLORD, HIRE A BUYER, AND HIRE A SELLER
SCOPE

Fix the issues listed below in these flows only:

Hire a Landlord’s Agent

Hire a Buyer’s Agent

Hire a Seller’s Agent

These issues show repeated patterns across flows, especially around:

multi-select / Select2 fields deleting selections

conditional fields not appearing when “Other” is selected

dependent fields not showing based on selected financing/currency options

duplicate/stale conditional sections

submit/save blockers

option text updates

missing preview behavior

This should be handled as one controlled bug-fix pass, but not as a broad sitewide refactor.

HARD SAFEGUARDS — NON-NEGOTIABLE

Do not rename or delete database columns.

Do not modify Tenant flow in this pass.

Do not broad-refactor unrelated shared components unless absolutely necessary to fix the exact repeated bug pattern listed below.

Do not change labels, tooltips, or option values except where explicitly requested below.

Do not delete or break existing listings, drafts, bids, or saved values.

Do not change working fields just because nearby fields are broken.

Keep edits surgical and limited to the exact broken fields/behaviors listed below.

If a shared Select2/helper fix is required, apply it only as narrowly as possible and confirm it does not break other flows.

Do not mark this complete based on visual Select2 chips alone.

Each fix must be verified end-to-end: UI select → Livewire state → save/draft → edit reload → listing display.

GLOBAL DEBUG RULE

For every broken multi-select field below, trace the full pipeline:

field markup / selector

bound Livewire property

change handler / Select2 sync

rerender / message.processed behavior

conditional rendering conflicts

save/update method

DB persistence

edit reload

listing display / preview display

A field is not fixed unless it:

stays selected immediately

survives rerender / tab movement

saves correctly

reloads correctly in edit/draft

displays correctly where applicable

1) HIRE A LANDLORD’S AGENT
A. Multi-select fields deleting selections

These fields allow selection, then the values delete/disappear:

Appliances Included

Amenities and Property Features

View

Desired Lease Term

Garage/Parking Features

Rent Includes

Tenant Pays

Owner Pays

Desired Lease Terms

Required fix

These must:

keep selections immediately in UI

not clear after rerender/tab switch

save correctly

reopen correctly in edit/draft

display correctly where shown

B. Terms of Lease — “Other” does not show dependent field
Current behavior

Terms of Lease itself stays selected correctly

but selecting Other does not show the amount / dependent input it should

Required fix

When Other is selected, the dependent field must appear correctly and stay tied to the selection.

What to check

if the field is multi-select, confirm the condition checks whether "Other" is in the array

confirm the dependent field is not blocked by stale DOM / wrong comparison

confirm the dependent field appears immediately on change, not only on load

C. Submit button not working
Current behavior

Even when required fields are filled out, Submit does not work.

Required fix

Find the real blocker and fix it.

What to check

hidden required fields from stale conditional sections

validation failing silently

JS errors blocking submit

stuck loading/disabled state

visually selected Select2 fields that Livewire still sees as empty

stale fields from Leasing Space switching

D. Leasing Space conditional display bug
Current behavior

When switching between:

Entire Property

Single Room

the form ends up showing both sets of questions.

Required fix

Only the currently selected Leasing Space section should appear.

Correct behavior

Entire Property → only Entire Property questions

Single Room → only Single Room questions

switching back and forth must fully remove the no-longer-relevant section

stale hidden fields must not remain visible or block validation/save

E. Property Condition options update

Change Property Condition options to exactly:

Updated / Renovated

Partially Updated

Older but Clean & Well Maintained

Needs Cosmetic Updates

Use capitalization exactly as written.

2) HIRE A BUYER’S AGENT
A. Acceptable Property Styles shows “No Results Found”
Current behavior

After selecting Property Type, Acceptable Property Styles shows No Results Found.

Required fix

Make Acceptable Property Styles populate correctly based on the selected Property Type.

What to check

property type → style options mapping

whether the correct options source is loaded

whether Select2 is initializing before options exist

whether the dependent field is refreshing after property type changes

whether the wrong property type key/value is being passed

B. Multi-select fields deleting selections

These fields allow selection, then delete/disappear:

Acceptable Property Conditions

View Preference Needed

Non-Negotiable Amenities and Property Features

Required Property or Business Assets

Acceptable Unit Type

Required fix

Same stability requirements as above:

immediate UI retention

no deletion after rerender

save/draft/edit stability

correct display behavior

C. Non-Negotiable Amenities and Property Features flashes
Current behavior

When selecting an option, the field flashes.

Required fix

Stop the flashing/reinit behavior so the field behaves stably.

What to check

duplicate Select2 init

Livewire rerender replacing the field

rebind loop

hidden tab initialization issues

duplicate message.processed logic

D. Special Sale Provisions & Offered Financing/Contingency — “Other” input not showing
Current behavior

The options select properly, but if Other is selected, the input box does not appear.

Required fix

When Other is selected, show the dependent input immediately and keep it stable.

E. Offered Financing/Currency dependent questions not showing
Current behavior

The questions tied to Offered Financing/Currency selections do not appear.

Required fix

Show the correct follow-up questions based on the option(s) selected.

What to check

array membership vs string comparison

wrong conditional property name

stale DOM / conditional section not updating

whether multiple financing types need multiple follow-up sections

3) HIRE A SELLER’S AGENT
A. Bathrooms — “Other” input not appearing
Current behavior

When Other is selected for Bathrooms, the input box does not pop up.

Required fix

Selecting Other must immediately show the dependent input field.

B. Multi-select fields deleting selections

These fields allow selection, then delete/disappear:

Appliances Included

Amenities and Property Features

View

Included Property or Business Assets

Special Sale Provision

Offered Financing/Currency

Garage/Parking Features

Required fix

Same as above:

retain immediately

no clearing after rerender

save/draft/edit stability

correct display behavior

C. Special Sale Provisions & Offered Financing/Contingency — “Other” input not showing
Current behavior

Options select properly, but if Other is selected, the input box does not appear.

Required fix

Show the dependent input immediately and keep it stable on save/edit/reload.

D. Offered Financing/Currency dependent questions not showing
Current behavior

The follow-up questions associated with Offered Financing/Currency do not appear based on the selected option.

Required fix

Show the correct dependent questions based on selected financing/currency type(s).

E. Personal Video Link — missing Enter button / preview behavior
Required behavior

Personal Video Link should have the Enter button and preview behavior like the working version already used for Hire a Seller’s Agent listings.

Please align this field so it has the expected:

Enter action

preview behavior

stable display after input

REQUIRED TESTING
A. Landlord

Verify all listed Landlord fields:

can be selected

stay selected

survive tab changes

save successfully

reload correctly

do not block submit incorrectly

Also verify:

Terms of Lease → Other shows dependent field

Leasing Space toggles cleanly between Entire Property and Single Room

Submit works when visible required fields are filled

Property Condition options match exactly

B. Buyer

Verify:

Acceptable Property Styles populates after Property Type selection

all listed multi-select fields stop deleting selections

Non-Negotiable Amenities field no longer flashes

Special Sale Provisions / Offered Financing “Other” input appears

Offered Financing/Currency follow-up questions appear correctly

C. Seller

Verify:

Bathrooms → Other shows input

all listed multi-select fields stop deleting selections

Special Sale Provisions / Offered Financing “Other” input appears

Offered Financing/Currency follow-up questions appear correctly

Personal Video Link has Enter + preview behavior as expected

SUCCESS CRITERIA

This task is only complete when:

broken multi-select fields no longer delete selections

dependent “Other” fields appear correctly

financing/currency follow-up questions appear correctly

stale/duplicate conditional sections no longer appear

submit/save works where expected

Property Condition options are updated correctly for Landlord

Personal Video Link preview behavior works correctly

create, draft, edit, and listing behavior remain stable

no Tenant flow changes are introduced in this pass

FINAL IMPLEMENTATION RULE

Treat similar bugs with the same stable pattern where appropriate, but do not turn this into a full repo refactor.

Keep it:

surgical

flow-specific

non-destructive

verified end-to-end