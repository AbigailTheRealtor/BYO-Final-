#862 — Deep Fix: Select2 Placeholder Consistency + Double-Init Elimination (Full Service only)

IMPORTANT SAFEGUARDS

FULL SERVICE flows ONLY.
DO NOT touch Limited Service code or anything inside initializeLimitedService().
DO NOT modify routes, controllers, validation, database schema, migrations, models, or Livewire properties.
DO NOT change native single-select wire:model dropdowns.
ONLY touch Select2 multiselect behavior.
Preserve all existing saved values, draft loading, edit loading, and tab navigation behavior.
Audit first before editing.

PHASE 1 — READ-ONLY AUDIT

Audit these files and identify:

Any global Select2 destroy/re-init loops.
Any .select2('destroy') calls running on Livewire hooks.
Any Select2 initializers missing:
placeholder: "Select"
allowClear: true
hasClass('select2-hidden-accessible') guard
Any select2-multiple fields missing:
data-placeholder="Select"
Any remaining: <option value="">Select</option> inside multiselects instead of: <option value=""></option>

Files to audit:

offer-seller-listing.blade.php
offer-seller-listing-edit.blade.php
all Hire Seller/Buyer/Landlord/Tenant Full Service tab partials listed in Task #862
select2-stable.js
select2-manager.js

Do NOT edit yet.
Return:

exact files
exact selectors
exact duplicate init paths
exact fields missing data-placeholder
exact remaining bad <option> markup

PHASE 2 — REMOVE DESTRUCTIVE GLOBAL RE-INIT

In:

resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php
resources/views/livewire/offer-listing/seller/offer-seller-listing-edit.blade.php

Remove or safely disable the global initSelect2Fields() destroy/re-init loop that:

unconditionally destroys Select2
reinitializes all .select2-multiple fields
runs on document.ready and livewire:message.processed

DO NOT remove:

select2-stable.js
initUninitialized()
repairBrokenSelects()

The goal:

eliminate triple-init race conditions
eliminate duplicate containers
stop wiping saved values after tab changes

IMPORTANT:
If any field still requires explicit init logic after removing the global loop:

move it into a field-specific init block
add hasClass('select2-hidden-accessible') guard
NEVER destroy existing initialized Select2 instances

PHASE 3 — STANDARDIZE MULTISELECT PLACEHOLDERS

For ALL Select2 multiselect fields in the affected Full Service partials:

Add:
data-placeholder="Select"

ONLY if missing.

Do NOT touch native single-select wire:model dropdowns.

PHASE 4 — FIX LEADING OPTION MARKUP

For every Select2 multiselect:

REQUIRED:

<option value=""></option>

FORBIDDEN:

<option value="">Select</option>

Do not alter native non-Select2 selects.

PHASE 5 — VERIFY ALL PER-FIELD INIT BLOCKS

In create/edit parent blades for:

Seller
Buyer
Landlord
Tenant

Verify every .select2({...}) init includes:

placeholder: "Select"
allowClear: true

AND:

hasClass('select2-hidden-accessible') guard before init

Do NOT introduce new global init loops.

PHASE 6 — REHYDRATION + DRAFT TESTING

After removing the destructive loop, verify:

Saved draft values still load.
Edit pages still preload values.
.val(saved).trigger('change.select2') still works.
Tab switches do not wipe values.
No duplicate Select2 containers appear.
Clearing a value restores placeholder "Select".

PHASE 7 — VISUAL QA

Test:

Seller Offer Listing create/edit
Hire Seller Agent create/edit
Hire Buyer Agent create/edit
Hire Landlord Agent create/edit
Hire Tenant Agent create/edit

Verify:

grey "Select" placeholder appears
no selectable "Select" option exists
clearing restores placeholder
no duplicate containers after tab switches
draft/edit values persist correctly

OUT OF SCOPE

Limited Service
Buyer/Tenant/Landlord Offer Listings
validation
controllers
DB changes
route changes
Livewire schema changes

That version is much safer because it forces Replit to:

audit first
stop using destroy/re-init
avoid touching unrelated flows
preserve rehydration order
avoid another “band-aid fix” cycle