Approved with scope clarification.

This is the correct Direct Hire / Offer Preset flow audit.

IMPORTANT IMPLEMENTATION RULES

Do NOT create a new counter summary page.
Do NOT create a new route.
Do NOT create a second summary rendering system.

The existing:
- hire_buyer_agent/view_counter_terms.blade.php
- hire_seller_agent/view_counter_terms.blade.php
- hire_landlord_agent/view_counter_terms.blade.php
- hire_tenant_agent/view_counter_terms.blade.php

are the correct submitted-counter summary pages and negotiation pages.

We only need to redirect users to the correct existing view_counter_terms route after submit.

APPROVED PHASES

PHASE 1 — Redirect fixes
Approved.

This is the highest priority and should be done first.

Update only the 7 submit() redirects listed in the audit so the Direct Hire / Offer Preset flow redirects to the correct existing view_counter_terms page.

Do NOT touch marketplace routes/components.

PHASE 2 — Controller cleanup
Approved.

Fix:
- BuyerCounteredTermsController dead-code copy/paste bugs
- AgentCounteredTermsController wrong landlord redirect

Keep changes minimal.

PHASE 3 — UI fixes
Approved, but do NOT over-engineer.

Add:
- icons
- comma formatting
- placeholder cleanup

using existing platform patterns only.

Do not introduce a second formatting system.

PHASE 4 — Referral Fee visibility
Approved.

Only show Referral & Cooperation Terms tabs when:
- listing was created by an Agent
AND
- signed-in user is an Agent

Match seller implementation pattern.

PHASE 5 — Landlord compensation display gap
Approved after targeted read confirms missing display fields.

IMPORTANT:
Do NOT alter AcceptedBidSummaryService architecture.
Do NOT add extra Client Details pages.
Client Details are already correctly part of the counter wizard.

After submit:
redirect → existing view_counter_terms page

After accept:
redirect → existing AcceptedBidSummary flow

That is the correct architecture.

Before commit:
- php -l all modified PHP files
- manually test Buyer/Seller/Landlord/Tenant flows
- verify counter-back still works
- verify accept still reaches accepted summary
- verify no marketplace flow changed