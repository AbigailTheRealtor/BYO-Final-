Yes — that top-right dropdown is still wrong and needs to be fixed.

It should mirror the **same 4 Hire Agent options** as the **Create Hire Agent Listing** button, not the old legacy listing menu.

Paste this to Replit:

---

Fix the **top-right “Hire Agent” dropdown on the agent dashboard**. It is still showing the old legacy listing menu and must be aligned with the new Hire Agent-only system.

## Problem

On the agent dashboard, the top-right **Hire Agent** dropdown still shows legacy items:

* Add Property Listing (Rental)
* Add Property Listing (Sale)
* Add Buyer Criteria Listing
* Add Tenant Criteria Listing
* Add Service Auction

This is incorrect.

The dropdown should show the **same Hire Agent listing options** as the **“+ Create Hire Agent Listing”** control.

## Goal

Make the top-right **Hire Agent** dropdown on the agent dashboard use the exact same Hire Agent role options as the Create Hire Agent Listing entry point.

## Required behavior

Replace the legacy dropdown items with ONLY:

* Hire Tenant’s Agent
* Hire Landlord’s Agent
* Hire Buyer’s Agent
* Hire Seller’s Agent

These should route to the same destinations used by the current **Create Hire Agent Listing** flow.

## Important rule

The top-right Hire Agent dropdown and the left-side “+ Create Hire Agent Listing” control must be consistent.

They should expose the same Hire Agent role choices and no legacy listing systems.

## Audit first

Before changing code, identify:

1. which Blade/partial renders the top-right Hire Agent dropdown
2. whether it is shared with other dashboards/pages
3. whether it currently uses old legacy listing config or menu data
4. what route targets the left-side Create Hire Agent Listing control currently uses

Then make the smallest safe change so both entry points match.

## Safeguards

* No DB changes
* No route deletions
* No breaking the current Create Hire Agent Listing flow
* No affecting Offer Playoff gating
* No unintended changes to unrelated roles/pages
* Stop and report if this dropdown is shared in a way that would affect user dashboards unexpectedly

## Deliverable

After the fix, report:

1. file(s) changed
2. what old dropdown items were removed
3. what new Hire Agent options were added
4. confirmation that the top-right dropdown now matches the Create Hire Agent Listing control exactly

---

That’s the right next fix.
