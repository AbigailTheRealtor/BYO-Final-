Below are **copy/paste Replit instructions with safeguards** to make **Hire a Seller’s Agent → Offered Financing/Currency** match the **Hire a Buyer’s Agent** format, specifically adding a **label before each financing type** exactly like the Buyer flow.

---

# Replit Instructions (WITH SAFEGUARDS)

## Hire a Seller’s Agent → Offered Financing/Currency

### Goal

In **Hire a Seller’s Agent listing creation + edit + drafts**, the **Offered Financing/Currency** section must display **a label before each financing type**, **identical to Hire a Buyer’s Agent**.

**Source of truth:** Copy the **Buyer** Offered Financing/Currency UI structure and apply it to **Seller**.

**Test login:** `tenant@exp.com`

---

## ✅ Required Behavior (Must Match Buyer Format)

In Hire a Seller’s Agent → Offered Financing/Currency:

* Each financing selection must show a **visible label/title line** directly above its related inputs (or within the row header), the same way Buyer does.
* Labels must remain consistent across:

  * Create listing
  * Save draft → reopen draft
  * Edit listing → submit → reload

**No “floating” inputs** without a clear label.

---

## 1) Identify Buyer’s Existing Implementation (Do Not Re-invent)

### A) Locate Buyer section

Find the **Hire a Buyer’s Agent** Offered Financing/Currency markup and logic (Blade + Livewire).

**You are copying:**

* The label/row header rendering
* The input grouping layout (each financing type as its own block)
* The conditional follow-up fields behavior

### B) Confirm Seller section exists but lacks labels

Locate the Seller Offered Financing/Currency section and compare.
Seller currently likely renders inputs without the same label wrappers.

---

## 2) Implementation Approach (Preferred: Shared Partial / Component)

### A) Create a shared Blade partial/component

Create a single reusable partial for Offered Financing/Currency, used by **both Buyer and Seller** so they can’t drift again.

Example structure:

* `resources/views/livewire/partials/offered-financing-currency.blade.php`

It should accept:

* `mode` or `role` (`buyer` vs `seller`) if needed
* the bound Livewire state object/array
* property type context if you conditionally show things

✅ **Safeguard:** Do not duplicate two separate versions long-term. Make one shared partial, then feed it role-specific copy only if necessary.

---

## 3) Seller Must Use the Same “Label Per Financing Type” Structure

### A) Financing types must render as repeatable “blocks”

Each financing type should render like Buyer:

**Block skeleton (conceptual):**

* Financing type label/header (the thing you want added)
* Checkbox/toggle (if applicable)
* The follow-up fields under that block
* Any “Other” text input under that block

**Examples of blocks:**

* Cash
* Conventional
* FHA
* VA
* Seller Financing
* Assumable
* Exchange/Trade
* Lease Option / Lease Purchase
* Cryptocurrency
* NFT
* Other

✅ **Safeguard:** The label must be tied to the financing type key, not hardcoded in random places. If the list changes, the label still displays.

---

## 4) Data Model Safeguard (Do Not Break Saved Drafts)

### A) Do NOT change how values are stored

This is a UI formatting change. Keep the Seller’s existing storage keys exactly the same.

* Same Livewire property names
* Same JSON structure in drafts
* Same DB columns/JSON keys

✅ **Safeguard:** If you rename keys, old drafts will not hydrate and will look “blank.”

### B) Add labels without changing bindings

The checkbox/input bindings must remain identical; only wrap them in the Buyer-style labeled blocks.

---

## 5) Conditional Fields Must Behave Exactly Like Buyer

For Seller Offered Financing/Currency:

* Checking a financing type reveals its follow-up fields
* Unchecking hides them
* If unchecked, clear dependent fields (optional but recommended for clean data)

### Clearing rules (safe + predictable)

When a financing type is unchecked:

* Clear that financing type’s dependent inputs only (not the whole section)

✅ **Safeguard:** Never clear other financing types’ inputs when one is toggled.

---

## 6) Draft + Edit + Listing Output Must Match

### A) Create Draft

* Select multiple financing types
* Fill follow-up fields
* Save draft
* Reopen draft → all labels present + all selected items repopulate

### B) Edit Listing

* Open existing Seller listing
* Verify labels appear for all financing types
* Save → reload page → labels persist

✅ **Safeguard:** Labels must not depend on “first render only” UI state. They must be static markup like Buyer.

---

## 7) Visual/UX Safeguards

* Match Buyer spacing, alignment, font weight, icon placement (if Buyer uses icons)
* Keep the section responsive (mobile)
* No label duplication (avoid showing label twice if Buyer already shows it)

✅ **Safeguard:** Use the same CSS classes as Buyer in Seller to avoid style drift.

---

# QA Checklist ([tenant@exp.com](mailto:tenant@exp.com))

## Create Seller Listing

1. Navigate to Hire a Seller’s Agent → Offered Financing/Currency
   ✅ Each financing type shows a label exactly like Buyer

2. Select Seller Financing + Assumable + Other
   ✅ Each selected financing type shows its own labeled follow-up fields
   ✅ Other shows its “specify” input under the Other label

## Draft Save/Load

3. Save draft → reopen
   ✅ All labels still present
   ✅ All selected financing types + inputs persist

## Edit Listing

4. Submit changes → reload
   ✅ Labels remain present and correct
   ✅ No data loss

---

## “Must Not Break” List

* Do not change Seller financing stored keys
* Do not change Buyer implementation (use it as source)
* Do not clear unrelated financing inputs on toggle
* Must work in Create + Draft + Edit consistently

---

If you want, paste a screenshot of the **Buyer** Offered Financing/Currency section (the one with the labels), and I’ll write the exact block structure (headings + label text) so your developer can match it 1:1 visually.
