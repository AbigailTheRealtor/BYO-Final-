Paste this into Replit:

Implement a **surgical Buyer parity fix** so the Buyer bid card and Buyer counter-bidding history match the working **Tenant** behavior and styling exactly.

## Goal

Fix **Buyer only** right now.

The Buyer listing/bid view must match Tenant in these two areas:

1. **Inside bid card footer state/actions**
2. **Counter Bidding History font sizing / structure**

Do NOT touch Seller, Landlord, or Tenant in this task.

---

## Part 1: Buyer bid card footer must mirror Tenant exactly

### Problem to fix

Buyer is still missing the same in-card state handling Tenant already has after counter activity.

Right now Buyer is missing these Tenant-style behaviors inside the bid card / modal footer area:

* after the user submits a counter, show the notice:

  * **You have submitted a counter offer for this bid.**
* show **View Counter Terms**
* show **Edit Counter Terms** when appropriate
* when a counter already exists, Buyer must show the same correct action/state handling Tenant already uses for:

  * accept
  * reject
  * counter
  * submitted-counter notice
  * view counter terms
  * edit counter terms

### Required behavior

Use Tenant as the exact reference implementation.

For Buyer:

* if the logged-in party already submitted a counter, show the same Tenant-style notice and buttons in the same location
* if a counter exists and actions should still appear, follow Tenant’s exact logic
* if the state should suppress certain buttons because a counter already exists, follow Tenant’s exact logic
* do not invent new Buyer-only logic
* do not leave action controls outside the bid card
* do not move the entire bid section to the bottom of the page

### Important

This is specifically the **inside bid card / modal footer state block**, not a new section elsewhere on the page.

Buyer must use the same structure Tenant already uses inside the card:

* notice banner
* View Counter Terms
* Edit Counter Terms
* Close button position
* accept/reject/counter behavior when applicable

---

## Part 2: Buyer counter bidding history must match Tenant font/spacing exactly

### Problem to fix

Buyer counter-bidding history still does not visually match Tenant.
The font sizing, spacing, and visual density are different.

### Required behavior

Use Tenant counter-bidding history as the exact styling reference for Buyer:

* same font sizes
* same heading sizes
* same spacing/padding
* same row density
* same section title size
* same label/value sizing
* same button sizing where applicable

Do not approximate.
Do not “make it similar.”
Make Buyer use the same classes/markup structure Tenant uses, wherever safely possible.

---

## Part 3: Scope control

### Edit only the smallest necessary Buyer files

Audit first and identify:

* the Tenant file/section that already handles the correct in-card counter notice + buttons
* the Buyer file/section currently rendering the wrong footer/action state
* the Tenant file/section rendering the correct counter-bidding history typography
* the Buyer file/section currently rendering the wrong typography

Then port only the missing Buyer logic/markup/styles needed for parity.

### Do NOT:

* modify Tenant behavior
* modify Seller
* modify Landlord
* move the bid card below other sections
* move the modal/action area to the bottom of the page
* refactor the whole page
* rewrite the entire counter system
* change database schema
* rename routes
* change unrelated bid logic
* touch listing type logic
* touch timer logic
* touch scoring logic

---

## Part 4: Layout safeguard

This is critical:

The fix must stay in the **current Buyer bid card area**.

Do NOT:

* re-render the action/footer block in a separate section later on the page
* append the bid state below the share section
* move the modal content container
* duplicate the state block in two places

If there is an existing Buyer outside-card state chain that is causing conflicts, remove or relocate only the incorrect Buyer-only block and place the correct Tenant-style state handling **inside the Buyer bid card/footer area**, exactly where Tenant has it.

---

## Part 5: Output required

Return:

1. exact Buyer files changed
2. exact Tenant reference files/sections used
3. what Buyer footer/action logic was added or corrected
4. what Buyer counter-history typography/spacing was changed
5. confirmation that nothing was moved below the bid card area
6. confirmation that Seller, Landlord, and Tenant were untouched

If Buyer cannot be made to match Tenant without a broader refactor, stop and report before making broad changes.
