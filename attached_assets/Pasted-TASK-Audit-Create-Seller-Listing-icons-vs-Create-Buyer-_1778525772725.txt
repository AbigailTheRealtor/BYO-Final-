TASK: Audit Create Seller Listing icons vs Create Buyer Listing icons

ISSUE:
On Full Service Create Offer Listing, Buyer Create shows field icons correctly on first load, but Seller Create is missing icons on the Listing Details tab until/unless something reinitializes.

SCOPE:
- Full Service Create Offer Listing only.
- Compare Seller Create vs Buyer Create.
- Audit first only unless the root cause is obvious and the fix is surgical.
- Do NOT touch Limited Service. Limited Service is legacy/frozen.
- Do NOT touch database, migrations, routes, validation, save/draft logic, controllers, or non-related flows.
- Do NOT globally rewrite icon JS/CSS.

FILES TO AUDIT:
1. Seller Create main/livewire blade and Seller tab partials:
   - offer-seller-listing / seller create blade files
   - offer-seller-tabs/commission-based/listing-details.blade.php
   - any Seller Create parent blade where addIconsToInputs(), DOMContentLoaded, Livewire hooks, tab hooks, or Select2 icon CSS are defined

2. Buyer Create equivalent files:
   - offer-buyer-listing / buyer create blade files
   - offer-buyer-tabs/commission-based/listing-details.blade.php
   - any Buyer Create parent blade where icon JS/CSS is defined

AUDIT QUESTIONS:
1. Do Seller Listing Details fields have the same required classes/attributes as Buyer?
   - class="has-icon"
   - data-icon="..."
   - correct input-cover wrapper
   - correct select icon wrapper class, if select/select2 is used

2. Does Seller Create run addIconsToInputs() at the same lifecycle moments as Buyer?
   Compare:
   - DOMContentLoaded
   - setTimeout/deferred first-load calls
   - livewire:load / livewire:update / message.processed
   - shown.bs.tab
   - draftLoaded or resume-draft event hooks

3. Does Seller Create have CSS that could hide icons?
   Compare:
   - .input-icon z-index
   - .input-cover positioning
   - .input-cover.has-select-icon select/select2 padding
   - select2 rendered padding/line-height rules

4. Is Seller using a different addIconsToInputs() implementation than Buyer?
   Check for:
   - previousElementSibling-based duplicate prevention
   - missing textarea/select support
   - missing data-icon-rendered sentinel
   - function defined but not called on first visible tab
   - function scoped inside a block that does not run for Seller

5. Is Seller Listing Details tab rendered differently on first page load?
   Check whether Buyer fields are present in DOM at DOMContentLoaded but Seller fields are inserted/updated later by Livewire, requiring a deferred call.

DONE LOOKS LIKE:
- Provide a read-only audit report first:
  - exact files compared
  - exact reason Seller icons are missing
  - exact difference from Buyer
  - proposed surgical fix
- If the fix is obvious and limited to Seller Create icon initialization/classes/CSS, apply it.
- After fixing, verify:
  - Seller Create Listing Details icons show immediately on first page load
  - Seller title/date/expiration/listing type icons match Buyer visually
  - Seller Select2/listing type padding does not overlap icon
  - Buyer still works
  - Landlord/Tenant were not modified
  - Limited Service was not modified

HARD SAFEGUARDS:
- Do not change form fields, labels, placeholders, tooltips, validation, wire:model names, database schema, routes, or draft/save behavior.
- Do not remove existing Seller tabs or logic.
- Do not edit Limited Service files.
- Keep changes surgical and report exact files changed.