Please do a cleanup pass before merge. The main fix is close, but 3 issues remain:

1. Remove duplicate placeholder options
Several selects now have both the old placeholder and the new one, for example:

<option value="">-- Select timeline --</option>
<option value="">Select</option>

The old placeholder must be replaced, not duplicated.

For every Direct Hire select touched in this task, leave only:

<option value="">Select</option>

Apply across:
- resources/views/hire-agent-direct/client-details/buyer.blade.php
- resources/views/hire-agent-direct/client-details/seller.blade.php
- resources/views/hire-agent-direct/client-details/landlord.blade.php
- resources/views/hire-agent-direct/client-details/tenant.blade.php
- resources/views/hire-agent-direct/counter.blade.php

2. Remove duplicate legacy Name field from counter.blade.php
In populateReviewTab(), separate First Name / Last Name fallback logic was added correctly, but the fields array still includes:

{ label: 'Name', name: 'client_name' },

Remove that from the fields array entirely.

Keep only the fallback logic above the array:
- show First Name / Last Name when available
- fallback to Name only when first/last are unavailable

3. Fix duplicate dd in submitted.blade.php
In the legacy fallback block, this currently renders two dd rows:

<dd>{{ $submittedDisplayName }}</dd>
<dd>{{ $contact['client_name'] }}</dd>

Replace it with only:

<dd>{{ $submittedDisplayName }}</dd>

because $submittedDisplayName already contains the fallback client_name value.

After cleanup, verify:
- no Direct Hire select contains two blank placeholder options
- all placeholder display text is exactly Select
- counter Review shows First Name and Last Name, not duplicate Name
- submitted pages show First Name and Last Name when available
- legacy Name fallback only appears when first/last are missing
- git diff remains limited to Direct Hire Blade/display files
- no routes, controllers, models, migrations, validation, schema, Marketplace, Offer Listing, or Limited Service files changed