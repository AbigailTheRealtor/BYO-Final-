Implement fixes from completed audit #825 ONLY.

STRICT SCOPE:
ONLY modify the Hire Landlord Agent Full Service flow.

Allowed files:
- app/Http/Livewire/HireLandlordAgent/LandlordAgentAuction.php
- resources/views/livewire/hire-landlord-agent/hire-landlord-agent.blade.php
- landlord-specific included tab partials used by this blade

ABSOLUTELY DO NOT TOUCH:
- Seller flows
- Buyer flows
- Tenant flows
- Create Offer Listing
- Edit Offer Listing
- Limited Service flows
- shared/global Select2 helpers unless landlord-specific bug cannot otherwise be fixed
- routes
- controllers outside LandlordAgentAuction
- migrations
- schema
- unrelated validation architecture
- unrelated Livewire lifecycle hooks

Fix ONLY the audit-confirmed root causes.

1. Duplicate Select2 handler stacking
Fields:
- Appliances Included
- Tenant Pays
- Terms of Lease

Fix:
- Remove duplicate message.processed rebinding behavior
- Ensure handlers are attached once only
- Do NOT add additional nested hooks
- Do NOT create duplicate Select2 containers

2. View Preference sync bug
Current issue:
Uses Livewire.emit() without a matching listener.

Fix:
Use direct @this.set('view_preference', value) syncing matching the working landlord Select2 fields.

3. Desired Lease Term draft hydration
Fix:
Ensure hydration/reload targets the real:
- element id
- wire:model
- property name

Do not use query selectors targeting non-existent wire:model attributes.

4. Lease Renewal/Extension Fee rerender bug
Fix:
Prevent rerender timing from wiping deferred child values.

Requirements:
- preserve selected dropdown option
- preserve typed child values
- persist through rerender
- persist through Save Draft
- persist after reload

Do NOT rewrite the compensation architecture.

5. Lease-option numeric fields clearing
Fields:
- Compensation for Creating the Lease-Option Agreement
- Compensation if Purchase Option is Exercised

Fix:
Remove/replace the premature wire:change flow firing before wire:model.defer syncs.

Do NOT introduce formatting masks or global currency JS.

6. Submit flashing / false required errors
Fix ONLY the landlord-specific required-field checker.

Requirements:
- Ignore Select2 aria-hidden mirror elements
- Validate actual Livewire values only
- Eliminate flickering caused by delayed banner reset after morphdom rerenders

Do NOT rewrite full validation flow.

7. Save Draft fix
Fix:
Ensure syncLandlordSelect2BeforeSave() executes before draft serialization.

Confirm:
- view_preference saves
- restored draft reloads correctly

Implementation rules:
- Surgical fixes only
- Reuse existing landlord patterns already working elsewhere in the same form
- No global refactors
- No duplicate initialization blocks
- No new abstraction layers
- No unrelated cleanup

Required verification:
Run browser verification after fixes.

Must verify:
1. Placeholder “Select” appears correctly
2. View Preference persists
3. Desired Lease Term restores after draft reload
4. Lease Renewal/Extension Fee persists correctly
5. Lease-option numeric fields no longer clear
6. Submit no longer flashes Offered Lease Term / Leasing Space incorrectly
7. Save Draft works
8. Reloaded draft restores all affected fields

Deliverable:
Provide:
- exact files changed
- exact root cause fixed in each file
- confirmation no non-landlord flows were modified
- browser test results for all 8 acceptance tests