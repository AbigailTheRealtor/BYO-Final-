Got it — this is the same class of issue as your Property Conditions bug: **Select2 shows selections, but Livewire isn’t reliably syncing/saving an array**, so the listing ends up blank or only one value.

Below are **copy-paste Replit instructions with safeguards** to make **Acceptable Unit Type** work for **Hire a Buyer’s Agent → Income property**, allowing **multiple selections** in **Create / Edit / Draft**, and ensuring the selected values **display on the actual listing**.

---

## Replit Instructions With Safeguards

### Hire a Buyer’s Agent (Income) — “Acceptable Unit Type” must support multi-select + persist + display

**Login example:** `tenant@exp.com` → Hire a Buyer’s Agent → choose **Income** property type
**Field:** “Acceptable Unit Type” (DB field you referenced earlier: `number_of_unit_type`)
**Requirement:** user can select **more than 1** unit type and the listing must display them.

---

# 1) Source of truth: confirm the Livewire property name + DB field

## Must be true (no exceptions)

* Livewire property (array): `public array $number_of_unit_type = [];`
* DB column (json/text storing array): `number_of_unit_type`

**Safeguard:** Do not create a second field like `acceptable_unit_type`. Use the existing DB column and make it actually save.

---

# 2) Make the dropdown a TRUE Select2 multi-select everywhere

In the Buyer Income listing form Blade (create + edit + draft):

### Ensure the `<select>` has:

* `multiple="multiple"`
* `wire:ignore` (because Select2)
* A stable id like `id="number_of_unit_type"`

Example pattern:

* `name="number_of_unit_type[]"` (optional, but helpful)
* `multiple`

**Safeguard:** If your `select` is missing `multiple`, Select2 will behave like single select even if it looks like it can tag.

---

# 3) REQUIRED: Dual-sync Select2 → Livewire (this is the fix)

You must update Livewire **directly** in JS on every change.

### In the Buyer create JS + Buyer edit JS (and draft if separate), add:

* On change:

  * `values = $('#number_of_unit_type').val() || [];`
  * `@this.set('number_of_unit_type', values);`

If you use a JSON bridge hidden field (highly recommended for hidden tabs / Livewire timing):

* also set:

  * `@this.set('number_of_unit_type_json', JSON.stringify(values));`

✅ **This is mandatory.**
**Safeguard:** Do not only set a hidden input value and hope Livewire picks it up. It won’t reliably on hidden tabs.

---

# 4) Handle hidden tab rendering (prevents “flash then disappears” / no selection)

Because Income fields often live in non-default tabs, Select2 can initialize while hidden and break.

### Add these safeguards:

1. Initialize Select2 ONLY when the tab is visible (bootstrap `shown.bs.tab`)
2. Or set Select2 `width: '100%'` and re-init when the Income section becomes visible

**Safeguard:** Do not destroy/recreate without re-binding the change handler. If you re-init, re-attach your `.on('change', ...)` handler every time.

---

# 5) Backend save safeguard (normalize BEFORE saving)

In the Buyer Livewire component save method that writes to DB:

### Required:

* If you use `number_of_unit_type_json`, decode it and overwrite the array property:

Rules:

* If json exists and decodes to array → assign to `$this->number_of_unit_type`
* Ensure `$this->number_of_unit_type` is always an array of strings
* Remove duplicates + reindex

✅ Allowed cleanup:

* trim strings
* `array_values(array_unique($arr))`

❌ Not allowed:

* anything that converts to a string
* anything that clears when multiple values exist

**Safeguard:** Do NOT use `implode()` when saving to DB. It must stay a JSON array.

---

# 6) Edit + Draft load safeguard (pre-populate Select2 correctly)

When loading an existing listing:

* Make sure `$this->number_of_unit_type` is set to an array (decode from DB if needed)

Then in Blade/JS after Select2 init:

* `$('#number_of_unit_type').val(existingArray).trigger('change.select2')`

**Safeguard:** If existing values don’t exactly match option strings, Select2 won’t show them. Your option strings must be consistent.

---

# 7) Listing display safeguard (must render multiple values)

On the actual listing view template for Buyer Income criteria:

### Required output behavior:

* If `number_of_unit_type` is an array and has values → display all selected values
* If 1 value → show as plain text
* If multiple → show comma-separated OR bullet list (either is fine, but must show all)

**Safeguard:** Do not display `[]` or blank just because it’s JSON. Always decode before render.

---

# 8) Critical: Don’t “accidentally fix Tenant but not Buyer”

You mentioned it “may be under tenant php where user has to select Buyer”.

That’s a common mistake: Buyer form might be using Tenant JS utilities or shared includes, but **the Buyer select id/property differs** so it never binds.

### Safeguard checklist:

* Confirm the JS selector `#number_of_unit_type` exists in the Buyer form DOM
* Confirm the Livewire property name in Buyer component is exactly `number_of_unit_type`
* Confirm the Buyer create/edit JS file is actually included on Buyer pages (view source, verify script is loaded)
* Confirm `wire:ignore` container isn’t preventing the hidden JSON input from updating if you rely on it

---

## QA Test (Must Pass)

Login `tenant@exp.com` → Hire a Buyer’s Agent → Income

1. Select “1 Bed/1 Bath”
2. Select “2 Bed/1 Bath”
3. Select “3 Bedroom”
   ✅ all 3 remain selected (chips)

Save Draft → reopen Draft:
✅ all 3 still selected

Publish → view actual listing:
✅ “Acceptable Unit Type:” shows all selected values

Edit listing → remove one → save:
✅ listing updates correctly

---

## If you want this locked down perfectly

Paste the **exact file names** you’re working in (or the paths) for:

1. Buyer create Blade (Income section)
2. Buyer edit Blade
3. Buyer Livewire component class
4. Any Buyer select2 JS file you edited (the one you said “doesn’t work”)

…and I’ll rewrite the above into **exact “change this block → paste this block” instructions** referencing those files so you can hand it to Replit AI with zero ambiguity.
