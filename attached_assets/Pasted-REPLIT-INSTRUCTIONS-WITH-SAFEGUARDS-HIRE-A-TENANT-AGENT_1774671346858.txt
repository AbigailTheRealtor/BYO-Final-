REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT AGENT COUNTER BID HISTORY MUST SHOW EXACT SELECTED SERVICES

SCOPE

Fix only the Hire a Tenant Agent counter-bid history display.

Problem:
The actual counter-bidding history is not showing the correct Tenant services that were selected in the counter form.

The counter form shows one set of checked services and additional services, but the counter history view is dropping some selected items and is not rendering the exact saved countered services.

This fix is only for:
Hire a Tenant Agent listings
counter-bid history / countered terms display
Offered Services rendering in the counter history view

Do not touch:
Seller
Buyer
Landlord
submit flow unless required to correctly persist the already-selected Tenant counter services
database schema
unrelated bid card styling
match score logic
notifications

GOAL

Make the Tenant counter-bid history show the exact same Offered Services that were selected in the Tenant counter form.

That means:

1. every checked service from the counter form must appear in counter history
2. every additional service entry must appear in counter history
3. no unchecked services should appear
4. no selected services should be missing
5. category grouping must remain correct
6. the counter history must reflect the actual saved counter values, not the original bid and not a filtered subset

HARD SAFEGUARDS — NON-NEGOTIABLE

1. Only work on Hire a Tenant Agent counter-bid history service rendering.
2. Do not modify Seller, Buyer, or Landlord files.
3. Do not rename routes, models, Livewire properties, DB columns, relationships, or saved keys.
4. Do not change database schema.
5. Do not hardcode service names.
6. Do not use a master/default service catalog as the rendered selected set.
7. Do not refactor unrelated counter-bid logic.
8. Make the smallest surgical fix possible.
9. Stop if the exact active Tenant files cannot be confirmed first.

SOURCE OF TRUTH

The source of truth must be:
the actual saved Tenant counter-bid Offered Services payload

Not:
the original agent bid services
a reduced comparison subset
a default catalog
a master service template
a partially mapped summary array

If a service was checked in the counter form and saved, it must appear in the counter history display.

REQUIRED FIRST STEP — TRACE THE EXACT ACTIVE FILES

Before editing anything, confirm the exact active files used for:

1. Tenant counter-bid form Offered Services tab
2. Tenant counter submit/save action
3. Tenant stored counter payload / hydration method
4. Tenant counter-bid history renderer
5. Tenant countered terms detail partial/component
6. Any helper/transformer used to format Tenant Offered Services in counter history

Do not guess.
Do not edit old unused Tenant files.
Do not assume the history view is using the same renderer as the main bid view without confirming.

CURRENT BUG

Based on the screenshots:
The Tenant counter form shows more checked services than what appears later in the actual counter-bidding history.

Examples of missing/mismatched behavior include:

1. selected items in Property Search, Alerts & Matching are missing
2. selected items in Property Showings & Virtual Tours are incomplete
3. selected items in Tenant Application Support are incomplete
4. selected items in Lease Preparation & Execution are incomplete
5. selected items in Move-In Support & Coordination are incomplete
6. selected items in Leasing Strategy & Guidance are incomplete
7. Additional Services entries may appear, but the full checked-service set is not being preserved in history

This means the counter history is not rendering the exact saved selected services set.

REQUIRED FIX

When a Tenant counter is saved, the counter-bid history must render the exact saved counter service selections.

Rules:

1. Show only selected services from the saved counter payload
2. Show all selected services from the saved counter payload
3. Show Additional Services exactly as entered
4. Preserve category grouping
5. Do not show unchecked items
6. Do not collapse to one-item-per-category unless only one item was actually selected
7. Do not render from a filtered “changed only” subset unless the actual Tenant history design specifically requires that and the screenshot proves otherwise
8. If the history is meant to show the full countered Offered Services block, it must render the full saved block

VERY IMPORTANT

Do not compare against the original bid and accidentally hide unchanged selected services if the intended history view is supposed to show the full countered services set.

The screenshot indicates the history should display the full resulting countered services block, not a reduced fragment.

LIKELY ROOT CAUSE AREAS TO CHECK

Check these exact issues:

1. The counter save action may not be persisting the full Tenant Offered Services array
2. The counter history view may be reading the wrong field:

   * original bid services instead of counter services
   * partial summary instead of full saved counter payload
3. The history transformer may be filtering only changed categories/items
4. The renderer may only be taking the first selected item per category
5. Additional Services may be coming from the correct payload while predefined selected services are coming from the wrong payload
6. A normalization/mapping step may be dropping checked booleans or nested selected items during save/load

IMPLEMENTATION REQUIREMENTS

1. Compare the Tenant counter form saved service payload against the Tenant counter history rendered payload line by line.
2. Confirm what is actually stored after counter submit.
3. Confirm what the counter history renderer is reading.
4. Make the history renderer use the actual saved counter services payload.
5. Preserve grouped rendering by category.
6. Preserve additional services.
7. Do not alter visual style beyond what is necessary to show the correct services.

TESTING REQUIREMENTS — MUST DO BEFORE REPORTING FIXED

Use a real Hire a Tenant Agent listing and create a counter with:

1. multiple checked services across multiple categories
2. multiple checked services within at least one category
3. two Additional Services entries

Then verify:

COUNTER FORM TEST

1. Checked services are visible before submit
2. Additional services are visible before submit

SAVE TEST

1. Submit counter successfully
2. Confirm saved counter payload contains all selected services

COUNTER HISTORY TEST

1. Counter history shows every checked service that was selected
2. Counter history shows every additional service entered
3. No selected service is missing
4. No unchecked service appears
5. Categories remain correct
6. Order is reasonable and consistent with current design
7. No undefined/null errors

REGRESSION CHECK

1. Tenant main bid view still works
2. Tenant counter form still works
3. Seller counter history remains unchanged
4. No regression to notifications or match score

DO NOT REPORT FIXED UNLESS YOU PROVIDE

1. Exact root cause
2. Exact files changed
3. Exact field/payload that was wrong
4. Exact renderer/helper changed
5. Confirmation that history now reads the saved counter services payload
6. Before vs after behavior summary
7. Proof that all selected Tenant counter services now appear in history

SUCCESS CRITERIA

This task is successful only if the Hire a Tenant Agent counter-bid history shows the exact services the user selected in the Tenant counter form, including Additional Services, with no missing checked items and no extra unchecked items.
