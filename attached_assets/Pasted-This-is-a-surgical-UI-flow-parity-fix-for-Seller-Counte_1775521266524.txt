This is a surgical UI/flow parity fix for **Seller Counter Agent Bid** only.

Tenant / Buyer / Landlord counter flow is the gold standard for navigation behavior.

Do NOT change database schema.
Do NOT change listing creation forms.
Do NOT change score logic.
Do NOT refactor unrelated bid logic.
Do NOT touch Tenant, Buyer, or Landlord except to use them as reference for correct counter navigation behavior.

---

## GOAL

Make **Seller Counter Agent Bid** behave like the other working counter flows.

Right now Seller counter terms pages are broken:

1. Broker Compensation & Agency Agreement Terms tab only shows **Next**
2. There is **no visible Previous button** on the first counter page
3. There is **no proper Submit button behavior**
4. On Additional Details and Offered Services tabs, the Previous button is present but appears **whited out / faded / invisible**
5. Seller Counter Agent Bid should have the same Previous / Next / Submit behavior as the correct counter flows

---

## EXPECTED FINAL BEHAVIOR

Seller Counter Agent Bid must behave like the working counter flows:

Page 1:

* Previous button visible and styled correctly
* Next button visible
* Submit button visible only if that is how the working reference flow behaves on every step
  OR
* Submit button visible on final step only if that is how the working reference flow behaves

Most important:
Use the exact working counter flow pattern from Tenant / Buyer / Landlord.
Do not invent a new Seller-only navigation pattern.

---

## WHAT TO AUDIT FIRST

Before coding, inspect:

1. the active Seller counter terms page/component/view
2. the working counter navigation in:

   * Tenant counter terms
   * Buyer counter terms
   * Landlord counter terms

Identify exactly:

* which component/view controls Previous / Next / Submit buttons
* which condition decides whether Previous shows
* which condition decides whether Submit shows
* which CSS/class is making Previous appear faded/white in Seller
* whether Seller is missing the same footer/button block used by the working flows

Do not code until the exact cause is identified.

---

## REQUIRED FIXES

1. SELLER BROKER COMPENSATION TAB

* Add the correct navigation/footer buttons so Seller Broker Compensation & Agency Agreement Terms tab has the same navigation as the working reference flow
* Previous button must be visible and styled correctly
* Next button must be visible and styled correctly
* Submit button behavior must match the working reference flow exactly

2. SELLER ADDITIONAL DETAILS TAB

* Fix Previous button so it is not faded/white/invisible
* Ensure button classes match the working reference flow exactly

3. SELLER OFFERED SERVICES TAB

* Fix Previous button so it is not faded/white/invisible
* Ensure Submit button appears correctly if this is the final step, matching the working reference flow exactly

4. BUTTON STYLE PARITY
   Seller Previous / Next / Submit must match the working reference flow in:

* button classes
* text color
* background color
* border
* opacity
* disabled state
* spacing/alignment

Do NOT leave Seller with a unique or partially broken button style.

---

## HARD SAFEGUARDS

* Do NOT rename or delete DB columns
* Do NOT change route structure unless a currently active Seller counter route is broken
* Do NOT change underlying counter data logic unless required to make the navigation/footer render correctly
* Do NOT change working Tenant/Buyer/Landlord behavior
* Do NOT hardcode a Seller-only one-off that differs from the working pattern
* Only modify the active Seller counter terms component/view and any minimal shared view partial if absolutely required

---

## FILES TO CHECK

Check:

* active Seller counter terms Livewire/component/controller
* active Seller counter terms Blade/view
* working Tenant counter terms component/view
* working Buyer counter terms component/view
* working Landlord counter terms component/view

Use the working flow as source of truth.

---

## VERIFICATION REQUIRED

After coding, provide:

1. exact files checked

2. exact files changed

3. exact root cause found for:

   * missing Previous on Broker Compensation tab
   * missing/incorrect Submit behavior
   * faded/whited-out Previous button on Additional Details / Offered Services

4. confirmation that Seller Counter Agent Bid now matches the working counter flow for:

   * Broker Compensation & Agency Agreement Terms
   * Additional Details
   * Offered Services

5. confirmation that:

   * Previous is visible
   * Next is visible where appropriate
   * Submit is visible where appropriate
   * buttons are no longer faded/white/invisible

Do not give a broad summary.
This is a Seller-only counter navigation parity fix.
