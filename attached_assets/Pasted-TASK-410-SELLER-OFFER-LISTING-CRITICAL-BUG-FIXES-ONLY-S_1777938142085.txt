TASK #410 — SELLER OFFER LISTING CRITICAL BUG FIXES ONLY

Scope:
- Seller Offer Listing create flow only
- Full-Service / Commission-Based path only
- Surgical fixes from Task #409 audit only

DO NOT touch:
- Landlord Offer Listing
- Buyer Offer Listing
- Tenant Offer Listing
- Hire Agent / Agent Preset flows
- Counter/Accept flows
- Database schema
- Unrelated styling/polish
- Tab order
- Field/model property names

Fix these six targeted issues:

1. Offered Financing Select2 Placeholder
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/seller-terms.blade.php

Action:
- Add this as the first option inside select#offered_financing:
  <option value=""></option>
- Do not alter the existing financing options.

2. Select2 Required Validation on Next
File:
resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php

Action:
- In the wizard Next handler, manually validate visible required Select2 fields.
- Check at minimum:
  #offered_financing
  #sale_provision
  #appliances

Validation rules:
- Use $(selector).val()
- Treat null, empty string, or empty array as invalid.
- Block moving to the next tab when invalid.
- Show/trigger the same validation error pattern used by normal required fields.
- Do not break existing native required-field validation.

3. Select2 Required Validation on Final Submit
File:
resources/views/livewire/offer-listing/seller/offer-seller-listing.blade.php

Action:
- On final submit / .wizard-step-finish click, validate all visible/relevant required fields across all tabs.
- Include Select2 validation for:
  #offered_financing
  #sale_provision
  #appliances
- If invalid:
  - Prevent submit
  - Move user to the first tab containing the invalid field
  - Show the validation error clearly
- Keep Livewire store() server-side validation unchanged.

4. Non-Negotiable Amenities “Other” Race Condition
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-preferences.blade.php

Action:
- Remove Blade d-none visibility logic from the Other input wrapper.
- Use only this inline style pattern:
  style="{{ is_array($non_negotiable_amenities) && in_array('Other', $non_negotiable_amenities) ? '' : 'display:none;' }}"
- Keep existing JS toggle behavior.
- Confirm Livewire message.processed still reapplies correct visibility.

5. Retainer Fee Error Placement Verification
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/broker-compensation.blade.php

Action:
- Verify retainer_fee_amount error span is not inside the input wrapper/input-group.
- If already fixed, leave unchanged.
- If still inside, move it immediately after the wrapper.

6. Carport Label Verification
File:
resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-preferences.blade.php

Action:
- Verify Carport label uses:
  class="fw-bold"
- If already fixed, leave unchanged.
- If broken as c lass, correct it.

After implementation, verify:
1. Only approved Seller files changed.
2. Seller full-service wizard still loads.
3. Next is blocked when required Select2 fields are empty.
4. Final Submit is blocked when required Select2 fields are empty.
5. Selecting “Other” for Non-Negotiable Amenities shows the custom field and remains visible after Livewire re-render.
6. Retainer fee error placement is correct.
7. Carport label is correct.

Report:
- Files changed
- Exact fixes made
- Items already fixed before this task
- Any deferred issues