Here are **copy-paste Replit instructions with strong safeguards** for these exact **Hire a Seller’s Agent** edits only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A SELLER’S AGENT ONLY — REMOVE REQUIRED STATUS / HIDE EMPTY LISTING SECTIONS / MINIMUM BATHROOMS OTHER EDIT FIX

## SCOPE

Make these fixes **only for Hire a Seller’s Agent**:

1. **Offered Financing/Currency**

   * remove the **red star**
   * make it **not a required field**

2. **Hide empty fields/sections on the listing**

   * if a field/section is not filled in, it should **not show on the listing**
   * specifically fix:

     * `Broker Compensation & Agency Agreement Terms`
     * the **Income** section/table:
       `Unit Type Configuration`
       with columns:
       `Unit Type / Beds / Baths / Garage / Carport / Other Spaces / # Units / # Occupied / Expected Rent / Description`

3. **Minimum Bathrooms Needed**

   * when user clicks **Edit**, it shows `Other`
   * but the custom input box does **not** appear
   * fix so the custom input box shows and repopulates correctly

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Seller’s Agent flow.**
2. **Do not modify Tenant, Buyer, or Landlord flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change Select2 initialization.**
10. **Do not change selection box format, width, styling, placeholder behavior, badge style, dropdown behavior, or general UI structure.**
11. **Do not change field labels, tooltips, icons, placeholders, or field order except removing the red star from Offered Financing/Currency.**
12. **Do not do a broad cleanup or refactor.**
13. **Do not touch any other Seller fields except the exact ones listed below if needed.**
14. **Keep this surgical and field-specific.**

---

## IMPORTANT

We have had issues before with edits being done in the wrong file or breaking select behavior.

### Hard stop:

Before changing anything, confirm the **actual active Seller files** for:

* Seller create/edit form
* Seller listing display
* Seller field for **Offered Financing/Currency**
* Seller listing display section for **Broker Compensation & Agency Agreement Terms**
* Seller **Income** listing display section for **Unit Type Configuration**
* Seller field for **Minimum Bathrooms Needed**

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap changes so they affect **Seller only**.

---

## STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Seller’s Agent**:

1. Livewire/component file handling Seller create/edit
2. Blade/partial rendering **Offered Financing/Currency**
3. Blade/partial rendering **Minimum Bathrooms Needed**
4. Blade/partial rendering the **actual Seller listing display**
5. Blade/partial rendering the **Broker Compensation & Agency Agreement Terms** section on the Seller listing
6. Blade/partial rendering the **Income Unit Type Configuration** section/table on the Seller listing
7. File/partial responsible for Seller edit-state repopulation of `Minimum Bathrooms Needed`

### Requirement

Return the exact files first, then edit **only those files**.

### Hard stop

**No other file should be edited or changed.**

---

## STEP 2 — REMOVE RED STAR FROM OFFERED FINANCING/CURRENCY AND MAKE IT NOT REQUIRED

## Required behavior

For **Seller Offered Financing/Currency only**:

* remove the visual **red star**
* remove it from required-field enforcement
* it should no longer block submit if left empty

### Important

Align all 3 layers for **Seller only**:

1. visual required marker
2. front-end required-field validation
3. server-side validation

### Do not:

* affect Buyer/Tenant/Landlord versions of this field
* change the selection input format
* change options
* change placeholder text

This is a **required-status removal only** for Seller.

---

## STEP 3 — HIDE EMPTY BROKER COMPENSATION SECTION ON SELLER LISTING

## Current bug

Seller listing page shows:

`Broker Compensation & Agency Agreement Terms`

even when there is no actual filled data in that section.

## Required behavior

If the section has no meaningful values, **do not show the section header**.
If rows inside the section are empty, **do not show those rows**.

### Important

This should match the same hide-empty behavior already used in the cleaner listing types like Tenant.

### Do not:

* hide filled values
* remove stored data
* affect Buyer, Tenant, or Landlord listing display

This is a **Seller listing display-only conditional rendering fix**.

---

## STEP 4 — HIDE EMPTY INCOME “UNIT TYPE CONFIGURATION” SECTION/TABLE

## Current bug

On Seller **Income** listing display, the **Unit Type Configuration** table shows even when there is no meaningful row data.

Columns include:

* Unit Type
* Beds
* Baths
* Garage
* Carport
* Other Spaces
* # Units
* # Occupied
* Expected Rent
* Description

## Required behavior

If there are no actual configured unit rows with meaningful values, **do not show the Unit Type Configuration section/table at all**.

### Important

* if real data exists, keep showing the table normally
* if rows are blank/empty, hide the entire section
* do not affect non-Income property categories
* do not change the table format when data exists

This is a **Seller Income listing display-only fix**.

---

## STEP 5 — FIX MINIMUM BATHROOMS NEEDED “OTHER” ON EDIT

## Current bug

When user clicks **Edit**:

* `Minimum Bathrooms Needed` shows `Other`
* but the custom input box does **not** appear

## Required behavior

For **Seller Minimum Bathrooms Needed only**:

* if the saved selection is `Other`, show the custom input box automatically on edit
* preload the saved custom value into the custom input
* preserve the current select/input format
* do not change the create/edit layout

## What to trace and fix

Confirm:

* main Bathrooms property
* Bathrooms custom `Other` property
* conditional logic that decides whether the custom input is visible
* edit hydration logic
* whether visibility depends only on JS change events instead of saved state

### Important

Fix only this exact Seller Bathrooms path.
Do **not** rewrite all “Other” logic globally.

---

## STEP 6 — PRESERVE BACKWARD COMPATIBILITY

If older Seller records already saved:

* Offered Financing/Currency values
* empty Broker Compensation sections
* empty Income Unit Type Configuration structures
* Minimum Bathrooms Needed = `Other` with custom value

They must still:

* load correctly
* display correctly
* not require migrations
* not break existing saved data

---

## STEP 7 — TEST THESE EXACT CASES

### A. Offered Financing/Currency

* expected: no red star
* expected: not required on submit

### B. Empty Broker Compensation section

* leave section empty
* expected: `Broker Compensation & Agency Agreement Terms` does **not** show on listing

### C. Empty Income Unit Type Configuration

* no meaningful income unit rows
* expected: section/table does **not** show on listing

### D. Minimum Bathrooms Needed → Other

* save listing with `Other` and custom value
* click Edit
* expected: custom input box shows automatically
* expected: custom value is populated

---

## STEP 8 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Tenant files changed
* no Buyer files changed
* no Landlord files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Seller fields changed

---

## STEP 9 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Seller files were identified first
3. exact root cause for:

   * Offered Financing/Currency being required
   * empty Broker Compensation section showing
   * empty Income Unit Type Configuration showing
   * Minimum Bathrooms Needed Other not showing on edit
4. exact fix applied for each
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof the Seller listing now hides empty sections/rows correctly

---

## IMPORTANT FINAL RULE

This is a **Seller-only, surgical fix**.

Do **not**:

* edit unrelated files
* change selection box format
* refactor global select logic
* touch Tenant, Buyer, or Landlord
* make broad UI changes

The final result should be:

* Offered Financing/Currency is not required
* empty Broker Compensation section does not show
* empty Income Unit Type Configuration does not show
* Minimum Bathrooms Needed Other shows correctly on edit
* no other file or selection input behavior is changed

---

If you want, I can also make this into a **shorter hard-stop version** for Replit.
