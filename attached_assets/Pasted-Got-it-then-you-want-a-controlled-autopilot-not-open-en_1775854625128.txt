Got it — then you want a **controlled autopilot**, not open-ended execution.

You need to:

* let Replit continue
* but **lock it into safe phases**
* and **prevent a dangerous refactor**

---

## ✅ Paste THIS into Replit (final instruction)

This will safely run everything without you babysitting:

---

Execute the remaining phases sequentially **without stopping**, but follow all safeguards strictly.

## Execution Order (REQUIRED)

Continue in this exact order:

### Phase 2D

* Part 3 — Seller digital photo enhancements “Other” field rendering
* Part 5 — Changed term highlighting (only ensure changed fields are visibly marked, do not rewrite scoring)
* Part 4A — Landlord renewal fee formatting (ONLY after verifying actual DB stored value)

### Phase 2E

* Part 1 — Agent visibility across Traditional listings (allow agents to see other agents’ bids using the same rules as Bidding Period listings)

---

## Global Rules (CRITICAL)

1. **NO state engine refactor**

   * Do NOT introduce new architecture
   * Do NOT invent new fields
   * Use only existing variables and data structures

2. **NO database changes**

   * No migrations
   * No schema edits

3. **SURGICAL ONLY**

   * Fix only the specific issues listed
   * Do not rewrite entire files or flows

4. **DO NOT BREAK EXISTING FIXES**

   * Seller, Tenant, Buyer, Landlord counter direction logic must remain intact
   * View Counter Terms gating must remain unchanged

5. **VISIBILITY RULE FOR PART 1**

   * Agents can see other agents’ bids on Traditional listings
   * BUT must follow same visibility rules as Bidding Period
   * Do NOT expose private-only data

6. **PART 4A SAFEGUARD**

   * Before changing renewal fee logic:

     * inspect actual stored DB value
     * confirm whether mismatch is due to string formatting (apostrophe mismatch, etc.)
   * Only then apply fix

7. **PART 5 SAFEGUARD**

   * Do NOT rebuild match scoring
   * Only ensure that when UI says “1 changed”, the changed field is visibly marked

8. **PART 3 SAFEGUARD**

   * Use Landlord implementation as reference
   * Ensure Seller renders “Other” custom text consistently

---

## After ALL phases complete

Provide a final report including:

1. All files changed (grouped by Part #)
2. Summary of each fix
3. Any issues that could NOT be safely fixed
4. Confirmation that:

   * counter direction logic still works
   * no schema changes were made
   * no state engine refactor was introduced

---

## STOP RULE

If ANY part requires:

* major refactor
* duplicate conflicting logic removal
* or risk of breaking existing flows

STOP and report instead of proceeding.

---

## 🚨 Why this works

This gives you:

* forward progress while you’re away
* guardrails against breaking your system
* no over-engineering
* no risky refactors

---

## 🔥 You’re at a critical point

After this run, your system will have:

* consistent counter logic
* consistent UI across all roles
* clean rendering of services and terms
* working competitive bid visibility

---

If you want later, when you’re back, I can:

* do a **full UI polish pass**
* or help you turn this into a **reusable system architecture (safely, not rushed)**
