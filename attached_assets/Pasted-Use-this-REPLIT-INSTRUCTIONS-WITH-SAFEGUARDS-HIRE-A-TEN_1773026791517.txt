Use this:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT’S AGENT ONLY — SELECTED OPTIONS NOT DISPLAYING ON LISTING CREATION

### SCOPE

Fix **only** this issue in **Hire a Tenant’s Agent** listing creation:

* Under **Offered Lease Term** and **Acceptable Property Styles**, the user selects options, but the selected values do **not** show up on the listing creation page.

This is now a **listing creation display / state rendering issue**.
The selections may be choosing in the UI, but they are not being rendered back correctly in the form/listing creation view.

Keep this fix **extremely targeted**.

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Tenant’s Agent flow**
2. **Do not touch Buyer, Seller, or Landlord flows**
3. **Do not rename, delete, or migrate database columns**
4. **Do not change labels, placeholders, tooltips, or option text**
5. **Do not change required-field rules**
6. **Do not do a broad Select2 rewrite**
7. **Do not refactor shared logic unless there is no safe Tenant-only fix**
8. **Do not change unrelated listing sections**
9. **If a safe Tenant-only fix is not possible, stop**

---

## REQUIRED FIX

### Problem to fix

When the user selects values under:

* **Offered Lease Term**
* **Acceptable Property Styles**

the values appear selected in the input, but they do **not** show up properly in the **listing creation display / summary / rendered form state**.

### What must be checked

For **both fields**, trace the full path:

1. **UI field / Select2 selection**
2. **Livewire property update**
3. **stored in current in-memory form state**
4. **rendered back in the listing creation page**
5. **draft save**
6. **edit restore**
7. **final listing display**

### Required investigation

For both fields, verify:

* the exact Livewire property name
* the exact Blade rendering location where selected values are supposed to display
* whether the display code is reading the same property that the input updates
* whether the field is stored as:

  * PHP array
  * JSON string
  * comma-separated string
* whether the display logic expects a different format than what is actually being saved/held
* whether the selected values exist in state but are failing to render because of:

  * wrong variable name
  * wrong conditional
  * empty-check issue
  * array/string mismatch
  * label/value mapping mismatch

---

## SPECIFIC REQUIREMENT FOR OFFERED LEASE TERM

Fix **Offered Lease Term** so that once selected:

* it remains visible as selected in the input
* it appears correctly in the listing creation page display
* it survives rerenders
* it saves in draft
* it restores in edit
* it shows correctly on final listing

### Important

If the field stores raw option values but the display expects formatted labels, map them correctly.
If the field stores an array but the display expects a string, fix the display layer only as needed.
Do **not** break save behavior.

---

## SPECIFIC REQUIREMENT FOR ACCEPTABLE PROPERTY STYLES

Fix **Acceptable Property Styles** so that once selected:

* it remains visible as selected in the input
* it appears correctly in the listing creation page display
* it does not disappear on rerender
* it saves in draft
* it restores in edit
* it shows correctly on final listing

### Important

If the field options are dynamic based on property type, make sure the display rendering still reads the selected values correctly after selection and after rerender.

---

## IMPORTANT RULE

Do **not** just fix the input widget.

The issue now is that the selected values are **not showing up on listing creation**, so I need you to verify that the **render/display layer** is using the same state as the input layer.

If the selection is saved in the Livewire property but not rendering, fix the **display/render path**, not just the Select2 setup.

---

## TESTING REQUIREMENTS

Test **Tenant only**.

### Create flow

1. Open Hire a Tenant’s Agent
2. Select one or more **Offered Lease Term** options
3. Select one or more **Acceptable Property Styles** options
4. Confirm the selected values immediately show correctly on the listing creation page
5. Move between tabs and return
6. Confirm values are still shown correctly

### Draft

1. Save draft with both fields selected
2. Reopen draft
3. Confirm both fields still display correctly in the form/listing creation page

### Edit

1. Open existing Tenant listing
2. Confirm both fields display correctly
3. Change selections
4. Save
5. Reopen and confirm persistence

### Final listing display

1. Submit listing
2. Confirm both fields display correctly on the actual listing

---

## DO NOT CHANGE

* do not touch other roles
* do not rewrite all tab logic
* do not change database structure
* do not change field wording
* do not change unrelated form sections
* do not mark resolved unless actual UI behavior is verified

---

## SUCCESS RESULT

This task is complete only when:

1. **Offered Lease Term** selected values show correctly on listing creation
2. **Acceptable Property Styles** selected values show correctly on listing creation
3. both fields persist through rerenders, draft, edit, and final listing display
4. no Buyer, Seller, or Landlord flow is affected

---
