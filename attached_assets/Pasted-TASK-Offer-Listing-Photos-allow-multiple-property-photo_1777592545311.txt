TASK: Offer Listing Photos — allow multiple property photo uploads

GOAL:
Allow agents to upload multiple property photos for Offer Listings, with support for at least 50 photos.

SCOPE:
- Agent Dashboard
- BidYourOffer / Offer Listings
- Create Seller Listing
- Create Landlord Listing
- Full-service / commission-based Offer Listing flows ONLY

DO NOT:
- Do not edit Hire Agent listings
- Do not edit BidYourAgent flows
- Do not edit Buyer/Tenant unless shared code is safely confirmed to be Offer Listing only
- Do not edit Limited Service / legacy files
- Do not change unrelated upload fields
- Do not rebuild the entire media system
- Do not remove existing video tour, 3D tour, or document upload functionality

REQUIRED CHANGE:
In the Photos, Tours & Documents tab:

Property Photos upload must allow multiple files.

Requirements:
- Add multiple attribute to the property photos input.
- Support selecting at least 50 image files.
- Validate as an array/list of images.
- Each file should validate as image type.
- Allowed file types:
  - jpg
  - jpeg
  - png
  - webp
- Use the current platform max file size pattern.
- Save each uploaded photo correctly.
- Reload/display uploaded photos if the current form already supports preview or existing photo display.
- Do not affect Documents upload unless it already supports multiple documents.
- Do not affect video tour URL or 3D tour URL.

BACKEND:
Update Livewire property/validation only as needed.
If current property is:
newPropertyPhotos
and currently expects one file, update it safely to support multiple files.

Important:
- If existing saved photo storage only supports one file and schema changes are required, STOP and report required schema changes before implementing.
- If current storage already supports multiple paths/JSON/EAV meta, use that existing pattern.

VALIDATION EXAMPLE:
- newPropertyPhotos: nullable array
- newPropertyPhotos.*: image, mimes:jpg,jpeg,png,webp, max:[existing max]

UI:
- Label remains: Property Photos
- Add helper text:
“You may upload multiple property photos. Recommended maximum: 50 photos.”
- Keep existing styling.

VERIFICATION:
Test Seller Offer Listing:
1. Select multiple photos.
2. Save Draft.
3. Reload.
4. Photos remain saved if existing preview/reload supports it.

Test Landlord Offer Listing:
1. Select multiple photos.
2. Save Draft.
3. Reload.
4. Photos remain saved if existing preview/reload supports it.

Confirm:
- Video Tour URL still works.
- 3D Tour URL still works.
- Documents upload still works.
- No Hire Agent changes.
- No Limited Service changes.
- No schema changes unless separately approved.

FINAL REPORT:
Provide:
- Files changed
- Validation changed
- Whether storage already supported multiple photos
- Confirmation Seller + Landlord Offer Listings only
- Confirmation Hire Agent / Limited Service untouched