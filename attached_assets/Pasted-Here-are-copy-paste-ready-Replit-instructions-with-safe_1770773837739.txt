Here are **copy/paste-ready Replit instructions with safeguards** for **Hire a Landlord’s Agent → Draft + Edit** to fix the two persistence issues you listed.

---

## Replit Instructions (WITH SAFEGUARDS) — Hire a Landlord’s Agent (Draft + Edit Persistence)

### Applies To

* Login example: `tenant@exp.com`
* Dashboard → **Hire a Landlord’s Agent**
* Property Types: **Residential + Commercial**
* Scope: **Draft save + reopen draft + edit listing + reload page** must persist and repopulate the fields below.

---

# NON-NEGOTIABLE SAFEGUARDS

### SAFEGUARD A — No Partial Saves / No Overwrites

1. **Draft save must persist the full listing payload** (all tabs), not only the active tab.
2. Saving a later tab must **not overwrite earlier tab values** with null/empty values.
3. Use **merge-save** logic:

   * merge the current Livewire state into the existing draft/listing record field-by-field
   * never replace the entire record with only the current tab subset

**Acceptance test:** Fill Tab 1 → Save Draft → Fill Tab 2 → Save Draft → reopen draft → Tab 1 values still present.

---

### SAFEGUARD B — Single Source of Truth for Field Keys

Ensure the same exact field keys are used across:

* Create form (`wire:model`)
* Draft save payload
* Draft reopen hydration
* Edit form hydration
* Edit save payload
* Actual listing view (if displayed)

No “nearly identical” key names (e.g., `county` vs `property_county`) between create/edit/draft.

---

### SAFEGUARD C — Residential + Commercial Must Both Work

These fixes must be validated in **both** flows:

* Residential landlord listing
* Commercial landlord listing

No “Residential-only” patching.

---

# REQUIRED FIXES (Draft + Edit)

## FIX 1 — Location fields not saving (Draft + Edit)

### Issue

These fields do not save / repopulate:

* **City**
* **State**
* **County**
* **Zip Code**

### Requirements

1. Include City/State/County/Zip in:

   * **draft create/save**
   * **draft reopen hydration**
   * **edit save**
   * **edit mount hydration**
2. Verify they are not being dropped due to:

   * model `$fillable` restrictions
   * missing DB columns / JSON keys
   * validation rules that reset them
   * payload builder only including some tabs

### Acceptance Tests

* Enter City/State/County/Zip → Save Draft → reopen draft → values still filled
* Edit City/State/County/Zip → Save changes → reload → values remain

---

## FIX 2 — Landlord’s Broker Lease Fee not saving (Draft + Edit)

### Issue

* **Landlord’s Broker Lease Fee** does not persist on Draft and Edit.

### Requirements

1. Ensure the field is persisted regardless of listing type:

   * Residential landlord listing (if applicable)
   * Commercial landlord listing (definitely applicable)
2. Include the field in:

   * draft save payload
   * draft reopen hydration
   * edit mount hydration
   * edit save payload
3. Confirm the input is not conditionally hidden/cleared on save.

   * If it’s shown only when certain options are selected, the stored value must still remain when set.
4. If formatting/masking is used (percent/currency), ensure:

   * it saves consistently (raw numeric vs formatted string—match your existing pattern)
   * it repopulates exactly in the input on edit/reopen

### Acceptance Tests

* Enter Landlord’s Broker Lease Fee → Save Draft → reopen → value remains
* Edit Landlord’s Broker Lease Fee → Save → reload → value remains

---

# DEV CHECKLIST (Must Complete Before Marking Done)

### Draft

* Residential: City/State/County/Zip + Landlord Broker Lease Fee → Save Draft → reopen → persists
* Commercial: City/State/County/Zip + Landlord Broker Lease Fee → Save Draft → reopen → persists

### Edit

* Residential: change values → Save → reload → persists
* Commercial: change values → Save → reload → persists

### Regression Guard

* Saving any later tab must **not clear** these fields.

---

# “NO TOUCH” RULE

Do **not** change tooltips, labels, field order, or business logic.
Only fix persistence + hydration for the listed fields.

---

If you want to make this bulletproof for your dev, tell them to add a one-time debug log (server-side) that prints the outgoing draft payload keys and values for these fields during save—then remove it after verification. This catches the exact spot where the values are being dropped.
