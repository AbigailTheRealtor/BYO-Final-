Paste this to Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A BUYER’S AGENT ONLY — ACTIVE FLOW BUG FIX PASS

### SCOPE

Fix **only** the following bugs in the **Hire a Buyer’s Agent** flow.

These issues look similar to bugs we already fixed in the past, so before editing anything, first verify the **actual active Buyer flow files** and confirm the fixes are being applied to the real current path, not an older or parallel file.

It is possible these reverted because:

* the wrong file was edited before
* the create flow was fixed but not the edit/shared flow
* a shared JS/init file was overwritten later
* a later change reintroduced stale Select2 sync behavior
* one field still uses different init logic than **Acceptable Property Conditions**, which is currently the working baseline

Do **not** guess. Confirm the active files first.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Buyer’s Agent flow.**
2. **Do not edit Tenant, Seller, or Landlord flows.**
3. **Do not rename, delete, or migrate any database columns.**
4. **Do not change unrelated labels, tooltips, placeholders, or business rules.**
5. **Do not refactor the whole component architecture.**
6. **Do not replace Livewire, Select2, or shared frontend systems.**
7. **Do not make broad cleanup edits.**
8. **If shared files must be touched, isolate the fix so it only affects Buyer flow unless absolutely necessary.**
9. **Do not break drafts, edits, listing display, or old saved data.**
10. **Use Acceptable Property Conditions as the working baseline. Do not break that field.**

---

# STEP 1 — VERIFY THE ACTUAL ACTIVE BUYER FILES FIRST

Before editing anything, identify the exact active files used by:

* Hire a Buyer’s Agent **create**
* Hire a Buyer’s Agent **draft reopen**
* Hire a Buyer’s Agent **edit**

Also identify:

1. the exact Livewire component(s)
2. the exact blade(s)
3. the exact partials for these fields:

   * Acceptable Property Styles
   * Acceptable Property Conditions
   * View Preference Needed
   * Garage/Parking Features
   * Non-Negotiable Amenities and Property Features
   * Acceptable Unit Type
   * Required Property or Business Assets
   * Offered Financing/Currency
   * Services
4. the exact JS/init code responsible for Select2 init, selected-state sync, and save-before-tab-change behavior

Do not assume the create flow and edit flow use the same files.

---

# ROOT-CAUSE DIRECTION

Most of these issues appear related and likely share the same underlying problem:

* Select2 selected-state not syncing immediately
* save-before-tab-change not capturing the current value
* selected DOM options not being marked properly
* fields showing selections only after reopening the dropdown
* stale placeholder/default option like `Select` leaking into saved/displayed output
* dependent conditional sections for Offered Financing/Currency not updating immediately
* some fields not using the same immediate sync behavior as **Acceptable Property Conditions**

Please fix the **underlying synchronization/persistence issue** for the affected Buyer fields rather than patching each symptom separately.

---

# BUGS THAT MUST BE FIXED

## 1) Acceptable Property Styles — “No results found” when selecting

Current issue:

* When selecting **Acceptable Property Styles**, it says **No results found**

Required behavior:

* The field must show and accept the actual available options
* It must not incorrectly show “No results found” for valid selections
* It must save correctly
* It must display correctly on listing output
* It must persist across tab changes, save, refresh, and reopen

Investigate whether:

* the option list is not being loaded correctly
* the Select2 init/search config is wrong
* the selected values are not matching the option values
* the wrong option source is being used

---

## 2) Acceptable Property Conditions label update

Required text change:

Change:

* **Older but Clean**

To:

* **Older but Clean & Well Maintained**

Requirements:

* Update the active Buyer source for this option
* Preserve backward compatibility for old saved values if needed
* Listing display/output must show the correct updated text going forward

---

## 3) View Preference Needed — selects then deletes options

Current issue:

* It lets me select options
* then it deletes them / they disappear
* It should behave like **Acceptable Property Conditions**, which works correctly

Required behavior:

* selected options must remain immediately
* they must show in the closed Select2 field
* they must persist after tab change
* they must persist after save, refresh, and reopen
* they must display correctly on listing output

---

## 4) Garage/Parking Features — selects then deletes options

Same issue and same required behavior as above.

It must behave like **Acceptable Property Conditions**:

* immediate selected-state
* no disappearing selections
* persistence across tab change/save/refresh/reopen
* correct listing display

---

## 5) Non-Negotiable Amenities and Property Features — “Select” shows as an option/listing value

Current issue:

* The word **Select** is showing up automatically as the first option / listing value
* I did not choose it
* It should not be a selectable saved value

Required behavior:

* `Select` must not be a selectable option
* `Select` must not be saved
* `Select` must not appear on listing output
* If this is a placeholder/default, it must remain only a placeholder and never behave like real data

Also inspect whether old saved records may contain `Select` and handle safely if needed.

---

## 6) Acceptable Unit Type — selects then deletes options

Same issue and same required behavior as **Acceptable Property Conditions**.

Selections must:

* stick immediately
* not disappear
* persist across tab changes/save/refresh/reopen
* display correctly on listing

---

## 7) Required Property or Business Assets — selects then deletes options

Same issue and same required behavior as **Acceptable Property Conditions**.

Selections must:

* stick immediately
* not disappear
* persist correctly
* display correctly on listing

---

## 8) Offered Financing/Currency — selected financing types should appear immediately above Buyer Pre-Approved for Loan

Current issue:

* If user selects **Conventional, FHA, Jumbo, No-Doc, Non-QM, USDA, or VA**, those words should show above **Buyer Pre-Approved for Loan**, similar to how **Assumable** already shows above the Assumable question
* Right now behavior is inconsistent / delayed
* Only **VA** sometimes shows
* Other financing types may appear later or only after interacting with Cryptocurrency terms

Required behavior:

* If user selects any of these:

  * Conventional
  * FHA
  * Jumbo
  * No-Doc
  * Non-QM
  * USDA
  * VA
* then all selected terms must appear immediately above **Buyer Pre-Approved for Loan**
* if multiple are selected, show all selected terms
* no delay
* no dependency on touching another financing section like Cryptocurrency
* if a term is removed, it should disappear immediately from the displayed heading/summary

### Important:

None of the questions triggered by **Offered Financing/Currency** selections are required.

That means:

* selecting these options should reveal their relevant follow-up questions/sections
* but those follow-up questions must **not** be treated as required
* they must not block tab progression or submit unless explicitly intended elsewhere

---

## 9) Services — selects then deletes

Current issue:

* selecting Services works briefly
* then selections delete/disappear

Required behavior:

* Services must behave like **Acceptable Property Conditions**
* selected services must stick immediately
* show correctly in closed field
* persist across tab changes/save/refresh/reopen
* display correctly on listing

---

# REQUIRED IMPLEMENTATION APPROACH

## 1) USE ACCEPTABLE PROPERTY CONDITIONS AS THE BASELINE

Because **Acceptable Property Conditions** is currently the working field, compare the broken fields against it and identify differences in:

* Select2 init
* change handlers
* Livewire sync
* selected option DOM updates
* edit hydration
* save-before-tab-change logic
* listing display mapping

Then make the broken Buyer fields behave the same way.

Do not break Acceptable Property Conditions.

---

## 2) FIX SAVE/PERSISTENCE BEFORE TAB CHANGE

For all affected Buyer fields that are “selects then deletes,” make sure:

* current selections are synchronized before tab change
* Livewire state is updated immediately
* re-render does not replace the field with stale values
* selected options remain visible without reopening the dropdown

---

## 3) FIX PLACEHOLDER / DEFAULT OPTION LEAKAGE

For **Non-Negotiable Amenities and Property Features**, ensure any placeholder like `Select` is not:

* treated as a real option
* saved as a selected value
* shown on listing output

---

## 4) FIX OFFERED FINANCING/CURRENCY IMMEDIATE CONDITIONAL UPDATES

For Offered Financing/Currency:

* the selected financing types must update the displayed labels/sections immediately
* no delayed update after unrelated field interaction
* follow-up sections/questions must not be required unless explicitly designed elsewhere
* removal of a selected financing type must update the display immediately

---

## 5) PRESERVE BACKWARD COMPATIBILITY

If old Buyer listings already contain prior values such as:

* `Older but Clean`
* `Select`
* older stored variants

do not break old listings or make edit pages crash.

Use a backward-compatible approach where needed, while ensuring new saves use the corrected behavior and text.

---

# QA TESTING REQUIRED

After fixing, test all of these in the **actual active Buyer flow**.

## Acceptable Property Styles

1. Open Buyer create
2. Open Buyer edit if applicable
3. Confirm valid options appear
4. Confirm no false “No results found”
5. Select options
6. Tab away and back
7. Confirm they remain
8. Refresh/reopen
9. Confirm they remain
10. Confirm listing output is correct

## Acceptable Property Conditions

1. Confirm the option now says:

   * **Older but Clean & Well Maintained**
2. Confirm old saved values still load safely if present

## View Preference Needed

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Garage/Parking Features

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Non-Negotiable Amenities and Property Features

1. Confirm `Select` does not appear as a real selected option
2. Confirm `Select` does not show on listing output
3. Confirm real selections save/display correctly

## Acceptable Unit Type

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Required Property or Business Assets

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Offered Financing/Currency

1. Select Conventional, FHA, Jumbo, No-Doc, Non-QM, USDA, VA in different combinations
2. Confirm all selected terms show immediately above Buyer Pre-Approved for Loan
3. Remove one or more
4. Confirm display updates immediately
5. Confirm none of the triggered follow-up questions are required
6. Confirm submit/tab progression is not blocked by those questions

## Services

1. Select services
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

---

# WHAT TO REPORT BACK

Report back with:

1. the exact active Buyer create/draft/edit files
2. the exact broken fields that were using different logic than Acceptable Property Conditions
3. the exact root cause of the “selects then deletes” behavior
4. the exact root cause of the false “No results found” in Acceptable Property Styles
5. the exact root cause of `Select` appearing as a saved/listing value
6. the exact root cause of the delayed Offered Financing/Currency display
7. the exact files changed
8. confirmation that Tenant, Seller, and Landlord flows were not changed

---

# FINAL REMINDER

This is a **surgical Hire a Buyer’s Agent bug-fix pass only**.

Fix only:

* Acceptable Property Styles “No results found”
* Acceptable Property Conditions text update
* View Preference Needed selections deleting
* Garage/Parking Features selections deleting
* Non-Negotiable Amenities and Property Features `Select` leakage
* Acceptable Unit Type selections deleting
* Required Property or Business Assets selections deleting
* Offered Financing/Currency delayed/improper display and non-required follow-ups
* Services selections deleting

Do not do unrelated cleanup or broad refactors.

---

Why this may have reverted after being fixed:

* the prior fix may have been applied to the wrong create/edit path
* a shared JS init file may have later been overwritten
* one field may still be using old sync logic while Acceptable Property Conditions uses the corrected logic
* a later refactor may have restored placeholder/default option behavior

So please verify the real active files first and fix the actual current path.
