Use this for Replit:

---

## Final Tenant parity polish: add bullets to each Offered Service item in View Bid

The Tenant **View Bid** page is now much closer, but the remaining mismatch is:

* the **bid card/canonical source** shows each service as a bulleted item
* the **View Bid** page is still rendering service lines without the same bullet formatting

This is a **format-parity task only** for the Tenant **Offered Services** section.

Do **not** change:

* service content
* service order
* service grouping
* category titles
* any other Tenant sections
* Buyer/Seller/Landlord files

---

## Goal

Make the Tenant **View Bid** page render each service item in **Offered Services** with the same bullet style/structure as the canonical Tenant bid card source.

The result must visually match the bid card:

* same section headings
* same service text
* same order
* same grouping
* same bullets for each service line

---

## What to audit first

Find the exact renderer for:

1. Tenant canonical bid card / full-bid Offered Services
2. Tenant View Bid Offered Services

Confirm whether:

* the canonical source uses `<ul><li>`
* the View Bid version uses plain lines, `<div>`, `<p>`, or stripped list markup
* a shared partial exists but is being rendered differently by CSS
* list-style is being removed by a parent CSS rule

This may be either:

* markup mismatch
* CSS mismatch
* both

Do not assume it is only CSS until you inspect both render paths.

---

## Required fix

Update Tenant View Bid **Offered Services** so each service item renders as a true bulleted list, matching the canonical bid card source.

### Requirements

* preserve all current service text exactly
* preserve current category headings exactly
* preserve current item order exactly
* preserve custom/additional services
* preserve spacing between categories
* bullets must be visible on desktop and mobile
* do not introduce duplicate bullets
* do not change icons used for category headings if those already match

### Preferred implementation

Best approach:

* make Tenant View Bid use the same shared partial/markup structure as the canonical bid card service list

If already using the same markup, then fix the CSS so bullets appear the same.

---

## Safeguards

* Do not hardcode bullets into each service string
* Do not manually prepend `•` to stored service text
* Do not alter service catalog data
* Do not edit Buyer/Seller/Landlord in this task
* Do not change compensation, links, credentials, or button logic
* Do not break mobile spacing or indentation

---

## QA checklist

Using the same Tenant bid:

* [ ] Every service item in View Bid shows a visible bullet
* [ ] Same service count as canonical source
* [ ] Same service text as canonical source
* [ ] Same category titles as canonical source
* [ ] Same ordering as canonical source
* [ ] Additional/custom services still render
* [ ] No duplicate bullets
* [ ] Desktop matches canonical source
* [ ] Mobile matches canonical source

---

## Deliverable back to me

Report:

1. files changed
2. whether the issue was markup, CSS, or both
3. whether Tenant View Bid now uses the same service-list partial/renderer as the canonical bid card

---

If they still miss the exact bullet look after this, send me the next screenshot and I’ll make the prompt even tighter.
