REPLIT INSTRUCTIONS WITH SAFEGUARDS — SELLER COUNTER BID FINAL UI PARITY FIX

SCOPE

This is a final Seller Counter Bid parity polish only.

The Seller Counter Bid form now appears to match the Agent bid structure and values correctly.
The remaining issue is UI parity:
the blue field icons / emojis shown in the Agent bid version are missing from the Seller Counter Bid version.

Fix only that final parity issue for Hire a Seller counter bids.

Do not touch:
Buyer
Landlord
Tenant
submit logic
save logic
database schema
field mappings
preload logic
validation rules
route logic

GOAL

Make the Seller Counter Bid Broker Compensation & Agency Agreement section visually match the Agent bid version exactly by restoring the same blue icons / field adornments inside the relevant inputs/selects.

HARD SAFEGUARDS — NON-NEGOTIABLE

1. Only work on Hire a Seller counter bid UI parity.
2. Do not change any saved values or preload behavior.
3. Do not rename routes, models, Livewire properties, DB columns, or schema.
4. Do not refactor unrelated shared components.
5. Do not touch Buyer, Landlord, or Tenant files unless reading them for reference only.
6. Do not alter field order, labels, conditional logic, or required logic.
7. Do not restyle the whole form.
8. Make the smallest possible fix.
9. Stop if the exact active Seller counter-bid Blade/component cannot be confirmed first.

REQUIRED FIRST STEP — CONFIRM ACTIVE FILES

Before editing, confirm the exact active files used for:

1. Seller Counter Bid Blade / partial for Broker Compensation & Agency Agreement
2. Seller Agent bid Blade / partial that already shows the blue icons correctly
3. Any shared input/select partial or component responsible for prepended icons
4. Any CSS class or wrapper used to display those icons

Do not guess.
Do not edit old unused files.

ISSUE TO FIX

The Seller Counter Bid form is functionally correct, but it is missing the blue icons / input adornments that appear in the Agent bid version.

Examples from the Agent bid version include icon styling inside:

* dropdown/select fields
* date/timeframe fields
* money/percent grouped inputs
* brokerage relationship field
* yes/no selectors
* other special formatted fields where icons already appear in the Agent bid version

REQUIRED FIX

Make the Seller Counter Bid version use the same icon/wrapper markup and same classes as the Agent bid version for the matching Seller fields.

This means:

1. Compare the Seller Agent bid markup line by line against the Seller Counter Bid markup
2. Find where the icon wrapper / prepended icon span / special input group markup is missing
3. Apply that same markup/classes to the Seller Counter Bid fields only
4. Keep all current values, bindings, and wire:model usage unchanged

IMPORTANT

This is not a data fix.
This is not a field mapping fix.
This is not a route fix.

It is only:
restore the same blue field icons / adornments so Seller Counter Bid visually matches the Agent bid version.

DO NOT DO THESE SHORTCUTS

1. Do not hardcode random icons that do not match the Agent bid version
2. Do not replace working input components with new ones unless necessary
3. Do not change field spacing or widths unless required by the icon wrapper
4. Do not break wire:model bindings
5. Do not touch Offered Services or Additional Details unless the same icon issue exists there and the exact same active Seller counter-bid files use those fields
6. Do not change conditional show/hide logic

TESTING REQUIREMENTS — MUST DO BEFORE REPORTING FIXED

Use a real Hire a Seller counter bid and verify:

1. Seller Counter Bid still preloads all values correctly
2. Blue icons now appear in the same fields where they appear in the Agent bid version
3. Dropdowns/selects still work
4. Money and percent grouped inputs still work
5. No Alpine/JS/Livewire errors
6. No layout breakage on conditional fields
7. No regression to save/counter flow

VISUAL PARITY CHECK

Compare Seller Counter Bid against Seller Agent bid and confirm:

1. Same field order
2. Same icon/adornment presence
3. Same overall field appearance
4. Same section look for Broker Compensation & Agency Agreement

DO NOT REPORT FIXED UNLESS YOU PROVIDE

1. Exact file(s) changed
2. Exact markup/component difference found
3. Exact fields where icon wrappers were restored
4. Confirmation that preload/bindings were untouched
5. Before vs after screenshot proof

SUCCESS CRITERIA

This task is successful only if the Seller Counter Bid form visually matches the Seller Agent bid form and the only remaining difference — the blue field icons / adornments — has been restored without breaking functionality.
