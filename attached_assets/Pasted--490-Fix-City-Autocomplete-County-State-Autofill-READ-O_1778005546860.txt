#490 — Fix City Autocomplete + County/State Autofill

READ-ONLY AUDIT FIRST:
Inspect only the files listed below. Confirm the current city autocomplete method names, dropdown markup, and autofill methods before editing.

SCOPE:
Create Offer Listing flows only:
- Seller
- Buyer
- Landlord
- Tenant
Commission-based only.

DO NOT TOUCH:
- Hire Agent flows
- Limited Service / flat-fee flows
- Edit flows
- routes
- database schema
- Next/Submit validation
- unrelated labels, layout, tooltips, or placeholders

FILES:
app/Http/Livewire/OfferListing/Seller/SellerOfferListing.php
app/Http/Livewire/OfferListing/Buyer/BuyerOfferListing.php
app/Http/Livewire/OfferListing/Landlord/LandlordOfferListing.php
app/Http/Livewire/OfferListing/Tenant/TenantOfferListing.php

resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/property-preferences.blade.php
resources/views/livewire/offer-listing/offer-buyer-tabs/commission-based/property-preferences.blade.php
resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/property-preferences.blade.php
resources/views/livewire/offer-listing/offer-tenant-tabs/commission-based/property-details.blade.php

FIX 1 — DROPDOWN CLICK RACE CONDITION:
Problem:
wire:blur clears city suggestions before wire:click fires.

In each affected city autocomplete input, remove:

wire:blur="selectPropertyCitySuggestion()"

Then update each city suggestion item from:

wire:click="selectPropertyCitySuggestion('...')"

to:

@mousedown.prevent="$wire.selectPropertyCitySuggestion('...')"

Apply to:
- Seller property_city suggestions
- Landlord property_city suggestions
- Landlord newCity acceptable-city suggestions
- Buyer newCity acceptable-city suggestions
- Tenant property_city suggestions
- Tenant newCity acceptable-city suggestions

Use existing method names exactly. Do not create new methods.

FIX 2 — COUNTY/STATE ALWAYS UPDATE:
Problem:
Autofill only updates county/state if fields are empty.

In these methods, remove empty() guards so city selection always overwrites county/state/state:

- SellerOfferListing.php: autoPopulateFromPropertyCity()
- LandlordOfferListing.php: autoPopulateFromPropertyCity()
- TenantOfferListing.php: autoPopulateFromPropertyCity()
- BuyerOfferListing.php: autoPopulateFromCity() for state

Do not change unrelated validation or submit behavior.

FIX 3 — VERIFY CITY DATA:
Check us_cities for:
Treasure Island

Confirm it can resolve:
County = Pinellas County, FL
State = Florida

Also check us_zip_codes fallback if the existing code uses it.

If missing, seed from the existing city dataset already used by the app.
Do not alter table structure.

VALIDATION:
Test all 4 commission-based create flows:

Type:
Treasure Island

Expected:
- dropdown appears
- click works
- city remains filled
- county = Pinellas County, FL
- state = Florida
- changing city updates county/state again

FINAL CHECK:
Run git diff and confirm no changes to:
- Hire Agent files
- Limited Service files
- routes
- database schema
- Next/Submit validation

STOP if a fix appears to require schema, route, or broad refactor changes.