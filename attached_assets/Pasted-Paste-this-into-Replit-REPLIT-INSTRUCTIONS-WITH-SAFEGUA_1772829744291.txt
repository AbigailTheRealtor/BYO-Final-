Paste this into Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — FINALIZE HIRE A TENANT’S AGENT CREATE + EDIT + REQUIRED FIELD SUMMARY

### IMPORTANT

This request includes **all final fixes** for **Hire a Tenant’s Agent** and should be handled in three separate buckets:

1. **Edit flow fixes**
2. **Create flow fixes**
3. **Required field / validation summary fixes**

Do not mix the logic carelessly.
Keep working behavior intact, especially **Leasing Space**, which is currently the reference model for stable selection behavior.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename or delete any database columns.**
2. **Do not modify Buyer, Seller, or Landlord flows.**
3. **Only fix Hire a Tenant’s Agent.**
4. **Do not remove or overwrite existing listings, drafts, bids, uploaded photos, or video links.**
5. **Do not change labels, tooltip wording, option wording, or field order unless explicitly requested below.**
6. **Do not break Leasing Space behavior.**
   Leasing Space currently works correctly and should be used as the model for the broken multi-select/select fields.
7. **Do not do a broad sitewide Select2 rewrite.**
8. **Do not allow JS visual state to differ from the actual Livewire property being saved.**
9. **Do not let placeholder/default logic overwrite real selected values.**
10. **Maintain backward compatibility for already-saved records.**
11. **If existing records are stored in mixed formats, normalize them safely without changing the schema.**
12. **Do not mark complete unless Create, Edit, Save Draft, Reopen Edit, and Listing Display all work correctly for the affected fields.**

---

# PART 1 — EDIT FLOW FIXES

## When user clicks Edit on an existing Hire a Tenant’s Agent listing

---

## 1) Acceptable Property Styles must load and display correctly

### Current problem

* It was previously selected
* It does not show correctly on the actual listing
* It does not auto-populate correctly when user clicks Edit

### Required fix

Make **Acceptable Property Styles** work correctly end-to-end on the Edit path:

* previously saved values must load correctly when Edit opens
* previously saved values must appear selected in the UI
* values must remain selected after saving edits
* values must display correctly on the final listing

### Safeguard

If this field is multi-select, normalize it consistently:

* Livewire/component property should behave as an array
* decode before rendering
* encode only if needed for persistence
* listing output should read normalized values safely

---

## 2) Minimum Bathrooms Needed = Other must show dependent input on Edit

### Current problem

On **Residential and Commercial Hire a Tenant’s Agent Edit**:

* dropdown shows **Other**
* custom box underneath does not appear

### Required fix

When Edit loads:

* if **Minimum Bathrooms Needed = Other**
* automatically show the dependent custom input field
* auto-populate the previously saved custom value

### Safeguard

Do not rely only on front-end change events.
This must work from the hydrated Edit state on initial load.

---

## 3) Minimum Bedrooms Needed = Other must show dependent input on Edit

### Current problem

On **Residential Hire a Tenant’s Agent Edit**:

* dropdown shows **Other**
* custom box underneath does not appear

### Required fix

When Edit loads:

* if **Minimum Bedrooms Needed = Other**
* automatically show the dependent custom input
* auto-populate the previously saved custom value

### Safeguard

This must work immediately on Edit load, not only after the user re-selects the option.

---

# PART 2 — CREATE FLOW FIXES

## When user creates a new Hire a Tenant’s Agent listing

---

## REFERENCE IMPLEMENTATION

**Leasing Space currently behaves correctly.**

It:

* stays selected
* does not flash
* does not delete
* shows dependent fields correctly
* saves correctly

Use the **same stable behavior pattern** for the broken fields below.

That means:

* one true Livewire property per field
* no duplicate state sources
* no re-init wipeout
* no placeholder overwrite
* no selection flash/delete after click
* proper hydration/init timing

---

## 4) Offered Lease Term flashes and deletes

### Required fix

On create:

* selected option(s) must stay selected immediately after click
* must not flash or delete
* must persist through tab navigation
* must save correctly
* must auto-populate later on Edit

---

## 5) Garage/Parking Spaces flashes and deletes

### Required fix

On create:

* selected option(s) must stay selected
* must not flash/delete
* must persist through tabs
* must save correctly
* must auto-populate later on Edit

---

## 6) Acceptable Property Styles flashes and deletes on create

### Required fix

On create:

* selected values must stay selected
* must not flash/delete
* must save correctly
* must auto-populate on Edit later
* must display correctly on the final listing

### Important

This field has both a **create issue** and an **edit/display issue**.
Fix both without breaking either one.

---

## 7) Non Negotiable Amenities and Property Features flashes/deletes and still says “select”

### Current problem

* after selection, it still says **select**
* chosen values flash and delete

### Required fix

On create:

* selected values must remain selected
* placeholder text must not continue to show once there is a real selection
* field must not reset after selection
* values must save correctly
* values must persist through drafts and edits

---

## 8) Services flash and delete

### Current problem

Some selected services flash and delete after being selected

### Required fix

On create:

* selected service options must remain selected
* no flash/delete
* all selected services must persist through Save Draft
* all selected services must auto-populate later on Edit

---

# PART 3 — REQUIRED FIELD CORRECTIONS

Some fields are incorrectly marked as required.

Update both:

* the actual required validation logic
* the red star indicators next to the labels

---

## Remove required status from these fields

These fields should **NOT** be required:

* Acceptable Cities
* Pets
* Garage/Parking Features
* Age Restricted Community
* Furnishings

### Required changes

For each field above:

* remove required validation
* remove the red star next to the label

---

## These fields MUST remain required

Make sure these still have:

* required validation
* red star indicator

### Keep required:

* Acceptable Counties
* Acceptable Property Styles
* Offered Lease Term
* Offered Lease Space

### Important

If any of these lost their required status, restore it.

---

# PART 4 — REQUIRED FIELD SUMMARY BOX FIX

This is a **display fix**, not a full validation removal.

---

## CLARIFICATION — DESIRED BEHAVIOR

### In the red summary box at the top:

Keep:

* the message
  **“Please complete the required fields before continuing.”**
* the list of missing field names underneath

Do **not** show the generic sentence:

* **“This field is required.”**
  inside the red summary box

### Under each actual field:

It is **OK** to continue showing:

* **“This field is required.”**

So:

* remove the generic sentence only from the **top red summary box**
* keep field-level validation text under the individual inputs

---

## Current problem

The red summary box currently shows:

* main message
* list of missing fields
* and also the extra generic line
  **“This field is required.”**

That generic line should not appear inside the red summary box.

---

Required fix

Update the summary error rendering so it behaves like this:

Keep in the red box:

Please complete the required fields before continuing.

Display a list of the missing required fields

Remove from the red box:

This field is required.

Keep under each field if applicable:

This field is required.

Important rule

The red box should only list fields that are currently required and missing.
If a field is no longer required, it should never appear in the summary list.

---

# ROOT-CAUSE CHECKS BEFORE IMPLEMENTING

Before changing code, confirm the actual causes of the flashing/deleting and edit hydration problems. Check for all of the following:

1. Livewire re-render replacing DOM after selection
2. Select2 or JS init re-running and wiping values
3. duplicate change listeners
4. wrong Livewire property being updated
5. Blade checking one property while JS updates another
6. hidden bridge input and Livewire property getting out of sync
7. JSON vs array inconsistencies
8. placeholder/default logic overwriting actual selections
9. create path and edit path using different normalization logic
10. dependent “Other” fields only reacting to user change events instead of initial hydrated values

Do not guess. Confirm the true source before finalizing.

---

# DATA NORMALIZATION RULES

## For multi-select fields

Use a consistent pattern:

* Livewire/component property = array
* DB storage = encode only if needed
* Edit load = decode back into array
* Listing display = normalized output

Support backward compatibility for:

* null
* empty string
* JSON string
* array
* comma-separated legacy values if present

---

## For single-select fields with dependent custom inputs

Ensure:

1. main value loads first
2. dependent visibility condition evaluates immediately on Edit load
3. saved custom value auto-populates

---

# REQUIRED TEST CASES — MUST PASS

## EDIT FLOW TESTS

### Acceptable Property Styles

1. Create a listing with selected property styles
2. Save it
3. Open the listing
4. Confirm property styles display on the listing
5. Click Edit
6. Confirm property styles auto-populate correctly
7. Save again
8. Confirm they still display correctly on the listing

---

### Minimum Bathrooms Needed = Other

1. Save listing with **Other** selected and custom value filled in
2. Click Edit
3. Confirm **Other** is still selected
4. Confirm the custom input box is visible immediately
5. Confirm the saved custom value is populated

---

### Minimum Bedrooms Needed = Other

1. Save listing with **Other** selected and custom value filled in
2. Click Edit
3. Confirm **Other** is still selected
4. Confirm the custom input box is visible immediately
5. Confirm the saved custom value is populated

---

## CREATE FLOW TESTS

### Offered Lease Term

1. Select option(s)
2. Confirm no flash/delete
3. Move tabs and return
4. Confirm selection remains
5. Save Draft
6. Reopen Draft
7. Confirm selection remains

---

### Garage/Parking Spaces

1. Select option(s)
2. Confirm no flash/delete
3. Move tabs and return
4. Save Draft
5. Reopen Draft/Edit
6. Confirm selection remains

---

### Acceptable Property Styles

1. Select multiple values
2. Confirm no flash/delete
3. Save Draft
4. Reopen
5. Confirm values remain
6. Submit listing
7. Confirm values display on final listing
8. Click Edit
9. Confirm values auto-populate

---

### Non Negotiable Amenities and Property Features

1. Select multiple values
2. Confirm placeholder no longer says **select**
3. Confirm no flash/delete
4. Save Draft
5. Reopen
6. Confirm values remain

---

### Services

1. Select multiple services
2. Confirm none flash/delete
3. Save Draft
4. Reopen Edit
5. Confirm all selected services remain

---

## REQUIRED FIELD TESTS

### Required / non-required field labels

1. Confirm red star is removed from:

   * Acceptable Cities
   * Pets
   * Garage/Parking Features
   * Age Restricted Community
   * Furnishings

2. Confirm red star remains on:

   * Acceptable Counties
   * Acceptable Property Styles
   * Offered Lease Term
   * Offered Lease Space

---

### Validation behavior

1. Trigger validation with multiple missing required fields
2. Confirm red summary box shows:

   * **Please complete the required fields before continuing.**
   * the list of missing fields
3. Confirm red summary box does **not** show:

   * **This field is required.**
4. Confirm field-level error text may still appear directly beneath the individual fields

---

# DO NOT MARK COMPLETE UNLESS ALL OF THE FOLLOWING ARE TRUE

* Acceptable Property Styles works in both Create and Edit
* Acceptable Property Styles displays correctly on listing
* Minimum Bathrooms Needed = Other shows custom field on Edit
* Minimum Bedrooms Needed = Other shows custom field on Edit
* Offered Lease Term no longer flashes/deletes
* Garage/Parking Spaces no longer flash/delete
* Non Negotiable Amenities and Property Features no longer flash/delete and no longer keeps the placeholder text after selection
* Services no longer flash/delete
* required field logic is corrected
* red star indicators are corrected
* top red summary box no longer shows **“This field is required.”**
* field-level validation text may still show beneath the actual fields
* Save Draft works
* Reopen Edit works
* Listing display works
* Leasing Space behavior remains unchanged
* changes remain limited to Hire a Tenant’s Agent only

---

# FINAL EXPECTATION

After implementation:

## When creating a Hire a Tenant’s Agent listing

* selections stay selected
* no flash/delete
* required fields behave correctly
* non-required fields no longer show red stars

## When editing a Hire a Tenant’s Agent listing

* saved values auto-populate correctly
* dependent “Other” fields appear automatically
* Acceptable Property Styles displays correctly on the listing and in Edit

## Validation UX

* red summary box only shows:

  * **Please complete the required fields before continuing**
  * list of missing fields
* red summary box does **not** show:

  * **This field is required**
* field-level error text under inputs may remain

---