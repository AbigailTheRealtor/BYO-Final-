Stop doing parity-by-description.

Buyer is still NOT matching Tenant visually or structurally.
The screenshots confirm this.

From this point forward, do NOT analyze whether Buyer is “at parity.”
Instead, implement exact Tenant-to-Buyer render parity.

==================================================
BUYER ONLY
==================================================

Do not touch Seller, Landlord, or Tenant unless absolutely required for shared partial reuse.

This task is ONLY for Buyer.

==================================================
SOURCE OF TRUTH
==================================================

Tenant is the exact source of truth.

You must identify the exact live Tenant render blocks/partials/components used for:

1. Match Summary in the main bid card
2. Match Summary in the Private Compensation & Agency Agreement Terms area / modal
3. Counter Bidding History section
4. Counter bid card body
5. Counter action area
6. View Counter Terms / Edit Counter Terms area
7. Any related banners / notices / section headings / spacing wrappers

Then make Buyer use the SAME render structure.

==================================================
IMPORTANT IMPLEMENTATION RULE
==================================================

Do NOT “approximate” Tenant.

Do one of these only:

OPTION A:
Reuse the exact same partial/component/markup pattern Tenant uses, adapted only for Buyer-specific field names/routes.

OPTION B:
Copy the exact Tenant HTML / Blade structure / classes / wrappers / spacing / typography into Buyer and only swap Buyer-specific variables and routes.

Do NOT create a separate Buyer version that is merely similar.

==================================================
WHAT MUST MATCH TENANT EXACTLY
==================================================

Buyer must match Tenant exactly in:

- font size
- heading size
- spacing
- padding
- margins
- section separation
- banner placement
- button placement
- button sizing
- label wording
- match summary structure
- Original Match / Counter Match structure
- note placement
- counter history heading
- counter card layout
- Changed / Added / Removed visual treatment
- private compensation rendering pattern

If Tenant has two summary boxes, Buyer must have two summary boxes in the same structure.
If Tenant has a certain wrapper/card/container hierarchy, Buyer must use the same wrapper/card/container hierarchy.

==================================================
KNOWN AREAS STILL WRONG
==================================================

Based on current screenshots, Buyer is still wrong in at least these areas:

1. Match Summary is still not the same as Tenant
2. Font / typography / spacing still differ from Tenant
3. Counter Bidding History layout still differs from Tenant
4. Counter bid card still differs from Tenant
5. Private Compensation & Agency Agreement Terms rendering still differs from Tenant

Do not claim these are already at parity.

==================================================
DO NOT SPEND TIME ON
==================================================

- logged-out public view
- paths gated off for non-authenticated traditional rendering
- explanations about why some paths are inactive

The target is the authenticated Buyer Traditional listing bid UI for listing 155.

==================================================
WORKING TARGET
==================================================

Use:
- /buyer/agent/auction/view/155

And compare directly against the equivalent Tenant authenticated listing UI.

==================================================
REQUIRED PROCESS
==================================================

1. First, list the exact Tenant files/line ranges or partials being used as the render source of truth for each of these:
   - main match summary
   - private compensation match summary
   - counter bidding history
   - counter card body
   - counter action area

2. Then list the exact Buyer files/line ranges that currently differ

3. Then implement exact render parity by reuse or direct markup/class copy

4. Then provide BEFORE / AFTER screenshots of:
   - Buyer expanded bid card
   - Buyer counter bidding history
   - Buyer private compensation area / modal
   - Tenant equivalent view for side-by-side comparison

==================================================
COMPLETION STANDARD
==================================================

Do not mark this complete unless Buyer visually matches Tenant in screenshots.

“Logic is the same” is not enough.
“Structurally similar” is not enough.
“AT PARITY” is not enough.

The completion standard is:
SCREENSHOT-LEVEL MATCH TO TENANT.

==================================================
DO NOT CHANGE
==================================================

- scoring formulas
- helper logic
- baseline logic
- database schema
- business logic
- routes
- other roles

Only Buyer rendering / structure / classes / layout.

==================================================
IF SHARED PARTIAL REUSE IS POSSIBLE
==================================================

That is preferred.
If shared partial reuse changes Tenant output, preserve Tenant exactly and adapt Buyer into that same structure safely.

If exact Tenant reuse is NOT possible, stop and explain the blocker instead of improvising a Buyer-only approximation.