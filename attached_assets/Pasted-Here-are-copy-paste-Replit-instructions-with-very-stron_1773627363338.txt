Here are **copy-paste Replit instructions with very strong safeguards** so this stays **surgical** and does **not** break other flows, selects, spacing logic, or working field behavior.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A LANDLORD AGENT ONLY — FIELD DISPLAY / EDIT / CONDITIONAL INPUT FIXES

## SCOPE

Make the following **targeted fixes only** in the **Hire a Landlord Agent** flow.

### Required fixes:

1. **Furnishings** shows on the listing even when it was **not filled in**
   → it should **not display** if empty

2. **Desired Lease Term**
   → remove the **red star** / required indicator

3. **Bathrooms**
   → when user selects **Other**, the custom input box must show in **edit mode**
   → the custom value must display correctly on the actual listing
   → the listing should not show a blank label like `Bathrooms:` with no value

4. **Carport**
   → when user selects **Other**, the custom input box must show in edit mode
   → the custom value must display correctly on the listing

5. **Desired Lease Term**
   → when user selects **Other**, the custom input box must show in edit mode
   → the selected values must display correctly on edit
   → current selections must not disappear

6. For **any fields/terms not filled in**, they should **not show on the listing**, matching the behavior used in **Hire a Tenant Agent**

7. In the actual listing, for **Leasing Terms**, the questions/rows are **not evenly spaced**
   → fix the spacing/layout only in the **Landlord listing display**

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Landlord Agent flow.**
2. **Do not modify Tenant, Buyer, or Seller flows.**
3. **Do not modify shared Select2/global select logic unless absolutely necessary, and if touched, wrap it so it only affects Landlord flow.**
4. **Do not change Select2 format, dropdown appearance, width, placeholder style, tag style, or interaction pattern.**
5. **Do not rename database columns, Livewire properties, wire:model bindings, IDs, names, route names, or tab IDs.**
6. **Do not run migrations.**
7. **Do not refactor broad listing rendering logic unless absolutely necessary, and if touched, wrap it to affect Landlord only.**
8. **Do not change any other required-field behavior except removing the red star from Desired Lease Term.**
9. **Do not change wording, tooltips, placeholders, or field order unless strictly needed for one of the issues above.**
10. **Do not break draft save, edit mode, listing display, or final submit.**
11. **Do not change any other “Other” field behavior unless it belongs to these exact Landlord fields.**
12. **Do not cause empty terms to display on other listing types.**
13. **Keep this surgical and field-specific.**

---

## IMPORTANT CONTEXT

We have had issues in the past with:

* select fields breaking
* edit/autopopulate failing
* “Other” inputs disappearing
* listing display showing blank labels
* changes affecting other listing types

Because of that:

* do **not** do a broad cleanup
* do **not** rewrite all field rendering
* do **not** touch unrelated selection boxes
* do **not** refactor all conditional input handling
* only fix these exact Landlord issues

---

# STEP 1 — IDENTIFY THE EXACT LANDLORD FILES FIRST

Before editing anything, confirm the exact active files for **Hire a Landlord Agent** related to:

1. create/edit form rendering
2. Landlord tab partials
3. conditional “Other” field rendering for:

   * Bathrooms
   * Carport
   * Desired Lease Term
4. listing/review display templates
5. Broker Compensation display section
6. Leasing Terms listing display section

Map the exact files first.

Typical places may include:

* `app/Http/Livewire/TenantAgentAuction.php` or Landlord shared component logic
* `resources/views/livewire/tenant-agent-auction.blade.php`
* `resources/views/livewire/...landlord...tabs/...`
* listing/review display partials

But do **not assume**. Confirm the real active files first.

---

# STEP 2 — FIX “FURNISHINGS” SO IT DOES NOT SHOW IF EMPTY

## Current bug

On the Landlord listing display, **Furnishings** appears even when it was not filled in.

## Required behavior

If **Furnishings** has no meaningful value, do **not** render the label or row at all.

### Important

Treat these as empty/non-display values if applicable:

* `null`
* empty string
* empty array
* whitespace-only
* placeholder/default non-answer values if those exist in this field’s display logic

### Do not:

* change Tenant listing display
* change Buyer/Seller display
* change how other filled Landlord fields render

This is a **Landlord display-only conditional rendering fix** for Furnishings.

---

# STEP 3 — REMOVE RED STAR FROM “DESIRED LEASE TERM”

## Required behavior

Remove only the **visual required marker** / red star from **Desired Lease Term** in **Hire a Landlord Agent**.

### Important safeguards

* do **not** change any other field’s red star
* do **not** change validation logic unless this field is incorrectly enforced elsewhere and absolutely must be aligned
* do **not** alter the field’s select format
* do **not** affect Desired Lease Term in other flows

This is primarily a **UI marker change** for Landlord only.

---

# STEP 4 — FIX “BATHROOMS → OTHER” INPUT ON EDIT + LISTING DISPLAY

## Current bugs

1. When user selected **Other** for **Bathrooms**, the custom input box does **not** show in edit mode
2. On the actual listing, it just says `Bathrooms:` but the value is blank, even though user filled it out

## Required behavior

For **Landlord Bathrooms** only:

* if the saved selection is **Other**, the custom input field must appear in edit mode
* the saved custom value must repopulate correctly
* the listing must display the custom value
* if no actual bathroom value exists, the label should not show at all

### Investigate and fix

Trace the exact wiring for:

* Bathrooms select field
* Bathrooms “Other” trigger
* Bathrooms custom text input
* saved property name for the custom value
* edit hydration
* listing display logic

### Important

Do **not** rewrite all “Other” logic globally.
Fix only the Bathrooms path for Landlord if possible.

---

# STEP 5 — FIX “CARPORT → OTHER” INPUT ON EDIT + LISTING DISPLAY

## Current bug

For **Carport**, it says `Yes`, but when user selected **Other**, the custom input box does not show on edit and the custom value is not being displayed properly.

## Required behavior

For **Landlord Carport** only:

* if **Other** was selected, the custom input box must show in edit mode
* the saved custom value must repopulate correctly
* the actual listing must display the custom value correctly
* if the term is empty, do not show a blank label/row

### Investigate and fix

Trace:

* Carport select
* Carport “Other” trigger
* Carport custom input
* saved value path
* edit hydration
* listing display rendering

### Important

Do **not** touch unrelated parking/garage/carport logic in other flows.

---

# STEP 6 — FIX “DESIRED LEASE TERM → OTHER” INPUT ON EDIT + SELECTION DISPLAY

## Current bugs

1. When user selects **Other** for **Desired Lease Term**, the input box does not show on edits
2. The selections do not show correctly on edit

## Required behavior

For **Landlord Desired Lease Term** only:

* if **Other** is selected, show the custom input box in edit mode
* selected lease term values must autopopulate correctly in edit mode
* custom “Other” value must repopulate correctly
* selections must not disappear
* listing display must show meaningful values only

### Important

This must be fixed **without changing the select format**

* do not change Select2 structure
* do not change dropdown behavior
* do not change event flow globally
* use the same field-safe pattern as working Landlord/Tenant fields

---

# STEP 7 — FOR EMPTY TERMS, DO NOT SHOW THEM ON THE LISTING

This is important and should match the **Hire a Tenant Agent listing behavior**.

## Required behavior

For **Hire a Landlord Agent listing display only**:
If a field/term has no actual value, do **not** show the label/row at all.

This applies especially to empty display rows like:

* `Landlord's Broker Compensation:`
* `Payment Timing & Renewal Terms:`
* `Property Management:`
* `Lease-Option Details:`
* `Purchase Fee Details:`
* `Legal Terms:`
* `Brokerage Relationship:`

If there is no content/value for a given row, hide that row completely.

If an entire section has no filled values, hide the section header too if that matches the Tenant behavior.

### Important safeguards

* do **not** hide filled rows
* do **not** affect other listing types
* do **not** remove data, only suppress empty display rows
* follow the same display rule pattern already used in the Tenant listing view if available

---

# STEP 8 — FIX LEASING TERMS SPACING ON THE ACTUAL LANDLORD LISTING

## Current bug

The questions/rows under **Leasing Terms** are not evenly spaced on the actual Landlord listing display.

## Required behavior

On the **Landlord listing display only**:

* make the Leasing Terms rows evenly spaced
* consistent vertical spacing between rows
* align with the visual structure used in the cleaner listing displays
* do not change content
* do not alter form field spacing in edit/create mode
* do not change spacing for other sections unless necessary to match the same Landlord listing layout style

### Important

This is a **display/layout fix only** for the actual listing view.

Do not:

* change database values
* change field labels
* change conditional visibility logic except where empty rows must be hidden

---

# STEP 9 — PRESERVE BACKWARD COMPATIBILITY

If older Landlord records already have saved values for:

* Bathrooms Other
* Carport Other
* Desired Lease Term Other

they must:

* still load correctly
* show the custom input on edit when applicable
* display correctly on listing
* not break existing saved records

Do not require data migration.

---

# STEP 10 — VERIFY THESE EXACT STATES

After the fix, test all of these for **Hire a Landlord Agent only**:

## A. Furnishings empty

* expected: does not show on listing

## B. Desired Lease Term

* expected: red star removed
* field still displays correctly
* select format unchanged

## C. Bathrooms → Other

* save value
* reopen edit
* expected: Other input visible and populated
* expected: listing shows actual custom bathroom value
* expected: no blank `Bathrooms:` row

## D. Carport → Other

* save value
* reopen edit
* expected: Other input visible and populated
* expected: listing shows actual custom value

## E. Desired Lease Term → Other

* save value
* reopen edit
* expected: Other input visible and populated
* expected: selected values still show correctly

## F. Empty Broker Compensation terms

* expected: empty rows do not appear on listing
* expected: section/header hidden too if fully empty and that matches Tenant behavior

## G. Leasing Terms spacing

* expected: rows evenly spaced on actual listing

---

# STEP 11 — DO NOT BREAK THESE

Explicitly confirm after the fix:

* no Select2 formatting changed
* no dropdown layout changed
* no placeholder behavior changed
* no unrelated field logic changed
* Tenant unchanged
* Buyer unchanged
* Seller unchanged
* only Landlord flow touched

---

# STEP 12 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. exact root cause for each issue:

   * Furnishings showing while empty
   * Bathrooms Other not showing on edit/display
   * Carport Other not showing on edit/display
   * Desired Lease Term Other not showing on edit/display
   * empty terms showing on listing
   * Leasing Terms spacing issue
3. exact fix applied for each
4. confirmation that Select2 format was not changed
5. proof edit mode now repopulates correctly
6. proof empty terms no longer show on listing
7. confirmation only Hire a Landlord Agent was touched

---

## IMPORTANT

This is a **Landlord-only, surgical cleanup**.

Do **not**:

* rewrite select logic broadly
* touch unrelated “Other” inputs
* change selection box format
* alter other listing types

The final result should be:

* empty fields do not show on Landlord listing
* Bathrooms/Carport/Desired Lease Term “Other” inputs repopulate correctly on edit
* listing shows actual saved values instead of blank labels
* Desired Lease Term red star removed
* Leasing Terms rows are evenly spaced
* no other flows or select behavior are changed

---

If you want, I can also convert this into a **shorter hard-stop Replit version** with less explanation and more direct commands.
