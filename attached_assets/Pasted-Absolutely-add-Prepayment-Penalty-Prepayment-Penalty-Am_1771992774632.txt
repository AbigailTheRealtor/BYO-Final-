Absolutely — add **Prepayment Penalty + Prepayment Penalty Amount** to the same Buyer listing display rule set.

Here are the updated **Replit instructions with safeguards** (Buyer **actual listing display only**) including **all 5** Yes/No + Amount pairs:

---

## ✅ REPLIT INSTRUCTIONS (Hire a Buyer’s Agent) — INLINE “YES (AMOUNT)” DISPLAY (UPDATED)

### 🔒 SAFEGUARDS (must follow)

* **DO NOT** change database schema/migrations.
* **DO NOT** rename stored keys/values, Livewire properties, or form inputs.
* **DO NOT** change listing creation/edit logic — **display-only**.
* **DO NOT** affect Seller/Tenant/Landlord listing views.
* **ONLY** modify **Buyer actual listing display** template/partial/helper used to render these fields.
* If using a shared renderer, add a **Buyer-only wrapper** or optional parameter so other roles remain unchanged.

---

## ✅ Fields to update (Buyer actual listing display)

Render each boolean + amount pair as:

* **Yes ($X,XXX)**
* **No** (and hide the amount line)

### Apply to these pairs:

1. **Offered Option Fee** + **Option Fee Amount**
2. **Buyer Pre-Approved for a Loan** + **Buyer Pre-Approval Amount**
3. **Balloon Payment** + **Balloon Payment Amount**
4. **Rent Credit Toward Purchase Price** + **Rent Credit Amount**
5. **Prepayment Penalty** + **Prepayment Penalty Amount**

---

## ✅ Display Rules (exact)

* If boolean is **Yes**:

  * show: `Yes ($amount)` (amount in parentheses)
  * hide the separate amount row
* If boolean is **No**:

  * show: `No`
  * hide the separate amount row
* If boolean is **Yes** but amount is blank:

  * show: `Yes` (no parentheses)

---

## ✅ Replit AI Copy/Paste Prompt (single block)

Paste this into Replit:

```text
Update Hire a Buyer’s Agent ACTUAL LISTING display to show certain Yes/No + Amount pairs inline as: Yes ($Amount). Display-only.

SAFETY RULES:
- Do NOT change DB schema/migrations.
- Do NOT rename keys/values or Livewire properties.
- Do NOT modify creation/edit UI.
- Do NOT affect Seller/Tenant/Landlord.
- Only modify Buyer actual listing display (and Buyer-only helper/partial if needed).

FIELDS TO CHANGE (Buyer actual listing display):
A) Offered Option Fee + Option Fee Amount
B) Buyer Pre-Approved for a Loan + Buyer Pre-Approval Amount
C) Balloon Payment + Balloon Payment Amount
D) Rent Credit Toward Purchase Price + Rent Credit Amount
E) Prepayment Penalty + Prepayment Penalty Amount

REQUIRED OUTPUT FORMAT:
- If boolean is Yes: show "Yes ($X,XXX)" with the amount inside parentheses.
- If boolean is No: show "No" and do not show any amount row/value.
- If Yes but amount is empty, show just "Yes" (no parentheses).

IMPLEMENTATION:
1) In the Buyer show/listing view, for each pair render the boolean line value as:
   Yes/No + optional parentheses amount (same currency formatting as existing amount display).
2) Remove or conditionally hide the separate Amount rows for these 5 pairs ONLY on Buyer actual listing display.
3) Keep all other fields unchanged.

QA:
- Open Buyer listings where these are Yes + amount and confirm they show: Yes ($amount)
- Confirm No hides the separate amount line
- Confirm formatting uses parentheses, not just spacing
- Confirm no other roles changed
```

---

If you want this super clean long-term: after this Buyer-only change works, we can apply the same helper to Seller/Tenant/Landlord display too (but I’m keeping this strictly Buyer-only per your safeguards).
