Copy/paste this to Replit. It’s **laser-focused** on the one bug: **Acceptable Exchange/Trade shows blank on Edit**. It also prevents Replit from “wandering” into other files.

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — Seller Edit ONLY: “Acceptable Exchange/Trade” must auto-populate

### HARD SAFEGUARDS

1. **Fix ONLY Hire A Seller’s Agent → EDIT** behavior for the field **Acceptable Exchange/Trade**.
2. **Do NOT touch Tenant/Buyer/Landlord files** or any shared/global Select2 managers.
3. **Do NOT change option lists, tooltips, labels, styling, or tab logic.**
4. **Do NOT rename/delete DB columns or modify migrations.**
5. Goal is ONLY: when clicking **Edit**, the input must **auto-populate** with the last saved value(s).

---

# WHAT’S BROKEN

On Seller listings, the **Acceptable Exchange/Trade** input is **blank on Edit**, even though it was previously saved.

This is almost always one of these:

* the Edit component **is not assigning the property in `mount()`**
* the blade input is bound to a **different Livewire property** than the one being loaded/saved
* the stored format is **JSON/array** but the input expects a **string**
* a “reset/normalize” block is unintentionally wiping the property during Edit initialization

---

# STEP 1 — IDENTIFY THE EXACT BINDING (Seller Edit view)

In the Seller Edit form where this field renders, find the input for **Acceptable Exchange/Trade** and identify:

* the exact `wire:model...` (example: `wire:model.defer="acceptable_exchange_item"` or `wire:model.defer="acceptable_exchange_trade"`)
* OR if it uses a `$metadata[...]` key (example: `wire:model.defer="metadata.acceptable_exchange_item"`)

✅ Replit must not change any other inputs—only this one if needed.

---

# STEP 2 — ENSURE EDIT `mount()` LOADS THE VALUE INTO THAT EXACT PROPERTY

In the Seller Edit Livewire component (the class used for Seller edits):

### Requirement

In `mount($id)` (or wherever the auction is loaded), after `$this->auction = ...` add/confirm:

1. Read the saved value from the auction record using the correct DB column key (whatever Seller save uses today).
2. Assign it into the **same Livewire property name** used by the Edit blade binding.

### Normalization rule (must be safe)

Because saved data may be stored as:

* string
* JSON string
* array/cast

Do this normalization when loading:

* If value is `null` → set edit property to `''` (for text input) OR `[]` (for multi-select)
* If value is a JSON string representing an array → decode
* If decoded result is an array AND the edit UI is a **text input** → convert to a readable string (e.g., comma-separated) OR whatever format you already use elsewhere
* If the edit UI is a **multi-select** → ensure it becomes an array

✅ Do NOT modify saving behavior here—only ensure the edit form receives the saved value.

---

# STEP 3 — MAKE SURE NOTHING RESETS IT AFTER LOADING

Search inside the Seller Edit component for any of these patterns that might wipe it:

* `$this->reset(...)`
* setting the property to `''` / `[]` during render
* “initialize defaults” blocks that run on edit
* watchers like `updatedXxx()` that re-normalize and overwrite

### Requirement

If any of those reset the Acceptable Exchange/Trade property during Edit, add a **guard**:

* Only set defaults if creating a new listing / draft with no record
* Do not overwrite if auction already exists and has saved value

---

# STEP 4 — VERIFY SAVE/EDIT USES THE SAME KEY

Still within Seller only:

* Confirm the Seller save method writes Acceptable Exchange/Trade to the same field it reads from.
* If save writes to `acceptable_exchange_item` but mount reads from `acceptable_exchange_trade` (or vice versa), fix the mismatch **in Seller Edit only** by reading the saved key.

✅ No DB changes. No cross-role changes.

---

# ACCEPTANCE TEST (MUST PASS)

1. Open an existing Seller listing that already has Acceptable Exchange/Trade saved.
2. Click **Edit**.
   ✅ The field must immediately show the last saved value(s), not blank.
3. Save without changing it.
4. Re-open Edit.
   ✅ It must still be populated.

---

## OPTIONAL “DO NOT WANDER” RULE (ADD THIS LINE)

**Before editing any code: locate where the Seller Edit blade binds Acceptable Exchange/Trade via `wire:model`, then only change the matching Seller Edit Livewire mount/load logic for that same property. Do not modify anything else.**

---

If you want, paste the **wire:model name** you’re using for that field (just that one line from the blade), and I’ll rewrite this into a super-specific “change X property in mount(), decode Y format, don’t touch anything else” version that Replit can’t misinterpret.
