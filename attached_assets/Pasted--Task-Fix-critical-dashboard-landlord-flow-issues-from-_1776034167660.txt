**Task: Fix critical dashboard + landlord flow issues (from audit report)**

## Scope

Apply ONLY the fixes listed below.

Do NOT refactor unrelated logic.
Do NOT change database.
Do NOT change working Tenant, Buyer, Seller flows beyond alignment.

---

# 🔴 FIX 1 — Landlord Accept / Reject / Counter buttons

File:
resources/views/hire_landlord_agent/view.blade.php

### Problem:

No action buttons rendered in private modal

### Fix:

Replicate the exact structure used in:

Tenant:
resources/views/hire_tenant_agent/view.blade.php

Buyer:
resources/views/hire_buyer_agent/view.blade.php

Seller:
resources/views/hire_seller_agent/view.blade.php

### Requirements:

* Add Accept button → correct landlord route
* Add Reject button → correct landlord route
* Add Counter button → correct landlord route
* Match:

  * styling
  * placement
  * conditions (owner only, active state)

---

# 🔴 FIX 2 — Landlord Accepted Bid Summary links

File:
resources/views/hire_landlord_agent/view.blade.php

### Problem:

No summary links exist

### Fix:

Add summary lookup using pattern from Tenant/Buyer:

AcceptedBidSummary::where('accepted_bid_id', $bid_id)
->where('agent_user_id', data_get($bid, 'user_id'))
->first();

### Then render:

* View Summary
* Sign
* Download PDF

### Must match:

* Tenant layout
* Buyer layout
* Seller layout

---

# 🔴 FIX 3 — Agent bidding list redirect bug

Files:
resources/views/agent_biding_listing/buyer.blade.php
resources/views/agent_biding_listing/seller.blade.php

### Problem:

Redirect goes to tenant route

### Fix:

Buyer:
route('buyer.biding.auctions.list')

Seller:
route('seller.biding.auctions.list')

---

# 🟠 FIX 4 — Buyer sidenav "My Listings" route

File:
resources/views/layouts/partials/sidenav.blade.php

### Problem:

Buyer "My Listings" → wrong route

### Replace:

route('buyer.agent.auctions')

WITH:

route('buyer.agent.auctions.list')

---

# 🟡 FIX 5 — Seller summary query cleanup

File:
resources/views/hire_seller_agent/view.blade.php

### Replace complex query WITH:

AcceptedBidSummary::where('accepted_bid_id', $bid_id)
->where('agent_user_id', data_get($bid, 'user_id'))
->first();

---

# ✅ AFTER FIXES — VERIFY

Test ALL 4 roles:

## Listing Owner

* View bids
* Accept
* Reject
* Counter
* View summary
* Sign
* Download PDF

## Agent

* View own bids
* Switch filters (Buyer/Seller/Tenant/Landlord)
* No wrong redirects

## Dashboard

* All "My Listings" routes correct
* No cross-role misrouting

---

# 🚫 SAFEGUARDS

* Do NOT rename routes
* Do NOT modify controllers unless required
* Do NOT change working Tenant flow
* Do NOT touch database

---

Return:

1. Files modified
2. Before/after behavior
3. Confirmation all 4 roles now match
