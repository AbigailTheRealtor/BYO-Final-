SCHEMA AUDIT ONLY — ALL 4 HIRE AGENT ROLES

Goal:
Audit schema consistency across all Hire Agent role flows:
- Tenant
- Buyer
- Seller
- Landlord

Important:
Services and Broker Compensation SHOULD vary by:
- user type / role
- property type
- agent-to-client vs agent-to-agent flow

So do NOT force all roles to have identical service or compensation fields.

Audit only. Do not modify files unless I approve a follow-up fix.

SCOPE:
Check all related:
- auction/listing tables
- bid tables
- meta tables
- bid meta tables
- Eloquent models
- saveMeta/getMeta methods
- controllers that save listing data
- controllers that save bids/counters
- accepted bid summary logic
- preview/detail views that read saved fields

ROLES TO AUDIT:
1. tenant_agent_auctions
2. buyer_agent_auctions
3. seller_agent_auctions
4. landlord_agent_auctions

Also audit related bid/meta tables for each role.

WHAT TO VERIFY:

1. Core Listing Tables
For each role, list whether these columns exist:
- id
- user_id
- agent_id if applicable
- property_type
- auction_type
- status
- title
- address
- city
- state
- zip
- created_at
- updated_at

Flag any controller that writes directly to a missing column.

2. Meta Tables
Confirm each role has:
- [role]_agent_auction_metas
- [role]_agent_auction_bid_metas

Confirm each meta table has:
- id
- parent auction/bid foreign key
- meta_key
- meta_value
- timestamps
- cascade delete foreign key

3. Save Logic
Search for all direct assignments like:
$listing->title =
$listing->address =
$listing->broker_compensation =
$bid->some_field =

Flag any assignment where the column does not exist.

If the field is role-specific, confirm it is saved to meta instead.

4. Services Logic
Audit services fields by role/property type.

Confirm:
- services can vary by role
- services can vary by property type
- other_services save properly
- selected services reload properly
- services display properly on:
  - preview
  - detail page
  - bid page
  - counter page
  - accepted summary if applicable

Do NOT recommend making all service lists the same.

5. Broker Compensation Logic
Audit broker compensation fields by role/property type.

Confirm compensation can vary by:
- Tenant
- Buyer
- Seller
- Landlord
- Residential
- Commercial
- Vacant Land
- Income
- Business if applicable

Confirm compensation display is gated correctly where required.

Flag:
- missing saved fields
- fields saved but never displayed
- fields displayed but never saved
- fields duplicated in wrong tabs
- compensation shown to anonymous users when it should be hidden

6. Agent-to-Agent Referral Logic
Audit separately from normal client-created listings.

Confirm referral fee/cooperation terms are only applied to agent-to-agent flows.

Flag if Referral Fee appears in:
- normal Tenant-created listing flows
- normal Buyer-created listing flows
- normal Seller-created listing flows
- normal Landlord-created listing flows

Referral Fee should be separate from Broker Compensation when agent-to-agent.

7. Views / Display Consistency
For each role, compare:
- create form
- edit form
- preview
- public/private detail page
- agent bid form
- counter view
- accepted bid summary

Flag fields that are:
- collected but not displayed
- displayed but not collected
- saved to meta but never read
- read from column but saved to meta
- duplicated

8. Silent Failure Risk
Search for catch blocks that swallow errors.

Flag any:
catch (\Throwable $e)
catch (\Exception $e)

where the error is not logged or shown.

Do not change yet — just report.

DELIVERABLE:
Return a table with:

Role | Table | Missing Column/Table | Code Writes To It? | Saved In Meta Instead? | Risk Level | Recommended Fix

Also include:
- Critical launch blockers
- Medium cleanup items
- False positives / safe differences
- Exact files and line numbers
- Do not modify anything yet

IMPORTANT:
This is an audit only. No migrations, no controller edits, no Blade edits, no schema changes unless I approve the next step.