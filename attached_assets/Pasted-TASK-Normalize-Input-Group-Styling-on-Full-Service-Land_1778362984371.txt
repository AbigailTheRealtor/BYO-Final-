TASK: Normalize $ Input Group Styling on Full Service Landlord Create Offer Listing

URL:
https://3ce01d2c-f79e-44c2-b048-2e8dc64316ef-00-tu7aronbf5wk.spock.replit.dev/offer-listing/landlord

Login:
abigailbaschuk@gmail.com
12345678

SAFEGUARDS:
- AUDIT FIRST before editing anything.
- Full Service Landlord Create Offer Listing ONLY.
- Limited Service is legacy/frozen — DO NOT touch it.
- DO NOT modify Buyer, Seller, Tenant, Hire Agent, Edit, Draft, or Counter flows.
- DO NOT touch validation, Livewire PHP components, controllers, models, routes, migrations, database schema, save/load logic, or formatting helpers.
- Surgical Blade/UI styling fix only.
- Reuse existing Seller/Hire Seller Agent styling patterns instead of inventing new markup/CSS.
- Do NOT globally change all currency fields across the platform.
- Only adjust the specific fields listed below.

ISSUE:
The currency input formatting/layout on the Full Service Landlord Create Offer Listing does NOT visually match the properly formatted dollar input groups used in:
- Hire a Seller’s Agent
- Seller Create Offer Listing

The following Landlord fields need to visually match the Seller-style currency layout:
- Desired Lease Price
- Security Deposit Required
- Total Move-In Funds Required
- Annual Property Taxes
- Annual CDD Fee
- Special Assessment Amount
- Association Fee Amount

REFERENCE:
Use the exact same visual structure/style already used by properly formatted Seller/Hire Seller Agent currency fields:
- Consistent $ prepend width/alignment
- Matching input height
- Matching border radius
- Matching spacing/padding
- Matching vertical alignment
- Matching Bootstrap input-group structure

AUDIT REQUIREMENTS:
1. Inspect the Seller Create Offer Listing fields shown in the attached screenshot.
2. Identify the exact Blade markup structure/classes used for properly formatted currency inputs.
3. Compare against the Landlord versions.
4. Reuse the same structure/classes on the Landlord fields.

IMPORTANT:
- Do NOT create new custom CSS unless absolutely necessary.
- Prefer reusing the existing Seller markup/classes/components.
- Do NOT alter placeholders, labels, wire:model bindings, validation, tooltips, conditional logic, or formatting behavior.
- Do NOT change any comma-formatting logic or JS masking behavior.
- Preserve all existing Livewire bindings exactly.

EXPECTED RESULT:
The listed Landlord currency fields should visually match Seller/Hire Seller Agent currency fields exactly:
- Same $ prepend appearance
- Same input-group alignment
- Same height
- Same spacing
- Same overall styling consistency

VERIFICATION:
After edits:
1. Hard refresh browser.
2. Open Create Offer Listing → Landlord.
3. Verify all listed fields visually match Seller-style currency fields.
4. Verify no regression to other fields/tabs.
5. Confirm only the intended Full Service Landlord Blade file(s) changed.

DELIVERABLE:
Provide:
- Exact files changed
- Exact fields updated
- Confirmation that Limited Service and all other roles/flows were untouched
- Before/after explanation of the markup difference that caused the inconsistency