BUG FIX ONLY — TENANT LISTING AGENT VIEW “VIEW BID” FOOTER / COUNTER STATE PARITY

Read carefully before making any changes.

I need a surgical bug fix only.

Do not redesign anything.
Do not refactor anything globally.
Do not touch database schema, routes, controller behavior, Livewire component logic, listing creation, bid submission logic, counter submission logic, accepted bid logic, or match score logic.

This task is only to fix a view-state inconsistency in the Tenant listing bid view for the agent side.

PROBLEM

On Tenant listings, when an agent clicks View Bid, the bottom state/action area is wrong.

Current wrong behavior

Instead of matching the other listing formats, the footer area is showing:

Accept
Counter
Reject

even when the user has already submitted a counter offer.

Also, in the affected case, the bottom area is effectively missing the expected counter-state presentation and does not show:

the counter notice
the View Counter Terms button

like the Seller flow does.

Expected behavior

Tenant listing bid view should behave like the working Seller format:

If the user has submitted counter terms, then the bottom state area should show:

counter offer notice
View Counter Terms button

and should not show Accept / Counter / Reject in that state.

This must be consistent with the other working listing formats.

GOAL

Make the Tenant listing agent-side View Bid footer state match the working behavior used in the other listing types when a counter exists.

If a user has already submitted counter terms for that bid, the agent should see the same counter-state style:

informational banner/notice
View Counter Terms
no incorrect default Accept / Counter / Reject action row in that countered state
HARD SAFEGUARDS
Do not touch:
listing creation flow
listing edit flow
bid submission flow
counter submission flow
accepted bid summary
notifications
match score calculations
route names
controller logic unless absolutely necessary
database schema
global CSS unrelated to this bug
seller, buyer, or landlord logic unless required as a read-only reference
Do not refactor:
shared bid state systems globally
modal architecture globally
all roles at once
Scope:

Fix Tenant only, unless you discover the same exact bug is caused by a single shared safe conditional that must be corrected in one place. If so, explain that clearly and keep the fix minimal.

REQUIRED APPROACH
Step 1 — inspect working reference first

Before editing anything, inspect the working Seller version that already correctly shows:

counter notice
View Counter Terms
correct state-specific footer

Use Seller as the reference implementation for the state handling and footer behavior.

Step 2 — inspect Tenant version

Compare the Tenant bid view file against the working Seller file and find exactly why the Tenant footer falls through to the wrong action state.

Likely causes to inspect:

wrong conditional order
missing counter-exists variable
incorrect role gate
incorrect per-bid counter detection
wrong footer branch inside modal or private bid section
fallback branch rendering Accept / Counter / Reject too early
Step 3 — minimal fix only

Patch only the state logic necessary so that:

if counter terms exist for that bid, Tenant agent-side view shows the counter-state footer
the default Accept / Counter / Reject footer does not render in that state
IMPORTANT BEHAVIOR RULES
For Tenant listing bid view:
If no counter exists and the bid is actionable

normal action buttons may appear if that is the intended state.

If a counter exists

show the counter-state footer like Seller:

counter notice
View Counter Terms button
no incorrect Accept / Counter / Reject fallback row
FILE SAFETY

Start by checking only the files directly involved in the Tenant bid display and the Seller reference display.

Do not make broad search-and-replace edits.

Do not edit unrelated roles unless absolutely necessary.

If the Tenant view uses a shared partial, edit only that partial if it is truly the source of the bug.

IMPLEMENTATION STYLE
preserve current formatting as much as possible
do not rename existing variables unless required
do not rewrite entire sections
keep diff as small as possible
prefer copying the working Seller conditional structure into Tenant if that is the safest fix
QA REQUIREMENTS

After the fix, verify these states for Tenant listing bid view on the agent side:

No counter exists
normal bid footer behavior still works
Counter exists
counter notice appears
View Counter Terms button appears
Accept / Counter / Reject does not show in that countered state
No regression
tenant bid modal/private section still opens correctly
other normal tenant bid details still render
seller flow remains unchanged
OUTPUT REQUIRED

When finished, return:

exact file(s) changed
exact condition/state bug found
short explanation of why Tenant was falling into the wrong footer branch
confirmation that Seller was used as the reference
confirmation that no listing creation, bidding, counter submission, match score, or accepted summary logic was changed
FINAL RULE

This is a state-parity bug fix only.
Do not make any extra UI changes outside what is needed to restore the correct Tenant counter footer behavior.