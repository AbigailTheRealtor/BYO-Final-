Below is a copy/paste-ready Replit instruction set to fix ONLY the issues you described on Hire a Tenant’s Agent (Residential + Commercial) → Agent Presentation & Marketing Materials:

✅ Error when adding a 2nd Marketing Material

✅ Next button not working only on this tab

✅ Submit triggers same error

✅ After bidding, clicking the same listing shows: “Cannot use object of type stdClass as array” in resources/views/hire_tenant_agent/view.blade.php (around line ~2467)

Replit Instructions — Fix Marketing Materials (2nd Item + Next Button + stdClass Error)
🔒 SAFEGUARDS (MUST FOLLOW)

Do NOT rename or remove any existing fields, tooltips, UI text, tabs, routes, or database columns.

Do NOT change any styling or layout outside this Marketing Materials feature.

Make changes only in the files listed below.

If you need new logic, add it without breaking existing arrays/JSON already saved.

After fixes, test Residential + Commercial bids.

A) FIX THE “stdClass as array” ERROR (Listing view after bid)
Problem

In resources/views/hire_tenant_agent/view.blade.php around line 2467, the code loops marketing materials like:

@foreach (data_get($bid, 'get.promoMaterials') as $index => $material)
    @if (!empty($material['link']))


But $material is sometimes a stdClass object, not an array, so $material['link'] throws:

Cannot use object of type stdClass as array

✅ Fix (Blade must support BOTH array + object safely)

File: resources/views/hire_tenant_agent/view.blade.php
Change ONLY the marketing materials rendering block (the one using get.promoMaterials).

Replace any usage like:

$material['type'], $material['link'], $material['other']
with safe accessors using data_get() and a small normalization.

Implement:

Convert objects to arrays inside the foreach, OR use data_get($material, 'link') everywhere.

Required change example (choose ONE approach):

✅ Approach 1 (recommended): normalize per item

@foreach ((array) data_get($bid, 'get.promoMaterials', []) as $index => $material)
    @php
        // Normalize stdClass → array
        if (is_object($material)) { $material = (array) $material; }

        $type = data_get($material, 'type');
        $other = data_get($material, 'other');
        $link = data_get($material, 'link');
    @endphp

    @if (!empty($type) || !empty($link))
        {{-- render using $type, $other, $link --}}
    @endif
@endforeach


✅ Approach 2: no conversion, only data_get

@foreach (data_get($bid, 'get.promoMaterials', []) as $index => $material)
    @php
        $type = data_get($material, 'type');
        $other = data_get($material, 'other');
        $link = data_get($material, 'link');
    @endphp
@endforeach

Result

Fixes the error when you click the listing after bidding

Fixes submit/view errors caused by inconsistent JSON decoding

B) FIX THE ROOT CAUSE: promoMaterials MUST DECODE AS ARRAY (not stdClass)
Problem

Some part of the system is decoding promo materials JSON like:

json_decode($value) // returns stdClass objects

✅ Fix

Wherever promoMaterials is decoded (commonly in a Model accessor, controller, or helper), force it to decode into an array:

json_decode($value, true)

What to do

Search the codebase for promoMaterials and json_decode.

Update ONLY the promoMaterials decoding (do not change other json_decode calls unless they are for promoMaterials).

Examples of acceptable fixes:

✅ If you have an accessor:
File (example): app/Models/Bid.php (or wherever the accessor lives)

public function getPromoMaterialsAttribute($value)
{
    return $value ? json_decode($value, true) : [];
}


✅ If promo materials are stored in a get JSON blob:
Wherever get.promoMaterials is created, ensure:

It is stored as a plain PHP array

It is retrieved as an array (not stdClass)

✅ If you have Eloquent casts available for the correct column:

protected $casts = [
  'promo_materials' => 'array',
];


(Only do this if the DB column actually exists and is correct—do not guess column names.)

Result

Prevents stdClass from being created in the first place

Stops future bids from breaking the listing view

C) FIX “ADD SECOND MARKETING MATERIAL” + “Next Button doesn’t work” on this tab

This is usually caused by one of these:

The “Add Another Marketing Material” button adds an item with the wrong structure (object/null keys)

Validation blocks “Next” due to dynamic fields but UI doesn’t show the validation message

The Next button is accidentally a submit button or is tied to the wrong step handler for this tab only

File upload binding name is wrong for the second item (example error shown: promoMaterials.files failed to upload)

✅ Required Behavior

When the user clicks + Add Another Marketing Material, create a NEW empty row with a consistent array shape:

[
  'type' => '',
  'link' => '',
  'other' => '',
  'files' => [], // if you support file uploads per item
]

Fix Steps

1) Locate the component that controls this tab
It’s likely one of:

Livewire component handling bid creation

Controller handling bid steps

Blade + JS stepper logic

Search for:

promoMaterials

Add Another Marketing Material

promoMaterials.files

the “Next” button handler for this tab

2) Ensure “Add Another” pushes the correct structure
When adding the new item, it must push the consistent array above (not stdClass, not empty string, not null).

3) Ensure Next works

The Next button on this tab must be type="button" (NOT submit)

It must call the same step-advance method as the other tabs

Example requirement:

<button type="button" ... onclick="goToNextStep()">Next</button>


(or the Livewire equivalent)

4) Ensure validation doesn’t silently block Next
If there are validation rules like promoMaterials.*.type required, then:

Display errors on-screen for this tab

OR make fields optional so Next isn’t blocked

Do NOT add new required rules. Only adjust so it matches the behavior of other tabs.

5) File uploads
If each marketing material row supports its own upload, then the binding must be indexed properly, like:

promoMaterials.0.files

promoMaterials.1.files

If your code uses a single promoMaterials.files, it will break for row 2.

✅ Update upload handling so it supports index-based uploads and stores them under the correct item.

D) TEST PLAN (MUST DO)

Create a Tenant Agent bid (Residential) → go to Agent Presentation & Marketing Materials

Add 2 marketing materials

Click Next ✅

Submit ✅

Repeat for Commercial ✅

Open the listing you bid on (the same URL that previously errored) ✅

Confirm no “stdClass as array” error ✅

What this fixes (mapped to your screenshots)

The stack trace error in hire_tenant_agent/view.blade.php line ~2467 ✅

The submit error ✅

The “click listing after bidding” error ✅

The second marketing material creation breaking the flow ✅

The Next button failing only on this tab ✅