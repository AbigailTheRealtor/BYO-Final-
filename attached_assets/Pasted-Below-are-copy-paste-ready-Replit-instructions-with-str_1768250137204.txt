Below are **copy-paste–ready Replit instructions with strict safeguards**, written in the same style you’ve been using. This only addresses **placing the “Edit Listing” button under the Listing ID** for the specified listing types—nothing else.

---

## 🔧 Replit Instructions — Add **“Edit Listing” Button Below Listing ID**

**Applies to:**

* Hire a **Landlord’s Agent** listings
* Hire a **Seller’s Agent** listings
* Hire a **Buyer’s Agent** listings

**Goal:**
Display the **Edit Listing** button **directly below the Listing ID**, exactly matching the behavior and placement already working correctly for **Hire a Tenant’s Agent** listings.

---

## ✅ Expected Behavior (Reference Standard)

Use **Hire a Tenant’s Agent → Actual Listing Display** as the **single source of truth**.

For **all four listing types**, the top of the listing should display in this order:

1. **Listing Status** (if applicable)
2. **Listing ID**
3. **Edit Listing** button (directly below Listing ID)

Spacing, button style, color, icon, and label must be **identical** across all listing types.

---

## 🧩 What to Change

1. Locate the **listing header / listing meta section** used for:

   * Hire a Landlord’s Agent
   * Hire a Seller’s Agent
   * Hire a Buyer’s Agent

2. Identify where the **Listing ID** is rendered.

3. **Immediately below the Listing ID**, render the same **Edit Listing** button component used in:

   * Hire a Tenant’s Agent listings

4. The button must:

   * Link to the correct **edit route for that listing type**
   * Preserve the correct **listing_id**
   * Open the existing edit flow (no new edit pages)

---

## 🚫 SAFEGUARDS — MUST FOLLOW (CRITICAL)

**DO NOT:**

* ❌ delete, rename, or refactor any database tables, columns, or migrations
* ❌ change any route names, controller logic, or Livewire component names
* ❌ modify form fields, validation rules, required logic, or submission behavior
* ❌ alter Listing ID formatting, prefixes, or visibility
* ❌ change button text, icon, color, size, hover state, or styling
* ❌ move, remove, or restyle the Edit Listing button for Tenant listings
* ❌ consolidate listing types into a shared component unless they already are

**THIS IS A DISPLAY-ONLY CHANGE.**

---

## 🧠 Implementation Guidance (Safe)

* Reuse the **exact same Blade / Livewire partial** (or markup) already used for:

  * Hire a Tenant’s Agent → Edit Listing button
* If conditionals are used, apply them **per listing type** without affecting Tenant logic.
* If the Tenant version uses a shared component, **call that same component** from:

  * Buyer
  * Seller
  * Landlord listing views

---

## 🧪 Testing Checklist (Required)

For **each listing type** (Tenant, Buyer, Seller, Landlord):

* ✅ Listing loads without errors
* ✅ Listing ID displays correctly
* ✅ Edit Listing button appears **directly below Listing ID**
* ✅ Clicking Edit Listing opens the correct edit form
* ✅ No fields are lost, reset, or remapped
* ✅ No console or Livewire errors

---

## 🛑 Scope Reminder

This task **ONLY** adds consistent placement of the **Edit Listing** button.
Any additional UI, routing, or submission issues must be handled in **separate tasks**.

---

If you want, next I can:

* Add a **single shared header component spec** (non-destructive)
* Or write a **QA fail-safe checklist** Replit must confirm before closing tickets
