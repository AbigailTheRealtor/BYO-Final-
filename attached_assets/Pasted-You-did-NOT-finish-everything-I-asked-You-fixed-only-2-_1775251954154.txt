You did NOT finish everything I asked. You fixed only 2 Buyer-related issues so far. I now need you to finish the entire remaining scope completely, not partially.

FIRST, do this exact targeted verification pass before touching unrelated items:

Do not change anything yet outside the Buyer bid private modal footer.

I want a final verification pass only for footer state rendering in the Buyer bid private data modal.

Audit and verify these exact states against the Tenant implementation and make Buyer match Tenant exactly in behavior, wording, visibility rules, and button placement:

1. Actionable bid for listing owner:
- Show Accept Bid button
- Show Reject Bid button
- Buttons must match Tenant styling and placement

2. Non-owner viewing the same bid:
- Do not show Accept/Reject buttons

3. Bidding Period still active:
- Show the same locked message pattern used in Tenant
- Do not show actionable buttons before timer ends

4. Accepted bid:
- Show accepted banner/message
- Show View Accepted Bid Summary link
- Show E-Sign Summary link if Tenant shows it in the same state

5. Rejected bid:
- Show rejected banner/message
- No actionable buttons

6. Expired traditional listing:
- Show expired notice matching Tenant pattern
- No actionable buttons

Safeguards for this Buyer footer verification pass:
- Do not modify score logic
- Do not modify Seller, Tenant, or Landlord files unless you must inspect them for parity
- Do not change routes unless a Buyer route is objectively wrong
- Do not rename variables or database fields
- Keep all changes scoped only to the Buyer bid private modal footer state rendering

After this Buyer footer verification, provide a short report of:
- which states were visually confirmed
- any mismatch found with Tenant
- exact file(s) touched

THEN, after that targeted Buyer footer pass is complete, continue and finish every remaining fix from my original request and audit. I do not want another partial update.

Required process for the remaining items:
1. List all remaining audit items in a numbered checklist.
2. Mark each one as:
   - Fixed
   - Not Fixed
   - Partially Fixed
3. For every item not fully fixed, implement it completely.
4. Verify each fix visually and functionally before moving to the next.
5. Do not stop after one or two items. Continue until all requested items are complete.
6. When done, give me a final completion report with:
   - every item
   - status
   - exact files touched
   - what changed
   - what was visually verified

Global safeguards:
- Do not change labels, tooltips, field wording, or options unless my request specifically requires it
- Do not rename database columns or variables unnecessarily
- Do not alter unrelated roles or flows
- Do not make broad refactors
- Keep changes surgical and scoped
- Use Tenant as the parity reference wherever I asked for matching behavior
- If a file must be inspected for parity, inspect it, but only modify the target role/flow I asked for
- Do not claim completion until every requested item is implemented and verified

Important:
I want full completion of the remaining files and fixes, not another audit summary. Finish the work.