This is a targeted fix for Seller bid uploaded files and Seller digital photo enhancement “Other” rendering.

Do not change listing creation forms.
Do not change database schema unless absolutely required.
Do not rename stored field keys unless absolutely required.
Do not refactor unrelated bid logic or unrelated roles.
Do not change score calculation logic.
Only modify the minimal active Seller bid save/load/render pipeline, and only extend the same fix to other roles if the exact same bug exists there.

---

## PROBLEMS TO FIX

1. Uploaded files are not showing in the bid view
2. Uploaded files are not downloadable in the bid view
3. Pre-saved business card is not carrying over to the bid
4. Pre-saved promotional materials are not carrying over to the bid
5. Manually uploaded business card before bid submit is not showing on the bid
6. Manually uploaded promotional materials before bid submit are not showing on the bid
7. On Seller bids, if user selects “Other” under Provide digital photo enhancements, the custom input box/value is not showing on the bid

---

## GOAL

Fix the full Seller pipeline:

UPLOAD / PRE-SAVED DATA
→ SAVE / REFERENCE
→ BID RECORD
→ LOAD
→ RENDER
→ DOWNLOAD

And also fix Seller rendering for digital photo enhancements “Other” custom value.

---

## HARD SAFEGUARDS

* Do NOT rename or delete DB columns
* Do NOT change listing creation form labels/options
* Do NOT change route structure unless a currently active download route is broken
* Do NOT create a brand new upload architecture
* Do NOT wipe or overwrite existing saved upload metadata
* Do NOT break existing pre-saved profile upload behavior
* Do NOT remove support for manual uploads during bid creation/edit
* Do NOT touch unrelated Buyer/Tenant/Landlord code unless you confirm the exact same bug exists there
* Tenant may be used as a reference only if it already handles a similar case correctly

---

## STEP 1 — AUDIT STORAGE AND LOAD PATH

For Seller bids only, identify the exact storage and load path for:

* business card
* promotional / marketing materials
* digital photo enhancement sub-options
* digital photo enhancement “Other” custom value

Show:

1. exact key names used when saving
2. exact key names used when loading
3. whether the data is stored in:

   * user profile
   * bid meta
   * both
4. whether Seller bid submit/edit correctly copies or references those values

Do not code yet until this is identified.

---

## STEP 2 — FIX FILE PERSISTENCE / REFERENCE

Ensure Seller bids correctly support BOTH:

A. pre-saved uploads

* pre-saved business card
* pre-saved promotional materials

B. manual uploads during bid

* manual business card upload before submit
* manual promotional materials upload before submit

When the bid is saved or updated, the bid view must have access to the correct uploaded file metadata.

Important:

* if the system intentionally references profile uploads instead of copying files into bid meta, that reference must still render correctly on the bid
* if the system copies metadata into bid meta, ensure it actually does so for Seller
* support both create and edit flows

---

## STEP 3 — NORMALIZE FILE DATA

Normalize Seller uploaded file data so the bid view can always render it safely.

Handle:

* array
* stdClass
* single file string
* multiple files array
* nested files arrays

Rules:

* convert to safe arrays for rendering
* remove null/empty values
* preserve file names / labels / paths
* do not silently drop valid saved files

---

## STEP 4 — RENDER + DOWNLOAD IN SELLER BID VIEW

In the active current Seller bid view/private modal:

If business card exists:

* show it visibly
* provide a working download link/button

If promotional materials exist:

* show all uploaded items visibly
* provide working download link/button for each file

Important:

* do not require user to re-upload manually just to see it
* pre-saved files must appear
* manually uploaded files must appear
* support one or many files

---

## STEP 5 — FIX SELLER DIGITAL PHOTO ENHANCEMENTS “OTHER”

For Seller bid Offered Services rendering:

If “Provide digital photo enhancements” is selected:

* show selected sub-items beneath it

If “Other” was selected under those enhancement sub-items:

* show the custom “Other” value/text beneath it too

Important:

* this is specifically for Seller bids
* do not just show the word “Other” with no custom value if custom text exists
* trace the exact stored key for the custom Other value and render it

Expected behavior example:
Provide digital photo enhancements

* Basic edits
* Object removal
* Other: twilight sky enhancement for exterior photo

---

## STEP 6 — VERIFY EDIT FLOW TOO

Confirm the above works for:

* new Seller bid submit
* Seller bid edit/update
* pre-saved profile assets used in a bid
* manually uploaded assets used in a bid

---

## STEP 7 — OPTIONAL CROSS-ROLE CHECK

After Seller is fixed, quickly verify whether Buyer, Tenant, or Landlord have the exact same upload rendering bug.

If yes:

* list it
* do not fix unless it is the exact same minimal pattern and can be patched safely without unrelated changes

---

## REQUIRED OUTPUT

Return:

1. exact Seller files checked
2. exact Seller files changed
3. exact storage keys found for:

* business card
* promotional materials
* digital photo enhancement “Other” value

4. exact root cause(s) found
   For example:

* saved under one key but loaded from another
* profile data not copied/referenced into bid
* stdClass vs array issue
* files array not normalized
* render block missing custom Other value

5. confirmation that all of these now work on Seller bids:

* pre-saved business card shows
* pre-saved business card downloads
* pre-saved promo materials show
* pre-saved promo materials download
* manually uploaded business card shows
* manually uploaded business card downloads
* manually uploaded promo materials show
* manually uploaded promo materials download
* digital photo enhancements sub-items show
* digital photo enhancements Other custom value shows

Do not give a broad summary only.
Give the exact storage path, exact root cause, and exact files changed.
