That response from Replit did **not** address your actual bug.

Your actual bug is:

> **Hire a Landlord listing submitted even though required fields in Property Details were not filled in.**

So the next instruction needs to be very direct: **stop focusing on banner persistence/label polish and fix the core required-field enforcement across every Landlord tab before submit.**

Here’s a clean copy-paste Replit instruction block with strong safeguards:

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A LANDLORD ONLY — REQUIRED FIELDS MUST ALL BE FILLED BEFORE SUBMIT

## SCOPE

The **Hire a Landlord** flow is still allowing submission even when required fields are missing, including required fields in **Property Details**.

This must be fixed so the form behaves like the finalized Tenant flow:

1. the listing **cannot submit** unless **every required field** is filled in
2. the system must check **all required fields across every relevant tab**, not just the current tab
3. if required fields are missing, the form must:

   * block submit
   * show a banner listing **every missing required field**
   * take the user through the missing required fields one by one
4. once all required fields are filled, submit must work normally

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Landlord flow.**
2. **Do not modify Tenant, Buyer, or Seller flows.**
3. **Do not rename database columns, Livewire properties, wire:model bindings, field IDs, field names, or tab IDs.**
4. **Do not run migrations.**
5. **Do not change Select2 formatting, layout, placeholders, width, or styling.**
6. **Do not refactor global/shared logic unless absolutely necessary, and if touched, wrap it so only Landlord behavior changes.**
7. **Do not weaken or remove any working Tenant validation/correction logic.**
8. **Do not focus on label wording or banner cosmetics until the actual submit blocking is correct.**
9. **Do not assume a field is filled because a draft/default value exists unless the actual Landlord requirement says that value satisfies the field.**
10. **Keep this surgical and limited to Landlord required-field enforcement.**

---

## ACTUAL BUG TO FIX

A **Landlord listing submitted even though required fields in Property Details were not filled in**.

That means the current Landlord required-field enforcement is still incomplete.

This task is **not done** until:

* every required Landlord field is truly enforced before submit
* required fields on Property Details cannot be skipped
* required fields on inactive tabs cannot be skipped
* submit is blocked until the full required set is complete

---

## STEP 1 — TRACE THE REAL LANDLORD SUBMIT PATH

Before editing, trace the exact submit chain for **Hire a Landlord**.

I need the exact answer to these:

* what route is active
* what Livewire component handles the Landlord create flow
* what blade file is used
* what partial renders **Property Details**
* what JS submit handler is active for Landlord
* what method ultimately handles submit (`store()` or equivalent)
* what Landlord-specific required-field collector is currently being used
* whether the current logic is mistakenly reusing Tenant assumptions

Do not assume. Confirm the real active files and the real active code path.

---

## STEP 2 — BUILD THE FULL LANDLORD REQUIRED-FIELD MATRIX

Before applying the fix, build a complete matrix of **all required fields** for **Hire a Landlord**, including:

* Listing Details
* Property Details
* Leasing Terms
* Broker Compensation / later tabs if required
* Landlord Information
* all Residential-only fields
* all Commercial-only fields
* all conditional fields

For each field, document:

* tab
* visible label
* property name / wire:model
* always required / conditionally required / optional
* whether current JS checks it
* whether current PHP validation checks it
* whether current banner includes it
* whether it can currently be skipped

Format:

| Tab | Field Label | Property | Required Type | JS Checked | PHP Checked | Banner Included | Can Skip? |

This is required before fixing.

---

## STEP 3 — FIX THE CORE PROBLEM: LANDLORD MUST CHECK EVERY REQUIRED FIELD ACROSS ALL TABS

For **Landlord only**, make the submit validation go through **every currently relevant required field**, including fields on tabs that are not currently visible.

The validation must not skip a field just because:

* it is on another tab
* the tab is inactive
* it is conditionally rendered but currently relevant
* it uses Select2/custom UI
* it is in Property Details rather than the last tab

Important:

* fields hidden because they are truly irrelevant should not block submit
* fields hidden only because the tab is inactive **must still be checked**

---

## STEP 4 — SPECIFICALLY FIX PROPERTY DETAILS REQUIRED FIELDS

The reported bug is that required fields in **Property Details** were left blank and the listing still submitted.

That means Property Details must be specifically audited and corrected.

For **every required field in Property Details**:

* confirm exact field key
* confirm it is included in the Landlord required collector
* confirm it is included in PHP validation
* confirm it appears in the missing-fields banner when blank
* confirm it blocks submit when blank

This is the heart of the fix.

---

## STEP 5 — ALIGN ALL 3 LAYERS FOR LANDLORD

For every required Landlord field, align:

1. **UI required state**
2. **front-end submit blocking**
3. **server-side validation**

No field should be:

* marked required but not enforced
* enforced in PHP but skipped in JS
* skipped in banner but still blocking submit
* allowed through because it is on a non-active tab

The Landlord flow must behave as one consistent required-field system.

---

## STEP 6 — DO NOT LET PREFILLED DEFAULTS MASK MISSING REQUIRED FIELDS

Be careful here.

If user profile data, mount defaults, or prior draft values prefill some fields, that is fine **only if the field truly has a valid value**.

Do not treat a field as valid just because:

* it has a placeholder
* it had a stale old value
* Livewire still has an old value not reflected in DOM
* a default string exists that does not satisfy the Landlord workflow

Use the same anti-stale-value logic that was needed in Tenant.

---

## STEP 7 — KEEP THE LANDLORD BANNER, BUT PRIORITIZE REAL ENFORCEMENT

Once the required-field enforcement is corrected, then make sure the banner:

* stays visible while correction mode is active
* lists every missing required Landlord field
* uses the correct Landlord labels
* updates as fields are filled

But the priority is not banner persistence alone. The priority is:

> **The Landlord listing must not submit until all required fields are complete.**

---

## STEP 8 — GUIDED CORRECTION MUST FOLLOW THE FULL LANDLORD REQUIRED SET

Once submit is blocked, the Landlord flow should:

* go to the first missing required field
* after user fills it, go to the next missing required field
* continue until all missing required fields are completed
* then allow normal submit

This must include Property Details fields and not skip them.

---

## STEP 9 — TEST BOTH RESIDENTIAL AND COMMERCIAL LANDLORD

Do not assume one branch means both branches work.

After the fix, test both separately.

### Residential Landlord

* leave Property Details required fields blank
* click submit
* expected: blocked, banner lists them, guided correction starts

### Commercial Landlord

* leave Property Details required fields blank
* click submit
* expected: blocked, banner lists them, guided correction starts

### Final

* after all required fields are filled
* expected: submit works normally

---

## STEP 10 — RETURN A FINAL REPORT

Return:

1. exact files changed
2. exact reason Landlord Property Details fields were being skipped
3. exact fix applied
4. full Landlord required-field matrix
5. proof Residential Landlord now blocks incomplete submit
6. proof Commercial Landlord now blocks incomplete submit
7. proof final valid submit still works
8. confirmation Tenant/Buyer/Seller were untouched

---

## IMPORTANT

Do not stop at:

* “banner now persists”
* “label now says Property Type”
* “all-done flow works”

Those are secondary.

The actual success condition is:

> **A Hire a Landlord listing cannot submit unless every required field across every relevant tab is filled in.**

---

If you want, I can also make this into a **shorter hard-stop version** for Replit that is even more blunt.
