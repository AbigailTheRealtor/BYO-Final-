TASK: Fix Agent Offer Preset counter/accept flow contact info placement

SCOPE:
Agent Offer Presets / Hire This Agent direct flow only.

Likely areas:
- Agent offer preset counter terms views/components
- HireAgentDirectController
- resources/views/hire-agent-direct/preview.blade.php
- Any role-specific counter blades/components for Seller, Landlord, Buyer, Tenant

DO NOT TOUCH:
- Offer Listing create/edit draft wizards
- BidYourOffer property listing forms
- Existing bid/counter logic unrelated to agent presets
- Database schema unless absolutely required
- Accepted Bid Summary generation except routing/contact display needs
- Services, compensation, or agreement term wording
- Existing saved preset data

GOAL 1 — Move Client Contact Information into its own tab:
Currently, when a user clicks “Request Changes / Counter Terms,” the “Client Contact Information” section appears inside the “Additional Details” tab.

Change this so all 4 user types and all property types have a separate tab:

Tab order should be:
1. Broker Compensation and Agency Agreement
2. Additional Details
3. Services the [Client Role] Requests from Their Agent
4. Client Contact Information

Apply to:
- Seller
- Landlord
- Buyer
- Tenant
- All property types under each role

Important:
- Do not rename existing fields.
- Do not change validation rules unless needed to keep current behavior working.
- Only move the UI section into its own tab.
- Existing input names, Livewire properties, request keys, and save behavior must remain unchanged.

GOAL 2 — Remove Property Address from agent offer presentation:
On the Agent’s offer presentation / Hire Agent preview page, remove this section entirely:

“PROPERTY ADDRESS”
“Enter the address of the property this hire request relates to.”
Address input placeholder:
“e.g. 123 Main St, Miami, FL 33101”

This should not appear on an agent bid/preset presentation.

Important:
- Remove from the presentation/preview only.
- Do not remove property address fields from listing creation/edit forms.
- Do not remove any database fields.

GOAL 3 — Accept & Submit Hire Request should route to acknowledgment/contact summary:
When user clicks:
“Accept & Submit Hire Request”

The flow should NOT ask for property address.

Instead, route the user to the acknowledgment summary step/page where the user can review the accepted terms and enter Client Contact Information.

The Client Contact Information fields should be the same fields currently used in the counter flow Client Contact Information tab, depending on role/property type.

Requirements:
- Keep Accept & Submit Hire Request as the acceptance action.
- After click, show acknowledgment/summary before finalizing.
- User must be able to enter/confirm Client Contact Information there.
- Do not finalize the agreement until the acknowledgment step is completed.
- Preserve existing e-sign / acknowledgment logic.
- Preserve existing accepted bid summary logic.
- Do not create duplicate accepted summaries.

SAFE IMPLEMENTATION PLAN:
1. Read-only audit first:
   - Find where “Client Contact Information” is rendered in the counter terms flow.
   - Find where the counter terms tabs are defined.
   - Find where “PROPERTY ADDRESS” is rendered in the hire preview/presentation.
   - Find where “Accept & Submit Hire Request” posts/routes.

2. UI move:
   - Cut/move only the Client Contact Information block from Additional Details into a new tab panel.
   - Add a new tab button labeled “Client Contact Information.”
   - Update tab indexes carefully so Next/Previous still works.
   - If JavaScript or Livewire active tab tracking exists, update indexes there too.

3. Remove property address:
   - Remove/hide only the property address card from the Agent offer presentation.
   - Confirm no Offer Listing create/edit address fields are affected.

4. Accept flow:
   - Reuse existing contact info partial/fields if possible.
   - Route Accept & Submit Hire Request to acknowledgment/summary step.
   - Add contact info fields there if missing.
   - On acknowledgment submit, save contact info using the same keys currently used by counter flow.
   - Then continue existing acceptance/finalization process.

5. Regression checks:
   - Seller: Request Changes / Counter Terms shows Client Contact Information as its own tab after Services.
   - Landlord: same.
   - Buyer: same.
   - Tenant: same.
   - Full-service and limited-service paths still work.
   - Next/Previous buttons still navigate correctly.
   - Additional Details tab no longer contains Client Contact Information.
   - Agent offer presentation no longer shows Property Address.
   - Accept & Submit Hire Request opens acknowledgment/contact summary, not property address.
   - Final accepted summary still saves once only.
   - No Offer Listing create/edit wizard tabs are changed.

STOP CONDITIONS:
- Stop and report back if moving the tab requires database changes.
- Stop and report back if the accept flow does not currently have a reusable acknowledgment page.
- Stop and report back before changing any core bid/counter/accepted-summary logic.