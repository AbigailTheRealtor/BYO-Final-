SURGICAL FIX — FILE BY FILE (NO GLOBAL SEARCH)

Do NOT scan entire project.
Do NOT apply patterns.

Open ONLY these files and fix EXACT sections.

--------------------------------------------------
FILE 1:
offer-landlord-tabs/commission-based/tax-legal-hoa-disclosures.blade.php
--------------------------------------------------

Find THIS exact block:

<input type="text" wire:model="tax_year" class="form-control">

DELETE that line completely.

KEEP ONLY:

<div class="input-cover">
    <input type="text" wire:model="tax_year" class="form-control has-icon">
</div>

--------------------------------------------------

Find:

<input type="number" wire:model="total_parcel_count" class="form-control">

DELETE it.

KEEP ONLY the .input-cover version.

--------------------------------------------------

Find:

<input type="number" wire:model="max_leases_per_year" class="form-control">

DELETE it.

KEEP ONLY the .input-cover version.

--------------------------------------------------
FILE 2:
offer-seller-tabs/commission-based/tax-legal-hoa-disclosures.blade.php
--------------------------------------------------

Repeat SAME deletions for:
- tax_year
- total_parcel_count
- max_leases_per_year

--------------------------------------------------
FILE 3:
offer-seller-tabs/commission-based/financial-details.blade.php
--------------------------------------------------

Find ENTIRE block:

id="other_reason_for_sale_wrapper"

DELETE EVERYTHING inside it.

Replace with ONLY:

@if ($reason_for_sale === 'Other')
    <div class="form-group mt-2">
        <div class="input-cover">
            <input type="text"
                   wire:model.defer="other_reason_for_sale"
                   class="form-control has-icon"
                   data-icon="fa-solid fa-tag"
                   placeholder="Enter Reason for Sale (e.g., Relocation)">
        </div>
    </div>
@endif

--------------------------------------------------
FILE 4:
ALL upload sections (seller + landlord)
--------------------------------------------------

Find:

<div class="form-group mt-2">
    <div class="conditional-upload-block">

DELETE ONLY:

<div class="form-group mt-2">

Leave:

<div class="conditional-upload-block">

--------------------------------------------------
RULES
--------------------------------------------------

- DO NOT search globally
- DO NOT apply patterns
- DO NOT add code
- ONLY delete exact lines and replace exact blocks

--------------------------------------------------
FINAL CHECK
--------------------------------------------------

Search:

wire:model="tax_year"

Must return ONE result only per file.

Repeat for:
- total_parcel_count
- max_leases_per_year