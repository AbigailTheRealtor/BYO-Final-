Fix Accepted Bid Summary document visibility on the AGENT side.

Current issue:
- Client uploads documents on the Accepted Bid Summary page
- Upload appears to save successfully
- But the matched hired agent cannot see those uploaded documents on their side

This task is to fix document persistence + retrieval + rendering only.

CRITICAL SAFEGUARD:
- Do not modify listing creation flows
- Do not modify bid submission, counter, accept/reject, or match score logic
- Do not redesign the Accepted Bid Summary flow
- Do not change database schema unless absolutely necessary
- Reuse the existing Accepted Bid Summary relationship if possible
- Preserve privacy: documents must only be visible to the listing owner and the matched hired agent tied to that accepted summary

==================================================
PART 1 — AUDIT THE CURRENT DOCUMENT SAVE FLOW
==================================================

Before changing anything, inspect and report:

1. Which route/controller or Livewire component handles Accepted Bid Summary document uploads
2. Which files are uploaded:
   - id_document
   - proof_of_income
   - proof_of_funds
   - pre_approval_letter
   - property_record_link if applicable
3. Exactly where each uploaded file is stored:
   - disk
   - folder path
4. Exactly where the uploaded file metadata is stored:
   - table name
   - columns
   - accepted_bid_summary_id linkage
5. Whether the upload currently:
   - saves file to disk only
   - saves DB record only
   - saves both correctly

Return the exact file(s), method(s), and model(s) involved.

==================================================
PART 2 — FIX SAVE LOGIC IF NEEDED
==================================================

If uploads are not creating a durable record linked to the Accepted Bid Summary, fix that.

Required behavior for each successful upload:
1. Save file to correct storage disk/path
2. Save a DB record or metadata entry linked to the correct accepted_bid_summary_id
3. Store enough info to render later:
   - accepted_bid_summary_id
   - document type/category
   - stored file path
   - original filename if available
   - uploaded_by user ID if available

If a document record model/table already exists, reuse it.
Do not create a parallel document system.

==================================================
PART 3 — FIX AGENT-SIDE RETRIEVAL
==================================================

Find the AGENT-side Accepted Bid Summary page/view/component.

Required behavior:
1. Query uploaded document records for the correct accepted_bid_summary_id
2. Ensure the query is using the same summary record the client uploaded against
3. Pass those document records into the agent-side view
4. Render all available uploaded documents on the agent side

Display for each document:
- document type label
- file name if available
- View / Download button

If documents are currently missing on the agent side because the view is not querying them, fix that query.
If documents are missing because the wrong accepted_bid_summary_id is used, fix the relationship/query.

==================================================
PART 4 — PRIVACY / PERMISSIONS
==================================================

Ensure uploaded documents are visible only to:
- the listing owner / client tied to the Accepted Bid Summary
- the matched hired agent tied to that Accepted Bid Summary
- admins if already supported

Documents must NOT be visible to:
- other agents
- unrelated users
- public visitors
- other listings/summaries

If the agent-side query is too broad or too narrow, fix it.

==================================================
PART 5 — STORAGE ACCESS
==================================================

Confirm files are accessible after upload.

If files are stored on the public disk, ensure storage linking is correct:
- storage/app/public/...
- public/storage symlink available

If needed, run or account for:
php artisan storage:link

Use the correct public URL or download route for document access.

==================================================
PART 6 — UI RENDERING
==================================================

On the AGENT side, add or fix a clear “Shared Documents” / similar section in the Accepted Bid Summary view.

Requirements:
1. Show uploaded documents only when they exist
2. If none exist, show a clean empty state
3. For each document, show:
   - document type
   - file name
   - View / Download button
4. Do not show broken links
5. Do not show the wrong user’s documents

If the CLIENT side already renders documents correctly, mirror that logic on the AGENT side using the same data source.

==================================================
PART 7 — VALIDATION / ERROR HANDLING
==================================================

Keep the recent upload UX improvements:
- max 20MB
- PDF/JPG/PNG
- disabled save button until valid input
- friendly error messages

Do not remove those.

==================================================
PART 8 — TEST CASES
==================================================

After fixing, test all of these:

1. Upload a valid JPG under limit on client side
   - save succeeds
   - file visible on client side
   - same file visible on correct agent side

2. Upload a valid PDF under limit on client side
   - save succeeds
   - file visible on client side
   - same file visible on correct agent side

3. Refresh both pages
   - file still visible

4. Confirm unrelated agent cannot see the file

5. Confirm file links open/download correctly

==================================================
OUTPUT REQUIRED
==================================================

Return:
1. exact files changed
2. where uploaded files are stored
3. which DB table/model/relationship is used for document records
4. what was wrong on the agent side
5. how the agent-side retrieval was fixed
6. confirmation that:
   - uploads save correctly
   - documents appear on both client and hired agent side
   - unrelated users cannot access them