Paste this into Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT’S AGENT ONLY — SURGICAL FIX PASS

### SCOPE

Fix **only** the following remaining issues in **Hire a Tenant’s Agent**.
Keep this surgical and limited. Do **not** touch Buyer, Seller, or Landlord flows. Do **not** do a broad refactor.

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Tenant’s Agent flow.**
2. **Do not rename, delete, or migrate database columns.**
3. **Do not modify unrelated files, unrelated components, or shared logic unless absolutely required for these exact bugs.**
4. **Do not change labels, tooltips, option text, field order, or validation copy unless explicitly requested below.**
5. **Do not break Leasing Space or Non-Negotiable Amenities and Property Features, since those are now the working reference behaviors.**
6. **Do not disable the Submit button entirely due to missing fields.**
7. **Do not remove existing validation.**
8. **Do not make sitewide Select2 or Livewire changes.**
9. **Keep edits minimal, targeted, and performance-conscious.**
10. **Stop if a fix would require touching multiple unrelated flows or shared cross-role code.**

---

## GOAL

Fix these four issues only:

1. **Acceptable Property Styles selection does not show on the listing right away and feels delayed**
2. **Submit button is not working correctly because of `Missing required fields: Tab 0: lease_for`**
3. **When user clicks `Other` under Offered Lease Term, the dependent input box does not appear**
4. **Under Property Preferences tab, other selections still cause a screen flash even though Non-Negotiable Amenities and Property Features now behaves correctly**

---

# 1) ACCEPTABLE PROPERTY STYLES — LISTING SHOULD UPDATE IMMEDIATELY

### Current problem

When the user clicks **Acceptable Property Styles**, the selection does not show on the listing right away. It appears delayed.

### Required fix

Make **Acceptable Property Styles** update immediately in the Hire a Tenant’s Agent flow so that:

* selection is saved to the correct Livewire property immediately
* listing preview/output reflects the current selection without delayed update
* there is no lag caused by stale bridge data, delayed sync, or waiting for a later render cycle
* selected values still persist correctly on Save Draft and Edit

### Safeguard

Do not rebuild the entire listing rendering system.
Only fix the sync path for **Acceptable Property Styles** so the displayed listing data reflects the selected value immediately.

### Implementation direction

Check whether:

* JS is updating a hidden field but not the real Livewire property
* the listing view is reading a different property than the one being updated
* the property requires immediate sync rather than delayed/deferred sync

Use the same immediate behavior pattern as the fields that already update correctly.

---

# 2) SUBMIT BUTTON MUST ALWAYS BE CLICKABLE

### Current problem

The Submit button is blocked by:

* `Missing required fields:`
* `Tab 0: lease_for`

This is bad UX.

### Required fix

The **Submit button should always be clickable**.

When the user clicks Submit:

* if required fields are missing, do **not** block the click itself
* instead, run validation
* show the required fields / missing fields notice
* keep the user on the form
* once the user fills in the missing required field(s), Submit should work normally

### Exact behavior wanted

* button remains enabled/clickable
* clicking it triggers validation
* validation shows missing required fields
* after missing fields are filled, clicking Submit submits successfully

### Safeguard

Do not remove required validation.
Do not make submit bypass validation.
Only remove the logic that makes the button effectively unusable because of pre-check failure.

### Required specific fix

Remove or adjust any logic that pre-blocks submission due to:

* `Missing required fields: Tab 0: lease_for`

Validation should happen **after click**, not by disabling submit functionality ahead of time.

---

# 3) OFFERED LEASE TERM — “OTHER” MUST SHOW DEPENDENT INPUT

### Current problem

When the user clicks **Other** under **Offered Lease Term**, the custom box that should appear does not appear.

### Required fix

When **Other** is selected for **Offered Lease Term**:

* the dependent custom input box must appear immediately
* it must stay visible while Other remains selected
* the entered custom value must save correctly
* the value must auto-populate correctly on Edit later

### Safeguard

Do not rely on a broken front-end-only toggle.
Make sure the visibility is tied to the same property being saved.

### Implementation direction

Confirm:

* the selected value for Offered Lease Term is being written to the actual Livewire property
* the Blade conditional for showing the “Other” input is checking that same property
* no re-render/reset is immediately hiding the field again

---

# 4) REMOVE SCREEN FLASH ON OTHER PROPERTY PREFERENCES FIELDS

### Current problem

Under the **Property Preferences** tab, other selectable fields still cause a visible screen flash even though **Non-Negotiable Amenities and Property Features** now behaves correctly.

### Required fix

Apply the same stable pattern used by **Non-Negotiable Amenities and Property Features** to the other affected Property Preferences fields so there is:

* no visible flash on selection
* no unnecessary re-render flicker
* no temporary placeholder reset
* no jumpy UI when selecting options

### Important

This is a **UI stability fix**, not a new behavior change.

### Safeguard

Do not rewrite the entire Property Preferences tab.
Only apply the same stable init/sync pattern already working for **Non-Negotiable Amenities and Property Features** to the remaining fields that still flash.

### Implementation direction

Check for:

* unnecessary destroy/re-init on each selection
* duplicate event listeners
* Livewire re-render hooks firing too aggressively
* placeholder/default reset during selection
* hidden-field sync happening after DOM repaint

Use the exact minimal pattern from the working field.

---

## ROOT-CAUSE CHECKS BEFORE EDITING

Before making changes, verify these exact causes:

1. **Acceptable Property Styles**

   * Is JS updating the wrong property?
   * Is listing output reading stale/deferred data?
   * Is there delayed sync instead of immediate sync?

2. **Submit button**

   * Is there a client-side pre-check blocking click?
   * Is submit being disabled because of a missing-fields gate before validation runs?

3. **Offered Lease Term = Other**

   * Is the conditional box tied to the wrong property?
   * Is the selected value not persisting long enough to trigger the conditional?
   * Is there a re-render hiding it?

4. **Property Preferences flash**

   * Are the remaining fields still using the old unstable init pattern?
   * Are they reinitializing or repainting unnecessarily on each selection?

Do not guess. Confirm the exact cause, then patch only that cause.

---

## DO NOT CHANGE

Do **not** change any of the following unless absolutely necessary for these exact fixes:

* database schema
* cross-role shared components
* Buyer/Seller/Landlord flows
* labels/tooltips/placeholders
* required field lists except for submit-click behavior
* working Non-Negotiable Amenities behavior
* working Leasing Space behavior

---

## REQUIRED TESTS BEFORE MARKING COMPLETE

### A. Acceptable Property Styles

1. Select one or more styles
2. Confirm selection appears on listing/preview immediately
3. Save Draft
4. Reopen
5. Confirm values remain
6. Edit listing
7. Confirm values still load correctly

### B. Submit button

1. Leave a required field blank
2. Click Submit
3. Confirm button is clickable
4. Confirm validation message appears
5. Fill the missing required field
6. Click Submit again
7. Confirm successful submission

### C. Offered Lease Term = Other

1. Select Other
2. Confirm custom input appears immediately
3. Enter custom value
4. Save Draft or Submit
5. Reopen/Edit
6. Confirm value persists and field shows correctly

### D. Property Preferences flash

1. Go through the Property Preferences tab
2. Select the fields that were still flashing
3. Confirm there is no visible screen flash/jump/reset
4. Confirm selections remain stable

---

## FINAL EXPECTATION

After this fix pass:

* **Acceptable Property Styles** updates on the listing right away
* **Submit button** is always clickable and uses validation correctly after click
* **Offered Lease Term = Other** shows its dependent box immediately
* remaining **Property Preferences** selection fields no longer flash on screen
* no unrelated files or flows are changed

---

## IMPORTANT DELIVERY RULE

Keep this to a **minimal-file, minimal-scope patch**.
Do not spend time tracing the whole architecture.
Fix only the exact broken behaviors above and stop.
