Implement fixes from the completed Hire Landlord Agent audit only.

Scope:
ONLY touch Hire Landlord Agent Full Service files.

Fix exactly these audit-confirmed bugs:

1. Remove duplicate message.processed Select2 handler stacking for:
- Appliances Included
- Tenant Pays
- Terms of Lease

2. Fix View Preference syncing:
- Replace broken Livewire.emit() behavior or add the correct listener.
- Preferred: sync directly with @this.set('view_preference', value) consistent with other Select2 fields.

3. Fix Desired Lease Term draft hydration:
- Ensure the JS hydration target matches the actual element/wire:model/property.

4. Fix Lease Renewal/Extension Fee:
- Prevent type dropdown from rerendering before related deferred sub-input values commit.
- Preserve selected option and related input values through rerenders, draft save, and reload.

5. Fix Lease-Option numeric fields clearing:
- Compensation for Creating the Lease-Option Agreement
- Compensation if Purchase Option is Exercised
Remove/replace the wire:change flow that fires before wire:model.defer syncs.

6. Fix Submit required-field false flashing:
- Do not treat Select2’s hidden aria-hidden element as the required field.
- Validate against the real Livewire property/value instead.
- Remove or correct the 300ms banner flicker/reset behavior.

7. Fix Save Draft:
- Ensure syncLandlordSelect2BeforeSave() is called before draft save.
- Confirm view_preference saves correctly.

Do not touch:
- Seller, Buyer, Tenant
- Create Offer Listing
- Edit Offer Listing
- Limited Service
- Routes
- Migrations
- Database schema
- Unrelated validation

Acceptance tests:
- All target Select2 fields show placeholder exactly “Select”
- View Preference selection persists
- Desired Lease Term restores from draft
- Lease Renewal/Extension Fee selection works and persists
- Lease-option numeric inputs do not clear while typing
- Submit no longer falsely flashes Offered Lease Term / Leasing Space
- Save Draft saves Select2 values, especially view_preference
- Reloaded draft restores all fixed fields

Deliverable:
List files changed, exact fixes made, and test results.