eplit Instructions — Fix Accept / Counter / Reject Buttons + Errors (Private “Terms” Modal)

Applies to: Hire a Tenant’s Agent → Actual Bids Display → Private “Terms” modal/panel (Residential + Commercial)

⚠️ SAFEGUARDS (Must Follow)

Do NOT delete or rename any existing fields, routes, Livewire components, blade files, models, controllers, or database tables.

Only add: (a) null-safe guards in blade, and (b) missing DB columns via new migration(s).

Do reflect current logic (Accepted/Rejected/Countered) but don’t restructure the flow.

After edits: run migrations + clear caches (config/view/route) before testing.

A) Fix Button Styling (Always Visible Colors + Correct Sizes)
1) Buttons must match this style (always colored, not hover-only)

Inside the Private “Terms” modal, update the button markup/classes so they render like this at all times:

Accept Bid: green background + white text (always)

Counter Bid: blue background + white text, same size as Reject

Reject Bid: red background + white text

✅ Implementation (Blade):
Find the blade file that renders the modal footer buttons (where you see Accept/Counter/Reject). Update classes like this:

Accept:

btn btn-success text-white

Counter:

btn btn-primary text-white

Reject:

btn btn-danger text-white

✅ Critical: remove any “outline” classes or hover-only styles like:

btn-outline-success, btn-outline-*, or custom CSS that sets background to transparent until hover.

✅ Sizing:
Make Counter and Reject the same size by including consistent padding and min-width, e.g.:

add px-4 py-3 (or your system’s equivalent)

add style="min-width: 180px;" to Counter and Reject

Accept can match too if you want the set perfectly uniform.

Acceptance:

Accept is green even when not hovering

Counter is blue, same size as Reject

Reject is red (not blue)

B) Fix “Counter Bid” Error: Undefined variable $broker_fee_timing

You got:

Undefined variable $broker_fee_timing
File: resources/views/livewire/tenant-agent-auction-bid-counter-tabs/broker-compensation.blade.php

This means the blade is referencing $broker_fee_timing but the view is not always receiving it.

2) Make $broker_fee_timing null-safe in the blade (no crash)

In:
resources/views/livewire/tenant-agent-auction-bid-counter-tabs/broker-compensation.blade.php

Do both of these:

(1) Any direct output must use null coalescing:
Replace:

{{ $broker_fee_timing }}


With:

{{ $broker_fee_timing ?? '' }}


(2) Any if/elseif that compares it must also be null-safe:
Replace patterns like:

<?php if($broker_fee_timing == 'Deducted from Rent Collected'): ?>


With:

<?php if(($broker_fee_timing ?? null) == 'Deducted from Rent Collected'): ?>


…and do the same for every elseif branch.

✅ This prevents the “Counter Bid” modal from crashing when that variable isn’t set yet.

3) Also ensure the Livewire component provides it (best practice)

In the Livewire component that renders those counter tabs (the class referenced in the stacktrace), make sure the property exists and is set to a default. Example:

Add a public property:

public $broker_fee_timing = null;


When loading existing terms, set it if available, otherwise keep null:

$this->broker_fee_timing = $terms->broker_fee_timing ?? null;


Acceptance:

Clicking Counter Bid no longer throws the $broker_fee_timing undefined error.

C) Fix Accept/Reject Errors: DB column accepted_date does not exist

You got:

SQLSTATE[42703] Undefined column: column "accepted_date" of relation "tenant_agent_auction_bids" does not exist

That means your controller is trying to update accepted_date, but the DB table doesn’t have that column.

4) Add the missing column(s) via migration (recommended safest fix)

Create a new migration that adds columns without modifying existing ones.

Migration name (example):
add_status_dates_to_tenant_agent_auction_bids_table

Add at least:

accepted_date (nullable timestamp)

Optionally (if the code also sets these elsewhere or you plan to):

rejected_date (nullable timestamp)

countered_date (nullable timestamp)

✅ Do NOT remove existing accepted / status fields. Only add columns.

Acceptance:

Clicking Accept Bid updates without DB error.

Clicking Reject Bid updates without DB error.

5) Confirm controller update matches the column names

In:
app/Http/Controllers/TenantAgentAuctionBidController.php (or the controller in your stack trace)

Find the accept/reject actions and confirm they’re setting the right columns.

Example expected logic:

Accept:

set status field to accepted (or whatever your system uses)

set accepted_date = now()

Reject:

set status field to rejected

set rejected_date = now() ONLY if you created this column

If your current code sets accepted_date on reject by mistake, correct it.

✅ Important: Do not change business logic beyond fixing the wrong column name usage.

D) Clear caches + test checklist (prevents “took effect for 1 second then white screen” issues)

After implementing the above, run:

php artisan view:clear

php artisan route:clear

php artisan config:clear

php artisan cache:clear

run migrations

Then test in this exact order:

Open listing → open bid → open Private Terms modal

Verify buttons display correctly (no hover-only)

Click Counter Bid (must not crash)

Click Accept Bid (must not DB error)

Click Reject Bid (must not DB error)

Final Acceptance Checklist

✅ Accept button is solid green with white text at all times
✅ Counter button is solid blue with white text and same size as Reject
✅ Reject button is solid red with white text
✅ Counter Bid no longer errors on $broker_fee_timing
✅ Accept/Reject no longer error on missing accepted_date column
✅ Changes are addition-only + null-safe guards (no destructive edits)