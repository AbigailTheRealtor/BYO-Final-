There is still a specific bid rendering bug that needs to be fixed.

ISSUE:
On the agent bid, under the service:
"Provide digital photo enhancements"

if the user selected:
"Other"

the bid is currently showing only:
"Other"

instead of showing the actual custom input the user entered.

REFERENCE:
Check the Landlord private Compensation & Agency Agreement Terms / bid view behavior.
For Landlord, when the user selects "Other" under "Provide digital photo enhancements," it correctly shows the actual custom input instead of only the word "Other."

I need Buyer / Seller / any affected role using this same service pattern to behave like the working Landlord implementation.

REQUIRED FIX:
When "Other" is selected under "Provide digital photo enhancements":
- do not display only the static label "Other"
- display the actual custom text entered by the user
- if both the checkbox label and custom text are needed for consistency, format it the same way Landlord does
- use the exact same rendering logic/pattern as the working Landlord bid view

WHAT TO CHECK:
1. Find where the bid card/private bid view renders the sub-options for:
   "Provide digital photo enhancements"
2. Find the field/meta key that stores the custom "Other" text
3. Compare the Landlord implementation vs the affected role implementation
4. Apply the Landlord formula to the affected role so the custom input renders correctly

SAFEGUARDS:
- Do not change unrelated services
- Do not change scoring logic unless required for this exact bug
- Do not rename database fields
- Do not refactor globally unless this sub-option rendering is shared in a safe reusable partial
- Keep the change surgical
- Use Landlord as the working source of truth

VERIFY:
After fixing, verify on a real bid that:
- standard sub-options still display normally
- if "Other" is selected with custom text, the bid displays the custom text entered
- it no longer shows only the generic word "Other"

FINAL REPORT:
When done, report:
1. exact file(s) changed
2. what was wrong
3. what logic was copied/matched from Landlord
4. what was visually verified