Add These Edits (Hire a Seller’s Agent → Broker Compensation & Agency Agreement Terms)
1) Label Fix (shown in your screenshot)

Current label: Buyer’s Broker Commission Structure:
Dropdown options: Select / % of Total Purchase Price / Flat Fee / Other

✅ Change the field label to:
Buyer’s Broker Commission Fee:
(Per your note: “this should be labeled Buyer’s Broker Commission Fee.”)

Do not change the dropdown options unless you already have a separate spec.

2) Seller’s Broker Leasing Fee — remove duplicate option

Under Seller’s Broker Leasing Fee:

✅ Residential, Income & Vacant Land:

Delete: Percentage of Each Rental Period (it’s duplicated/repeated)

Keep the remaining correct options exactly as you previously defined (do not add new ones unless specified).

3) Clear prior input when switching compensation types (VERY IMPORTANT)

Add this rule to ALL compensation dropdowns + property type switches inside this tab (and in Agent Bids/Counter Bids):

✅ Required behavior
When a user changes any of the following:

switches from Flat Fee → Percentage

switches from Percentage → Flat Fee

switches to Other

switches between different compensation structures within the same section

switches property type (Residential ↔ Commercial/Business/Income/Vacant Land)

➡️ Clear the previous value(s) from the fields that no longer apply, so only the active selection has data.

Implementation detail (important so Replit doesn’t half-do it):

Clearing must happen in the same Livewire state object that stores those amounts (not just visually).

Also clear hidden fields so old values don’t submit in the request payload.

✅ Acceptance test

Enter $5000 under Flat Fee → switch to Percentage → Flat Fee input clears.

Enter % value → switch to Other → % input clears.

Enter Commercial values → switch to Residential → Commercial-only values are cleared and not submitted.

Fix: “Filled required fields but can’t submit bid” (Hire a Seller’s Agent)

This is a validation / conditional required issue 99% of the time, or a hidden field still failing validation.

Replit must do ALL of this (Seller bids only)
4) Identify what is failing validation (don’t guess)

When bid submission fails, show the actual validation error(s) at the top of the form (or under the field).

Log the validation error keys to Laravel log so we can see exactly which field fails.

✅ Required:

If submit fails, user must see: “Missing/invalid: ___” with the exact field label.

5) Fix hidden-field validation

If a field is hidden due to a dropdown choice, it must NOT remain required and must NOT be validated.

Examples:

If the user selects “Percentage of Gross Rent” and you hide “# of months”, then “# of months” cannot be required.

If the user is on Residential and Commercial-only fields are hidden, Commercial-only fields cannot be required.

✅ Implementation requirements

Update validation rules to be conditional based on:

selected property type

selected compensation option

whether lease-option is Yes/No

If field is not applicable, validation should be nullable.

6) Clear stale values before validate/submit (ties to your clearing rule)

Even if hidden fields are not required, stale values can still cause logic conflicts.

✅ On submit:

sanitize the payload and unset/clear values for non-active fields before saving.

SAFEGUARDS (add this to your instructions)

DO NOT:

loosen validation globally for all listing types

change buyer/tenant/landlord rules while fixing seller submit

remove required rules entirely

DO:

scope validation fixes to Hire a Seller’s Agent bids + counters

ensure conditional fields validate only when visible/selected

Quick Test Script (Replit must run)

Login as tenant@exp.com

Create Hire a Seller listing

Place an agent bid (or test bid form)

Use Flat Fee → switch to % → confirm old value clears

Switch property type → confirm prior type values clear

Submit bid → if it fails, error message must show exact missing field

Fix until submit succeeds with all visible required fields completed