Yep — Replit AI went off on a **PDF download system** tangent. Your issues are **NOT PDF**. They’re:

1. **Actual listing display** (web UI) not showing **Other text** for:

   * Non-Negotiable Property Features & Amenities (Other)
   * Offered Lease Term (Other)
   * (and now Commercial Garage/Parking “Other”)
2. **Edit UI flashing/scroll jump** on Acceptable Property Conditions

So the fix is: **do not touch the PDF packet / field maps / download controller.** You need to modify the **actual listing show/view renderer** and the **edit tab component**.

Below is a **copy-paste prompt** you can drop into Replit AI that forces it to stay on track and implement the fix without “debugging” or refactoring.

---

## ✅ Copy/Paste to Replit AI (Hard Guardrails + Exact Scope)

```text
STOP. Do NOT work on PDF downloads, packet.blade.php, ListingExportFormatter.php, ListingPdfDataBuilder.php, ListingDownloadController.php, or any field maps. Those are already complete and not part of this request.

TASK SCOPE = WEB UI ONLY:
A) ACTUAL LISTING DISPLAY (the web page users view for a Tenant listing)
B) EDIT LISTING UI (the edit tabs for Hire a Tenant)

DO NOT ASK ME TO DEBUG. DO NOT ADD TEMP DEBUG UI. DO NOT “verify data flow.” Assume the “Other” text values are saved correctly and the bug is ONLY that the web listing renderer is not displaying those saved “Other” values.

GOAL 1 (ACTUAL LISTING DISPLAY):
Fix Hire a Tenant actual listing web display so that the following fields ALWAYS show the “Other” text input if it exists:
1) Non Negotiable Property Features and Amenities: append (Other: <text>) if corresponding *_other column is non-empty.
2) Offered Lease Term: append (Other: <text>) if corresponding *_other column is non-empty.
3) Commercial only: Garage/Parking Features “Other” must show in the merged display (see Goal 3).

Implementation requirements:
- Find the exact blade/Livewire view that renders the ACTUAL listing display for Tenant auctions.
- Modify ONLY the listing display rendering (not create/edit forms, not PDF).
- Do NOT rename DB columns.
- Do NOT remove existing output formatting except where specified.
- Add a safe JSON-to-array decoder in the blade if the field can be stored as JSON string.

GOAL 2 (EDIT UI FLASH/SCROLL JUMP):
In Hire a Tenant edit listing, the Acceptable Property Conditions options are flashing on screen and causing the tab to scroll/jump, making it hard to click Next.
Fix it by:
- adding x-cloak on that section
- adding stable wire:key wrapper around that section
- changing that section’s checkbox/multi-select bindings to wire:model.defer (not live)
- do NOT change option values or ordering
- do NOT change the rest of the page layout

GOAL 3 (COMMERCIAL MERGE DISPLAY):
Commercial Tenant listings ONLY:
On the ACTUAL listing display, do not show two separate lines for:
- Garage/Parking Features
- Garage/Parking Features Needed
Instead show ONE line:
Garage/Parking Features Needed: Yes + bubbles of selected options
If Needed = No: show “Garage/Parking Features Needed: No” only (no bubbles)
Also include “Other: <text>” as a bubble if the other text exists.

DELIVERABLE:
- Provide the exact files edited (paths)
- Provide the exact code changes applied in those files
- Keep changes minimal and non-destructive
```

---

## Why this will fix the Replit AI behavior

That pasted response you got is classic “it found a big system and decided to work on it.” The prompt above **locks it** to:

* **Actual listing display renderer** (your real problem)
* **Edit UI flashing** (your other real problem)
* Explicitly **forbids PDF work**

---

## If Replit AI still “can’t find” the actual listing display

Add one more line to the prompt (optional), which forces it to search the right way:

```text
To locate the actual listing display file, search the codebase for the literal label text:
"Non-Negotiable Property Features" and "Offered Lease Term:" and "Acceptable Property Conditions"
Edit the file(s) where those labels are rendered on the public listing page (not the PDF template).
```

---

If you paste me the **URL route** you use to view the “actual Tenant listing” (even just the route name like `tenant-agent-auctions.show`), I’ll tailor the Replit prompt with the most likely exact file paths and variable names so it’s even harder for it to drift.
