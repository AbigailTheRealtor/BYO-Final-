Use this for Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A SELLER’S AGENT ONLY — SPECIAL SALE PROVISION + OFFERED FINANCING/CURRENCY ARE WIRED TO THE WRONG TRIGGERS

### SCOPE

Fix **only** these remaining **Hire a Seller’s Agent** issues in the **Sale Terms** section.

This is a **Seller-only** pass.

Do **not** modify:

* Hire a Buyer’s Agent
* Hire a Landlord’s Agent
* Hire a Tenant’s Agent

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename or delete database columns.**
2. **Do not change labels, tooltips, or option values.**
3. **Do not broad-refactor unrelated shared components unless absolutely necessary for this Seller-only fix.**
4. **Do not remove or alter saved drafts, listings, or bids.**
5. **Do not mark this complete based only on code inspection.**
6. **You must verify these exact Seller issues visually in the browser.**
7. **Do not treat delayed appearance as fixed. These dependent fields must appear immediately after the correct selection.**
8. **Do not leave these fields tied to Occupant Type or Target Closing Timeframe if that is not the intended trigger.**

---

# CORE ISSUE

The conditional logic in **Hire a Seller’s Agent → Sale Terms** is cross-wired.

Right now:

* **Assignment Contract** only triggers its sub-question after selecting **Target Closing Timeframe**
* **Other** under **Special Sale Provision** does not pop up until **Occupant Type** is filled in
* **Offered Financing/Currency** sub-questions only pop up after selecting **Occupant Type**

That is wrong.

These dependent fields must respond to **their own correct trigger selections**, not unrelated fields.

---

# ISSUE 1 — SPECIAL SALE PROVISION → ASSIGNMENT CONTRACT IS TIED TO TARGET CLOSING TIMEFRAME

## Current issue

When **Assignment Contract** is selected under **Special Sale Provision**, the sub-question:

* **Seller Under Contract for Assignment**

does **not** immediately pop up.

Right now it only appears if the user selects an option under:

* **Target Closing Timeframe**

That is wrong.

## Required fix

When **Assignment Contract** is selected under **Special Sale Provision**:

* **Seller Under Contract for Assignment** must appear **immediately**
* it must not wait for a selection under **Target Closing Timeframe**
* it must hide immediately when **Assignment Contract** is unselected
* it must remain visible on edit/reload if **Assignment Contract** remains selected
* its value must save and reload correctly

## What to check

1. whether the **Seller Under Contract for Assignment** block is incorrectly nested under **Target Closing Timeframe**
2. whether the conditional is watching the wrong property
3. whether the wrong array membership check is being used
4. whether wrapper/div structure causes the assignment sub-question to be controlled by the wrong field
5. whether delayed appearance is caused by a later rerender instead of the correct immediate trigger

---

# ISSUE 2 — SPECIAL SALE PROVISION → OTHER IS TIED TO OCCUPANT TYPE

## Current issue

If the user selects **Other** under **Special Sale Provision**, the custom input box does **not** pop up until the user fills in:

* **Occupant Type**

That is wrong.

## Required fix

When **Other** is selected under **Special Sale Provision**:

* the custom input box must appear **immediately**
* it must not depend on **Occupant Type**
* it must hide immediately when **Other** is unselected
* it must remain visible on edit/reload if **Other** remains selected
* the custom value must save and reload correctly

## What to check

1. whether the **Other** custom input is incorrectly nested under an **Occupant Type** conditional
2. whether the wrong property is being watched for the display condition
3. whether the conditional block placement is wrong
4. whether the UI is waiting for an unrelated rerender instead of reacting to the actual **Special Sale Provision** selection

---

# ISSUE 3 — OFFERED FINANCING/CURRENCY SUB-QUESTIONS ARE TIED TO OCCUPANT TYPE

## Current issue

The sub-questions for **Offered Financing/Currency** only pop up once the user selects an option from:

* **Occupant Type**

That is wrong.

The Offered Financing/Currency sub-questions should pop up immediately after each related financing selection.

Example:

* if the user selects **Seller Financing**, the related sub-question(s) should appear immediately

## Required fix

For **Offered Financing/Currency**:

* each trigger option must immediately show its corresponding sub-question(s)
* these sub-questions must **not** depend on **Occupant Type**
* removing the trigger option must hide the corresponding sub-question(s)
* if the trigger option remains selected on edit/reload, the corresponding sub-question(s) must remain visible
* entered values must save and reload correctly

## What to check

1. whether the Offered Financing/Currency dependent blocks are incorrectly nested under **Occupant Type**
2. whether the wrong property is being watched for conditional visibility
3. whether the dependent sections are placed under the wrong wrapper/conditional
4. whether each financing option is correctly mapped to its own follow-up question(s)
5. whether the current behavior is delayed because the wrong field triggers rerender first

---

# REQUIRED IMPLEMENTATION APPROACH

## A. Fix the field ownership / dependencies

Make sure each dependent block is controlled only by its correct trigger:

* **Special Sale Provision → Assignment Contract** controls only:

  * **Seller Under Contract for Assignment**
* **Special Sale Provision → Other** controls only:

  * its own custom input
* **Offered Financing/Currency** controls only:

  * its own financing/currency follow-up questions
* **Occupant Type** does **not** control these fields unless there is an explicit intended rule
* **Target Closing Timeframe** does **not** control Assignment Contract logic unless there is an explicit intended rule

## B. Immediate display behavior

For all of these:

* Assignment Contract sub-question
* Special Sale Provision → Other custom input
* Offered Financing/Currency sub-questions

the UI must appear **immediately after the correct selection**, not after a later field is filled out.

## C. Keep it rerender-safe

The corrected logic must still work after:

* tab switch
* draft save
* edit reopen
* normal Livewire rerenders

---

# REQUIRED DEBUG METHOD

Please trace the **Seller Sale Terms** field structure and confirm:

1. which property controls **Seller Under Contract for Assignment**
2. which property controls the **Other** input under **Special Sale Provision**
3. which property controls each **Offered Financing/Currency** sub-question
4. whether any of those dependent blocks are incorrectly placed inside:

   * **Occupant Type**
   * **Target Closing Timeframe**
   * another unrelated wrapper/conditional
5. whether wrapper/div placement is causing later fields to be controlled by the wrong earlier field

Do not patch this blindly. Fix the actual conditional ownership.

---

# REQUIRED TESTING — BROWSER ONLY

## A) ASSIGNMENT CONTRACT TEST

1. open **Hire a Seller’s Agent**
2. go to **Special Sale Provision**
3. select **Assignment Contract**
4. confirm **Seller Under Contract for Assignment** appears **immediately**
5. do **not** touch **Target Closing Timeframe**
6. confirm the sub-question still appears correctly
7. unselect **Assignment Contract**
8. confirm the sub-question hides immediately
9. save
10. reopen edit
11. confirm the sub-question remains visible if **Assignment Contract** is still selected

---

## B) SPECIAL SALE PROVISION → OTHER TEST

1. select **Other** under **Special Sale Provision**
2. confirm the custom input appears **immediately**
3. do **not** touch **Occupant Type**
4. confirm the input still appears correctly
5. unselect **Other**
6. confirm the input hides immediately
7. reselect **Other**
8. enter custom value
9. save
10. reopen edit
11. confirm the custom input remains visible with saved value

---

## C) OFFERED FINANCING/CURRENCY TEST

1. go to **Offered Financing/Currency**
2. select **Seller Financing**
3. confirm the related sub-question(s) appear **immediately**
4. do **not** touch **Occupant Type**
5. confirm the sub-question(s) still appear correctly
6. select additional financing/currency options that should trigger other follow-up questions
7. confirm each corresponding sub-question appears immediately after its selection
8. remove one trigger option
9. confirm only that option’s corresponding sub-question hides
10. save
11. reopen edit
12. confirm the correct financing sub-questions remain visible for the still-selected terms

---

# SUCCESS CRITERIA

This task is only complete when all of the following are true in **Hire a Seller’s Agent**:

* selecting **Assignment Contract** immediately shows **Seller Under Contract for Assignment**
* **Seller Under Contract for Assignment** is no longer tied to **Target Closing Timeframe**
* selecting **Other** under **Special Sale Provision** immediately shows its custom input
* the **Other** input is no longer tied to **Occupant Type**
* selecting terms under **Offered Financing/Currency** immediately shows their corresponding sub-questions
* **Offered Financing/Currency** sub-questions are no longer tied to **Occupant Type**
* create / draft / edit / save / reopen all remain stable

---

# FINAL RULE

Do **not** respond that this is fixed based on code inspection or partial improvement.

Please verify visually in the browser that:

1. **Assignment Contract** triggers its own sub-question immediately
2. **Other** triggers its own custom input immediately
3. **Offered Financing/Currency** triggers its own sub-questions immediately
4. none of these are still incorrectly tied to **Occupant Type** or **Target Closing Timeframe**.
