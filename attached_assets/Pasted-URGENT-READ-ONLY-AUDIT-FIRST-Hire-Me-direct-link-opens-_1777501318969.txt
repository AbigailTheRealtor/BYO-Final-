URGENT READ-ONLY AUDIT FIRST — Hire Me direct link opens blank/broken page.

ISSUE:
From My Offer Presets → Edit Preset → “Open Hire Me Page” / “Copy Hire Me Link”
URL example:
/hire/agent/direct/142/buyer/residential

The page shows a blank light background with a broken image/loading icon in the center.

GOAL:
Find why the direct Hire Me page is not rendering.

DO NOT:
- Change database
- Delete/recreate presets
- Change routes yet
- Change hire flow logic
- Clear cache as a “fix”
- Edit files until root cause is confirmed

AUDIT STEPS:

1. Identify route for:
/hire/agent/direct/{agent}/{role}/{propertyType}

Report:
- route definition
- route name
- controller/component
- middleware
- expected parameters

2. Confirm preset exists:
- agent/user id 142
- role buyer
- property type residential

Check whether route expects:
- numeric user ID
- agent profile ID
- short_id
- preset ID
- slug

3. Check Laravel logs:
tail -n 150 storage/logs/laravel.log

Look for errors when visiting:
/hire/agent/direct/142/buyer/residential

4. Check browser/server response:
Use curl:
curl -I [local route]
curl [local route] | head -n 80

Report:
- HTTP status code
- whether HTML is returned
- whether it redirects
- whether assets/images fail

5. Trace controller/view:
Find the method handling the direct hire page.
Report:
- exact file
- exact method
- exact Blade view returned
- any data required by view

6. Check whether the broken center icon is:
- missing image/logo asset
- Livewire component failed to mount
- JavaScript error
- loading spinner stuck
- missing Vite/build asset
- auth redirect issue
- exception hidden by production settings

7. Compare the working “Preview Hire Me Page” button:
The preset editor also has “Preview Hire Me Page.”
Check whether it uses a different route than “Open Hire Me Page.”
Report both URLs and route names.

8. Output ONLY audit report:
- root cause
- exact file/line causing issue
- whether route parameter 142 is wrong type
- whether the public hire link should use short_id instead
- recommended minimal fix
- do not apply fix yet