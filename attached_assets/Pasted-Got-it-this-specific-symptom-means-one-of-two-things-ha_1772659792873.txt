Got it — this specific symptom means one of two things happened:

1. the **edit form selection DID save**, but the **listing display is reading/rendering the wrong field or not decoding it**, so it still shows the old values (or nothing), **OR**
2. the **edit form selection never actually persisted to DB** (even though it looked selected), so the listing display has nothing new to show.

Below are **copy-paste Replit instructions with safeguards** to diagnose and fix it fast, focused ONLY on **Buyers → Income → Acceptable Unit Type (`number_of_unit_type`)** after **Edit**.

---

## Replit Instructions With Safeguards

### Buyer Income — Edited “Acceptable Unit Type” selections not showing on listing

**Status:** Create/Edit selection UI may appear correct, but listing display does not reflect newly edited selections.

### Safeguards

* Do **not** rename DB column: `number_of_unit_type`
* Do **not** change Acceptable Property Condition logic
* Do **not** change other fields
* Fix only **save + load + listing display** for `number_of_unit_type`

---

# Step 1 — Confirm whether the edit actually saved to the database (must verify first)

Run a DB check on a listing you just edited (use the same auction id):

* Confirm `number_of_unit_type` is NOT `[]`
* Confirm it contains the newly selected values

✅ If DB **did update** → jump to **Step 3 (Display)**
❌ If DB **did not update** → do **Step 2 (Edit Save Path)**

---

# Step 2 — Fix the Edit save path (Tenant shared edit component)

Replit already confirmed Edit routes through:

* `TenantAgentAuctionEdit.php`
* `tenant-agent-auction-edit.blade.php`

### 2A) Ensure the EDIT blade always syncs values to Livewire before saving

Even if you added a change handler, edits can still fail if the user clicks save while Select2 is focused.

**Add/confirm a `syncAllSelect2BeforeSave()` on the Edit page** that force-syncs:

* `selected = $('#number_of_unit_type').val() || [];`
* `@this.set('number_of_unit_type', selected);`
* if using JSON bridge:

  * `@this.set('number_of_unit_type_json', JSON.stringify(selected));`

Then call `syncAllSelect2BeforeSave()` immediately before the Livewire save/update action runs.

**Safeguard:** This must run only on Save/Update button click (not continuously).

### 2B) In `TenantAgentAuctionEdit.php` update() method, decode JSON bridge if used

If you have a `number_of_unit_type_json` property, decode it right before persisting:

* `$decoded = json_decode($this->number_of_unit_type_json, true);`
* if is_array($decoded) → `$this->number_of_unit_type = $decoded;`

Then normalize safely:

* ensure array
* trim
* unique
* reindex

**Safeguard:** Do not `implode()`; save as JSON array.

---

# Step 3 — Fix the listing display (most likely cause if DB updated but UI didn’t)

If the DB has the new selections but the listing doesn’t show them, the listing view is likely:

* reading the wrong column,
* decoding incorrectly,
* or showing a mapped “pretty” label list that doesn’t include those values.

### 3A) Confirm the listing template uses `number_of_unit_type`

In the Buyer Income listing display blade (where “Acceptable Unit Type” renders):

* Verify it references `number_of_unit_type` (not `unit_type`, not `number_of_units`, not `unit_types`)
* Verify it uses the **updated listing record**, not a cached old variable.

### 3B) Force decode before rendering

If `number_of_unit_type` is stored as JSON/text, ensure the display code does:

* if string → `json_decode(...)`
* if already array → use as-is

Then render all values:

* comma-separated or bullet list

**Safeguard:** Do not treat `[]` as a string and print nothing silently.

### 3C) Ensure the “Income-only” conditional isn’t blocking display

A very common bug: the field is saved, but display is wrapped in the wrong condition (e.g., checks “Residential” or checks the wrong property type variable).

Fix the conditional so it shows when:

* listing is Buyer AND
* property type includes Income (or your specific Income flag)

---

# Step 4 — Fix stale data after update (if you redirect back to the listing)

If you save and immediately redirect to the listing view, and it shows old data, you might be using:

* an already-loaded model instance that isn’t refreshed,
* or cached computed metadata.

### Required safeguard

After update, ensure you reload fresh from DB for the show page:

* use a fresh query (not the same instance)
* if using relationships/metadata, call refresh or re-fetch.

**Safeguard:** Do not cache the “auction” object across the update → show transition.

---

## QA Test (Must Pass)

1. Edit Buyer Income listing → select 3 unit types → Save
2. Immediately check DB for that id → `number_of_unit_type` contains the 3 values
3. View listing → “Acceptable Unit Type” shows the 3 values
4. Edit again → remove 1 → Save → DB + listing update accordingly

---

## What to tell Replit AI (copy/paste)

“Buyer Income Acceptable Unit Type (`number_of_unit_type`) edits are not showing on the listing. First verify DB is updating for the edited auction id. If DB is not updating, add a pre-save syncAllSelect2BeforeSave() on `tenant-agent-auction-edit.blade.php` to force `@this.set('number_of_unit_type', selected)` (and json bridge if used) right before update(). Also ensure `TenantAgentAuctionEdit.php` decodes json bridge before saving. If DB IS updating but listing still shows old values, fix the listing display template to reference `number_of_unit_type`, decode JSON properly, and ensure the Income-only conditional isn’t blocking display. Also ensure the show page reloads fresh data after update (no stale model/cached metadata). Do not modify any other fields.”

---

If you can tell me just one thing: **when you edited and saved, did the DB value for that auction id change from `[]` to a list?**

* If **yes**, it’s 95% a **display/conditional/stale model** issue (Step 3/4).
* If **no**, it’s a **pre-save sync / decode / save path** issue (Step 2).
