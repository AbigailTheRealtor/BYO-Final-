REPLIT INSTRUCTIONS — Accepted Bid Summary Layout Polish (Hire a Tenant)

Applies ONLY to:
BidYourAgent → Hire a Tenant → Accepted Bid Summary page (Residential + Commercial)

🚨 SAFEGUARDS (MANDATORY)

DO NOT:

change any database columns, models, migrations, or routes

change bid acceptance or e-sign logic

change any text content, field labels, or section ordering

change which data displays (display/layout only)

refactor unrelated components or shared styling globally

YOU MAY ONLY:

update CSS/layout of the Accepted Bid Summary page

adjust container widths, padding, alignment, typography, and spacing

add print/PDF-friendly CSS rules (no logic changes)

🎯 PROBLEM

The “Summary Document” content area renders noticeably narrower than the outer container, making it look misaligned and less polished.

✅ GOAL

Improve visual polish by:

Making the “Summary Document” content align cleanly within the container

Improving readability (spacing/typography)

Preserving print/PDF friendliness

1) WIDTH + ALIGNMENT FIX (REQUIRED)
A) Make summary content fill container width

The inner “Accepted Bid Summary” document should not appear smaller than its surrounding box.

Ensure the document content area uses:

width: 100%

consistent left/right padding

B) Optional max-width for readability (preferred)

If you want a “document page” look:

set max-width: 950px (or similar)

center it with margin: 0 auto

keep outer container aligned so it doesn’t look like an accidental narrow column

Important: whichever approach is used, it must look intentional and aligned — not “floating.”

2) SPACING + TYPOGRAPHY POLISH (REQUIRED)

Without changing content:

reduce excessive whitespace above the document title

apply consistent spacing between sections (Parties, Listing Details, Services, Compensation, etc.)

increase list readability:

line-height for bullet items

consistent indent spacing

keep headings consistent in size/weight

3) OPTIONAL “DOCUMENT PAGE” STYLING (ALLOWED IF CLEAN)

If you add a page look, keep it subtle:

white background

light border

very soft shadow

consistent padding

Do NOT add heavy borders, loud colors, or card-in-card clutter.

4) PRINT/PDF CSS (RECOMMENDED)

Add print-friendly rules so the summary exports cleanly:

prevent section headers from being orphaned at page bottom

avoid splitting bullet lists awkwardly

ensure the summary uses full printable width

hide page-only buttons/controls in print view

✅ ACCEPTANCE CHECKS

Summary document is visually aligned and no longer looks smaller than the outer container

Readability is improved (spacing + bullets + headings)

No content/wording/data changes

E-sign + bid acceptance logic unaffected

Print/PDF output still looks clean and professional