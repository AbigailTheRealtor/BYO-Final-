Yes, that screenshot shows the real issue more clearly.

The **Tenant counter/match logic is pulling Buyer services behind the scenes**, which means this is not just a counting problem. It is also a **wrong source catalog / wrong role mapping** problem.

So the Replit instructions need to be updated to make clear that:

* **Hire a Tenant Agent** must only use the **Tenant service catalog**
* it must never load **Buyer services**
* match score and counter comparison must be built from the **correct role-specific source data**
* all normalization/filtering must happen **after role + property type are locked correctly**

Here is the updated **copy-paste Replit instruction with safeguards**:

---

# Replit Instructions With Safeguards

## Core issue

Fix the **Hire a Tenant Agent** match score / counter-offer comparison logic.

The problem is **not only inflated counting**. The system is also pulling the **wrong role’s services behind the scenes**. In Tenant counter/match comparison, it is loading **Buyer services/purchase criteria services** instead of the correct **Tenant leasing services**.

This causes:

* wrong “Services Not Included in Counter” lists
* wrong match score denominator
* wrong matched count
* wrong counter comparison output
* wrong services shown in history/comparison panels

Example of wrong behavior:
Tenant flow is showing Buyer service text like:

* “Create a branded flyer summarizing the Buyer’s purchase criteria”
* “Post the Buyer’s purchase criteria...”
* “Send email alerts with new listings from the MLS that match the Buyer’s purchase criteria”

That should never appear in a **Tenant** listing, Tenant bid, Tenant counter, or Tenant match score flow.

---

## Goal

For **Hire a Tenant Agent**:

1. Lock the scoring/comparison engine to the correct **role-specific catalog**
2. Lock the service comparison to the correct **property-type-specific Tenant services**
3. Ensure **Buyer services are never loaded** in Tenant bid/counter/match logic
4. Ensure **Offered Services count only actual Tenant-selected checkboxes**
5. Ensure **Broker Compensation & Agency Agreement Terms count logical Tenant terms only**
6. Apply the same fix to both:

   * regular Match Score
   * Counter Offer Match Score
   * “Services Not Included in Counter”
   * any related added/removed services display

---

# Safeguards

## 1. Scope only to Tenant flow first

Fix only the active logic used for:

* Hire a Tenant Agent bid comparison
* Hire a Tenant Agent counter comparison
* Tenant services comparison panels
* Tenant match score summary

Do not modify Buyer, Seller, or Landlord flows unless a shared helper must be fixed and can be updated safely without side effects.

---

## 2. No destructive DB changes

* Do not rename/drop/delete tables or columns
* Do not wipe old bids, counters, or history
* Do not rewrite stored data in bulk
* Prefer fixing role resolution, normalization, filtering, and scoring logic only

---

## 3. Do not change listing form fields or wording

Do not change:

* labels
* section order
* tooltips
* visible wording on forms

This is a **role/source resolution + comparison/scoring logic fix**, not a UI rewrite.

---

## 4. Use listing creation as source of truth

For Tenant flow, use the **Tenant listing creation catalog** as the only source of truth for:

* Tenant offered services
* Tenant broker compensation & agency agreement logical term structure

Do not use stale shared catalogs or fallback role catalogs.

---

# Root cause to inspect first

Find the exact code path used to build these Tenant outputs:

* Match Score
* Counter Offer Match Score
* Services Not Included in Counter
* Added Services / Removed Services
* Terms Match
* Changed Terms count

Then inspect where it resolves:

* role / user type
* property type
* services catalog
* terms map

The likely bug is one of these:

### A. Wrong role fallback

Tenant flow is falling back to Buyer services because the role is null, stale, mismatched, or defaulting to Buyer.

### B. Shared helper using wrong catalog

A shared helper is using a generic purchase/buyer services map instead of the Tenant leasing catalog.

### C. Old meta map / stale stored key

Comparison code is reading old saved normalized values from Buyer logic instead of Tenant logic.

### D. Wrong source-of-truth method

The comparison engine may be using the wrong form definition file/component when reconstructing selected services.

---

# Required fix

## Part 1: Hard-lock role resolution before comparison

Before any service or term scoring happens, explicitly resolve and lock:

* flow role = Tenant
* property type = actual listing property type
* service catalog = Tenant property-type-specific catalog only
* term map = Tenant broker compensation & agency agreement term map only

Do not allow a Buyer fallback in Tenant flow.

If role resolution fails, stop and log/report it instead of silently using another role’s catalog.

---

## Part 2: Tenant services must come only from Tenant catalog

For Tenant bid and counter comparison:

### Residential Tenant listings

Use only Residential Tenant services catalog

### Commercial Tenant listings

Use only Commercial Tenant services catalog

Never load Buyer purchase services for Tenant comparison.

This applies to:

* selected original services
* added services
* removed services
* “services not included in counter”
* matched services count

---

## Part 3: Filter out any wrong-role services before scoring

Add a Tenant-only normalization/filtering step for comparison.

When building Tenant comparison sets:

1. load original selected services
2. normalize labels
3. intersect only with the valid current Tenant catalog for that property type
4. remove duplicates
5. ignore blanks
6. ignore headings/helper rows
7. discard any service strings that belong to Buyer, Seller, or Landlord catalogs

This means if Buyer services somehow exist in stored or reconstructed comparison arrays, they must be excluded from Tenant scoring and Tenant comparison output.

---

## Part 4: Fix “Services Not Included in Counter”

The “Services Not Included in Counter” panel for Tenant flow must compare only:

* Tenant original selected services
  vs
* Tenant counter-selected services

It must not show Buyer services.

If a service does not belong to the valid Tenant catalog for that property type, do not include it in:

* removed list
* added list
* matched count
* denominator

---

## Part 5: Fix Offered Services denominator

The services denominator must equal only:

**the actual Tenant-selected service checkboxes from the original Tenant listing**

Not:

* Buyer services
* section headings
* metadata
* dynamic helper inputs
* repeated array leaves
* “Other” text inputs by themselves
* hidden fields
* wrong-role values

If Tenant selected 38 service checkboxes:

* denominator = 38
* not 102

---

## Part 6: Fix Tenant terms scoring to use logical terms only

The Broker Compensation & Agency Agreement Terms scorer must use a **Tenant-specific logical term map**.

Count each logical term once, not each raw child input box.

Examples of logical Tenant terms:

* Tenant’s Broker Commission Structure
* Tenant’s Broker Lease Fee
* Payment Timing for Broker Fees
* Interested in Purchasing a Property
* Tenant’s Broker Purchase Fee
* Interested in a Lease-Option Agreement
* Compensation for Creating the Lease-Option Agreement
* Compensation if Purchase Option is Exercised
* Protection Period Timeframe
* Early Termination Fee
* Retainer Fee
* Retainer Fee Amount
* Retainer Fee Application
* Tenant Agency Agreement Timeframe
* Acceptable Brokerage Relationship
* Additional Terms

Combo structures still count as one logical term.

---

## Part 7: Conditional terms must stay Tenant-specific

Only include dependent terms when applicable:

* If Interested in Purchasing = No, exclude Tenant’s Broker Purchase Fee
* If Interested in Lease-Option = No, exclude lease-option compensation fields
* If Retainer Fee = No, exclude Retainer Fee Amount and Retainer Fee Application
* If Timeframe = Other, custom text belongs to that same logical term, not a separate extra term

---

## Part 8: Use same fixed logic for both regular and counter scoring

Do not maintain separate inconsistent code paths.

The same Tenant-specific helpers should power:

* Match Score
* Counter Offer Match Score
* Added/Removed services
* Services Not Included in Counter
* Terms Match
* Changed Terms count

---

# What Replit should inspect first

Find the exact active code used for:

1. Tenant bid match score summary
2. Tenant counter-offer comparison summary
3. Tenant added/removed/not included services lists
4. service normalization/comparison helper
5. terms normalization/comparison helper
6. any role/property type catalog resolver

Then verify:

* how role is determined
* how property type is determined
* where the catalog comes from
* whether a Buyer fallback exists
* whether comparison is using a shared purchase catalog by mistake

---

# Required implementation pattern

## A. Add a Tenant-specific catalog resolver

Create or fix a helper that explicitly returns the correct catalog for:

* role = Tenant
* property_type = Residential or Commercial

Something like:

* `resolveTenantServicesCatalogForScoring()`
* `resolveTenantTermsMapForScoring()`

This resolver must never default to Buyer.

---

## B. Add a guard against wrong-role catalog contamination

Before scoring, validate that the catalog and comparison values belong to the Tenant role.

If strings clearly belong to Buyer catalog in Tenant flow, exclude them and log/debug it.

---

## C. Build clean Tenant service sets for comparison

Use only:

* current valid Tenant catalog values
* actual selected Tenant service labels
* unique values only

Then compare:

* original Tenant selected set
  vs
* current bid/counter Tenant selected set

---

## D. Build clean Tenant logical term map for scoring

Normalize the Tenant bid/counter terms into logical fields, not raw payload leaf counts.

---

## E. Remove wrong-role fallback logic

If the scorer currently does anything like:

* default to Buyer
* use purchase criteria services as generic default
* pull from a shared “criteria services” map without role lock

replace that behavior for Tenant flow.

---

# Copy-paste prompt for Replit

Use this exactly:

> Fix the Hire a Tenant Agent match score and counter comparison logic. This is not only a counting issue. The Tenant flow is incorrectly pulling Buyer services behind the scenes, which is why Buyer purchase-criteria service text is appearing inside Tenant “Services Not Included in Counter” and likely inflating the services denominator and comparison output.
>
> Scope this fix only to the active Tenant scoring/comparison flow first. Do not modify Buyer, Seller, or Landlord flows unless a shared helper must be updated safely without side effects. No destructive DB changes. Do not change form labels, tooltips, field order, or saved data structure.
>
> Use Tenant listing creation as the source of truth for both the offered services catalog and the Tenant Broker Compensation & Agency Agreement logical term structure. Before any comparison/scoring happens, explicitly resolve and lock role = Tenant and property_type = the actual listing property type, then load only the matching Tenant services catalog and Tenant terms map. Do not allow a Buyer fallback in Tenant flow. If role resolution fails, stop and log/report instead of silently using another role’s catalog.
>
> Update Tenant services comparison so it counts and displays only valid Tenant service checkbox values for that property type. Normalize the original selected services and current bid/counter services, intersect them only with the valid current Tenant catalog, deduplicate them, ignore headings/helper inputs/blank rows/metadata, and discard any wrong-role values such as Buyer services. The “Services Not Included in Counter” panel for Tenant flow must compare only Tenant original selected services versus Tenant counter-selected services.
>
> Also update Tenant terms scoring so each logical agreement term counts once rather than counting every child input box. Use a Tenant-specific logical term map and apply conditional inclusion rules properly. Reuse the same fixed normalization/comparison logic for both regular Match Score and Counter Offer Match Score.
>
> After the fix, confirm that Buyer purchase services never appear anywhere in Tenant match/counter comparison output, that services denominator reflects only actual Tenant-selected services for that listing, and that terms denominator reflects only logical Tenant agreement terms.

---

# What Replit should test

## Test 1

Open Tenant counter comparison
Expected:

* no Buyer service text anywhere

## Test 2

“Services Not Included in Counter”
Expected:

* only Tenant services from the correct Tenant catalog for that property type

## Test 3

Tenant services denominator
Expected:

* equals actual Tenant-selected checkboxes only
* not inflated by Buyer services or metadata

## Test 4

Added/Removed services
Expected:

* only Tenant services appear
* no cross-role contamination

## Test 5

Terms denominator
Expected:

* logical Tenant terms only
* not every child field

## Test 6

Counter offer scoring
Expected:

* uses exact same Tenant-only role-locked logic

---

The key thing Replit needs to understand is this:

**First fix the role-to-catalog resolution. Then fix the counting.**
Because if it keeps loading Buyer catalogs in Tenant flow, the match score will stay wrong no matter how much they tweak the denominator.

I can also turn this into a **shorter “hard stop if Buyer text appears in Tenant flow” version** for Replit.
