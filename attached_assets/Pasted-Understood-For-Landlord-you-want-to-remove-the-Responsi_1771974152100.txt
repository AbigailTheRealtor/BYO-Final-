Understood. For **Landlord**, you want to **remove the “Responsibilities” section header entirely**, and **everything currently shown under “Responsibilities” should appear as part of “Leasing Terms”** (same tab/section, no flash, no separate block).

Below are **complete Replit instructions** to do exactly that, without changing your database or field names.

---

## ✅ Replit AI Instructions (copy/paste)

**Goal (Landlord listing display):**

* Remove the “Responsibilities” heading/section entirely.
* Move all fields currently rendered under “Responsibilities” so they render directly under **Leasing Terms**.
* Do not change database schema, field names, or saved values.
* Do not remove tooltips/inputs elsewhere—this is display/layout only.

**Hard rules:**

* DO NOT rename any fields/columns.
* DO NOT edit migrations.
* ONLY move Blade markup / rendering blocks.
* Keep the exact same field labels and values—just reposition them.

---

## 1) Find the Landlord “Show/Preview” template

In Replit, search the codebase for:

* `Responsibilities`
* `Owner Responsible For`
* `Desired Rental Amount`
* `Lease Amount Frequency`
* `Desired Lease Term`
* `Rent Includes`

Likely file paths:

* `resources/views/livewire/landlord-*.blade.php`
* `resources/views/landlord/*.blade.php`
* `resources/views/listings/landlord/*.blade.php`

Open the file that contains the **exact** text “Responsibilities”.

---

## 2) Remove the “Responsibilities” section header + wrapper

You’ll see something similar to:

```blade
<h2>Responsibilities</h2>
```

or

```blade
<div class="section-title">Responsibilities</div>
```

or a whole block like:

```blade
<div class="section-block">
  <h3>Responsibilities</h3>
  ...
</div>
```

✅ Delete ONLY the header and the wrapper container (if it exists).
⚠️ Do NOT delete the fields inside—those need to be moved.

---

## 3) Move the fields into the “Leasing Terms” block

Right above the Responsibilities block, you already have:

```blade
<h2>Leasing Terms:</h2>
```

(or an equivalent container)

### Do this:

* Cut everything that is currently *inside* the Responsibilities section (the fields)
* Paste it **immediately after** the last Leasing Terms line item, before the next major section begins.

Based on your screenshot, these should be moved under Leasing Terms:

* `Owner Responsible For:`
* `Desired Rental Amount:`
* `Lease Amount Frequency:`
* `Desired Lease Term:`
* `Rent Includes:` (all the chips)
* Anything else currently grouped under Responsibilities

So the output order becomes:

**Leasing Terms**

* Occupant Type
* Occupied Until
* Leasing Space
* Restrictions Include
* Maintenance & Repairs Handled By
* Maintenance Response Time
* Included Storage Space
* Storage Space Size
* **Owner Responsible For**
* **Desired Rental Amount**
* **Lease Amount Frequency**
* **Desired Lease Term**
* **Rent Includes** (chips)

---

## 4) If the Responsibilities fields are inside an `@if` block, remove that wrapper

You might see something like:

```blade
@if($activeTab === 'responsibilities')
  ...fields...
@endif
```

✅ Remove the `@if(...)` wrapper entirely
✅ Keep the fields and paste them under the Leasing Terms block (inside the Leasing Terms `@if($activeTab === 'leasing_terms')` block if you’re using tabs).

This step prevents the “flash then disappear” behavior too.

---

## 5) Make sure they render under the Leasing Terms tab condition (if tabs exist)

If your Leasing Terms content is wrapped like:

```blade
@if($activeTab === 'leasing_terms')
   ...leasing terms fields...
@endif
```

Then **all the moved fields must be inside that same block**.

So:

```blade
@if($activeTab === 'leasing_terms')
   ...original leasing terms...

   {{-- moved fields start --}}
   ...owner responsible for...
   ...desired rental amount...
   ...lease amount frequency...
   ...desired lease term...
   ...rent includes...
   {{-- moved fields end --}}

@endif
```

---

## 6) Don’t let Livewire reset the tab (anti-flash safeguard)

In the Landlord Livewire component (commonly one of these):

* `app/Http/Livewire/LandlordListingShow.php`
* `app/Http/Livewire/LandlordListingView.php`
* `app/Livewire/Landlord/...`

Add:

```php
public function updatedActiveTab($value)
{
    $allowed = array_keys($this->tabs ?? []);
    if (!in_array($value, $allowed, true)) {
        $this->activeTab = $allowed[0] ?? 'details';
    }
}
```

Then also remove any `'responsibilities'` tab entry from `$this->tabs` if it exists.

---

## 7) Visual spacing (so it looks like one continuous section)

After removing the header, you may want a thin divider before the moved fields (optional):

```blade
<hr class="my-4">
```

But don’t add a new heading.

---

# ✅ What I need from you (so I can make this 100% exact)

Paste the **Blade snippet** where “Responsibilities” is printed (even 30–80 lines is enough), and I’ll tell you exactly what to cut and where to paste it—line-for-line—so Replit doesn’t guess.

If you prefer not to paste code, tell me the filename you found when you searched “Responsibilities”.
