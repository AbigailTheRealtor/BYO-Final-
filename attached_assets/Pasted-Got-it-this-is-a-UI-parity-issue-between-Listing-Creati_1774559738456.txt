Got it — this is a **UI parity issue between Listing Creation vs Agent Bid form**, specifically the **Nominal Consideration Fee input styling (grey boxed container with $ prefix).**

Here are **clean, copy-paste Replit instructions with safeguards** exactly how you like them 👇

---

## 🔧 TASK: Fix Nominal Consideration Fee UI in Seller Agent Bids

### 🎯 GOAL

Make **Nominal Consideration Fee** in **Agent Bid form (Hire a Seller Agent)** look **IDENTICAL** to:

* Seller Listing Creation form
* Same grey background container
* Same `$` prefix styling
* Same spacing, padding, font, and alignment

---

## 🚨 SAFEGUARDS (DO NOT SKIP)

1. **DO NOT rename any database fields**
2. **DO NOT change validation or backend logic**
3. **DO NOT modify listing creation files**
4. **ONLY update Agent Bid form UI**
5. **MATCH styling from listing creation exactly (copy, not recreate)**
6. **DO NOT change labels, tooltips, or order**
7. **DO NOT affect Buyer, Tenant, or Landlord unless explicitly reused component**
8. If shared component is used → confirm before editing

---

## 🔍 STEP 1: LOCATE FILES

Find the **Seller Agent Bid form blade file**

Likely locations:

```
resources/views/livewire/agent-bids/
resources/views/livewire/seller-agent-auction/
resources/views/livewire/tenant-agent-auction.blade.php (shared)
```

You are looking for:

```
Nominal Consideration Fee
```

---

## 🔍 STEP 2: FIND SOURCE OF TRUTH (IMPORTANT)

Open the **Seller Listing Creation form** where this is already correct:

Likely:

```
resources/views/livewire/tenant-agent-auction.blade.php
OR
resources/views/livewire/seller-agent-auction.blade.php
```

Search:

```
Nominal Consideration Fee
```

---

## 📌 STEP 3: COPY EXACT UI BLOCK

You are NOT recreating this.

👉 Copy the FULL wrapper that includes:

* outer grey container
* flex alignment
* `$` prefix box
* input styling
* padding/margins

It will look similar to:

```html
<div class="flex items-center w-full bg-gray-100 border rounded-lg overflow-hidden">
    <span class="px-3 text-gray-600">$</span>
    <input type="number" class="w-full bg-transparent border-0 focus:ring-0" />
</div>
```

⚠️ Your actual code may differ — COPY EXACTLY from working version

---

## 🔁 STEP 4: REPLACE IN AGENT BID FORM

Find current broken version (likely looks like plain input):

```html
<input type="number" wire:model="nominal_consideration_fee">
```

🚫 DELETE only the input (not logic)

✅ REPLACE with copied full UI block

---

## ⚠️ STEP 5: PRESERVE BINDING

Inside copied block, update ONLY:

```html
wire:model="..."
```

Match existing model from bid form:

Example:

```html
wire:model="bid.nominal_consideration_fee"
```

DO NOT change variable name

---

## 🎨 STEP 6: MATCH EXACT STYLING

Verify the following match listing creation:

* Same grey background (bg-gray-100 or equivalent)
* Same border radius
* Same `$` spacing
* Same input height
* Same font size
* Same padding (px-3 etc)
* No extra margins or gaps

---

## 🧪 STEP 7: TEST

### Test Case 1

* Open Seller Listing Creation → confirm style

### Test Case 2

* Open Agent Bid form → should now match EXACTLY

### Test Case 3

* Enter value → confirm it saves correctly

### Test Case 4

* Edit bid → value persists

---

## 🚫 COMMON ISSUES TO AVOID

* Do NOT wrap inside additional divs (causes spacing mismatch)
* Do NOT use different Tailwind classes
* Do NOT recreate manually (must copy source)
* Do NOT forget `$` prefix span
* Do NOT break Livewire binding

---

## ✅ FINAL RESULT

Agent Bid form should now:

* Look identical to listing creation
* Have grey container
* Have `$` prefix
* Have consistent spacing across platform

---

If you want next level consistency, I’d recommend after this:

👉 Extract this into a reusable component like:

```
<x-currency-input />
```

So this never breaks again across:

* Seller
* Buyer
* Tenant
* Landlord
* Bids

I can write that for you next if you want 👍
