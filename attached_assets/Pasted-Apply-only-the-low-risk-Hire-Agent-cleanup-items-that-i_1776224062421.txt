Apply only the low-risk Hire Agent cleanup items that improve user-facing consistency before launch.

IMPORTANT:
Do this only after confirming Pass 1 regression tests are clean.

CRITICAL SAFEGUARD:
- No schema changes
- No route/controller renames
- No creation flow redesign
- No bid logic changes
- No Accepted Bid Summary logic changes
- No deletion of backup/copy files unless they are definitively unreferenced and safe

----------------------------------
PASS 2A — COPY / LABEL CONSISTENCY ONLY
----------------------------------

1. Standardize naming consistency across user-facing text:
- choose one style and apply consistently:
  - “Hire Tenant’s Agent”
  - “Hire Landlord’s Agent”
  - “Hire Buyer’s Agent”
  - “Hire Seller’s Agent”

2. Ensure Bidding Period / Traditional copy remains aligned with actual system behavior across all four flows.

3. Clean up inconsistent user-facing “Listing” vs “Auction” wording where already identified and safe to update as presentation text only.
   - Do not rename routes, controllers, variables, or database terms
   - UI text only

----------------------------------
PASS 2B — SAFE TEMPLATE CLEANUP ONLY
----------------------------------

4. Remove or isolate outdated commented-out tooltip/copy blocks only where they are confirmed dead and not used.

5. Review the Tenant Bidding Period Length required attribute only if the active form behavior is actually wrong in real use.
   - If behavior is fine, leave it alone.

6. Leave Seller bid edit route naming alone unless there is a real user-facing problem.
   - Do not change for cosmetic reasons only.

----------------------------------
PASS 2C — FILE CLEANUP
----------------------------------

7. Do NOT bulk-delete .bak or copy files in this pass unless each file is explicitly verified as:
- not referenced
- not active
- safe to remove

If uncertain, report them and leave them untouched.

----------------------------------
OUTPUT REQUIRED
----------------------------------

Return:
1. exact files changed
2. what was standardized
3. what was left untouched and why
4. confirmation that no live route or workflow was broken