Below are **copy-paste Replit instructions (with safeguards)** to make **Tenant listing “Services” display match EXACTLY** what the user checked during listing creation — **same items, same order, same section order** — for **Residential and Commercial** under **Hire a Tenant’s Agent** ([Tenant@exp.com](mailto:Tenant@exp.com)).

---

## Replit Instructions — [Tenant@exp.com](mailto:Tenant@exp.com)

### Hire a Tenant’s Agent → Services Display Must Match Selection (Exact Items + Exact Order)

## Problem

On the actual Tenant listing page, the **Services section is missing items and/or reordering them** (likely due to sorting, filtering, or rendering from a different master list than what was used in the form).

## Goal

For **Tenant listings only**:

1. Show **every service item that was checked** during listing creation (no missing items).
2. Preserve the **exact order** as shown in listing creation:

   * **Section order** (📢 then 🔍 then 🏡 …)
   * **Item order within each section**
3. Works for **Residential** and **Commercial** Tenant service lists.
4. No impact to Buyer/Seller/Landlord.

---

# SAFEGUARDS (MANDATORY)

1. **Tenant-only scope:** Only modify Tenant listing creation + Tenant listing display components.
2. **Do not rename DB fields/tables.** Addition-only changes are allowed.
3. **Backward compatible:** Existing Tenant listings that stored services in the “old” format must still display (fallback rendering).
4. **No destructive migrations:** If we add a new column, it must be nullable and default-safe.
5. **Do not change the actual service text.** Must match the canonical text used in the checklist (including punctuation/capitalization).
6. **Do not sort services alphabetically** anywhere in Tenant display.

---

# ROOT CAUSE (WHAT’S HAPPENING)

One (or more) of these is true right now:

* The display page is rebuilding the services from a **master list** and then filtering selected items — but that master list is different than the form list (so items drop).
* The display page is sorting selected services (e.g., `sort()`, `ksort()`, `orderBy()`) which changes order.
* Services are stored as an associative set (keys) and then rendered without a stable ordering source.

**Fix:** Store a stable, ordered representation at save-time OR render using the same canonical ordered structure used on the form.

---

# REQUIRED FIX (BEST PRACTICE): STORE ORDERED “SERVICE BLUEPRINT” ON SAVE

## Step 1 — Add a new nullable column to Tenant listings

Add a column to the Tenant listing table (whatever your tenant auction table is called, e.g. `tenant_agent_auctions`):

**Column:** `services_snapshot` (LONGTEXT / JSON)

### Migration (addition-only)

* Create a new migration:

  * Add `services_snapshot` as `longText()->nullable()`
  * Do NOT remove or rename any existing service fields.

**Safeguard:** This column is optional and will not break old listings.

---

## Step 2 — In Tenant listing creation (Livewire), generate and save the snapshot

### Location

Tenant listing creation component, e.g.:

* `app/Http/Livewire/TenantAgentAuction.php` (create)
* and/or `TenantAgentAuctionEdit.php` (edit)

### Requirement

When user saves draft or submits, build a snapshot that includes:

* listing type (Residential/Commercial)
* canonical sections in canonical order
* canonical items in canonical order
* `checked: true/false` for each item
* ONLY display checked items later (but snapshot preserves the order)

### Snapshot structure (example)

```json
[
  {
    "section": "📢 Tenant Criteria Marketing & Promotion",
    "items": [
      {"text":"Create a branded flyer summarizing the Tenant’s rental criteria","checked":true},
      ...
    ]
  },
  ...
]
```

### How to build it (logic requirement)

1. Use the **same canonical source array** that the checklist uses to render options on the form.
2. Loop through sections/items **in that exact order**.
3. Mark each item checked by comparing to saved selections (IDs or text).
4. Save the full snapshot JSON into `services_snapshot`.

**Critical:** Do not derive display order later by sorting selections; the order is captured here.

### Save on BOTH:

* Save Draft
* Submit
* Edit Save

---

## Step 3 — Tenant listing display must render from `services_snapshot` first

### Location

Tenant listing show page and any listing cards/preview components that show services:

* `resources/views/...tenant...show.blade.php`
* `resources/views/livewire/...tenant...show.blade.php`
* any shared partial used in Tenant listing display

### Rendering rules

1. If `services_snapshot` exists and is valid JSON:

   * Render sections in snapshot order
   * Under each section, render only items with `"checked": true`
   * Preserve item order exactly as stored
2. If `services_snapshot` is null/invalid (older listings):

   * Fallback to the current method, BUT **do not sort**; render in canonical order by filtering selected items from the canonical list.

---

# FALLBACK FIX (FOR OLD LISTINGS) — CANONICAL ORDER FILTER

If snapshot is missing, do this:

1. Load the canonical ordered service structure **for Residential** or **Commercial** (depending on listing property type).
2. For each canonical item, check if it’s selected.
3. Print in canonical order.

**Do NOT loop over selected items directly** (that’s how order gets lost).

---

# STEP 4 — ENSURE RESIDENTIAL AND COMMERCIAL USE THEIR OWN CANONICAL LISTS

### Requirement

* Residential Tenant Services list must match the Residential checklist order exactly (your long list starting with “📢 Tenant Criteria Marketing & Promotion”).
* Commercial Tenant Services must match the Commercial checklist order exactly (the commercial version you finalized).

### Safeguard

Do not reuse Buyer services, and do not mix Residential/Commercial arrays.

---

# STEP 5 — REMOVE ANY SORTING IN DISPLAY

Search Tenant show templates and Livewire for:

* `sort()`, `asort()`, `ksort()`
* `collect(...)->sort()`, `sortBy()`
* `orderBy()` on services
* `array_values()` on associative arrays that were keyed in a way that changes order

**Do not sort services anywhere for Tenant listings.**

---

# QA TESTS (REQUIRED)

## Residential

1. Create Tenant listing → check **all** items under:

   * 📢 Tenant Criteria Marketing & Promotion
   * 🔍 Property Search, Alerts & Matching
   * 🏡 Property Showings & Virtual Tours
   * 📝 Tenant Application Support
   * 📃 Lease Preparation & Execution
   * 🚚 Move-In Support & Coordination
   * 💡 Leasing Strategy & Guidance
2. Submit listing → listing display shows:

   * all sections present
   * every checked item present
   * exact same order as checklist
3. Save draft → reopen → still exact match.

## Commercial

Repeat with Commercial Tenant services.

## Regression

* Buyer/Seller/Landlord services unaffected.
* Old Tenant listings without snapshot still render without missing items.

---

# “DONE” DEFINITION

Tenant listing Services on the public listing page match **exactly** what was checked during listing creation — **same items, same order, for Residential and Commercial** — using snapshot-first rendering and canonical fallback.

---

## OPTIONAL (HIGHLY RECOMMENDED) SAFEGUARD: SERVICE IDs

If you’re currently saving selections by raw text, future text edits can break matching. The safe approach:

* Assign stable IDs for each service item (e.g., `tenant_res_marketing_flyer`)
* Store IDs in DB
* Snapshot stores both `{id, text, checked}`

**Do not change existing saved data**—just support IDs going forward.

---

If you paste (1) the **current Tenant Livewire property that stores the selected services** and (2) the Blade snippet that renders the checklist, I’ll rewrite the exact snapshot builder + exact display loop using your real variable names and file paths (still Tenant-only, addition-only).
