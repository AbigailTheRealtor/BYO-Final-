eplit Instructions — Reorder “Broker Compensation & Agency Agreement Terms” (Hire a Landlord’s Agent)
Goal

Rearrange the questions in Broker Compensation & Agency Agreement Terms to this exact order, and ensure the same order is used in:

Listing Creation (Tab: Broker Compensation & Agency Agreement Terms)

Listing View page (read-only display)

Agent Bid form (when an Agent bids on this listing)

Counter Bid form (Tenant or Agent countering)

Any “View Bid / View Counter Terms” screens

Any PDF / Accepted Bid Summary output (if this section is included)

Correct Field Order (Must Match Exactly)

Landlord’s Broker Lease Fee

Tenant's Broker Commission Structure (Residential only)

Tenant's Broker Commission Fee (Residential only)

Payment Timing for Broker Fees

Lease Renewal/Extension Fee

Expansion Commission for Lease Amendment (Commercial only)

Interested in Property Management

Interested in Offering a Lease-Option Agreement

Interested in Selling

Landlord’s Broker Purchase Fee

Protection Period Timeframe (Days)

Early Termination (Residential only)

Landlord Agency Agreement Timeframe

Acceptable Brokerage Relationship

Additional Terms

SAFEGUARDS (MUST FOLLOW — DO NOT BREAK ANYTHING)
Do NOT

Do NOT delete, rename, or alter any database columns, migrations, model fields, API keys, or JSON keys.

Do NOT change existing tooltips, placeholder text, labels, icons, validation rules, or conditional logic (Residential vs Commercial) except where required to preserve behavior after reordering.

Do NOT change any other flows (Hire a Tenant / Buyer / Seller) unless they share the exact same component and the change is scoped only to the Landlord flow.

Do NOT change how values are saved, calculated, or mapped—this task is presentation order only.

Must

Use a single shared ordering source (one config / constant) so create + bids + counters + display can’t drift out of sync again.

Preserve existing Residential-only and Commercial-only visibility logic exactly as it works today.

Implementation Plan (Recommended)
Step 1 — Identify the shared field keys

In code, each of the above fields already maps to some internal key (examples):

landlord_broker_lease_fee

tenant_broker_commission_structure

tenant_broker_commission_fee

payment_timing_broker_fees

lease_renewal_extension_fee

expansion_commission_lease_amendment

interested_property_management

interested_lease_option_agreement

interested_in_selling

landlord_broker_purchase_fee

protection_period_days

early_termination_fee

landlord_agency_agreement_timeframe

acceptable_brokerage_relationship

additional_terms

✅ Do not rename keys—just confirm what they are in your code.

Step 2 — Create ONE “order config” used everywhere

Create a config file (or constants section) like:

LANDLORD_COMP_TERMS_ORDER_RESIDENTIAL

LANDLORD_COMP_TERMS_ORDER_COMMERCIAL

Residential order includes the Residential-only fields and excludes Commercial-only:
1,2,3,4,5,7,8,9,10,11,12,13,14,15

Commercial order includes the Commercial-only field and excludes Residential-only:
1,4,5,6,7,8,9,10,11,13,14,15
(Notice: no Tenant’s Broker Commission Structure/Fee, no Early Termination)

✅ This keeps the UI from showing empty “not applicable” fields.

Step 3 — Update the Listing Creation UI to render fields by config

Where the form currently renders fields in a hard-coded order, change it to:

Determine listing subtype context:

If property type is Residential → use ORDER_RESIDENTIAL

If property type is Commercial → use ORDER_COMMERCIAL

Render the form fields by iterating the ordered list and calling the existing field component for each key.

Important: Reordering must not affect any existing onChange, validation, state binding, or save logic. You are only changing render order.

Step 4 — Apply the SAME config to Agent Bid + Counter Bid forms

Find the components used for:

Agent bid creation/edit

Tenant counter

Agent counter

“View Bid” / “View Counter Terms”

Anywhere this section is displayed, it must use the same ordered array.

✅ If your code duplicates the form in multiple places, refactor by extracting:

CompTermsFields({ mode, propertyType, values, onChange, readOnly })
…and pass readOnly=true where needed.

Step 5 — Update Listing View / Summary display order

Anywhere you display these values (read-only list, card, details page), switch to:

same config array

same conditional rules (Residential vs Commercial)

✅ This ensures the user sees the same order on creation and on the listing.

Step 6 — Confirm conditional visibility stays correct

Residential only

Tenant’s Broker Commission Structure

Tenant’s Broker Commission Fee

Early Termination

Commercial only

Expansion Commission for Lease Amendment

Safeguard behavior:

If a field is not applicable, it should not render at all (or remain hidden exactly as current behavior).

Acceptance Checklist (QA must pass)
Listing Creation (Tenant profile → Hire a Landlord’s Agent)

Fields appear in the exact order above

Residential listing shows Residential-only fields; Commercial listing shows Commercial-only field

Agent Bids

Bid form shows fields in same order

Agent “View Bid” shows same order

Counter Bids (both sides)

Tenant counter screen shows same order

Agent counter screen shows same order

“View Counter Terms” shows same order

Data Integrity

Values save exactly as before

No keys/columns changed

No tooltips/placeholders changed

Extra Safeguard (Prevents future drift)

Add a small dev-only test (or console assertion in dev mode):

Verify the rendered field keys match the config order exactly

If mismatch, log a clear error: "Comp Terms order mismatch — do not hardcode field order"