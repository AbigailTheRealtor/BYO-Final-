Replit Instructions — DELETE “Agent Bid Visibility Preference” From All 4 Listing Flows (UI + Validation Only)
Applies to (ALL):

Hire a Tenant’s Agent → Listing creation

Hire a Buyer’s Agent → Listing creation

Hire a Seller’s Agent → Listing creation

Hire a Landlord’s Agent → Listing creation

Goal:

Completely remove Agent Bid Visibility Preference from:

the UI form (Tab 1 / Listing Details)

any required validation tied to it

any front-end show/hide conditionals tied to auction_type/listing_type

✅ Do NOT remove database columns
✅ Do NOT remove models / migrations
✅ Do NOT rename keys (we’re removing usage, not refactoring)

0) SAFETY RULES (Must Follow Exactly)

DO NOT:

delete or modify any database column (even if it becomes unused)

delete migrations, models, controllers, or shared components

change any other tab or any other field’s logic

“cleanup” or “refactor” nearby markup

ONLY DO:

remove the exact UI block that renders “Agent Bid Visibility Preference”

remove the exact validation rule(s) that make it required / validated

remove any front-end JS that toggles it (if present)

1) UI REMOVAL — Delete the Field Block in All 4 Forms
Where:

In each of the 4 listing creation views/templates, go to:

Tab 1: Listing Details → locate the label/text:

“Agent Bid Visibility Preference”
(or similar variants like “Agent Visibility Preference”, “Bid Visibility Preference”)

Action:

Delete the entire field container (not just the label).
This includes:

the wrapping <div> / form-group container

the <label>

the <select> / <input>

help text / tooltip icon (if any)

the conditional @if (...) wrapper around it (if present)

✅ Result: the field is not rendered under any listing type (Traditional or Bidding Period).

Important: If the field appears in a shared partial (e.g., included Blade file used by all 4 flows), remove it once in the shared partial rather than 4 times—but only if it is clearly the same shared file.

2) VALIDATION REMOVAL — Remove “Required” and Any Validation Rules
Where to check:

Search the codebase for any of these (case-insensitive):

agent_bid_visibility

bid_visibility_preference

agent_visibility_preference

the exact name="____" used by that field

the request key used in validation

Action:

Remove:

any required attribute in HTML (if present)

any server-side validation rules like:

required

in:...

nullable|required combos

conditional validation tied to auction type

Examples of what must be removed (patterns):

@if (...) required @endif

$request->validate(['agent_bid_visibility_preference' => 'required|...'])

Rule::requiredIf(...) rules for this field

✅ Result: listings can be created with no reference to this field and no validation errors.

3) DISPLAY LAYERS — Remove It From “Actual Listing Display” and “Bid Terms” (If It Shows Anywhere)

Even if the creation field is removed, it may still show on:

listing summary pages

agent views

“Actual Bids Display → Private Terms”

admin views

Action:

Search for the same key/label and remove the display row(s), e.g.:

“Agent Bid Visibility Preference: ___”

✅ Result: users/agents never see this preference anywhere.

4) PRESERVE EXISTING DATA (No Wipes)

If there is logic that clears the value when auction type changes (examples):

JS like onChange auction_type => set field null

controller logic that sets it to null

mutators that overwrite it

Action:

Do not add new clearing logic.
If clearing logic exists, remove only the clearing logic for this field (leave other fields untouched).

✅ Result: any old records keep their stored value (even though it’s no longer used).

5) TEST PLAN (Must Run Exactly)

For each of the 4 listing flows:

Create listing → choose Traditional

Confirm: field is not present

Confirm: listing saves with no validation errors

Create listing → choose Bidding Period

Confirm: field is not present

Confirm: listing saves with no validation errors

Open any existing listing that previously had a stored value

Confirm: no UI shows it

Confirm: no crashes/errors (undefined index / trying to read missing field)