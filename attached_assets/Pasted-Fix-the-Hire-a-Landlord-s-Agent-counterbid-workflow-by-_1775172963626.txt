Fix the Hire a Landlord’s Agent counterbid workflow by matching the working Tenant workflow exactly.

Goals:
1. When a landlord counters an agent bid, the agent must receive a notification and dashboard notice.
2. When an agent counters a landlord bid, the landlord must receive a notification and dashboard notice.
3. Same requirement for accepted and rejected bids.
4. Notices must be bid-specific, not listing-wide.
5. Landlord response buttons must match Tenant colors exactly:
   - Accept green
   - Reject red
   - Counter blue
6. Counter forms must pre-populate exactly like Tenant:
   - Broker Compensation & Agency Agreement Terms
   - Additional Details
   - Offered Services
7. If landlord submitted the latest counter, agent sees landlord’s latest counter prefilled.
8. If agent submitted the latest counter, landlord sees agent’s latest counter prefilled.

Safeguards:
- Do not modify Tenant logic except to inspect/copy its working behavior.
- Do not rename DB columns or remove fields.
- Do not overwrite original bid data; preserve audit/history.
- Do not make notification logic listing-wide; keep everything bid_id-specific.
- Do not change labels, field order, tooltips, or property-type service catalogs.
- Do not use global CSS overrides; copy Tenant’s exact button classes into Landlord.
- Do not hydrate from listing-level fields when bid/counter-level fields are required.
- Filter prefilled services against the active landlord property-type catalog only.

Implementation steps:
1. Compare Tenant vs Landlord action handlers for counter, accept, reject.
2. Copy Tenant notification creation + recipient targeting + dashboard notice behavior into Landlord.
3. Compare Tenant dashboard queries and ensure landlord counters/accepts/rejects appear in the recipient dashboard and recent notices.
4. Copy Tenant’s exact response button classes into Landlord “Respond to this Counter Offer” section.
5. Compare Tenant counter form mount/hydration logic and copy the same prefill sequence into Landlord:
   - load bid thread
   - load latest counter state for that bid
   - hydrate broker comp/agreement
   - hydrate additional details
   - hydrate offered services
   - fallback appropriately if no counter exists
6. Ensure all child broker-comp fields populate, not just toggles.
7. Ensure services are filtered to the active landlord catalog before checking boxes.
8. Test with real scenarios:
   - landlord counters agent
   - agent counters landlord
   - accept
   - reject
   - multiple bids on same listing to confirm no cross-bid leakage

Deliverable:
Provide a summary of files changed, why each file changed, and confirmation that Landlord now matches Tenant for notifications, dashboard notices, counter prefill, and button colors.