REPLIT INSTRUCTIONS — QA + Fix Flow Issues (Hire a Tenant’s Agent Only)
Applies to:

BidYourAgent → Hire a Tenant’s Agent listing flow
Both listing formats:

Traditional (Residential + Commercial)

Bidding Period (Residential + Commercial)

Objective:

Run the full Hire a Tenant flow end-to-end and fix ONLY issues where:

checkboxes don’t reliably check/uncheck

conditional follow-up sections don’t appear/disappear correctly

fields are slow/laggy to update

selected values don’t save or don’t display in review/summary

the UI requires refresh to show changes

“Other” doesn’t show custom input

validation behaves inconsistently

🚨 SAFEGUARDS (MUST FOLLOW)

DO NOT:

delete/rename database columns, models, migrations, tables, or enums

change field names/keys used by forms, bids, counters, summaries, or PDFs

redesign UI, change fonts/colors, change icons, tooltips, or placeholder text

move fields to different tabs or reorder your flow

refactor unrelated code or introduce new frameworks

change Buyer/Seller/Landlord flows

change business rules (only fix broken behavior implementing the existing rules)

YOU MAY ONLY:

fix UI state bugs (checkbox toggle, conditional rendering, slow updates)

fix conditional logic wiring so follow-up boxes appear correctly

fix debouncing/throttling so inputs feel responsive

fix saving/loading issues where data doesn’t persist

add targeted logs for debugging (remove after)

add null-safe rendering so sections don’t disappear incorrectly

✅ REQUIRED QA TEST PLAN (MUST COMPLETE)
A) Test matrix (run all 4 paths)

Replit must test and fix bugs for each path:

Traditional → Residential

Traditional → Commercial

Bidding Period → Residential

Bidding Period → Commercial

Each must be tested from:

Create listing → Review listing → Agent bids → Counter flow → Accept → Summary → E-sign

B) Listing Creation QA (All Tabs)
1) Conditional fields must appear instantly and correctly

For every conditional question:

selecting a value that requires a follow-up must show the follow-up box

changing selection must hide/show the correct follow-up

clearing selection must hide the follow-up and clear dependent values (only if your intended logic already does that)

Example issues to fix:

User selects an option that should reveal a sub-box but it doesn’t show

It shows only after refresh

It shows but then disappears on next click

It’s delayed/slow due to excessive re-renders

✅ Fix by ensuring conditional rendering relies on the correct state source (one source of truth).

2) Checkbox services must toggle reliably (check/uncheck)

In Services Requested:

Check a service → it appears in “selected services”

Uncheck it → it is removed immediately

Check/uncheck multiple quickly → state remains accurate

If “Select all” exists → must work without lag

Common causes to fix:

stale state

array mutation vs immutable updates

key collisions in lists

debounced state updates too aggressive

3) “Other” option must show typed value everywhere

If user selects “Other”:

the UI must show the custom input text (not the word “Other”)

it must persist after saving and revisiting

it must show on listing display + bid display + summary

4) Performance / slowness

Fix any lag where:

typing has input delay

checkbox toggles freeze the UI

conditional boxes pop in seconds later

Allowed performance fixes:

reduce unnecessary re-renders (memoization where appropriate)

debounce only where needed (do not debounce checkbox toggles)

avoid recalculating large arrays every keystroke

ensure unique keys for rendered lists

C) Bid / Counter Bid QA
1) Bid forms must pre-fill correctly

When an Agent bids:

Broker Compensation & Agency Agreement Terms and Offered Services must auto-fill from Tenant’s latest terms (as you designed)

Agent edits should persist
When Tenant counters:

It must start from the Agent’s last bid

Tenant edits persist
Repeat until accepted.

2) Match Score (if implemented) must update correctly (display-only)

Full match if not edited

Not a match if edited

Highlight mismatched fields

Baseline always uses the other party’s latest version

✅ If the match score exists, ensure it uses the correct “latest counter” reference.

D) Accept Bid QA → Summary Generation → Notifications
1) Accept must always produce:

Accepted status

Accepted Bid Summary generated and accessible

Other party notified (in-app minimum)

2) Summary must accurately display:

Services grouped by category

Broker Comp values including $ and %

“Other” shows typed values

Date/time + timezone correct per signer device

IP captured properly (real client IP)

E) E-Sign QA (Tenant + Agent)

Checkbox must be required to sign

Signature name must save

Signed date/time must be correct and show immediately after signing

IP must show as captured value (not placeholder)

🔧 DEBUGGING REQUIREMENTS (HOW TO FIND ISSUES)

Replit must do the following while testing each path:

Add temporary console/server logs for:

conditional field state values

services selected array state

form submission payload

saved DB values

reload and verify values persisted

Confirm no duplicate state sources exist:

If form data is stored in both component state + backend session + DB, ensure rendering reads from the correct canonical source consistently.

Confirm key uniqueness in checkbox lists:

Each service item must use a stable unique key (service ID), not label text.

✅ DELIVERABLES REQUIRED FROM REPLIT (SO YOU CAN VERIFY)

After fixes, Replit must provide:

1) A checklist report (4 flows)

For each of the 4 paths (Traditional Res, Traditional Com, Bidding Res, Bidding Com), they must confirm:

Conditional fields show/hide correctly

Services check/uncheck works

All entered values persist after save + reload

No lag

Bid/counter fill logic works

Accept generates summary + notification

E-sign saves signature/date/time/ip correctly

2) List of files touched

So you can confirm the scope stayed limited.

✅ “DO NOT OVERREACH” NOTE TO REPLIT

This task is NOT a redesign.
This is a stability + reliability QA pass for Hire a Tenant only.