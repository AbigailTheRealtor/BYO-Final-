TASK: Re-audit HOA Approval conditional layout — Seller + Landlord still broken

Issue:
The previous fixes did NOT fully resolve the HOA / Association layout break.

Both screenshots still show the same structural collapse after the approval select is set to Yes:

Seller:
- Association Approval Required for Purchase = Yes
- Approval Process Details label appears, but its input is missing/collapsed
- Association Application Fee label appears, but its input is missing/collapsed
- Following HOA fields shift upward

Landlord:
- Association Approval Required for Tenancy = Yes
- Approval Process Details label appears, but its input is missing/collapsed
- Some following fields now render, but spacing/structure is still wrong
- This confirms the issue is deeper than only removing one stray col-12 wrapper

Files to audit/fix:
1. resources/views/livewire/offer-listing/offer-seller-tabs/commission-based/tax-legal-hoa-disclosures.blade.php
2. resources/views/livewire/offer-listing/offer-landlord-tabs/commission-based/tax-legal-hoa-disclosures.blade.php

Safeguards:
- Full Service Create Offer Listing Seller + Landlord HOA partials ONLY.
- Do NOT touch Buyer, Tenant, Hire Agent, Edit, Draft, Counter, or Limited Service.
- Do NOT change PHP, validation, database, routes, models, controllers, save/load logic.
- Do NOT make global CSS or global Select2 changes.
- Surgical Blade structure fix only.

Audit requirement:
Compare the broken conditional blocks line-by-line against a known working field pattern in the same file.

Specifically inspect the @if approval == Yes block for:
- Approval Process Details
- Association Application Fee

Find why labels/icons render but inputs are missing/collapsed.

Likely causes:
- malformed input-cover wrapper
- icon placed outside/inside wrong div
- select/input missing after icon
- unclosed div before the input
- input accidentally inside a hidden wrapper
- row/col imbalance
- duplicate/incorrect wrapper ID
- conditional @if closing in wrong location
- style/display collapse on input-cover
- input class missing form-control
- typo in variable/wire:model causing Livewire morph issue

Required fix:
For both Seller and Landlord, make these two conditional fields use the exact normal field pattern:

Approval Process Details:
- form-group full width
- label
- input-cover.has-icon
- icon
- visible form-control text input or textarea
- correct wire:model
- placeholder visible

Association Application Fee:
- form-group full width
- label
- input-cover input-group style
- dollar prepend visible
- visible form-control input
- correct wire:model
- placeholder visible

Do NOT leave a label with only an icon.
Do NOT use hidden/collapsed wrappers.
Do NOT rely only on CSS bandaids.

Done looks like:
With HOA = Yes and approval = Yes:

Seller:
- Approval Process Details input is visible
- Association Application Fee input is visible
- What Does the Association Fee Include is full width and properly spaced
- Association Amenities is full width and properly spaced
- Leasing / Rental Restrictions is full width and properly spaced

Landlord:
- Approval Process Details input is visible
- Association Application Fee input is visible
- What Does the Association Fee Include is full width and properly spaced
- Association Amenities is full width and properly spaced
- Leasing / Rental Restrictions is full width and properly spaced

Verification:
- Test Seller with approval = Yes
- Test Landlord with approval = Yes
- Test toggling approval Yes → No/Unknown → Yes again
- Confirm no collapsed labels/icons remain
- Confirm only the two listed Blade files changed