**Task: Rebuild the main user dashboard only ([tenant@exp.com-style](mailto:tenant@exp.com-style) logged-in dashboard) into a clean control center for Tenant, Buyer, Seller, and Landlord — without touching listing/bid business logic**

## Goal

Revamp the **main logged-in user dashboard** so it becomes the proper front door for all 4 user roles:

* Tenant
* Buyer
* Seller
* Landlord

This dashboard should help the user:

* create listings
* view their listings
* view bids on their listings
* see items needing attention
* open counter terms
* open accepted bid summaries
* open acknowledgement/signature pages
* see recent notices/activity

## Critical rule

This is a **dashboard-only task**.

### Do NOT modify:

* listing creation logic
* listing edit logic
* draft logic
* bid submission logic
* bid edit logic
* counter logic
* accept/reject logic
* accepted summary generation
* acknowledgement/signature business logic
* agent bid pages
* listing detail pages
* summary services
* controllers/models unless absolutely required for dashboard read-only data loading

The dashboard should be a **UI/navigation layer** that points into the already-working flows.

---

## Hard safeguards

1. **Do not touch listing creation forms**
2. **Do not touch listing edit forms**
3. **Do not touch bid creation/edit/counter/accept/reject logic**
4. **Do not touch accepted summary services**
5. **Do not touch acknowledgement/signature logic**
6. **Do not change routes unless absolutely necessary for dashboard navigation**
7. **Do not rewrite business logic**
8. **Prefer read-only queries and existing routes/pages**
9. **Audit first, edit second**
10. **If a dashboard feature would require changing core business logic, stop and report it instead**

---

# PHASE 1 — READ-ONLY AUDIT FIRST

**Do not edit yet.**

Audit the current main logged-in user dashboard that a normal user sees after sign-in.

## A. Identify current dashboard source of truth

Report:

1. exact view/component/controller powering the current main dashboard
2. exact sidebar partial used
3. exact widgets/cards currently shown
4. which parts are current vs old/legacy

## B. Audit existing dashboard sections

Audit the current sections/pages reachable from this user dashboard:

* Dashboard
* Hire Tenant’s Agent
* Hire Landlord’s Agent
* Hire Buyer’s Agent
* Hire Seller’s Agent
* My Bids
* Connections
* Messages
* Profile Settings

For each:

1. current route
2. current controller/view
3. whether it is still useful
4. whether it belongs in the revamped dashboard
5. whether it is old/legacy/stale

## C. Determine safest dashboard data sources

Without changing business logic, identify the best existing source-of-truth pages/routes/data for:

* active listings by role
* bids needing review
* counter terms pending
* accepted deals / summaries
* recent notices
* listings by status
* quick actions

The dashboard should reuse existing safe pages, not duplicate or reimplement workflow logic.

## D. Recommend a new dashboard structure

Design the dashboard as a clean user control center.

I want your proposed structure before coding.

Recommended sections should likely include:

1. **Quick Actions**

   * Create New Listing
   * View My Listings
   * View Bids Requiring Action
   * View Counter Terms
   * View Accepted Summaries

2. **Listings Overview**

   * Tenant listings count
   * Buyer listings count
   * Seller listings count
   * Landlord listings count
   * status chips (Live / Draft / Accepted / Expired if already supported)

3. **Needs Attention**

   * bids awaiting decision
   * pending counter terms
   * pending acknowledgements/signatures
   * accepted summaries awaiting completion

4. **Recent Activity / Notices**

   * recent bid submitted
   * counter received
   * bid accepted
   * summary ready to sign

5. **Role-based Listing Panels**

   * Hire Tenant’s Agent
   * Hire Landlord’s Agent
   * Hire Buyer’s Agent
   * Hire Seller’s Agent
     each with a cleaner summary card + button to open full list

## E. Preservation rule

The dashboard should only:

* summarize
* link
* filter
* route

It should not become a second workflow engine.

---

# PHASE 2 — REPORT BEFORE CODING

Before changing anything, reply with:

## 1. Current dashboard source files

* exact files/components/views
* what is old vs current

## 2. Proposed new dashboard structure

Show the exact sections/cards you recommend

## 3. Existing routes/pages to reuse

List the existing pages you will point the dashboard to for:

* listings
* bids
* counters
* summaries
* signatures

## 4. Minimal implementation plan

Explain how you will revamp the dashboard **without touching core listing/bid logic**

**Do not code before giving this report.**

---

# PHASE 3 — IMPLEMENT DASHBOARD-ONLY REVAMP

After the audit/report, update only the user dashboard shell.

## Required outcome

The main user dashboard should become a clean command center.

### Keep sidebar, but modernize what matters

You may keep the role sections, but improve the dashboard center panel so it clearly shows:

## A. Quick Actions

Buttons/cards for:

* Create Listing
* My Listings
* My Bids
* Pending Counter Terms
* Accepted Summaries / Signatures

## B. Listings by Role

Show simple counts/cards for:

* Tenant listings
* Landlord listings
* Buyer listings
* Seller listings

Each card should have:

* count
* status snapshot
* button to open full list

## C. Needs Attention

Show actionable items only, such as:

* bids awaiting decision
* counters awaiting response
* accepted summaries awaiting signature
* listings with active bids

## D. Recent Notices / Activity

Keep this useful and compact.
No clutter.

## E. Preserve navigation to current working pages

Every button/card should route to already-working current pages.
Do not invent new workflow pages unless absolutely necessary.

---

# UI direction

Use the current dashboard layout as the base, but make it feel like a proper operating dashboard.

### Strong preference

* cleaner center panel
* more actionable summary cards
* fewer stale/empty sections
* emphasize “what needs action now”
* preserve existing role list pages as drill-down destinations

### Avoid

* empty widgets
* stale placeholder blocks
* duplicating data already visible elsewhere
* heavy logic in the Blade view
* touching core bid/listing flows

---

# Explicitly do NOT touch in this task

* create/edit listing forms
* modals in listing pages
* bid cards in listing detail pages
* accepted summary HTML builders
* counter term pages
* agent dashboards
* agent bidding list pages
* messaging/connections internals unless just linking to them from dashboard

---

# QA after changes

1. user dashboard loads correctly
2. quick actions go to correct pages
3. all 4 role listing links still work
4. bids needing action open correctly
5. counter term links open correctly
6. accepted summary / sign links open correctly
7. no existing listing/bid/summaries logic changed
8. no regressions to working flows

---

# Output required after coding

1. exact files changed
2. exact views/components changed
3. what dashboard sections were removed, kept, or redesigned
4. what existing routes/pages are now linked from the dashboard
5. confirmation that no core listing/bid/counter/summary logic was modified

## Stop rule

If making the dashboard smarter would require touching business logic, stop and report the exact blocker rather than changing core flows.
