Here are **copy-paste Replit instructions with strong safeguards** for this exact **Hire a Landlord Agent** fix only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A LANDLORD AGENT ONLY — DESIRED LEASE TERM EDIT REPOPULATION + “OTHER” FLASH/DELETE FIX

## SCOPE

Fix **Desired Lease Term** for **Hire a Landlord Agent only**.

### Current bugs

1. When user clicks **Edit**, the selected **Desired Lease Term** options that are already on the listing do **not** show correctly.
2. If the user clicks **Other**, the custom input box **flashes and then deletes/disappears**.
3. The field should behave like working Landlord fields such as **Rent Includes**, where:

   * previously selected options repopulate correctly on edit
   * the `Other` custom input stays visible when applicable
   * the `Other` custom value remains saved and visible

### Required behavior

For **Landlord Desired Lease Term only**:

* previously selected options must appear on edit
* if `Other` is selected, the custom input must stay visible
* if a custom `Other` value was saved, it must repopulate on edit
* the field behavior should match the stable behavior of **Rent Includes**

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Landlord Agent flow.**
2. **Do not modify Tenant, Buyer, or Seller flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change Select2 initialization globally.**
10. **Do not change selection box format, layout, styling, placeholder behavior, width, badge style, or dropdown behavior.**
11. **Do not change tooltips, icons, field labels, field order, or placeholders.**
12. **Do not do a broad cleanup or refactor.**
13. **Do not touch any other Landlord fields except the exact Desired Lease Term field and its custom `Other` input behavior.**
14. **Keep this surgical and field-specific.**
15. **Use Rent Includes as the reference pattern, but do not change Rent Includes itself.**

---

## IMPORTANT

We have had issues before with:

* the wrong file getting edited
* Select2/multiselect behavior breaking
* `Other` inputs flashing and disappearing due to Livewire re-render issues
* saved selections not repopulating on edit

Because of that:

### Hard stop:

Before editing anything, first confirm the **actual active Landlord files** for:

* Landlord create/edit form
* Landlord field for **Desired Lease Term**
* Landlord custom `Other` input for Desired Lease Term
* Landlord field for **Rent Includes** (reference only, not to edit unless absolutely necessary)
* any JS/Livewire hydration code affecting Desired Lease Term on edit

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap changes so they affect **Landlord only**.

---

## STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Landlord Agent**:

1. Livewire/component file handling Landlord create/edit
2. Blade/partial rendering **Desired Lease Term**
3. Blade/partial rendering the custom `Other` input for **Desired Lease Term**
4. File/partial/JS responsible for edit-state repopulation of **Desired Lease Term**
5. Blade/partial rendering **Rent Includes** (reference pattern only)

### Requirement

Return the exact files first, then edit **only those files**.

### Hard stop

**No other file should be edited or changed.**

---

## STEP 2 — TRACE WHY DESIRED LEASE TERM DOES NOT REPOPULATE ON EDIT

## Current bug

When user clicks **Edit**, the selections already on the listing do not appear correctly in **Desired Lease Term**.

## Required investigation

Trace the exact issue for **Desired Lease Term only**:

* exact `wire:model`
* whether values are stored as array / JSON / string
* how values are hydrated in edit mode
* how selected values are re-applied to the field
* whether the field is re-rendering before selections are restored
* whether the edit page is reading the wrong saved value
* whether the field is missing the same stable repopulation pattern used by **Rent Includes**

### Important

Do not guess.
Find the exact gap between saved value → edit hydration → displayed selections.

---

## STEP 3 — MAKE DESIRED LEASE TERM REPOPULATE LIKE RENT INCLUDES

Use **Rent Includes** as the working reference pattern.

For **Desired Lease Term only**:

* apply the same style of repopulation/hydration behavior used by Rent Includes
* previously selected options must show on edit
* if `Other` was selected, it must stay selected
* if custom `Other` text was saved, it must repopulate

### Important

Do **not** change Rent Includes behavior.
Do **not** copy code blindly into shared/global logic.
Only make Desired Lease Term behave consistently with it.

---

## STEP 4 — FIX THE “OTHER” INPUT FLASHES AND DELETES BUG

## Current bug

When user clicks `Other` for **Desired Lease Term**, the custom input box flashes briefly and then disappears.

This usually means:

* the field is controlled only by a JS change event
* Livewire re-renders and wipes the visibility state
* or the visibility condition is not tied to the saved/current model value

## Required behavior

For **Desired Lease Term only**:

* if `Other` is selected, the custom input must remain visible
* if `Other` is already saved, the custom input must be visible on edit
* the custom input must not disappear after Livewire updates/re-renders

### Required fix approach

Make the custom input visibility depend on the **actual saved/current field state**, not just a temporary front-end event.

The visibility pattern should match a stable working field like **Rent Includes** if applicable.

### Important

Do not rewrite all `Other` logic globally.
Fix only the Desired Lease Term path.

---

## STEP 5 — PRESERVE SAVE + EDIT + DISPLAY BEHAVIOR

After the fix, **Desired Lease Term** must work end-to-end:

* select options on create
* select `Other` if needed
* save
* reopen/edit
* options still show
* `Other` stays visible
* custom value stays visible

If the listing display already shows Desired Lease Term correctly, do not change that unless absolutely necessary.

---

## STEP 6 — PRESERVE FIELD FORMAT

This is critical.

For **Desired Lease Term**:

* do **not** change the selection input format
* do **not** change Select2 structure
* do **not** change placeholder behavior
* do **not** change width/layout
* do **not** change badge/tag appearance
* do **not** change options list wording
* do **not** change labels or tooltips

Only fix:

* repopulation on edit
* `Other` input persistence/visibility

---

## STEP 7 — PRESERVE BACKWARD COMPATIBILITY

Older Landlord records that already saved:

* Desired Lease Term selections
* Desired Lease Term `Other` values

must still:

* load correctly on edit
* not require migrations
* not lose data
* not break existing saved listings

---

## STEP 8 — TEST THESE EXACT CASES

### A. Desired Lease Term saved selections

* save listing with multiple Desired Lease Term selections
* click Edit
* expected: previously selected options show correctly

### B. Desired Lease Term with `Other`

* select `Other`
* expected: custom input appears and stays visible
* save custom value
* click Edit
* expected: `Other` remains selected
* expected: custom input remains visible
* expected: custom value is populated

### C. Compare behavior to Rent Includes

* expected: Desired Lease Term now behaves like Rent Includes in terms of repopulation and stable `Other` input handling

---

## STEP 9 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Tenant files changed
* no Buyer files changed
* no Seller files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Landlord fields changed
* Rent Includes itself was not changed unless absolutely necessary and Landlord-only wrapped

---

## STEP 10 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Landlord files were identified first
3. exact root cause for:

   * Desired Lease Term not showing saved options on edit
   * `Other` flashing and disappearing
4. exact fix applied
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof Desired Lease Term now behaves like Rent Includes on edit
8. proof `Other` stays visible and repopulates correctly

---

## IMPORTANT FINAL RULE

This is a **Landlord-only, surgical fix**.

Do **not**:

* edit unrelated files
* change selection box format
* refactor global select logic
* touch Tenant, Buyer, or Seller
* make broad UI changes

The final result should be:

* Desired Lease Term selections show correctly on edit
* `Other` no longer flashes and disappears
* custom `Other` value repopulates correctly
* field behaves like Rent Includes
* no other file or selection input behavior is changed

---

I can also make you a **shorter hard-stop version** if you want something even tighter for Replit.
