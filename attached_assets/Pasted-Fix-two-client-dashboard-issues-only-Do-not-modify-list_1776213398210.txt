Fix two client dashboard issues only. Do not modify listing creation flows, bid logic, counter logic, match score logic, Accepted Bid Summary logic, or database schema. This is a navigation/count consistency task only.

ISSUE 1 — Buyer count mismatch

Current bug:
- The Buyer sidebar badge count is still not matching the Buyer listings page count the user sees after clicking the Buyer tab.
- The Buyer role must follow the same consistency rule as Tenant, Landlord, and Seller:
  the sidebar/dashboard count must match the default count shown on the destination page.

Required fix:
1. Audit the Buyer listings page default filter and determine the exact query used to populate the current default dropdown count shown on page load.
2. Make the Buyer sidebar red badge and any Buyer dashboard summary count use that exact same filter/query.
3. Do not use a broader all-status count for Buyer if the page defaults to a narrower filtered count.
4. After the fix, clicking Hire Buyer’s Agent should show the same count the user saw in the sidebar/dashboard.

ISSUE 2 — “My Listings” should open ALL listings, not dashboard

Current bug:
- Clicking “My Listings” goes to the dashboard.
- That feels wrong because “My Listings” should open a listings page, not loop back to the dashboard.

Required behavior:
1. “My Listings” must open an ALL LISTINGS page/view for the logged-in user.
2. That page should show every listing the user has created across:
   - Tenant
   - Landlord
   - Buyer
   - Seller
3. This page can be a simple grouped overview page, grouped by role/type, using existing data sources.
4. Each listing entry should include:
   - listing type
   - listing title / identifier
   - status
   - bid count if available
   - action button(s) to open the correct existing role-specific listing page
5. Do not merge or replace the underlying role-specific pages.
6. Do not default to Tenant.
7. Do not send “My Listings” back to dashboard.
8. “My Listings” should be a true all-listings hub for the user.

Routing rule:
- Quick Action “My Listings” should route to this new all-listings page/view.
- Sidebar role tabs (Hire Tenant’s Agent, Hire Landlord’s Agent, Hire Buyer’s Agent, Hire Seller’s Agent) should remain unchanged and continue going to their existing role-specific pages.

CRITICAL SAFEGUARD:
- Do not modify Hire a Tenant’s Agent, Hire a Landlord’s Agent, Hire a Buyer’s Agent, or Hire a Seller’s Agent listing creation flows
- Do not modify bid submission, counter, accept/reject, match score, Accepted Bid Summary, or messaging logic
- Do not rename routes, controllers, models, tables, or columns unless absolutely necessary for the new all-listings page route/view
- Do not replace existing role-specific pages
- This task is limited to:
  1. fixing Buyer count consistency
  2. making “My Listings” open a true all-listings page

Acceptance criteria:
- Buyer sidebar/dashboard count matches the default Buyer page count
- Clicking “My Listings” no longer goes to dashboard
- Clicking “My Listings” opens a page showing all user-created listings across Tenant, Landlord, Buyer, and Seller
- Each listing on that page links to the correct existing role-specific destination
- No listing creation flow or bid logic is changed