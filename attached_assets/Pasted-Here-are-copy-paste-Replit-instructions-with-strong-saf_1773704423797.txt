Here are **copy-paste Replit instructions with strong safeguards** for these exact **Hire a Landlord Agent** edits only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A LANDLORD AGENT ONLY — DESIRED LEASE TERM EDIT REPOPULATION + LEASING TERMS LISTING SPACING FIX

## SCOPE

Make these fixes **only for Hire a Landlord Agent**:

1. **Desired Lease Term**

   * when user clicks **Edit**, the previously selected options must show correctly
   * selected options must not disappear
   * if this field includes an `Other` option and a custom value was saved, preserve that too

2. **Leasing Terms spacing on the actual listing**

   * the questions/rows are not evenly spaced
   * fix the spacing/layout on the **actual Landlord listing display only**

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Landlord Agent flow.**
2. **Do not modify Tenant, Buyer, or Seller flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change Select2 initialization.**
10. **Do not change selection box format, layout, styling, placeholder behavior, width, badge style, or dropdown behavior.**
11. **Do not change tooltips, icons, field labels, field order, or placeholders.**
12. **Do not do a broad cleanup or refactor.**
13. **Do not touch any other Landlord fields except the exact Desired Lease Term field and the listing-display spacing for Leasing Terms.**
14. **Keep this surgical and field-specific.**

---

## IMPORTANT

We have had issues before with Replit editing the wrong file or breaking selection inputs.

### Hard stop:

Before editing anything, first confirm the **actual active Landlord files** for:

* Landlord create/edit form
* Landlord field for **Desired Lease Term**
* Landlord listing display
* Landlord **Leasing Terms** listing section

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap changes so they affect **Landlord only**.

---

## STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Landlord Agent**:

1. Livewire/component file handling Landlord create/edit
2. Blade/partial rendering **Desired Lease Term**
3. File/partial responsible for edit-state repopulation of **Desired Lease Term**
4. Blade/partial rendering the **actual Landlord listing display**
5. Blade/partial rendering the **Leasing Terms** section on the Landlord listing display

### Requirement

Return the exact files first, then edit **only those files**.

### Hard stop

**No other file should be edited or changed.**

---

## STEP 2 — FIX DESIRED LEASE TERM SO SELECTED OPTIONS SHOW ON EDIT

## Current bug

When user clicks **Edit**, the selected **Desired Lease Term** options do not show.

## Required behavior

For **Landlord Desired Lease Term only**:

* previously selected options must appear when editing
* if `Other` is part of this field and a custom value exists, preserve that too
* do not change the selection box format
* do not change Select2 behavior globally
* do not break save logic

## What to trace and fix

Confirm:

* exact `wire:model`
* how the values are stored
* whether they are stored as array / JSON / string
* how the values are decoded/hydrated for edit mode
* how selected options are re-applied on load
* whether the edit page is reading the correct saved value
* whether the field is failing because the DOM/select is not being rehydrated with saved selections

### Important

Use the same working repopulation pattern already used by Landlord/Tenant fields that correctly reload saved selections.
Do **not** do a broad Select2 rewrite.

---

## STEP 3 — PRESERVE “OTHER” IF APPLICABLE

If **Desired Lease Term** includes an `Other` selection:

* if the saved values include `Other`, keep it selected on edit
* if there is a separate custom text value, repopulate it
* do not change the field format
* do not affect other fields

If there is no `Other` branch for this field, do not invent one.

---

## STEP 4 — FIX LEASING TERMS SPACING ON THE ACTUAL LANDLORD LISTING

## Current bug

On the actual Landlord listing, the **Leasing Terms** rows/questions are not evenly spaced.

The screenshot shows inconsistent vertical spacing and extra separator lines under the Leasing Terms content.

## Required behavior

On the **Landlord listing display only**:

* make Leasing Terms rows evenly spaced
* use consistent vertical spacing between each row
* keep labels and values aligned cleanly
* remove any unnecessary stacked empty line separators/gaps under this section if they are part of the bug
* do not change create/edit form spacing
* do not change field content
* do not affect other sections unless necessary to match the same Landlord listing style

### Important

This is a **listing display layout fix only**.

Do **not**:

* change stored values
* change labels
* change form markup outside what is necessary for display spacing
* affect Tenant, Buyer, or Seller listing spacing

---

## STEP 5 — MATCH THE CLEANER LISTING DISPLAY PATTERN

If another listing type or another section in Landlord already uses a cleaner row layout:

* mirror that spacing pattern for **Leasing Terms** only

Use consistent spacing classes / wrappers / margins / padding.

Do not redesign the whole listing page.

---

## STEP 6 — PRESERVE BACKWARD COMPATIBILITY

Older Landlord records that already have:

* Desired Lease Term selections saved
* existing Leasing Terms content

must still:

* load correctly on edit
* display correctly on listing
* not require migrations
* not break existing saved data

---

## STEP 7 — TEST THESE EXACT CASES

### A. Desired Lease Term

* save selected options
* click Edit
* expected: previously selected options show correctly
* expected: nothing disappears

### B. Desired Lease Term with `Other` if applicable

* save selection including `Other`
* click Edit
* expected: `Other` stays selected
* expected: custom value repopulates if that field exists

### C. Leasing Terms listing spacing

* open actual Landlord listing
* expected: rows are evenly spaced
* expected: no awkward large gaps or stacked empty lines
* expected: labels/values align cleanly

---

## STEP 8 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Tenant files changed
* no Buyer files changed
* no Seller files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Landlord fields changed

---

## STEP 9 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Landlord files were identified first
3. exact root cause for:

   * Desired Lease Term selections not showing on edit
   * Leasing Terms uneven spacing on actual listing
4. exact fix applied for each
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof Desired Lease Term now repopulates correctly on edit
8. proof Leasing Terms spacing is now even on the Landlord listing display

---

## IMPORTANT FINAL RULE

This is a **Landlord-only, surgical fix**.

Do **not**:

* edit unrelated files
* change selection box format
* refactor global select logic
* touch Tenant, Buyer, or Seller
* make broad UI changes

The final result should be:

* Desired Lease Term selections show correctly on edit
* Leasing Terms rows are evenly spaced on the actual Landlord listing
* no other file or selection input behavior is changed

---

If you want, I can also make this into a **shorter hard-stop version** for Replit.
