Yes — and you already did the hard part by giving the **gold list** and the **actual output**. Here are **Replit instructions with safeguards** to fix the 3 real issues I see:

### What’s wrong on the live listing right now (exactly)

1. **Two marketing items are missing** in the first category:

   * ✅ **Post the property on Craigslist under the “Multi-Family for Sale” category** (missing)
   * ✅ **Create a branded flyer with key rental data…** is present, but the Craigslist line should be **right after** it.

2. **One showings item is missing**:

   * ✅ **Coordinate showings with Buyer’s Agents** (missing)

3. **One contract/closing item is missing**:

   * ✅ **Manage communication with the Buyer’s Agent or Buyers** (missing)
   * ✅ **Coordinate with the Buyer’s Agent, Buyer’s Lender, Title, Escrow, and/or Attorney to prepare for Closing** (missing)

4. **Digital Photo Enhancements** is not formatted correctly:

   * Live listing shows the enhancement options as if they’re normal services.
   * It should show:

     * “Provide digital photo enhancements”
     * Then a sub-bullet line: “Select enhancement types:”
     * Then the selected enhancement types as indented bullets
     * Then **Other: [user text]** only if Other selected + text provided

---

# REPLIT INSTRUCTIONS (COPY/PASTE) — HIRE A SELLER’S AGENT → INCOME → SERVICES DISPLAY ORDER + MISSING ITEMS

## Test Account

Login: **[tenant@exp.com](mailto:tenant@exp.com)**
Flow: **Hire a Seller’s Agent → Income → create listing → select services → submit → view live listing → edit → ensure services persist and display correctly**

---

## 1) Establish ONE Source of Truth for “Income Services Master List” (Order Locked)

### Create a dedicated config for Income Seller services in the exact order you provided.

Add file (or update existing):
**/config/seller_services.php** (or wherever service lists are stored)

Structure must be **ordered arrays** (NOT associative objects that can reorder).

Example structure:

* `income => [ category_blocks ]`
* Each block has:

  * `category_title`
  * `items` (ordered)
  * special item types (see digital enhancements below)

✅ Safeguard:

* **Do not** sort alphabetically anywhere.
* **Do not** use `ksort()` / `sort()` / `collect()->sort()` on services.
* The config order is the canonical order.

---

## 2) Fix Storage: Save Selected Services as an ORDERED ARRAY

### Current bug pattern:

Live listing is likely rendering from a “master list filtered by selected booleans” BUT some items are not included in the master list OR the mapping keys don’t match, so they never render.

### Required:

When saving services for Income listings:

* Save `selected_services` as a **JSON array of service IDs** (in the master-list order).
* If you currently store as booleans per service, KEEP that if needed, but also store the ordered array for display.

**DB Field (preferred):**

* `selected_services_ordered` JSON

On save (create & edit):

* Loop the master list IN ORDER and push the item if selected.
* Save the resulting ordered array.

✅ Safeguard:

* Rendering must use `selected_services_ordered` (not recomputed).
* This guarantees order never changes and missing items can be detected.

---

## 3) Fix Mapping Keys (Why specific items are missing)

The missing items are almost always caused by one of these:

* item exists in the creation UI but NOT in the display config
* item exists but with a slightly different label (string mismatch)
* item exists but with a different ID/key than what is stored

### REQUIRED FIX

Use stable service IDs (not raw strings) across:

* Creation checklist
* DB save
* Edit repopulation
* Live listing display

For the 4 missing services, confirm they exist in the Income master list **with IDs**:

### Must exist (Income → master list):

* `income_marketing_craigslist_multifamily_for_sale`
* `income_showings_coordinate_with_buyer_agents`
* `income_contract_manage_communication_with_buyer_agent_or_buyers`
* `income_closing_coordinate_with_buyer_lender_title_escrow_attorney`

✅ Safeguard:

* If you rely on matching raw text, you will keep breaking this. IDs must be canonical.

---

## 4) Render Logic: Live Listing Must Use the Master Order + Selected Ordered Array

In the live listing Blade/view:

* Load `selected_services_ordered`
* Group them by category using the master list’s category mapping
* Render categories in the master order
* Render items in the stored order

✅ Safeguard:

* Do not render from “whatever is selected in the DB” without ordering. JSON arrays preserve order; DB queries do not unless ordered.

---

## 5) SPECIAL CASE: “Provide digital photo enhancements” must display as a parent item + nested enhancement types

Right now your listing shows:

* Provide digital photo enhancements
* Basic edits
* Twilight conversion
* Object removal
* etc.

That is incorrect. It must display like this:

* **Provide digital photo enhancements**

  * **Select enhancement types:**

    * Basic edits (brightness, contrast, cropping)
    * Twilight conversion (convert daytime photo to sunset look)
    * Object removal (e.g., cars, trash cans, furniture, etc.)
    * Virtual twilight photography
    * Color correction or sky replacement
    * **Other: Example Custom enhancement** (only if Other chosen + text entered)

### Required data structure

Store enhancement types separately:

* `digital_enhancements_selected` JSON array
* `digital_enhancements_other_text` string nullable

✅ Safeguards:

* If user did NOT select “Provide digital photo enhancements”, do NOT show enhancement types.
* If user selected “Provide digital photo enhancements” but no types selected, show only the parent line.
* Only show “Other:” line if:

  * `digital_enhancements_selected` contains `other`
  * AND `digital_enhancements_other_text` is not empty

---

## 6) Edit Repopulation Safeguard (So it doesn’t vanish after edit)

In Edit component mount:

* Rehydrate:

  * `selected_services_ordered` → checkbox state
  * enhancement types + other text

If you store only the ordered array:

* Convert ordered array back into checkbox selected IDs for the UI.

✅ Safeguard:

* Do not reset arrays on every render.
* Only initialize on mount, not inside render().

---

# QUICK QA CHECKLIST (MUST PASS)

After fix, Income live listing must include these missing lines in correct positions:

### Marketing section must include:

✅ **Post the property on Craigslist under the “Multi-Family for Sale” category** (after branded flyer)

### Showings section must include:

✅ **Coordinate showings with Buyer’s Agents**

### Offer & Contract section must include:

✅ **Manage communication with the Buyer’s Agent or Buyers**

### Closing section must include:

✅ **Coordinate with the Buyer’s Agent, Buyer’s Lender, Title, Escrow, and/or Attorney to prepare for Closing**

### Digital photo enhancements must display nested:

✅ Parent + “Select enhancement types:” + indented bullets + “Other: text”

---

## If you want me to make this 100% plug-and-play

Paste (just once) the file paths you’re using for:

1. Seller Income **create** Livewire component
2. Seller Income **edit** Livewire component
3. Seller Income **live listing display** Blade/view
4. Where services are currently defined (config/array)

…and I’ll rewrite the above as **exact line-level instructions per file** (including the exact array structure for Income services + IDs) so you can hand it to Harry with zero interpretation.
