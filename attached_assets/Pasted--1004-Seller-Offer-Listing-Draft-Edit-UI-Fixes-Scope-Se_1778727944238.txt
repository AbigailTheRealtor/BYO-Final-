#1004 - Seller Offer Listing Draft/Edit UI Fixes

Scope:
Seller Offer Listing only:
- /offer-listing/seller
- /offer-listing/seller/edit/{id}

Do not touch:
- Buyer
- Tenant
- Landlord
- Hire Agent
- Limited Service
- routes
- controllers
- schema
- validation
- save logic
- stored meta keys

Important:
The Save Draft vs Save Edit issue is already fixed.
Do NOT rework draft/edit save behavior unless the audit proves a regression.

Remaining Issues To Fix

1. Next button not working after draft/edit load
Problem:
On Seller draft/edit listings, the Next button stops working after loading the listing.

Expected:
- Next/Back work normally after draft/edit loads
- Tab validation still works
- No duplicate event listeners
- No console errors

Audit:
Check wizard/tab event binding after:
- Livewire hydration
- draftLoaded events
- Select2 reinitialization

2. Business Property Type → Property Style placeholder missing
Problem:
When Property Type = Business, Property Style appears without the Select placeholder.

Expected:
- Empty Property Style field shows “Select”
- Saved values still reload correctly
- No duplicate Select2 containers

3. Property Style field visually broken on draft/edit
Problem:
The Property Style Select2 field is visually compressed/clipped.
The icon overlaps the selected value.
Example screenshot shows:
- “or More”
instead of:
- “Five or More”

Expected:
- Field height matches surrounding Select2 fields
- Icon does not overlap text
- Full selected value displays
- Clear “x” and dropdown arrow remain aligned
- Empty state still shows “Select”

Likely fix:
Audit:
- #property_style_select
- .input-cover.has-select-icon
- Select2 padding/height rules
- select2-selection--single spacing

4. Sewer Available to Site missing icon
Problem:
“Sewer Available to Site” select field is missing its icon, while the related “Other” input field icon appears correctly.

Expected:
- Sewer Available to Site select shows the proper icon
- Use the existing:
  - has-icon
  - has-select-icon
  - data-icon
  - addIconsToInputs()
pattern already used by Water, Electric, Gas, and Telecom

Do not hardcode duplicate icons.

Done looks like:
- Next/Back work on Seller draft/edit listings
- Business Property Style empty state shows “Select”
- Property Style field height/layout matches other fields
- Full Property Style value displays correctly
- Icon no longer overlaps Property Style text
- Sewer Available to Site icon appears
- No duplicate Select2 containers
- No console errors
- Existing saved values remain intact