Got it — **ONLY these 2 fields** on **Hire a Buyer’s Agent** listings must render as **regular text (no bubble/pill)**:

1. **Maintenance / Repair Responsibility**
2. **Option Fee Credit Toward Purchase Price**

Here are the **copy/paste Replit instructions with safeguards**, scoped to just those two.

---

# Replit Instructions — Hire a Buyer’s Agent

## Remove Bubble/Pill Format for ONLY 2 Fields (Live Listing + Snapshot)

### Applies To

* Login: `tenant@exp.com`
* Flow: Dashboard → **Hire a Buyer’s Agent**
* Must apply to:

  * Live Listing Display (all Buyer listings)
  * Download Listing Snapshot (must match)

---

## Required Change (Buyer Listings Only)

### Field 1

**Maintenance / Repair Responsibility:**

* Render the value as **plain text** (example: `Seller`)
* **Do NOT** wrap the value in any badge/pill/chip UI

### Field 2

**Option Fee Credit Toward Purchase Price:**

* Render the value as **plain text** (example: `Yes` / `No`)
* **Do NOT** wrap the value in any badge/pill/chip UI

✅ Output format must be:

* `Maintenance / Repair Responsibility: Seller`
* `Option Fee Credit Toward Purchase Price: Yes`

---

## Implementation Steps

### 1) Update Live Listing Renderer

In the Buyer live listing display component/template where these fields are rendered:

* Locate the lines that output these values using a badge/pill (ex: `<span class="badge">...</span>`).
* For these **two labels only**, replace the badge wrapper with **plain text rendering**.

**Do not change how other fields render.**

### 2) Update Snapshot Renderer

If Snapshot uses a separate template/formatter:

* Apply the exact same two-field override so Snapshot matches Live Listing.

---

## Safeguards (Mandatory)

1. **Display-only change** — do not modify database fields/columns or stored values.
2. **Buyer-only** — do not affect Seller/Landlord/Tenant listing styles.
3. Do NOT remove bubble styling globally—only override these two labels/field keys.
4. Must work consistently across all Buyer listing variations (any property type / any conditional section).
5. Snapshot must match live listing output.

---

## QA Checklist

Using `tenant@exp.com`:

✅ Open multiple Buyer listings

* Both fields show plain text (no pills) every time

✅ Download Listing Snapshot

* Both fields show plain text (no pills)

---

If you paste the **exact code snippet** (just the small block where the badge is rendered for one of these fields), I can rewrite it into the exact replacement lines so your dev can implement it in seconds.
