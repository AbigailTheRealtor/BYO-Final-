Task: Implement Phase 1 only for a BidYourAgent referral partner tracking system.

GOAL:
Add the database foundation only. Do not build routes, controllers, services, dashboard UI, admin UI, signup hooks, listing hooks, or accepted-hire hooks yet.

CRITICAL SAFEGUARDS:
- Surgical changes only.
- Do not redesign anything.
- Do not rename or delete any existing tables or columns.
- Do not modify existing Buyer/Seller/Landlord/Tenant flows.
- Do not touch Livewire components, bidding logic, match scoring, timers, counters, summaries, PDFs, or dashboard behavior.
- Only add nullable columns and new tables.
- If the accepted hire source-of-truth table is unclear, inspect first and report before changing anything.

PHASE 1 — DATABASE ONLY

1. Add nullable columns to users table:
- referred_by_agent_id
- referral_source_code
- referral_captured_at

2. Add nullable columns to listings table:
- referring_agent_id
- referral_source_code
- referral_captured_at
- referral_locked boolean default false

3. Identify the true accepted hire / accepted bid source-of-truth table used by this codebase.
If clear, add nullable columns:
- referring_agent_id
- referral_source_code
- referral_status
- platform_referral_amount decimal(10,2) nullable
- partner_referral_amount decimal(10,2) nullable

If ambiguous, STOP and report the exact candidate tables/files instead of guessing.

4. Create table agent_referral_links:
- id
- agent_id
- code unique
- slug nullable
- is_active boolean default true
- click_count integer default 0
- signup_count integer default 0
- listing_count integer default 0
- hire_count integer default 0
- timestamps

5. Create table referral_visits:
- id
- agent_id
- referral_code
- session_id nullable
- ip_address nullable
- user_agent nullable text
- landing_url nullable text
- visitor_user_id nullable
- listing_id nullable
- converted_to_signup boolean default false
- converted_to_listing boolean default false
- converted_to_hire boolean default false
- timestamps

IMPLEMENTATION RULES:
- Prefer foreign keys only if they are clearly safe and consistent with the existing schema conventions.
- Match the project’s migration style and naming conventions.
- Do not guess table names; inspect first.
- After changes, report:
  1. migrations created
  2. exact tables altered
  3. any ambiguity found
  4. whether accepted-hire table was confirmed or deferred

DELIVERABLE:
Only Phase 1 migration work and a concise audit summary. No other code changes.