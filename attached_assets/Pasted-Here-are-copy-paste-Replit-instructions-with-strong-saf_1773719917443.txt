Here are **copy-paste Replit instructions with strong safeguards** for these exact **Hire a Seller’s Agent** fixes only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A SELLER’S AGENT ONLY — INCOME TOTAL SQFT DISPLAY + INCLUDED PROPERTY OR BUSINESS ASSETS SAVE/EDIT FIX + BUSINESS MEDIA OPTIONS ORDER FIX

## SCOPE

Make these fixes **only for Hire a Seller’s Agent**.

### Fix 1 — Seller Income Listing

**Total Sqft** is filled in but is **not showing up on the listing**.

### Fix 2 — Seller Income, Commercial, and Vacant Land

**Included Property or Business Assets**

* selected options must save correctly when user saves a **draft**
* selected options must save correctly when user saves **edits**
* if user selects **Other**, the custom input box must open
* on edit, the saved selections must repopulate correctly
* on edit, the saved custom `Other` value must repopulate correctly
* right now it is showing blank when user clicks **Edit Listing**

### Fix 3 — Seller Business Listings

For the listing display row:
**Provide digital enhancements to media assets**

The options are showing in the wrong order on the listing.

### Current incorrect order showing on listing:

* Basic edits (brightness, contrast, cropping)
* Object removal (e.g., cars, trash cans, furniture, etc.)
* Virtual twilight photography
* Object removal (e.g., cars, trash cans, furniture)
* Other input

### Correct order that must show on listing:

* Basic edits (brightness, contrast, cropping)
* Twilight conversion (convert daytime photo to sunset look)
* Object removal (e.g., cars, trash cans, furniture)
* Virtual twilight photography
* Color correction or sky replacement
* Other input

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Seller’s Agent flow.**
2. **Do not modify Tenant, Buyer, or Landlord flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change Select2 initialization.**
10. **Do not change selection box format, width, styling, placeholder behavior, badge style, dropdown behavior, or general UI structure.**
11. **Do not change field labels, tooltips, icons, placeholders, or field order in the form UI unless strictly required for these exact fixes.**
12. **Do not do a broad cleanup or refactor.**
13. **Do not touch any Seller fields other than the exact ones listed in this request.**
14. **Do not change unrelated listing display sections.**
15. **Keep this surgical and field-specific.**
16. **Make sure the correct active Seller files are identified first before editing anything.**

---

## IMPORTANT

We have had issues before with:

* the wrong file being edited
* saved values not repopulating on edit
* `Other` input boxes not showing
* listing display using the wrong value or wrong order
* shared logic being changed and breaking other flows

### Hard stop:

Before changing anything, first confirm the **actual active Seller files** for:

* Seller create/edit form
* Seller draft save path
* Seller edit hydration path
* Seller listing display
* Seller field for **Total Sqft**
* Seller field for **Included Property or Business Assets**
* Seller field/listing display for **Provide digital enhancements to media assets**

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap the change so it affects **Seller only**.

---

# STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Seller’s Agent**:

1. Livewire/component file handling Seller create/edit/draft save
2. Blade/partial rendering **Total Sqft**
3. Blade/partial rendering **Included Property or Business Assets**
4. File/partial responsible for save/update/hydration of **Included Property or Business Assets**
5. Blade/partial rendering the **actual Seller listing display**
6. File/partial responsible for the Business listing display row:
   **Provide digital enhancements to media assets**

### Requirement

Return the exact files first, then edit **only those files**.

### Hard stop

**No other file should be edited or changed.**

---

# PART 1 — SELLER INCOME LISTING — TOTAL SQFT FILLED BUT NOT SHOWING

## CURRENT BUG

On **Hire a Seller’s Agent Income Listing**, **Total Sqft** is filled in but does **not** show up on the listing.

## REQUIRED BEHAVIOR

For **Seller Income only**:

* if **Total Sqft** has a real saved value, it must show on the listing
* if it is empty, it should remain hidden like other empty fields

## WHAT TO TRACE AND FIX

Confirm:

* exact property used to store Total Sqft
* whether the create/edit form is saving the correct property
* whether the listing display is referencing the wrong property name or wrong conditional
* whether the display template is hiding the row incorrectly
* whether Income listing display is using a different key than save/edit logic

### Important

Do not change:

* the field label
* the form format
* other square-footage fields
* other listing types

Fix only the Seller Income listing display path for **Total Sqft**.

---

# PART 2 — SELLER INCOME / COMMERCIAL / VACANT LAND — INCLUDED PROPERTY OR BUSINESS ASSETS SAVE + EDIT + OTHER INPUT FIX

## CURRENT BUGS

For **Included Property or Business Assets** in:

* **Income**
* **Commercial**
* **Vacant Land**

the field is still broken:

* selected options do not persist correctly on draft save
* selected options do not persist correctly on edit save
* when user selects **Other**, the custom input box does not open correctly
* on edit, saved values show blank
* on edit, the `Other` custom value is not repopulating correctly

## REQUIRED BEHAVIOR

For **Seller Included Property or Business Assets only**:

* selected options must save on **draft**
* selected options must save on **edit/update**
* saved selections must repopulate correctly on edit
* if user selects **Other**, the custom input box must open immediately
* if `Other` was previously saved, the custom input must show automatically on edit
* the saved custom `Other` value must repopulate correctly
* listing display must show the saved values correctly
* if empty, the listing row should remain hidden

## WHAT TO TRACE AND FIX

Confirm:

* exact main property name for Included Property or Business Assets
* exact custom `Other` property name
* exact `wire:model`
* how values are stored (array / JSON / string)
* draft save path
* update/edit save path
* edit hydration path
* visibility logic for the `Other` input
* listing display path

### Important

Do not rewrite all `Other` logic globally.
Do not change the selection input format.
Fix only this exact Seller field path.

### Behavior requirement

This field must behave like the other working multi-select + `Other` fields that already:

* save selections
* repopulate on edit
* keep `Other` visible when selected

---

# PART 3 — SELLER BUSINESS LISTINGS — DIGITAL ENHANCEMENTS OPTIONS IN CORRECT ORDER ON LISTING

## CURRENT BUG

On **Seller Business Listings**, the listing row:

**Provide digital enhancements to media assets**

shows the correct general content but in the wrong order, and one item appears duplicated/inconsistent.

## REQUIRED BEHAVIOR

On the **listing display only**, the options must appear in this exact order:

1. **Basic edits (brightness, contrast, cropping)**
2. **Twilight conversion (convert daytime photo to sunset look)**
3. **Object removal (e.g., cars, trash cans, furniture)**
4. **Virtual twilight photography**
5. **Color correction or sky replacement**
6. **Other input**

## IMPORTANT

This is a **Seller Business listing display formatting/order fix**.

Do **not**:

* change the form option order unless the form itself is also wrong and absolutely needs to match
* change stored values
* rename the option values in the database unless absolutely necessary
* affect other listing types

## WHAT TO TRACE AND FIX

Confirm:

* how the selected values are stored
* how the listing display is generating the order
* whether it is using raw storage order instead of the intended canonical display order
* whether duplicate label mapping is causing the repeated Object Removal entry
* whether old label variants need a display-only normalization

### Best approach

Prefer a **display-order mapping** for Seller Business listing output:

* keep saved values intact
* normalize display labels if necessary
* render them in the correct canonical order

---

# STEP 2 — PRESERVE BACKWARD COMPATIBILITY

Older Seller records must still work.

This includes records with:

* Total Sqft already saved
* Included Property or Business Assets already saved
* `Other` custom values already saved
* old digital enhancement selections already saved

They must still:

* load correctly on edit
* display correctly on listing
* not require migrations
* not lose data

If old label variations exist for the Business media enhancements, use a **display-only normalization** if needed instead of changing stored data.

---

# STEP 3 — TEST THESE EXACT CASES

## A. Seller Income — Total Sqft

* fill in Total Sqft
* save listing
* expected: Total Sqft shows on listing

## B. Included Property or Business Assets — Draft save

* select multiple options
* save draft
* reopen draft
* expected: selections still show correctly

## C. Included Property or Business Assets — Edit save

* select options including `Other`
* enter custom value
* save
* click Edit
* expected: saved options still show correctly
* expected: `Other` stays selected
* expected: custom input shows and is populated

## D. Included Property or Business Assets — Listing display

* expected: saved values show correctly on listing
* expected: blank row does not show when field is empty

## E. Seller Business listing — Digital enhancements order

* save multiple enhancement options
* expected: listing shows them in this exact order:

  1. Basic edits (brightness, contrast, cropping)
  2. Twilight conversion (convert daytime photo to sunset look)
  3. Object removal (e.g., cars, trash cans, furniture)
  4. Virtual twilight photography
  5. Color correction or sky replacement
  6. Other input

---

# STEP 4 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Tenant files changed
* no Buyer files changed
* no Landlord files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Seller fields changed

---

# STEP 5 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Seller files were identified first
3. exact root cause for:

   * Total Sqft not showing on Income listing
   * Included Property or Business Assets not saving/repopulating correctly
   * `Other` input not opening/repopulating
   * Business media enhancements showing in wrong order
4. exact fix applied for each
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof Total Sqft now shows on Seller Income listing
8. proof Included Property or Business Assets now saves in draft and edit correctly
9. proof `Other` now opens and repopulates correctly
10. proof Business media enhancements now show in the correct order on listing

---

## IMPORTANT FINAL RULE

This is a **Seller-only, surgical fix**.

Do **not**:

* edit unrelated files
* change selection box format
* refactor global select logic
* touch Tenant, Buyer, or Landlord
* make broad UI changes

The final result should be:

* **Total Sqft** shows on Seller Income listing when filled
* **Included Property or Business Assets** saves correctly in draft and edit for Income, Commercial, and Vacant Land
* `Other` opens and repopulates correctly
* Seller Business listing shows media enhancement options in the correct order
* no other file or selection input behavior is changed

---

If you want, I can also turn this into a **shorter hard-stop version** for Replit with even tighter wording.
