Replit Instructions — Tenant Agency Agreement Timeframe: Hide “Other” and Show Only the Custom Input

Applies to: Hire a Tenant’s Agent (Residential + Commercial)
Where: Broker Compensation & Agency Agreement Terms display (private terms + listing/bid views wherever this line appears)

🔒 SAFEGUARDS (MANDATORY)

Do NOT change any labels, headings, section order, tooltips, or layout.

Do NOT change any saved values or database schema.

Display-only fix: adjust the text shown for “Tenant Agency Agreement Timeframe” when “Other” is selected.

Apply fix anywhere this field is displayed (Actual Listing Display + Bid view/summary if applicable), but do not touch other fields.

✅ Required behavior
Current (wrong)

Other (Example 9 Months)

Correct

If selection is Other, display only the custom input text, e.g.:
Example 9 Months
(or 9 Months, whatever the user typed)

If selection is NOT Other, display the selected option as normal.

1) Locate the display line in the Tenant compensation terms view(s)

Find the line rendering:

“Tenant Agency Agreement Timeframe:”

Likely in:

resources/views/hire_tenant_agent/view.blade.php

and/or the partial used for the private compensation panel/modal

2) Replace the value output with this conditional logic (use data_get + fallbacks)

Use the same stored keys already used by your form. If you have a “timeframe” select + a separate “other” input field, do:

@php
  $timeframe = data_get($bid, 'get.tenantAgencyAgreementTimeframe')
            ?? data_get($bid, 'get.tenant_agency_agreement_timeframe');

  $timeframeOther = data_get($bid, 'get.tenantAgencyAgreementTimeframeOther')
                 ?? data_get($bid, 'get.tenant_agency_agreement_timeframe_other')
                 ?? data_get($bid, 'get.tenantAgencyAgreementTimeframeCustom')
                 ?? data_get($bid, 'get.tenant_agency_agreement_timeframe_custom');

  $isOther = is_string($timeframe) && strtolower(trim($timeframe)) === 'other';
@endphp

<span>
  {{ $isOther ? ($timeframeOther ?: '') : ($timeframe ?: '') }}
</span>

If you want a safe fallback (recommended)

If timeframe is “Other” but the custom input is blank, display “Other” (so it’s not empty):

{{ $isOther ? ($timeframeOther ?: 'Other') : ($timeframe ?: '') }}

3) Acceptance tests (must pass)

Select Tenant Agency Agreement Timeframe = Other and type 9 Months
✅ Display must show: 9 Months (not “Other (9 Months)”)

Select a normal option like 6 Months
✅ Display must show: 6 Months

Confirm it works in both:

Residential Tenant listing bids

Commercial Tenant listing bids