Understood — and thank you for laying out both the **selected lists** and what actually displays. The pattern is consistent:

* The **entire 📢 Marketing section is being dropped** on show pages.
* Several **selected items in other sections are being dropped** (e.g., “Send email alerts…”, “Submit complete application packages…”, “Coordinate lease negotiation…”, “LOI draft…”, etc.).
* The show page is only rendering a **hard-coded subset** (or rendering from the wrong “master list” that doesn’t include those items).

Below is a **single Tenant-only, Residential+Commercial, “fix it for real” instruction set**. It forces the listing display to render from the **exact same canonical catalog used to render the checkboxes**, and it preserves **exact order** everywhere by storing a **snapshot** at save-time.

---

## Replit Instructions — [Tenant@exp.com](mailto:Tenant@exp.com)

### Hire a Tenant’s Agent → Services Must Match Selections EXACTLY (All Items + Exact Order)

**Applies to:** Residential + Commercial
**Must apply everywhere:** Create, Drafts, Edit, Listing Show, Bid View, Counterbid View, Summaries/PDFs

---

# 1) SAFEGUARDS (MANDATORY)

1. **Tenant-only scope:** Do not modify Buyer/Seller/Landlord services logic or UI.
2. **Addition-only DB changes:** No renames/deletes. Adding a nullable column is allowed.
3. **Do not edit service wording** (strings must match listing creation exactly).
4. **No sorting:** Do not alphabetize sections or items anywhere.
5. **Backward compatible:** Old Tenant listings must still display services without errors.
6. **Single source of truth:** Services must come from one canonical catalog shared by:

   * the checklist on listing creation/edit
   * the listing show page
   * bids/counterbids/summaries

---

# 2) ROOT CAUSE (WHAT TO FIX)

The show page is almost certainly doing one of the following:

* Rendering from a **different list** than listing creation (missing Marketing + missing specific lines), OR
* Filtering selected items by **string mismatch**, OR
* Rendering from a **hard-coded subset** of services.

**Fix:**
✅ Use one canonical ordered catalog **for Tenant Residential** and **Tenant Commercial**, and save a `services_snapshot` in canonical order on Save Draft/Submit/Edit. Then render from the snapshot everywhere.

---

# 3) CANONICAL CATALOG (REQUIRED)

## Step 3.1 — Create a Tenant services catalog (Residential + Commercial)

Create:

**`app/Support/TenantServicesCatalog.php`**

It must return EXACT ordered arrays (section order preserved) for BOTH:

* `residential()`: your full Residential list (including all Marketing items and every line)
* `commercial()`: your full Commercial list (including all Marketing items and every line)

**Structure requirement:**

```php
return [
  "📢 Tenant Criteria Marketing & Promotion" => [
     "Create a branded flyer ...",
     ...
  ],
  "🔍 Property Search, Alerts & Matching" => [
     ...
  ],
  ...
];
```

**IMPORTANT:** Copy/paste the strings exactly as used in the checkbox list on listing creation. Do not “clean” punctuation/quotes.

---

# 4) STORE ORDER + SELECTIONS (SNAPSHOT) — THIS IS THE FIX

## Step 4.1 — Add a snapshot column to Tenant listings table

Add a nullable column to the Tenant listing table (example):

* `tenant_agent_auctions.services_snapshot` as `LONGTEXT` (or JSON if supported)

**Migration rule:** add-only, nullable.

---

## Step 4.2 — Build snapshot on Save Draft + Submit + Edit Save

### Location

Tenant Livewire create/edit components, e.g.:

* `app/Http/Livewire/TenantAgentAuction.php`
* `app/Http/Livewire/TenantAgentAuctionEdit.php`

### Required behavior

Whenever saving (draft or submit or edit update):

1. Determine property type: Residential vs Commercial
2. Load catalog:

   * `TenantServicesCatalog::residential()` OR `TenantServicesCatalog::commercial()`
3. Compare against what the user selected (your existing selected services variable)
4. Build `services_snapshot` in **catalog order**, storing checked status per item

**Snapshot format requirement (ordered):**

```json
[
  {
    "section": "📢 Tenant Criteria Marketing & Promotion",
    "items": [
      {"text":"Create a branded flyer ...","checked":true},
      {"text":"Post the Tenant’s ...","checked":false}
    ]
  },
  ...
]
```

### Critical safeguard

**Do not rely on the show page to “rebuild” services** later. Snapshot is the source of truth.

---

# 5) UNIFY HOW SELECTIONS ARE IDENTIFIED (VERY IMPORTANT)

Right now, services are likely stored as:

* an array of strings, OR
* an array of keys, OR
* JSON

To guarantee matching:

* If you store strings: snapshot builder must compare strings exactly.
* If you store keys: catalog should store keys + labels.

**Preferred (safe) approach (no breaking changes):**

* Keep existing storage as-is.
* In snapshot builder, support BOTH:

  1. selected keys
  2. selected text

Example logic:

* If selected list contains the exact service text → checked
* Else if selected list contains a slug/key → checked

Do not change existing stored data.

---

# 6) RENDERING FIX — SHOW PAGE MUST RENDER FROM SNAPSHOT

## Step 6.1 — Create one Tenant-only partial used everywhere

Create:

**`resources/views/partials/tenant/services_snapshot.blade.php`**

### Rendering rules

1. Decode snapshot JSON.
2. For each section in order:

   * collect checked items in order
   * if none checked → do not show the section header
3. Render checked items in exact order.

### Must handle empty safely

If snapshot missing/invalid → fallback to canonical catalog + filter selections in canonical order (NO sorting).

---

## Step 6.2 — Replace all Tenant services rendering with this partial

Update ALL places that show Tenant services to include the same partial:

* Listing show page
* Draft preview
* Edit preview
* Bid view
* Counterbid view
* Summaries / PDFs

Use:

```php
@include('partials.tenant.services_snapshot', ['auction' => $auction])
```

**Safeguard:** Do not reuse this partial in Buyer/Seller/Landlord.

---

# 7) FALLBACK (FOR OLD LISTINGS WITHOUT SNAPSHOT)

In the partial, if snapshot is missing:

1. Determine property type
2. Load canonical catalog for that type
3. Render only checked items in canonical order

🚫 Do NOT loop through selected items directly.

This ensures old listings still show correctly, in the right order.

---

# 8) WHY YOUR MARKETING SECTION IS DISAPPEARING (WHAT TO REMOVE)

Search Tenant show code for any logic like:

* `if section != Marketing` / ignoring first section
* rendering only certain section names
* hardcoded list of allowed sections
* `array_intersect` with an incomplete array
* a catalog that starts at Search instead of Marketing

**Delete that subset logic for Tenant flow** and replace with snapshot rendering.

---

# 9) QA TESTS (REQUIRED)

## Residential Test

1. Create Tenant Residential listing
2. Select:

   * all 11 Marketing items
   * all 4 Search items (including “Send email alerts…”)
   * Application items including “Submit complete and organized application packages…”
   * Lease item “Coordinate lease negotiation…”
   * Move-in “Coordinate move-in date and key handoff logistics…”
3. Save Draft → open actual listing → must match exactly in order
4. Submit → listing show → must match exactly in order
5. Bid/counterbid/summary → must match exactly in order

## Commercial Test

1. Create Tenant Commercial listing
2. Select:

   * all Marketing items
   * Search items including “Send listing alerts…” and “Search for off-market…”
   * Application “Submit complete…”
   * LOI item “Draft or assist with preparing a Letter of Intent (LOI)…”
   * “Coordinate with Landlord’s Agent… to finalize lease terms”
3. Save Draft / Submit / Show / Summary → must match exactly in order

---

# 10) “DONE” DEFINITION

For **Hire a Tenant’s Agent** (Residential + Commercial), everywhere services appear:

* **Only checked items show**
* **All checked items show**
* **Exact section order + item order matches listing creation**
* **No missing Marketing section**
* Applies to **drafts, edits, bids, counterbids, summaries, PDFs**

---

## QUICK NOTE FOR YOUR DEV (SO THEY DON’T “HALF FIX” IT)

If they only adjust the show page without snapshot + shared catalog, it will keep breaking as lists evolve. The snapshot + single partial is the durable fix.

---

If you paste **(A)** the current Tenant Livewire variable that holds selected services (example: `$this->services_requested`) and **(B)** the current Tenant show-page code block rendering services, I’ll rewrite the exact snapshot builder and the Blade partial using your real variable names so you can paste it straight into Replit with zero guessing.
