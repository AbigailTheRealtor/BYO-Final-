Here are **copy-paste Replit instructions with strong safeguards** for this exact **Hire a Tenant Agent** display fix only.

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A TENANT AGENT ONLY — PAYMENT TIMING FOR BROKER FEES DISPLAY FORMAT FIX

## SCOPE

Make this fix **only for Hire a Tenant Agent**:

### Current issue

On the Tenant listing display, **Payment Timing for Broker Fees** is showing in a different/awkward format like:

`Payment Timing for Broker Fees: Deducted from Rent Collected — 5 calendar days to pay`

### Required behavior

Make the Tenant listing display match the cleaner **Landlord-style format** so it reads more like a natural sentence/value, consistent with Landlord.

Example style target:

`Payment Timing for Broker Fees: Deducted from Rent Collected Within 5 Calendar Days`

or whatever exact sentence structure matches your saved Tenant option text best, but it should display in the same **clean style** as Landlord instead of the current dashed format.

### Goal

Tenant should not show:

* awkward dash formatting
* split/odd phrasing
* inconsistent capitalization/wording versus Landlord display style

This should be a **Tenant listing display formatting fix only** unless a tiny Tenant-only helper adjustment is absolutely required.

---

## HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only edit the Hire a Tenant Agent flow.**
2. **Do not modify Landlord, Buyer, or Seller flows.**
3. **Do not edit any unrelated file.**
4. **Do not rename database columns.**
5. **Do not rename Livewire properties.**
6. **Do not rename wire:model bindings.**
7. **Do not change routes.**
8. **Do not run migrations.**
9. **Do not change selection input format.**
10. **Do not change Select2 initialization or behavior.**
11. **Do not change field labels, tooltips, placeholders, icons, or field order.**
12. **Do not change how the value is saved unless absolutely necessary.**
13. **Do not refactor global compensation-section logic.**
14. **Do not affect Landlord display, since Landlord is already the correct reference format.**
15. **Keep this surgical and display-specific.**

---

## IMPORTANT

We do **not** want a broad Broker Compensation rewrite.

This should be a **Tenant-only listing display formatting fix** so the output reads cleanly and consistently with the Landlord presentation style.

### Hard stop:

Before editing anything, first confirm the **actual active Tenant files** for:

* Tenant listing display
* Tenant Broker Compensation display section
* the display row for **Payment Timing for Broker Fees**
* any helper/formatter currently building that Payment Timing text

Do **not** assume.
Do **not** edit shared files unless absolutely necessary.
If a shared file must be touched, wrap the change so it affects **Tenant only**.

---

## STEP 1 — CONFIRM THE CORRECT ACTIVE FILES FIRST

Before making changes, identify the exact active files for **Hire a Tenant Agent**:

1. Livewire/component or display file handling Tenant listing output
2. Blade/partial rendering the **Broker Compensation & Agency Agreement Terms** section for Tenant
3. The specific file/partial/helper responsible for showing:

   * `Payment Timing for Broker Fees`

### Requirement

Return the exact files first, then edit **only those files**.

### Hard stop

**No other file should be edited or changed.**

---

## STEP 2 — FIX TENANT PAYMENT TIMING DISPLAY TO MATCH LANDLORD STYLE

## Current bug

Tenant listing currently shows:

`Payment Timing for Broker Fees: Deducted from Rent Collected — 5 calendar days to pay`

## Required behavior

Display it in a cleaner Landlord-style sentence format.

Example acceptable style:

* `Payment Timing for Broker Fees: Deducted from Rent Collected Within 5 Calendar Days`
* or equivalent wording using the same saved Tenant meaning, but formatted cleanly as one natural phrase

### Important

Do **not** change the underlying saved Tenant data unless absolutely necessary.
Prefer to fix the **display rendering only**.

Do not keep the awkward:

* em dash style
* trailing “calendar days to pay” phrase if it reads clunky
* inconsistent formatting relative to Landlord

---

## STEP 3 — PRESERVE OTHER TENANT PAYMENT TIMING OPTIONS

Make sure this display fix works safely for the other possible Tenant payment timing choices.

### Required behavior

If the selected timing option does **not** involve a day count, do **not** force one into the text.

If the selected timing option **does** involve a day count:

* incorporate the number naturally into the sentence
* keep the wording consistent and readable

### Important

Do not create duplicated text or awkward formatting.

---

## STEP 4 — DO NOT AFFECT LANDLORD DISPLAY

Landlord already displays this correctly.

Do **not**:

* change Landlord files
* change Landlord wording
* change any shared compensation display globally unless wrapped so it affects **Tenant only**

---

## STEP 5 — PRESERVE BACKWARD COMPATIBILITY

Older Tenant listings that already have:

* payment timing phrase saved
* calendar day count saved

must still:

* render correctly after the fix
* not require migrations
* not lose data
* not break existing saved listings

---

## STEP 6 — TEST THESE EXACT CASES

### A. Tenant payment timing option with day count

* expected: one clean sentence in Landlord-style format
* expected: no awkward dash phrasing

### B. Tenant payment timing option without day count

* expected: clean display with no broken wording

### C. Existing older Tenant listing

* expected: still renders correctly after the fix

### D. Landlord listing

* expected: unchanged

---

## STEP 7 — VERIFY NOTHING ELSE CHANGED

After the fix, explicitly confirm:

* no Landlord files changed
* no Buyer files changed
* no Seller files changed
* no selection input format changed
* no Select2 UI changed
* no placeholders changed
* no tooltips changed
* no icons changed
* no unrelated Tenant fields changed

---

## STEP 8 — RETURN A FINAL REPORT

After implementation, return:

1. exact files changed
2. confirmation the correct active Tenant files were identified first
3. exact root cause for the awkward Tenant Payment Timing display
4. exact fix applied
5. confirmation no other files were edited
6. confirmation selection inputs/Select2 format were not changed
7. proof Tenant now displays Payment Timing for Broker Fees in the cleaner Landlord-style format

---

## IMPORTANT FINAL RULE

This is a **Tenant-only, surgical display fix**.

Do **not**:

* edit unrelated files
* change selection input format
* refactor global compensation logic
* touch Landlord, Buyer, or Seller
* make broad UI changes

The final result should be:

* Tenant Payment Timing for Broker Fees displays in a cleaner Landlord-style sentence format
* Landlord remains unchanged
* no other file or selection behavior is changed

---

If you want, I can also make this into a **shorter hard-stop version** for Replit.
