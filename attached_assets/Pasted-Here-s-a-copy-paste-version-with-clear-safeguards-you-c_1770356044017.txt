Here’s a copy/paste version **with clear safeguards** you can send to Replit.

---

All draft edits for **Hire a Landlord’s Agent** are still not saving. I’m concerned the wrong files/components are being edited.

**Test Path (Required):**
Login **[tenant@exp.com](mailto:tenant@exp.com)** → select **Hire a Landlord’s Agent** (Residential + Commercial) → complete fields → click **Save Draft** → open the draft → confirm everything re-populates.

## Safeguards (Must Follow)

1. **Scope Lock:** Changes must apply **ONLY** to **Hire a Landlord** drafts (Residential + Commercial). Do **NOT** affect Tenant/Buyer/Seller drafts or listings.
2. **No DB Breaking Changes:** Do **NOT** rename tables/columns. Do **NOT** delete columns. Avoid migrations unless absolutely required and non-destructive.
3. **Save Draft = Save Everything:** When user clicks **Save Draft**, **every single field** that exists on the form must persist (no missing fields). Do not rely on partial payloads or “only changed fields.”
4. **Draft Load = Restore Everything:** When reopening a draft, **all saved values** must re-populate exactly as entered, including all toggles, dropdowns, multi-selects, and numeric fields.
5. **“Other” Must Restore Custom Text:** For any field with an **Other** option:

   * The “Other” text input must **appear** when Other is selected, and
   * The custom text must **save and reload** on draft open (never display only the word “Other,” and never load blank).
6. **Do Not Override Loaded Data:** Do not run default setters after loading a draft (no resetting City/State arrays, clearing multi-selects, etc.). Defaults should apply **only** to brand-new listings.
7. **Multi-Select Casts:** Any landlord multi-select fields saved as JSON must be correctly cast to arrays in the **Landlord model only** so “Other” conditions work on reload.
8. **Personal Photo Draft Persistence:** If a photo is uploaded and draft is saved, it must still be visible when the draft is reopened (store and reload the file path). Do not require the user to re-upload.
9. **No Regressions:** After changes, confirm Submit still works and draft saving still allows incomplete forms (draft save should not force full submit validation).
10. **Proof Required:** Provide:

* a list of **changed files**, and
* screenshots/video showing: Save Draft → reopen draft → values persist (including at least one “Other” field and the photo).

**Bottom line:** Every field a user fills out when creating a Hire a Landlord listing must save to draft and restore perfectly—no exceptions.

---
