Got it — and this is the smoking gun:

You gave **the exact same URL** for **Landlord Create** and **Landlord Draft**:

`/hire/agent/auction/landlord`

That means when you click a “draft,” it’s **not actually routing to a true Edit/Draft screen** (or it’s routing there but **not passing a draft/listing ID**, so it loads the create component every time). In that situation, your dev can “fix” `LandLordAgentAuctionEdit.php` all day long and you’ll never see it — because you’re not hitting it.

Below are the **Replit instructions with safeguards** to fix this correctly.

---

## Replit Instructions (WITH SAFEGUARDS) — Landlord Draft Opens Same URL as Create (Wrong Route / Missing ID)

### Applies To

* Tenant login: `tenant@exp.com`
* Hire a Landlord’s Agent
* Problem: **Create and Draft open the same URL** (`/hire/agent/auction/landlord`) so drafts are not loading into the edit component with an ID.

---

# FIX 1 — Draft must open an Edit route that includes a draft/listing identifier

## Requirement

When the user clicks a draft, the URL MUST become something like one of these patterns:

**Option A (recommended):**

* `/hire/agent/auction/landlord/edit/{listing_id}`

**Option B:**

* `/hire/agent/auction/landlord/{listing_id}/edit`

**Option C (acceptable but less clean):**

* `/hire/agent/auction/landlord?draft_id={listing_id}`

### Safeguard

🚫 Drafts must **never** route to the plain create URL with no identifier.

**Pass condition:** Create URL ≠ Draft URL.

---

# FIX 2 — Update the Drafts List links (this is where the bug usually is)

## Location

Wherever the tenant dashboard displays “Hire a Landlord” drafts (your “My Drafts” / “Saved Drafts” list).

## Required change

Each draft row/card must link to the EDIT route with the correct ID.

Examples:

### If using Option A:

* Link target should be: `route('tenant.landlord.auction.edit', ['id' => $draft->id])`

### If using Option C:

* Link target should be: `/hire/agent/auction/landlord?draft_id={{ $draft->id }}`

### Safeguard

Add a hard assertion in code:

* If draft link is missing an ID, do **not** render the link (or render disabled) and log an error.

---

# FIX 3 — Ensure the correct component loads on Edit (and ONLY on Edit)

## Requirement

* Create route loads the **Create** component (ex: `LandLordAgentAuction.php`)
* Edit route loads the **Edit** component (ex: `LandLordAgentAuctionEdit.php`)

### Safeguard: Add “DEBUG fingerprints” (temporary, must be removed after)

In the create blade:

* `DEBUG: LANDLORD CREATE COMPONENT LOADED`

In the edit blade:

* `DEBUG: LANDLORD EDIT COMPONENT LOADED`

**Pass condition:**

* Create page shows CREATE debug
* Clicking a draft shows EDIT debug

If the draft still shows the CREATE debug, the drafts list is still linking wrong or the route still points to create.

---

# FIX 4 — If they refuse to add a new edit route, support draft loading via query param (fallback)

If your system must keep `/hire/agent/auction/landlord` as the only route:

## Required behavior

* When a draft is clicked, it must append: `?draft_id=XXXX`
* In the create component `mount()`, if `draft_id` exists:

  * load draft data into form state
  * set mode to “editing draft”
  * save actions update the draft record instead of creating a new record

### Safeguard

If `draft_id` is present but not found in DB:

* show a clear error “Draft not found”
* do not silently load blank create form

---

# FIX 5 — Only after routing is fixed, implement your persistence fixes

Once draft actually loads the edit/draft context, THEN the persistence fixes matter.

For your Landlord issue list:

* City / State / County / Zip Code not saving
* Landlord’s Broker Lease Fee not saving

### Required persistence implementation

These fields must be included in BOTH:

1. draft save payload (create/save draft)
2. draft hydration (edit mount / loadDraft)
3. edit save payload
4. post-save reload (must repopulate)

### Safeguard

Use merge-save: saving Tab 2 cannot wipe Tab 1.

---

# Acceptance Tests (Must pass exactly)

### Test A — URL changes

1. Open create: `/hire/agent/auction/landlord`
2. Save draft
3. Click draft from drafts list
4. ✅ URL must include an ID (path param or `?draft_id=`)

### Test B — Component confirmation

* Create shows: `DEBUG: LANDLORD CREATE COMPONENT LOADED`
* Draft shows: `DEBUG: LANDLORD EDIT COMPONENT LOADED`

### Test C — Field persistence

1. Enter City/State/County/Zip + Landlord Broker Lease Fee
2. Save draft
3. Reopen draft
4. ✅ All fields still filled

---

## Why this happened (simple explanation)

If drafts open the same URL as create **without an ID**, the app has **no way to know which draft to load**, so it renders a blank create form — making it look like “draft didn’t save.”

---

If you paste the URL you get when you click a specific draft *from the drafts list* (even if it stays the same), I can tell you which of the three is broken:

* the drafts list link (most likely),
* the routes mapping,
* or the edit component mount not receiving the ID.
