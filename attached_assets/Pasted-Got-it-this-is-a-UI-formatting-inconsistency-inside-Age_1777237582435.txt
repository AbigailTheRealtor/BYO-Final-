Got it — this is a **UI formatting inconsistency inside Agent-to-Agent bid view**, not a data issue. We’re fixing **label + icon + formatting only**, and **ONLY inside Private Compensation & Agreement Terms for Agent-to-Agent bids**.

Here’s the clean, surgical Replit instruction set 👇

---

## 🎯 GOAL

Fix Referral Fee display so it matches other fields:

**FROM:**

* Has yellow % icon
* Says: `Referral Fee (%) (Agent-to-Agent):`
* Styled like a special section

**TO:**

* No icon
* Label matches standard format:
  **Referral Fee (%):**
* Value aligned like other compensation fields
* No “Agent-to-Agent” text

---

## 🚨 CRITICAL SAFEGUARDS

* DO NOT change database fields
* DO NOT rename `referral_fee_percent`
* DO NOT touch:

  * Seller / Buyer / Tenant / Landlord listing flows
  * Public views
  * Non-agent bids
* ONLY modify:
  👉 Agent-to-Agent bid **Private Compensation display layer**
* No controller logic changes unless absolutely necessary (this is view-level)

---

## 🔍 STEP 1 — LOCATE THE VIEW

Search in Replit:

```
Referral Fee (%)
```

Then find the file used in:
👉 Agent bid → “Private Compensation & Agreement Terms”

Most likely one of:

```
resources/views/livewire/...agent-bid...
resources/views/...accepted-bid-summary...
resources/views/components/compensation...
```

---

## 🔧 STEP 2 — REMOVE ICON + HEADER STYLE

You will find something like:

```blade
<div class="flex items-center gap-2">
    <span class="icon-percent"></span>
    <h3>Referral Fee & Cooperation Terms</h3>
</div>
```

### ✅ REPLACE WITH:

```blade
{{-- Removed special section styling for consistency --}}
```

👉 Delete the icon + section header entirely IF it's only wrapping referral fee

---

## 🔧 STEP 3 — FIX LABEL FORMAT

### FIND:

```blade
Referral Fee (%) (Agent-to-Agent):
```

### REPLACE WITH:

```blade
Referral Fee (%):
```

---

## 🔧 STEP 4 — ENSURE CONSISTENT FIELD STYLE

Match how other fields look in that same section.

### Standard format should look like:

```blade
<div class="flex justify-between py-2">
    <span class="text-gray-600">Referral Fee (%):</span>
    <span class="font-medium">{{ $referral_fee_percent }}%</span>
</div>
```

---

## 🔧 STEP 5 — REMOVE DUPLICATE (IMPORTANT)

You mentioned it's showing twice — that means it's being rendered in:

* main compensation section
* AND separate referral section

### Search:

```
referral_fee_percent
```

### Ensure it's only rendered ONCE inside:

👉 Private Compensation & Agreement Terms main block

### DELETE the duplicate block entirely

---

## 🔧 STEP 6 — CONDITIONAL (AGENT-ONLY SAFETY)

Wrap it to ensure it ONLY shows for agent-to-agent bids:

```blade
@if($isAgentToAgent ?? false)
    <div class="flex justify-between py-2">
        <span class="text-gray-600">Referral Fee (%):</span>
        <span class="font-medium">{{ $referral_fee_percent }}%</span>
    </div>
@endif
```

⚠️ If `$isAgentToAgent` doesn’t exist, use existing condition already used elsewhere (DO NOT invent new logic if avoidable)

---

## 🧪 STEP 7 — TEST CASES

Test ALL of these before moving on:

### ✅ Agent → Agent Bid

* Shows **Referral Fee (%)**
* No icon
* No “Agent-to-Agent”
* Only appears once
* Matches styling of other fields

### ✅ Seller / Buyer / Tenant / Landlord Listings

* ❌ NO CHANGE

### ✅ Public View

* ❌ Still hidden (no compensation leak)

### ✅ Accepted Bid Summary

* Still shows correctly with %

---

## ✅ EXPECTED FINAL RESULT

Inside Private Compensation:

```
Lease Fee:                $4,000 + 4%
Purchase Fee:             2.5%
Referral Fee (%):         25%
```

Clean. Consistent. No special treatment.

---

## 💡 WHY THIS MATTERS (IMPORTANT)

Right now it:

* Breaks visual hierarchy
* Looks like a separate system
* Confuses users (“why is this different?”)

This fix:
✔ Normalizes agent-to-agent flow
✔ Keeps compliance clean
✔ Matches your “Tenant gold standard” UI

---

If you want next step after this, I’d fix:
👉 **Referral Fee tab placement + separation logic (Agent vs Platform referrals)**
That’s the bigger structural improvement you were hinting at earlier.
