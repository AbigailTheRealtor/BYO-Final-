Replit Instructions — Tenant@exp.com
Hire a Landlord’s Agent → Tab 2: Property Details
Residential + Income Listing Types — Tooltip Text Edits Only (Pets Section)
Applies To

Tenant@exp.com

Flow: Hire a Landlord’s Agent

Tab: 2 — Property Details

Listing Types: Residential and Income only

Change Type: Tooltip text edits only

Where this must apply:

Listing creation form tooltips

Edit listing form tooltips

Any preview/summary screens that reuse these tooltip keys (if applicable)

Required Updates (Replace Tooltip Text With Exactly)
1) Field: Pets Allowed

Replace tooltip text with exactly:

Select whether pets are allowed at the property. If “Yes” is selected, provide details including the number of pets allowed, acceptable pet types, maximum weight per pet, and any applicable pet restrictions.

2) Field: Number of Pets Allowed

Replace tooltip text with exactly:

Enter the maximum number of pets the Landlord will allow for this property (e.g., 2).

3) Field: Acceptable Pet Types

Replace tooltip text with exactly:

Enter the types of pets the Landlord will allow (e.g., Dog, Cat).

4) Field: Maximum Weight Per Pet (lbs)

Replace tooltip text with exactly:

Enter the maximum allowed weight for each individual pet, in pounds. Leave blank if there is no weight restriction.

5) Field: Pet Restrictions

Replace tooltip text with exactly:

Enter any pet restrictions the Landlord requires. Include any HOA or insurance-related restrictions if applicable.

Scope Rules

These tooltip updates apply only to Residential and Income listings within Hire a Landlord’s Agent.

Do not apply these changes to Commercial, Single Room, Business, Vacant Land, or any other flows.

SAFEGUARDS (MUST FOLLOW)
DO NOT

Modify any field logic, visibility conditions, validations, or required/optional status.

Change placeholders, labels, field order, icons, styling, emojis, or section headers.

Rename or refactor field keys, component names, bindings, database columns, or API payloads.

Apply these tooltip changes to Seller, Buyer, or Tenant flows.

Remove or overwrite shared tooltip components outside the exact fields listed.

MUST

Update only the tooltip text strings for the five fields listed above.

Preserve capitalization and role usage (Landlord) exactly as written.

Ensure tooltips render consistently in Create, Edit, and Preview/Display contexts.

If tooltips are centrally managed, scope changes precisely to Hire a Landlord’s Agent → Residential/Income only.

Verification Checklist (QA)

Create Landlord → Residential listing → Tab 2 → hover each Pets-related tooltip → text matches exactly.

Create Landlord → Income listing → Tab 2 → same result.

Create Landlord → Commercial listing → confirm these tooltips did not change.

Edit an existing Landlord Residential/Income listing → tooltips persist and display correctly.