Perfect — below is a **fully rewritten, consolidated instruction set** that now **explicitly covers BOTH Save Draft and Edit Listing**, with **zero ambiguity**, **Tenant.php as the source of truth**, and **strong safeguards**.

This is **copy-paste ready for Replit**.

---

# Replit Instructions — [Tenant@exp.com](mailto:Tenant@exp.com)

## Hire a Buyer’s Agent

### **Save Draft + Edit Listing Must Populate 100% of Fields**

(Traditional + Bidding | Residential, Income, Commercial, Business, Vacant Land)

---

## APPLIES TO

* Workspace: **[tenant@exp.com](mailto:tenant@exp.com)**
* Flow: **Hire a Buyer’s Agent**
* Backend: **Tenant.php (Livewire component)**
* Listing Modes:

  * Traditional
  * Bidding Period
* Property Types:

  * Residential
  * Income
  * Commercial
  * Business
  * Vacant Land

---

## CRITICAL CONTEXT (DO NOT SKIP)

* Hire a Buyer’s Agent is **implemented inside Tenant infrastructure**
* There is **NO Buyer-specific draft or edit component**
* **Tenant.php controls:**

  * Create Listing
  * Save Draft
  * Edit Listing
  * Submit Listing
* Residential behavior is correct
* **Income, Commercial, Business, and Vacant Land currently fail to fully persist or repopulate**

---

## SAFEGUARDS (MANDATORY)

1. **DO NOT rename or replace `Tenant.php`**
2. **DO NOT create Buyer-specific components**
3. **DO NOT delete or change existing Residential logic**
4. **DO NOT clear values when sections are hidden**
5. **DO NOT require submit-level validation for drafts**
6. **ALL changes must be additive and non-destructive**

---

## SUCCESS CRITERIA (NON-NEGOTIABLE)

### Save Draft

* Every field the user enters or reveals is saved
* No field is dropped due to property type or listing mode
* Draft always saves successfully

### Edit Listing (CRITICAL ADDITION)

* When editing a saved or submitted listing:

  * **Every field must populate**
  * UI must look **identical to Create Listing**
  * All conditional sections must reopen correctly
* No field may:

  * show blank
  * visually appear filled but fail validation
  * disappear due to missing backend hydration

---

# SECTION A — SAVE DRAFT REQUIREMENTS

## STEP A1 — TENANT.PHP IS THE ONLY SOURCE OF TRUTH

All Buyer Save Draft logic must live inside:

```
Tenant.php
```

Includes:

* saveDraft()
* draft hydration logic
* conditional visibility logic

🚫 Do NOT reference BuyerAgentAuctionFields.php
🚫 Do NOT split logic by property type file

---

## STEP A2 — SAVE DRAFT MUST PERSIST **ALL FIELDS**

### Required Fix

In `Tenant.php → saveDraft()`:

* Build **one complete payload**
* Include:

  * Tenant base fields
  * Buyer-specific fields
  * **ALL property-type fields** (Income, Commercial, Business, Vacant Land)

### SAFEGUARD

❌ Do NOT exclude fields because they are hidden
❌ Do NOT gate persistence by property type

Hidden fields must still be saved.

---

## STEP A3 — USE A SINGLE JSON PAYLOAD

* All draft data must be stored in **one JSON column**

  * Example: `draft_payload`, `form_data`
* If already present:

  * **Expand it**
  * Do NOT create parallel storage

This prevents silent field loss.

---

## STEP A4 — DRAFT VALIDATION RULES

### Draft

* Format validation only (date, numeric)
* **Never block Save Draft**

### Submit

* Enforce required fields

🚫 Save Draft must never fail due to submit requirements.

---

# SECTION B — EDIT LISTING REQUIREMENTS (NEW & CRITICAL)

## STEP B1 — EDIT LISTING MUST HYDRATE FROM SAME SOURCE AS DRAFT

When opening **Edit Listing**:

* Load the same JSON payload used for drafts/submitted listings
* Hydrate **every Livewire property** inside `Tenant.php`

This includes:

* Buyer-specific fields
* Property-type-specific fields
* Conditional fields previously revealed

---

## STEP B2 — NO PARTIAL HYDRATION

### Requirement

Do NOT selectively hydrate based on:

* property type
* listing mode
* tab

Instead:

* Loop through **all keys in stored payload**
* Assign them to Livewire state

### SAFEGUARD

❌ Do NOT use `empty()`
✅ Use `array_key_exists()`

This preserves valid values like:

* `0`
* `false`
* empty arrays

---

## STEP B3 — EDIT LISTING UI MUST MATCH CREATE LISTING EXACTLY

### Requirement

When editing:

* Same tabs
* Same order
* Same conditional logic
* Same required markers

No “edit-only” shortcuts or trimmed views.

---

## STEP B4 — CONDITIONAL VISIBILITY MUST REPLAY AFTER HYDRATION

After Edit Listing hydration:

* Re-run:

  * Property Type logic
  * Listing Mode logic
  * Financing / Special Provision logic

### SAFEGUARD

🚫 Never reset values when hiding sections
🚫 Never overwrite user data on re-render

---

## STEP B5 — EDIT LISTING MUST BE SUBMITTABLE

After hydration:

* Submit button must enable when required fields are met
* No hidden missing backend values
* No “filled but not recognized” state

This fixes:

* disabled submit
* silent validation failure
* missing backend state

---

# SECTION C — PROPERTY TYPE COVERAGE CHECKLIST

Verify **Save Draft + Edit Listing** both support:

### Income

* Unit count
* Cap rate
* NOI
* Financial thresholds
* Multi-unit selections

### Commercial

* Property subtype
* Lease structure fields
* Zoning-related inputs

### Business

* Business type
* Asset / goodwill fields

### Vacant Land

* Acreage
* Zoning / land use
* Utility availability

---

# SECTION D — TEMPORARY DEBUG SAFEGUARD (DEV ONLY)

Add logging in Tenant.php:

* Compare expected field list vs stored payload
* Log missing keys by:

  * Listing ID
  * Property Type
  * Mode

Remove after confirmation.

---

## FINAL QA REQUIREMENT

For **each property type** and **each mode**:

1. Create listing
2. Fill all fields (including conditionals)
3. Save Draft
4. Reload → confirm all fields populate
5. Submit listing
6. Open Edit Listing
7. Confirm **every field is still populated**
8. Re-submit without errors

✅ No data loss
✅ No missing fields
✅ UI matches Create Listing
✅ Submit works

---

## IMPORTANT NOTE

This fix pattern will later apply cleanly to:

* Hire a Seller’s Agent
* Hire a Landlord’s Agent

Because they share the same Tenant-first architecture.

---

If you want next, I can:

* Produce a **field-by-field Buyer Income audit checklist**
* Write a **one-page “Do exactly this” message for Harry**
* Or help you **lock this logic so it never breaks again**

Just tell me what you want next 👍
