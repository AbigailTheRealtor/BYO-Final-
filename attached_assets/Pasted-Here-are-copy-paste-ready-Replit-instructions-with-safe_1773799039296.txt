Here are copy-paste-ready Replit instructions with safeguards:

---

# REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A BUYER’S AGENT — ACTUAL LISTING STILL SHOWS REMOVED FINANCING OPTIONS

## ISSUE

This is **still not fixed** on the **actual listing**.

### What I am seeing:

On edit, the selected financing now shows fewer options in the form, for example:

* FHA
* Jumbo
* VA
* USDA

But on the **actual listing**, it still shows old removed options like:

* Conventional
* No-Doc
* Non-QM

So the edit form and saved listing display are **not matching**.

This means one of two things is still wrong:

1. the actual listing display is reading the wrong field / stale field / old summary text, or
2. removed financing selections are not actually being cleared from the saved record used by the actual listing page

---

# GOAL

For **Hire a Buyer’s Agent**, under **Offered Financing/Currency**, the **actual listing** must show **only the currently selected options** after edit.

### Example:

If the current saved selection is:

* FHA
* Jumbo
* VA
* USDA

then the actual listing must show only:

**FHA / Jumbo / VA / USDA**

and must **not** show:

* Conventional
* No-Doc
* Non-QM

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Buyer’s Agent flow.**
2. **Do not change Seller, Tenant, or Landlord financing logic unless a shared file is proven to control Buyer too and can be safely scoped.**
3. **Do not rename, delete, or migrate any database columns.**
4. **Do not rename wire:model names, validation keys, backend properties, or storage keys.**
5. **Do not do broad text replacements across multiple listing views.**
6. **Do not patch this with string-hiding hacks in Blade.**
7. **Do not hardcode a filtered label list unrelated to the saved source of truth.**
8. **Do not assume the form file controls the actual listing page.**
9. **Do not break create mode, draft mode, or edit mode.**
10. **If there is a stale derived summary field, fix the regeneration or source-of-truth usage surgically.**
11. **If there are multiple Buyer listing views, identify the exact actual listing view first before editing.**
12. **Stop if this would require destructive schema changes. Use the smallest safe fix.**

---

# IMPORTANT — DO NOT GUESS

The screenshots prove the form selection and the actual listing output are different.

So before changing code, trace the exact data path for the **actual listing page**.

We need to know:

1. what field the edit form is saving
2. what field the actual listing page is reading
3. whether removed financing options are still present in the saved data
4. whether the actual listing is rendering from a stale summary string or stale cached field

---

# FIRST STEP — TRACE THE ACTUAL LISTING SOURCE OF TRUTH

Please inspect the exact Buyer actual listing page and confirm:

## A) Which file renders the actual listing financing line?

Find the exact file/partial that outputs:

**Conventional / FHA / Jumbo / VA / No-Doc / Non-QM / USDA**

Do not assume this is the same file as the edit form.

## B) Which database/model property is the actual listing using?

Confirm whether the actual listing financing line is built from:

* the live selected financing array/json
* an old derived summary string
* a formatted field stored separately
* a cached listing summary value
* a merged/fallback field

## C) Is the saved record actually updated?

After edit/save, inspect the Buyer listing record and confirm whether removed values like:

* Conventional
* No-Doc
* Non-QM

are still present in the saved field the actual listing uses.

---

# WHAT TO SEARCH FOR

Search specifically for the actual listing render path and financing output by looking for:

* `Conventional`
* `FHA`
* `Jumbo`
* `VA`
* `No-Doc`
* `Non-QM`
* `USDA`
* `Buyer Pre-Approved for a Loan`
* `Offered Financing/Currency`

This should identify:

* the actual listing Blade file
* any formatter/helper
* any summary-builder logic
* any stored display string

---

# LIKELY ROOT CAUSES TO CHECK

## 1) Actual listing reads a stale summary string

Example:

* form correctly updates selected financing
* listing page still prints an old combined text field

## 2) Save/update merges instead of replaces

Example:

* new values added
* old removed values not cleared

## 3) Listing page uses a different model property than edit form

Example:

* form writes to one field
* listing reads from another

## 4) Derived display field is not regenerated after edit

Example:

* summary built on create
* not rebuilt on later edit

## 5) Multi-select deselection is not syncing correctly on save

Example:

* UI looks deselected
* backend still retains old values in array/json

---

# REQUIRED FIX

For the **actual Buyer listing page**, the financing line must be generated from the **current saved selected financing options only**.

That means:

* removed options must be removed from the source used by the actual listing
* actual listing must render only current selections
* no stale options may linger after edit/save

---

# VERY IMPORTANT IMPLEMENTATION RULE

Do **not** fix this by manually hiding specific labels.

The fix must come from the real source of truth:

* correct saved financing values
* correct actual listing render source

The actual listing should reflect what is currently selected in edit mode after save.

---

# ACCEPTANCE TESTS

## Test 1 — Create Buyer listing

Create a Buyer listing with:

* Conventional
* FHA
* Jumbo
* VA
* No-Doc
* Non-QM
* USDA

Confirm actual listing shows all selected options.

---

## Test 2 — Edit and remove financing options

Edit the same listing and remove:

* Conventional
* No-Doc
* Non-QM

Leave selected:

* FHA
* Jumbo
* VA
* USDA

Save edits.

Confirm actual listing now shows exactly:

**FHA / Jumbo / VA / USDA**

and does **not** show:

* Conventional
* No-Doc
* Non-QM

---

## Test 3 — Reopen edit

Reopen edit on that same listing.

Confirm the form also still shows only:

* FHA
* Jumbo
* VA
* USDA

This ensures form and actual listing match.

---

## Test 4 — Add one back

Edit again and add back:

* Conventional

Save.

Confirm actual listing shows:

**Conventional / FHA / Jumbo / VA / USDA**

and still does not show:

* No-Doc
* Non-QM

unless reselected

---

## Test 5 — Actual listing page specifically

Verify this on the **actual listing page**, not only on the form or preview.

---

# REPORT BACK AFTER CHANGES

Please report back with:

1. **Exact file that renders the actual Buyer listing financing line**
2. **Exact field/property that actual listing uses as its source**
3. **Whether the root cause was save-side or display-side**
4. **Whether removed financing values were still present in saved data**
5. **Exact files changed**
6. **Confirmation that only Hire a Buyer’s Agent was changed**
7. **Confirmation that DB fields, wire:model names, and storage keys were not renamed**

---

## IMPORTANT NOTE TO FOLLOW

Do **not** tell me it is fixed unless you verify the **actual listing page** now shows only the currently selected financing options.
