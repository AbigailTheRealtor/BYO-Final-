Fix Acceptable Unit Type Field

File to edit (Hire a Buyer’s Agent – Property Preferences tab):
resources/views/livewire/.../hire-buyer-agent-auction-tabs/property-preferences.blade.php
(Use the same file where “Non-Negotiable Amenities” is defined.)

1. Make Acceptable Unit Type a multi-select (like Amenities)

In the Acceptable Unit Type section, find the current <select> element.
It probably looks something like:

<select
    class="form-control"
    name="acceptable_unit_type"
    wire:model="acceptable_unit_type"
>


Change it to a multi-select that behaves exactly like Non-Negotiable Amenities:

Add multiple and use an array name.

Use the same CSS classes / JS initialization as the Non-Negotiable Amenities multi-select.

Bind to an array on Livewire.

<select
    class="form-control select2-multiple acceptable-unit-types-select"
    name="acceptable_unit_types[]"
    multiple
    wire:model="acceptable_unit_types"
    data-placeholder="Select"
>
    <option value=""></option>
    <option value="1 Bed/1 Bath">1 Bed/1 Bath</option>
    <option value="1 Bedroom">1 Bedroom</option>
    <option value="2 Bed/1 Bath">2 Bed/1 Bath</option>
    <option value="2 Bed/2 Bath">2 Bed/2 Bath</option>
    <option value="2 Bedroom">2 Bedroom</option>
    <option value="3 Bed/1 Bath">3 Bed/1 Bath</option>
    <option value="3 Bed/2 Bath">3 Bed/2 Bath</option>
    <option value="3 Bedroom">3 Bedroom</option>
    <option value="4 Bedroom or More">4 Bedroom or More</option>
    <option value="4+ Bed/1 Bath">4+ Bed/1 Bath</option>
    <option value="4+ Bed/2 Bath">4+ Bed/2 Bath</option>
    <option value="Apartments">Apartments</option>
    <option value="Efficiency">Efficiency</option>
    <option value="Loft">Loft</option>
    <option value="Manager's Unit">Manager's Unit</option>
    <option value="Multi-Level">Multi-Level</option>
    <option value="Penthouse">Penthouse</option>
    <option value="Studio">Studio</option>
    <option value="Other">Other</option>
</select>


In the JS / Alpine / init section where select2 (or the multi-select plugin) is initialized for Non-Negotiable Amenities, also initialize it for .acceptable-unit-types-select so it shows the same chips / tags layout and allows multiple selections.

2. Show a text box only when “Other” is selected

Remove the “Other Unit Type:” label completely from the Blade file.
(Delete the line that renders that label.)

Directly under the Acceptable Unit Type multi-select, add a conditional text input that appears only when "Other" is one of the selected values:

@if(in_array('Other', $acceptable_unit_types ?? []))
    <div class="form-group mt-2">
        <input
            type="text"
            class="form-control"
            name="acceptable_unit_type_other"
            wire:model="acceptable_unit_type_other"
            placeholder="Enter acceptable unit types (e.g., Live/Work Unit, Boarding House, Accessory Dwelling Unit)"
        >
    </div>
@endif


Note: no label above this field – just the input with the placeholder.

In the Livewire component for this form, make sure you have:

public $acceptable_unit_types = [];
public $acceptable_unit_type_other;

3. Behavior requirements (for QA)

Ask Replit / the dev to verify:

Multi-select works:

User can select multiple unit types (e.g., “2 Bed/2 Bath”, “3 Bed/1 Bath”, etc.).

Selected values show as tags/chips in a single line like Non-Negotiable Amenities.

Other logic:

When “Other” is selected in the multi-select, the text box appears directly below it, with this exact placeholder:

Enter acceptable unit types (e.g., Live/Work Unit, Boarding House, Accessory Dwelling Unit)

When “Other” is deselected, the text box disappears and the value is cleared (optional but preferred).

Styling:

The Acceptable Unit Type control looks visually identical to the Non-Negotiable Amenities control (same height, width, icon, and behavior).