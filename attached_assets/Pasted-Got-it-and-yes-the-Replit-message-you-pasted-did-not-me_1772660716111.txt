Got it — and yes, the Replit message you pasted **did not** mention this issue at all. It only talked about `condition_prop_buyer`, `number_of_unit_type`, and shared Tenant components.

Below are **copy-paste Replit instructions with safeguards** to fix:

✅ **Buyer → Vacant Land only**
✅ **Acceptable Property Styles** (NOT Property Types)
✅ When user selects **Other**, the **custom input box must appear**, save, and **display the custom text on the listing** (Create + Draft + Edit + Listing Display)
✅ Must work even though Buyer Draft/Edit routes through the **Tenant shared components**

---

## Replit Instructions With Safeguards

### Buyer (Vacant Land only) — Acceptable Property Styles “Other” must show custom input + save + display

**Status note:** Acceptable Unit Type is already fixed. Do not touch it.
**Focus ONLY:** Buyer → Vacant Land → **Acceptable Property Styles** “Other” custom field.

---

# 1) Confirm the correct flows (IMPORTANT)

Buyer listing uses different components depending on stage:

* **Create:** `BuyerAgentAuction.php` + `hire-buyer-agent.blade.php`
* **Draft Resume:** `TenantAgentAuction.php` + `tenant-agent-auction.blade.php`
* **Edit:** `TenantAgentAuctionEdit.php` + `tenant-agent-auction-edit.blade.php`

✅ This fix must be applied in **all 3 blade files** (because Draft/Edit use tenant shared files).

---

# 2) Define one consistent “Other” text property (single source of truth)

Create ONE Livewire property used everywhere, example:

* `public $acceptable_property_styles_other = '';`

**Safeguards**

* Do not create multiple names (ex: `other_style`, `property_style_other`, etc.)
* Use the same property name in Buyer create + Tenant draft + Tenant edit

---

# 3) Vacant Land only: show the “Other” input when “Other” is selected

In the Buyer Vacant Land “Acceptable Property Styles” multi-select:

### Requirement:

If the selected styles include `"Other"`:

* show the custom input field

If `"Other"` is removed:

* hide the input
* clear the stored value (optional but recommended)

✅ This must be implemented in:

1. `hire-buyer-agent.blade.php`
2. `tenant-agent-auction.blade.php`
3. `tenant-agent-auction-edit.blade.php`

---

# 4) Select2 → Livewire dual-sync for the styles field (prevents disappearing/empty saves)

Because this is a Select2 multi-select, you must always sync the array to Livewire on change.

### In the Select2 change handler for Acceptable Property Styles:

* `values = $('#acceptable_property_styles').val() || [];`
* set Livewire array property: `@this.set('acceptable_property_styles', values);`

If you use a JSON bridge (recommended for hidden tabs):

* `@this.set('acceptable_property_styles_json', JSON.stringify(values));`

**Safeguard**

* Do NOT rely on DOM-only values.
* Do NOT only update a hidden input without `@this.set()`.

---

# 5) Wire the “Other text input” to Livewire and ensure it saves

Add the input field (Vacant Land only) and bind it:

Example:

* `wire:model.defer="acceptable_property_styles_other"`

**Safeguard**

* This field must exist in all 3 blades (create/draft/edit) and must not be inside a condition that fails during rerender.

---

# 6) Server-side save safeguard (Draft + Edit are the ones that usually break)

In BOTH:

* `TenantAgentAuction.php`
* `TenantAgentAuctionEdit.php`

Ensure:

* `acceptable_property_styles` is saved as an array/JSON
* `acceptable_property_styles_other` is saved as plain text

If a JSON bridge exists:

* decode it before saving:

  * `$this->acceptable_property_styles = json_decode($this->acceptable_property_styles_json, true) ?? [];`

Then normalize:

* array_unique + array_values
* trim strings

**Safeguard**

* Never drop `"Other"` from the array during normalization.
* Never wipe `acceptable_property_styles_other` unless `"Other"` is not selected.

Recommended rule:

* If `"Other"` not selected → set other text to `null` or `''`
* If `"Other"` selected → keep other text

---

# 7) Listing display rule (this is what you’re asking for)

On the actual listing display for Buyer Vacant Land:

### Required display behavior:

* If Acceptable Property Styles includes `"Other"` and `acceptable_property_styles_other` has text:

  * display the custom text **instead of the word “Other”**
* Display all non-Other selections normally

Example:
User selects:

* `Agriculture`
* `Other` with text `Billboard Site`

Listing must show:

* `Agriculture, Billboard Site`
  NOT:
* `Agriculture, Other`

**Safeguard**

* Do not display both “Other” and the custom text.
* Do not hide the entire section if only “Other” is selected.

---

# 8) Pre-populate on Draft Resume + Edit

When loading an existing listing:

* if array contains `"Other"` → show the input field automatically
* input is prefilled with `acceptable_property_styles_other`

**Safeguard**

* Ensure your JS initialization runs after Livewire renders (use your guarded `initializeFullService()` pattern).

---

## QA Test (Must Pass)

Login: `tenant@exp.com` → Hire Buyer’s Agent → select **Vacant Land**

1. Select Acceptable Property Styles:

* Select `Other`
  ✅ custom input appears immediately

2. Enter:

* `Billboard Site`

3. Save Draft → reopen draft:
   ✅ Other still selected, text still filled

4. Publish → view listing:
   ✅ listing shows `Billboard Site` (not the word “Other”)

5. Edit listing:
   ✅ Other preselected + text prefilled, and saves again

---

### If you want this to be bulletproof

Tell Replit AI to **search your codebase for the exact field names** currently used for “Acceptable Property Styles” (Vacant Land) and then apply the fix using those exact ids/properties.

If you paste a snippet or tell me the current field/property names you’re using for “Acceptable Property Styles” on Vacant Land, I can rewrite the above using your *exact* ids so it’s zero-guessing for Replit.
