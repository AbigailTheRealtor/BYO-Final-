Task #571D — FINAL FIX (Un-nest + Remove Early Return)

THIS IS NOT AN EXPERIMENT.
THIS IS A STRUCTURAL FIX.

---

STEP 1 — REMOVE NESTING (CRITICAL)

In ALL 4 files:

- seller/offer-seller-listing.blade.php
- buyer/offer-buyer-listing.blade.php
- landlord/offer-landlord-listing.blade.php
- tenant/offer-tenant-listing.blade.php

FIND this pattern:

function something() {
    function initSelect2Fields() {

CUT the entire initSelect2Fields() function out.

PASTE it ABOVE all functions (top-level inside <script>).

---

STEP 2 — FIX FUNCTION CONTENT

REPLACE initSelect2Fields() with EXACTLY:

function initSelect2Fields() {
    $('.select2-multiple, .select2-single').each(function () {
        var $el = $(this);

        // DESTROY FIRST (always)
        if ($el.hasClass('select2-hidden-accessible')) {
            $el.select2('destroy');
        }

        var placeholder = $el.attr('data-placeholder') || 'Select';

        $el.select2({
            placeholder: placeholder,
            allowClear: true,
            width: '100%'
        });
    });
}

---

STEP 3 — DELETE THIS LINE EVERYWHERE

REMOVE every instance of:

if ($el.hasClass('select2-hidden-accessible')) return;

This line is the root bug.

---

STEP 4 — ENSURE ONLY ONE CALL EXISTS

You should ONLY have:

$(document).ready(function () {
    initSelect2Fields();
});

document.addEventListener('livewire:load', function () {
    Livewire.hook('message.processed', function () {
        initSelect2Fields();
    });
});

NO DUPLICATES
NO EXTRA INIT FUNCTIONS

---

STEP 5 — DELETE ALL OLD PATCHES

REMOVE:
- forceSelect2Placeholders()
- setTimeout(forceSelect2Placeholders)
- placeholder CSS hacks
- initSelect2Properly()
- duplicate initSelect2Fields()

---

STEP 6 — HARD REFRESH TEST

CTRL + SHIFT + R

CHECK:

Tenant:
- Offered Lease Term → must show "Select"
- Leasing Space → must show "Select"

Landlord:
- Appliances Included → must show "Select"

Seller:
- Special Sale Provision → must show "Select"

Buyer:
- Acceptable Special Sale Provisions → must show "Select"

---

SUCCESS =

- Placeholder shows
- No blank Select2
- No icon-only fields
- No duplicate containers
- No console errors

---

STOP:

If STILL broken:
Send ONE field's:
- <select>
- .select2-container

DO NOT keep patching blindly