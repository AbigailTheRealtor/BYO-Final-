The new dedicated bid detail pages are still not correct.

## Problem

You created standalone bid detail pages, but they do **not** match the exact existing `View Bid` experience from inside the listing flow.

### Evidence

Compared to the listing-flow View Bid experience:

* Landlord page is incomplete
* Tenant / Buyer / Seller pages are closer but still not exact
* services are not grouped under the correct titles
* Broker Compensation & Agency Agreement Terms are using generic wording instead of the exact displayed values
* structure/order/content do not fully match the current listing-flow View Bid presentation

## Required correction

Do **not** continue maintaining separate reconstructed bid-detail templates.

Instead:

# SOURCE OF TRUTH RULE

For each role, the dedicated bid detail page must render the **exact same bid-detail body** currently shown when the user clicks `View Bid` from inside the listing flow.

That means:

* same structure
* same content
* same formatting
* same grouped services
* same compensation wording
* same labels
* same ordering

The dedicated page should only add:

* page wrapper
* back link
* page header if needed

But the bid-detail body itself must come from the same source as the listing-flow View Bid UI.

---

# REQUIRED IMPLEMENTATION STRATEGY

## Step 1 — Read-only audit first

For each role:

* Tenant
* Landlord
* Buyer
* Seller

report:

1. exact file/partial/blade block currently rendering the correct `View Bid` content inside the listing flow
2. whether that content is:

   * inline blade block
   * modal partial
   * included partial
   * controller-provided html fragment
3. exact minimal extraction plan so that this same content can be reused in both:

   * listing-flow View Bid
   * dedicated bid detail page

Do not code before giving this report.

---

## Step 2 — Refactor to shared source-of-truth partials

For each role, extract the existing correct View Bid body into a shared partial.

### Then use that same partial in:

1. listing-flow View Bid
2. dedicated bid detail page

### Important

Do NOT rewrite the content by hand again.
Do NOT create another parallel template.

The goal is:

* one role = one source of truth for bid detail rendering

---

## Scope

This applies to all 4 roles:

* Hire Tenant’s Agent
* Hire Landlord’s Agent
* Hire Buyer’s Agent
* Hire Seller’s Agent

---

## Safeguards

Do NOT:

* change bid business logic
* change accept/reject/counter logic
* change listing creation/edit
* change accepted summary logic
* change acknowledgement/signature logic
* change DB schema

You may:

* extract shared blade partials
* update controller methods only as needed to pass the same data the listing-flow View Bid already uses
* update dedicated bid pages to render the extracted partials

---

## Required final result

After this:

* dedicated bid detail pages for all 4 roles match the listing-flow View Bid experience exactly
* no more drift between modal/listing view and dedicated page
* services are under correct titles
* compensation formatting matches exactly
* section order/content matches exactly

---

## Output required after coding

1. exact shared partial(s) created
2. exact original listing-flow files updated to use the shared partials
3. exact dedicated bid detail pages updated
4. confirmation that each role now uses a single source of truth for bid detail rendering
5. confirmation no business logic was changed

## Stop rule

If any role’s listing-flow View Bid body cannot be safely extracted without changing business logic, stop and report the blocker before proceeding.
