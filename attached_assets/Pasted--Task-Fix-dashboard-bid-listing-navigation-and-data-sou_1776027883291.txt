**Task: Fix dashboard bid/listing navigation and data-source sync across Tenant, Buyer, Seller, and Landlord before full system audit**

## Goal

The redesigned dashboard is not fully wired to the latest bid/listing flow.

Current issues shown:

* **Hire Buyer’s Agent** and **Hire Seller’s Agent** dashboard entries are throwing a 500 error
* **Hire Tenant’s Agent** and **Hire Landlord’s Agent** still appear to be using older flow behavior
* dashboard pages are not consistently opening the latest working bid/listing pages
* both the **listing creator dashboard** and the **agent dashboard/profile** need to be updated to the latest code paths

This needs to be fixed **before** the full system audit, because the dashboard is currently not a reliable entry point.

---

## Scope

Audit and fix dashboard navigation + data sources for all 4 roles:

* Tenant
* Buyer
* Seller
* Landlord

And for both dashboard perspectives:

* **listing creator / client dashboard** (example: `tenant@exp.com`)
* **agent dashboard/profile**

---

## Hard safeguards

1. **Do not change DB schema**
2. **Do not change working bid/business logic unless the dashboard is pointing to the wrong route**
3. **Do not redesign dashboard UI**
4. **Do not change listing creation forms in this task**
5. **Do not refactor broadly**
6. **Fix routing/data-source mismatches surgically**
7. **Audit first, edit second**
8. **If multiple old/new dashboard systems are coexisting, identify exactly which one should be the source of truth before editing**
9. **Preserve the redesigned dashboard layout**
10. **Goal is to make the dashboard open the latest correct flow, not to invent a new one**

---

# PHASE 1 — READ-ONLY AUDIT FIRST

**Do not edit any code yet.**

I need a dashboard-specific audit first.

## A. Listing creator dashboard audit

For each sidebar/dashboard section:

* Hire Tenant’s Agent
* Hire Landlord’s Agent
* Hire Buyer’s Agent
* Hire Seller’s Agent
* My Bids
* any recent bids / recent notices / bid cards / listing cards / action buttons

Report:

1. exact file/view/component/controller powering the redesigned dashboard
2. exact route or action each card/button/sidebar item currently opens
3. whether it points to:

   * latest working flow
   * old legacy flow
   * broken/missing route
4. which roles are fully wired vs partially wired vs broken

## B. Agent dashboard/profile audit

For agent-side pages/profile/dashboard:

1. identify exact file/view/component/controller
2. identify how agent bid lists are loaded for each role
3. identify whether the bid links open the latest correct pages
4. identify whether any roles still point to old/legacy pages
5. identify any Buyer/Seller/Tenant/Landlord inconsistencies

## C. 500 error root cause audit

Specifically trace the 500 error when clicking:

* **Hire Buyer’s Agent**
* **Hire Seller’s Agent**

Report:

1. exact route being hit
2. exact controller/component/method
3. exact exception/root cause
4. whether the route is outdated, missing data, or loading the wrong view/model

## D. Old-vs-new dashboard system audit

Determine whether the app currently has:

* one redesigned dashboard with mixed old links
* multiple dashboard implementations
* role-specific dashboard partials/views still pointing to older controllers/routes

Report:

1. which dashboard files are current source of truth
2. which legacy files/routes are still being referenced
3. minimal safest path to unify behavior without broad rewrite

## E. Data-source sync audit

For each role and both dashboard types, verify the cards/lists pull from the correct current source:

* latest listings
* latest bids
* correct status
* correct counters
* correct action buttons
* correct accepted/rejected/countered state
* correct links to view listing / view bid / view counter / accepted summary

---

# PHASE 2 — REQUIRED AUDIT OUTPUT BEFORE CODING

Before making any changes, reply with a structured report:

## 1. Redesigned dashboard source of truth

* exact files
* exact controllers/components
* which parts are current
* which parts are still legacy

## 2. Listing creator dashboard by role

For Tenant / Buyer / Seller / Landlord:

* status: working / partially broken / broken
* current route path
* whether latest flow is used
* issues found

## 3. Agent dashboard by role

For Tenant / Buyer / Seller / Landlord:

* status: working / partially broken / broken
* current route path
* whether latest flow is used
* issues found

## 4. Buyer/Seller 500 error report

* exact root cause
* exact file/method causing it
* minimal fix plan

## 5. Recommended fix order

List the safest order to fix the dashboard so it becomes the reliable front door for the full audit.

**Do not code before giving this report.**

---

# PHASE 3 — IMPLEMENT ONLY CONFIRMED DASHBOARD FIXES

After the audit, fix only confirmed issues.

## Priority order

1. fix Buyer/Seller dashboard 500 errors
2. make all 4 listing creator dashboard sections open the latest correct pages
3. make all 4 agent dashboard sections open the latest correct pages
4. align dashboard data cards/lists/buttons to latest current flow
5. remove or bypass old legacy route references still being used by redesigned dashboard
6. keep UI intact, change only the plumbing needed

---

# REQUIRED OUTCOME

After the fix:

## Listing creator dashboard

For all 4 roles:

* sidebar item opens correctly
* listing tables/cards load correctly
* bid view buttons work
* accept/counter/reject buttons point to correct latest pages
* no 500 errors
* no old flow mixed in

## Agent dashboard/profile

For all 4 roles:

* agent sees correct role-specific bids
* bid links open correct latest pages
* counter/accepted/rejected links are correct
* no broken legacy routes
* no mismatch between dashboard and latest implementation

## Cross-role parity

* Tenant, Buyer, Seller, Landlord dashboard behavior should be aligned unless intentionally different
* redesigned dashboard should be the actual working source of truth

---

# QA AFTER FIX

After implementing:

1. test listing creator dashboard for all 4 roles
2. test agent dashboard/profile for all 4 roles
3. click every role-specific sidebar entry
4. click listing/bid action buttons
5. confirm Buyer/Seller 500 errors are gone
6. confirm Tenant/Landlord are using latest flow too, not old links
7. confirm no regressions to actual bid flow logic

---

# OUTPUT REQUIRED AFTER CODING

1. exact files changed
2. exact methods/views/components changed
3. Buyer/Seller 500 root cause
4. what legacy links/routes were replaced
5. how listing creator dashboard is now synced for all 4 roles
6. how agent dashboard is now synced for all 4 roles
7. confirmation that the dashboard is now ready to be used as the starting point for the full audit

## Stop rule

If fixing this cleanly requires a broader dashboard architecture decision, stop and report the options before proceeding. Otherwise make the smallest surgical fixes needed to make the redesigned dashboard fully functional and up to date.
