## Replit Instructions — Lock Buyer Agent Services Order to Match Create Listing (All Property Types)

**Applies To:** `tenant@exp.com` → **Hire a Buyer’s Agent**
**Property Types:** **Residential, Income, Commercial, Business, Vacant Land**
**Goal:** The **Services** section must display in the **exact same order** on the **actual listing** as it appears on the **Create Listing** screen for each property type.
This must be consistent everywhere Services appear: **actual listing, edits, drafts, bids, counter-bids, summaries, PDFs/exports, compare views, widgets**.

---

# SAFEGUARDS (Non-Negotiable)

1. **Do NOT rename** any DB tables/columns.
2. **Do NOT delete** or overwrite any saved service selections.
3. **Do NOT re-save** listings to “fix order” (no mass updates, no migrations for ordering).
4. **Do NOT change** the text of service items, emojis, headings, punctuation, or capitalization.
5. **Do NOT sort alphabetically** or by “checked order.”
6. Fix must be **render-time ordering only**, using a **single source of truth** per property type.
7. Must be **backward compatible** with existing listings saved with out-of-order arrays/JSON.
8. Must affect **Hire a Buyer’s Agent only** (do not change Tenant/Seller/Landlord services).

---

# REQUIRED OUTCOME

For each property type (Residential, Income, Commercial, Business, Vacant Land):

* Category headings must appear in the same order as Create Listing.
* Items within each category must appear in the same order as Create Listing.
* “✍️ Additional Services” must remain last.
* Any typed Additional Services must stay under “✍️ Additional Services” in the order entered.

---

# STEP 1 — Establish the Create Listing Screen as the Canonical Source

You must treat the Create Listing services configuration as the **canonical order** for each property type.

✅ Create a single source of truth (choose ONE approach):

### Option A (Preferred): Config

Create/update:
`config/services_order.php`

Add canonical lists for:

* `buyer_agent.residential`
* `buyer_agent.income`
* `buyer_agent.commercial`
* `buyer_agent.business`
* `buyer_agent.vacant_land`

### Option B: Support Class

Create/update:
`app/Support/ServicesOrder.php`
with methods/constants for each property type above.

**Safeguard:** Do not duplicate lists in multiple files. One source only.

---

# STEP 2 — Implement/Use One Shared Formatter to Order Selected Services

Create/update a shared formatter used everywhere:

`app/Support/ServicesFormatter.php`

### Required method

`orderSelectedServices($selectedServices, $flowKey)`

**Must:**

1. Normalize saved selections (array/json/string) into a flat array of service strings.
2. Load the canonical ordered list for the correct `$flowKey`.
3. Return grouped output in canonical order:

   * Include only selected items
   * Preserve category order
   * Preserve item order
4. Handle legacy stored formats safely (extra whitespace, casing, duplicates).
5. Append “✍️ Additional Services” last (typed items separate; do not mix).

**Do NOT:**

* Alphabetically sort
* Reorder based on selection time
* Modify the stored DB value

---

# STEP 3 — Route ALL Service Rendering Through the Formatter (Everywhere)

Every location that outputs Services must use the formatter output rather than raw stored arrays.

✅ Minimum required surfaces:

* Actual listing view
* **Edit listing preview / recap**
* **Draft preview**
* Agent bid view
* Counter-bid view
* Summary/confirmation pages
* PDF/export/Accepted Bid Summary
* Compare All Bids / ranking cards (if services appear)
* Any widget/embed that prints services

**Implementation rule:**
Determine property type and call the correct key:

* Residential → `buyer_agent.residential`
* Income → `buyer_agent.income`
* Commercial → `buyer_agent.commercial`
* Business → `buyer_agent.business`
* Vacant Land → `buyer_agent.vacant_land`

Example usage pattern (conceptual):

* `$flowKey = ServicesOrder::keyForBuyerAgent($propertyType)`
* `$ordered = ServicesFormatter::orderSelectedServices($model->services, $flowKey)`

**Safeguard:** Listings show listing services; bids show bid services; counters show counter services (do not accidentally read the wrong model).

---

# STEP 4 — Remove/BYPASS Any Existing Sorting That Breaks Canonical Order

Search codebase for any ordering applied to services such as:

* `sort()`, `asort()`, `ksort()`
* `collect()->sortBy()`, `->sort()`
* `orderBy()` on service rows
* Any “displayMap” that reindexes services alphabetically

✅ Do NOT remove globally. Only bypass for **Hire a Buyer’s Agent Services** and only after routing through the formatter.

---

# STEP 5 — Canonical Lists Must Match Create Listing EXACTLY

For each property type, the canonical config list must be copied from the **Create Listing** UI order exactly:

* Same headings
* Same item text
* Same order

**Important:** If the text differs even slightly, matching can fail. Use exact strings.

---

# STEP 6 — Testing & Proof Checklist (Required)

Developer must prove order matches Create Listing for each property type **in at least these places**:

### For EACH property type (Residential / Income / Commercial / Business / Vacant Land):

1. Create Listing screen order confirmed (reference)
2. Actual listing view matches it exactly
3. Edit listing view matches it exactly
4. Draft save → reload → matches it exactly
5. Agent bid view matches it exactly
6. Counter-bid view matches it exactly
7. Summary page matches it exactly
8. PDF/export matches it exactly (if includes services)

✅ Also confirm:

* Additional Services stays last
* Typed additional services remain under Additional Services
* No changes to Tenant/Seller/Landlord services

---

# STEP 7 — Deliverable

Provide a completion report with:

* List of files changed
* Confirmation of canonical lists added for all 5 property types
* Screenshots (or recordings) showing at least 1 listing per property type with correct ordering on the actual listing page
* Regression confirmation

---

## NOTE FOR REPLIT

Right now “a few are out of order” because services are being rendered from stored selection order or a sort step. This instruction set fixes it permanently by enforcing a single canonical order per property type and using it everywhere services are displayed.

If you want, paste the **Residential** and **Income** “correct order” lists (like you did for Business/Commercial/Vacant Land) and I’ll format them into ready-to-drop `services_order.php` blocks so Replit can copy/paste without typos.
