REPLIT INSTRUCTIONS WITH SAFEGUARDS — SELLER BID VIEW DATA + FORMAT PARITY FIX

SCOPE

Fix ONLY Seller Agent Bid View so it:

1. Uses the exact SAME services that were selected on the listing (no extra services)
2. Uses the exact SAME formatting as the Seller listing view for:

   * compensation
   * percentages
   * combined values (no split “Amount” rows)
3. Removes incorrect service injection
4. Matches listing display format exactly for all Broker Compensation & Terms

Do NOT touch:

* Buyer
* Landlord
* Tenant (except referencing for structure)
* submit flow
* edit flow
* database schema

This is a Seller bid VIEW + DATA MAPPING fix only.

---

🚨 ROOT PROBLEMS IDENTIFIED

1. Seller bid is NOT using the listing’s selected services
   → It is pulling from a master/default service list instead

2. Seller compensation formatting is using a DIFFERENT formatter than listing view

3. Seller terms are split incorrectly:
   ❌ “Field” + separate “Amount”
   instead of:
   ✅ combined display like listing

4. Percent formatting is wrong:
   ❌ 3.00%, 50.00%
   ✅ 3%, 50%

5. Lease + lease-option formatting is incorrect:
   ❌ Amount: 6
   ✅ 6% of Net Aggregate Rent

---

✅ GOAL

Seller Bid View must be a **visual + data mirror of the listing view**, using:

* SAME selected services
* SAME formatting rules
* SAME display logic
* SAME combined field structure

NOT a separate system.

---

🔒 HARD SAFEGUARDS — NON-NEGOTIABLE

1. DO NOT change database structure
2. DO NOT rename fields or models
3. DO NOT modify listing creation logic
4. DO NOT modify Tenant logic
5. DO NOT hardcode values
6. DO NOT use fallback/default service lists
7. ONLY use saved bid/listing data
8. DO NOT refactor unrelated files
9. Make surgical changes only
10. STOP if you cannot trace the correct data source

---

🧠 STEP 1 — FIND TRUE DATA SOURCE

You MUST confirm:

Where Seller bid services are being pulled from.

Likely issue:
Seller view is using something like:

* config/services list
* master service array
* default template

Instead of:

* saved bid data
* OR saved listing-selected services

👉 The correct source must be:
the ACTUAL saved selected services tied to that listing/bid

---

🧠 STEP 2 — FIX SERVICES (CRITICAL)

PROBLEM:
Seller bid is showing services that are NOT in the listing


FIX:

1. Locate Seller services render logic
2. Replace data source with:

ONLY:

* selected services stored on the listing
  OR
* selected services stored on the agent bid (depending on architecture)

3. DO NOT:

* merge with default services
* include all categories
* include unchecked services

4. Render ONLY:

* selected services
* selected sub-options (like photo enhancements)
* additional services entered by user

5. Maintain grouping:
   same categories as listing

---

🧠 STEP 3 — MATCH LISTING FORMAT (VERY IMPORTANT)

Seller Bid View must reuse SAME formatting logic as listing view.

DO NOT recreate formatting manually.

👉 Find listing view formatter for:
Broker Compensation & Terms

Reuse or call that same logic.

---

🧠 STEP 4 — FIX COMBINED FIELD FORMAT

Replace this pattern:
❌
Early Termination Fee: Yes
Amount: $4,000

WITH:
✅
Early Termination Fee: Yes ($4,000)

---

Replace:
❌
Seller's Broker Leasing Fee: Percentage of Net Aggregate Rent
Amount: 6

WITH:
✅
Seller’s Broker Leasing Fee: 6% of Net Aggregate Rent

---

Replace:
❌
Compensation for Creating Lease-Option Agreement: 5.00%

WITH:
✅
5% of Option Consideration

---

📌 RULE:

If a field has:

* type + value

→ combine them into ONE display line

---

🧠 STEP 5 — FIX PERCENT FORMATTING

Globally for Seller Bid View ONLY:

Convert:

* 3.00% → 3%
* 50.00% → 50%
* 5.50% → keep if needed

RULE:

* trim trailing zeros
* preserve meaningful decimals

---

🧠 STEP 6 — TERMS SECTION PARITY WITH LISTING

Seller Bid View MUST include ALL sections that exist in listing:

A) Seller’s Broker Compensation
B) Lease Terms
C) Lease-Option Terms
D) Legal Terms
E) Brokerage Relationship
F) Additional Terms

DO NOT:

* drop fields
* simplify sections
* collapse structure

---

🧠 STEP 7 — DO NOT SPLIT DATA

If listing shows:
Seller’s Broker Purchase Fee:
5% of Total Purchase Price + $6,000

Seller bid must show EXACT SAME STRING

NOT:

* broken into pieces
* reassembled incorrectly

---

🧠 STEP 8 — VERIFY DISPLAY MATCHES LISTING

Side-by-side check REQUIRED:

Seller Listing View vs Seller Bid View must match for:

1. Services (exact same items)
2. Service grouping
3. Compensation formatting
4. Percent formatting
5. Lease terms formatting
6. Legal terms formatting
7. Combined value formatting

---

🧪 TESTING — MUST PASS

Use Seller listing:
https://3ce01d2c-f79e-44c2-b048-2e8dc64316ef-00-tu7aronbf5wk.spock.replit.dev/seller/agent/auction/view/68

TEST:

SERVICES

1. Only selected services appear
2. No extra services appear
3. Categories match listing
4. Sub-options display correctly

COMPENSATION

1. Seller’s Broker Purchase Fee matches listing EXACTLY
2. No split “Amount” rows
3. Lease fee shows as:
   6% of Net Aggregate Rent
4. Lease-option shows correctly
5. Early termination shows:
   Yes ($4,000)

PERCENTAGES

1. No .00%
2. Clean values only

TERMS

1. All sections present
2. No missing fields
3. No duplicate fields

---

🚫 DO NOT DO

1. Do not hardcode “%”
2. Do not manually rebuild strings without checking listing formatter
3. Do not use default service arrays
4. Do not flatten services
5. Do not remove categories
6. Do not modify listing code
7. Do not touch Tenant

---

📌 SUCCESS CRITERIA

This is ONLY complete when:

1. Seller Bid View = exact mirror of Seller Listing View (format + data)
2. No extra services appear
3. All values formatted correctly
4. Percentages cleaned
5. Combined fields display properly
6. No missing sections
7. No regressions elsewhere

---

📌 REQUIRED RESPONSE FROM REPLIT

Must include:

1. Root cause of wrong services appearing
2. Root cause of split formatting
3. Exact files changed
4. Exact data source corrected
5. Confirmation listing formatter reused or matched
6. Before vs after behavior
7. Proof services now match listing exactly

DO NOT say fixed without proving service list + formatting match listing exactly
