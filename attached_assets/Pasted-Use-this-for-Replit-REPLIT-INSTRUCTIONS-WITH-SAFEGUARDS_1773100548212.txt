Use this for Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A LANDLORD’S AGENT ONLY — “OTHER” BOX NOT APPEARING FOR SPECIFIC FIELDS

### SCOPE

Fix **only** the remaining **Hire a Landlord’s Agent** issue where selecting **Other** in certain fields does **not** show the related custom input box.

The core multi-select selection problem appears improved.
This task is now only about the **dependent “Other” field display logic** for the Landlord flow.

Use **View** as the **working reference example**, because under **View**, when the user selects **Other**, the custom box correctly appears underneath.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename or delete database columns.**
2. **Do not modify Buyer, Seller, or Tenant flows.**
3. **Do not change labels, tooltips, or option values.**
4. **Do not broad-refactor shared components unless absolutely necessary for this Landlord-only fix.**
5. **Do not change the working behavior of View.**
6. **Keep this fix surgical and limited to the specific Landlord fields listed below.**
7. **Do not break create, draft, edit, or listing display.**

---

# ISSUE

In **Hire a Landlord’s Agent**, the options now select correctly, but for the following fields, when the user selects **Other**, the related custom input box does **not** appear.

### Correct working example

* **View** → user selects **Other** → custom box appears underneath ✅

### Still broken

* **Appliances Included**
* **Desired Lease Term**
* **Tenant Pays**
* **Owner Pays**
* **Desired Lease Terms**
* **Rent Includes**

---

# REQUIRED FIX

For each broken field above:

* when user selects **Other**, the corresponding custom input box must appear immediately underneath
* when **Other** is unselected, the custom input box must hide again
* if **Other** remains selected, the custom input box must stay visible after rerender
* the custom value must save and reload correctly in draft/edit
* listing display must remain correct

---

# REQUIRED DEBUG METHOD

## Use **View** as the reference pattern

Compare each broken field directly against the working **View** implementation.

For each field, compare:

1. the select field markup
2. the Livewire property name
3. the “other text” property name
4. the conditional Blade logic that shows the custom input
5. whether the condition is checking for `"Other"` correctly inside a multi-select array
6. whether the custom input is inside the correct conditional block
7. whether rerender is hiding it again
8. whether the saved custom value reloads correctly

---

# MOST LIKELY ROOT CAUSE TO CHECK

These fields are multi-select, so the conditional logic must check whether **"Other" exists in the selected array**.

Examples of what to inspect:

* if the condition is written as though the field is a single string instead of an array
* if the condition is checking the wrong property
* if the field name and the “other” companion field name do not match
* if the dependent input block is placed under the wrong section or wrong condition

---

# FIELD-SPECIFIC REQUIREMENT

Please confirm each of these has its own correctly wired custom “Other” input:

* **Appliances Included** → selecting **Other** shows custom box
* **Desired Lease Term** → selecting **Other** shows custom box
* **Tenant Pays** → selecting **Other** shows custom box
* **Owner Pays** → selecting **Other** shows custom box
* **Desired Lease Terms** → selecting **Other** shows custom box
* **Rent Includes** → selecting **Other** shows custom box

---

# REQUIRED TESTING

For **each** of the broken fields above, test this exact flow:

1. open field
2. select one normal option
3. select **Other**
4. confirm the custom input box appears immediately underneath
5. type a custom value
6. save draft or save listing
7. reopen edit
8. confirm **Other** is still selected
9. confirm the custom input box is still visible
10. confirm the saved custom value is still there

Also test:

1. select **Other**
2. confirm box appears
3. unselect **Other**
4. confirm box hides again

---

# SUCCESS CRITERIA

This task is only complete when all of the following are true in **Hire a Landlord’s Agent**:

* **Appliances Included** → Other box appears correctly
* **Desired Lease Term** → Other box appears correctly
* **Tenant Pays** → Other box appears correctly
* **Owner Pays** → Other box appears correctly
* **Desired Lease Terms** → Other box appears correctly
* **Rent Includes** → Other box appears correctly
* **View** continues working correctly
* create / draft / edit / save / reopen all remain stable

---

# FINAL RULE

Please use **View** as the known-good pattern and make the broken Landlord fields follow that exact same “Other selected → custom box appears” behavior.

Do not mark this complete until each listed field has been tested in the browser end-to-end.
