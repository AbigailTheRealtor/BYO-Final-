Got it — these screenshots are exactly what we needed. Below are:

1. the **exact missing fields** (as shown in your screenshots), and
2. **Replit instructions with safeguards** to ensure these fields **(a) save in Draft**, **(b) re-populate on Draft reopen/Edit**, and **(c) display on the public Listing view** for **Hire a Buyer Agent**.

---

## 1) Exact missing fields (from your screenshots)

### Seller Financing (follow-up fields)

* **Amortization Type**
* **Payment Frequency**
* **Late Payment Fee**

### Exchange / Trade (follow-up fields)

* **Transfer Method / Logistics**
* **Liens / Encumbrances Disclosure**
* **Inspection / Verification Rights**

### Lease Option / Lease Purchase (follow-up fields)

* **Maintenance / Repair Responsibility**
* **Extension Terms**
* **Rent Credit Toward Purchase Price** (Yes/No + **Rent Credit Dollar Amount**)
* **Non-Refundable Deposit / Purchase Deposit**

### Cryptocurrency (follow-up fields)

* **Exchange / Conversion Method**
* **Custodian / Wallet for Transfer**
* **Transaction Fees Responsibility**
* **Timing of Transfer**

### NFT (follow-up fields)

* **NFT Valuation Method**
* **NFT Transfer Method**
* **Transaction Fees Responsibility (Gas Fees)**

---

# 2) Replit Instructions — Hire a Buyer Agent

## Fix: These fields must save in Draft AND show on Listing (with safeguards)

## ⚠️ CRITICAL SAFEGUARDS

* ✅ Apply ONLY to **Hire a Buyer Agent** flow
* ✅ **Do NOT mass-update database rows**
* ✅ **Do NOT wipe** existing saved values when users edit/reopen drafts
* ✅ Do **NOT** store placeholder text as values
* ✅ Must work in **all** render contexts:

  * Create Listing
  * Save Draft
  * Reopen Draft
  * Edit Listing
  * Public Listing page (the share/auction listing view)

---

## A) Make sure these fields are bound to persistent Livewire state (required)

### Step A1 — Find the Offered Financing/Currency tab Blade file(s)

In Replit search (Cmd/Ctrl+Shift+F) for these labels to locate the correct file(s):

* `Amortization Type`
* `Transfer Method / Logistics`
* `Exchange / Conversion Method`
* `NFT Valuation Method`

### Step A2 — Update `wire:model` for each missing field (non-negotiable)

Every missing field must bind into ONE persistent structure, for example:

**Recommended structure (single source of truth):**
`wire:model.defer="offered_financing_details.<section>.<field>"`

Use these exact mappings:

#### Seller Financing

* Amortization Type
  `wire:model.defer="offered_financing_details.seller_financing.amortization_type"`
* Payment Frequency
  `wire:model.defer="offered_financing_details.seller_financing.payment_frequency"`
* Late Payment Fee
  `wire:model.defer="offered_financing_details.seller_financing.late_payment_fee"`

#### Exchange / Trade

* Transfer Method / Logistics
  `wire:model.defer="offered_financing_details.exchange_trade.transfer_method_logistics"`
* Liens / Encumbrances Disclosure
  `wire:model.defer="offered_financing_details.exchange_trade.liens_encumbrances_disclosure"`
* Inspection / Verification Rights
  `wire:model.defer="offered_financing_details.exchange_trade.inspection_verification_rights"`

#### Lease Option / Lease Purchase

(If you have separate sections for lease_option and lease_purchase, put them in BOTH. If you only have one combined block, store once under a shared key like `lease_terms`.)

* Maintenance / Repair Responsibility
  `wire:model.defer="offered_financing_details.lease_terms.maintenance_repair_responsibility"`
* Extension Terms
  `wire:model.defer="offered_financing_details.lease_terms.extension_terms"`
* Rent Credit Toward Purchase Price (Yes/No)
  `wire:model.defer="offered_financing_details.lease_terms.rent_credit_enabled"`
* Rent Credit Dollar Amount
  `wire:model.defer="offered_financing_details.lease_terms.rent_credit_amount"`
* Non-Refundable Deposit / Purchase Deposit
  `wire:model.defer="offered_financing_details.lease_terms.non_refundable_deposit_amount"`

#### Cryptocurrency

* Exchange / Conversion Method
  `wire:model.defer="offered_financing_details.cryptocurrency.exchange_conversion_method"`
* Custodian / Wallet for Transfer
  `wire:model.defer="offered_financing_details.cryptocurrency.custodian_wallet_for_transfer"`
* Transaction Fees Responsibility
  `wire:model.defer="offered_financing_details.cryptocurrency.transaction_fees_responsibility"`
* Timing of Transfer
  `wire:model.defer="offered_financing_details.cryptocurrency.timing_of_transfer"`

#### NFT

* NFT Valuation Method
  `wire:model.defer="offered_financing_details.nft.nft_valuation_method"`
* NFT Transfer Method
  `wire:model.defer="offered_financing_details.nft.nft_transfer_method"`
* Transaction Fees Responsibility (Gas Fees)
  `wire:model.defer="offered_financing_details.nft.gas_fees_responsibility"`

✅ **Safeguard:** Use **`.defer`** on all of these so Livewire doesn’t drop values when users switch tabs.

✅ **Safeguard:** Wrap each conditional section in a stable `wire:key`, e.g.

* `wire:key="buyer-seller-financing-extra"`
* `wire:key="buyer-exchange-trade-extra"`
* `wire:key="buyer-crypto-extra"`
* `wire:key="buyer-nft-extra"`

This prevents Livewire from recreating DOM blocks and losing unsaved input state.

---

## B) Draft saving must persist these keys (merge, don’t overwrite)

### Step B1 — In Buyer Livewire component: ensure Save Draft writes BOTH:

1. `offered_financing` (the selected financing types list)
2. `offered_financing_details` (the follow-up answers)

### Step B2 — Merge safeguard (critical)

In `saveDraft()` (and in final `save()` / `publish()` if separate), do:

* Load existing `$existingDetails` from model
* Merge with `$this->offered_financing_details` **recursively**
* Save merged back to model

✅ **Safeguard:** Do NOT do a plain assignment if conditional sections can be hidden at save-time (that’s how fields get wiped).

---

## C) Draft re-open / Edit must rehydrate before render

### Step C1 — In `mount()` / `loadDraft()` / `editMount()`:

* Normalize `offered_financing` to an array (never string)
* Hydrate `offered_financing_details` from model into Livewire property **before** the first render

### Step C2 — Ensure default objects exist (so conditionals load)

After hydration, ensure these keys exist even if empty:

* `seller_financing: {}`
* `exchange_trade: {}`
* `lease_terms: {}`
* `cryptocurrency: {}`
* `nft: {}`

✅ **Safeguard:** Do NOT set `offered_financing_details = []` after hydration (that wipes data).

---

## D) Listing page must display these exact fields

Right now, your listing is showing many financing fields, but not these missing ones. So the listing template is not rendering these keys.

### Step D1 — Locate the Buyer public listing display template

Search for the line that prints:

* `Offered Financing/Currency:`

### Step D2 — Add display rows for each missing field (show-if-filled)

For the public listing view, add rows like:

**Seller Financing**

* Amortization Type
* Payment Frequency
* Late Payment Fee

**Exchange/Trade**

* Transfer Method / Logistics
* Liens / Encumbrances Disclosure
* Inspection / Verification Rights

**Lease terms**

* Maintenance / Repair Responsibility
* Extension Terms
* Rent Credit Toward Purchase Price (and amount if enabled)
* Non-Refundable Deposit / Purchase Deposit

**Crypto**

* Exchange / Conversion Method
* Custodian / Wallet for Transfer
* Transaction Fees Responsibility
* Timing of Transfer

**NFT**

* NFT Valuation Method
* NFT Transfer Method
* Transaction Fees Responsibility (Gas Fees)

✅ **Safeguard:** only display a row if the value is not empty.

---

## E) QA checklist (must pass)

1. Fill these missing fields

2. Save Draft

3. Exit

4. Reopen Draft
   ✅ All values are still filled

5. Publish/view public listing
   ✅ These fields appear on the listing

6. Click Edit Listing
   ✅ Same layout as Create, values still present

---

If you want, paste the **Buyer Livewire component class name** (or the file path for the Buyer create/edit component), and I’ll turn sections **B + C** into exact “open file X → paste this code under saveDraft() and mount()” instructions using your real variable names.
