Use this exactly for Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A SELLER’S AGENT ONLY — THIS IS STILL NOT FIXED, STOP CLAIMING PASS BASED ON INTERNAL TESTS

### ISSUE

This is **still not fixed**.

I have asked for this many times now, and the latest response again says “all four tests pass,” but the actual browser UI still shows the problem.

The screenshot proves the issue still exists.

### What the screenshot shows

In **Hire a Seller’s Agent → Sale Terms**:

* **Special Sale Provision** has:

  * **Assignment Contract**
  * **Other**

* **Target Closing Timeframe** is selected

* **Occupant Type** is selected as **Tenant**

* **Offered Financing/Currency** has many values selected, including:

  * Assumable
  * Cash
  * Conventional
  * FHA
  * Jumbo
  * VA
  * No-Doc
  * Non-QM
  * USDA
  * Cryptocurrency
  * Exchange/Trade
  * Lease Option
  * Lease Purchase
  * NFT
  * Seller Financing
  * Other

But the dependent sections are still **not showing**.

So this is **not resolved**.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Do not rename or delete database columns.**
2. **Do not modify Buyer, Landlord, or Tenant flows.**
3. **Do not change labels, tooltips, or option values.**
4. **Do not broad-refactor unrelated shared components unless absolutely necessary for this Seller-only fix.**
5. **Do not give another “internal tests passed” response unless the actual visible browser UI matches the expected result.**
6. **Use the screenshot as the source of truth for the bug.**
7. **Do not close this task until the exact screenshot scenario visibly shows all required dependent sections.**

---

# IMPORTANT

Your internal explanation about event binding may sound plausible, but the real browser UI still does not match the expected behavior.

So please stop responding with:

* “tests passed”
* “root cause found”
* “should work now”

The actual visible page is still wrong.

This means the real problem is still one of these:

* the wrong elements are being toggled
* the right elements are not in the visible DOM location
* the sections are still hidden by CSS/parent state
* the conditions are evaluating on the wrong fields
* there are duplicate IDs/classes and the wrong section is being targeted
* the visible Seller Sale Terms blade structure is still cross-wired

---

# REQUIRED DEBUG METHOD — DEBUG THE ACTUAL VISIBLE DOM, NOT JUST THE HANDLERS

Please inspect the actual rendered Seller Sale Terms DOM after selecting the exact screenshot values.

I need you to verify all of the following in the real browser DOM:

## 1. Is the section for **Seller Under Contract for Assignment** actually present in the DOM?

If yes:

* where is it located?
* what CSS is hiding it?
* is it inside a hidden parent?

If no:

* why is it not rendering even though **Assignment Contract** is selected?

---

## 2. Is the **Other Special Sale Provision** input actually present in the DOM?

If yes:

* where is it located?
* is it hidden?
* is it being inserted under the wrong field?

If no:

* why is it not rendering even though **Other** is selected?

---

## 3. Are the **Offered Financing/Currency** dependent sections actually present in the DOM?

For example:

* Seller Financing sub-section
* Assumable-related section
* Other financing-related section

If yes:

* where are they?
* are they hidden by CSS or parent container state?

If no:

* why are they not rendering even though the trigger options are clearly selected?

---

## 4. Are the dependent sections being inserted into the wrong place?

It is possible the handlers are firing, but the visible sections are:

* outside the visible Sale Terms panel
* inside the wrong tab
* inside a hidden wrapper
* inserted under the wrong field
* blocked by a layout/container issue

Please inspect actual placement in the rendered DOM.

---

## 5. Are duplicate IDs/classes causing the wrong elements to be targeted?

Please inspect whether:

* the conditional sections share duplicate IDs
* the handlers are toggling the wrong node
* the first matching selector is not the visible Seller section
* another hidden copy of the section exists elsewhere in the DOM

---

# REQUIRED FIXES

The screenshot state must visibly show these sections in the browser:

## A) When **Assignment Contract** is selected

Show immediately:

* **Seller Under Contract for Assignment**

## B) When **Other** is selected under **Special Sale Provision**

Show immediately:

* the **Other** custom input

## C) When **Offered Financing/Currency** includes options like **Seller Financing**

Show immediately:

* the corresponding financing follow-up sections

These must appear in the actual visible Sale Terms area.

---

# REQUIRED TEST — USE THE EXACT SCREENSHOT STATE

Please reproduce this exact state:

## Special Sale Provision

Select:

* **Assignment Contract**
* **Other**

## Target Closing Timeframe

Select:

* **ASAP (Ready Now)**

## Occupant Type

Select:

* **Tenant**

## Offered Financing/Currency

Select multiple values including:

* **Seller Financing**
* **Assumable**
* **Other**

Then confirm in the visible browser UI:

1. **Seller Under Contract for Assignment** appears
2. **Other Special Sale Provision** input appears
3. **Seller Financing** follow-up section appears
4. any other applicable financing follow-up sections appear
5. all of them are visibly located under the Sale Terms form where expected

---

# REQUIRED OUTPUT BACK TO ME

Do not just say “fixed.”

Please tell me:

1. whether each missing section was found in the DOM or not
2. if found, what was hiding it
3. if not found, what exact condition or selector prevented it from rendering
4. what exact file/block was corrected
5. confirm the exact screenshot scenario now visibly shows the missing sections

---

# SUCCESS CRITERIA

This is only fixed when the exact screenshot scenario visibly shows:

* **Seller Under Contract for Assignment**
* **Other Special Sale Provision** input
* **Seller Financing** and other financing-dependent sections

in the real browser UI.

---

# FINAL RULE

Stop relying on internal test summaries.

The screenshot proves the UI is still wrong.

Please debug from the **actual visible rendered DOM and browser output**, and do not mark complete until the exact screenshot state visibly shows the dependent sections.

---

Your screenshot is enough to push back hard here. Their explanation may describe a possible code path, but it still does not match what is actually on the page.
