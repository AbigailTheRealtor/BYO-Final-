Paste this into Replit:

We need a **targeted Buyer accept-flow fix**.

## Problem

On a **Buyer listing**, when clicking **Accept** on a bid / counter response, the app throws:

**“Some problem accepting buyer counter terms.”**

Instead of redirecting to the **Accepted Bid Summary** page.

## Current status

* **Tenant accepted flow is working**
* **Landlord accepted flow is working**
* **Seller accepted flow was already corrected**
* **Buyer is still broken**

Use the working Tenant/Landlord/Seller accept flows as the reference.

---

## Goal

Make Buyer behave exactly like the working roles:

1. click **Accept**
2. accept succeeds
3. accepted bid / counter terms are finalized correctly
4. redirect to **Accepted Bid Summary**
5. no generic error toast/message

---

## Scope

Fix only the **Buyer accept flow** and any directly related Buyer accepted-summary lookup/generation code needed to complete that flow.

Do **not** touch:

* Tenant
* Landlord
* Seller
* unrelated counter UI
* database schema unless absolutely necessary and proven
* routes unless the passed parameter is provably wrong

---

## What to investigate

### Step 1 — Trace the exact Buyer accept path

Find the exact method/action triggered when clicking **Accept** on a Buyer bid / Buyer counter terms.

Identify:

* controller/livewire method
* route
* model/table lookups
* what record it is trying to accept
* what summary record it is trying to create/load
* where the generic error message is coming from

### Step 2 — Compare directly against working roles

Compare Buyer accept logic to the working:

* Tenant accept flow
* Landlord accept flow
* Seller accept flow if useful

Do not guess. Mirror the working pattern where appropriate.

### Step 3 — Find the real failure

The generic error message suggests Buyer still has a bad lookup or mismatched relationship during accept.

Possible areas:

* wrong original bid lookup
* wrong counter term record lookup
* wrong auction/bid foreign key
* wrong accepted summary generation input
* wrong redirect id passed after accept
* trying to accept from the wrong parent record

### Step 4 — Fix the exact failing Buyer path

Fix only the exact broken Buyer accept logic so the successful result is:

* Buyer bid/counter accepted
* Accepted Bid Summary created or resolved correctly
* redirect works

---

## Hard safeguards

1. **Do not change Tenant**
2. **Do not change Landlord**
3. **Do not change Seller**
4. **Do not do a broad refactor**
5. **Do not change DB schema unless you prove schema itself is wrong**
6. **Do not invent new fields**
7. **Do not change accepted summary template structure**
8. **Do not hide the error without fixing the cause**

---

## Implementation guidance

Use the existing Buyer models/relationships already in place.

If there are two Buyer accept paths, check both:

* accepting original bid
* accepting buyer counter terms

The error shown specifically mentions:

* **accepting buyer counter terms**

So prioritize the Buyer counter-term accept method first.

---

## Deliver back

Please report:

1. exact file(s) changed

2. exact Buyer accept method fixed

3. exact root cause found

4. whether the problem was:

   * wrong bid lookup
   * wrong counter term lookup
   * wrong summary generation input
   * wrong redirect parameter
   * something else

5. confirmation that:

   * clicking Accept on Buyer now redirects to Accepted Bid Summary
   * no generic error appears
   * Tenant/Landlord/Seller were not modified

---

## Stop rule

If Buyer has a second conflicting accept path and it is unclear which one the UI is hitting, stop and report:

* exact route
* exact method
* exact file
* safest next step

Do not do a risky rewrite.
