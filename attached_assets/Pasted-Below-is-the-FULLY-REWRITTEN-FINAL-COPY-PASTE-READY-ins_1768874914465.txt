Below is the **FULLY REWRITTEN, FINAL, COPY-PASTE-READY instruction set** with **Listing Details included**, **correct file locations**, **strict safeguards**, **unified Acceptable Property Condition**, and **mandatory verification across all 5 property types**.

This is the **single source of truth** you can paste into Replit.

---

# Replit Instructions — [Tenant@exp.com](mailto:Tenant@exp.com)

## Hire a Buyer’s Agent

### **Save Draft + Edit Listing + Listing Display MUST WORK 100%**

**(Residential · Income · Commercial · Business · Vacant Land | Traditional + Bidding Period)**

---

## PRIMARY FLOW — USE THESE FILES ONLY

When a user at **[tenant@exp.com](mailto:tenant@exp.com)** clicks **“Hire a Buyer’s Agent”** from the dashboard, the system uses:

| Component                             | File Path                                          |
| ------------------------------------- | -------------------------------------------------- |
| Create / Save Draft                   | `app/Http/Livewire/TenantAgentAuction.php`         |
| Edit Listing / Edit Draft             | `app/Http/Livewire/TenantAgentAuctionEdit.php`     |
| Views (Create, Edit, Listing Display) | `resources/views/livewire/tenant-agent-auction/**` |

**Important:**
The `user_type = 'buyer'` parameter tells `TenantAgentAuction.php` to use the **HireBuyerAgentAuction** model/table.

🚫 **DO NOT EDIT:** `BuyerAgentAuction.php`
That file is tied to `/buyer/add-auction` and is **not the primary user path**.

---

## SAFEGUARDS (MANDATORY — DO NOT VIOLATE)

1. **DO NOT rename** files, classes, routes, DB tables, or DB columns
2. **DO NOT delete or alter** existing fields, labels, tooltips, or dropdown options
3. **DO NOT reset or wipe values** when sections hide due to conditional logic
4. **DO NOT block Save Draft** due to submit-level validation
5. **DO NOT create property-type-specific dropdown lists** for shared fields
6. All changes must be **additive and non-destructive**

---

## UNIFIED FIELD — ACCEPTABLE PROPERTY CONDITION

This field applies to **ALL 5 PROPERTY TYPES**:

* Residential
* Income
* Commercial
* Business
* Vacant Land

### **Single Source of Truth (EXACT wording)**

The dropdown options for **Acceptable Property Condition** must be **identical** in:

* Create Listing
* Save Draft
* Edit Listing
* Listing Display validation

**Use this exact list everywhere:**

1. **No updates needed: Completely updated**
2. **Currently being built**
3. **New Construction**
4. **Not updated: Requires a complete update**
5. **Open to any type of property condition**
6. **Pre-Construction**
7. **Semi-updated: Needs minor updates**
8. **Tear Down: Requires complete demolition and reconstruction**

### Safeguard

* If older drafts contain legacy values:

  * **Do NOT wipe them**
  * Display the saved value or safely map it without data loss

---

## CORE REQUIREMENT — SAVE DRAFT MUST PERSIST EVERYTHING

### Applies To

* Traditional **and** Bidding Period
* ALL property types
* ALL tabs

### Implementation (TenantAgentAuction.php)

* Save Draft must persist **ALL Livewire form fields** in **ONE unified payload**
* Persistence must **NOT** depend on:

  * property type
  * active tab
  * field visibility

**Rule:**
👉 *Visibility ≠ Persistence*

### Draft Validation

* Draft saves may validate **format only** (dates, numbers)
* Draft saves must **never fail** due to missing required submit fields

---

## EDIT LISTING REQUIREMENT — FULL HYDRATION REQUIRED

### Applies To

* Edit Draft
* Edit Published Listing

### Implementation (TenantAgentAuctionEdit.php)

When editing a listing:

1. Load the stored payload
2. Hydrate **EVERY Livewire property**
3. Use `array_key_exists()` (NOT `empty()`) to preserve:

   * `0`
   * `false`
   * empty arrays

After hydration:

* Re-run conditional logic so correct sections reappear
* **Never reset values** when sections hide

---

## REQUIRED — ALL TABS MUST SAVE + RELOAD (INCLUDING LISTING DETAILS)

Save Draft **and** Edit Listing must persist **and repopulate 100% of fields** across **ALL tabs**, including:

* **Listing Details**
* **Property Preferences**
* **Purchasing Terms**
* **Additional Details**
* **Broker Compensation & Agency Agreement Terms**
* **Buyer Information**

### Mandatory Rules

* 🚫 **No partial tab saves**
* 🚫 **No overwriting earlier tabs** when later tabs are saved
* 🚫 **No clearing values** when switching tabs or property types
* ✅ **One unified payload** must persist all fields at once
* ✅ Edit Listing must hydrate **every field** from the same payload

---

## LISTING DISPLAY REQUIREMENT

For **every property type**, confirm:

* Every applicable field appears on the **actual listing**
* Specifically confirm:

  * **Acceptable Unit Type** (Income) displays correctly if selected

### Implementation

* Fix missing display keys in:

  ```
  resources/views/livewire/tenant-agent-auction/**
  ```
* Do **NOT** rename stored keys to “make it show”
* If a value is stored as an array, render it as a clean comma-separated list

---

## PROPERTY-TYPE REGRESSION (MANDATORY)

Replit must perform **FULL verification** for:

* Residential
* Income
* Commercial
* Business
* Vacant Land

For **BOTH**:

* Traditional
* Bidding Period

---

## QA TEST SCRIPT (MUST PASS 100%)

### Test A — Create → Save Draft → Reload Draft

1. Create listing
2. Fill **every field in every tab**
3. Trigger conditional fields
4. Click **Save Draft**
5. Refresh browser
6. Open Draft

✅ All fields repopulate
✅ Dropdowns show saved values
✅ No missing tabs or fields

---

### Test B — Publish → Listing Display

1. Submit listing
2. View listing

✅ All relevant fields appear
✅ Acceptable Property Condition displays correctly
✅ Acceptable Unit Type displays (Income)

---

### Test C — Edit Listing → Save → Reload

1. Edit listing
2. Verify all fields populated
3. Change a few values
4. Save
5. Reload listing

✅ Changes persist
✅ No validation errors
✅ Submit button works

---

## COMPLETION REQUIREMENT

This task is **NOT complete** until:

* All 5 property types pass all tests
* Save Draft works with **zero data loss**
* Edit Listing repopulates **every field**
* Listing display shows **all applicable fields**
* **No console or Livewire errors** occur

---

## FINAL SAFETY NOTE

I previously edited `BuyerAgentAuction.php` by mistake.
**All work for this task must remain limited to:**

* `TenantAgentAuction.php`
* `TenantAgentAuctionEdit.php`
* `resources/views/livewire/tenant-agent-auction/**`

---

If you want next, I can:

* Convert this into a **Replit checklist**
* Create a **field-by-field audit table** for all 5 property types
* Lock this logic with a **future-proof pattern** so it never breaks again
