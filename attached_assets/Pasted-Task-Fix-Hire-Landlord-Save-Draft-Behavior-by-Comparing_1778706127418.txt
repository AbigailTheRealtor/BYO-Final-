Task: Fix Hire Landlord Save Draft Behavior by Comparing Against Seller

Problem:
On Hire Landlord’s Agent, the user does not receive any “saved” message, and the draft does not appear to save/load.

Example:
 /hire/agent/auction/landlord/38

Use Seller as the working reference:
Compare Hire Landlord draft save behavior against Hire Seller draft save behavior because both are property-owner flows.

Scope:
Shared Hire Agent create/draft flow only:
- TenantAgentAuction.php
- tenant-agent-auction.blade.php
- landlord-specific partials only if audit proves needed

Critical safeguards:
- Do NOT touch Hire Agent Direct.
- Do NOT touch Offer Listing flows.
- Do NOT touch Limited Service / initializeLimitedService().
- Do NOT change database schema, migrations, routes, validation, or saved meta keys unless audit proves absolutely necessary.
- Do NOT break Seller, Tenant, or Buyer draft behavior.
- Do NOT rewrite the whole flow.
- Do NOT create a duplicate landlord draft system.
- Fix Landlord by mirroring the existing working Seller save/draft pattern.

Read-only audit first:

1. Trace Seller save draft path
Find exactly what happens when Seller saves a draft:
- Which button triggers save
- Which Livewire method runs
- Which model is used
- Which fields are saved
- How is_draft is set
- How listingId is handled
- What success message/flash/toast appears
- How $hasDrafts is refreshed
- How draft appears in modal
- How direct draft URL loads

2. Trace Landlord save draft path
Compare the same items for Landlord:
- Does the Save Draft button exist?
- Does it call the same method as Seller?
- Does the method branch correctly for user_type=landlord?
- Does it use the correct landlord model?
- Does it set is_draft=true?
- Does it set user_id=current user?
- Does it update listingId after first save?
- Does it show the same success message as Seller?
- Does it refresh $hasDrafts after save?
- Does it dispatch the same toast/browser event as Seller?
- Does getDrafts() return the newly saved Landlord draft?
- Does /hire/agent/auction/landlord/{draftId} hydrate the saved draft?

3. Identify exact mismatch
Before editing, document the exact difference between Seller and Landlord behavior.

Likely root causes to check:
- Landlord branch missing in saveDraft().
- Landlord uses wrong model/class/table.
- Landlord model name mismatch such as HireLandLordAgentAuction vs HireLandlordAgentAuction.
- Landlord save path still points to old LandLordAgentAuction component.
- Landlord draft does not set is_draft=true.
- Landlord draft does not set user_id.
- Landlord does not update listingId after first save.
- Landlord does not trigger the success message/toast.
- Landlord is not included in draftModelClass(), getDrafts(), loadDraft(), or hydration logic.
- Direct URL /hire/agent/auction/landlord/{draftId} queries the wrong model.

Required fix:
Make Hire Landlord Save Draft behave exactly like Hire Seller Save Draft:
- Same save method pattern.
- Same success confirmation behavior.
- Same draft persistence behavior.
- Same listingId update behavior.
- Same draft modal visibility behavior.
- Same direct draft URL loading behavior.

Do not change Seller behavior.
Only fix the Landlord mismatch.

Done looks like:
- Open /hire/agent/auction/seller and save a draft to confirm reference behavior.
- Open /hire/agent/auction/landlord and save a draft.
- Landlord shows the same saved confirmation message/toast pattern as Seller.
- Landlord draft is saved in the correct landlord model/table.
- Landlord draft has user_id=current user.
- Landlord draft has is_draft=true.
- listingId updates correctly after first save.
- Landlord draft appears in the draft picker modal.
- /hire/agent/auction/landlord/{draftId} loads the saved Landlord draft.
- /hire/agent/auction/landlord/38 loads correctly if draft 38 belongs to the current user.
- Seller, Tenant, and Buyer draft save/load still work.

QA:
1. Test Seller first as the working reference.
2. Test Landlord after fix.
3. Confirm saved message appears.
4. Confirm database/model record exists.
5. Confirm is_draft=true.
6. Confirm user_id is correct.
7. Confirm listingId updates after save.
8. Confirm modal shows the Landlord draft.
9. Confirm /hire/agent/auction/landlord/{draftId} loads it.
10. Smoke test Seller, Buyer, and Tenant draft save/load.
11. Confirm git diff only touches necessary shared Hire Agent draft/save files.