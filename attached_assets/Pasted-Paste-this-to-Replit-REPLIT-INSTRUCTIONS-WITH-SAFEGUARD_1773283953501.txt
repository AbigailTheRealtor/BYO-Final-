Paste this to Replit:

---

## REPLIT INSTRUCTIONS WITH SAFEGUARDS — HIRE A BUYER’S AGENT ONLY — ACTIVE FLOW VERIFICATION + BUG FIX PASS

### SCOPE

These **Hire a Buyer’s Agent** edits were reported before and are **still not fixed**, so this time you must first verify the **actual active files** before changing anything.

I believe some of these may have “reverted” because one of the following happened:

* the wrong file/path was edited before
* only create was fixed, but edit/draft/shared flow still uses a different file
* a shared Select2/init file was later overwritten
* one field still uses old sync logic while **Acceptable Property Conditions** uses the corrected logic
* a later change reintroduced placeholder/default option leakage
* a field is rendering from a different option source than expected

Do **not** guess.
Do **not** fix a parallel or inactive file.
First prove the exact active Buyer files and exact JS/init path being used now.

---

# HARD SAFEGUARDS — NON-NEGOTIABLE

1. **Only work on Hire a Buyer’s Agent flow.**
2. **Do not edit Tenant, Seller, or Landlord flows.**
3. **Do not rename, delete, or migrate any database columns.**
4. **Do not change unrelated labels, tooltips, placeholders, or business rules.**
5. **Do not refactor the whole component architecture.**
6. **Do not replace Livewire, Select2, or shared frontend systems.**
7. **Do not make broad cleanup changes.**
8. **If shared files must be touched, isolate the fix so it only affects Buyer flow unless absolutely necessary.**
9. **Do not break drafts, edits, listing display, or old saved data.**
10. **Use Acceptable Property Conditions as the working baseline. Do not break that field while fixing the others.**

---

# STEP 1 — FIRST VERIFY THE EXACT ACTIVE BUYER FILES

Before editing anything, identify the exact active files used by:

* Hire a Buyer’s Agent **create**
* Hire a Buyer’s Agent **draft reopen**
* Hire a Buyer’s Agent **edit**

Also identify:

1. the exact Livewire component(s)
2. the exact blade(s)
3. the exact partial(s) for:

   * Acceptable Property Styles
   * Acceptable Property Conditions
   * View Preference Needed
   * Garage/Parking Features
   * Non-Negotiable Amenities and Property Features
   * Acceptable Unit Type
   * Required Property or Business Assets
   * Offered Financing/Currency
   * Services
4. the exact JS/init code responsible for:

   * Select2 initialization
   * selected-state sync
   * save-before-tab-change behavior
   * listing display synchronization
   * conditional show/hide updates for Offered Financing/Currency

### Important:

Do **not** assume create and edit use the same files.
Do **not** assume the blade itself is the true option source.
Do **not** begin edits until you confirm the actual active path.

---

# REQUIRED REPORT BEFORE CODING

Before writing any code, report back:

1. the exact active Buyer create files
2. the exact active Buyer edit files
3. whether draft reopen uses the same or different files
4. the exact JS/init file or inline script controlling these multi-select fields
5. whether the broken fields are using different logic than **Acceptable Property Conditions**

If you cannot prove the actual active files, stop and report instead of guessing.

---

# ROOT-CAUSE DIRECTION

Most of these bugs appear related and likely share the same underlying cause:

* Select2 selected-state not syncing immediately
* selections not being pushed into Livewire state before tab change
* selected DOM options not being marked correctly
* closed Select2 field not reflecting saved values
* placeholder/default option `Select` leaking into saved/displayed values
* dependent display logic for Offered Financing/Currency not updating immediately
* some Buyer fields still using old sync logic while **Acceptable Property Conditions** uses the corrected logic

Please fix the **underlying synchronization and active-path problem**, not just each symptom separately.

---

# BUGS THAT MUST BE FIXED

## 1) Acceptable Property Styles — “No results found” when selecting

Current issue:

* When selecting **Acceptable Property Styles**, it says **No results found**

Required behavior:

* It must show the real available options
* It must not falsely show “No results found” for valid selections
* It must save correctly
* It must persist through tab changes, save, refresh, and reopen
* It must display correctly on listing output

Investigate whether:

* the wrong option source is bound
* selected values do not match actual option values
* the Select2 config/search data is incorrect
* the active file/path is still using an old array

---

## 2) Acceptable Property Conditions — update option label

Required text change:

Change:

* **Older but Clean**

To:

* **Older but Clean & Well Maintained**

Requirements:

* update the actual active Buyer option source
* preserve backward compatibility for old saved values if needed
* listing display/output should show the corrected text going forward

---

## 3) View Preference Needed — selects then deletes options

Current issue:

* it selects
* then deletes/disappears

Required behavior:

* It must behave like **Acceptable Property Conditions**
* selections must stick immediately
* appear in the closed Select2 field
* remain selected after tab change
* remain after save, refresh, and reopen
* display correctly on listing output

---

## 4) Garage/Parking Features — selects then deletes options

Same issue and same required behavior as above.

It must behave like **Acceptable Property Conditions**:

* immediate selected-state
* no disappearing selections
* persistence through tab changes/save/refresh/reopen
* correct listing display

---

## 5) Non-Negotiable Amenities and Property Features — `Select` is appearing as a selected/listing value

Current issue:

* the word **Select** shows up automatically as the first option/value
* I did not choose it
* it is showing on the listing

Required behavior:

* `Select` must not be treated as a real selectable value
* `Select` must not be saved
* `Select` must not appear on listing output
* if it is a placeholder/default option, it must remain only a placeholder and never become real data

Also inspect whether any old saved Buyer records contain `Select` and handle safely if needed.

---

## 6) Acceptable Unit Type — selects then deletes options

Same issue and same required behavior as **Acceptable Property Conditions**.

Selections must:

* stick immediately
* not disappear
* persist after tab changes/save/refresh/reopen
* display correctly on listing output

---

## 7) Required Property or Business Assets — selects then deletes options

Same issue and same required behavior as **Acceptable Property Conditions**.

Selections must:

* stick immediately
* not disappear
* persist correctly
* display correctly on listing output

---

## 8) Offered Financing/Currency — selected financing types should show immediately above Buyer Pre-Approved for Loan

Current issue:

* If user selects **Conventional, FHA, Jumbo, No-Doc, Non-QM, USDA, or VA**, those words should show above **Buyer Pre-Approved for Loan**, similar to how **Assumable** shows above the Assumable question
* Behavior is inconsistent/delayed
* only **VA** may appear at first
* other financing types may not appear until interacting with Cryptocurrency terms
* there is a delay or sync issue

Required behavior:
If user selects any of:

* Conventional
* FHA
* Jumbo
* No-Doc
* Non-QM
* USDA
* VA

Then:

* all selected terms must appear **immediately** above **Buyer Pre-Approved for Loan**
* if multiple are selected, show all selected terms
* if one is removed, it must disappear immediately
* there must be no delay
* there must be no dependence on interacting with Cryptocurrency or another financing section

### Important:

None of the questions triggered by **Offered Financing/Currency** selections are required.

That means:

* selecting these options may show their relevant follow-up questions/sections
* but those follow-up questions must **not** be treated as required
* they must not block tab progression or submit unless already explicitly intended elsewhere

---

## 9) Services — selects then deletes

Current issue:

* selecting Services works briefly
* then selections delete/disappear

Required behavior:

* Services must behave like **Acceptable Property Conditions**
* selected services must stick immediately
* show correctly in the closed field
* persist through tab changes/save/refresh/reopen
* display correctly on listing output

---

# REQUIRED IMPLEMENTATION APPROACH

## 1) USE ACCEPTABLE PROPERTY CONDITIONS AS THE BASELINE

Because **Acceptable Property Conditions** is the currently working field, compare the broken fields against it and identify differences in:

* Select2 init
* change handlers
* Livewire sync
* selected option DOM updates
* edit hydration
* save-before-tab-change logic
* listing display mapping

Then make the broken Buyer fields behave the same way.

Do not break Acceptable Property Conditions.

---

## 2) FIX SAVE/PERSISTENCE BEFORE TAB CHANGE

For all affected Buyer fields that are “selects then deletes,” make sure:

* current selections are synchronized before tab change
* Livewire state is updated immediately
* re-render does not replace the field with stale values
* selected values remain visible without reopening the dropdown

---

## 3) FIX PLACEHOLDER / DEFAULT OPTION LEAKAGE

For **Non-Negotiable Amenities and Property Features**, ensure any placeholder like `Select` is not:

* treated as a real option
* saved as a selected value
* shown on listing output

---

## 4) FIX OFFERED FINANCING/CURRENCY IMMEDIATE CONDITIONAL DISPLAY

For Offered Financing/Currency:

* selected financing types must update the displayed labels/sections immediately
* no delayed update after unrelated field interaction
* follow-up questions must **not** be required
* removing a selected financing type must update the display immediately

---

## 5) PRESERVE BACKWARD COMPATIBILITY

If old Buyer listings already contain prior values such as:

* `Older but Clean`
* `Select`
* older stored variants

do not break old listings or make edit pages crash.

Use a backward-compatible approach where needed, while ensuring new saves use corrected behavior and corrected text.

---

# QA TESTING REQUIRED

After fixing, test all of these in the **actual active Buyer flow**.

## Acceptable Property Styles

1. Open Buyer create
2. Open Buyer edit if applicable
3. Confirm valid options appear
4. Confirm no false “No results found”
5. Select options
6. Tab away and back
7. Confirm they remain
8. Refresh/reopen
9. Confirm they remain
10. Confirm listing output is correct

## Acceptable Property Conditions

1. Confirm the option now says:

   * **Older but Clean & Well Maintained**
2. Confirm old saved values still load safely if present

## View Preference Needed

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Garage/Parking Features

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Non-Negotiable Amenities and Property Features

1. Confirm `Select` does not appear as a real selected option
2. Confirm `Select` does not show on listing output
3. Confirm real selections save/display correctly

## Acceptable Unit Type

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Required Property or Business Assets

1. Select options
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

## Offered Financing/Currency

1. Select Conventional, FHA, Jumbo, No-Doc, Non-QM, USDA, and VA in different combinations
2. Confirm all selected terms show immediately above Buyer Pre-Approved for Loan
3. Remove one or more
4. Confirm display updates immediately
5. Confirm none of the triggered follow-up questions are required
6. Confirm submit/tab progression is not blocked by those questions

## Services

1. Select services
2. Confirm they stick immediately
3. Tab away and back
4. Confirm they remain
5. Confirm listing output is correct

---

# WHAT TO REPORT BACK

Do **not** just say “fixed.”

Report back with:

1. the exact active Buyer create files
2. the exact active Buyer edit files
3. whether draft reopen uses the same or different files
4. the exact JS/init file controlling the affected multi-selects
5. the exact broken fields that were using different logic than Acceptable Property Conditions
6. the exact root cause of:

   * the false “No results found” in Acceptable Property Styles
   * the “selects then deletes” behavior
   * `Select` appearing as a saved/listing value
   * the delayed Offered Financing/Currency display
7. the exact files changed
8. confirmation that Tenant, Seller, and Landlord flows were not changed

---

# FINAL REMINDER

This is a **surgical Hire a Buyer’s Agent bug-fix pass only**.

Fix only:

* Acceptable Property Styles “No results found”
* Acceptable Property Conditions text update
* View Preference Needed selections deleting
* Garage/Parking Features selections deleting
* Non-Negotiable Amenities and Property Features `Select` leakage
* Acceptable Unit Type selections deleting
* Required Property or Business Assets selections deleting
* Offered Financing/Currency delayed/improper display and non-required follow-ups
* Services selections deleting

Do not do unrelated cleanup or broad refactors.

---

Why this may have reverted after being fixed:

* the prior fix may have been applied to the wrong create/edit path
* a shared JS init file may have later been overwritten
* one field may still be using old sync logic while Acceptable Property Conditions uses the corrected logic
* a later refactor may have restored placeholder/default option behavior

So verify the actual active files first, then fix the real current path.
