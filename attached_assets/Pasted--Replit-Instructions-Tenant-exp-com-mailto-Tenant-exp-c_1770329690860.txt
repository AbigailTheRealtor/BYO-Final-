## Replit Instructions — [Tenant@exp.com](mailto:Tenant@exp.com)

### Hire a Tenant’s Agent → “Select Pool Type” must NEVER show for Commercial (Switch Residential → Commercial)

This is the same “stale Residential-only state persists after switching” bug. The fix must be enforced in **4 layers**: UI render, Livewire state cleanup, save enforcement, and display filtering.

---

# SAFEGUARDS (MANDATORY)

1. **Tenant-only** (Hire a Tenant’s Agent). Do not touch Buyer/Seller/Landlord.
2. **No DB renames/deletes.** Column can remain even if Residential-only.
3. **Clear ONLY pool-related fields** when switching to Commercial. Do not clear other fields.
4. Must apply everywhere: **Create, Draft, Edit, Actual Listing, Bids, Counterbids, Summaries/PDFs**.
5. If a Commercial listing already has pool values saved from prior bug, they must **not display**.

---

# REQUIRED RESULT

* If `Property Type = Commercial`: **Pool Type field is hidden** and cannot remain checked.
* Switching **Residential → Commercial** immediately removes the Pool Type section and clears its values.
* Pool Type never appears on the actual listing for Commercial.

---

# STEP 1 — UI: Render Pool Type ONLY for Residential (Create + Edit)

### Location

Tenant listing create/edit Blade:

* `resources/views/livewire/tenant-agent-auction.blade.php`
* `resources/views/livewire/tenant-agent-auction-edit.blade.php`

Wrap the entire Pool Type field block:

```php
@if($property_type === 'Residential')
  {{-- Select Pool Type UI --}}
@endif
```

✅ **Safeguard:** Do not delete the field block; only conditionally render.

---

# STEP 2 — Livewire: Clear Pool Type when switching to Commercial

### Location

Tenant Livewire components:

* `app/Http/Livewire/TenantAgentAuction.php`
* `app/Http/Livewire/TenantAgentAuctionEdit.php`

## Add/update the property type watcher

If your property type is `property_type`:

```php
public function updatedProperty_type($value)
{
    $this->enforceResidentialOnlyFields($value);
}
```

(or `updatedPropertyType` if that’s the actual property)

## Add handler

```php
private function enforceResidentialOnlyFields($value): void
{
    if ($value !== 'Residential') {
        // Pool Type can be array (multi-check) OR string (single select)
        if (is_array($this->pool_type ?? null)) {
            $this->pool_type = [];
        } else {
            $this->pool_type = null;
        }

        // If you store pool in any other related vars, clear them too
        // $this->pool = null; etc.
    }
}
```

✅ **Safeguard:** Only clears pool-related state.

---

# STEP 3 — Save enforcement: Never persist pool data for Commercial

In **Save Draft**, **Submit**, and **Edit Update** before `$auction->save()`:

```php
if (($this->property_type ?? null) === 'Residential') {
    $auction->pool_type = $this->pool_type;   // use your real DB field
} else {
    $auction->pool_type = null;               // force clear for Commercial
}
```

✅ Prevents stale pool values being stored.

---

# STEP 4 — Mount safety (Draft reopen / Edit)

When loading an existing listing into the form, ensure Commercial clears pool state so the UI can’t show it via stale hydration.

```php
public function mount(...)
{
    ...
    if (($this->property_type ?? null) !== 'Residential') {
        if (is_array($this->pool_type ?? null)) {
            $this->pool_type = [];
        } else {
            $this->pool_type = null;
        }
    }
}
```

---

# STEP 5 — Display filtering: Do not show pool on actual listing for Commercial

### Location (Tenant-only)

* listing show page
* preview pages
* bid/counter views
* summaries/PDF templates

Wrap pool output:

```php
@if(($auction->property_type ?? null) === 'Residential')
   {{-- show pool type --}}
@endif
```

✅ Even if DB contains old data, it won’t display.

---

# QA TESTS (REQUIRED)

1. Create Residential Tenant listing → select Private + Community
2. Switch to Commercial → Pool Type section disappears immediately and clears
3. Save Draft → reopen draft as Commercial → Pool Type not present/not saved
4. Submit Commercial → actual listing shows no Pool Type
5. Existing Commercial listing that previously showed Pool Type → no longer displays it anywhere

---

# DONE DEFINITION

“Select Pool Type” never appears and never persists for Commercial Tenant listings after switching from Residential → Commercial, across create/draft/edit/show/bid/counter/summary.

---

If your Pool Type is stored as `pool_types[]` (multi-checkbox) instead of `pool_type`, tell me the exact Livewire property name and I’ll rewrite the clear/save logic with the correct variable names so it’s paste-ready.
