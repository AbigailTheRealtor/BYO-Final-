Here are **full, copy-paste Replit instructions** (with **strict safeguards**) for the specific missing draft fields you just confirmed on **Hire a Landlord**: **City/State/County/ZIP**, **Landlord’s Broker Lease Fee % input**, **Desired Lease Term → Other input**, and **Personal Photo** not saving/restoring.

---

# Replit Instructions — Hire a Landlord Draft Save/Restore Fix (City/State/County/ZIP + Lease Fee % + Desired Lease Term Other + Personal Photo)

## Login / Repro (Required)

* Login: **[tenant@exp.com](mailto:tenant@exp.com)**
* Select: **Hire a Landlord’s Agent**
* Property Types: **Residential + Commercial**
* Steps: Fill fields → **Save Draft** → open draft → verify fields are still filled

## Issues Confirmed

1. **City, State, County, ZIP Code** do not save/restore on drafts
2. **Landlord’s Broker Lease Fee** when dropdown = “Percentage of the Rent Due Each Rental Period” → the **% input value** does not save/restore
3. **Desired Lease Term** when selection includes **Other** → the **Other text input** does not show / does not restore
4. **Personal Photo** does not save/restore on draft

---

## Safeguards (Must Follow)

1. **Scope Lock:** Apply changes **ONLY** to **Hire a Landlord** (Residential + Commercial) under the **[tenant@exp.com](mailto:tenant@exp.com)** path. Do **NOT** affect Tenant/Buyer/Seller flows.
2. **No DB Breaking Changes:** Do not rename/drop tables/columns. No destructive migrations.
3. **Draft must save all values:** Save Draft must persist the **same values as Submit**, but without requiring full validation.
4. **Draft reopen must restore:** When opening a draft, all saved values must re-populate and conditional inputs must appear.
5. **No default overwrite:** Do not reset these values in mount/init after loading a draft.
6. **Proof required:** Provide list of changed files + screenshots showing each field persists after Save Draft → reopen.

---

# Part 1 — Confirm correct component/file is being edited (mandatory)

Because Hire a Landlord is being created under [tenant@exp.com](mailto:tenant@exp.com), ensure the developer is editing the **actual component used by this flow**.

### Step 1A — Add dev-only fingerprint

Add a dev-only HTML comment at the bottom of the Hire a Landlord form view:
`<!-- HIRE LANDLORD DRAFT FORM LOADED: {full blade path} -->`

### Step 1B — Add dev-only log for Save Draft

Inside the function executed when clicking **Save Draft**, log:

* component/class name
* method name
* listing id
* user id

If either fingerprint/log does not appear, the wrong file is being edited. Remove fingerprints/logs after confirming.

---

# Part 2 — Fix draft Save + draft Load for the 4 missing areas

Implement fixes in **both**:

* **Save Draft handler** (`saveDraft()`)
* **Draft load / mount / edit initializer** (`mount($id)` / `loadDraft()` / hydration method)

## 2A) City / State / County / ZIP must persist

### Requirements

* Must save on **Save Draft**
* Must restore on **Open Draft**
* Must not be overwritten by defaults

### Implementation

1. Confirm each input has a Livewire binding:

* City: `wire:model.defer="city"`
* State: `wire:model.defer="state"`
* County: `wire:model.defer="county"`
* ZIP: `wire:model.defer="zip"` (or `zip_code`)

✅ If any of these inputs are missing `wire:model`, add it.

2. In `saveDraft()` explicitly persist all 4 fields to the landlord listing record:

* `city`
* `state`
* `county`
* `zip` / `zip_code`

3. In draft load/mount explicitly assign them back:

* `$this->city = $listing->city;`
* `$this->state = $listing->state;`
* `$this->county = $listing->county;`
* `$this->zip = $listing->zip;`

4. Ensure defaults run ONLY when creating a brand-new listing (no $id).

* If `$id` exists → load draft first → do not set these to blank afterwards.

---

## 2B) Landlord’s Broker Lease Fee — % input must persist when that option is selected

### Requirements

When dropdown is:
**“Percentage of the Rent Due Each Rental Period”**
the numeric percent entered must save + restore.

### Implementation

1. Find the numeric input under Landlord’s Broker Lease Fee and confirm it has a Livewire binding like:

* `wire:model.defer="landlord_broker_lease_fee_percent"` (or similar)

✅ If missing, add it.

2. Ensure both values save:

* the selected lease fee type (dropdown)
* the numeric percent value

In `saveDraft()` explicitly save:

* `landlord_broker_lease_fee_type`
* `landlord_broker_lease_fee_percent`

3. In draft load/mount explicitly restore:

* `$this->landlord_broker_lease_fee_type = $listing->landlord_broker_lease_fee_type;`
* `$this->landlord_broker_lease_fee_percent = $listing->landlord_broker_lease_fee_percent;`

4. Check for any `updated*()` handler that clears the percent field.

* If there is code clearing percent on changes, it must NOT run during mount/hydration.
* Only clear percent if user actively switches away from the percent option.

---

## 2C) Desired Lease Term — “Other” input must show and restore

### Requirements

If user selects **Other** for Desired Lease Term:

* the Other text input must appear
* the typed value must save + restore

### Implementation

1. Confirm the dropdown/multi-select property name:

* `desired_lease_term` (string/array)

2. Confirm there is a separate property for Other text:

* `desired_lease_term_other`

3. In the form Blade:

* the “Other” input must render when “Other” is selected.

Examples depending on whether it’s string or array:

**If string:**

* show input when `$desired_lease_term === 'Other'`

**If array/multi-select:**

* show input when `in_array('Other', (array)$desired_lease_term)`

4. Save Draft must persist BOTH:

* `desired_lease_term`
* `desired_lease_term_other`

5. Draft load/mount must restore BOTH:

* `$this->desired_lease_term = $listing->desired_lease_term;`
* `$this->desired_lease_term_other = $listing->desired_lease_term_other;`

---

## 2D) Personal Photo must save + show on draft reopen

### Requirements

* If user uploads a personal photo and saves draft, it must still be visible after reopening draft.
* Do not require re-upload.

### Implementation

1. Ensure landlord listing table has a column for file path:

* `personal_photo_path` (or existing equivalent)

**Safeguard:** No renames; use existing if present.

2. In `saveDraft()`:

* If `$this->personal_photo` is a Livewire upload, store it:

  * `storage/app/public/...`
* Save the stored path to `personal_photo_path`
* If no new upload, do not overwrite the existing path with null.

3. In draft load/mount:

* `$this->personal_photo_path = $listing->personal_photo_path;`

4. In the view:

* If `personal_photo_path` exists, show preview using `Storage::url($personal_photo_path)`

---

# Part 3 — Required acceptance tests (must pass)

Using [tenant@exp.com](mailto:tenant@exp.com):

### Test A (Location)

1. Enter City/State/County/ZIP
2. Save Draft
3. Reopen draft
   ✅ all four values still filled

### Test B (Lease Fee %)

1. Select “Percentage of the Rent Due Each Rental Period”
2. Enter “10”
3. Save Draft → reopen
   ✅ dropdown still selected and % input shows “10”

### Test C (Desired Lease Term Other)

1. Select “Other”
2. Type “18 months”
3. Save Draft → reopen
   ✅ Other input visible and contains “18 months”

### Test D (Personal Photo)

1. Upload photo
2. Save Draft → reopen
   ✅ photo preview still visible

---

# Copy/Paste Message to Replit

All of the following fields are still NOT saving/restoring in Hire a Landlord drafts ([tenant@exp.com](mailto:tenant@exp.com) → Hire a Landlord Agent). Please implement these fixes ONLY for Hire a Landlord (Residential + Commercial), with no impact to tenant/buyer/seller flows.

1. City/State/County/ZIP are not saving/restoring on drafts. Confirm each input has wire:model.defer, persist all 4 fields in saveDraft(), and restore them in mount/load when opening a draft. Ensure defaults do not overwrite loaded values.

2. Broker Compensation tab: Landlord’s Broker Lease Fee — when dropdown is “Percentage of the Rent Due Each Rental Period” the numeric percent input does not save/restore. Persist BOTH lease fee type and percent value in saveDraft() and restore BOTH on draft load. Ensure no updated* handler clears percent during hydration.

3. Desired Lease Term: when user selects Other, the Other text input does not show and does not restore. Add a dedicated desired_lease_term_other property, save it in saveDraft(), restore it in mount/load, and render the input whenever Other is selected.

4. Personal Photo does not persist in drafts. Store the uploaded file and save personal_photo_path on draft save, restore the path on draft load, and show preview via Storage::url(path). Do not wipe path when no new upload occurs.

MANDATORY: First prove you’re editing the correct component used by [tenant@exp.com](mailto:tenant@exp.com) Hire a Landlord by adding a dev-only fingerprint comment in the form Blade and a dev-only log line in the actual saveDraft() method. Provide list of changed files + screenshots proving Save Draft → reopen draft retains City/State/County/ZIP, lease fee percent, desired lease term other text, and photo preview.

---

If you want, send me the **exact “wire:model” names** for these fields (or paste the Blade snippets), and I’ll rewrite the instructions using your exact variable/column names so the developer can’t misinterpret anything.
