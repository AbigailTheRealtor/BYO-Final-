I need a full audit of bid status behavior and counter-bid comparison behavior across all four roles:

- Tenant
- Seller
- Buyer
- Landlord

This audit must cover:

1. Accepted / Rejected / Countered behavior
2. Automatic rejection of competing bids when one bid is accepted
3. Counter-bid parity across roles using Tenant as the starting reference
4. Accuracy of Changed / Removed / Added comparison logic and colors

This is an AUDIT FIRST, then fix only confirmed issues. Do not do a broad refactor before identifying exact problem areas.

==================================================
AUDIT GOALS
==================================================

I want to confirm the following works correctly for Tenant, Seller, Buyer, and Landlord listings:

A. STATUS BEHAVIOR
- Accept works correctly
- Reject works correctly
- Counter works correctly
- Correct buttons/actions appear in the correct states
- Correct notices/banners/messages appear in the correct states
- Status updates correctly in bid cards, private bid views, dashboards, and notifications

B. ACCEPTING ONE BID REJECTS THE OTHERS
- If one bid/offer is accepted on a listing, all other active competing bids/offers on that same listing should automatically become rejected
- This must be audited for all four roles
- Verify database status updates, UI status updates, and notification behavior
- Verify counter-bids tied to non-winning bids also cannot remain active in a conflicting way

C. COUNTER-BID PARITY
- Tenant is the starting reference because it is the most complete flow, but do not assume Tenant is fully correct
- Audit Tenant first, then compare Seller, Buyer, and Landlord to it
- Verify that Seller, Buyer, and Landlord match the intended Tenant-style counter behavior where appropriate:
  - correct action buttons
  - correct route targets
  - correct permission gating
  - correct latest-counter baseline
  - correct status behavior after countering
  - correct dashboard and notification flow
  - correct acceptance/rejection handling after a counter exists

D. CHANGED / REMOVED / ADDED COMPARISON LOGIC
- Audit whether Changed, Removed, and Added are correctly calculated for:
  - Broker Compensation & Agency Agreement Terms
  - Offered Services
- Verify fields are truly categorized correctly
- Verify no false positives
- Verify no missing changes
- Verify Added is green, not yellow
- Confirm Changed / Removed / Added color usage is correct and consistent across roles

==================================================
SPECIFIC AUDIT QUESTIONS TO ANSWER
==================================================

1. When one bid is accepted on a listing, does the system automatically reject all other bids on:
- Tenant listings
- Seller listings
- Buyer listings
- Landlord listings

2. If not, exactly which roles/controllers/services fail to do this?

3. After acceptance, do all related views reflect final state correctly?
- listing detail bid cards
- private modal/footer actions
- my bids dashboard
- notifications
- accepted summary / e-sign entry points if applicable

4. Does countering behave consistently across roles?
- Can both sides counter where intended?
- Do action buttons show correctly after a counter?
- Does latest counter become the comparison baseline where intended?
- Do routes open the correct listing/bid/counter view?
- Are statuses shown correctly as Countered / Active / Accepted / Rejected?

5. Does Changed / Removed / Added logic correctly detect:
- changed values
- removed items
- newly added items
for both terms and services?

6. Are there formatting-only false changes?
Examples:
- percent vs numeric formatting
- currency formatting
- whitespace/casing differences
- stored arrays/order issues
- old service strings vs canonical service strings

7. Is Added currently styled yellow anywhere it should be green?
- Audit all role views and counter comparison blocks
- Standardize Added to green if that is the intended rule

==================================================
REQUIRED AUDIT METHOD
==================================================

For each role, trace the full pipeline.

STEP 1 — STATUS / ACTION FLOW
For Tenant, Seller, Buyer, and Landlord:
- identify accept action handler
- identify reject action handler
- identify counter action handler
- identify related controller/service/model updates
- identify what happens to competing bids when one is accepted
- identify notification and dashboard status updates

STEP 2 — COUNTER FLOW
For each role:
- identify the latest counter source
- identify what baseline the comparison uses
- identify whether the user sees correct Accept / Counter / Reject actions after a counter exists
- identify whether permissions/routes are correct
- identify whether all states are represented properly in UI

STEP 3 — CHANGED / REMOVED / ADDED LOGIC
For each role:
- inspect the comparison helper/service/view logic
- inspect how terms are compared
- inspect how services are compared
- verify with real bid/counter records if possible
- confirm color classes and labels are correct

STEP 4 — COMPARE TO REAL DATA
Use real records where possible and verify:
- one accepted bid on a multi-bid listing
- one countered bid
- one rejected bid
- one case with changed/removed/added fields
- one case with services added/removed

Do not rely only on code assumptions.

==================================================
THINGS TO CATCH
==================================================

STATUS / ACCEPTANCE
- one bid accepted but others remain active
- accepted bid summary generated but competing bids not rejected
- dashboards still showing wrong status
- notifications linking to wrong states/routes
- counter records left active in a conflicting way
- action buttons still visible after final resolution

COUNTER FLOW
- latest counter not used as comparison baseline
- wrong bid opens from notification
- 403/permission issues
- missing Accept / Counter / Reject buttons after counter
- stale original bid shown instead of latest terms
- inconsistent behavior between roles

CHANGED / REMOVED / ADDED
- incorrect classification of modified fields
- removed item shown as changed instead of removed
- added item shown as changed instead of added
- service lists using wrong baseline
- false differences from formatting only
- Added color yellow instead of green

==================================================
DELIVERABLE BEFORE BROAD FIXES
==================================================

Before doing broad changes, provide a numbered audit report with:

1. Role
2. Flow type:
   - Accept / Reject / Counter
   - Auto-reject competing bids
   - Changed / Removed / Added logic
3. Exact issue found
4. Root cause
5. Exact file(s) involved
6. Whether it affects:
   - controller/service logic
   - model logic
   - view rendering
   - notification routing
   - dashboard status
   - comparison baseline
   - color/styling only
7. Recommended surgical fix

Do not say it is correct unless it was actually verified against code paths and real records.

==================================================
IF FIXES ARE NEEDED
==================================================

After the audit, implement only the confirmed fixes.

Required fix priorities:
1. Auto-reject all competing bids when one is accepted
2. Counter flow parity/errors
3. Changed / Removed / Added classification accuracy
4. Added color must be green, not yellow

==================================================
SAFEGUARDS
==================================================

- Audit first, then fix
- No broad refactors
- No schema renames or destructive DB changes
- Do not alter unrelated listing flows
- Preserve working accepted summary / e-sign logic
- Keep changes surgical and scoped
- Tenant can be used as the starting reference, but do not assume Tenant is correct without verifying
- If a shared helper/service is the root cause, fix it carefully and then verify all affected roles
- Verify with real bids/counters where possible
- Do not mark work complete until all four roles have been audited

==================================================
FINAL REQUIRED OUTPUT
==================================================

When finished, provide:

1. Full audit checklist by role
2. Every issue found
3. Exact files changed
4. What each fix corrected
5. What was visually/functionally verified
6. Explicit confirmation of whether all competing bids are now auto-rejected when one is accepted
7. Explicit confirmation that Added is now green wherever applicable
8. Explicit confirmation that Changed / Removed / Added logic is accurate across Tenant, Seller, Buyer, and Landlord