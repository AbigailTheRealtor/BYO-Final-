We need to correct the Buyer counter architecture and also fully clean up the Landlord match score / counter baseline issue.

This is a targeted implementation task with safeguards.
Do not do a broad refactor.
Do not change unrelated roles.

==================================================
PRIMARY GOALS
==================================================

1. BUYER COUNTER TERMS MUST BE PER AGENT / PER BID
- Buyer counter behavior should match the Tenant pattern
- It should NOT be auction-wide
- A Buyer counter must apply to the specific agent bid being countered
- Do not show one Buyer counter as if it applies to every bid on the listing

2. LANDLORD MATCH SCORE / COUNTER BASELINE MUST BE CLEAN AND RELIABLE
- Fix the Landlord logic so match score comparisons use the correct latest counter baseline
- Clean up any fragile status-based logic causing incorrect baseline selection
- Landlord should behave consistently and correctly like the intended counter flow

==================================================
PART 1 — BUYER COUNTER ARCHITECTURE FIX
==================================================

Problem:
Buyer counter terms are currently acting auction-level instead of per-agent / per-bid.
That is wrong for the intended behavior.

Expected behavior:
- If Buyer counters Agent A's bid, that counter applies only to Agent A's bid
- Agent B's bid should not show as countered because Buyer countered Agent A
- Buyer counter state, banner, view-counter, edit-counter-terms, baseline comparison, and status flow should all be tied to the specific bid

Use Tenant as the exact working reference pattern.

Required audit + implementation:

1. Identify every place Buyer counter logic currently uses auction-level scope instead of bid-level scope
This includes at minimum:
- BuyerCounterTerm storage
- BuyerCounterTerm lookup queries
- Buyer counter banner/state detection
- Buyer "View Counter Terms" route behavior
- Buyer "Edit Counter Terms" route behavior
- Buyer match score baseline selection
- Buyer comparison baseline used in changed/removed/added views
- Buyer notifications / dashboards if affected

2. Change Buyer counter terms to be stored and retrieved per bid, matching Tenant pattern
- Use the specific Buyer bid ID as the owning reference
- Do not leave it tied only to the auction ID if that causes all bids to inherit the same counter

3. Update all Buyer counter routes/controllers/components/views accordingly
- view-counter must load the correct counter for the specific bid
- edit-counter-terms must edit the specific bid’s counter terms
- Buyer bid card / modal state must only show countered for the specific bid that actually has a Buyer-submitted counter

4. Update Buyer baseline logic
- When a Buyer counter exists for a specific bid, that bid’s match score and comparison baseline should use the latest Buyer counter terms for THAT bid only
- Other bids should continue using the original listing baseline unless they have their own Buyer counter

5. Preserve existing Buyer data if possible
- If there is legacy auction-scoped Buyer counter data, do not destroy it blindly
- If a compatibility fallback is needed, add it carefully and document it
- Prefer a safe migration path or compatibility handling over breaking old data

==================================================
PART 2 — LANDLORD MATCH SCORE / COUNTER BASELINE CLEANUP
==================================================

Problem:
Landlord match score / counter baseline behavior has been fragile because status handling is inconsistent and the wrong counter selection logic was used.

Expected behavior:
- If Landlord submitted counter terms for a specific bid, the latest valid Landlord counter for that bid should become the comparison baseline where intended
- Landlord bid state and match score should be accurate and not depend on unreliable status assumptions

Required audit + implementation:

1. Audit Landlord counter term lifecycle
- how counter terms are created
- what status values they receive
- whether status is actually reliable
- which records represent the latest valid Landlord counter for a bid

2. Make Landlord baseline selection deterministic
- Use the correct latest Landlord counter for that specific bid
- Do not rely on where('status', 1) if that is not reliable in real data
- Do not accidentally use accepted/rejected historical records as active comparison baseline if that would be wrong
- Define and implement the correct rule clearly in code

3. Make Landlord state/banner behavior consistent with the corrected rule
- countered should show only when appropriate
- accepted/rejected terminal states must still take precedence
- view/edit counter terms must correspond to the correct specific bid

4. Verify Landlord match score and changed/removed/added comparison use the same correct baseline
- no stale original baseline when a valid latest counter exists
- no wrong counter record selected

==================================================
TENANT AS REFERENCE
==================================================

Tenant is the working per-bid reference model.

Use Tenant to mirror:
- per-bid counter storage pattern
- per-bid query pattern
- banner/state logic
- view/edit counter routing pattern
- latest counter baseline selection logic

Do not change Tenant unless a truly shared bug requires a safe shared fix.

==================================================
BUYER + LANDLORD FILES / AREAS TO AUDIT
==================================================

Please audit and update all affected areas, including as applicable:
- BuyerCounterTerm model
- Buyer counter controllers
- Buyer counter Livewire/component classes
- Buyer bid detail Blade view
- Buyer view-counter controller/action
- Buyer edit-counter controller/action
- Buyer match score helper integration points
- Buyer notification/dashboard links if affected

- LandlordCounterTerm model
- Landlord counter controllers/components
- Landlord bid detail Blade view
- Landlord match score baseline selection logic
- Landlord counter state/banner logic
- Landlord changed/removed/added baseline selection if affected

==================================================
SAFEGUARDS
==================================================

- Do not refactor unrelated roles
- Do not rename DB columns unless absolutely necessary
- Do not make destructive schema changes without documenting exactly why
- If a schema adjustment is required for Buyer to become per-bid, do the safest possible version
- Prefer additive / backward-compatible changes
- Do not break Tenant or Seller
- Do not break accepted/rejected/e-sign flows
- Keep all changes surgical and tied only to Buyer counter per-bid correction and Landlord baseline cleanup
- If a shared helper/service must be changed, verify all affected roles afterward

==================================================
REQUIRED IMPLEMENTATION PROCESS
==================================================

Step 1
Audit Buyer and Landlord current behavior and identify exact files and root causes.

Step 2
Provide a short implementation plan before broad edits:
- Buyer root cause
- Landlord root cause
- files to change
- whether schema changes are needed

Step 3
Implement the fixes.

Step 4
Verify with real records and browser checks:
BUYER
- counter Buyer bid A only
- verify only bid A shows countered
- verify bid B does not show countered
- verify view/edit counter terms open the correct counter for bid A
- verify match score baseline for bid A uses its counter
- verify other bids do not inherit bid A's counter baseline

LANDLORD
- verify latest correct counter is used as baseline
- verify terminal states still override correctly
- verify counter banner appears only where appropriate
- verify changed/removed/added output uses the correct baseline

==================================================
FINAL REQUIRED OUTPUT
==================================================

When complete, provide:

1. Root cause summary for Buyer
2. Root cause summary for Landlord
3. Exact files changed
4. Whether any schema change was required
5. What logic was changed
6. What was verified in browser / with real records
7. Explicit confirmation that:
   - Buyer counters are now per bid / per agent
   - Buyer no longer applies one counter across all bids
   - Landlord match score now uses the correct latest counter baseline
   - Landlord counter state and comparison behavior are consistent