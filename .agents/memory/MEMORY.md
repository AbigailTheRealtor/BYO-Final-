- [users table NOT NULL columns for test helpers](users-not-null-columns.md) — DB::table('users')->insertGetId() requires: first_name, last_name, name, short_id, user_name, email, password, user_type; boolean cols (is_approved/is_super/is_deleted) default false.
- [TenantAgentAuction and BuyerAgentAuction test creation patterns](auction-test-creation-patterns.md) — TenantAgentAuction is fully guarded (no $guarded=[]); use DB::table('tenant_agent_auctions')->insertGetId() then findOrFail(); buyer_agent_auctions requires NOT NULL title column.
- [MLS property_type role-specific normalization](mls-property-type-normalization.md) — seller uses 'Residential' (no suffix); landlord uses 'Residential Property' (full form); fix lives in HasMlsImport::normalizePropertyTypeForRole().
- [Phase D year_built collision resolution](phase-d-year-built-collision.md) — year_built was pre-existing in both Landlord components before Phase D; skipped per stop-and-report rule.
- [Seller schema typo — rental_restrictions_desription](seller-schema-typo.md) — DB column is misspelled (missing 'c'); use the typo in code until a migration renames it.
- [Ask AI test constructor drift pattern](ask-ai-test-constructor-drift.md) — when AskAiContextBuilderService or AskAiPromptBuilderService gain new constructor deps, 4+ integration test files each have their own makeContextBuilder() factory that must be updated; no shared base class exists yet.
- [Bidding Period countdown source](bidding-period-countdown.md) — expiration_date is platform source of truth; fallback to created_at+auction_time for legacy records.
- [PostgreSQL Gate resolver — raw DB over Eloquent](postgres-gate-resolver.md) — use DB::table() not Eloquent models in Gate callbacks; Eloquent eager-load ($with) aborts the PG transaction on any query error, poisoning subsequent writes even inside try/catch.
- [tenant_criteria_auctions missing table guard](postgres-gate-resolver.md) — Schema::hasTable() check required before querying tenant_criteria_auctions; table absent in this environment; two tests skipped until table is created.
- [MLS parser boolean bleed fix](mls-boolean-bleed.md) — boolean fields (has_hoa, has_cdd) must use tight char-class captures (Yes|No|Y|N)\b, not open-ended ([^\|\n]{1,10}); boundary stop won't fire on partial words.
- [MLS coverage report universe rule](mls-coverage-universe.md) — reporter universe must be fieldInventory() (complete per-form field list), not parsedKeys ∪ fieldMapKeys; fields without parser branches appear as null-canonical-key rows (Safe=N).
- [Ask AI FAQ coverage pattern](ask-ai-faq-coverage-pattern.md) — Registry has 4 keyword_route_status values (pinned/umbrella_only/match_criteria/opaque_key); harness must use pinnedPaths() not allCanonicalPaths() for FAQ_KEY_KEYWORD_MAP checks; deriveFieldLabel fallback is 'The requested information' (not 'Listing field'). Full registry must include listing-model fields too, with ≥2 natural questions per entry.
- [Ask AI registry dual-structure](ask-ai-registry-dual-structure.md) — registry() is FAQ-only (168 entries); listingFieldRegistry() is the separate listing.* registry (45 entries); sample_question_2 injected by withSecondQuestions(), not stored inline.
- [MLS seller price maps to maximum_budget](mls-seller-price-mapping.md) — seller list price must target maximum_budget (Desired Sale Price on Sale Terms tab), NOT purchase_price (Seller Financing sub-field, hidden by default).
- [Ask AI listing field guard pattern](ask-ai-listing-field-guard.md) — Guard B fires for listing.* null fields before OpenAI; OpenAI json_object needs "json" in messages; infoGet returns '' not null for empty EAV metas.
- [Ask AI classifier boundary collision](ask-ai-classifier-boundary-collision.md) — listing_facts keywords must not be substrings of buyer_tenant_match questions; KEYWORD_RULES evaluated in order, listing_facts first; case M tests guard known protected phrases.
- [Ask AI final listing_facts fallback](ask-ai-final-listing-facts-fallback.md) — RunnerV2 has a backstop after specific faq/listing fallbacks: if adapter fails + listing_facts + prompt_ready and no specific fallback fired, returns insufficient_context (not failed). Also: docblock `listing.*/faq_answers.*` closes the comment block early — use `listing.field or faq_answers.field` instead.
- [Laravel flash + assertSessionHas in tests](laravel-flash-assert-pattern.md) — assertSessionHas fires after ageFlashData; check _flash.old instead, or assert business outcomes.
- [Ask AI router normalizer shape and trace](ask-ai-router-normalizer-shape.md) — normalizer max_tokens is 80 (not 60); three-shape response {status:matched/unsupported/prohibited}; runner trace has deterministic_question_type + router_called/status/context_path; normalizer 'unknown' maps to 'unsupported' in router_status; tests that pin max_tokens must use 80.
- [Offer test submitter-hiding pattern](offer-test-submitter-pattern.md) — show.blade.php hides Accept/Reject/Counter for the submitter; use makeOfferByOtherUser() helper so the test user is the listing owner, not the submitter.
- [Ask AI live DB key mismatches — view/rent/utilities/lease/budget](ask-ai-live-db-key-mismatches.md) — 6 extractFactualFields() reads used wrong EAV meta keys (always null); confirmed by querying real listing IDs 87/121/97/71/170.
- [Ask AI Guard B test phrasing rule](ask-ai-guard-b-test-phrases.md) — Guard B pipeline tests must use phrases from LISTING_KEY_KEYWORD_MAP verbatim; phrases not in the map leave normalizedFieldKey null, adapter gets called, and the never() expectation throws/returns 'failed'.
- [Ask AI decodeJsonField Other-leak](ask-ai-decode-json-other-leak.md) — decodeJsonField filters literal "Other" (case-insensitive) from JSON arrays; listing.rental_budget has no LISTING_KEY_KEYWORD_MAP entry so it cannot be Guard-B tested via pipeline; source-grep is the correct coverage layer.
- [Ask AI LISTING_KEY_KEYWORD_MAP — context key parity rule](ask-ai-listing-key-context-parity.md) — every LISTING_KEY_KEYWORD_MAP entry must use a key name that exactly matches ctx['listing'][key]; tenant budget is ctx['listing']['max_rent'] (not 'rental_budget'); runner result trace key is listing_key_detected (not normalized_field_key).
- [Landlord MLS wiring gaps — RESOLVED](landlord-mls-wiring-gaps.md) — lot_dimensions/roof_type/exterior_construction/foundation were documented as missing; live audit confirms all 4 are fully wired on LandlordOfferListing; static crosswalk doc was stale.
- [MLS PDF source location](mls-pdf-source-location.md) — all 7 Stellar MLS Data Entry Form PDFs in attached_assets/; fieldInventory() is the code-internal representation; PDF fields absent from fieldInventory() are reporter blind spots.
- [Landlord loadDraft double-decode bug](landlord-loaddraft-double-decode.md) — LandlordAgentAuction::getGetAttribute() auto-decodes valid-JSON strings to arrays; loadDraft() then calls json_decode(array) → TypeError; Seller not affected; fix: remove redundant json_decode() calls from loadDraft().
- [MLS parser boundary-stop over-firing pattern](mls-parser-boundary-stop.md) — $labelStop uses \b word boundaries; 8 confirmed failures where stop words appear as sub-words in valid values (Furnished inside Unfurnished, City inside Electricity, Sewer inside Public Sewer, etc.); rent_includes boundary=true fixed in Phase 2.
- [MLS labelStop gap patterns](mls-labelstop-gaps.md) — Missing labels cause field bleed: bare HOA\b misses "HOA Dues:"; Special\s+Assessment\b misses "Special Assessment Y/N:"; School District/Neighborhood/Monthly Fee were absent entirely. Always add Y/N and multi-word label variants when extending labelStop.
- [Ask AI Phase 1 lineage remediation — phantom key pattern](ask-ai-phase1-lineage-remediation.md) — LISTING_KEY_KEYWORD_MAP entries must be cross-checked against extractFactualFields() for all roles; buy_now_price was wired in form but not in extractor (phantom routing); hoa_fee_requirement/condo_fee had no data source at all (removed); 49 approved fields after Phase 1.
- [Ask AI snapshot PostgreSQL version-select pattern](ask-ai-snapshot-pg-version-select.md) — lockForUpdate()+max()/aggregate functions are rejected by PostgreSQL; use orderByDesc('version')->value('version') + retry loop (MAX_RETRIES=5) on unique constraint violations instead.
- [RefreshDatabase vs DatabaseTransactions on SQLite :memory:](sqlite-memory-test-pattern.md) — RefreshDatabase with :memory: has NO per-method rollback (refreshInMemoryDatabase skips beginDatabaseTransaction); use DatabaseTransactions instead; also never use expectException for DB constraint tests — use try/catch to avoid aborting the PostgreSQL outer transaction.
- [Ask AI Phase 4 database-first search — answer key dual-form](ask-ai-phase4-answer-key-dualform.md) — ask_ai_answers.canonical_key may be bare ('roof_age_and_condition') or full-path ('faq_answers.roof_age_and_condition'); search service must try both forms; facts table always uses bare key (no 'listing.' prefix).
- [Ask AI harness FAQ/listing collision guard](ask-ai-harness-faq-collision.md) — harness questionA/B for listing.* fields must not contain any substring from FAQ_KEY_KEYWORD_MAP; faq_answers.internet_utility_providers has 'internet available' — "Is internet available on this land?" collides; use 'telecom availability on this land' instead. Cross-check both questions against the FAQ map before committing harness entries.
- [MLS regex parenthesized label form](mls-paren-label-regex.md) — parenthesized labels like "Minimum Lease (Months):" need alternation `(?:\s*\(Months?\)|\s+Months?)` not a bare `\s+Months?`; the bare form doesn't match parens.
- [AskAi context builder test mock patterns](ask-ai-context-builder-mock-patterns.md) — PropertyIntelligenceProfileService namespace is App\Services\Dna; buildPayloadReadOnly() is non-nullable array so never use willReturn(null); MlsFieldMap::landlord() is private — use forRole('landlord').
- [Ask AI three-file coordination rule](ask-ai-three-file-coordination.md) — every new listing.* field requires coordinated changes in all three files: context builder (EAV alias), runner LISTING_KEY_KEYWORD_MAP (phrases), classifier listing_facts (at least one keyword); missing any one causes silent routing failure.
- [Ask AI LISTING_KEY_KEYWORD_MAP collision detection](ask-ai-map-collision-pattern.md) — phrases containing substrings of earlier map entries will be intercepted silently; test via Reflection on detectListingFieldKey(); two known collision classes: (1) substring of existing residential phrase, (2) bare phrase that appears in a different field's entry.
- [Ask AI duplicate LISTING_KEY_KEYWORD_MAP key risk](ask-ai-duplicate-map-key-risk.md) — PHP const arrays silently use last-key-wins for duplicate keys; role-segmented map blocks (e.g. two 'listing.zoning' entries) drop the first entry's phrases entirely; always merge into one entry; add a map-key-uniqueness test to catch future regressions.
- [Preset auto-pop: retainer_fee_option missing from Landlord](landlord-retainer-props.md) — LandlordAgentAuctionBid was missing retainer_fee_option/amount/application public props; broker-compensation blade uses them directly; added in P2 #2570.
- [Preset auto-pop: brokerage not on users table](preset-brokerage-not-users.md) — users.brokerage column does not exist; $user->brokerage is always null; blank-field protection tests must use first_name (factory-seeded) not brokerage.
- [Accessor normalizer — bid stdClass vs auction anonymous class](accessor-normalizer-pattern.md) — bid models return stdClass; auction models return anonymous class with private $data; use method_exists(toArray)+get_object_vars() not (array) cast to normalize.
- [MLS labelStop compound label rule](mls-labelstop-compound-label.md) — boundary closure requires \s*: immediately after the matched label word; multi-word labels like "Tax Legal Desc:" need the FULL prefix (Tax\s+Legal\s+Desc\b) not just the first words (Tax\s+Legal\b) because \s*: won't skip over "Desc" to reach ":".
- [MLS waterfront_feet PHP zero falsy gotcha](mls-waterfront-feet-zero-falsy.md) — string "0" is falsy in PHP; if ($v = $extract(...)) silently skips numeric-zero parsed values; use if (($v = $extract(...)) !== null) for any field that might legitimately return "0".
- [MLS null-mapped fields are invisible to user](mls-null-mapped-invisible.md) — null in MlsFieldMap means isset() returns false, so the field is skipped ENTIRELY from preview modal (not just Apply Selected); to show a field as preview-only, it must have a real prop name; "preview-only" requires a dummy prop or a separate preview list.
- [Landlord pet_policy cascade order](landlord-pet-policy-eav.md) — real data is in `pets` EAV key (not `pet_policy`); context builder reads `pets` first (fixed); CANONICAL_SOURCE_MAP is `['pets', 'pet_policy']` to match UI view priority.
- [Ask AI normalizer enabled via platform env, not .env](ask-ai-normalizer-env.md) — ASK_AI_ENABLE_OPENAI_INTENT_NORMALIZATION=true is set as a Replit platform-level env var; not in .env file; confirmed via printenv; isEnabled() reads from config() which reads system env correctly.
- [Ask AI description field mapping per role](ask-ai-description-field-mapping.md) — seller=EAV additional_details→native description fallback (I-1 fix); buyer=native additional_details; tenant/landlord=EAV additional_details only.
- [Ask AI Step 1a-desc fail-open gate pattern](ask-ai-step1a-desc-failopen.md) — Step 1a-desc must be fail-open: only Stage 1 (greeting/ack hard rejects) blocks; Stage 2 keyword list removed because false negatives block valid listing answers; resolveOtherValue() extended to handle See Remarks/TBD/N/A/None; isBareAnswerPlaceholder() sanitizes bare placeholder final answers.
- [MLS terms_of_lease apply-time routing](mls-terms-of-lease-routing.md) — landlord residential MLS exports produce duration values ('Month-to-Month') under terms_of_lease canonical key; apply-time guard in HasMlsImport re-routes to desired_lease_length when role=landlord+property_type='Residential Property'.
- [water_view vs view_preference meta key split](water-view-meta-key-split.md) — seller+landlord Livewire forms save BOTH water_view (specific water bodies) and view_preference (scenic preference) as separate meta keys; Ask AI must read water_view first with view_preference fallback; public view only reads view_preference (pre-existing stage 8 gap).
- [Livewire third-party map/widget stable-ID pattern](livewire-third-party-map-pattern.md) — never use uniqid() for Livewire-hosted widget containers; use stable ID + wire:ignore or re-renders destroy the element and break JS closures.
- [Replit secret vs workflow process env gap](replit-secret-vs-workflow-env.md) — Replit secrets visible in bash but NOT in artisan serve workflow; write to .env so phpdotenv loads at startup; diagnostic: curl a temp route to check config() vs printenv.
- [Ask AI classifier KEYWORD_RULES section placement](ask-ai-classifier-section-placement.md) — listing_facts occupies lines 141-1267; property_standout starts at 1285; neighborhood/area keywords MUST be in property_standout block or they route to listing_facts (which excludes location_intelligence from context).
- [Ask AI synthesis quality guard — heuristic 4](ask-ai-synthesis-quality-guard.md) — isResponseDegraded() heuristic 4: any answer lacking terminal punctuation (.!?) is degraded; triggers quality rewrite; also patched FAQ+listing.* adapter-failed raw-return paths.
- [Ask AI Truth Source Contract — CANONICAL_SOURCE_MAP + conflictDetect](ask-ai-truth-source-contract.md) — buildForListing() top-level '_sources' key; conflictDetect() treats '[]'/'{}' as absent; meta table columns are meta_key/meta_value; listing.lease_terms routes BEFORE listing.lease_length in LISTING_KEY_KEYWORD_MAP (line 1323 < 1357).
- [EAV meta table column names](eav-meta-column-names.md) — seller_agent_auction_metas and landlord_agent_auction_metas columns: meta_key, meta_value (NOT key/value); FK is seller_agent_auction_id / landlord_agent_auction_id.
- [BYA compatibility comparison — exact string equality, no normalizer](bya-compat-exact-equality.md) — ByaCompatibilityComparisonService uses (string)$a === (string)$b; consumer and agent blade option values must be byte-for-byte identical for a "same" result.
- [ldna DrawingManager race + lazy init pattern](ldna-drawing-manager-race.md) — !google.maps.drawing check is insufficient; need typeof DrawingManager === 'function'; ldnaSetDrawMode() must lazily create it on first click to handle race.
- [ldna boundary API sources](ldna-boundary-apis.md) — cities/counties: Nominatim 1 req/sec queue; ZIPs: Census TIGERweb ZCTA2020 REST; neighborhoods: no reliable free US GeoJSON source (matching/scoring only).
- [ldna validation flash — keyup on ldna-* inputs](ldna-validation-flash.md) — initializeFullService() called on every message.processed; fix: :not([id^="ldna-"]) in querySelectorAll selector; tenant-edit uses updateSaveButton (no fix needed there).
